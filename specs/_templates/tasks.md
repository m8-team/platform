# Задачи: <Название возможности>

- Спецификация: `<ссылка на spec.md>`.
- План — при наличии: `<ссылка на plan.md>`.
- Issue: `<ссылка на трекер>`.

Создавайте этот документ по сложности изменения. Для небольшой задачи достаточно issue. Каждый шаг должен быть небольшим и проверяемым; завершение отмечается только по фактическому подтверждению. Приоритеты, сроки и ответственные остаются в трекере.

## Реализация

- [ ] `TASK-<number>`: <Небольшой результат реализации>.
  - Требования: `<REQ-ID>`.
  - Зависит от: `<TASK-ID или явно «нет»>`.
  - Критерий завершения: <Наблюдаемое условие>.
  - Подтверждение: `<ссылка на результат после выполнения>`.

<Повторите блок для необходимых шагов; обозначьте зависимости явно.>

## Проверка

- [ ] `TASK-<number>`: <Проверить указанные требования и сценарии>.
  - Требования и сценарии: `<REQ-ID / AC-ID / NFR-ID>`.
  - Зависит от: `<TASK-ID>`.
  - Критерий завершения: <Фактические проверки дают ожидаемый результат; ограничения записаны>.
  - Подтверждение: `<команда или процедура, результат и ссылка после выполнения>`.

## Документация

- [ ] `TASK-<number>`: <Обновить затронутые документы, ссылки и статус>.
  - Требования: `<REQ-ID>`.
  - Зависит от: `<TASK-ID>`.
  - Критерий завершения: <Документы согласованы с контрактами, реализацией и проверками; статус подтверждён>.
  - Подтверждение: `<ссылка на проверенное изменение после выполнения>`.
