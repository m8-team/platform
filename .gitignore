.idea/
.DS_Store

# Flink installation credentials are provisioned out of band or by a secret operator.
**/kafka-secret.yaml
**/object-storage-secret.yaml
**/s3-credentials.secret.yaml

# Helm dependencies are reproducible from Chart.lock.
deploy/flink/shared/operator/charts/
