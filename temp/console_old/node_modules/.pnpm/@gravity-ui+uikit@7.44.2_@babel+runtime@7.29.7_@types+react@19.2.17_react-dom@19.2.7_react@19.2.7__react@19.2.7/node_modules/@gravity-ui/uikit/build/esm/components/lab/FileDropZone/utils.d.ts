export declare const FILE_REJECTION_REASONS: {
    readonly INVALID_TYPE: "invalid-type";
    readonly TOO_MANY_FILES: "too-many-files";
};
export type FileDropZoneAccept = string[];
export type FileRejectionReason = (typeof FILE_REJECTION_REASONS)[keyof typeof FILE_REJECTION_REASONS];
export interface FileRejection {
    item: DataTransferItem;
    reasons: FileRejectionReason[];
}
interface ValidateOptions {
    accept: FileDropZoneAccept;
    multiple?: boolean;
}
export declare function getSeparatedItems(dataTransfer: DataTransfer, options: ValidateOptions): {
    accepted: DataTransferItem[];
    rejected: FileRejection[];
};
export {};
