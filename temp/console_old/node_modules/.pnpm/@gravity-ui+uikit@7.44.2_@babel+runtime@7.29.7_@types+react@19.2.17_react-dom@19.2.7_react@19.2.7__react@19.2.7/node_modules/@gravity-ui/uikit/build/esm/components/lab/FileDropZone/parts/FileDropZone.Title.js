import { jsx as _jsx } from "react/jsx-runtime";
import { Text } from "../../../index.js";
import { useFileZoneContext } from "../FileDropZone.Provider.js";
import { FileDropZoneQa } from "../constants.js";
import i18n from "../i18n/index.js";
export const FileDropZoneTitle = ({ className }) => {
    const { title, multiple, errorMessage } = useFileZoneContext();
    const { t } = i18n.useTranslation();
    const postfix = multiple ? 'multiple' : 'single';
    const defaultTitle = title || t(`label_title-${postfix}`);
    const isError = Boolean(errorMessage);
    const displayTitle = isError ? errorMessage : defaultTitle;
    return (_jsx(Text, { variant: "subheader-1", className: className, qa: FileDropZoneQa.TITLE, children: displayTitle }));
};
//# sourceMappingURL=FileDropZone.Title.js.map
