import type { AxisDomain, AxisScale } from 'd3-axis';
import type { ScaleBand, ScaleTime } from 'd3-scale';
import type { ChartScale, PreparedAxis, PreparedAxisPlotBand, PreparedSplit } from '../../../hooks';
import type { ChartAxis } from '../../../types';
import type { AxisDirection } from '../types';
export declare function getTicksCountByPixelInterval({ axis, axisWidth, }: {
    axis: PreparedAxis;
    axisWidth: number;
}): number | undefined;
export declare function isBandScale(scale?: ChartScale | AxisScale<AxisDomain>): scale is ScaleBand<string>;
export declare function isTimeScale(scale?: ChartScale | AxisScale<AxisDomain>): scale is ScaleTime<number, number>;
export declare function getXAxisOffset(): 0 | 0.5;
export declare function getXTickPosition({ scale, offset }: {
    scale: AxisScale<AxisDomain>;
    offset: number;
}): (d: unknown) => number;
export declare function getAxisItems({ scale, count, maxCount, }: {
    scale: ChartScale | AxisScale<AxisDomain>;
    count?: number;
    maxCount?: number;
}): string[] | number[] | Date[];
export declare function getAxisHeight(args: {
    split: PreparedSplit;
    boundsHeight: number;
}): number;
interface GetBandsPositionArgs {
    band: PreparedAxisPlotBand;
    axisScale: AxisScale<AxisDomain>;
    axis: AxisDirection;
}
export declare const getAxisPlotsPosition: (axis: PreparedAxis, split: PreparedSplit | undefined, width?: number) => number[];
export declare function getBandsPosition(args: GetBandsPositionArgs): {
    from: number;
    to: number;
};
export declare function getClosestPointsRange(axis: PreparedAxis, points: AxisDomain[]): number | undefined;
export declare function getAxisCategories({ categories: originalCategories, max, min, order, }?: Partial<ChartAxis>): string[] | undefined;
export declare function thinOut<T>(items: T[], delta: number): T[];
export declare function shouldSyncAxisWithPrimary(axis: ChartAxis, primaryAxis: ChartAxis): boolean | undefined;
export {};
