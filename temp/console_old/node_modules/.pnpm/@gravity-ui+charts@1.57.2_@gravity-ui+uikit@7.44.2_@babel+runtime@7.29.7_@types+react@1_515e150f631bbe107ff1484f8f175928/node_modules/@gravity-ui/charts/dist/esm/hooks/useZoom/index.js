import React from 'react';
import { select } from 'd3-selection';
import { selectionToZoomBounds } from '../../core/zoom/utils';
import { useBrush } from '../useBrush';
export function useZoom(props) {
    const { node, onUpdate, plotContainerHeight, plotContainerWidth, preparedSplit, preparedZoom, rangeSliderDomain, xAxis, xScale, yAxis, yScale, } = props;
    const isBrushDisabled = Boolean(!xAxis || !yAxis || !xScale || !yScale);
    const areas = React.useMemo(() => {
        const result = [];
        if (!preparedZoom) {
            return result;
        }
        if (preparedSplit && preparedSplit.plots.length > 1) {
            preparedSplit.plots.forEach((plot) => {
                result.push({
                    extent: [
                        [0, plot.top],
                        [plotContainerWidth, plot.top + plot.height],
                    ],
                });
            });
        }
        else {
            result.push({
                extent: [
                    [0, 0],
                    [plotContainerWidth, plotContainerHeight],
                ],
            });
        }
        return result;
    }, [plotContainerHeight, plotContainerWidth, preparedSplit, preparedZoom]);
    const handleChartBrushEnd = React.useCallback(function (brushInstance, selection) {
        if (selection && yScale && xAxis && yAxis && xScale && (preparedZoom === null || preparedZoom === void 0 ? void 0 : preparedZoom.type)) {
            const nextZoomState = selectionToZoomBounds({
                selection,
                xAxis,
                xScale,
                yAxes: yAxis,
                yScales: yScale,
                zoomType: preparedZoom.type,
            });
            if (rangeSliderDomain && (nextZoomState === null || nextZoomState === void 0 ? void 0 : nextZoomState.x)) {
                const [minRangeSlider, maxRangeSlider] = rangeSliderDomain;
                const [minZoom, maxZoom] = nextZoomState.x;
                if (minZoom < minRangeSlider) {
                    nextZoomState.x[0] = minRangeSlider;
                }
                if (maxZoom > maxRangeSlider) {
                    nextZoomState.x[1] = maxRangeSlider;
                }
            }
            onUpdate(nextZoomState);
            brushInstance.clear(select(this));
        }
    }, [onUpdate, preparedZoom === null || preparedZoom === void 0 ? void 0 : preparedZoom.type, rangeSliderDomain, xAxis, xScale, yAxis, yScale]);
    // Chart brush for manual zoom handling
    useBrush({
        areas,
        brushOptions: preparedZoom === null || preparedZoom === void 0 ? void 0 : preparedZoom.brush,
        disabled: isBrushDisabled,
        node,
        type: preparedZoom === null || preparedZoom === void 0 ? void 0 : preparedZoom.type,
        onBrushEnd: handleChartBrushEnd,
    });
}
