import React from 'react';
import type { ChartTooltip, ChartXAxis, ChartYAxis, TooltipDataChunk, ValueFormat } from '../../../types';
type Props = {
    hovered: TooltipDataChunk[];
    pinned?: boolean;
    rows?: ChartTooltip['rows'];
    rowRenderer?: ChartTooltip['rowRenderer'];
    totals?: ChartTooltip['totals'];
    valueFormat?: ValueFormat;
    headerFormat?: ChartTooltip['headerFormat'];
    xAxis?: ChartXAxis | null;
    yAxis?: ChartYAxis;
    qa?: string;
};
export declare const DefaultTooltipContent: ({ hovered, pinned, rows, rowRenderer: tooltipRowRenderer, totals, valueFormat, headerFormat, xAxis, yAxis, qa, }: Props) => React.JSX.Element;
export {};
