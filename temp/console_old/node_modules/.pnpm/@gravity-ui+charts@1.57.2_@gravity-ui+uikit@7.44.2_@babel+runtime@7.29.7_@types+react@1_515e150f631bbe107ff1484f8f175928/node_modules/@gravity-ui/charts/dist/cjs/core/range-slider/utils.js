import { isBandScale } from '../utils';
export function getRangeSliderOffsetTop(args) {
    var _a, _b;
    const { height, preparedChart, preparedLegend, preparedRangeSlider } = args;
    const legendHeight = (preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.enabled) ? ((_a = preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.height) !== null && _a !== void 0 ? _a : 0) : 0;
    const legendMargin = (preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.enabled) ? ((_b = preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.margin) !== null && _b !== void 0 ? _b : 0) : 0;
    return (height -
        preparedRangeSlider.height -
        legendHeight -
        legendMargin -
        preparedChart.margin.bottom);
}
export function getRangeSliderSelection(args) {
    const { rangeSliderState, xScale } = args;
    if (!rangeSliderState || !xScale || isBandScale(xScale)) {
        return undefined;
    }
    return [xScale(rangeSliderState.min), xScale(rangeSliderState.max)];
}
