import type { PrepareSeriesArgs } from '../../core/series/plugin';
import type { PreparedLineSeries } from '../../core/series/types';
import type { LineSeries } from '../../types';
export declare const DEFAULT_LEGEND_SYMBOL_SIZE = 16;
export declare const DEFAULT_LINE_WIDTH = 1;
export declare const DEFAULT_DASH_STYLE: "Solid";
export declare const DEFAULT_MARKER: {
    enabled: boolean;
    symbol: `${import("../../core/constants").SymbolType}`;
    borderColor: string;
    borderWidth: number;
    radius: number;
};
export declare function prepareLineSeries(args: PrepareSeriesArgs<LineSeries>): PreparedLineSeries[];
