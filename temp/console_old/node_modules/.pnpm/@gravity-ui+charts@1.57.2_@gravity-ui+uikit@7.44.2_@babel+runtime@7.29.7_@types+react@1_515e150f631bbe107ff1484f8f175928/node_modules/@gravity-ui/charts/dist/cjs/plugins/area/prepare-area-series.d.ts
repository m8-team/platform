import type { PrepareSeriesArgs } from '../../core/series/plugin';
import type { PreparedAreaSeries } from '../../core/series/types';
import type { AreaSeries } from '../../types';
export declare const DEFAULT_LINE_WIDTH = 1;
export declare const DEFAULT_MARKER: {
    enabled: boolean;
    symbol: `${import("../../core/constants").SymbolType}`;
    borderColor: string;
    borderWidth: number;
    radius: number;
};
export declare function prepareAreaSeries(args: PrepareSeriesArgs<AreaSeries>): PreparedAreaSeries[];
