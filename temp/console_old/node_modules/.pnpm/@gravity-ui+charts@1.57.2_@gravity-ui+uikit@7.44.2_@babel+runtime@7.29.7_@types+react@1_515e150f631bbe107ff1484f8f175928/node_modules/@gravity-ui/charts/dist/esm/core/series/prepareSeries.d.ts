import type { ChartData, ChartXAxis, ChartYAxis } from '../../types';
import type { PreparedLegend, PreparedSeries } from './types';
export declare const getPreparedSeries: ({ seriesData, seriesOptions, colors, preparedLegend, xAxis, yAxis, }: {
    seriesData: ChartData["series"]["data"];
    seriesOptions: ChartData["series"]["options"];
    colors: string[];
    preparedLegend?: PreparedLegend | null;
    xAxis?: ChartXAxis | null;
    yAxis?: ChartYAxis[];
}) => Promise<PreparedSeries[]>;
