'use strict';

var React = require('react');

/* Used by renderContent AsideHeader prop */
const RenderContent = React.memo(({ renderContent, size }) => {
    return React.createElement(React.Fragment, null, renderContent({ size }));
});
RenderContent.displayName = 'RenderContent';
const Content = ({ size, // TODO: move to context when MobileHeader will support it
className, cssSizeVariableName = '--gn-aside-header-size', renderContent, children, }) => {
    return (React.createElement("div", { className: className, style: Object.assign({}, { [cssSizeVariableName]: `${size}px` }) }, typeof renderContent === 'function' ? (React.createElement(RenderContent, { size: size, renderContent: renderContent })) : (children)));
};

exports.Content = Content;
//# sourceMappingURL=Content.js.map
