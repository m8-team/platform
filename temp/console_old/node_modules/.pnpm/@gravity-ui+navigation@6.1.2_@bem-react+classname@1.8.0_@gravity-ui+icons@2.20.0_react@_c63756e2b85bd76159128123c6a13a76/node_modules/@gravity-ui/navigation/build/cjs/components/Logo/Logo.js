import './Logo.css';
'use strict';

var tslib = require('tslib');
var React = require('react');
var uikit = require('@gravity-ui/uikit');
var cn = require('../utils/cn.js');
var Logo_module = require('./Logo.module.scss.js');

const b = cn.createBlock('logo', Logo_module.default);
const Logo = ({ text, icon, iconSrc, iconClassName, iconPlaceClassName, iconSize = 24, textSize = 15, href, target = '_self', wrapper, onClick, compact, className, buttonClassName, 'aria-label': ariaLabel, 'aria-labelledby': ariaLabelledby, }) => {
    const hasWrapper = typeof wrapper === 'function';
    let buttonIcon;
    if (iconSrc) {
        buttonIcon = (React.createElement("span", { className: iconClassName },
            React.createElement("img", { alt: "logo icon", src: iconSrc, width: iconSize, height: iconSize })));
    }
    else if (icon) {
        buttonIcon = React.createElement(uikit.Icon, { data: icon, size: iconSize, className: iconClassName });
    }
    let logo;
    if (typeof text === 'function') {
        logo = text();
    }
    else {
        logo = (React.createElement("div", { className: b('logo'), style: { fontSize: textSize } }, text));
    }
    const _a = href
        ? {
            tag: 'a',
            target,
            rel: target === '_self' ? undefined : 'noreferrer',
            href,
            'aria-label': ariaLabel,
            'aria-labelledby': ariaLabelledby,
        }
        : { tag: 'span' }, { tag: Button } = _a, buttonProps = tslib.__rest(_a, ["tag"]);
    const button = (React.createElement(Button, Object.assign({}, buttonProps, { className: b('btn-logo', buttonClassName), onClick: onClick }),
        React.createElement("span", { className: b('logo-icon-place', iconPlaceClassName) }, buttonIcon),
        !compact && logo));
    return (React.createElement("div", { className: b(null, className) }, hasWrapper ? wrapper(button, Boolean(compact)) : button));
};

exports.Logo = Logo;
//# sourceMappingURL=Logo.js.map
