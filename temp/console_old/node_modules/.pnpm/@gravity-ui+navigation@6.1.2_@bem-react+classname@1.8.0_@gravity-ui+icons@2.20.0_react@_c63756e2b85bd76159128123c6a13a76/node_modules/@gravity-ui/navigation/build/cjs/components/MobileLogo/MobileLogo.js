import './MobileLogo.css';
'use strict';

var React = require('react');
var uikit = require('@gravity-ui/uikit');
var cn = require('../utils/cn.js');
var MobileLogo_module = require('./MobileLogo.module.scss.js');

const b = cn.createBlock('mobile-logo', MobileLogo_module.default);
const MobileLogo = ({ text, compact, icon, iconSrc, iconClassName, iconSize = 32, textSize = 20, href = '/', target = '_self', wrapper, onClick, className, }) => {
    const hasWrapper = typeof wrapper === 'function';
    let logoIcon;
    if (iconSrc) {
        logoIcon = (React.createElement("img", { alt: "logo icon", src: iconSrc, width: iconSize, height: iconSize, className: iconClassName }));
    }
    else if (icon) {
        logoIcon = React.createElement(uikit.Icon, { data: icon, size: iconSize, className: b('icon', iconClassName) });
    }
    let logoTitle;
    if (typeof text === 'function') {
        logoTitle = text();
    }
    else {
        logoTitle = (React.createElement("span", { className: b('title'), style: { fontSize: textSize } }, text));
    }
    const logo = (React.createElement(React.Fragment, null,
        logoIcon,
        logoTitle));
    return hasWrapper ? (React.createElement("div", { className: b(null, className), onClick: onClick }, wrapper(logo, compact))) : (React.createElement("a", { href: href, target: target, ref: target === '_self' ? undefined : 'noreferrer', className: b(null, className), onClick: onClick }, logo));
};

exports.MobileLogo = MobileLogo;
//# sourceMappingURL=MobileLogo.js.map
