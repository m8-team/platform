'use strict';

var React = require('react');
var uikit = require('@gravity-ui/uikit');
var Logo = require('../../Logo/Logo.js');
var constants = require('../../constants.js');
var AsideHeaderContext = require('../AsideHeaderContext.js');
var utils = require('../utils.js');
var CompositeBar = require('./CompositeBar/CompositeBar.js');
var dividerCollapsed = require('../../../assets/icons/divider-collapsed.svg.js');

const DEFAULT_SUBHEADER_ITEMS = [];
const HEADER_COMPOSITE_ID = 'gravity-ui/navigation-header-composite-bar';
const Header = () => {
    const { logo, onItemClick, onClosePanel, headerDecoration, subheaderItems, compact } = AsideHeaderContext.useAsideHeaderInnerContext();
    const onLogoClick = React.useCallback((event) => {
        var _a;
        onClosePanel === null || onClosePanel === undefined ? undefined : onClosePanel();
        (_a = logo === null || logo === undefined ? undefined : logo.onClick) === null || _a === undefined ? undefined : _a.call(logo, event);
    }, [onClosePanel, logo]);
    return (React.createElement("div", { className: utils.b('header', { ['with-decoration']: headerDecoration }) },
        logo && (React.createElement(Logo.Logo, Object.assign({}, logo, { onClick: onLogoClick, compact: compact, buttonClassName: utils.b('logo-button'), iconPlaceClassName: utils.b('logo-icon-place') }))),
        React.createElement(CompositeBar.CompositeBar, { compositeId: HEADER_COMPOSITE_ID, type: "subheader", menuItemClassName: utils.b('menu-item'), compact: compact, items: subheaderItems || DEFAULT_SUBHEADER_ITEMS, onItemClick: onItemClick }),
        headerDecoration && (React.createElement(uikit.Icon, { data: dividerCollapsed.default, className: utils.b('header-divider'), width: constants.ASIDE_HEADER_COMPACT_WIDTH, height: constants.HEADER_DIVIDER_HEIGHT }))));
};

exports.Header = Header;
//# sourceMappingURL=Header.js.map
