import React from 'react';
import { TopAlertProps } from '../types';
type Props = {
    alert?: TopAlertProps;
    className?: string;
    mobileView?: boolean;
};
export declare const TopAlert: ({ alert, className, mobileView }: Props) => React.JSX.Element | null;
export {};
