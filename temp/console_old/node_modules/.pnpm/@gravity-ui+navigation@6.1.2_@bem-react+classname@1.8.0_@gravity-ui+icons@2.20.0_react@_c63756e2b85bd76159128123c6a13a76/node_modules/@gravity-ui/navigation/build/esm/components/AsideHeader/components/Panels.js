import { __rest } from 'tslib';
import React__default from 'react';
import { Drawer } from '@gravity-ui/uikit';
import { useAsideHeaderInnerContext } from '../AsideHeaderContext.js';
import { b } from '../utils.js';

const Panels = () => {
    const { panelItems, onClosePanel, size } = useAsideHeaderInnerContext();
    return panelItems ? (React__default.createElement(React__default.Fragment, null, panelItems.map((_a) => {
        var { id, className, style: itemStyle } = _a, rest = __rest(_a, ["id", "className", "style"]);
        return (React__default.createElement(Drawer, Object.assign({}, rest, { key: id, className: b('panels'), onOpenChange: (open) => !open && (onClosePanel === null || onClosePanel === undefined ? undefined : onClosePanel()), style: Object.assign(Object.assign({}, itemStyle), { left: size, top: 'var(--gn-top-alert-height, 0px)' }), contentClassName: b('panel', className) })));
    }))) : null;
};

export { Panels };
//# sourceMappingURL=Panels.js.map
