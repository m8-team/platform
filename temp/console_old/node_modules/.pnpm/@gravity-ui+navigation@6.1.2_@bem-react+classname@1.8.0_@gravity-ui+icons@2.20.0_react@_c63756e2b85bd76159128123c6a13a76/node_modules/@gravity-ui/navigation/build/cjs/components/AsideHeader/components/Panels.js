'use strict';

var tslib = require('tslib');
var React = require('react');
var uikit = require('@gravity-ui/uikit');
var AsideHeaderContext = require('../AsideHeaderContext.js');
var utils = require('../utils.js');

const Panels = () => {
    const { panelItems, onClosePanel, size } = AsideHeaderContext.useAsideHeaderInnerContext();
    return panelItems ? (React.createElement(React.Fragment, null, panelItems.map((_a) => {
        var { id, className, style: itemStyle } = _a, rest = tslib.__rest(_a, ["id", "className", "style"]);
        return (React.createElement(uikit.Drawer, Object.assign({}, rest, { key: id, className: utils.b('panels'), onOpenChange: (open) => !open && (onClosePanel === null || onClosePanel === undefined ? undefined : onClosePanel()), style: Object.assign(Object.assign({}, itemStyle), { left: size, top: 'var(--gn-top-alert-height, 0px)' }), contentClassName: utils.b('panel', className) })));
    }))) : null;
};

exports.Panels = Panels;
//# sourceMappingURL=Panels.js.map
