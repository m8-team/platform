import './CollapseButton.css';
'use strict';

var React = require('react');
var uikit = require('@gravity-ui/uikit');
var cn = require('../../../utils/cn.js');
var AsideHeaderContext = require('../../AsideHeaderContext.js');
var index = require('../../i18n/index.js');
var controlMenuButton = require('../../../../assets/icons/control-menu-button.svg.js');
var CollapseButton_module = require('./CollapseButton.module.scss.js');

const b = cn.createBlock('collapse-button', CollapseButton_module.default);
const CollapseButton = ({ className }) => {
    const { onChangeCompact, compact, expandTitle, collapseTitle, collapseButtonWrapper } = AsideHeaderContext.useAsideHeaderInnerContext();
    const onCollapseButtonClick = React.useCallback(() => {
        onChangeCompact === null || onChangeCompact === undefined ? undefined : onChangeCompact(!compact);
    }, [compact, onChangeCompact]);
    const buttonTitle = compact
        ? expandTitle || index.default('button_expand')
        : collapseTitle || index.default('button_collapse');
    const defaultButton = (React.createElement("button", { className: b({ compact }, className), onClick: onCollapseButtonClick, title: buttonTitle },
        React.createElement(uikit.Icon, { data: controlMenuButton.default, className: b('icon'), width: "16", height: "10" })));
    if (collapseButtonWrapper) {
        return collapseButtonWrapper(defaultButton, { compact, onChangeCompact });
    }
    return defaultButton;
};

exports.CollapseButton = CollapseButton;
//# sourceMappingURL=CollapseButton.js.map
