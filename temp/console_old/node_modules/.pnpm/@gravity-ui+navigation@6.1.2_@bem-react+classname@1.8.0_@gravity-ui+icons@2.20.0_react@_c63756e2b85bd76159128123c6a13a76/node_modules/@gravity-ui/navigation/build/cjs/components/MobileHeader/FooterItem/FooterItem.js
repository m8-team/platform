import './FooterItem.css';
'use strict';

var React = require('react');
var uikit = require('@gravity-ui/uikit');
var cn = require('../../utils/cn.js');
var constants = require('../constants.js');
var FooterItem_module = require('./FooterItem.module.scss.js');

const b = cn.createBlock('mobile-header-footer-item', FooterItem_module.default);
const FooterItem = ({ icon, iconSize = constants.MOBILE_HEADER_ICON_SIZE, className, modalItem = { visible: false }, onClick, eventBrokerMeta, }) => {
    var _a;
    const handleClick = React.useCallback((event) => {
        uikit.eventBroker.publish({
            componentId: 'MobileHeaderFooterItem',
            eventId: 'click',
            domEvent: event,
            meta: eventBrokerMeta,
        });
        onClick === null || onClick === undefined ? undefined : onClick(event);
    }, [onClick, eventBrokerMeta]);
    return (React.createElement("div", { className: b() },
        React.createElement("button", { className: b('button', className), onClick: handleClick }, icon ? React.createElement(uikit.Icon, { data: icon, size: iconSize, className: b('icon') }) : null),
        React.createElement(uikit.Sheet, { id: modalItem.id, title: modalItem.title, visible: modalItem.visible, className: b('modal', modalItem.className), contentClassName: b('modal-content', modalItem.contentClassName), allowHideOnContentScroll: modalItem.modalAllowHideOnContentScroll, onClose: modalItem.onClose }, (_a = modalItem.renderContent) === null || _a === undefined ? undefined : _a.call(modalItem))));
};

exports.FooterItem = FooterItem;
//# sourceMappingURL=FooterItem.js.map
