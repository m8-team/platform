import './Footer.css';
'use strict';

var React = require('react');
var icons = require('@gravity-ui/icons');
var uikit = require('@gravity-ui/uikit');
var useOverflowingHorizontalListItems = require('../../../hooks/useOverflowingHorizontalListItems/useOverflowingHorizontalListItems.js');
var Logo = require('../../Logo/Logo.js');
var cn = require('../../utils/cn.js');
var MenuItem = require('../MenuItem/MenuItem.js');
var Footer_module = require('./Footer.module.scss.js');

const b = cn.createBlock('footer', Footer_module.default);
const modalId = 'footer-more-items';
const MobileFooter = ({ className, menuItems: providedMenuItems, withDivider, moreButtonTitle, onMoreButtonClick, view = 'normal', logo, logoWrapperClassName, copyright, }) => {
    const [moreItemsMenuVisible, setMoreItemsMenuVisible] = React.useState(false);
    const menuContainerRef = React.useRef(null);
    const handleOpenMoreItemsMenu = React.useCallback((event) => {
        setMoreItemsMenuVisible(true);
        onMoreButtonClick === null || onMoreButtonClick === undefined ? undefined : onMoreButtonClick(event);
    }, [onMoreButtonClick]);
    const handleCloseMoreItemsMenu = React.useCallback(() => {
        setMoreItemsMenuVisible(false);
    }, []);
    const menuItems = view === 'clear' ? undefined : providedMenuItems;
    const { visibleItems, hiddenItems, measured } = useOverflowingHorizontalListItems.useOverflowingHorizontalListItems({
        containerRef: menuContainerRef,
        items: menuItems,
        itemSelector: `.${b('menu-item')}`,
        moreButtonWidth: 28,
    });
    const renderMenu = (items) => (React.createElement(uikit.Menu, { className: b('list') }, items.map((item, index) => (React.createElement(MenuItem.MenuItem, Object.assign({ key: index }, item, { className: b('menu-item', item.className) }))))));
    return (React.createElement("footer", { className: b({ mobile: true, 'with-divider': withDivider, view }, className) },
        React.createElement("div", { className: b('menu', { measured }), ref: menuContainerRef },
            visibleItems.length > 0 && renderMenu(visibleItems),
            hiddenItems.length > 0 && (React.createElement(React.Fragment, null,
                React.createElement(uikit.Button, { view: "flat-secondary", size: "l", onClick: handleOpenMoreItemsMenu, title: moreButtonTitle },
                    React.createElement(uikit.Icon, { data: icons.Ellipsis, size: 16 })),
                React.createElement(uikit.Sheet, { id: modalId, visible: moreItemsMenuVisible, className: b('modal'), contentClassName: b('modal-content'), onClose: handleCloseMoreItemsMenu }, renderMenu(hiddenItems))))),
        React.createElement("div", { className: b('bottom-row') },
            React.createElement("small", { className: b('copyright') }, copyright),
            view !== 'clear' && logo && (React.createElement("div", { className: logoWrapperClassName },
                React.createElement(Logo.Logo, Object.assign({}, logo)))))));
};

exports.MobileFooter = MobileFooter;
//# sourceMappingURL=Footer.js.map
