import './Title.css';
'use strict';

var React = require('react');
var icons = require('@gravity-ui/icons');
var uikit = require('@gravity-ui/uikit');
var cn = require('../utils/cn.js');
var index = require('./i18n/index.js');
var Title_module = require('./Title.module.scss.js');

const b = cn.createBlock('title', Title_module.default);
const Title = ({ children, closeIconSize = 23, hasSeparator, closeTitle = index.default('button_close'), onClose, }) => {
    return (React.createElement("div", { className: b({ separator: hasSeparator }) },
        React.createElement(uikit.Text, { className: b('text'), as: 'h3', variant: 'subheader-3' }, children),
        onClose && (React.createElement(uikit.Button, { onClick: onClose, view: "flat", size: "l", "aria-label": closeTitle },
            React.createElement(uikit.Icon, { data: icons.Xmark, size: closeIconSize })))));
};
Title.displayName = 'Title';

exports.Title = Title;
//# sourceMappingURL=Title.js.map
