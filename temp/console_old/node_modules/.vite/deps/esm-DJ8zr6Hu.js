import { n as __toESM, t as __commonJSMin } from "./rolldown-runtime-FDOR9p9I.js";
import { t as require_react } from "./react.js";
import { t as require_react_dom } from "./react-dom.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { t as I18N } from "./esm-BELMsyD0.js";
import { Ba as LayoutHeaderCellsLarge, Cc as Envelope, F as TriangleExclamationFill, I as TriangleExclamation, Jo as Gear, Jp as ArrowUpArrowDown, Ls as FileQuestion, Mo as Grip, Ns as FileZipper, Om as ArrowDown, Pd as ChevronsLeft, Qd as ChevronLeft, Rl as Code, Tc as Ellipsis, X as Thunderbolt, Xd as ChevronUp, Y as ThunderboltFill, Yp as ArrowUp, Zd as ChevronRight, _l as Copy, ad as CircleExclamation, af as Check, at as TextAlignLeft, bd as CircleCheckFill, ca as Lock, cl as CopyXmark, ef as ChevronDown, gc as Eye, hl as CopyCheck, ia as LogoAcrobat, js as Filmstrip, lu as CircleQuestion, nd as CircleInfo, nu as CircleXmark, pc as EyeSlash, r as Xmark, sd as CircleDashed, sr as Picture, td as CircleInfoFill, ti as MusicNote, tu as CircleXmarkFill, wc as EllipsisVertical, xd as CircleCheck } from "./esm-I_MRbCbt.js";
import { t as require_shim } from "./shim-Cc3yyvLa.js";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Text/colorText/colorText.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Icon/Icon.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ArrowToggle/ArrowToggle.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Disclosure/Disclosure.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Accordion/AccordionSummary/AccordionSummary.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Accordion/AccordionItem/AccordionItem.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Accordion/Accordion.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Hotkey/Hotkey.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Portal/Portal.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Tooltip/Tooltip.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ActionTooltip/ActionTooltip.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Button/Button.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Text/text/text.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Menu/Menu.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Popup/Popup.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/DropdownMenu.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ActionsPanel/components/CollapseActions.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ActionsPanel/ActionsPanel.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/Col/Col.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/Row/Row.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/spacing/spacing.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/Box/Box.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/Flex/Flex.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/Container/Container.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Card/Card.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Alert/Alert.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Avatar/Avatar.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/AvatarStack/AvatarStack.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/lab/ListItemView/ListItemView.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Breadcrumbs/Breadcrumbs.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ControlLabel/ControlLabel.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Checkbox/Checkbox.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ClipboardButton/ClipboardButton.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/HelpMark/HelpMark.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DefinitionList/DefinitionList.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Modal/Modal.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Dialog/ButtonClose/ButtonClose.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Dialog/DialogBody/DialogBody.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Dialog/DialogDivider/DialogDivider.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Dialog/DialogFooter/DialogFooter.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Dialog/DialogHeader/DialogHeader.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Dialog/Dialog.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Divider/Divider.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Drawer/components/Drawer.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/FilePreview/FilePreviewActions/DesktopActionsMenu/DesktopActionsMenu.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/common/ClearButton/ClearButton.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/common/OuterAdditionalContent/OuterAdditionalContent.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/TextArea/TextArea.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/TextInput/TextInput.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/PasswordInput/PasswordInput.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Loader/Loader.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/List/List.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Sheet/Sheet.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/FilePreview/FilePreviewActions/MobileActionsMenu/MobileActionsMenu.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/FilePreview/FilePreview.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Label/Label.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Link/Link.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/NumberInput/NumericArrows/NumericArrows.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/NumberInput/NumberInput.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Overlay/Overlay.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/components/PaginationEllipsis/PaginationEllipsis.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/components/PaginationPage/PaginationPage.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/components/PaginationPageOf/PaginationPageOf.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/components/PaginationInput/PaginationInput.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/EmptyOptions/EmptyOptions.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectClear/SelectClear.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectCounter/SelectCounter.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectControl/SelectControl.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectFilter/SelectFilter.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectList/SelectList.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectPopup/SelectPopup.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/Select.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/Pagination.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Palette/Palette.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/PinInput/PinInput.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/PlaceholderContainer/PlaceholderContainer.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Progress/Progress.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Radio/Radio.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/RadioGroup/RadioGroup.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/SegmentedRadioGroup/SegmentedRadioGroup.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Skeleton/Skeleton.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Slider/BaseSlider/BaseSlider.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Slider/SliderTooltip/SliderTooltip.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Slider/Slider.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Stepper/Stepper.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Spin/Spin.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Switch/Switch.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/Table.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/hoc/withTableSelection/withTableSelection.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/hoc/withTableActions/withTableActions.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/hoc/withTableCopy/withTableCopy.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/hoc/withTableSorting/withTableSorting.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/components/ListItemExpandIcon/ListItemExpandIcon.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/components/ListItemView/ListItemView.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/components/ListRecursiveRenderer/ListRecursiveRenderer.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/components/ListContainerView/ListContainerView.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/TreeSelect/TreeSelect.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/hoc/withTableSettings/TableColumnSetup/TableColumnSetup.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/hoc/withTableSettings/withTableSettings.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/TableColumnSetup/TableColumnSetup.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/lab/Menu/MenuDivider.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/lab/Menu/MenuItem.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/lab/Menu/Menu.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/Tab.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/TabListCollapseItem/TabListCollapseItem.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/TabList.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/TabPanel.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toaster/Toast/Toast.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toaster/ToastList/ToastAnimation.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toaster/ToastList/ToastList.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toc/TocItem/TocItem.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toc/TocSections/TocSections.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/User/User.css";
import "/Users/serg/GolandProjects/platform/ui/apps/console/node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/UserLabel/UserLabel.css";
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isArray.js
var require_isArray = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = Array.isArray;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_freeGlobal.js
var require__freeGlobal = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = typeof global == "object" && global && global.Object === Object && global;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_root.js
var require__root = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var freeGlobal = require__freeGlobal();
	/** Detect free variable `self`. */
	var freeSelf = typeof self == "object" && self && self.Object === Object && self;
	module.exports = freeGlobal || freeSelf || Function("return this")();
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_Symbol.js
var require__Symbol = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__root().Symbol;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_getRawTag.js
var require__getRawTag = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Symbol = require__Symbol();
	/** Used for built-in method references. */
	var objectProto = Object.prototype;
	/** Used to check objects for own properties. */
	var hasOwnProperty = objectProto.hasOwnProperty;
	/**
	* Used to resolve the
	* [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
	* of values.
	*/
	var nativeObjectToString = objectProto.toString;
	/** Built-in value references. */
	var symToStringTag = Symbol ? Symbol.toStringTag : void 0;
	/**
	* A specialized version of `baseGetTag` which ignores `Symbol.toStringTag` values.
	*
	* @private
	* @param {*} value The value to query.
	* @returns {string} Returns the raw `toStringTag`.
	*/
	function getRawTag(value) {
		var isOwn = hasOwnProperty.call(value, symToStringTag), tag = value[symToStringTag];
		try {
			value[symToStringTag] = void 0;
			var unmasked = true;
		} catch (e) {}
		var result = nativeObjectToString.call(value);
		if (unmasked) if (isOwn) value[symToStringTag] = tag;
		else delete value[symToStringTag];
		return result;
	}
	module.exports = getRawTag;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_objectToString.js
var require__objectToString = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Used to resolve the
	* [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
	* of values.
	*/
	var nativeObjectToString = Object.prototype.toString;
	/**
	* Converts `value` to a string using `Object.prototype.toString`.
	*
	* @private
	* @param {*} value The value to convert.
	* @returns {string} Returns the converted string.
	*/
	function objectToString(value) {
		return nativeObjectToString.call(value);
	}
	module.exports = objectToString;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseGetTag.js
var require__baseGetTag = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Symbol = require__Symbol();
	var getRawTag = require__getRawTag();
	var objectToString = require__objectToString();
	/** `Object#toString` result references. */
	var nullTag = "[object Null]";
	var undefinedTag = "[object Undefined]";
	/** Built-in value references. */
	var symToStringTag = Symbol ? Symbol.toStringTag : void 0;
	/**
	* The base implementation of `getTag` without fallbacks for buggy environments.
	*
	* @private
	* @param {*} value The value to query.
	* @returns {string} Returns the `toStringTag`.
	*/
	function baseGetTag(value) {
		if (value == null) return value === void 0 ? undefinedTag : nullTag;
		return symToStringTag && symToStringTag in Object(value) ? getRawTag(value) : objectToString(value);
	}
	module.exports = baseGetTag;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isObjectLike.js
var require_isObjectLike = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Checks if `value` is object-like. A value is object-like if it's not `null`
	* and has a `typeof` result of "object".
	*
	* @static
	* @memberOf _
	* @since 4.0.0
	* @category Lang
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is object-like, else `false`.
	* @example
	*
	* _.isObjectLike({});
	* // => true
	*
	* _.isObjectLike([1, 2, 3]);
	* // => true
	*
	* _.isObjectLike(_.noop);
	* // => false
	*
	* _.isObjectLike(null);
	* // => false
	*/
	function isObjectLike(value) {
		return value != null && typeof value == "object";
	}
	module.exports = isObjectLike;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isSymbol.js
var require_isSymbol = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseGetTag = require__baseGetTag();
	var isObjectLike = require_isObjectLike();
	/** `Object#toString` result references. */
	var symbolTag = "[object Symbol]";
	/**
	* Checks if `value` is classified as a `Symbol` primitive or object.
	*
	* @static
	* @memberOf _
	* @since 4.0.0
	* @category Lang
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is a symbol, else `false`.
	* @example
	*
	* _.isSymbol(Symbol.iterator);
	* // => true
	*
	* _.isSymbol('abc');
	* // => false
	*/
	function isSymbol(value) {
		return typeof value == "symbol" || isObjectLike(value) && baseGetTag(value) == symbolTag;
	}
	module.exports = isSymbol;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_isKey.js
var require__isKey = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isArray = require_isArray();
	var isSymbol = require_isSymbol();
	/** Used to match property names within property paths. */
	var reIsDeepProp = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/;
	var reIsPlainProp = /^\w*$/;
	/**
	* Checks if `value` is a property name and not a property path.
	*
	* @private
	* @param {*} value The value to check.
	* @param {Object} [object] The object to query keys on.
	* @returns {boolean} Returns `true` if `value` is a property name, else `false`.
	*/
	function isKey(value, object) {
		if (isArray(value)) return false;
		var type = typeof value;
		if (type == "number" || type == "symbol" || type == "boolean" || value == null || isSymbol(value)) return true;
		return reIsPlainProp.test(value) || !reIsDeepProp.test(value) || object != null && value in Object(object);
	}
	module.exports = isKey;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isObject.js
var require_isObject = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Checks if `value` is the
	* [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)
	* of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)
	*
	* @static
	* @memberOf _
	* @since 0.1.0
	* @category Lang
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is an object, else `false`.
	* @example
	*
	* _.isObject({});
	* // => true
	*
	* _.isObject([1, 2, 3]);
	* // => true
	*
	* _.isObject(_.noop);
	* // => true
	*
	* _.isObject(null);
	* // => false
	*/
	function isObject(value) {
		var type = typeof value;
		return value != null && (type == "object" || type == "function");
	}
	module.exports = isObject;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isFunction.js
var require_isFunction = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseGetTag = require__baseGetTag();
	var isObject = require_isObject();
	/** `Object#toString` result references. */
	var asyncTag = "[object AsyncFunction]";
	var funcTag = "[object Function]";
	var genTag = "[object GeneratorFunction]";
	var proxyTag = "[object Proxy]";
	/**
	* Checks if `value` is classified as a `Function` object.
	*
	* @static
	* @memberOf _
	* @since 0.1.0
	* @category Lang
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is a function, else `false`.
	* @example
	*
	* _.isFunction(_);
	* // => true
	*
	* _.isFunction(/abc/);
	* // => false
	*/
	function isFunction(value) {
		if (!isObject(value)) return false;
		var tag = baseGetTag(value);
		return tag == funcTag || tag == genTag || tag == asyncTag || tag == proxyTag;
	}
	module.exports = isFunction;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_coreJsData.js
var require__coreJsData = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__root()["__core-js_shared__"];
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_isMasked.js
var require__isMasked = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var coreJsData = require__coreJsData();
	/** Used to detect methods masquerading as native. */
	var maskSrcKey = function() {
		var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || "");
		return uid ? "Symbol(src)_1." + uid : "";
	}();
	/**
	* Checks if `func` has its source masked.
	*
	* @private
	* @param {Function} func The function to check.
	* @returns {boolean} Returns `true` if `func` is masked, else `false`.
	*/
	function isMasked(func) {
		return !!maskSrcKey && maskSrcKey in func;
	}
	module.exports = isMasked;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_toSource.js
var require__toSource = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/** Used to resolve the decompiled source of functions. */
	var funcToString = Function.prototype.toString;
	/**
	* Converts `func` to its source code.
	*
	* @private
	* @param {Function} func The function to convert.
	* @returns {string} Returns the source code.
	*/
	function toSource(func) {
		if (func != null) {
			try {
				return funcToString.call(func);
			} catch (e) {}
			try {
				return func + "";
			} catch (e) {}
		}
		return "";
	}
	module.exports = toSource;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseIsNative.js
var require__baseIsNative = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isFunction = require_isFunction();
	var isMasked = require__isMasked();
	var isObject = require_isObject();
	var toSource = require__toSource();
	/**
	* Used to match `RegExp`
	* [syntax characters](http://ecma-international.org/ecma-262/7.0/#sec-patterns).
	*/
	var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;
	/** Used to detect host constructors (Safari). */
	var reIsHostCtor = /^\[object .+?Constructor\]$/;
	/** Used for built-in method references. */
	var funcProto = Function.prototype;
	var objectProto = Object.prototype;
	/** Used to resolve the decompiled source of functions. */
	var funcToString = funcProto.toString;
	/** Used to check objects for own properties. */
	var hasOwnProperty = objectProto.hasOwnProperty;
	/** Used to detect if a method is native. */
	var reIsNative = RegExp("^" + funcToString.call(hasOwnProperty).replace(reRegExpChar, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$");
	/**
	* The base implementation of `_.isNative` without bad shim checks.
	*
	* @private
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is a native function,
	*  else `false`.
	*/
	function baseIsNative(value) {
		if (!isObject(value) || isMasked(value)) return false;
		return (isFunction(value) ? reIsNative : reIsHostCtor).test(toSource(value));
	}
	module.exports = baseIsNative;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_getValue.js
var require__getValue = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Gets the value at `key` of `object`.
	*
	* @private
	* @param {Object} [object] The object to query.
	* @param {string} key The key of the property to get.
	* @returns {*} Returns the property value.
	*/
	function getValue(object, key) {
		return object == null ? void 0 : object[key];
	}
	module.exports = getValue;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_getNative.js
var require__getNative = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseIsNative = require__baseIsNative();
	var getValue = require__getValue();
	/**
	* Gets the native function at `key` of `object`.
	*
	* @private
	* @param {Object} object The object to query.
	* @param {string} key The key of the method to get.
	* @returns {*} Returns the function if it's native, else `undefined`.
	*/
	function getNative(object, key) {
		var value = getValue(object, key);
		return baseIsNative(value) ? value : void 0;
	}
	module.exports = getNative;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_nativeCreate.js
var require__nativeCreate = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__getNative()(Object, "create");
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_hashClear.js
var require__hashClear = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var nativeCreate = require__nativeCreate();
	/**
	* Removes all key-value entries from the hash.
	*
	* @private
	* @name clear
	* @memberOf Hash
	*/
	function hashClear() {
		this.__data__ = nativeCreate ? nativeCreate(null) : {};
		this.size = 0;
	}
	module.exports = hashClear;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_hashDelete.js
var require__hashDelete = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Removes `key` and its value from the hash.
	*
	* @private
	* @name delete
	* @memberOf Hash
	* @param {Object} hash The hash to modify.
	* @param {string} key The key of the value to remove.
	* @returns {boolean} Returns `true` if the entry was removed, else `false`.
	*/
	function hashDelete(key) {
		var result = this.has(key) && delete this.__data__[key];
		this.size -= result ? 1 : 0;
		return result;
	}
	module.exports = hashDelete;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_hashGet.js
var require__hashGet = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var nativeCreate = require__nativeCreate();
	/** Used to stand-in for `undefined` hash values. */
	var HASH_UNDEFINED = "__lodash_hash_undefined__";
	/** Used to check objects for own properties. */
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	/**
	* Gets the hash value for `key`.
	*
	* @private
	* @name get
	* @memberOf Hash
	* @param {string} key The key of the value to get.
	* @returns {*} Returns the entry value.
	*/
	function hashGet(key) {
		var data = this.__data__;
		if (nativeCreate) {
			var result = data[key];
			return result === HASH_UNDEFINED ? void 0 : result;
		}
		return hasOwnProperty.call(data, key) ? data[key] : void 0;
	}
	module.exports = hashGet;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_hashHas.js
var require__hashHas = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var nativeCreate = require__nativeCreate();
	/** Used to check objects for own properties. */
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	/**
	* Checks if a hash value for `key` exists.
	*
	* @private
	* @name has
	* @memberOf Hash
	* @param {string} key The key of the entry to check.
	* @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
	*/
	function hashHas(key) {
		var data = this.__data__;
		return nativeCreate ? data[key] !== void 0 : hasOwnProperty.call(data, key);
	}
	module.exports = hashHas;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_hashSet.js
var require__hashSet = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var nativeCreate = require__nativeCreate();
	/** Used to stand-in for `undefined` hash values. */
	var HASH_UNDEFINED = "__lodash_hash_undefined__";
	/**
	* Sets the hash `key` to `value`.
	*
	* @private
	* @name set
	* @memberOf Hash
	* @param {string} key The key of the value to set.
	* @param {*} value The value to set.
	* @returns {Object} Returns the hash instance.
	*/
	function hashSet(key, value) {
		var data = this.__data__;
		this.size += this.has(key) ? 0 : 1;
		data[key] = nativeCreate && value === void 0 ? HASH_UNDEFINED : value;
		return this;
	}
	module.exports = hashSet;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_Hash.js
var require__Hash = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var hashClear = require__hashClear();
	var hashDelete = require__hashDelete();
	var hashGet = require__hashGet();
	var hashHas = require__hashHas();
	var hashSet = require__hashSet();
	/**
	* Creates a hash object.
	*
	* @private
	* @constructor
	* @param {Array} [entries] The key-value pairs to cache.
	*/
	function Hash(entries) {
		var index = -1, length = entries == null ? 0 : entries.length;
		this.clear();
		while (++index < length) {
			var entry = entries[index];
			this.set(entry[0], entry[1]);
		}
	}
	Hash.prototype.clear = hashClear;
	Hash.prototype["delete"] = hashDelete;
	Hash.prototype.get = hashGet;
	Hash.prototype.has = hashHas;
	Hash.prototype.set = hashSet;
	module.exports = Hash;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_listCacheClear.js
var require__listCacheClear = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Removes all key-value entries from the list cache.
	*
	* @private
	* @name clear
	* @memberOf ListCache
	*/
	function listCacheClear() {
		this.__data__ = [];
		this.size = 0;
	}
	module.exports = listCacheClear;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/eq.js
var require_eq = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Performs a
	* [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
	* comparison between two values to determine if they are equivalent.
	*
	* @static
	* @memberOf _
	* @since 4.0.0
	* @category Lang
	* @param {*} value The value to compare.
	* @param {*} other The other value to compare.
	* @returns {boolean} Returns `true` if the values are equivalent, else `false`.
	* @example
	*
	* var object = { 'a': 1 };
	* var other = { 'a': 1 };
	*
	* _.eq(object, object);
	* // => true
	*
	* _.eq(object, other);
	* // => false
	*
	* _.eq('a', 'a');
	* // => true
	*
	* _.eq('a', Object('a'));
	* // => false
	*
	* _.eq(NaN, NaN);
	* // => true
	*/
	function eq(value, other) {
		return value === other || value !== value && other !== other;
	}
	module.exports = eq;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_assocIndexOf.js
var require__assocIndexOf = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var eq = require_eq();
	/**
	* Gets the index at which the `key` is found in `array` of key-value pairs.
	*
	* @private
	* @param {Array} array The array to inspect.
	* @param {*} key The key to search for.
	* @returns {number} Returns the index of the matched value, else `-1`.
	*/
	function assocIndexOf(array, key) {
		var length = array.length;
		while (length--) if (eq(array[length][0], key)) return length;
		return -1;
	}
	module.exports = assocIndexOf;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_listCacheDelete.js
var require__listCacheDelete = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var assocIndexOf = require__assocIndexOf();
	/** Built-in value references. */
	var splice = Array.prototype.splice;
	/**
	* Removes `key` and its value from the list cache.
	*
	* @private
	* @name delete
	* @memberOf ListCache
	* @param {string} key The key of the value to remove.
	* @returns {boolean} Returns `true` if the entry was removed, else `false`.
	*/
	function listCacheDelete(key) {
		var data = this.__data__, index = assocIndexOf(data, key);
		if (index < 0) return false;
		if (index == data.length - 1) data.pop();
		else splice.call(data, index, 1);
		--this.size;
		return true;
	}
	module.exports = listCacheDelete;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_listCacheGet.js
var require__listCacheGet = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var assocIndexOf = require__assocIndexOf();
	/**
	* Gets the list cache value for `key`.
	*
	* @private
	* @name get
	* @memberOf ListCache
	* @param {string} key The key of the value to get.
	* @returns {*} Returns the entry value.
	*/
	function listCacheGet(key) {
		var data = this.__data__, index = assocIndexOf(data, key);
		return index < 0 ? void 0 : data[index][1];
	}
	module.exports = listCacheGet;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_listCacheHas.js
var require__listCacheHas = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var assocIndexOf = require__assocIndexOf();
	/**
	* Checks if a list cache value for `key` exists.
	*
	* @private
	* @name has
	* @memberOf ListCache
	* @param {string} key The key of the entry to check.
	* @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
	*/
	function listCacheHas(key) {
		return assocIndexOf(this.__data__, key) > -1;
	}
	module.exports = listCacheHas;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_listCacheSet.js
var require__listCacheSet = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var assocIndexOf = require__assocIndexOf();
	/**
	* Sets the list cache `key` to `value`.
	*
	* @private
	* @name set
	* @memberOf ListCache
	* @param {string} key The key of the value to set.
	* @param {*} value The value to set.
	* @returns {Object} Returns the list cache instance.
	*/
	function listCacheSet(key, value) {
		var data = this.__data__, index = assocIndexOf(data, key);
		if (index < 0) {
			++this.size;
			data.push([key, value]);
		} else data[index][1] = value;
		return this;
	}
	module.exports = listCacheSet;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_ListCache.js
var require__ListCache = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var listCacheClear = require__listCacheClear();
	var listCacheDelete = require__listCacheDelete();
	var listCacheGet = require__listCacheGet();
	var listCacheHas = require__listCacheHas();
	var listCacheSet = require__listCacheSet();
	/**
	* Creates an list cache object.
	*
	* @private
	* @constructor
	* @param {Array} [entries] The key-value pairs to cache.
	*/
	function ListCache(entries) {
		var index = -1, length = entries == null ? 0 : entries.length;
		this.clear();
		while (++index < length) {
			var entry = entries[index];
			this.set(entry[0], entry[1]);
		}
	}
	ListCache.prototype.clear = listCacheClear;
	ListCache.prototype["delete"] = listCacheDelete;
	ListCache.prototype.get = listCacheGet;
	ListCache.prototype.has = listCacheHas;
	ListCache.prototype.set = listCacheSet;
	module.exports = ListCache;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_Map.js
var require__Map = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__getNative()(require__root(), "Map");
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_mapCacheClear.js
var require__mapCacheClear = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Hash = require__Hash();
	var ListCache = require__ListCache();
	var Map = require__Map();
	/**
	* Removes all key-value entries from the map.
	*
	* @private
	* @name clear
	* @memberOf MapCache
	*/
	function mapCacheClear() {
		this.size = 0;
		this.__data__ = {
			"hash": new Hash(),
			"map": new (Map || ListCache)(),
			"string": new Hash()
		};
	}
	module.exports = mapCacheClear;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_isKeyable.js
var require__isKeyable = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Checks if `value` is suitable for use as unique object key.
	*
	* @private
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is suitable, else `false`.
	*/
	function isKeyable(value) {
		var type = typeof value;
		return type == "string" || type == "number" || type == "symbol" || type == "boolean" ? value !== "__proto__" : value === null;
	}
	module.exports = isKeyable;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_getMapData.js
var require__getMapData = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isKeyable = require__isKeyable();
	/**
	* Gets the data for `map`.
	*
	* @private
	* @param {Object} map The map to query.
	* @param {string} key The reference key.
	* @returns {*} Returns the map data.
	*/
	function getMapData(map, key) {
		var data = map.__data__;
		return isKeyable(key) ? data[typeof key == "string" ? "string" : "hash"] : data.map;
	}
	module.exports = getMapData;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_mapCacheDelete.js
var require__mapCacheDelete = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getMapData = require__getMapData();
	/**
	* Removes `key` and its value from the map.
	*
	* @private
	* @name delete
	* @memberOf MapCache
	* @param {string} key The key of the value to remove.
	* @returns {boolean} Returns `true` if the entry was removed, else `false`.
	*/
	function mapCacheDelete(key) {
		var result = getMapData(this, key)["delete"](key);
		this.size -= result ? 1 : 0;
		return result;
	}
	module.exports = mapCacheDelete;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_mapCacheGet.js
var require__mapCacheGet = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getMapData = require__getMapData();
	/**
	* Gets the map value for `key`.
	*
	* @private
	* @name get
	* @memberOf MapCache
	* @param {string} key The key of the value to get.
	* @returns {*} Returns the entry value.
	*/
	function mapCacheGet(key) {
		return getMapData(this, key).get(key);
	}
	module.exports = mapCacheGet;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_mapCacheHas.js
var require__mapCacheHas = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getMapData = require__getMapData();
	/**
	* Checks if a map value for `key` exists.
	*
	* @private
	* @name has
	* @memberOf MapCache
	* @param {string} key The key of the entry to check.
	* @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
	*/
	function mapCacheHas(key) {
		return getMapData(this, key).has(key);
	}
	module.exports = mapCacheHas;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_mapCacheSet.js
var require__mapCacheSet = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getMapData = require__getMapData();
	/**
	* Sets the map `key` to `value`.
	*
	* @private
	* @name set
	* @memberOf MapCache
	* @param {string} key The key of the value to set.
	* @param {*} value The value to set.
	* @returns {Object} Returns the map cache instance.
	*/
	function mapCacheSet(key, value) {
		var data = getMapData(this, key), size = data.size;
		data.set(key, value);
		this.size += data.size == size ? 0 : 1;
		return this;
	}
	module.exports = mapCacheSet;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_MapCache.js
var require__MapCache = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var mapCacheClear = require__mapCacheClear();
	var mapCacheDelete = require__mapCacheDelete();
	var mapCacheGet = require__mapCacheGet();
	var mapCacheHas = require__mapCacheHas();
	var mapCacheSet = require__mapCacheSet();
	/**
	* Creates a map cache object to store key-value pairs.
	*
	* @private
	* @constructor
	* @param {Array} [entries] The key-value pairs to cache.
	*/
	function MapCache(entries) {
		var index = -1, length = entries == null ? 0 : entries.length;
		this.clear();
		while (++index < length) {
			var entry = entries[index];
			this.set(entry[0], entry[1]);
		}
	}
	MapCache.prototype.clear = mapCacheClear;
	MapCache.prototype["delete"] = mapCacheDelete;
	MapCache.prototype.get = mapCacheGet;
	MapCache.prototype.has = mapCacheHas;
	MapCache.prototype.set = mapCacheSet;
	module.exports = MapCache;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/memoize.js
var require_memoize = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var MapCache = require__MapCache();
	/** Error message constants. */
	var FUNC_ERROR_TEXT = "Expected a function";
	/**
	* Creates a function that memoizes the result of `func`. If `resolver` is
	* provided, it determines the cache key for storing the result based on the
	* arguments provided to the memoized function. By default, the first argument
	* provided to the memoized function is used as the map cache key. The `func`
	* is invoked with the `this` binding of the memoized function.
	*
	* **Note:** The cache is exposed as the `cache` property on the memoized
	* function. Its creation may be customized by replacing the `_.memoize.Cache`
	* constructor with one whose instances implement the
	* [`Map`](http://ecma-international.org/ecma-262/7.0/#sec-properties-of-the-map-prototype-object)
	* method interface of `clear`, `delete`, `get`, `has`, and `set`.
	*
	* @static
	* @memberOf _
	* @since 0.1.0
	* @category Function
	* @param {Function} func The function to have its output memoized.
	* @param {Function} [resolver] The function to resolve the cache key.
	* @returns {Function} Returns the new memoized function.
	* @example
	*
	* var object = { 'a': 1, 'b': 2 };
	* var other = { 'c': 3, 'd': 4 };
	*
	* var values = _.memoize(_.values);
	* values(object);
	* // => [1, 2]
	*
	* values(other);
	* // => [3, 4]
	*
	* object.a = 2;
	* values(object);
	* // => [1, 2]
	*
	* // Modify the result cache.
	* values.cache.set(object, ['a', 'b']);
	* values(object);
	* // => ['a', 'b']
	*
	* // Replace `_.memoize.Cache`.
	* _.memoize.Cache = WeakMap;
	*/
	function memoize(func, resolver) {
		if (typeof func != "function" || resolver != null && typeof resolver != "function") throw new TypeError(FUNC_ERROR_TEXT);
		var memoized = function() {
			var args = arguments, key = resolver ? resolver.apply(this, args) : args[0], cache = memoized.cache;
			if (cache.has(key)) return cache.get(key);
			var result = func.apply(this, args);
			memoized.cache = cache.set(key, result) || cache;
			return result;
		};
		memoized.cache = new (memoize.Cache || MapCache)();
		return memoized;
	}
	memoize.Cache = MapCache;
	module.exports = memoize;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_memoizeCapped.js
var require__memoizeCapped = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var memoize = require_memoize();
	/** Used as the maximum memoize cache size. */
	var MAX_MEMOIZE_SIZE = 500;
	/**
	* A specialized version of `_.memoize` which clears the memoized function's
	* cache when it exceeds `MAX_MEMOIZE_SIZE`.
	*
	* @private
	* @param {Function} func The function to have its output memoized.
	* @returns {Function} Returns the new memoized function.
	*/
	function memoizeCapped(func) {
		var result = memoize(func, function(key) {
			if (cache.size === MAX_MEMOIZE_SIZE) cache.clear();
			return key;
		});
		var cache = result.cache;
		return result;
	}
	module.exports = memoizeCapped;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_stringToPath.js
var require__stringToPath = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var memoizeCapped = require__memoizeCapped();
	/** Used to match property names within property paths. */
	var rePropName = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g;
	/** Used to match backslashes in property paths. */
	var reEscapeChar = /\\(\\)?/g;
	module.exports = memoizeCapped(function(string) {
		var result = [];
		if (string.charCodeAt(0) === 46) result.push("");
		string.replace(rePropName, function(match, number, quote, subString) {
			result.push(quote ? subString.replace(reEscapeChar, "$1") : number || match);
		});
		return result;
	});
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_arrayMap.js
var require__arrayMap = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* A specialized version of `_.map` for arrays without support for iteratee
	* shorthands.
	*
	* @private
	* @param {Array} [array] The array to iterate over.
	* @param {Function} iteratee The function invoked per iteration.
	* @returns {Array} Returns the new mapped array.
	*/
	function arrayMap(array, iteratee) {
		var index = -1, length = array == null ? 0 : array.length, result = Array(length);
		while (++index < length) result[index] = iteratee(array[index], index, array);
		return result;
	}
	module.exports = arrayMap;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseToString.js
var require__baseToString = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Symbol = require__Symbol();
	var arrayMap = require__arrayMap();
	var isArray = require_isArray();
	var isSymbol = require_isSymbol();
	/** Used as references for various `Number` constants. */
	var INFINITY = Infinity;
	/** Used to convert symbols to primitives and strings. */
	var symbolProto = Symbol ? Symbol.prototype : void 0;
	var symbolToString = symbolProto ? symbolProto.toString : void 0;
	/**
	* The base implementation of `_.toString` which doesn't convert nullish
	* values to empty strings.
	*
	* @private
	* @param {*} value The value to process.
	* @returns {string} Returns the string.
	*/
	function baseToString(value) {
		if (typeof value == "string") return value;
		if (isArray(value)) return arrayMap(value, baseToString) + "";
		if (isSymbol(value)) return symbolToString ? symbolToString.call(value) : "";
		var result = value + "";
		return result == "0" && 1 / value == -INFINITY ? "-0" : result;
	}
	module.exports = baseToString;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/toString.js
var require_toString = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseToString = require__baseToString();
	/**
	* Converts `value` to a string. An empty string is returned for `null`
	* and `undefined` values. The sign of `-0` is preserved.
	*
	* @static
	* @memberOf _
	* @since 4.0.0
	* @category Lang
	* @param {*} value The value to convert.
	* @returns {string} Returns the converted string.
	* @example
	*
	* _.toString(null);
	* // => ''
	*
	* _.toString(-0);
	* // => '-0'
	*
	* _.toString([1, 2, 3]);
	* // => '1,2,3'
	*/
	function toString(value) {
		return value == null ? "" : baseToString(value);
	}
	module.exports = toString;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_castPath.js
var require__castPath = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isArray = require_isArray();
	var isKey = require__isKey();
	var stringToPath = require__stringToPath();
	var toString = require_toString();
	/**
	* Casts `value` to a path array if it's not one.
	*
	* @private
	* @param {*} value The value to inspect.
	* @param {Object} [object] The object to query keys on.
	* @returns {Array} Returns the cast property path array.
	*/
	function castPath(value, object) {
		if (isArray(value)) return value;
		return isKey(value, object) ? [value] : stringToPath(toString(value));
	}
	module.exports = castPath;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_toKey.js
var require__toKey = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isSymbol = require_isSymbol();
	/** Used as references for various `Number` constants. */
	var INFINITY = Infinity;
	/**
	* Converts `value` to a string key if it's not a string or symbol.
	*
	* @private
	* @param {*} value The value to inspect.
	* @returns {string|symbol} Returns the key.
	*/
	function toKey(value) {
		if (typeof value == "string" || isSymbol(value)) return value;
		var result = value + "";
		return result == "0" && 1 / value == -INFINITY ? "-0" : result;
	}
	module.exports = toKey;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseGet.js
var require__baseGet = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var castPath = require__castPath();
	var toKey = require__toKey();
	/**
	* The base implementation of `_.get` without support for default values.
	*
	* @private
	* @param {Object} object The object to query.
	* @param {Array|string} path The path of the property to get.
	* @returns {*} Returns the resolved value.
	*/
	function baseGet(object, path) {
		path = castPath(path, object);
		var index = 0, length = path.length;
		while (object != null && index < length) object = object[toKey(path[index++])];
		return index && index == length ? object : void 0;
	}
	module.exports = baseGet;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/get.js
var require_get = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseGet = require__baseGet();
	/**
	* Gets the value at `path` of `object`. If the resolved value is
	* `undefined`, the `defaultValue` is returned in its place.
	*
	* @static
	* @memberOf _
	* @since 3.7.0
	* @category Object
	* @param {Object} object The object to query.
	* @param {Array|string} path The path of the property to get.
	* @param {*} [defaultValue] The value returned for `undefined` resolved values.
	* @returns {*} Returns the resolved value.
	* @example
	*
	* var object = { 'a': [{ 'b': { 'c': 3 } }] };
	*
	* _.get(object, 'a[0].b.c');
	* // => 3
	*
	* _.get(object, ['a', '0', 'b', 'c']);
	* // => 3
	*
	* _.get(object, 'a.b.c', 'default');
	* // => 'default'
	*/
	function get(object, path, defaultValue) {
		var result = object == null ? void 0 : baseGet(object, path);
		return result === void 0 ? defaultValue : result;
	}
	module.exports = get;
}));
//#endregion
//#region node_modules/.pnpm/@bem-react+classname@1.8.0/node_modules/@bem-react/classname/build/classname.development.mjs
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var import_jsx_runtime = require_jsx_runtime();
/**
* BEM className configure function.
*
* @example
* ``` ts
*
* import { withNaming } from '@bem-react/classname';
*
* const cn = withNaming({ n: 'ns-', e: '__', m: '_' });
*
* cn('block', 'elem'); // 'ns-block__elem'
* ```
*
* @param preset settings for the naming convention
*/
function withNaming(preset) {
	var nameSpace = preset.n || "";
	var modValueDelimiter = preset.v || preset.m;
	function stringify(b, e, m, mix) {
		var entityName = e ? nameSpace + b + preset.e + e : nameSpace + b;
		var className = entityName;
		if (m) {
			var modPrefix = " " + className + preset.m;
			for (var k in m) if (m.hasOwnProperty(k)) {
				var modVal = m[k];
				if (modVal === true) className += modPrefix + k;
				else if (modVal) className += modPrefix + k + modValueDelimiter + modVal;
			}
		}
		if (mix !== void 0) {
			mix = Array.isArray(mix) ? mix : [mix];
			for (var i = 0, len = mix.length; i < len; i++) {
				var value = mix[i];
				if (!value || typeof value.valueOf() !== "string") continue;
				var mixes = value.valueOf().split(" ");
				for (var j = 0; j < mixes.length; j++) {
					var val = mixes[j];
					if (val !== entityName) className += " " + val;
				}
			}
		}
		return className;
	}
	return function cnGenerator(b, e) {
		return function(elemOrMods, elemModsOrBlockMix, elemMix) {
			if (typeof elemOrMods === "string") {
				if (typeof elemModsOrBlockMix === "string" || Array.isArray(elemModsOrBlockMix)) return stringify(b, elemOrMods, void 0, elemModsOrBlockMix);
				return stringify(b, elemOrMods, elemModsOrBlockMix, elemMix);
			}
			return stringify(b, e, elemOrMods, elemModsOrBlockMix);
		};
	};
}
withNaming({
	e: "-",
	m: "_"
});
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_trimmedEndIndex.js
var require__trimmedEndIndex = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/** Used to match a single whitespace character. */
	var reWhitespace = /\s/;
	/**
	* Used by `_.trim` and `_.trimEnd` to get the index of the last non-whitespace
	* character of `string`.
	*
	* @private
	* @param {string} string The string to inspect.
	* @returns {number} Returns the index of the last non-whitespace character.
	*/
	function trimmedEndIndex(string) {
		var index = string.length;
		while (index-- && reWhitespace.test(string.charAt(index)));
		return index;
	}
	module.exports = trimmedEndIndex;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseTrim.js
var require__baseTrim = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var trimmedEndIndex = require__trimmedEndIndex();
	/** Used to match leading whitespace. */
	var reTrimStart = /^\s+/;
	/**
	* The base implementation of `_.trim`.
	*
	* @private
	* @param {string} string The string to trim.
	* @returns {string} Returns the trimmed string.
	*/
	function baseTrim(string) {
		return string ? string.slice(0, trimmedEndIndex(string) + 1).replace(reTrimStart, "") : string;
	}
	module.exports = baseTrim;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/toNumber.js
var require_toNumber = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseTrim = require__baseTrim();
	var isObject = require_isObject();
	var isSymbol = require_isSymbol();
	/** Used as references for various `Number` constants. */
	var NAN = NaN;
	/** Used to detect bad signed hexadecimal string values. */
	var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;
	/** Used to detect binary string values. */
	var reIsBinary = /^0b[01]+$/i;
	/** Used to detect octal string values. */
	var reIsOctal = /^0o[0-7]+$/i;
	/** Built-in method references without a dependency on `root`. */
	var freeParseInt = parseInt;
	/**
	* Converts `value` to a number.
	*
	* @static
	* @memberOf _
	* @since 4.0.0
	* @category Lang
	* @param {*} value The value to process.
	* @returns {number} Returns the number.
	* @example
	*
	* _.toNumber(3.2);
	* // => 3.2
	*
	* _.toNumber(Number.MIN_VALUE);
	* // => 5e-324
	*
	* _.toNumber(Infinity);
	* // => Infinity
	*
	* _.toNumber('3.2');
	* // => 3.2
	*/
	function toNumber(value) {
		if (typeof value == "number") return value;
		if (isSymbol(value)) return NAN;
		if (isObject(value)) {
			var other = typeof value.valueOf == "function" ? value.valueOf() : value;
			value = isObject(other) ? other + "" : other;
		}
		if (typeof value != "string") return value === 0 ? value : +value;
		value = baseTrim(value);
		var isBinary = reIsBinary.test(value);
		return isBinary || reIsOctal.test(value) ? freeParseInt(value.slice(2), isBinary ? 2 : 8) : reIsBadHex.test(value) ? NAN : +value;
	}
	module.exports = toNumber;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseSlice.js
var require__baseSlice = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* The base implementation of `_.slice` without an iteratee call guard.
	*
	* @private
	* @param {Array} array The array to slice.
	* @param {number} [start=0] The start position.
	* @param {number} [end=array.length] The end position.
	* @returns {Array} Returns the slice of `array`.
	*/
	function baseSlice(array, start, end) {
		var index = -1, length = array.length;
		if (start < 0) start = -start > length ? 0 : length + start;
		end = end > length ? length : end;
		if (end < 0) end += length;
		length = start > end ? 0 : end - start >>> 0;
		start >>>= 0;
		var result = Array(length);
		while (++index < length) result[index] = array[index + start];
		return result;
	}
	module.exports = baseSlice;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_castSlice.js
var require__castSlice = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseSlice = require__baseSlice();
	/**
	* Casts `array` to a slice if it's needed.
	*
	* @private
	* @param {Array} array The array to inspect.
	* @param {number} start The start position.
	* @param {number} [end=array.length] The end position.
	* @returns {Array} Returns the cast slice.
	*/
	function castSlice(array, start, end) {
		var length = array.length;
		end = end === void 0 ? length : end;
		return !start && end >= length ? array : baseSlice(array, start, end);
	}
	module.exports = castSlice;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_hasUnicode.js
var require__hasUnicode = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/** Used to detect strings with [zero-width joiners or code points from the astral planes](http://eev.ee/blog/2015/09/12/dark-corners-of-unicode/). */
	var reHasUnicode = RegExp("[\\u200d\\ud800-\\udfff\\u0300-\\u036f\\ufe20-\\ufe2f\\u20d0-\\u20ff\\ufe0e\\ufe0f]");
	/**
	* Checks if `string` contains Unicode symbols.
	*
	* @private
	* @param {string} string The string to inspect.
	* @returns {boolean} Returns `true` if a symbol is found, else `false`.
	*/
	function hasUnicode(string) {
		return reHasUnicode.test(string);
	}
	module.exports = hasUnicode;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_asciiToArray.js
var require__asciiToArray = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Converts an ASCII `string` to an array.
	*
	* @private
	* @param {string} string The string to convert.
	* @returns {Array} Returns the converted array.
	*/
	function asciiToArray(string) {
		return string.split("");
	}
	module.exports = asciiToArray;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_unicodeToArray.js
var require__unicodeToArray = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/** Used to compose unicode character classes. */
	var rsAstralRange = "\\ud800-\\udfff";
	var rsComboRange = "\\u0300-\\u036f\\ufe20-\\ufe2f\\u20d0-\\u20ff";
	var rsVarRange = "\\ufe0e\\ufe0f";
	/** Used to compose unicode capture groups. */
	var rsAstral = "[" + rsAstralRange + "]";
	var rsCombo = "[" + rsComboRange + "]";
	var rsFitz = "\\ud83c[\\udffb-\\udfff]";
	var rsModifier = "(?:" + rsCombo + "|" + rsFitz + ")";
	var rsNonAstral = "[^" + rsAstralRange + "]";
	var rsRegional = "(?:\\ud83c[\\udde6-\\uddff]){2}";
	var rsSurrPair = "[\\ud800-\\udbff][\\udc00-\\udfff]";
	var rsZWJ = "\\u200d";
	/** Used to compose unicode regexes. */
	var reOptMod = rsModifier + "?";
	var rsOptVar = "[" + rsVarRange + "]?";
	var rsOptJoin = "(?:" + rsZWJ + "(?:" + [
		rsNonAstral,
		rsRegional,
		rsSurrPair
	].join("|") + ")" + rsOptVar + reOptMod + ")*";
	var rsSeq = rsOptVar + reOptMod + rsOptJoin;
	var rsSymbol = "(?:" + [
		rsNonAstral + rsCombo + "?",
		rsCombo,
		rsRegional,
		rsSurrPair,
		rsAstral
	].join("|") + ")";
	/** Used to match [string symbols](https://mathiasbynens.be/notes/javascript-unicode). */
	var reUnicode = RegExp(rsFitz + "(?=" + rsFitz + ")|" + rsSymbol + rsSeq, "g");
	/**
	* Converts a Unicode `string` to an array.
	*
	* @private
	* @param {string} string The string to convert.
	* @returns {Array} Returns the converted array.
	*/
	function unicodeToArray(string) {
		return string.match(reUnicode) || [];
	}
	module.exports = unicodeToArray;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_stringToArray.js
var require__stringToArray = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var asciiToArray = require__asciiToArray();
	var hasUnicode = require__hasUnicode();
	var unicodeToArray = require__unicodeToArray();
	/**
	* Converts `string` to an array.
	*
	* @private
	* @param {string} string The string to convert.
	* @returns {Array} Returns the converted array.
	*/
	function stringToArray(string) {
		return hasUnicode(string) ? unicodeToArray(string) : asciiToArray(string);
	}
	module.exports = stringToArray;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_createCaseFirst.js
var require__createCaseFirst = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var castSlice = require__castSlice();
	var hasUnicode = require__hasUnicode();
	var stringToArray = require__stringToArray();
	var toString = require_toString();
	/**
	* Creates a function like `_.lowerFirst`.
	*
	* @private
	* @param {string} methodName The name of the `String` case method to use.
	* @returns {Function} Returns the new case function.
	*/
	function createCaseFirst(methodName) {
		return function(string) {
			string = toString(string);
			var strSymbols = hasUnicode(string) ? stringToArray(string) : void 0;
			var chr = strSymbols ? strSymbols[0] : string.charAt(0);
			var trailing = strSymbols ? castSlice(strSymbols, 1).join("") : string.slice(1);
			return chr[methodName]() + trailing;
		};
	}
	module.exports = createCaseFirst;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/upperFirst.js
var require_upperFirst = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__createCaseFirst()("toUpperCase");
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/capitalize.js
var require_capitalize = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var toString = require_toString();
	var upperFirst = require_upperFirst();
	/**
	* Converts the first character of `string` to upper case and the remaining
	* to lower case.
	*
	* @static
	* @memberOf _
	* @since 3.0.0
	* @category String
	* @param {string} [string=''] The string to capitalize.
	* @returns {string} Returns the capitalized string.
	* @example
	*
	* _.capitalize('FRED');
	* // => 'Fred'
	*/
	function capitalize(string) {
		return upperFirst(toString(string).toLowerCase());
	}
	module.exports = capitalize;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_arrayPush.js
var require__arrayPush = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Appends the elements of `values` to `array`.
	*
	* @private
	* @param {Array} array The array to modify.
	* @param {Array} values The values to append.
	* @returns {Array} Returns `array`.
	*/
	function arrayPush(array, values) {
		var index = -1, length = values.length, offset = array.length;
		while (++index < length) array[offset + index] = values[index];
		return array;
	}
	module.exports = arrayPush;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseIsArguments.js
var require__baseIsArguments = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseGetTag = require__baseGetTag();
	var isObjectLike = require_isObjectLike();
	/** `Object#toString` result references. */
	var argsTag = "[object Arguments]";
	/**
	* The base implementation of `_.isArguments`.
	*
	* @private
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is an `arguments` object,
	*/
	function baseIsArguments(value) {
		return isObjectLike(value) && baseGetTag(value) == argsTag;
	}
	module.exports = baseIsArguments;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isArguments.js
var require_isArguments = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseIsArguments = require__baseIsArguments();
	var isObjectLike = require_isObjectLike();
	/** Used for built-in method references. */
	var objectProto = Object.prototype;
	/** Used to check objects for own properties. */
	var hasOwnProperty = objectProto.hasOwnProperty;
	/** Built-in value references. */
	var propertyIsEnumerable = objectProto.propertyIsEnumerable;
	module.exports = baseIsArguments(function() {
		return arguments;
	}()) ? baseIsArguments : function(value) {
		return isObjectLike(value) && hasOwnProperty.call(value, "callee") && !propertyIsEnumerable.call(value, "callee");
	};
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_isFlattenable.js
var require__isFlattenable = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Symbol = require__Symbol();
	var isArguments = require_isArguments();
	var isArray = require_isArray();
	/** Built-in value references. */
	var spreadableSymbol = Symbol ? Symbol.isConcatSpreadable : void 0;
	/**
	* Checks if `value` is a flattenable `arguments` object or array.
	*
	* @private
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is flattenable, else `false`.
	*/
	function isFlattenable(value) {
		return isArray(value) || isArguments(value) || !!(spreadableSymbol && value && value[spreadableSymbol]);
	}
	module.exports = isFlattenable;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseFlatten.js
var require__baseFlatten = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var arrayPush = require__arrayPush();
	var isFlattenable = require__isFlattenable();
	/**
	* The base implementation of `_.flatten` with support for restricting flattening.
	*
	* @private
	* @param {Array} array The array to flatten.
	* @param {number} depth The maximum recursion depth.
	* @param {boolean} [predicate=isFlattenable] The function invoked per iteration.
	* @param {boolean} [isStrict] Restrict to values that pass `predicate` checks.
	* @param {Array} [result=[]] The initial result value.
	* @returns {Array} Returns the new flattened array.
	*/
	function baseFlatten(array, depth, predicate, isStrict, result) {
		var index = -1, length = array.length;
		predicate || (predicate = isFlattenable);
		result || (result = []);
		while (++index < length) {
			var value = array[index];
			if (depth > 0 && predicate(value)) if (depth > 1) baseFlatten(value, depth - 1, predicate, isStrict, result);
			else arrayPush(result, value);
			else if (!isStrict) result[result.length] = value;
		}
		return result;
	}
	module.exports = baseFlatten;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_stackClear.js
var require__stackClear = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var ListCache = require__ListCache();
	/**
	* Removes all key-value entries from the stack.
	*
	* @private
	* @name clear
	* @memberOf Stack
	*/
	function stackClear() {
		this.__data__ = new ListCache();
		this.size = 0;
	}
	module.exports = stackClear;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_stackDelete.js
var require__stackDelete = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Removes `key` and its value from the stack.
	*
	* @private
	* @name delete
	* @memberOf Stack
	* @param {string} key The key of the value to remove.
	* @returns {boolean} Returns `true` if the entry was removed, else `false`.
	*/
	function stackDelete(key) {
		var data = this.__data__, result = data["delete"](key);
		this.size = data.size;
		return result;
	}
	module.exports = stackDelete;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_stackGet.js
var require__stackGet = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Gets the stack value for `key`.
	*
	* @private
	* @name get
	* @memberOf Stack
	* @param {string} key The key of the value to get.
	* @returns {*} Returns the entry value.
	*/
	function stackGet(key) {
		return this.__data__.get(key);
	}
	module.exports = stackGet;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_stackHas.js
var require__stackHas = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Checks if a stack value for `key` exists.
	*
	* @private
	* @name has
	* @memberOf Stack
	* @param {string} key The key of the entry to check.
	* @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
	*/
	function stackHas(key) {
		return this.__data__.has(key);
	}
	module.exports = stackHas;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_stackSet.js
var require__stackSet = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var ListCache = require__ListCache();
	var Map = require__Map();
	var MapCache = require__MapCache();
	/** Used as the size to enable large array optimizations. */
	var LARGE_ARRAY_SIZE = 200;
	/**
	* Sets the stack `key` to `value`.
	*
	* @private
	* @name set
	* @memberOf Stack
	* @param {string} key The key of the value to set.
	* @param {*} value The value to set.
	* @returns {Object} Returns the stack cache instance.
	*/
	function stackSet(key, value) {
		var data = this.__data__;
		if (data instanceof ListCache) {
			var pairs = data.__data__;
			if (!Map || pairs.length < LARGE_ARRAY_SIZE - 1) {
				pairs.push([key, value]);
				this.size = ++data.size;
				return this;
			}
			data = this.__data__ = new MapCache(pairs);
		}
		data.set(key, value);
		this.size = data.size;
		return this;
	}
	module.exports = stackSet;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_Stack.js
var require__Stack = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var ListCache = require__ListCache();
	var stackClear = require__stackClear();
	var stackDelete = require__stackDelete();
	var stackGet = require__stackGet();
	var stackHas = require__stackHas();
	var stackSet = require__stackSet();
	/**
	* Creates a stack cache object to store key-value pairs.
	*
	* @private
	* @constructor
	* @param {Array} [entries] The key-value pairs to cache.
	*/
	function Stack(entries) {
		var data = this.__data__ = new ListCache(entries);
		this.size = data.size;
	}
	Stack.prototype.clear = stackClear;
	Stack.prototype["delete"] = stackDelete;
	Stack.prototype.get = stackGet;
	Stack.prototype.has = stackHas;
	Stack.prototype.set = stackSet;
	module.exports = Stack;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_setCacheAdd.js
var require__setCacheAdd = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/** Used to stand-in for `undefined` hash values. */
	var HASH_UNDEFINED = "__lodash_hash_undefined__";
	/**
	* Adds `value` to the array cache.
	*
	* @private
	* @name add
	* @memberOf SetCache
	* @alias push
	* @param {*} value The value to cache.
	* @returns {Object} Returns the cache instance.
	*/
	function setCacheAdd(value) {
		this.__data__.set(value, HASH_UNDEFINED);
		return this;
	}
	module.exports = setCacheAdd;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_setCacheHas.js
var require__setCacheHas = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Checks if `value` is in the array cache.
	*
	* @private
	* @name has
	* @memberOf SetCache
	* @param {*} value The value to search for.
	* @returns {boolean} Returns `true` if `value` is found, else `false`.
	*/
	function setCacheHas(value) {
		return this.__data__.has(value);
	}
	module.exports = setCacheHas;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_SetCache.js
var require__SetCache = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var MapCache = require__MapCache();
	var setCacheAdd = require__setCacheAdd();
	var setCacheHas = require__setCacheHas();
	/**
	*
	* Creates an array cache object to store unique values.
	*
	* @private
	* @constructor
	* @param {Array} [values] The values to cache.
	*/
	function SetCache(values) {
		var index = -1, length = values == null ? 0 : values.length;
		this.__data__ = new MapCache();
		while (++index < length) this.add(values[index]);
	}
	SetCache.prototype.add = SetCache.prototype.push = setCacheAdd;
	SetCache.prototype.has = setCacheHas;
	module.exports = SetCache;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_arraySome.js
var require__arraySome = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* A specialized version of `_.some` for arrays without support for iteratee
	* shorthands.
	*
	* @private
	* @param {Array} [array] The array to iterate over.
	* @param {Function} predicate The function invoked per iteration.
	* @returns {boolean} Returns `true` if any element passes the predicate check,
	*  else `false`.
	*/
	function arraySome(array, predicate) {
		var index = -1, length = array == null ? 0 : array.length;
		while (++index < length) if (predicate(array[index], index, array)) return true;
		return false;
	}
	module.exports = arraySome;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_cacheHas.js
var require__cacheHas = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Checks if a `cache` value for `key` exists.
	*
	* @private
	* @param {Object} cache The cache to query.
	* @param {string} key The key of the entry to check.
	* @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
	*/
	function cacheHas(cache, key) {
		return cache.has(key);
	}
	module.exports = cacheHas;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_equalArrays.js
var require__equalArrays = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SetCache = require__SetCache();
	var arraySome = require__arraySome();
	var cacheHas = require__cacheHas();
	/** Used to compose bitmasks for value comparisons. */
	var COMPARE_PARTIAL_FLAG = 1;
	var COMPARE_UNORDERED_FLAG = 2;
	/**
	* A specialized version of `baseIsEqualDeep` for arrays with support for
	* partial deep comparisons.
	*
	* @private
	* @param {Array} array The array to compare.
	* @param {Array} other The other array to compare.
	* @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
	* @param {Function} customizer The function to customize comparisons.
	* @param {Function} equalFunc The function to determine equivalents of values.
	* @param {Object} stack Tracks traversed `array` and `other` objects.
	* @returns {boolean} Returns `true` if the arrays are equivalent, else `false`.
	*/
	function equalArrays(array, other, bitmask, customizer, equalFunc, stack) {
		var isPartial = bitmask & COMPARE_PARTIAL_FLAG, arrLength = array.length, othLength = other.length;
		if (arrLength != othLength && !(isPartial && othLength > arrLength)) return false;
		var arrStacked = stack.get(array);
		var othStacked = stack.get(other);
		if (arrStacked && othStacked) return arrStacked == other && othStacked == array;
		var index = -1, result = true, seen = bitmask & COMPARE_UNORDERED_FLAG ? new SetCache() : void 0;
		stack.set(array, other);
		stack.set(other, array);
		while (++index < arrLength) {
			var arrValue = array[index], othValue = other[index];
			if (customizer) var compared = isPartial ? customizer(othValue, arrValue, index, other, array, stack) : customizer(arrValue, othValue, index, array, other, stack);
			if (compared !== void 0) {
				if (compared) continue;
				result = false;
				break;
			}
			if (seen) {
				if (!arraySome(other, function(othValue, othIndex) {
					if (!cacheHas(seen, othIndex) && (arrValue === othValue || equalFunc(arrValue, othValue, bitmask, customizer, stack))) return seen.push(othIndex);
				})) {
					result = false;
					break;
				}
			} else if (!(arrValue === othValue || equalFunc(arrValue, othValue, bitmask, customizer, stack))) {
				result = false;
				break;
			}
		}
		stack["delete"](array);
		stack["delete"](other);
		return result;
	}
	module.exports = equalArrays;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_Uint8Array.js
var require__Uint8Array = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__root().Uint8Array;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_mapToArray.js
var require__mapToArray = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Converts `map` to its key-value pairs.
	*
	* @private
	* @param {Object} map The map to convert.
	* @returns {Array} Returns the key-value pairs.
	*/
	function mapToArray(map) {
		var index = -1, result = Array(map.size);
		map.forEach(function(value, key) {
			result[++index] = [key, value];
		});
		return result;
	}
	module.exports = mapToArray;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_setToArray.js
var require__setToArray = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Converts `set` to an array of its values.
	*
	* @private
	* @param {Object} set The set to convert.
	* @returns {Array} Returns the values.
	*/
	function setToArray(set) {
		var index = -1, result = Array(set.size);
		set.forEach(function(value) {
			result[++index] = value;
		});
		return result;
	}
	module.exports = setToArray;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_equalByTag.js
var require__equalByTag = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Symbol = require__Symbol();
	var Uint8Array = require__Uint8Array();
	var eq = require_eq();
	var equalArrays = require__equalArrays();
	var mapToArray = require__mapToArray();
	var setToArray = require__setToArray();
	/** Used to compose bitmasks for value comparisons. */
	var COMPARE_PARTIAL_FLAG = 1;
	var COMPARE_UNORDERED_FLAG = 2;
	/** `Object#toString` result references. */
	var boolTag = "[object Boolean]";
	var dateTag = "[object Date]";
	var errorTag = "[object Error]";
	var mapTag = "[object Map]";
	var numberTag = "[object Number]";
	var regexpTag = "[object RegExp]";
	var setTag = "[object Set]";
	var stringTag = "[object String]";
	var symbolTag = "[object Symbol]";
	var arrayBufferTag = "[object ArrayBuffer]";
	var dataViewTag = "[object DataView]";
	/** Used to convert symbols to primitives and strings. */
	var symbolProto = Symbol ? Symbol.prototype : void 0;
	var symbolValueOf = symbolProto ? symbolProto.valueOf : void 0;
	/**
	* A specialized version of `baseIsEqualDeep` for comparing objects of
	* the same `toStringTag`.
	*
	* **Note:** This function only supports comparing values with tags of
	* `Boolean`, `Date`, `Error`, `Number`, `RegExp`, or `String`.
	*
	* @private
	* @param {Object} object The object to compare.
	* @param {Object} other The other object to compare.
	* @param {string} tag The `toStringTag` of the objects to compare.
	* @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
	* @param {Function} customizer The function to customize comparisons.
	* @param {Function} equalFunc The function to determine equivalents of values.
	* @param {Object} stack Tracks traversed `object` and `other` objects.
	* @returns {boolean} Returns `true` if the objects are equivalent, else `false`.
	*/
	function equalByTag(object, other, tag, bitmask, customizer, equalFunc, stack) {
		switch (tag) {
			case dataViewTag:
				if (object.byteLength != other.byteLength || object.byteOffset != other.byteOffset) return false;
				object = object.buffer;
				other = other.buffer;
			case arrayBufferTag:
				if (object.byteLength != other.byteLength || !equalFunc(new Uint8Array(object), new Uint8Array(other))) return false;
				return true;
			case boolTag:
			case dateTag:
			case numberTag: return eq(+object, +other);
			case errorTag: return object.name == other.name && object.message == other.message;
			case regexpTag:
			case stringTag: return object == other + "";
			case mapTag: var convert = mapToArray;
			case setTag:
				var isPartial = bitmask & COMPARE_PARTIAL_FLAG;
				convert || (convert = setToArray);
				if (object.size != other.size && !isPartial) return false;
				var stacked = stack.get(object);
				if (stacked) return stacked == other;
				bitmask |= COMPARE_UNORDERED_FLAG;
				stack.set(object, other);
				var result = equalArrays(convert(object), convert(other), bitmask, customizer, equalFunc, stack);
				stack["delete"](object);
				return result;
			case symbolTag: if (symbolValueOf) return symbolValueOf.call(object) == symbolValueOf.call(other);
		}
		return false;
	}
	module.exports = equalByTag;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseGetAllKeys.js
var require__baseGetAllKeys = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var arrayPush = require__arrayPush();
	var isArray = require_isArray();
	/**
	* The base implementation of `getAllKeys` and `getAllKeysIn` which uses
	* `keysFunc` and `symbolsFunc` to get the enumerable property names and
	* symbols of `object`.
	*
	* @private
	* @param {Object} object The object to query.
	* @param {Function} keysFunc The function to get the keys of `object`.
	* @param {Function} symbolsFunc The function to get the symbols of `object`.
	* @returns {Array} Returns the array of property names and symbols.
	*/
	function baseGetAllKeys(object, keysFunc, symbolsFunc) {
		var result = keysFunc(object);
		return isArray(object) ? result : arrayPush(result, symbolsFunc(object));
	}
	module.exports = baseGetAllKeys;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_arrayFilter.js
var require__arrayFilter = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* A specialized version of `_.filter` for arrays without support for
	* iteratee shorthands.
	*
	* @private
	* @param {Array} [array] The array to iterate over.
	* @param {Function} predicate The function invoked per iteration.
	* @returns {Array} Returns the new filtered array.
	*/
	function arrayFilter(array, predicate) {
		var index = -1, length = array == null ? 0 : array.length, resIndex = 0, result = [];
		while (++index < length) {
			var value = array[index];
			if (predicate(value, index, array)) result[resIndex++] = value;
		}
		return result;
	}
	module.exports = arrayFilter;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/stubArray.js
var require_stubArray = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* This method returns a new empty array.
	*
	* @static
	* @memberOf _
	* @since 4.13.0
	* @category Util
	* @returns {Array} Returns the new empty array.
	* @example
	*
	* var arrays = _.times(2, _.stubArray);
	*
	* console.log(arrays);
	* // => [[], []]
	*
	* console.log(arrays[0] === arrays[1]);
	* // => false
	*/
	function stubArray() {
		return [];
	}
	module.exports = stubArray;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_getSymbols.js
var require__getSymbols = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var arrayFilter = require__arrayFilter();
	var stubArray = require_stubArray();
	/** Built-in value references. */
	var propertyIsEnumerable = Object.prototype.propertyIsEnumerable;
	var nativeGetSymbols = Object.getOwnPropertySymbols;
	module.exports = !nativeGetSymbols ? stubArray : function(object) {
		if (object == null) return [];
		object = Object(object);
		return arrayFilter(nativeGetSymbols(object), function(symbol) {
			return propertyIsEnumerable.call(object, symbol);
		});
	};
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseTimes.js
var require__baseTimes = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* The base implementation of `_.times` without support for iteratee shorthands
	* or max array length checks.
	*
	* @private
	* @param {number} n The number of times to invoke `iteratee`.
	* @param {Function} iteratee The function invoked per iteration.
	* @returns {Array} Returns the array of results.
	*/
	function baseTimes(n, iteratee) {
		var index = -1, result = Array(n);
		while (++index < n) result[index] = iteratee(index);
		return result;
	}
	module.exports = baseTimes;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/stubFalse.js
var require_stubFalse = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* This method returns `false`.
	*
	* @static
	* @memberOf _
	* @since 4.13.0
	* @category Util
	* @returns {boolean} Returns `false`.
	* @example
	*
	* _.times(2, _.stubFalse);
	* // => [false, false]
	*/
	function stubFalse() {
		return false;
	}
	module.exports = stubFalse;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isBuffer.js
var require_isBuffer = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var root = require__root();
	var stubFalse = require_stubFalse();
	/** Detect free variable `exports`. */
	var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
	/** Detect free variable `module`. */
	var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
	/** Built-in value references. */
	var Buffer = freeModule && freeModule.exports === freeExports ? root.Buffer : void 0;
	module.exports = (Buffer ? Buffer.isBuffer : void 0) || stubFalse;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_isIndex.js
var require__isIndex = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/** Used as references for various `Number` constants. */
	var MAX_SAFE_INTEGER = 9007199254740991;
	/** Used to detect unsigned integer values. */
	var reIsUint = /^(?:0|[1-9]\d*)$/;
	/**
	* Checks if `value` is a valid array-like index.
	*
	* @private
	* @param {*} value The value to check.
	* @param {number} [length=MAX_SAFE_INTEGER] The upper bounds of a valid index.
	* @returns {boolean} Returns `true` if `value` is a valid index, else `false`.
	*/
	function isIndex(value, length) {
		var type = typeof value;
		length = length == null ? MAX_SAFE_INTEGER : length;
		return !!length && (type == "number" || type != "symbol" && reIsUint.test(value)) && value > -1 && value % 1 == 0 && value < length;
	}
	module.exports = isIndex;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isLength.js
var require_isLength = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/** Used as references for various `Number` constants. */
	var MAX_SAFE_INTEGER = 9007199254740991;
	/**
	* Checks if `value` is a valid array-like length.
	*
	* **Note:** This method is loosely based on
	* [`ToLength`](http://ecma-international.org/ecma-262/7.0/#sec-tolength).
	*
	* @static
	* @memberOf _
	* @since 4.0.0
	* @category Lang
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is a valid length, else `false`.
	* @example
	*
	* _.isLength(3);
	* // => true
	*
	* _.isLength(Number.MIN_VALUE);
	* // => false
	*
	* _.isLength(Infinity);
	* // => false
	*
	* _.isLength('3');
	* // => false
	*/
	function isLength(value) {
		return typeof value == "number" && value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
	}
	module.exports = isLength;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseIsTypedArray.js
var require__baseIsTypedArray = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseGetTag = require__baseGetTag();
	var isLength = require_isLength();
	var isObjectLike = require_isObjectLike();
	/** `Object#toString` result references. */
	var argsTag = "[object Arguments]";
	var arrayTag = "[object Array]";
	var boolTag = "[object Boolean]";
	var dateTag = "[object Date]";
	var errorTag = "[object Error]";
	var funcTag = "[object Function]";
	var mapTag = "[object Map]";
	var numberTag = "[object Number]";
	var objectTag = "[object Object]";
	var regexpTag = "[object RegExp]";
	var setTag = "[object Set]";
	var stringTag = "[object String]";
	var weakMapTag = "[object WeakMap]";
	var arrayBufferTag = "[object ArrayBuffer]";
	var dataViewTag = "[object DataView]";
	var float32Tag = "[object Float32Array]";
	var float64Tag = "[object Float64Array]";
	var int8Tag = "[object Int8Array]";
	var int16Tag = "[object Int16Array]";
	var int32Tag = "[object Int32Array]";
	var uint8Tag = "[object Uint8Array]";
	var uint8ClampedTag = "[object Uint8ClampedArray]";
	var uint16Tag = "[object Uint16Array]";
	var uint32Tag = "[object Uint32Array]";
	/** Used to identify `toStringTag` values of typed arrays. */
	var typedArrayTags = {};
	typedArrayTags[float32Tag] = typedArrayTags[float64Tag] = typedArrayTags[int8Tag] = typedArrayTags[int16Tag] = typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] = typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] = typedArrayTags[uint32Tag] = true;
	typedArrayTags[argsTag] = typedArrayTags[arrayTag] = typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] = typedArrayTags[dataViewTag] = typedArrayTags[dateTag] = typedArrayTags[errorTag] = typedArrayTags[funcTag] = typedArrayTags[mapTag] = typedArrayTags[numberTag] = typedArrayTags[objectTag] = typedArrayTags[regexpTag] = typedArrayTags[setTag] = typedArrayTags[stringTag] = typedArrayTags[weakMapTag] = false;
	/**
	* The base implementation of `_.isTypedArray` without Node.js optimizations.
	*
	* @private
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is a typed array, else `false`.
	*/
	function baseIsTypedArray(value) {
		return isObjectLike(value) && isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
	}
	module.exports = baseIsTypedArray;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseUnary.js
var require__baseUnary = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* The base implementation of `_.unary` without support for storing metadata.
	*
	* @private
	* @param {Function} func The function to cap arguments for.
	* @returns {Function} Returns the new capped function.
	*/
	function baseUnary(func) {
		return function(value) {
			return func(value);
		};
	}
	module.exports = baseUnary;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_nodeUtil.js
var require__nodeUtil = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var freeGlobal = require__freeGlobal();
	/** Detect free variable `exports`. */
	var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
	/** Detect free variable `module`. */
	var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
	/** Detect free variable `process` from Node.js. */
	var freeProcess = freeModule && freeModule.exports === freeExports && freeGlobal.process;
	module.exports = function() {
		try {
			var types = freeModule && freeModule.require && freeModule.require("util").types;
			if (types) return types;
			return freeProcess && freeProcess.binding && freeProcess.binding("util");
		} catch (e) {}
	}();
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isTypedArray.js
var require_isTypedArray = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseIsTypedArray = require__baseIsTypedArray();
	var baseUnary = require__baseUnary();
	var nodeUtil = require__nodeUtil();
	var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;
	module.exports = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_arrayLikeKeys.js
var require__arrayLikeKeys = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseTimes = require__baseTimes();
	var isArguments = require_isArguments();
	var isArray = require_isArray();
	var isBuffer = require_isBuffer();
	var isIndex = require__isIndex();
	var isTypedArray = require_isTypedArray();
	/** Used to check objects for own properties. */
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	/**
	* Creates an array of the enumerable property names of the array-like `value`.
	*
	* @private
	* @param {*} value The value to query.
	* @param {boolean} inherited Specify returning inherited property names.
	* @returns {Array} Returns the array of property names.
	*/
	function arrayLikeKeys(value, inherited) {
		var isArr = isArray(value), isArg = !isArr && isArguments(value), isBuff = !isArr && !isArg && isBuffer(value), isType = !isArr && !isArg && !isBuff && isTypedArray(value), skipIndexes = isArr || isArg || isBuff || isType, result = skipIndexes ? baseTimes(value.length, String) : [], length = result.length;
		for (var key in value) if ((inherited || hasOwnProperty.call(value, key)) && !(skipIndexes && (key == "length" || isBuff && (key == "offset" || key == "parent") || isType && (key == "buffer" || key == "byteLength" || key == "byteOffset") || isIndex(key, length)))) result.push(key);
		return result;
	}
	module.exports = arrayLikeKeys;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_isPrototype.js
var require__isPrototype = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/** Used for built-in method references. */
	var objectProto = Object.prototype;
	/**
	* Checks if `value` is likely a prototype object.
	*
	* @private
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is a prototype, else `false`.
	*/
	function isPrototype(value) {
		var Ctor = value && value.constructor;
		return value === (typeof Ctor == "function" && Ctor.prototype || objectProto);
	}
	module.exports = isPrototype;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_overArg.js
var require__overArg = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Creates a unary function that invokes `func` with its argument transformed.
	*
	* @private
	* @param {Function} func The function to wrap.
	* @param {Function} transform The argument transform.
	* @returns {Function} Returns the new function.
	*/
	function overArg(func, transform) {
		return function(arg) {
			return func(transform(arg));
		};
	}
	module.exports = overArg;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_nativeKeys.js
var require__nativeKeys = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__overArg()(Object.keys, Object);
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseKeys.js
var require__baseKeys = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototype = require__isPrototype();
	var nativeKeys = require__nativeKeys();
	/** Used to check objects for own properties. */
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	/**
	* The base implementation of `_.keys` which doesn't treat sparse arrays as dense.
	*
	* @private
	* @param {Object} object The object to query.
	* @returns {Array} Returns the array of property names.
	*/
	function baseKeys(object) {
		if (!isPrototype(object)) return nativeKeys(object);
		var result = [];
		for (var key in Object(object)) if (hasOwnProperty.call(object, key) && key != "constructor") result.push(key);
		return result;
	}
	module.exports = baseKeys;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isArrayLike.js
var require_isArrayLike = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isFunction = require_isFunction();
	var isLength = require_isLength();
	/**
	* Checks if `value` is array-like. A value is considered array-like if it's
	* not a function and has a `value.length` that's an integer greater than or
	* equal to `0` and less than or equal to `Number.MAX_SAFE_INTEGER`.
	*
	* @static
	* @memberOf _
	* @since 4.0.0
	* @category Lang
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is array-like, else `false`.
	* @example
	*
	* _.isArrayLike([1, 2, 3]);
	* // => true
	*
	* _.isArrayLike(document.body.children);
	* // => true
	*
	* _.isArrayLike('abc');
	* // => true
	*
	* _.isArrayLike(_.noop);
	* // => false
	*/
	function isArrayLike(value) {
		return value != null && isLength(value.length) && !isFunction(value);
	}
	module.exports = isArrayLike;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/keys.js
var require_keys = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var arrayLikeKeys = require__arrayLikeKeys();
	var baseKeys = require__baseKeys();
	var isArrayLike = require_isArrayLike();
	/**
	* Creates an array of the own enumerable property names of `object`.
	*
	* **Note:** Non-object values are coerced to objects. See the
	* [ES spec](http://ecma-international.org/ecma-262/7.0/#sec-object.keys)
	* for more details.
	*
	* @static
	* @since 0.1.0
	* @memberOf _
	* @category Object
	* @param {Object} object The object to query.
	* @returns {Array} Returns the array of property names.
	* @example
	*
	* function Foo() {
	*   this.a = 1;
	*   this.b = 2;
	* }
	*
	* Foo.prototype.c = 3;
	*
	* _.keys(new Foo);
	* // => ['a', 'b'] (iteration order is not guaranteed)
	*
	* _.keys('hi');
	* // => ['0', '1']
	*/
	function keys(object) {
		return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
	}
	module.exports = keys;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_getAllKeys.js
var require__getAllKeys = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseGetAllKeys = require__baseGetAllKeys();
	var getSymbols = require__getSymbols();
	var keys = require_keys();
	/**
	* Creates an array of own enumerable property names and symbols of `object`.
	*
	* @private
	* @param {Object} object The object to query.
	* @returns {Array} Returns the array of property names and symbols.
	*/
	function getAllKeys(object) {
		return baseGetAllKeys(object, keys, getSymbols);
	}
	module.exports = getAllKeys;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_equalObjects.js
var require__equalObjects = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getAllKeys = require__getAllKeys();
	/** Used to compose bitmasks for value comparisons. */
	var COMPARE_PARTIAL_FLAG = 1;
	/** Used to check objects for own properties. */
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	/**
	* A specialized version of `baseIsEqualDeep` for objects with support for
	* partial deep comparisons.
	*
	* @private
	* @param {Object} object The object to compare.
	* @param {Object} other The other object to compare.
	* @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
	* @param {Function} customizer The function to customize comparisons.
	* @param {Function} equalFunc The function to determine equivalents of values.
	* @param {Object} stack Tracks traversed `object` and `other` objects.
	* @returns {boolean} Returns `true` if the objects are equivalent, else `false`.
	*/
	function equalObjects(object, other, bitmask, customizer, equalFunc, stack) {
		var isPartial = bitmask & COMPARE_PARTIAL_FLAG, objProps = getAllKeys(object), objLength = objProps.length;
		if (objLength != getAllKeys(other).length && !isPartial) return false;
		var index = objLength;
		while (index--) {
			var key = objProps[index];
			if (!(isPartial ? key in other : hasOwnProperty.call(other, key))) return false;
		}
		var objStacked = stack.get(object);
		var othStacked = stack.get(other);
		if (objStacked && othStacked) return objStacked == other && othStacked == object;
		var result = true;
		stack.set(object, other);
		stack.set(other, object);
		var skipCtor = isPartial;
		while (++index < objLength) {
			key = objProps[index];
			var objValue = object[key], othValue = other[key];
			if (customizer) var compared = isPartial ? customizer(othValue, objValue, key, other, object, stack) : customizer(objValue, othValue, key, object, other, stack);
			if (!(compared === void 0 ? objValue === othValue || equalFunc(objValue, othValue, bitmask, customizer, stack) : compared)) {
				result = false;
				break;
			}
			skipCtor || (skipCtor = key == "constructor");
		}
		if (result && !skipCtor) {
			var objCtor = object.constructor, othCtor = other.constructor;
			if (objCtor != othCtor && "constructor" in object && "constructor" in other && !(typeof objCtor == "function" && objCtor instanceof objCtor && typeof othCtor == "function" && othCtor instanceof othCtor)) result = false;
		}
		stack["delete"](object);
		stack["delete"](other);
		return result;
	}
	module.exports = equalObjects;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_DataView.js
var require__DataView = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__getNative()(require__root(), "DataView");
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_Promise.js
var require__Promise = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__getNative()(require__root(), "Promise");
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_Set.js
var require__Set = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__getNative()(require__root(), "Set");
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_WeakMap.js
var require__WeakMap = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__getNative()(require__root(), "WeakMap");
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_getTag.js
var require__getTag = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var DataView = require__DataView();
	var Map = require__Map();
	var Promise = require__Promise();
	var Set = require__Set();
	var WeakMap = require__WeakMap();
	var baseGetTag = require__baseGetTag();
	var toSource = require__toSource();
	/** `Object#toString` result references. */
	var mapTag = "[object Map]";
	var objectTag = "[object Object]";
	var promiseTag = "[object Promise]";
	var setTag = "[object Set]";
	var weakMapTag = "[object WeakMap]";
	var dataViewTag = "[object DataView]";
	/** Used to detect maps, sets, and weakmaps. */
	var dataViewCtorString = toSource(DataView);
	var mapCtorString = toSource(Map);
	var promiseCtorString = toSource(Promise);
	var setCtorString = toSource(Set);
	var weakMapCtorString = toSource(WeakMap);
	/**
	* Gets the `toStringTag` of `value`.
	*
	* @private
	* @param {*} value The value to query.
	* @returns {string} Returns the `toStringTag`.
	*/
	var getTag = baseGetTag;
	if (DataView && getTag(new DataView(/* @__PURE__ */ new ArrayBuffer(1))) != dataViewTag || Map && getTag(new Map()) != mapTag || Promise && getTag(Promise.resolve()) != promiseTag || Set && getTag(new Set()) != setTag || WeakMap && getTag(new WeakMap()) != weakMapTag) getTag = function(value) {
		var result = baseGetTag(value), Ctor = result == objectTag ? value.constructor : void 0, ctorString = Ctor ? toSource(Ctor) : "";
		if (ctorString) switch (ctorString) {
			case dataViewCtorString: return dataViewTag;
			case mapCtorString: return mapTag;
			case promiseCtorString: return promiseTag;
			case setCtorString: return setTag;
			case weakMapCtorString: return weakMapTag;
		}
		return result;
	};
	module.exports = getTag;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseIsEqualDeep.js
var require__baseIsEqualDeep = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Stack = require__Stack();
	var equalArrays = require__equalArrays();
	var equalByTag = require__equalByTag();
	var equalObjects = require__equalObjects();
	var getTag = require__getTag();
	var isArray = require_isArray();
	var isBuffer = require_isBuffer();
	var isTypedArray = require_isTypedArray();
	/** Used to compose bitmasks for value comparisons. */
	var COMPARE_PARTIAL_FLAG = 1;
	/** `Object#toString` result references. */
	var argsTag = "[object Arguments]";
	var arrayTag = "[object Array]";
	var objectTag = "[object Object]";
	/** Used to check objects for own properties. */
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	/**
	* A specialized version of `baseIsEqual` for arrays and objects which performs
	* deep comparisons and tracks traversed objects enabling objects with circular
	* references to be compared.
	*
	* @private
	* @param {Object} object The object to compare.
	* @param {Object} other The other object to compare.
	* @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
	* @param {Function} customizer The function to customize comparisons.
	* @param {Function} equalFunc The function to determine equivalents of values.
	* @param {Object} [stack] Tracks traversed `object` and `other` objects.
	* @returns {boolean} Returns `true` if the objects are equivalent, else `false`.
	*/
	function baseIsEqualDeep(object, other, bitmask, customizer, equalFunc, stack) {
		var objIsArr = isArray(object), othIsArr = isArray(other), objTag = objIsArr ? arrayTag : getTag(object), othTag = othIsArr ? arrayTag : getTag(other);
		objTag = objTag == argsTag ? objectTag : objTag;
		othTag = othTag == argsTag ? objectTag : othTag;
		var objIsObj = objTag == objectTag, othIsObj = othTag == objectTag, isSameTag = objTag == othTag;
		if (isSameTag && isBuffer(object)) {
			if (!isBuffer(other)) return false;
			objIsArr = true;
			objIsObj = false;
		}
		if (isSameTag && !objIsObj) {
			stack || (stack = new Stack());
			return objIsArr || isTypedArray(object) ? equalArrays(object, other, bitmask, customizer, equalFunc, stack) : equalByTag(object, other, objTag, bitmask, customizer, equalFunc, stack);
		}
		if (!(bitmask & COMPARE_PARTIAL_FLAG)) {
			var objIsWrapped = objIsObj && hasOwnProperty.call(object, "__wrapped__"), othIsWrapped = othIsObj && hasOwnProperty.call(other, "__wrapped__");
			if (objIsWrapped || othIsWrapped) {
				var objUnwrapped = objIsWrapped ? object.value() : object, othUnwrapped = othIsWrapped ? other.value() : other;
				stack || (stack = new Stack());
				return equalFunc(objUnwrapped, othUnwrapped, bitmask, customizer, stack);
			}
		}
		if (!isSameTag) return false;
		stack || (stack = new Stack());
		return equalObjects(object, other, bitmask, customizer, equalFunc, stack);
	}
	module.exports = baseIsEqualDeep;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseIsEqual.js
var require__baseIsEqual = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseIsEqualDeep = require__baseIsEqualDeep();
	var isObjectLike = require_isObjectLike();
	/**
	* The base implementation of `_.isEqual` which supports partial comparisons
	* and tracks traversed objects.
	*
	* @private
	* @param {*} value The value to compare.
	* @param {*} other The other value to compare.
	* @param {boolean} bitmask The bitmask flags.
	*  1 - Unordered comparison
	*  2 - Partial comparison
	* @param {Function} [customizer] The function to customize comparisons.
	* @param {Object} [stack] Tracks traversed `value` and `other` objects.
	* @returns {boolean} Returns `true` if the values are equivalent, else `false`.
	*/
	function baseIsEqual(value, other, bitmask, customizer, stack) {
		if (value === other) return true;
		if (value == null || other == null || !isObjectLike(value) && !isObjectLike(other)) return value !== value && other !== other;
		return baseIsEqualDeep(value, other, bitmask, customizer, baseIsEqual, stack);
	}
	module.exports = baseIsEqual;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseIsMatch.js
var require__baseIsMatch = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Stack = require__Stack();
	var baseIsEqual = require__baseIsEqual();
	/** Used to compose bitmasks for value comparisons. */
	var COMPARE_PARTIAL_FLAG = 1;
	var COMPARE_UNORDERED_FLAG = 2;
	/**
	* The base implementation of `_.isMatch` without support for iteratee shorthands.
	*
	* @private
	* @param {Object} object The object to inspect.
	* @param {Object} source The object of property values to match.
	* @param {Array} matchData The property names, values, and compare flags to match.
	* @param {Function} [customizer] The function to customize comparisons.
	* @returns {boolean} Returns `true` if `object` is a match, else `false`.
	*/
	function baseIsMatch(object, source, matchData, customizer) {
		var index = matchData.length, length = index, noCustomizer = !customizer;
		if (object == null) return !length;
		object = Object(object);
		while (index--) {
			var data = matchData[index];
			if (noCustomizer && data[2] ? data[1] !== object[data[0]] : !(data[0] in object)) return false;
		}
		while (++index < length) {
			data = matchData[index];
			var key = data[0], objValue = object[key], srcValue = data[1];
			if (noCustomizer && data[2]) {
				if (objValue === void 0 && !(key in object)) return false;
			} else {
				var stack = new Stack();
				if (customizer) var result = customizer(objValue, srcValue, key, object, source, stack);
				if (!(result === void 0 ? baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG, customizer, stack) : result)) return false;
			}
		}
		return true;
	}
	module.exports = baseIsMatch;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_isStrictComparable.js
var require__isStrictComparable = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isObject = require_isObject();
	/**
	* Checks if `value` is suitable for strict equality comparisons, i.e. `===`.
	*
	* @private
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` if suitable for strict
	*  equality comparisons, else `false`.
	*/
	function isStrictComparable(value) {
		return value === value && !isObject(value);
	}
	module.exports = isStrictComparable;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_getMatchData.js
var require__getMatchData = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isStrictComparable = require__isStrictComparable();
	var keys = require_keys();
	/**
	* Gets the property names, values, and compare flags of `object`.
	*
	* @private
	* @param {Object} object The object to query.
	* @returns {Array} Returns the match data of `object`.
	*/
	function getMatchData(object) {
		var result = keys(object), length = result.length;
		while (length--) {
			var key = result[length], value = object[key];
			result[length] = [
				key,
				value,
				isStrictComparable(value)
			];
		}
		return result;
	}
	module.exports = getMatchData;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_matchesStrictComparable.js
var require__matchesStrictComparable = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* A specialized version of `matchesProperty` for source values suitable
	* for strict equality comparisons, i.e. `===`.
	*
	* @private
	* @param {string} key The key of the property to get.
	* @param {*} srcValue The value to match.
	* @returns {Function} Returns the new spec function.
	*/
	function matchesStrictComparable(key, srcValue) {
		return function(object) {
			if (object == null) return false;
			return object[key] === srcValue && (srcValue !== void 0 || key in Object(object));
		};
	}
	module.exports = matchesStrictComparable;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseMatches.js
var require__baseMatches = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseIsMatch = require__baseIsMatch();
	var getMatchData = require__getMatchData();
	var matchesStrictComparable = require__matchesStrictComparable();
	/**
	* The base implementation of `_.matches` which doesn't clone `source`.
	*
	* @private
	* @param {Object} source The object of property values to match.
	* @returns {Function} Returns the new spec function.
	*/
	function baseMatches(source) {
		var matchData = getMatchData(source);
		if (matchData.length == 1 && matchData[0][2]) return matchesStrictComparable(matchData[0][0], matchData[0][1]);
		return function(object) {
			return object === source || baseIsMatch(object, source, matchData);
		};
	}
	module.exports = baseMatches;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseHasIn.js
var require__baseHasIn = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* The base implementation of `_.hasIn` without support for deep paths.
	*
	* @private
	* @param {Object} [object] The object to query.
	* @param {Array|string} key The key to check.
	* @returns {boolean} Returns `true` if `key` exists, else `false`.
	*/
	function baseHasIn(object, key) {
		return object != null && key in Object(object);
	}
	module.exports = baseHasIn;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_hasPath.js
var require__hasPath = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var castPath = require__castPath();
	var isArguments = require_isArguments();
	var isArray = require_isArray();
	var isIndex = require__isIndex();
	var isLength = require_isLength();
	var toKey = require__toKey();
	/**
	* Checks if `path` exists on `object`.
	*
	* @private
	* @param {Object} object The object to query.
	* @param {Array|string} path The path to check.
	* @param {Function} hasFunc The function to check properties.
	* @returns {boolean} Returns `true` if `path` exists, else `false`.
	*/
	function hasPath(object, path, hasFunc) {
		path = castPath(path, object);
		var index = -1, length = path.length, result = false;
		while (++index < length) {
			var key = toKey(path[index]);
			if (!(result = object != null && hasFunc(object, key))) break;
			object = object[key];
		}
		if (result || ++index != length) return result;
		length = object == null ? 0 : object.length;
		return !!length && isLength(length) && isIndex(key, length) && (isArray(object) || isArguments(object));
	}
	module.exports = hasPath;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/hasIn.js
var require_hasIn = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseHasIn = require__baseHasIn();
	var hasPath = require__hasPath();
	/**
	* Checks if `path` is a direct or inherited property of `object`.
	*
	* @static
	* @memberOf _
	* @since 4.0.0
	* @category Object
	* @param {Object} object The object to query.
	* @param {Array|string} path The path to check.
	* @returns {boolean} Returns `true` if `path` exists, else `false`.
	* @example
	*
	* var object = _.create({ 'a': _.create({ 'b': 2 }) });
	*
	* _.hasIn(object, 'a');
	* // => true
	*
	* _.hasIn(object, 'a.b');
	* // => true
	*
	* _.hasIn(object, ['a', 'b']);
	* // => true
	*
	* _.hasIn(object, 'b');
	* // => false
	*/
	function hasIn(object, path) {
		return object != null && hasPath(object, path, baseHasIn);
	}
	module.exports = hasIn;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseMatchesProperty.js
var require__baseMatchesProperty = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseIsEqual = require__baseIsEqual();
	var get = require_get();
	var hasIn = require_hasIn();
	var isKey = require__isKey();
	var isStrictComparable = require__isStrictComparable();
	var matchesStrictComparable = require__matchesStrictComparable();
	var toKey = require__toKey();
	/** Used to compose bitmasks for value comparisons. */
	var COMPARE_PARTIAL_FLAG = 1;
	var COMPARE_UNORDERED_FLAG = 2;
	/**
	* The base implementation of `_.matchesProperty` which doesn't clone `srcValue`.
	*
	* @private
	* @param {string} path The path of the property to get.
	* @param {*} srcValue The value to match.
	* @returns {Function} Returns the new spec function.
	*/
	function baseMatchesProperty(path, srcValue) {
		if (isKey(path) && isStrictComparable(srcValue)) return matchesStrictComparable(toKey(path), srcValue);
		return function(object) {
			var objValue = get(object, path);
			return objValue === void 0 && objValue === srcValue ? hasIn(object, path) : baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG);
		};
	}
	module.exports = baseMatchesProperty;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/identity.js
var require_identity = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* This method returns the first argument it receives.
	*
	* @static
	* @since 0.1.0
	* @memberOf _
	* @category Util
	* @param {*} value Any value.
	* @returns {*} Returns `value`.
	* @example
	*
	* var object = { 'a': 1 };
	*
	* console.log(_.identity(object) === object);
	* // => true
	*/
	function identity(value) {
		return value;
	}
	module.exports = identity;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseProperty.js
var require__baseProperty = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* The base implementation of `_.property` without support for deep paths.
	*
	* @private
	* @param {string} key The key of the property to get.
	* @returns {Function} Returns the new accessor function.
	*/
	function baseProperty(key) {
		return function(object) {
			return object == null ? void 0 : object[key];
		};
	}
	module.exports = baseProperty;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_basePropertyDeep.js
var require__basePropertyDeep = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseGet = require__baseGet();
	/**
	* A specialized version of `baseProperty` which supports deep paths.
	*
	* @private
	* @param {Array|string} path The path of the property to get.
	* @returns {Function} Returns the new accessor function.
	*/
	function basePropertyDeep(path) {
		return function(object) {
			return baseGet(object, path);
		};
	}
	module.exports = basePropertyDeep;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/property.js
var require_property = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseProperty = require__baseProperty();
	var basePropertyDeep = require__basePropertyDeep();
	var isKey = require__isKey();
	var toKey = require__toKey();
	/**
	* Creates a function that returns the value at `path` of a given object.
	*
	* @static
	* @memberOf _
	* @since 2.4.0
	* @category Util
	* @param {Array|string} path The path of the property to get.
	* @returns {Function} Returns the new accessor function.
	* @example
	*
	* var objects = [
	*   { 'a': { 'b': 2 } },
	*   { 'a': { 'b': 1 } }
	* ];
	*
	* _.map(objects, _.property('a.b'));
	* // => [2, 1]
	*
	* _.map(_.sortBy(objects, _.property(['a', 'b'])), 'a.b');
	* // => [1, 2]
	*/
	function property(path) {
		return isKey(path) ? baseProperty(toKey(path)) : basePropertyDeep(path);
	}
	module.exports = property;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseIteratee.js
var require__baseIteratee = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseMatches = require__baseMatches();
	var baseMatchesProperty = require__baseMatchesProperty();
	var identity = require_identity();
	var isArray = require_isArray();
	var property = require_property();
	/**
	* The base implementation of `_.iteratee`.
	*
	* @private
	* @param {*} [value=_.identity] The value to convert to an iteratee.
	* @returns {Function} Returns the iteratee.
	*/
	function baseIteratee(value) {
		if (typeof value == "function") return value;
		if (value == null) return identity;
		if (typeof value == "object") return isArray(value) ? baseMatchesProperty(value[0], value[1]) : baseMatches(value);
		return property(value);
	}
	module.exports = baseIteratee;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_createBaseFor.js
var require__createBaseFor = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Creates a base function for methods like `_.forIn` and `_.forOwn`.
	*
	* @private
	* @param {boolean} [fromRight] Specify iterating from right to left.
	* @returns {Function} Returns the new base function.
	*/
	function createBaseFor(fromRight) {
		return function(object, iteratee, keysFunc) {
			var index = -1, iterable = Object(object), props = keysFunc(object), length = props.length;
			while (length--) {
				var key = props[fromRight ? length : ++index];
				if (iteratee(iterable[key], key, iterable) === false) break;
			}
			return object;
		};
	}
	module.exports = createBaseFor;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseFor.js
var require__baseFor = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__createBaseFor()();
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseForOwn.js
var require__baseForOwn = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseFor = require__baseFor();
	var keys = require_keys();
	/**
	* The base implementation of `_.forOwn` without support for iteratee shorthands.
	*
	* @private
	* @param {Object} object The object to iterate over.
	* @param {Function} iteratee The function invoked per iteration.
	* @returns {Object} Returns `object`.
	*/
	function baseForOwn(object, iteratee) {
		return object && baseFor(object, iteratee, keys);
	}
	module.exports = baseForOwn;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_createBaseEach.js
var require__createBaseEach = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isArrayLike = require_isArrayLike();
	/**
	* Creates a `baseEach` or `baseEachRight` function.
	*
	* @private
	* @param {Function} eachFunc The function to iterate over a collection.
	* @param {boolean} [fromRight] Specify iterating from right to left.
	* @returns {Function} Returns the new base function.
	*/
	function createBaseEach(eachFunc, fromRight) {
		return function(collection, iteratee) {
			if (collection == null) return collection;
			if (!isArrayLike(collection)) return eachFunc(collection, iteratee);
			var length = collection.length, index = fromRight ? length : -1, iterable = Object(collection);
			while (fromRight ? index-- : ++index < length) if (iteratee(iterable[index], index, iterable) === false) break;
			return collection;
		};
	}
	module.exports = createBaseEach;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseEach.js
var require__baseEach = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseForOwn = require__baseForOwn();
	module.exports = require__createBaseEach()(baseForOwn);
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_apply.js
var require__apply = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* A faster alternative to `Function#apply`, this function invokes `func`
	* with the `this` binding of `thisArg` and the arguments of `args`.
	*
	* @private
	* @param {Function} func The function to invoke.
	* @param {*} thisArg The `this` binding of `func`.
	* @param {Array} args The arguments to invoke `func` with.
	* @returns {*} Returns the result of `func`.
	*/
	function apply(func, thisArg, args) {
		switch (args.length) {
			case 0: return func.call(thisArg);
			case 1: return func.call(thisArg, args[0]);
			case 2: return func.call(thisArg, args[0], args[1]);
			case 3: return func.call(thisArg, args[0], args[1], args[2]);
		}
		return func.apply(thisArg, args);
	}
	module.exports = apply;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_overRest.js
var require__overRest = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var apply = require__apply();
	var nativeMax = Math.max;
	/**
	* A specialized version of `baseRest` which transforms the rest array.
	*
	* @private
	* @param {Function} func The function to apply a rest parameter to.
	* @param {number} [start=func.length-1] The start position of the rest parameter.
	* @param {Function} transform The rest array transform.
	* @returns {Function} Returns the new function.
	*/
	function overRest(func, start, transform) {
		start = nativeMax(start === void 0 ? func.length - 1 : start, 0);
		return function() {
			var args = arguments, index = -1, length = nativeMax(args.length - start, 0), array = Array(length);
			while (++index < length) array[index] = args[start + index];
			index = -1;
			var otherArgs = Array(start + 1);
			while (++index < start) otherArgs[index] = args[index];
			otherArgs[start] = transform(array);
			return apply(func, this, otherArgs);
		};
	}
	module.exports = overRest;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/constant.js
var require_constant = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Creates a function that returns `value`.
	*
	* @static
	* @memberOf _
	* @since 2.4.0
	* @category Util
	* @param {*} value The value to return from the new function.
	* @returns {Function} Returns the new constant function.
	* @example
	*
	* var objects = _.times(2, _.constant({ 'a': 1 }));
	*
	* console.log(objects);
	* // => [{ 'a': 1 }, { 'a': 1 }]
	*
	* console.log(objects[0] === objects[1]);
	* // => true
	*/
	function constant(value) {
		return function() {
			return value;
		};
	}
	module.exports = constant;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_defineProperty.js
var require__defineProperty = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getNative = require__getNative();
	module.exports = function() {
		try {
			var func = getNative(Object, "defineProperty");
			func({}, "", {});
			return func;
		} catch (e) {}
	}();
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseSetToString.js
var require__baseSetToString = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var constant = require_constant();
	var defineProperty = require__defineProperty();
	var identity = require_identity();
	module.exports = !defineProperty ? identity : function(func, string) {
		return defineProperty(func, "toString", {
			"configurable": true,
			"enumerable": false,
			"value": constant(string),
			"writable": true
		});
	};
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_shortOut.js
var require__shortOut = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/** Used to detect hot functions by number of calls within a span of milliseconds. */
	var HOT_COUNT = 800;
	var HOT_SPAN = 16;
	var nativeNow = Date.now;
	/**
	* Creates a function that'll short out and invoke `identity` instead
	* of `func` when it's called `HOT_COUNT` or more times in `HOT_SPAN`
	* milliseconds.
	*
	* @private
	* @param {Function} func The function to restrict.
	* @returns {Function} Returns the new shortable function.
	*/
	function shortOut(func) {
		var count = 0, lastCalled = 0;
		return function() {
			var stamp = nativeNow(), remaining = HOT_SPAN - (stamp - lastCalled);
			lastCalled = stamp;
			if (remaining > 0) {
				if (++count >= HOT_COUNT) return arguments[0];
			} else count = 0;
			return func.apply(void 0, arguments);
		};
	}
	module.exports = shortOut;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_setToString.js
var require__setToString = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseSetToString = require__baseSetToString();
	module.exports = require__shortOut()(baseSetToString);
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseRest.js
var require__baseRest = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var identity = require_identity();
	var overRest = require__overRest();
	var setToString = require__setToString();
	/**
	* The base implementation of `_.rest` which doesn't validate or coerce arguments.
	*
	* @private
	* @param {Function} func The function to apply a rest parameter to.
	* @param {number} [start=func.length-1] The start position of the rest parameter.
	* @returns {Function} Returns the new function.
	*/
	function baseRest(func, start) {
		return setToString(overRest(func, start, identity), func + "");
	}
	module.exports = baseRest;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_isIterateeCall.js
var require__isIterateeCall = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var eq = require_eq();
	var isArrayLike = require_isArrayLike();
	var isIndex = require__isIndex();
	var isObject = require_isObject();
	/**
	* Checks if the given arguments are from an iteratee call.
	*
	* @private
	* @param {*} value The potential iteratee value argument.
	* @param {*} index The potential iteratee index or key argument.
	* @param {*} object The potential iteratee object argument.
	* @returns {boolean} Returns `true` if the arguments are from an iteratee call,
	*  else `false`.
	*/
	function isIterateeCall(value, index, object) {
		if (!isObject(object)) return false;
		var type = typeof index;
		if (type == "number" ? isArrayLike(object) && isIndex(index, object.length) : type == "string" && index in object) return eq(object[index], value);
		return false;
	}
	module.exports = isIterateeCall;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/now.js
var require_now = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var root = require__root();
	/**
	* Gets the timestamp of the number of milliseconds that have elapsed since
	* the Unix epoch (1 January 1970 00:00:00 UTC).
	*
	* @static
	* @memberOf _
	* @since 2.4.0
	* @category Date
	* @returns {number} Returns the timestamp.
	* @example
	*
	* _.defer(function(stamp) {
	*   console.log(_.now() - stamp);
	* }, _.now());
	* // => Logs the number of milliseconds it took for the deferred invocation.
	*/
	var now = function() {
		return root.Date.now();
	};
	module.exports = now;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/debounce.js
var require_debounce = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isObject = require_isObject();
	var now = require_now();
	var toNumber = require_toNumber();
	/** Error message constants. */
	var FUNC_ERROR_TEXT = "Expected a function";
	var nativeMax = Math.max;
	var nativeMin = Math.min;
	/**
	* Creates a debounced function that delays invoking `func` until after `wait`
	* milliseconds have elapsed since the last time the debounced function was
	* invoked. The debounced function comes with a `cancel` method to cancel
	* delayed `func` invocations and a `flush` method to immediately invoke them.
	* Provide `options` to indicate whether `func` should be invoked on the
	* leading and/or trailing edge of the `wait` timeout. The `func` is invoked
	* with the last arguments provided to the debounced function. Subsequent
	* calls to the debounced function return the result of the last `func`
	* invocation.
	*
	* **Note:** If `leading` and `trailing` options are `true`, `func` is
	* invoked on the trailing edge of the timeout only if the debounced function
	* is invoked more than once during the `wait` timeout.
	*
	* If `wait` is `0` and `leading` is `false`, `func` invocation is deferred
	* until to the next tick, similar to `setTimeout` with a timeout of `0`.
	*
	* See [David Corbacho's article](https://css-tricks.com/debouncing-throttling-explained-examples/)
	* for details over the differences between `_.debounce` and `_.throttle`.
	*
	* @static
	* @memberOf _
	* @since 0.1.0
	* @category Function
	* @param {Function} func The function to debounce.
	* @param {number} [wait=0] The number of milliseconds to delay.
	* @param {Object} [options={}] The options object.
	* @param {boolean} [options.leading=false]
	*  Specify invoking on the leading edge of the timeout.
	* @param {number} [options.maxWait]
	*  The maximum time `func` is allowed to be delayed before it's invoked.
	* @param {boolean} [options.trailing=true]
	*  Specify invoking on the trailing edge of the timeout.
	* @returns {Function} Returns the new debounced function.
	* @example
	*
	* // Avoid costly calculations while the window size is in flux.
	* jQuery(window).on('resize', _.debounce(calculateLayout, 150));
	*
	* // Invoke `sendMail` when clicked, debouncing subsequent calls.
	* jQuery(element).on('click', _.debounce(sendMail, 300, {
	*   'leading': true,
	*   'trailing': false
	* }));
	*
	* // Ensure `batchLog` is invoked once after 1 second of debounced calls.
	* var debounced = _.debounce(batchLog, 250, { 'maxWait': 1000 });
	* var source = new EventSource('/stream');
	* jQuery(source).on('message', debounced);
	*
	* // Cancel the trailing debounced invocation.
	* jQuery(window).on('popstate', debounced.cancel);
	*/
	function debounce(func, wait, options) {
		var lastArgs, lastThis, maxWait, result, timerId, lastCallTime, lastInvokeTime = 0, leading = false, maxing = false, trailing = true;
		if (typeof func != "function") throw new TypeError(FUNC_ERROR_TEXT);
		wait = toNumber(wait) || 0;
		if (isObject(options)) {
			leading = !!options.leading;
			maxing = "maxWait" in options;
			maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
			trailing = "trailing" in options ? !!options.trailing : trailing;
		}
		function invokeFunc(time) {
			var args = lastArgs, thisArg = lastThis;
			lastArgs = lastThis = void 0;
			lastInvokeTime = time;
			result = func.apply(thisArg, args);
			return result;
		}
		function leadingEdge(time) {
			lastInvokeTime = time;
			timerId = setTimeout(timerExpired, wait);
			return leading ? invokeFunc(time) : result;
		}
		function remainingWait(time) {
			var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime, timeWaiting = wait - timeSinceLastCall;
			return maxing ? nativeMin(timeWaiting, maxWait - timeSinceLastInvoke) : timeWaiting;
		}
		function shouldInvoke(time) {
			var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime;
			return lastCallTime === void 0 || timeSinceLastCall >= wait || timeSinceLastCall < 0 || maxing && timeSinceLastInvoke >= maxWait;
		}
		function timerExpired() {
			var time = now();
			if (shouldInvoke(time)) return trailingEdge(time);
			timerId = setTimeout(timerExpired, remainingWait(time));
		}
		function trailingEdge(time) {
			timerId = void 0;
			if (trailing && lastArgs) return invokeFunc(time);
			lastArgs = lastThis = void 0;
			return result;
		}
		function cancel() {
			if (timerId !== void 0) clearTimeout(timerId);
			lastInvokeTime = 0;
			lastArgs = lastCallTime = lastThis = timerId = void 0;
		}
		function flush() {
			return timerId === void 0 ? result : trailingEdge(now());
		}
		function debounced() {
			var time = now(), isInvoking = shouldInvoke(time);
			lastArgs = arguments;
			lastThis = this;
			lastCallTime = time;
			if (isInvoking) {
				if (timerId === void 0) return leadingEdge(lastCallTime);
				if (maxing) {
					clearTimeout(timerId);
					timerId = setTimeout(timerExpired, wait);
					return invokeFunc(lastCallTime);
				}
			}
			if (timerId === void 0) timerId = setTimeout(timerExpired, wait);
			return result;
		}
		debounced.cancel = cancel;
		debounced.flush = flush;
		return debounced;
	}
	module.exports = debounce;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/toFinite.js
var require_toFinite = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var toNumber = require_toNumber();
	/** Used as references for various `Number` constants. */
	var INFINITY = Infinity;
	var MAX_INTEGER = 17976931348623157e292;
	/**
	* Converts `value` to a finite number.
	*
	* @static
	* @memberOf _
	* @since 4.12.0
	* @category Lang
	* @param {*} value The value to convert.
	* @returns {number} Returns the converted number.
	* @example
	*
	* _.toFinite(3.2);
	* // => 3.2
	*
	* _.toFinite(Number.MIN_VALUE);
	* // => 5e-324
	*
	* _.toFinite(Infinity);
	* // => 1.7976931348623157e+308
	*
	* _.toFinite('3.2');
	* // => 3.2
	*/
	function toFinite(value) {
		if (!value) return value === 0 ? value : 0;
		value = toNumber(value);
		if (value === INFINITY || value === -INFINITY) return (value < 0 ? -1 : 1) * MAX_INTEGER;
		return value === value ? value : 0;
	}
	module.exports = toFinite;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/toInteger.js
var require_toInteger = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var toFinite = require_toFinite();
	/**
	* Converts `value` to an integer.
	*
	* **Note:** This method is loosely based on
	* [`ToInteger`](http://www.ecma-international.org/ecma-262/7.0/#sec-tointeger).
	*
	* @static
	* @memberOf _
	* @since 4.0.0
	* @category Lang
	* @param {*} value The value to convert.
	* @returns {number} Returns the converted integer.
	* @example
	*
	* _.toInteger(3.2);
	* // => 3
	*
	* _.toInteger(Number.MIN_VALUE);
	* // => 0
	*
	* _.toInteger(Infinity);
	* // => 1.7976931348623157e+308
	*
	* _.toInteger('3.2');
	* // => 3
	*/
	function toInteger(value) {
		var result = toFinite(value), remainder = result % 1;
		return result === result ? remainder ? result - remainder : result : 0;
	}
	module.exports = toInteger;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_createRound.js
var require__createRound = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var root = require__root();
	var toInteger = require_toInteger();
	var toNumber = require_toNumber();
	var toString = require_toString();
	var nativeIsFinite = root.isFinite;
	var nativeMin = Math.min;
	/**
	* Creates a function like `_.round`.
	*
	* @private
	* @param {string} methodName The name of the `Math` method to use when rounding.
	* @returns {Function} Returns the new round function.
	*/
	function createRound(methodName) {
		var func = Math[methodName];
		return function(number, precision) {
			number = toNumber(number);
			precision = precision == null ? 0 : nativeMin(toInteger(precision), 292);
			if (precision && nativeIsFinite(number)) {
				var pair = (toString(number) + "e").split("e");
				pair = (toString(func(pair[0] + "e" + (+pair[1] + precision))) + "e").split("e");
				return +(pair[0] + "e" + (+pair[1] - precision));
			}
			return func(number);
		};
	}
	module.exports = createRound;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/round.js
var require_round = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__createRound()("round");
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseAssignValue.js
var require__baseAssignValue = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var defineProperty = require__defineProperty();
	/**
	* The base implementation of `assignValue` and `assignMergeValue` without
	* value checks.
	*
	* @private
	* @param {Object} object The object to modify.
	* @param {string} key The key of the property to assign.
	* @param {*} value The value to assign.
	*/
	function baseAssignValue(object, key, value) {
		if (key == "__proto__" && defineProperty) defineProperty(object, key, {
			"configurable": true,
			"enumerable": true,
			"value": value,
			"writable": true
		});
		else object[key] = value;
	}
	module.exports = baseAssignValue;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_assignMergeValue.js
var require__assignMergeValue = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseAssignValue = require__baseAssignValue();
	var eq = require_eq();
	/**
	* This function is like `assignValue` except that it doesn't assign
	* `undefined` values.
	*
	* @private
	* @param {Object} object The object to modify.
	* @param {string} key The key of the property to assign.
	* @param {*} value The value to assign.
	*/
	function assignMergeValue(object, key, value) {
		if (value !== void 0 && !eq(object[key], value) || value === void 0 && !(key in object)) baseAssignValue(object, key, value);
	}
	module.exports = assignMergeValue;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_cloneBuffer.js
var require__cloneBuffer = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var root = require__root();
	/** Detect free variable `exports`. */
	var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
	/** Detect free variable `module`. */
	var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
	/** Built-in value references. */
	var Buffer = freeModule && freeModule.exports === freeExports ? root.Buffer : void 0;
	var allocUnsafe = Buffer ? Buffer.allocUnsafe : void 0;
	/**
	* Creates a clone of  `buffer`.
	*
	* @private
	* @param {Buffer} buffer The buffer to clone.
	* @param {boolean} [isDeep] Specify a deep clone.
	* @returns {Buffer} Returns the cloned buffer.
	*/
	function cloneBuffer(buffer, isDeep) {
		if (isDeep) return buffer.slice();
		var length = buffer.length, result = allocUnsafe ? allocUnsafe(length) : new buffer.constructor(length);
		buffer.copy(result);
		return result;
	}
	module.exports = cloneBuffer;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_cloneArrayBuffer.js
var require__cloneArrayBuffer = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Uint8Array = require__Uint8Array();
	/**
	* Creates a clone of `arrayBuffer`.
	*
	* @private
	* @param {ArrayBuffer} arrayBuffer The array buffer to clone.
	* @returns {ArrayBuffer} Returns the cloned array buffer.
	*/
	function cloneArrayBuffer(arrayBuffer) {
		var result = new arrayBuffer.constructor(arrayBuffer.byteLength);
		new Uint8Array(result).set(new Uint8Array(arrayBuffer));
		return result;
	}
	module.exports = cloneArrayBuffer;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_cloneTypedArray.js
var require__cloneTypedArray = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var cloneArrayBuffer = require__cloneArrayBuffer();
	/**
	* Creates a clone of `typedArray`.
	*
	* @private
	* @param {Object} typedArray The typed array to clone.
	* @param {boolean} [isDeep] Specify a deep clone.
	* @returns {Object} Returns the cloned typed array.
	*/
	function cloneTypedArray(typedArray, isDeep) {
		var buffer = isDeep ? cloneArrayBuffer(typedArray.buffer) : typedArray.buffer;
		return new typedArray.constructor(buffer, typedArray.byteOffset, typedArray.length);
	}
	module.exports = cloneTypedArray;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_copyArray.js
var require__copyArray = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Copies the values of `source` to `array`.
	*
	* @private
	* @param {Array} source The array to copy values from.
	* @param {Array} [array=[]] The array to copy values to.
	* @returns {Array} Returns `array`.
	*/
	function copyArray(source, array) {
		var index = -1, length = source.length;
		array || (array = Array(length));
		while (++index < length) array[index] = source[index];
		return array;
	}
	module.exports = copyArray;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseCreate.js
var require__baseCreate = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isObject = require_isObject();
	/** Built-in value references. */
	var objectCreate = Object.create;
	module.exports = function() {
		function object() {}
		return function(proto) {
			if (!isObject(proto)) return {};
			if (objectCreate) return objectCreate(proto);
			object.prototype = proto;
			var result = new object();
			object.prototype = void 0;
			return result;
		};
	}();
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_getPrototype.js
var require__getPrototype = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require__overArg()(Object.getPrototypeOf, Object);
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_initCloneObject.js
var require__initCloneObject = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseCreate = require__baseCreate();
	var getPrototype = require__getPrototype();
	var isPrototype = require__isPrototype();
	/**
	* Initializes an object clone.
	*
	* @private
	* @param {Object} object The object to clone.
	* @returns {Object} Returns the initialized clone.
	*/
	function initCloneObject(object) {
		return typeof object.constructor == "function" && !isPrototype(object) ? baseCreate(getPrototype(object)) : {};
	}
	module.exports = initCloneObject;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isArrayLikeObject.js
var require_isArrayLikeObject = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isArrayLike = require_isArrayLike();
	var isObjectLike = require_isObjectLike();
	/**
	* This method is like `_.isArrayLike` except that it also checks if `value`
	* is an object.
	*
	* @static
	* @memberOf _
	* @since 4.0.0
	* @category Lang
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is an array-like object,
	*  else `false`.
	* @example
	*
	* _.isArrayLikeObject([1, 2, 3]);
	* // => true
	*
	* _.isArrayLikeObject(document.body.children);
	* // => true
	*
	* _.isArrayLikeObject('abc');
	* // => false
	*
	* _.isArrayLikeObject(_.noop);
	* // => false
	*/
	function isArrayLikeObject(value) {
		return isObjectLike(value) && isArrayLike(value);
	}
	module.exports = isArrayLikeObject;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isPlainObject.js
var require_isPlainObject = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseGetTag = require__baseGetTag();
	var getPrototype = require__getPrototype();
	var isObjectLike = require_isObjectLike();
	/** `Object#toString` result references. */
	var objectTag = "[object Object]";
	/** Used for built-in method references. */
	var funcProto = Function.prototype;
	var objectProto = Object.prototype;
	/** Used to resolve the decompiled source of functions. */
	var funcToString = funcProto.toString;
	/** Used to check objects for own properties. */
	var hasOwnProperty = objectProto.hasOwnProperty;
	/** Used to infer the `Object` constructor. */
	var objectCtorString = funcToString.call(Object);
	/**
	* Checks if `value` is a plain object, that is, an object created by the
	* `Object` constructor or one with a `[[Prototype]]` of `null`.
	*
	* @static
	* @memberOf _
	* @since 0.8.0
	* @category Lang
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is a plain object, else `false`.
	* @example
	*
	* function Foo() {
	*   this.a = 1;
	* }
	*
	* _.isPlainObject(new Foo);
	* // => false
	*
	* _.isPlainObject([1, 2, 3]);
	* // => false
	*
	* _.isPlainObject({ 'x': 0, 'y': 0 });
	* // => true
	*
	* _.isPlainObject(Object.create(null));
	* // => true
	*/
	function isPlainObject(value) {
		if (!isObjectLike(value) || baseGetTag(value) != objectTag) return false;
		var proto = getPrototype(value);
		if (proto === null) return true;
		var Ctor = hasOwnProperty.call(proto, "constructor") && proto.constructor;
		return typeof Ctor == "function" && Ctor instanceof Ctor && funcToString.call(Ctor) == objectCtorString;
	}
	module.exports = isPlainObject;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_safeGet.js
var require__safeGet = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Gets the value at `key`, unless `key` is "__proto__" or "constructor".
	*
	* @private
	* @param {Object} object The object to query.
	* @param {string} key The key of the property to get.
	* @returns {*} Returns the property value.
	*/
	function safeGet(object, key) {
		if (key === "constructor" && typeof object[key] === "function") return;
		if (key == "__proto__") return;
		return object[key];
	}
	module.exports = safeGet;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_assignValue.js
var require__assignValue = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseAssignValue = require__baseAssignValue();
	var eq = require_eq();
	/** Used to check objects for own properties. */
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	/**
	* Assigns `value` to `key` of `object` if the existing value is not equivalent
	* using [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
	* for equality comparisons.
	*
	* @private
	* @param {Object} object The object to modify.
	* @param {string} key The key of the property to assign.
	* @param {*} value The value to assign.
	*/
	function assignValue(object, key, value) {
		var objValue = object[key];
		if (!(hasOwnProperty.call(object, key) && eq(objValue, value)) || value === void 0 && !(key in object)) baseAssignValue(object, key, value);
	}
	module.exports = assignValue;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_copyObject.js
var require__copyObject = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var assignValue = require__assignValue();
	var baseAssignValue = require__baseAssignValue();
	/**
	* Copies properties of `source` to `object`.
	*
	* @private
	* @param {Object} source The object to copy properties from.
	* @param {Array} props The property identifiers to copy.
	* @param {Object} [object={}] The object to copy properties to.
	* @param {Function} [customizer] The function to customize copied values.
	* @returns {Object} Returns `object`.
	*/
	function copyObject(source, props, object, customizer) {
		var isNew = !object;
		object || (object = {});
		var index = -1, length = props.length;
		while (++index < length) {
			var key = props[index];
			var newValue = customizer ? customizer(object[key], source[key], key, object, source) : void 0;
			if (newValue === void 0) newValue = source[key];
			if (isNew) baseAssignValue(object, key, newValue);
			else assignValue(object, key, newValue);
		}
		return object;
	}
	module.exports = copyObject;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_nativeKeysIn.js
var require__nativeKeysIn = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* This function is like
	* [`Object.keys`](http://ecma-international.org/ecma-262/7.0/#sec-object.keys)
	* except that it includes inherited enumerable properties.
	*
	* @private
	* @param {Object} object The object to query.
	* @returns {Array} Returns the array of property names.
	*/
	function nativeKeysIn(object) {
		var result = [];
		if (object != null) for (var key in Object(object)) result.push(key);
		return result;
	}
	module.exports = nativeKeysIn;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseKeysIn.js
var require__baseKeysIn = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isObject = require_isObject();
	var isPrototype = require__isPrototype();
	var nativeKeysIn = require__nativeKeysIn();
	/** Used to check objects for own properties. */
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	/**
	* The base implementation of `_.keysIn` which doesn't treat sparse arrays as dense.
	*
	* @private
	* @param {Object} object The object to query.
	* @returns {Array} Returns the array of property names.
	*/
	function baseKeysIn(object) {
		if (!isObject(object)) return nativeKeysIn(object);
		var isProto = isPrototype(object), result = [];
		for (var key in object) if (!(key == "constructor" && (isProto || !hasOwnProperty.call(object, key)))) result.push(key);
		return result;
	}
	module.exports = baseKeysIn;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/keysIn.js
var require_keysIn = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var arrayLikeKeys = require__arrayLikeKeys();
	var baseKeysIn = require__baseKeysIn();
	var isArrayLike = require_isArrayLike();
	/**
	* Creates an array of the own and inherited enumerable property names of `object`.
	*
	* **Note:** Non-object values are coerced to objects.
	*
	* @static
	* @memberOf _
	* @since 3.0.0
	* @category Object
	* @param {Object} object The object to query.
	* @returns {Array} Returns the array of property names.
	* @example
	*
	* function Foo() {
	*   this.a = 1;
	*   this.b = 2;
	* }
	*
	* Foo.prototype.c = 3;
	*
	* _.keysIn(new Foo);
	* // => ['a', 'b', 'c'] (iteration order is not guaranteed)
	*/
	function keysIn(object) {
		return isArrayLike(object) ? arrayLikeKeys(object, true) : baseKeysIn(object);
	}
	module.exports = keysIn;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/toPlainObject.js
var require_toPlainObject = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var copyObject = require__copyObject();
	var keysIn = require_keysIn();
	/**
	* Converts `value` to a plain object flattening inherited enumerable string
	* keyed properties of `value` to own properties of the plain object.
	*
	* @static
	* @memberOf _
	* @since 3.0.0
	* @category Lang
	* @param {*} value The value to convert.
	* @returns {Object} Returns the converted plain object.
	* @example
	*
	* function Foo() {
	*   this.b = 2;
	* }
	*
	* Foo.prototype.c = 3;
	*
	* _.assign({ 'a': 1 }, new Foo);
	* // => { 'a': 1, 'b': 2 }
	*
	* _.assign({ 'a': 1 }, _.toPlainObject(new Foo));
	* // => { 'a': 1, 'b': 2, 'c': 3 }
	*/
	function toPlainObject(value) {
		return copyObject(value, keysIn(value));
	}
	module.exports = toPlainObject;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseMergeDeep.js
var require__baseMergeDeep = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var assignMergeValue = require__assignMergeValue();
	var cloneBuffer = require__cloneBuffer();
	var cloneTypedArray = require__cloneTypedArray();
	var copyArray = require__copyArray();
	var initCloneObject = require__initCloneObject();
	var isArguments = require_isArguments();
	var isArray = require_isArray();
	var isArrayLikeObject = require_isArrayLikeObject();
	var isBuffer = require_isBuffer();
	var isFunction = require_isFunction();
	var isObject = require_isObject();
	var isPlainObject = require_isPlainObject();
	var isTypedArray = require_isTypedArray();
	var safeGet = require__safeGet();
	var toPlainObject = require_toPlainObject();
	/**
	* A specialized version of `baseMerge` for arrays and objects which performs
	* deep merges and tracks traversed objects enabling objects with circular
	* references to be merged.
	*
	* @private
	* @param {Object} object The destination object.
	* @param {Object} source The source object.
	* @param {string} key The key of the value to merge.
	* @param {number} srcIndex The index of `source`.
	* @param {Function} mergeFunc The function to merge values.
	* @param {Function} [customizer] The function to customize assigned values.
	* @param {Object} [stack] Tracks traversed source values and their merged
	*  counterparts.
	*/
	function baseMergeDeep(object, source, key, srcIndex, mergeFunc, customizer, stack) {
		var objValue = safeGet(object, key), srcValue = safeGet(source, key), stacked = stack.get(srcValue);
		if (stacked) {
			assignMergeValue(object, key, stacked);
			return;
		}
		var newValue = customizer ? customizer(objValue, srcValue, key + "", object, source, stack) : void 0;
		var isCommon = newValue === void 0;
		if (isCommon) {
			var isArr = isArray(srcValue), isBuff = !isArr && isBuffer(srcValue), isTyped = !isArr && !isBuff && isTypedArray(srcValue);
			newValue = srcValue;
			if (isArr || isBuff || isTyped) if (isArray(objValue)) newValue = objValue;
			else if (isArrayLikeObject(objValue)) newValue = copyArray(objValue);
			else if (isBuff) {
				isCommon = false;
				newValue = cloneBuffer(srcValue, true);
			} else if (isTyped) {
				isCommon = false;
				newValue = cloneTypedArray(srcValue, true);
			} else newValue = [];
			else if (isPlainObject(srcValue) || isArguments(srcValue)) {
				newValue = objValue;
				if (isArguments(objValue)) newValue = toPlainObject(objValue);
				else if (!isObject(objValue) || isFunction(objValue)) newValue = initCloneObject(srcValue);
			} else isCommon = false;
		}
		if (isCommon) {
			stack.set(srcValue, newValue);
			mergeFunc(newValue, srcValue, srcIndex, customizer, stack);
			stack["delete"](srcValue);
		}
		assignMergeValue(object, key, newValue);
	}
	module.exports = baseMergeDeep;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseMerge.js
var require__baseMerge = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Stack = require__Stack();
	var assignMergeValue = require__assignMergeValue();
	var baseFor = require__baseFor();
	var baseMergeDeep = require__baseMergeDeep();
	var isObject = require_isObject();
	var keysIn = require_keysIn();
	var safeGet = require__safeGet();
	/**
	* The base implementation of `_.merge` without support for multiple sources.
	*
	* @private
	* @param {Object} object The destination object.
	* @param {Object} source The source object.
	* @param {number} srcIndex The index of `source`.
	* @param {Function} [customizer] The function to customize merged values.
	* @param {Object} [stack] Tracks traversed source values and their merged
	*  counterparts.
	*/
	function baseMerge(object, source, srcIndex, customizer, stack) {
		if (object === source) return;
		baseFor(source, function(srcValue, key) {
			stack || (stack = new Stack());
			if (isObject(srcValue)) baseMergeDeep(object, source, key, srcIndex, baseMerge, customizer, stack);
			else {
				var newValue = customizer ? customizer(safeGet(object, key), srcValue, key + "", object, source, stack) : void 0;
				if (newValue === void 0) newValue = srcValue;
				assignMergeValue(object, key, newValue);
			}
		}, keysIn);
	}
	module.exports = baseMerge;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_createAssigner.js
var require__createAssigner = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseRest = require__baseRest();
	var isIterateeCall = require__isIterateeCall();
	/**
	* Creates a function like `_.assign`.
	*
	* @private
	* @param {Function} assigner The function to assign values.
	* @returns {Function} Returns the new assigner function.
	*/
	function createAssigner(assigner) {
		return baseRest(function(object, sources) {
			var index = -1, length = sources.length, customizer = length > 1 ? sources[length - 1] : void 0, guard = length > 2 ? sources[2] : void 0;
			customizer = assigner.length > 3 && typeof customizer == "function" ? (length--, customizer) : void 0;
			if (guard && isIterateeCall(sources[0], sources[1], guard)) {
				customizer = length < 3 ? void 0 : customizer;
				length = 1;
			}
			object = Object(object);
			while (++index < length) {
				var source = sources[index];
				if (source) assigner(object, source, index, customizer);
			}
			return object;
		});
	}
	module.exports = createAssigner;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/merge.js
var require_merge = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseMerge = require__baseMerge();
	module.exports = require__createAssigner()(function(object, source, srcIndex) {
		baseMerge(object, source, srcIndex);
	});
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_arrayAggregator.js
var require__arrayAggregator = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* A specialized version of `baseAggregator` for arrays.
	*
	* @private
	* @param {Array} [array] The array to iterate over.
	* @param {Function} setter The function to set `accumulator` values.
	* @param {Function} iteratee The iteratee to transform keys.
	* @param {Object} accumulator The initial aggregated object.
	* @returns {Function} Returns `accumulator`.
	*/
	function arrayAggregator(array, setter, iteratee, accumulator) {
		var index = -1, length = array == null ? 0 : array.length;
		while (++index < length) {
			var value = array[index];
			setter(accumulator, value, iteratee(value), array);
		}
		return accumulator;
	}
	module.exports = arrayAggregator;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseAggregator.js
var require__baseAggregator = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseEach = require__baseEach();
	/**
	* Aggregates elements of `collection` on `accumulator` with keys transformed
	* by `iteratee` and values set by `setter`.
	*
	* @private
	* @param {Array|Object} collection The collection to iterate over.
	* @param {Function} setter The function to set `accumulator` values.
	* @param {Function} iteratee The iteratee to transform keys.
	* @param {Object} accumulator The initial aggregated object.
	* @returns {Function} Returns `accumulator`.
	*/
	function baseAggregator(collection, setter, iteratee, accumulator) {
		baseEach(collection, function(value, key, collection) {
			setter(accumulator, value, iteratee(value), collection);
		});
		return accumulator;
	}
	module.exports = baseAggregator;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_createAggregator.js
var require__createAggregator = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var arrayAggregator = require__arrayAggregator();
	var baseAggregator = require__baseAggregator();
	var baseIteratee = require__baseIteratee();
	var isArray = require_isArray();
	/**
	* Creates a function like `_.groupBy`.
	*
	* @private
	* @param {Function} setter The function to set accumulator values.
	* @param {Function} [initializer] The accumulator object initializer.
	* @returns {Function} Returns the new aggregator function.
	*/
	function createAggregator(setter, initializer) {
		return function(collection, iteratee) {
			var func = isArray(collection) ? arrayAggregator : baseAggregator, accumulator = initializer ? initializer() : {};
			return func(collection, setter, baseIteratee(iteratee, 2), accumulator);
		};
	}
	module.exports = createAggregator;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/groupBy.js
var require_groupBy = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseAssignValue = require__baseAssignValue();
	var createAggregator = require__createAggregator();
	/** Used to check objects for own properties. */
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	module.exports = createAggregator(function(result, value, key) {
		if (hasOwnProperty.call(result, key)) result[key].push(value);
		else baseAssignValue(result, key, [value]);
	});
}));
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/utils/configure.js
var import_merge = /* @__PURE__ */ __toESM(require_merge(), 1);
var Lang;
(function(Lang) {
	Lang["Ru"] = "ru";
	Lang["En"] = "en";
})(Lang || (Lang = {}));
var subs = [];
var config = {
	lang: Lang.En,
	fallbackLang: Lang.En
};
var configure = (newConfig) => {
	Object.assign(config, newConfig);
	subs.forEach((sub) => {
		sub(config);
	});
};
var subscribeConfigure = (sub) => {
	subs.push(sub);
	return () => {
		subs = subs.filter((item) => item !== sub);
	};
};
var getConfig = () => config;
withNaming({
	e: "__",
	m: "_"
});
var block = withNaming({
	n: "g-",
	e: "__",
	m: "_"
});
/**
* Extracts modifiers part from className
*/
function modsClassName(className) {
	return className.split(/\s(.*)/)[1];
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/mobile/constants.js
var Platform;
(function(Platform) {
	Platform["IOS"] = "ios";
	Platform["ANDROID"] = "android";
	Platform["BROWSER"] = "browser";
})(Platform || (Platform = {}));
var rootMobileClassName = block("root")({ mobile: true }).split(/\s+/)[1];
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/mobile/MobileContext.js
var initialValue = {
	mobile: false,
	platform: Platform.BROWSER,
	useHistory: () => ({
		action: "",
		replace() {},
		push() {},
		goBack() {}
	}),
	useLocation: () => ({
		pathname: "",
		search: "",
		hash: ""
	})
};
var MobileContext = import_react.createContext(initialValue);
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/mobile/MobileProvider.js
function useHistoryMock() {
	return {
		action: "",
		replace() {},
		push() {},
		goBack() {}
	};
}
function useLocationMock() {
	return {
		pathname: "",
		search: "",
		hash: ""
	};
}
function MobileProvider({ mobile = false, __experimentalMobileModals = false, platform = Platform.BROWSER, useHistory = useHistoryMock, useLocation = useLocationMock, children }) {
	const useHistoryFunction = import_react.useCallback(function useHistoryFunction() {
		const { goBack, back, ...props } = useHistory();
		let goBackFunction;
		if (typeof goBack === "function") goBackFunction = goBack;
		else if (typeof back === "function") goBackFunction = back;
		else goBackFunction = () => {};
		return {
			...props,
			goBack: goBackFunction
		};
	}, [useHistory]);
	import_react.useEffect(() => {
		document.body.classList.toggle(rootMobileClassName, mobile);
	}, [rootMobileClassName, mobile]);
	const contextValue = import_react.useMemo(() => {
		return {
			mobile,
			__experimentalMobileModals,
			platform,
			useLocation,
			useHistory: useHistoryFunction
		};
	}, [
		mobile,
		__experimentalMobileModals,
		platform,
		useLocation,
		useHistoryFunction
	]);
	return (0, import_jsx_runtime.jsx)(MobileContext.Provider, {
		value: contextValue,
		children
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/mobile/useMobile.js
function useMobile() {
	return import_react.useContext(MobileContext).mobile;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/mobile/usePlatform.js
function usePlatform() {
	return import_react.useContext(MobileContext).platform;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/getComponentName.js
function getComponentName(Component) {
	return Component.displayName || Component.name || "Component";
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/mobile/withPlatform.js
function withPlatform(WrappedComponent) {
	const componentName = getComponentName(WrappedComponent);
	return class WithPlatformComponent extends import_react.Component {
		static displayName = `withPlatform(${componentName})`;
		static contextType = MobileContext;
		render() {
			return (0, import_jsx_runtime.jsx)(WrappedComponent, {
				...this.props,
				platform: this.context.platform
			});
		}
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/mobile/withMobile.js
function withMobile(WrappedComponent) {
	const componentName = getComponentName(WrappedComponent);
	return class WithMobileComponent extends import_react.Component {
		static displayName = `withMobile(${componentName})`;
		static contextType = MobileContext;
		render() {
			return (0, import_jsx_runtime.jsx)(WrappedComponent, {
				...this.props,
				mobile: this.context.mobile,
				platform: this.context.platform,
				useHistory: this.context.useHistory,
				useLocation: this.context.useLocation
			});
		}
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/ThemeContext.js
var ThemeContext = import_react.createContext(void 0);
ThemeContext.displayName = "ThemeContext";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/constants.js
var KeyCode$1 = {
	BACKSPACE: "Backspace",
	ENTER: "Enter",
	TAB: "Tab",
	SPACE: "Space",
	SPACEBAR: " ",
	SPACEBAR_OLD: "Spacebar",
	ESCAPE: "Escape",
	ARROW_UP: "ArrowUp",
	ARROW_DOWN: "ArrowDown",
	ARROW_LEFT: "ArrowLeft",
	ARROW_RIGHT: "ArrowRight",
	PAGE_UP: "PageUp",
	PAGE_DOWN: "PageDown",
	SHIFT: "Shift",
	HOME: "Home",
	END: "End"
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useActionHandlers/useActionHandlers.js
var import_react_dom = /* @__PURE__ */ __toESM(require_react_dom());
var import_isObject = /* @__PURE__ */ __toESM(require_isObject(), 1);
var import_memoize = /* @__PURE__ */ __toESM(require_memoize(), 1);
var import_get = /* @__PURE__ */ __toESM(require_get(), 1);
var import_capitalize = /* @__PURE__ */ __toESM(require_capitalize(), 1);
var import_round = /* @__PURE__ */ __toESM(require_round(), 1);
var import_groupBy = /* @__PURE__ */ __toESM(require_groupBy(), 1);
function createOnKeyDownHandler(callback) {
	return (event) => {
		if (callback && [
			KeyCode$1.ENTER,
			KeyCode$1.SPACEBAR,
			KeyCode$1.SPACEBAR_OLD
		].includes(event.key)) {
			event.preventDefault();
			callback(event);
		}
	};
}
/**
* Emulates behaviour of system controls, that respond to Enter and Spacebar
* @param callback
* @returns {onKeyDown}
*/
function useActionHandlers(callback) {
	return { onKeyDown: import_react.useMemo(() => createOnKeyDownHandler(callback), [callback]) };
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useAsyncActionHandler/useAsyncActionHandler.js
function useAsyncActionHandler({ handler }) {
	const [isLoading, setLoading] = import_react.useState(false);
	return {
		isLoading,
		handler: import_react.useCallback(async (...args) => {
			setLoading(true);
			try {
				return await handler(...args);
			} finally {
				setLoading(false);
			}
		}, [handler])
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useControlledState/useControlledState.js
function useControlledState(value, defaultValue, onUpdate) {
	const [innerValue, setInnerValue] = import_react.useState(value ?? defaultValue);
	const isControlledRef = import_react.useRef(value !== void 0);
	const isControlled = value !== void 0;
	import_react.useEffect(() => {
		const wasControlled = isControlledRef.current;
		if (wasControlled !== isControlled) console.error(`[useControlledState] A component changed from ${wasControlled ? "controlled" : "uncontrolled"} to ${isControlled ? "controlled" : "uncontrolled"}.`);
		isControlledRef.current = isControlled;
	}, [isControlled]);
	let currentValue = isControlled ? value : innerValue;
	const setState = import_react.useCallback((newValue, ...args) => {
		if (!Object.is(currentValue, newValue)) onUpdate?.(newValue, ...args);
		if (!isControlled) {
			currentValue = newValue;
			setInnerValue(newValue);
		}
	}, [
		isControlled,
		onUpdate,
		currentValue
	]);
	return [currentValue, setState];
}
var LIGHT_THEMES = ["light", "light-hc"];
var DARK_THEMES = ["dark", "dark-hc"];
[...LIGHT_THEMES, ...DARK_THEMES];
var ROOT_CLASSNAME = "root";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/getThemeType.js
function getThemeType(theme) {
	return DARK_THEMES.includes(theme) ? "dark" : "light";
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/useThemeContext.js
function useThemeContext() {
	const state = import_react.useContext(ThemeContext);
	if (state === void 0) throw new Error("useTheme* hooks must be used within ThemeProvider");
	return state;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/useThemeValue.js
function useThemeValue() {
	return useThemeContext().themeValue;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/useThemeType.js
function useThemeType() {
	return getThemeType(useThemeValue());
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useColorGenerator/constants.js
var colorOptions = {
	light: {
		lightness: [55, 60],
		chroma: [20, 25]
	},
	dark: {
		lightness: [75, 80],
		chroma: [12, 17]
	}
};
var textColorVarName = "--g-color-text-inverted-primary";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useColorGenerator/hash-utils.js
var getHue = (hash) => {
	return Math.abs(hash) % 36e3 / 100;
};
var getHash = (seed) => {
	let hash = 2166136261;
	const len = seed.length;
	hash ^= len;
	hash = Math.imul(hash, 16777619);
	for (let i = 0; i < len; i++) {
		hash ^= seed.charCodeAt(i);
		hash = Math.imul(hash, 16777619);
	}
	hash ^= hash >>> 16;
	hash = Math.imul(hash, 2246822507);
	hash ^= hash >>> 13;
	hash = Math.imul(hash, 3266489909);
	hash ^= hash >>> 16;
	hash = Math.imul(hash, 668265261);
	hash ^= hash >>> 15;
	return hash | 0;
};
var normalizeHash = (hash, min, max) => {
	return Math.floor(Math.abs(hash) % (max - min + 1) + min);
};
var extractHashPart = (hash, offset) => {
	let part = hash ^ offset * 2654435769;
	part ^= part >>> 16;
	part = Math.imul(part, 2246822507);
	part ^= part >>> 13;
	part = Math.imul(part, 3266489909);
	part ^= part >>> 16;
	return part >>> 0;
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useColorGenerator/color.js
var linearToSrgb = (channel) => {
	if (channel <= .0031308) return 12.92 * channel;
	else return 1.055 * Math.pow(channel, 1 / 2.4) - .055;
};
var oklchToRgb = (l, c, h) => {
	const hRadians = h * Math.PI / 180;
	const a = c * Math.cos(hRadians);
	const b = c * Math.sin(hRadians);
	const l_ = l + .3963377774 * a + .2158037573 * b;
	const m_ = l - .1055613458 * a - .0638541728 * b;
	const s_ = l - .0894841775 * a - 1.291485548 * b;
	const lCubed = l_ * l_ * l_;
	const mCubed = m_ * m_ * m_;
	const sCubed = s_ * s_ * s_;
	const linearR = 4.0767416621 * lCubed - 3.3077115913 * mCubed + .2309699292 * sCubed;
	const linearG = -1.2684380046 * lCubed + 2.6097574011 * mCubed - .3413193965 * sCubed;
	const linearB = -.0041960863 * lCubed - .7034186147 * mCubed + 1.707614701 * sCubed;
	return [
		Math.max(0, Math.min(255, Math.round(255 * linearToSrgb(linearR)))),
		Math.max(0, Math.min(255, Math.round(255 * linearToSrgb(linearG)))),
		Math.max(0, Math.min(255, Math.round(255 * linearToSrgb(linearB))))
	];
};
var generateColorFromRanges = (seed, lightnessRange, chromaRange) => {
	const hash = getHash(seed);
	const hue = getHue(hash);
	const saturationHash = extractHashPart(hash, 0);
	const lightnessHash = extractHashPart(hash, 1);
	const chroma = normalizeHash(saturationHash, chromaRange[0], chromaRange[1]);
	const lightness = normalizeHash(lightnessHash, lightnessRange[0], lightnessRange[1]);
	const [red, green, blue] = oklchToRgb(lightness / 100, chroma / 100, hue);
	return {
		hash,
		oklch: {
			l: lightness,
			c: chroma,
			h: hue
		},
		rgb: {
			r: red,
			g: green,
			b: blue
		},
		textColor: `var(${textColorVarName})`
	};
};
var generateColor = ({ seed, theme }) => {
	const { lightness: lightnessRange, chroma: chromaRange } = colorOptions[theme];
	return generateColorFromRanges(seed, lightnessRange, chromaRange);
};
var generateCustomColor = ({ seed, lightnessRange, chromaRange }) => {
	return generateColorFromRanges(seed, lightnessRange, chromaRange);
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useColorGenerator/useColorGenerator.js
/**
* The `useColorGenerator` hook generates a unique (but consistent) background color based on some unique attribute (e.g., name, id, email).
* The background color remains unchanged with each update.
* Theme is resolved automatically from context; passing `theme` in props has no effect.
* @param {object} props
* @param {string} props.seed - unique attribute of the entity (e.g., name, id, email).
* @example
*
*  import React from 'react';
*  import {Avatar} from '@gravity-ui/uikit';
*
*  const Component = ({ token, text, ...avatarProps }) => {
*      const {rgb, textColor} = useColorGenerator({
*          seed,
*      });
*
*      return (
*          <Avatar
*              {...avatarProps}
*              text={text}
*              color={text ? textColor : undefined}
*              backgroundColor={`rgb(${rgb.r}, ${rgb.g}, ${rgb.b})`}
*          />
*      );
*  };
* @returns {ColorDetails} returns an object with color details:
* - hash: number - hash value generated from seed
* - oklch: object with lightness (l), chroma (c), and hue (h) values
* - rgb: object with red (r), green (g), and blue (b) values
* - textColor: string - text color (dark or light), ensuring higher contrast on generated color
*/
function useColorGenerator({ seed }) {
	const theme = useThemeType();
	return import_react.useMemo(() => {
		return generateColor({
			seed,
			theme
		});
	}, [seed, theme]);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useFileInput/useFileInput.js
/**
* Used to shape props for input with type "file".
*
* Usage example:
```tsx
import * as React from 'react';
import {Button, useFileInput} from '@gravity-ui/uikit';

const Component = () => {
const onUpdate = React.useCallback((files: File[]) => console.log(files), []);
const {controlProps, triggerProps} = useFileInput({onUpdate});

return (
<React.Fragment>
<input {...controlProps} />
<Button {...triggerProps}>Upload</Button>
</React.Fragment>
);
};
```
*/
function useFileInput({ onUpdate, onChange }) {
	const ref = import_react.useRef(null);
	const handleChange = import_react.useCallback((event) => {
		onChange?.(event);
		onUpdate?.(Array.from(event.target.files || []));
		event.target.value = "";
	}, [onChange, onUpdate]);
	const openDeviceStorage = import_react.useCallback(() => {
		ref.current?.click();
	}, []);
	return import_react.useMemo(() => ({
		controlProps: {
			ref,
			type: "file",
			tabIndex: -1,
			style: {
				opacity: 0,
				position: "absolute",
				width: 1,
				height: 1
			},
			onChange: handleChange,
			"aria-hidden": true
		},
		triggerProps: { onClick: openDeviceStorage }
	}), [handleChange, openDeviceStorage]);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useFocusWithin/SyntheticFocusEvent.js
var SyntheticFocusEvent = class {
	nativeEvent;
	target;
	currentTarget;
	relatedTarget;
	bubbles;
	cancelable;
	defaultPrevented;
	eventPhase;
	isTrusted;
	timeStamp;
	type;
	constructor(type, nativeEvent, override = {}) {
		this.nativeEvent = nativeEvent;
		this.target = override.target ?? nativeEvent.target;
		this.currentTarget = override.currentTarget ?? nativeEvent.currentTarget;
		this.relatedTarget = nativeEvent.relatedTarget;
		this.bubbles = nativeEvent.bubbles;
		this.cancelable = nativeEvent.cancelable;
		this.defaultPrevented = nativeEvent.defaultPrevented;
		this.eventPhase = nativeEvent.eventPhase;
		this.isTrusted = nativeEvent.isTrusted;
		this.timeStamp = nativeEvent.timeStamp;
		this.type = type;
	}
	isDefaultPrevented() {
		return this.nativeEvent.defaultPrevented;
	}
	preventDefault() {
		this.defaultPrevented = true;
		this.nativeEvent.preventDefault();
	}
	stopPropagation() {
		this.nativeEvent.stopPropagation();
		this.isPropagationStopped = () => true;
	}
	isPropagationStopped() {
		return false;
	}
	persist() {}
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useFocusWithin/useSyntheticBlurEvent.js
function useSyntheticBlurEvent(onBlur) {
	const stateRef = import_react.useRef({
		isFocused: false,
		observer: null
	});
	import_react.useEffect(() => {
		const state = stateRef.current;
		return () => {
			if (state.observer) {
				state.observer.disconnect();
				state.observer = null;
			}
		};
	}, []);
	return import_react.useCallback((event) => {
		const target = event.target;
		if (target instanceof HTMLButtonElement || target instanceof HTMLInputElement || target instanceof HTMLTextAreaElement || target instanceof HTMLSelectElement) {
			stateRef.current.isFocused = true;
			const handleBlur = (e) => {
				stateRef.current.isFocused = false;
				if (target.disabled) onBlur?.(new SyntheticFocusEvent("blur", e));
				if (stateRef.current.observer) {
					stateRef.current.observer.disconnect();
					stateRef.current.observer = null;
				}
			};
			target.addEventListener("focusout", handleBlur, { once: true });
			const observer = new MutationObserver(() => {
				if (stateRef.current.isFocused && target.disabled) {
					observer.disconnect();
					stateRef.current.observer = null;
					const relatedTarget = target === document.activeElement ? null : document.activeElement;
					target.dispatchEvent(new FocusEvent("blur", { relatedTarget }));
					target.dispatchEvent(new FocusEvent("focusout", {
						relatedTarget,
						bubbles: true
					}));
				}
			});
			observer.observe(target, {
				attributes: true,
				attributeFilter: ["disabled"]
			});
			stateRef.current.observer = observer;
		}
	}, [onBlur]);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useFocusWithin/useFocusWithin.js
/**
* Callback on focus outside event.
* @callback onFocusEventCallback
* @param {FocusEvent} event
*/
/**
* Callback on focus change event.
* @callback onFocusChangeCallback
* @param {boolean} isFocusWithin
*/
/**
* Handles focus events for the target and its descendants.
* @param {object} props
* @param {boolean} [props.isDisabled] - whether the focus within events should be disabled.
* @param {onFocusEventCallback} props.onFocusWithin - handler that is called when the target element or a descendant receives focus.
* @param {onFocusEventCallback} props.onBlurWithin - handler that is called when the target element and all descendants lose focus.
* @param {onFocusChangeCallback} props.onFocusChange - handler that is called when the the focus within state changes.
* @returns container props
* @example
*
* function Select() {
*   const [open, setOpen] = React.useState(false);
*
*   const handleFocusOutside = React.useCallback(() => {setOpen(false);}, []);
*
*   const {focusWithinProps} = useFocusWithin({onBlurWithin: handleFocusOutside});
*
*   return (
*     <span {...focusWithinProps}>
*       <Button onClick={() => {setOpen(true)}}>Select</Button>
*       <Popup open={open}>
*          ...
*       </Popup>
*     </span>
*   );
*  }
*/
function useFocusWithin(props) {
	const { onFocusWithin, onBlurWithin, onFocusWithinChange, isDisabled } = props;
	const isFocusWithinRef = import_react.useRef(false);
	const { onBlur: onBlurHandler, onFocus: onFocusHandler } = useFocusEvents({
		onFocus: import_react.useCallback((event) => {
			if (!isFocusWithinRef.current && document.activeElement === event.target) {
				isFocusWithinRef.current = true;
				if (onFocusWithin) onFocusWithin(event);
				if (onFocusWithinChange) onFocusWithinChange(true);
			}
		}, [onFocusWithin, onFocusWithinChange]),
		onBlur: import_react.useCallback((event) => {
			if (!isFocusWithinRef.current) return;
			isFocusWithinRef.current = false;
			if (onBlurWithin) onBlurWithin(event);
			if (onFocusWithinChange) onFocusWithinChange(false);
		}, [onBlurWithin, onFocusWithinChange])
	});
	if (isDisabled) return { focusWithinProps: {
		onFocus: void 0,
		onBlur: void 0
	} };
	return { focusWithinProps: {
		onFocus: onFocusHandler,
		onBlur: onBlurHandler
	} };
}
function useFocusEvents({ onFocus, onBlur }) {
	const capturedRef = import_react.useRef(false);
	const onBlurHandler = import_react.useCallback((event) => {
		if (event.currentTarget.contains(event.relatedTarget)) return;
		capturedRef.current = false;
		const newEvent = new SyntheticFocusEvent("blur", event.nativeEvent, { currentTarget: event.currentTarget });
		setTimeout(() => {
			if (!capturedRef.current) onBlur(newEvent);
		}, 0);
	}, [onBlur]);
	const onSyntheticFocus = useSyntheticBlurEvent(onBlur);
	return {
		onBlur: onBlurHandler,
		onFocus: import_react.useCallback((event) => {
			capturedRef.current = true;
			onSyntheticFocus(event);
			onFocus(event);
		}, [onSyntheticFocus, onFocus])
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useForkRef/setRef.js
function setRef(ref, value) {
	if (typeof ref === "function") ref(value);
	else if (ref) ref.current = value;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useForkRef/mergeRefs.js
function mergeRefs(...refs) {
	return function mergedRefs(value) {
		for (const ref of refs) setRef(ref, value);
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useForkRef/useForkRef.js
function useForkRef(...refs) {
	return import_react.useMemo(() => {
		if (refs.every((ref) => ref === null || ref === void 0)) return null;
		return mergeRefs(...refs);
	}, refs);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useIntersection/useIntersection.js
var useIntersection = ({ element, options, onIntersect }) => {
	import_react.useEffect(() => {
		const observer = new IntersectionObserver(([entry]) => {
			if (entry.isIntersecting) onIntersect?.();
		}, options);
		if (element) observer.observe(element);
		return () => element === null ? void 0 : observer.unobserve(element);
	}, [
		element,
		options,
		onIntersect
	]);
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useLayoutEffect/useLayoutEffect.js
var useLayoutEffect$4 = typeof window === "undefined" ? () => {} : import_react.useLayoutEffect;
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useListNavigation/moveBack.js
function moveBack(items, activeItemIndex, steps = 1, skip) {
	const newActiveItemIndex = (items.length + activeItemIndex - steps % items.length) % items.length;
	if (skip && skip(items[newActiveItemIndex])) return moveBack(items, newActiveItemIndex, 1, skip);
	return newActiveItemIndex;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useListNavigation/moveForward.js
function moveForward(items, activeItemIndex, steps = 1, skip) {
	const newActiveItemIndex = (activeItemIndex + steps) % items.length;
	if (skip && skip(items[newActiveItemIndex])) return moveForward(items, newActiveItemIndex, 1, skip);
	return newActiveItemIndex;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useListNavigation/useListNavigation.js
function useListNavigation$1({ items, skip, pageSize, processHomeKey = true, processEndKey = true, anchorRef, disabled = false, initialValue = -1, onAnchorKeyDown }) {
	const [activeItemIndex, setActiveItemIndex] = import_react.useState(initialValue);
	const reset = import_react.useCallback(() => {
		setActiveItemIndex(initialValue);
	}, [initialValue]);
	import_react.useEffect(() => {
		if (items) reset();
	}, [items, reset]);
	useLayoutEffect$4(() => {
		if (disabled) return;
		if (!items.some((item) => !skip?.(item))) return;
		const anchor = anchorRef?.current;
		if (!anchor) return;
		const handleKeyDown = (event) => {
			if (onAnchorKeyDown?.(activeItemIndex, event) === false) return;
			switch (event.key) {
				case KeyCode$1.ARROW_DOWN:
					event.preventDefault();
					setActiveItemIndex((previousActiveItemIndex) => moveForward(items, previousActiveItemIndex, 1, skip));
					break;
				case KeyCode$1.ARROW_UP:
					event.preventDefault();
					setActiveItemIndex((previousActiveItemIndex) => moveBack(items, previousActiveItemIndex, 1, skip));
					break;
				case KeyCode$1.PAGE_DOWN:
					if (!pageSize) return;
					event.preventDefault();
					setActiveItemIndex((previousActiveItemIndex) => moveForward(items, previousActiveItemIndex, pageSize, skip));
					break;
				case KeyCode$1.PAGE_UP:
					if (!pageSize) return;
					event.preventDefault();
					setActiveItemIndex((previousActiveItemIndex) => moveBack(items, previousActiveItemIndex, pageSize, skip));
					break;
				case KeyCode$1.HOME:
					if (!processHomeKey) return;
					event.preventDefault();
					setActiveItemIndex((previousActiveItemIndex) => moveBack(items, previousActiveItemIndex, previousActiveItemIndex, skip));
					break;
				case KeyCode$1.END:
					if (!processEndKey) return;
					event.preventDefault();
					setActiveItemIndex((previousActiveItemIndex) => moveBack(items, previousActiveItemIndex, previousActiveItemIndex + 1, skip));
					break;
			}
		};
		anchor.addEventListener("keydown", handleKeyDown);
		return () => {
			anchor.removeEventListener("keydown", handleKeyDown);
		};
	}, [
		activeItemIndex,
		anchorRef,
		disabled,
		items,
		onAnchorKeyDown,
		pageSize,
		processEndKey,
		processHomeKey,
		skip
	]);
	return {
		activeItemIndex,
		setActiveItemIndex,
		reset
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useOutsideClick/useOutsideClick.js
/**
* Hook for observing clicks outside a given target
* @param ref - purpose of observation
* @param handler - callback when a click is triggered outside the observation target
* @returns - nothing
*/
var useOutsideClick = ({ ref, handler }) => {
	import_react.useEffect(() => {
		const callback = (e) => {
			const elem = ref?.current;
			if (elem && !elem.contains(e.target) && handler) handler(e);
		};
		window.addEventListener("mouseup", callback, { capture: true });
		window.addEventListener("touchend", callback, { capture: true });
		return () => {
			window.removeEventListener("mouseup", callback, { capture: true });
			window.removeEventListener("touchend", callback, { capture: true });
		};
	}, [handler, ref]);
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/usePortalContainer/PortalProvider.js
var PortalContext$1 = import_react.createContext({ current: null });
PortalContext$1.displayName = "PortalContext";
function PortalProvider({ container, children }) {
	return (0, import_jsx_runtime.jsx)(PortalContext$1.Provider, {
		value: container,
		children
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/usePortalContainer/usePortalContainer.js
function usePortalContainer() {
	const context = import_react.useContext(PortalContext$1);
	let defaultContainer = null;
	if (typeof window === "object") defaultContainer = window.document.body;
	return context.current ?? defaultContainer;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useResizeObserver/useResizeObserver.js
function useResizeObserver({ ref, onResize, box }) {
	import_react.useEffect(() => {
		const element = ref?.current;
		if (!element) return;
		if (typeof window.ResizeObserver === "undefined") {
			const handleResize = () => {
				onResize({});
			};
			window.addEventListener("resize", handleResize, false);
			return () => {
				window.removeEventListener("resize", handleResize, false);
			};
		}
		const observer = new ResizeObserver((entries) => {
			if (!entries.length) return;
			onResize({ observer });
		});
		observer.observe(element, { box });
		return () => {
			observer.disconnect();
		};
	}, [
		ref,
		onResize,
		box
	]);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useSelect/useOpenState.js
var useOpenState = (props) => {
	const { onOpenChange, onClose } = props;
	const handleOpenChange = import_react.useCallback((newOpen) => {
		onOpenChange?.(newOpen);
		if (newOpen === false && onClose) onClose();
	}, [onOpenChange, onClose]);
	const [open, setOpenState] = useControlledState(props.open, props.defaultOpen ?? false, handleOpenChange);
	return {
		open,
		toggleOpen: import_react.useCallback((val) => {
			setOpenState(typeof val === "boolean" ? val : !open);
		}, [open, setOpenState])
	};
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useSelect/useSelect.js
var useSelect = ({ defaultOpen, onClose, onOpenChange, open, value: valueProps, defaultValue = [], multiple, onUpdate, disabled }) => {
	const [value, setValueInner] = useControlledState(valueProps, defaultValue, onUpdate);
	const [activeIndex, setActiveIndex] = import_react.useState();
	const { toggleOpen, ...openState } = useOpenState({
		defaultOpen,
		onClose,
		onOpenChange,
		open
	});
	const setValue = import_react.useCallback((v) => {
		if (!disabled) setValueInner(v);
	}, [setValueInner, disabled]);
	const handleSingleSelection = import_react.useCallback((option) => {
		if (!value.includes(option.value)) {
			const nextValue = [option.value];
			setValue(nextValue);
		}
		toggleOpen(false);
	}, [
		value,
		setValue,
		toggleOpen
	]);
	const handleMultipleSelection = import_react.useCallback((option) => {
		const nextValue = value.includes(option.value) ? value.filter((iteratedVal) => iteratedVal !== option.value) : [...value, option.value];
		setValue(nextValue);
	}, [value, setValue]);
	return {
		value,
		activeIndex,
		setValue,
		handleSelection: import_react.useCallback((option) => {
			if (multiple) handleMultipleSelection(option);
			else handleSingleSelection(option);
		}, [
			multiple,
			handleSingleSelection,
			handleMultipleSelection
		]),
		handleClearValue: import_react.useCallback(() => {
			setValue([]);
		}, [setValue]),
		toggleOpen,
		setActiveIndex,
		...openState
	};
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useTimeout/useTimeout.js
function useTimeout(callback, ms) {
	import_react.useEffect(() => {
		if (typeof ms !== "number") return;
		const timer = setTimeout(() => {
			callback();
		}, ms);
		return () => {
			clearTimeout(timer);
		};
	}, [callback, ms]);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useViewportSize/useViewportSize.js
var getViewportSize = () => ({
	width: window?.visualViewport?.width ?? window?.innerWidth ?? void 0,
	height: window?.visualViewport?.height ?? window?.innerHeight ?? void 0
});
/**
* A hook to get the size of the viewport when resizing
* @returns - {width, height}
*/
var useViewportSize = () => {
	const [size, setSize] = import_react.useState(getViewportSize());
	import_react.useEffect(() => {
		const onResize = () => {
			let newSize = getViewportSize();
			if (newSize.width === size?.width && newSize.height === size?.height) newSize = size;
			setSize(newSize);
		};
		(window.visualViewport ?? window).addEventListener("resize", onResize);
		return () => {
			(window.visualViewport ?? window).removeEventListener("resize", onResize);
		};
	}, [size]);
	return size;
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useVirtualElementRef/useVirtualElementRef.js
var initialPosition = {
	top: 0,
	right: 0,
	bottom: 0,
	left: 0
};
function useVirtualElementRef(props = {}) {
	const { rect, contextElement } = props;
	const rectRef = import_react.useRef(initialPosition);
	const ref = import_react.useRef({
		contextElement,
		getBoundingClientRect() {
			const { top, right, bottom, left } = rectRef.current;
			return {
				top,
				right,
				bottom,
				left,
				width: right - left,
				height: bottom - top
			};
		}
	});
	ref.current.contextElement = contextElement;
	if (rect) {
		const { top = 0, left = 0, right = left, bottom = top } = rect;
		rectRef.current = {
			top,
			right,
			bottom,
			left
		};
	} else rectRef.current = initialPosition;
	return ref;
}
function useVirtualElement(rect) {
	const rectRef = import_react.useRef(rect);
	const [anchor, setAnchor] = import_react.useState({
		getBoundingClientRect() {
			const { top = 0, left = 0, right = left, bottom = top } = rectRef.current;
			return {
				top,
				left,
				bottom,
				right,
				width: right - left,
				height: bottom - top
			};
		},
		contextElement: void 0
	});
	const setContextElement = import_react.useCallback((node) => {
		setAnchor({
			getBoundingClientRect() {
				const { top = 0, left = 0, right = left, bottom = top } = rectRef.current;
				return {
					top,
					left,
					bottom,
					right,
					width: right - left,
					height: bottom - top
				};
			},
			contextElement: node ?? void 0
		});
	}, []);
	const { top, left, bottom, right } = rect;
	import_react.useEffect(() => {
		rectRef.current = {
			top,
			left,
			bottom,
			right
		};
	}, [
		top,
		left,
		bottom,
		right
	]);
	return {
		anchor,
		setContextElement
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Text/colorText/colorText.js
var b$114 = block("color-text");
var TEXT_COLORS = [
	"primary",
	"complementary",
	"secondary",
	"hint",
	"info",
	"info-heavy",
	"positive",
	"positive-heavy",
	"warning",
	"warning-heavy",
	"danger",
	"danger-heavy",
	"utility",
	"utility-heavy",
	"misc",
	"misc-heavy",
	"brand",
	"link",
	"link-hover",
	"link-visited",
	"link-visited-hover",
	"dark-primary",
	"dark-complementary",
	"dark-secondary",
	"light-primary",
	"light-complementary",
	"light-secondary",
	"light-hint",
	"inverted-primary",
	"inverted-complementary",
	"inverted-secondary",
	"inverted-hint"
];
/**
* Utility to generate text colors classes.
* Storybook: https://preview.gravity-ui.com/uikit/?path=/story/colors--texts
*
* ---
* For example:
* ```jsx
* // "color-text color-text_color_inherit some-class"
* colorText({color: 'inherit'}, 'some-class')
* ```
*/
var colorText = ({ color }, className) => b$114({ color }, className);
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/PrivateDefaultPropsProvider.js
var EMPTY = {};
var PrivateDefaultPropsContext = import_react.createContext(EMPTY);
function PrivateDefaultPropsProvider({ value = EMPTY, children }) {
	return (0, import_jsx_runtime.jsx)(PrivateDefaultPropsContext.Provider, {
		value,
		children
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/useDefaultProps.js
function useDefaultProps(componentName, componentProps) {
	const defaultProps = import_react.useContext(PrivateDefaultPropsContext)[componentName];
	if (!defaultProps) return componentProps;
	const cleanedProps = {};
	for (const k in componentProps) if (componentProps[k] !== void 0) cleanedProps[k] = componentProps[k];
	return [defaultProps, cleanedProps].reduce((acc, props) => {
		for (const [key, value] of Object.entries(props)) acc[key] = value;
		return acc;
	}, {});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/svg.js
var a11yHiddenSvgProps = { "aria-hidden": true };
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Icon/utils.js
function isSpriteData(data) {
	return typeof data === "object";
}
function isSvgrData(data) {
	return typeof data === "function" && (!data.prototype || !data.prototype.render);
}
function isComponentSvgData(data) {
	return (typeof data === "object" || typeof data === "function") && "defaultProps" in data;
}
function isStringSvgData(data) {
	return typeof data === "string";
}
function prepareStringData(data) {
	return data.replace(/<svg[^>]*>/, (match) => {
		return match.replace(/(width|height)=(["']?)\d+\2/g, "").replace(/(\s){2,}\b/g, "$1").replace(/(\s)+>/g, ">");
	});
}
function getStringViewBox(data) {
	const match = data.match(/viewBox=(["']?)([\d\s,-]+)\1/);
	return match ? match[2] : void 0;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Icon/Icon.js
var b$113 = block("icon");
var Icon = import_react.forwardRef((rawProps, ref) => {
	const { data, width, height, size, className, style, color, fill = "currentColor", stroke = "none", qa } = useDefaultProps("Icon", rawProps);
	let w, h;
	if (size) {
		w = size;
		h = size;
	}
	if (width) w = width;
	if (height) h = height;
	let viewBox;
	if (isSpriteData(data)) ({viewBox} = data);
	else if (isStringSvgData(data)) viewBox = getStringViewBox(data);
	else if (isComponentSvgData(data)) ({viewBox} = data.defaultProps);
	else if (isSvgrData(data)) {
		const el = data({});
		if (el) ({viewBox} = el.props);
	}
	if (viewBox && (!w || !h)) {
		const values = viewBox.split(/\s+|\s*,\s*/);
		if (!w) w = values[2];
		if (!h) h = values[3];
	}
	const hasStyleColor = style?.color !== void 0;
	const svgClassName = color && !hasStyleColor ? colorText({ color }, b$113(null, className)) : b$113(null, className);
	const props = {
		xmlns: "http://www.w3.org/2000/svg",
		xmlnsXlink: "http://www.w3.org/1999/xlink",
		width: w,
		height: h,
		className: svgClassName,
		style,
		fill,
		stroke,
		"data-qa": qa,
		...a11yHiddenSvgProps
	};
	if (isStringSvgData(data)) {
		const preparedData = prepareStringData(data);
		return (0, import_jsx_runtime.jsx)("svg", {
			...props,
			ref,
			dangerouslySetInnerHTML: { __html: preparedData }
		});
	}
	if (isSpriteData(data)) {
		const href = Icon.prefix + (data.url || `#${data.id}`);
		return (0, import_jsx_runtime.jsx)("svg", {
			...props,
			viewBox,
			ref,
			children: (0, import_jsx_runtime.jsx)("use", {
				href,
				xlinkHref: href
			})
		});
	}
	const IconComponent = data;
	if (IconComponent.defaultProps) IconComponent.defaultProps.width = IconComponent.defaultProps.height = void 0;
	return (0, import_jsx_runtime.jsx)("svg", {
		...props,
		ref,
		children: (0, import_jsx_runtime.jsx)(IconComponent, {
			width: void 0,
			height: void 0
		})
	});
});
Icon.displayName = "Icon";
Icon.prefix = "";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/isOfType.js
function isOfType(Component) {
	return function isMatching(component) {
		if (!import_react.isValidElement(component)) return false;
		const { type } = component;
		if (type === Component) return true;
		if (typeof Component === "string" || typeof type === "string") return false;
		const displayName = type.displayName;
		return Boolean(displayName && displayName === Component.displayName);
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/common.js
var nextUniqueId = 1;
function getUniqId() {
	return `g-uniq-${nextUniqueId++}`;
}
var isSvg = isOfType("svg");
var isIcon = isOfType(Icon);
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useUniqId/useUniqId.js
function useUniqIdFallback() {
	const idRef = import_react.useRef();
	if (idRef.current === void 0) idRef.current = getUniqId();
	return idRef.current;
}
function useIdNative() {
	return `g-${import_react.useId()}`;
}
var useUniqId = typeof import_react.useId === "function" ? useIdNative : useUniqIdFallback;
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isEqual.js
var require_isEqual = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseIsEqual = require__baseIsEqual();
	/**
	* Performs a deep comparison between two values to determine if they are
	* equivalent.
	*
	* **Note:** This method supports comparing arrays, array buffers, booleans,
	* date objects, error objects, maps, numbers, `Object` objects, regexes,
	* sets, strings, symbols, and typed arrays. `Object` objects are compared
	* by their own, not inherited, enumerable properties. Functions and DOM
	* nodes are compared by strict equality, i.e. `===`.
	*
	* @static
	* @memberOf _
	* @since 0.1.0
	* @category Lang
	* @param {*} value The value to compare.
	* @param {*} other The other value to compare.
	* @returns {boolean} Returns `true` if the values are equivalent, else `false`.
	* @example
	*
	* var object = { 'a': 1 };
	* var other = { 'a': 1 };
	*
	* _.isEqual(object, other);
	* // => true
	*
	* object === other;
	* // => false
	*/
	function isEqual(value, other) {
		return baseIsEqual(value, other);
	}
	module.exports = isEqual;
}));
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/useCollapseChildren/useCollapseChildren.js
var import_isEqual = /* @__PURE__ */ __toESM(require_isEqual(), 1);
function useCollapseChildren({ enabled = true, containerRef, preservedRefs = [], minCount = 0, maxCount = Infinity, direction = "end", gap = 0, childSelector = "*", getChildWidth = (child) => child.getBoundingClientRect().width }) {
	const exertedProps = [
		minCount,
		maxCount,
		direction,
		gap,
		childSelector
	];
	const [exertedPropsState, setExertedPropsState] = import_react.useState(exertedProps);
	const [calculated, setCalculated] = import_react.useState(false);
	const isCalculatingRef = import_react.useRef(true);
	const [visibleCount, setVisibleCount] = import_react.useState(maxCount);
	const calculate = (desiredVisibleCount) => {
		const container = containerRef.current;
		if (!container) return;
		const containerChildren = Array.from(container.querySelectorAll(`:scope > :where(${childSelector})`));
		const containerWidth = container.getBoundingClientRect().width;
		const childWidthMap = new WeakMap(containerChildren.map((child) => [child, getChildWidth(child)]));
		if (direction === "start") containerChildren.reverse();
		const preservedItems = preservedRefs.flatMap((ref) => ref.current ?? []);
		containerChildren.sort((itemA, itemB) => {
			const valueA = preservedItems.includes(itemA) ? 1 : 0;
			return (preservedItems.includes(itemB) ? 1 : 0) - valueA;
		});
		let availableWidth = containerWidth;
		let newVisibleCount = 0;
		for (const item of containerChildren) {
			if (newVisibleCount >= maxCount) break;
			availableWidth -= childWidthMap.get(item) ?? 0;
			if (availableWidth < 0) break;
			availableWidth -= gap;
			if (!preservedItems.includes(item)) newVisibleCount++;
		}
		const minNewVisibleCount = Math.max(newVisibleCount, minCount);
		if (minNewVisibleCount === desiredVisibleCount) setCalculated(true);
		else setVisibleCount(minNewVisibleCount);
	};
	const recalculate = import_react.useCallback(() => {
		if (enabled && !isCalculatingRef.current) {
			isCalculatingRef.current = true;
			setVisibleCount(maxCount);
			setCalculated(false);
		}
	}, [enabled, maxCount]);
	if (!(0, import_isEqual.default)(exertedProps, exertedPropsState)) {
		recalculate();
		setExertedPropsState(exertedProps);
	}
	useResizeObserver({
		ref: containerRef,
		onResize: recalculate
	});
	useLayoutEffect$4(() => {
		if (calculated) {
			isCalculatingRef.current = false;
			return;
		}
		if (enabled) calculate(visibleCount);
		else {
			setVisibleCount(Infinity);
			setCalculated(true);
		}
	}, [
		enabled,
		calculated,
		visibleCount
	]);
	return {
		calculated,
		recalculate,
		visibleCount
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/constants.js
var CSS_SIZE_EXCEPTION = { "0.5": "half" };
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/contexts/LayoutContext.js
var LayoutContext = import_react.createContext({
	theme: {
		breakpoints: {
			xs: 0,
			s: 576,
			m: 768,
			l: 1080,
			xl: 1200,
			xxl: 1400,
			xxxl: 1920
		},
		spaceBaseSize: 4,
		components: { container: {
			gutters: 3,
			media: { l: { gutters: 5 } }
		} }
	},
	activeMediaQuery: "s",
	fixBreakpoints: false
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/hooks/useCurrentActiveMediaQuery.js
var mockMediaQueryList = {
	media: "",
	matches: false,
	onchange: () => {},
	addListener: () => {},
	removeListener: () => {},
	addEventListener: () => {},
	removeEventListener: () => {},
	dispatchEvent: (_) => true
};
var makeCurrentActiveMediaExpressions = (mediaToValue) => ({
	xs: `(max-width: ${mediaToValue.s - 1}px)`,
	s: `(min-width: ${mediaToValue.s}px) and (max-width: ${mediaToValue.m - 1}px)`,
	m: `(min-width: ${mediaToValue.m}px) and (max-width: ${mediaToValue.l - 1}px)`,
	l: `(min-width: ${mediaToValue.l}px) and (max-width: ${mediaToValue.xl - 1}px)`,
	xl: `(min-width: ${mediaToValue.xl}px) and (max-width: ${mediaToValue.xxl - 1}px)`,
	xxl: `(min-width: ${mediaToValue.xxl}px) and (max-width: ${mediaToValue.xxxl - 1}px)`,
	xxxl: `(min-width: ${mediaToValue.xxxl}px)`
});
var safeMatchMedia = (query) => {
	if (typeof window === "undefined" || typeof window.matchMedia !== "function") return mockMediaQueryList;
	return window.matchMedia(query);
};
var Queries = class {
	fix;
	queryListsDecl = [];
	constructor(breakpointsMap, fixBreakpoints) {
		const mediaToExpressionMap = makeCurrentActiveMediaExpressions(breakpointsMap);
		this.fix = fixBreakpoints;
		this.queryListsDecl = [
			["xs", safeMatchMedia(mediaToExpressionMap.xs)],
			["s", safeMatchMedia(mediaToExpressionMap.s)],
			["m", safeMatchMedia(mediaToExpressionMap.m)],
			["l", safeMatchMedia(mediaToExpressionMap.l)],
			["xl", safeMatchMedia(mediaToExpressionMap.xl)],
			["xxl", safeMatchMedia(mediaToExpressionMap.xxl)],
			["xxxl", safeMatchMedia(mediaToExpressionMap.xxxl)]
		];
	}
	getCurrentActiveMedia() {
		const activeMedia = this.queryListsDecl.find(([_, queryList]) => queryList.matches)?.[0];
		if (!activeMedia) return this.fix ? "xs" : "s";
		else if (activeMedia === "xs" && !this.fix) return "s";
		return activeMedia;
	}
	addListeners(fn) {
		this.queryListsDecl.forEach(([_, queryList]) => queryList.addEventListener("change", fn));
	}
	removeListeners(fn) {
		this.queryListsDecl.forEach(([_, queryList]) => queryList.removeEventListener("change", fn));
	}
};
/**
* @private
*/
var useCurrentActiveMediaQuery = (breakpointsMap, fixBreakpoints, initialMediaQuery) => {
	const [state, _setState] = import_react.useState(initialMediaQuery ?? (fixBreakpoints ? "xs" : "s"));
	useLayoutEffect$4(() => {
		const queries = new Queries(breakpointsMap, fixBreakpoints);
		const setState = () => {
			_setState(queries.getCurrentActiveMedia());
		};
		queries.addListeners(setState);
		setState();
		return () => {
			queries.removeListeners(setState);
		};
	}, [breakpointsMap, fixBreakpoints]);
	return state;
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/utils/overrideLayoutTheme.js
/**
* Use this function to override default `DEFAULT_LAYOUT_THEME`
*/
function overrideLayoutTheme({ theme, override }) {
	return (0, import_merge.default)(theme, override);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/LayoutProvider/LayoutProvider.js
function PrivateLayoutProvider({ children, config: override, initialMediaQuery, fixBreakpoints = false }) {
	const parentContext = import_react.useContext(LayoutContext);
	const theme = import_react.useMemo(() => overrideLayoutTheme({
		theme: parentContext.theme,
		override
	}), [override, parentContext.theme]);
	const activeMediaQuery = useCurrentActiveMediaQuery(theme.breakpoints, fixBreakpoints, initialMediaQuery);
	const value = import_react.useMemo(() => ({
		activeMediaQuery,
		theme,
		fixBreakpoints
	}), [
		activeMediaQuery,
		theme,
		fixBreakpoints
	]);
	return (0, import_jsx_runtime.jsx)(LayoutContext.Provider, {
		value,
		children
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/ThemeSettingsContext.js
var ThemeSettingsContext = import_react.createContext(void 0);
ThemeSettingsContext.displayName = "ThemeSettingsContext";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/dom-helpers.js
var b$112 = block(ROOT_CLASSNAME);
var rootClassName = b$112();
function updateBodyClassName({ theme, className, prevClassName }) {
	const bodyEl = document.body;
	if (!bodyEl) return;
	if (!bodyEl.classList.contains(rootClassName)) bodyEl.classList.add(rootClassName);
	if (prevClassName) prevClassName.split(" ").forEach((cls) => {
		if (cls) bodyEl.classList.remove(cls);
	});
	if (className) className.split(" ").forEach((cls) => {
		if (cls && !bodyEl.classList.contains(cls)) bodyEl.classList.add(cls);
	});
	[...bodyEl.classList].forEach((cls) => {
		if (cls.startsWith(modsClassName(b$112({ theme: true })))) bodyEl.classList.remove(cls);
	});
	bodyEl.classList.add(modsClassName(b$112({ theme })));
}
function updateBodyDirection(direction) {
	const bodyEl = document.body;
	if (!bodyEl) return;
	if (direction === "ltr") bodyEl.removeAttribute("dir");
	else bodyEl.setAttribute("dir", direction);
}
var supportsMatchMedia = typeof window !== "undefined" && typeof window.matchMedia === "function";
var getDarkMediaMatch = () => window.matchMedia("(prefers-color-scheme: dark)");
function getSystemTheme() {
	if (supportsMatchMedia) return getDarkMediaMatch().matches ? "dark" : "light";
	else return "light";
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/useLang.js
var import_shim = require_shim();
var defaultLangOptions = {
	lang: "en",
	fallbackLang: "en"
};
var LangContext = import_react.createContext(void 0);
function useLang() {
	const config = (0, import_shim.useSyncExternalStore)(subscribeConfigure, getConfig, getConfig);
	return import_react.useContext(LangContext) || config;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/useSystemTheme.js
function addListener(matcher, handler) {
	const isLegacyMethod = typeof matcher.addEventListener !== "function";
	if (isLegacyMethod) matcher.addListener(handler);
	else matcher.addEventListener("change", handler);
	return () => {
		if (isLegacyMethod) matcher.removeListener(handler);
		else matcher.removeEventListener("change", handler);
	};
}
function useSystemTheme() {
	const [theme, setTheme] = import_react.useState(getSystemTheme());
	import_react.useEffect(() => {
		if (!supportsMatchMedia) return;
		function onChange(event) {
			setTheme(event.matches ? "dark" : "light");
		}
		const unsubscribe = addListener(getDarkMediaMatch(), onChange);
		return () => unsubscribe();
	}, []);
	return theme;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/ThemeProvider.js
var b$111 = block(ROOT_CLASSNAME);
function ThemeProvider({ theme: themeProp, systemLightTheme: systemLightThemeProp, systemDarkTheme: systemDarkThemeProp, direction: directionProp, scoped: scopedProp = false, rootClassName = "", children, layout, lang, fallbackLang, defaultProps }) {
	const parentThemeState = import_react.useContext(ThemeContext);
	const systemThemeState = import_react.useContext(ThemeSettingsContext);
	const langOptionsState = import_react.useContext(LangContext);
	const parentDefaultProps = import_react.useContext(PrivateDefaultPropsContext);
	const scoped = parentThemeState !== void 0 || scopedProp;
	const parentTheme = parentThemeState?.theme ?? "system";
	const theme = themeProp ?? parentTheme;
	const systemLightTheme = systemLightThemeProp ?? systemThemeState?.systemLightTheme ?? "light";
	const systemDarkTheme = systemDarkThemeProp ?? systemThemeState?.systemDarkTheme ?? "dark";
	const parentDirection = parentThemeState?.direction ?? "ltr";
	const direction = directionProp ?? parentDirection;
	const systemTheme = useSystemTheme() === "light" ? systemLightTheme : systemDarkTheme;
	const themeValue = theme === "system" ? systemTheme : theme;
	const prevRootClassName = import_react.useRef("");
	useLayoutEffect$4(() => {
		if (!scoped) {
			updateBodyClassName({
				theme: themeValue,
				className: rootClassName,
				prevClassName: prevRootClassName.current
			});
			updateBodyDirection(direction);
			prevRootClassName.current = rootClassName;
		}
	}, [
		scoped,
		themeValue,
		direction,
		rootClassName
	]);
	const contextValue = import_react.useMemo(() => ({
		theme,
		themeValue,
		direction,
		scoped
	}), [
		theme,
		themeValue,
		direction,
		scoped
	]);
	const themeSettingsContext = import_react.useMemo(() => ({
		systemLightTheme,
		systemDarkTheme
	}), [systemLightTheme, systemDarkTheme]);
	const mergedDefaultProps = import_react.useMemo(() => {
		if (!defaultProps) return parentDefaultProps;
		return [parentDefaultProps, defaultProps].reduce((acc, props) => {
			for (const [key, value] of Object.entries(props)) acc[key] = value;
			return acc;
		}, {});
	}, [parentDefaultProps, defaultProps]);
	const langOptionsFinal = lang || fallbackLang ? {
		...defaultLangOptions,
		...langOptionsState,
		...lang ? { lang } : void 0,
		...fallbackLang ? { fallbackLang } : void 0
	} : langOptionsState;
	return (0, import_jsx_runtime.jsx)(PrivateLayoutProvider, {
		...layout,
		children: (0, import_jsx_runtime.jsx)(PrivateDefaultPropsProvider, {
			value: mergedDefaultProps,
			children: (0, import_jsx_runtime.jsx)(ThemeContext.Provider, {
				value: contextValue,
				children: (0, import_jsx_runtime.jsx)(ThemeSettingsContext.Provider, {
					value: themeSettingsContext,
					children: (0, import_jsx_runtime.jsx)(LangContext.Provider, {
						value: langOptionsFinal,
						children: scoped ? (0, import_jsx_runtime.jsx)("div", {
							className: b$111({ theme: themeValue }, rootClassName),
							dir: direction,
							children
						}) : children
					})
				})
			})
		})
	});
}
ThemeProvider.displayName = "ThemeProvider";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/useTheme.js
function useTheme() {
	return useThemeContext().theme;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/useThemeSettings.js
function useThemeSettings() {
	const settings = import_react.useContext(ThemeSettingsContext);
	if (settings === void 0) throw new Error("useThemeSettings must be used within ThemeProvider");
	return settings;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/useDirection.js
function useDirection() {
	return useThemeContext().direction;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/withTheme.js
function withTheme(WrappedComponent) {
	const componentName = getComponentName(WrappedComponent);
	const component = function WithThemeComponent(props) {
		const theme = useTheme();
		return (0, import_jsx_runtime.jsx)(WrappedComponent, {
			...props,
			theme
		});
	};
	component.displayName = `withTheme(${componentName})`;
	return component;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/withThemeValue.js
function withThemeValue(WrappedComponent) {
	const componentName = getComponentName(WrappedComponent);
	const component = function WithThemeValueComponent(props) {
		const themeValue = useThemeValue();
		return (0, import_jsx_runtime.jsx)(WrappedComponent, {
			...props,
			themeValue
		});
	};
	component.displayName = `withThemeValue(${componentName})`;
	return component;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/theme/withDirection.js
function withDirection(WrappedComponent) {
	const componentName = getComponentName(WrappedComponent);
	const component = function WithDirectionComponent(props) {
		const direction = useDirection();
		return (0, import_jsx_runtime.jsx)(WrappedComponent, {
			...props,
			direction
		});
	};
	component.displayName = `withDirection(${componentName})`;
	return component;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Accordion/AccordionContext.js
var AccordionContext = import_react.createContext({
	size: "m",
	view: "solid",
	multiple: false,
	arrowPosition: "start",
	ariaLevel: 3,
	items: null,
	updateItems: () => {},
	registerSummary: () => {},
	unregisterSummary: () => {},
	getSummaryRefs: () => []
});
function AccordionProvider(props) {
	const { ariaLevel, children, arrowPosition, size, view, multiple, defaultValue, onUpdate, value } = props;
	const getDefaultValue = () => {
		if (defaultValue !== void 0) return defaultValue;
		return multiple ? [] : null;
	};
	const [items, setItems] = useControlledState(value, getDefaultValue(), onUpdate);
	const summaryRefsRef = import_react.useRef(/* @__PURE__ */ new Map());
	import_react.useEffect(() => {
		if (!Array.isArray(items) && multiple) {
			setItems(items ? [items] : []);
			return;
		}
	}, [multiple]);
	const updateItems = (itemValue) => {
		if (multiple) {
			const castedPrev = items || [];
			let newValue = [];
			if (castedPrev.includes(itemValue)) newValue = castedPrev.filter((item) => item !== itemValue);
			else newValue = [...castedPrev, itemValue];
			setItems(newValue);
			return;
		}
		let newValue = itemValue;
		if (itemValue === items) newValue = null;
		setItems(newValue);
	};
	const registerSummary = import_react.useCallback((id, ref) => {
		summaryRefsRef.current.set(id, ref);
	}, []);
	const unregisterSummary = import_react.useCallback((id) => {
		summaryRefsRef.current.delete(id);
	}, []);
	const getSummaryRefs = import_react.useCallback(() => {
		return Array.from(summaryRefsRef.current.values()).filter((ref) => !ref.disabled);
	}, []);
	return (0, import_jsx_runtime.jsx)(AccordionContext.Provider, {
		value: {
			ariaLevel,
			arrowPosition,
			size,
			view,
			multiple,
			items,
			updateItems,
			registerSummary,
			unregisterSummary,
			getSummaryRefs
		},
		children
	});
}
function useAccordion() {
	return import_react.useContext(AccordionContext);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Disclosure/DisclosureContext.js
var DisclosureAttributesContext = import_react.createContext(void 0);
var DisclosureToggleContext = import_react.createContext(void 0);
function DisclosureProvider(props) {
	const { size, disabled, defaultExpanded, arrowPosition, summary, keepMounted, onUpdate, onSummaryKeyDown, expanded: controlledExpanded } = props;
	const [expanded, setExpanded] = import_react.useState(() => Boolean(defaultExpanded));
	const controlledMode = controlledExpanded !== void 0;
	const handleToggle = () => {
		setExpanded((prev) => !prev);
		onUpdate(controlledMode ? !controlledExpanded : !expanded);
	};
	const ariaControls = useUniqId();
	const ariaLabelledby = `disclosure${ariaControls}`;
	return (0, import_jsx_runtime.jsx)(DisclosureAttributesContext.Provider, {
		value: {
			size,
			disabled,
			summary,
			arrowPosition,
			keepMounted,
			expanded: controlledMode ? controlledExpanded : expanded,
			ariaControls,
			ariaLabelledby,
			onSummaryKeyDown
		},
		children: (0, import_jsx_runtime.jsx)(DisclosureToggleContext.Provider, {
			value: handleToggle,
			children: props.children
		})
	});
}
function useDisclosureAttributes() {
	const state = import_react.useContext(DisclosureAttributesContext);
	if (state === void 0) throw new Error("useDisclosureAttributes must be used within DisclosureProvider");
	return state;
}
function useToggleDisclosure() {
	const state = import_react.useContext(DisclosureToggleContext);
	if (state === void 0) throw new Error("useToggleDisclosure must be used within DisclosureProvider");
	return state;
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/extends.js
function _extends() {
	return _extends = Object.assign ? Object.assign.bind() : function(n) {
		for (var e = 1; e < arguments.length; e++) {
			var t = arguments[e];
			for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]);
		}
		return n;
	}, _extends.apply(null, arguments);
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js
function _objectWithoutPropertiesLoose(r, e) {
	if (null == r) return {};
	var t = {};
	for (var n in r) if ({}.hasOwnProperty.call(r, n)) {
		if (-1 !== e.indexOf(n)) continue;
		t[n] = r[n];
	}
	return t;
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/setPrototypeOf.js
function _setPrototypeOf(t, e) {
	return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(t, e) {
		return t.__proto__ = e, t;
	}, _setPrototypeOf(t, e);
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/inheritsLoose.js
function _inheritsLoose(t, o) {
	t.prototype = Object.create(o.prototype), t.prototype.constructor = t, _setPrototypeOf(t, o);
}
//#endregion
//#region node_modules/.pnpm/react-is@16.13.1/node_modules/react-is/cjs/react-is.development.js
/** @license React v16.13.1
* react-is.development.js
*
* Copyright (c) Facebook, Inc. and its affiliates.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_react_is_development = /* @__PURE__ */ __commonJSMin(((exports) => {
	(function() {
		"use strict";
		var hasSymbol = typeof Symbol === "function" && Symbol.for;
		var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for("react.element") : 60103;
		var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for("react.portal") : 60106;
		var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for("react.fragment") : 60107;
		var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for("react.strict_mode") : 60108;
		var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for("react.profiler") : 60114;
		var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for("react.provider") : 60109;
		var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for("react.context") : 60110;
		var REACT_ASYNC_MODE_TYPE = hasSymbol ? Symbol.for("react.async_mode") : 60111;
		var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for("react.concurrent_mode") : 60111;
		var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for("react.forward_ref") : 60112;
		var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for("react.suspense") : 60113;
		var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for("react.suspense_list") : 60120;
		var REACT_MEMO_TYPE = hasSymbol ? Symbol.for("react.memo") : 60115;
		var REACT_LAZY_TYPE = hasSymbol ? Symbol.for("react.lazy") : 60116;
		var REACT_BLOCK_TYPE = hasSymbol ? Symbol.for("react.block") : 60121;
		var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for("react.fundamental") : 60117;
		var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for("react.responder") : 60118;
		var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for("react.scope") : 60119;
		function isValidElementType(type) {
			return typeof type === "string" || typeof type === "function" || type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === "object" && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);
		}
		function typeOf(object) {
			if (typeof object === "object" && object !== null) {
				var $$typeof = object.$$typeof;
				switch ($$typeof) {
					case REACT_ELEMENT_TYPE:
						var type = object.type;
						switch (type) {
							case REACT_ASYNC_MODE_TYPE:
							case REACT_CONCURRENT_MODE_TYPE:
							case REACT_FRAGMENT_TYPE:
							case REACT_PROFILER_TYPE:
							case REACT_STRICT_MODE_TYPE:
							case REACT_SUSPENSE_TYPE: return type;
							default:
								var $$typeofType = type && type.$$typeof;
								switch ($$typeofType) {
									case REACT_CONTEXT_TYPE:
									case REACT_FORWARD_REF_TYPE:
									case REACT_LAZY_TYPE:
									case REACT_MEMO_TYPE:
									case REACT_PROVIDER_TYPE: return $$typeofType;
									default: return $$typeof;
								}
						}
					case REACT_PORTAL_TYPE: return $$typeof;
				}
			}
		}
		var AsyncMode = REACT_ASYNC_MODE_TYPE;
		var ConcurrentMode = REACT_CONCURRENT_MODE_TYPE;
		var ContextConsumer = REACT_CONTEXT_TYPE;
		var ContextProvider = REACT_PROVIDER_TYPE;
		var Element = REACT_ELEMENT_TYPE;
		var ForwardRef = REACT_FORWARD_REF_TYPE;
		var Fragment = REACT_FRAGMENT_TYPE;
		var Lazy = REACT_LAZY_TYPE;
		var Memo = REACT_MEMO_TYPE;
		var Portal = REACT_PORTAL_TYPE;
		var Profiler = REACT_PROFILER_TYPE;
		var StrictMode = REACT_STRICT_MODE_TYPE;
		var Suspense = REACT_SUSPENSE_TYPE;
		var hasWarnedAboutDeprecatedIsAsyncMode = false;
		function isAsyncMode(object) {
			if (!hasWarnedAboutDeprecatedIsAsyncMode) {
				hasWarnedAboutDeprecatedIsAsyncMode = true;
				console["warn"]("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.");
			}
			return isConcurrentMode(object) || typeOf(object) === REACT_ASYNC_MODE_TYPE;
		}
		function isConcurrentMode(object) {
			return typeOf(object) === REACT_CONCURRENT_MODE_TYPE;
		}
		function isContextConsumer(object) {
			return typeOf(object) === REACT_CONTEXT_TYPE;
		}
		function isContextProvider(object) {
			return typeOf(object) === REACT_PROVIDER_TYPE;
		}
		function isElement(object) {
			return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
		}
		function isForwardRef(object) {
			return typeOf(object) === REACT_FORWARD_REF_TYPE;
		}
		function isFragment(object) {
			return typeOf(object) === REACT_FRAGMENT_TYPE;
		}
		function isLazy(object) {
			return typeOf(object) === REACT_LAZY_TYPE;
		}
		function isMemo(object) {
			return typeOf(object) === REACT_MEMO_TYPE;
		}
		function isPortal(object) {
			return typeOf(object) === REACT_PORTAL_TYPE;
		}
		function isProfiler(object) {
			return typeOf(object) === REACT_PROFILER_TYPE;
		}
		function isStrictMode(object) {
			return typeOf(object) === REACT_STRICT_MODE_TYPE;
		}
		function isSuspense(object) {
			return typeOf(object) === REACT_SUSPENSE_TYPE;
		}
		exports.AsyncMode = AsyncMode;
		exports.ConcurrentMode = ConcurrentMode;
		exports.ContextConsumer = ContextConsumer;
		exports.ContextProvider = ContextProvider;
		exports.Element = Element;
		exports.ForwardRef = ForwardRef;
		exports.Fragment = Fragment;
		exports.Lazy = Lazy;
		exports.Memo = Memo;
		exports.Portal = Portal;
		exports.Profiler = Profiler;
		exports.StrictMode = StrictMode;
		exports.Suspense = Suspense;
		exports.isAsyncMode = isAsyncMode;
		exports.isConcurrentMode = isConcurrentMode;
		exports.isContextConsumer = isContextConsumer;
		exports.isContextProvider = isContextProvider;
		exports.isElement = isElement;
		exports.isForwardRef = isForwardRef;
		exports.isFragment = isFragment;
		exports.isLazy = isLazy;
		exports.isMemo = isMemo;
		exports.isPortal = isPortal;
		exports.isProfiler = isProfiler;
		exports.isStrictMode = isStrictMode;
		exports.isSuspense = isSuspense;
		exports.isValidElementType = isValidElementType;
		exports.typeOf = typeOf;
	})();
}));
//#endregion
//#region node_modules/.pnpm/react-is@16.13.1/node_modules/react-is/index.js
var require_react_is = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_react_is_development();
}));
//#endregion
//#region node_modules/.pnpm/object-assign@4.1.1/node_modules/object-assign/index.js
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/
var require_object_assign = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getOwnPropertySymbols = Object.getOwnPropertySymbols;
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	var propIsEnumerable = Object.prototype.propertyIsEnumerable;
	function toObject(val) {
		if (val === null || val === void 0) throw new TypeError("Object.assign cannot be called with null or undefined");
		return Object(val);
	}
	function shouldUseNative() {
		try {
			if (!Object.assign) return false;
			var test1 = /* @__PURE__ */ new String("abc");
			test1[5] = "de";
			if (Object.getOwnPropertyNames(test1)[0] === "5") return false;
			var test2 = {};
			for (var i = 0; i < 10; i++) test2["_" + String.fromCharCode(i)] = i;
			if (Object.getOwnPropertyNames(test2).map(function(n) {
				return test2[n];
			}).join("") !== "0123456789") return false;
			var test3 = {};
			"abcdefghijklmnopqrst".split("").forEach(function(letter) {
				test3[letter] = letter;
			});
			if (Object.keys(Object.assign({}, test3)).join("") !== "abcdefghijklmnopqrst") return false;
			return true;
		} catch (err) {
			return false;
		}
	}
	module.exports = shouldUseNative() ? Object.assign : function(target, source) {
		var from;
		var to = toObject(target);
		var symbols;
		for (var s = 1; s < arguments.length; s++) {
			from = Object(arguments[s]);
			for (var key in from) if (hasOwnProperty.call(from, key)) to[key] = from[key];
			if (getOwnPropertySymbols) {
				symbols = getOwnPropertySymbols(from);
				for (var i = 0; i < symbols.length; i++) if (propIsEnumerable.call(from, symbols[i])) to[symbols[i]] = from[symbols[i]];
			}
		}
		return to;
	};
}));
//#endregion
//#region node_modules/.pnpm/prop-types@15.8.1/node_modules/prop-types/lib/ReactPropTypesSecret.js
/**
* Copyright (c) 2013-present, Facebook, Inc.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_ReactPropTypesSecret = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
}));
//#endregion
//#region node_modules/.pnpm/prop-types@15.8.1/node_modules/prop-types/lib/has.js
var require_has$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = Function.call.bind(Object.prototype.hasOwnProperty);
}));
//#endregion
//#region node_modules/.pnpm/prop-types@15.8.1/node_modules/prop-types/checkPropTypes.js
/**
* Copyright (c) 2013-present, Facebook, Inc.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_checkPropTypes = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var printWarning = function() {};
	var ReactPropTypesSecret = require_ReactPropTypesSecret();
	var loggedTypeFailures = {};
	var has = require_has$1();
	printWarning = function(text) {
		var message = "Warning: " + text;
		if (typeof console !== "undefined") console.error(message);
		try {
			throw new Error(message);
		} catch (x) {}
	};
	/**
	* Assert that the values match with the type specs.
	* Error messages are memorized and will only be shown once.
	*
	* @param {object} typeSpecs Map of name to a ReactPropType
	* @param {object} values Runtime values that need to be type-checked
	* @param {string} location e.g. "prop", "context", "child context"
	* @param {string} componentName Name of the component for error messages.
	* @param {?Function} getStack Returns the component stack.
	* @private
	*/
	function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
		for (var typeSpecName in typeSpecs) if (has(typeSpecs, typeSpecName)) {
			var error;
			try {
				if (typeof typeSpecs[typeSpecName] !== "function") {
					var err = Error((componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
					err.name = "Invariant Violation";
					throw err;
				}
				error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
			} catch (ex) {
				error = ex;
			}
			if (error && !(error instanceof Error)) printWarning((componentName || "React class") + ": type specification of " + location + " `" + typeSpecName + "` is invalid; the type checker function must return `null` or an `Error` but returned a " + typeof error + ". You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).");
			if (error instanceof Error && !(error.message in loggedTypeFailures)) {
				loggedTypeFailures[error.message] = true;
				var stack = getStack ? getStack() : "";
				printWarning("Failed " + location + " type: " + error.message + (stack != null ? stack : ""));
			}
		}
	}
	/**
	* Resets warning cache when testing.
	*
	* @private
	*/
	checkPropTypes.resetWarningCache = function() {
		loggedTypeFailures = {};
	};
	module.exports = checkPropTypes;
}));
//#endregion
//#region node_modules/.pnpm/prop-types@15.8.1/node_modules/prop-types/factoryWithTypeCheckers.js
/**
* Copyright (c) 2013-present, Facebook, Inc.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_factoryWithTypeCheckers = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var ReactIs = require_react_is();
	var assign = require_object_assign();
	var ReactPropTypesSecret = require_ReactPropTypesSecret();
	var has = require_has$1();
	var checkPropTypes = require_checkPropTypes();
	var printWarning = function() {};
	printWarning = function(text) {
		var message = "Warning: " + text;
		if (typeof console !== "undefined") console.error(message);
		try {
			throw new Error(message);
		} catch (x) {}
	};
	function emptyFunctionThatReturnsNull() {
		return null;
	}
	module.exports = function(isValidElement, throwOnDirectAccess) {
		var ITERATOR_SYMBOL = typeof Symbol === "function" && Symbol.iterator;
		var FAUX_ITERATOR_SYMBOL = "@@iterator";
		/**
		* Returns the iterator method function contained on the iterable object.
		*
		* Be sure to invoke the function with the iterable as context:
		*
		*     var iteratorFn = getIteratorFn(myIterable);
		*     if (iteratorFn) {
		*       var iterator = iteratorFn.call(myIterable);
		*       ...
		*     }
		*
		* @param {?object} maybeIterable
		* @return {?function}
		*/
		function getIteratorFn(maybeIterable) {
			var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
			if (typeof iteratorFn === "function") return iteratorFn;
		}
		/**
		* Collection of methods that allow declaration and validation of props that are
		* supplied to React components. Example usage:
		*
		*   var Props = require('ReactPropTypes');
		*   var MyArticle = React.createClass({
		*     propTypes: {
		*       // An optional string prop named "description".
		*       description: Props.string,
		*
		*       // A required enum prop named "category".
		*       category: Props.oneOf(['News','Photos']).isRequired,
		*
		*       // A prop named "dialog" that requires an instance of Dialog.
		*       dialog: Props.instanceOf(Dialog).isRequired
		*     },
		*     render: function() { ... }
		*   });
		*
		* A more formal specification of how these methods are used:
		*
		*   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
		*   decl := ReactPropTypes.{type}(.isRequired)?
		*
		* Each and every declaration produces a function with the same signature. This
		* allows the creation of custom validation functions. For example:
		*
		*  var MyLink = React.createClass({
		*    propTypes: {
		*      // An optional string or URI prop named "href".
		*      href: function(props, propName, componentName) {
		*        var propValue = props[propName];
		*        if (propValue != null && typeof propValue !== 'string' &&
		*            !(propValue instanceof URI)) {
		*          return new Error(
		*            'Expected a string or an URI for ' + propName + ' in ' +
		*            componentName
		*          );
		*        }
		*      }
		*    },
		*    render: function() {...}
		*  });
		*
		* @internal
		*/
		var ANONYMOUS = "<<anonymous>>";
		var ReactPropTypes = {
			array: createPrimitiveTypeChecker("array"),
			bigint: createPrimitiveTypeChecker("bigint"),
			bool: createPrimitiveTypeChecker("boolean"),
			func: createPrimitiveTypeChecker("function"),
			number: createPrimitiveTypeChecker("number"),
			object: createPrimitiveTypeChecker("object"),
			string: createPrimitiveTypeChecker("string"),
			symbol: createPrimitiveTypeChecker("symbol"),
			any: createAnyTypeChecker(),
			arrayOf: createArrayOfTypeChecker,
			element: createElementTypeChecker(),
			elementType: createElementTypeTypeChecker(),
			instanceOf: createInstanceTypeChecker,
			node: createNodeChecker(),
			objectOf: createObjectOfTypeChecker,
			oneOf: createEnumTypeChecker,
			oneOfType: createUnionTypeChecker,
			shape: createShapeTypeChecker,
			exact: createStrictShapeTypeChecker
		};
		/**
		* inlined Object.is polyfill to avoid requiring consumers ship their own
		* https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
		*/
		function is(x, y) {
			if (x === y) return x !== 0 || 1 / x === 1 / y;
			else return x !== x && y !== y;
		}
		/**
		* We use an Error-like object for backward compatibility as people may call
		* PropTypes directly and inspect their output. However, we don't use real
		* Errors anymore. We don't inspect their stack anyway, and creating them
		* is prohibitively expensive if they are created too often, such as what
		* happens in oneOfType() for any type before the one that matched.
		*/
		function PropTypeError(message, data) {
			this.message = message;
			this.data = data && typeof data === "object" ? data : {};
			this.stack = "";
		}
		PropTypeError.prototype = Error.prototype;
		function createChainableTypeChecker(validate) {
			var manualPropTypeCallCache = {};
			var manualPropTypeWarningCount = 0;
			function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
				componentName = componentName || ANONYMOUS;
				propFullName = propFullName || propName;
				if (secret !== ReactPropTypesSecret) {
					if (throwOnDirectAccess) {
						var err = /* @__PURE__ */ new Error("Calling PropTypes validators directly is not supported by the `prop-types` package. Use `PropTypes.checkPropTypes()` to call them. Read more at http://fb.me/use-check-prop-types");
						err.name = "Invariant Violation";
						throw err;
					} else if (typeof console !== "undefined") {
						var cacheKey = componentName + ":" + propName;
						if (!manualPropTypeCallCache[cacheKey] && manualPropTypeWarningCount < 3) {
							printWarning("You are manually calling a React.PropTypes validation function for the `" + propFullName + "` prop on `" + componentName + "`. This is deprecated and will throw in the standalone `prop-types` package. You may be seeing this warning due to a third-party PropTypes library. See https://fb.me/react-warning-dont-call-proptypes for details.");
							manualPropTypeCallCache[cacheKey] = true;
							manualPropTypeWarningCount++;
						}
					}
				}
				if (props[propName] == null) {
					if (isRequired) {
						if (props[propName] === null) return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required " + ("in `" + componentName + "`, but its value is `null`."));
						return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required in " + ("`" + componentName + "`, but its value is `undefined`."));
					}
					return null;
				} else return validate(props, propName, componentName, location, propFullName);
			}
			var chainedCheckType = checkType.bind(null, false);
			chainedCheckType.isRequired = checkType.bind(null, true);
			return chainedCheckType;
		}
		function createPrimitiveTypeChecker(expectedType) {
			function validate(props, propName, componentName, location, propFullName, secret) {
				var propValue = props[propName];
				if (getPropType(propValue) !== expectedType) {
					var preciseType = getPreciseType(propValue);
					return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + preciseType + "` supplied to `" + componentName + "`, expected ") + ("`" + expectedType + "`."), { expectedType });
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createAnyTypeChecker() {
			return createChainableTypeChecker(emptyFunctionThatReturnsNull);
		}
		function createArrayOfTypeChecker(typeChecker) {
			function validate(props, propName, componentName, location, propFullName) {
				if (typeof typeChecker !== "function") return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside arrayOf.");
				var propValue = props[propName];
				if (!Array.isArray(propValue)) {
					var propType = getPropType(propValue);
					return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an array."));
				}
				for (var i = 0; i < propValue.length; i++) {
					var error = typeChecker(propValue, i, componentName, location, propFullName + "[" + i + "]", ReactPropTypesSecret);
					if (error instanceof Error) return error;
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createElementTypeChecker() {
			function validate(props, propName, componentName, location, propFullName) {
				var propValue = props[propName];
				if (!isValidElement(propValue)) {
					var propType = getPropType(propValue);
					return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement."));
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createElementTypeTypeChecker() {
			function validate(props, propName, componentName, location, propFullName) {
				var propValue = props[propName];
				if (!ReactIs.isValidElementType(propValue)) {
					var propType = getPropType(propValue);
					return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement type."));
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createInstanceTypeChecker(expectedClass) {
			function validate(props, propName, componentName, location, propFullName) {
				if (!(props[propName] instanceof expectedClass)) {
					var expectedClassName = expectedClass.name || ANONYMOUS;
					var actualClassName = getClassName(props[propName]);
					return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + actualClassName + "` supplied to `" + componentName + "`, expected ") + ("instance of `" + expectedClassName + "`."));
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createEnumTypeChecker(expectedValues) {
			if (!Array.isArray(expectedValues)) {
				if (arguments.length > 1) printWarning("Invalid arguments supplied to oneOf, expected an array, got " + arguments.length + " arguments. A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z]).");
				else printWarning("Invalid argument supplied to oneOf, expected an array.");
				return emptyFunctionThatReturnsNull;
			}
			function validate(props, propName, componentName, location, propFullName) {
				var propValue = props[propName];
				for (var i = 0; i < expectedValues.length; i++) if (is(propValue, expectedValues[i])) return null;
				var valuesString = JSON.stringify(expectedValues, function replacer(key, value) {
					if (getPreciseType(value) === "symbol") return String(value);
					return value;
				});
				return new PropTypeError("Invalid " + location + " `" + propFullName + "` of value `" + String(propValue) + "` " + ("supplied to `" + componentName + "`, expected one of " + valuesString + "."));
			}
			return createChainableTypeChecker(validate);
		}
		function createObjectOfTypeChecker(typeChecker) {
			function validate(props, propName, componentName, location, propFullName) {
				if (typeof typeChecker !== "function") return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside objectOf.");
				var propValue = props[propName];
				var propType = getPropType(propValue);
				if (propType !== "object") return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an object."));
				for (var key in propValue) if (has(propValue, key)) {
					var error = typeChecker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
					if (error instanceof Error) return error;
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createUnionTypeChecker(arrayOfTypeCheckers) {
			if (!Array.isArray(arrayOfTypeCheckers)) {
				printWarning("Invalid argument supplied to oneOfType, expected an instance of array.");
				return emptyFunctionThatReturnsNull;
			}
			for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
				var checker = arrayOfTypeCheckers[i];
				if (typeof checker !== "function") {
					printWarning("Invalid argument supplied to oneOfType. Expected an array of check functions, but received " + getPostfixForTypeWarning(checker) + " at index " + i + ".");
					return emptyFunctionThatReturnsNull;
				}
			}
			function validate(props, propName, componentName, location, propFullName) {
				var expectedTypes = [];
				for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
					var checker = arrayOfTypeCheckers[i];
					var checkerResult = checker(props, propName, componentName, location, propFullName, ReactPropTypesSecret);
					if (checkerResult == null) return null;
					if (checkerResult.data && has(checkerResult.data, "expectedType")) expectedTypes.push(checkerResult.data.expectedType);
				}
				var expectedTypesMessage = expectedTypes.length > 0 ? ", expected one of type [" + expectedTypes.join(", ") + "]" : "";
				return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`" + expectedTypesMessage + "."));
			}
			return createChainableTypeChecker(validate);
		}
		function createNodeChecker() {
			function validate(props, propName, componentName, location, propFullName) {
				if (!isNode(props[propName])) return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`, expected a ReactNode."));
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function invalidValidatorError(componentName, location, propFullName, key, type) {
			return new PropTypeError((componentName || "React class") + ": " + location + " type `" + propFullName + "." + key + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + type + "`.");
		}
		function createShapeTypeChecker(shapeTypes) {
			function validate(props, propName, componentName, location, propFullName) {
				var propValue = props[propName];
				var propType = getPropType(propValue);
				if (propType !== "object") return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
				for (var key in shapeTypes) {
					var checker = shapeTypes[key];
					if (typeof checker !== "function") return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
					var error = checker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
					if (error) return error;
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createStrictShapeTypeChecker(shapeTypes) {
			function validate(props, propName, componentName, location, propFullName) {
				var propValue = props[propName];
				var propType = getPropType(propValue);
				if (propType !== "object") return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
				for (var key in assign({}, props[propName], shapeTypes)) {
					var checker = shapeTypes[key];
					if (has(shapeTypes, key) && typeof checker !== "function") return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
					if (!checker) return new PropTypeError("Invalid " + location + " `" + propFullName + "` key `" + key + "` supplied to `" + componentName + "`.\nBad object: " + JSON.stringify(props[propName], null, "  ") + "\nValid keys: " + JSON.stringify(Object.keys(shapeTypes), null, "  "));
					var error = checker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
					if (error) return error;
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function isNode(propValue) {
			switch (typeof propValue) {
				case "number":
				case "string":
				case "undefined": return true;
				case "boolean": return !propValue;
				case "object":
					if (Array.isArray(propValue)) return propValue.every(isNode);
					if (propValue === null || isValidElement(propValue)) return true;
					var iteratorFn = getIteratorFn(propValue);
					if (iteratorFn) {
						var iterator = iteratorFn.call(propValue);
						var step;
						if (iteratorFn !== propValue.entries) {
							while (!(step = iterator.next()).done) if (!isNode(step.value)) return false;
						} else while (!(step = iterator.next()).done) {
							var entry = step.value;
							if (entry) {
								if (!isNode(entry[1])) return false;
							}
						}
					} else return false;
					return true;
				default: return false;
			}
		}
		function isSymbol(propType, propValue) {
			if (propType === "symbol") return true;
			if (!propValue) return false;
			if (propValue["@@toStringTag"] === "Symbol") return true;
			if (typeof Symbol === "function" && propValue instanceof Symbol) return true;
			return false;
		}
		function getPropType(propValue) {
			var propType = typeof propValue;
			if (Array.isArray(propValue)) return "array";
			if (propValue instanceof RegExp) return "object";
			if (isSymbol(propType, propValue)) return "symbol";
			return propType;
		}
		function getPreciseType(propValue) {
			if (typeof propValue === "undefined" || propValue === null) return "" + propValue;
			var propType = getPropType(propValue);
			if (propType === "object") {
				if (propValue instanceof Date) return "date";
				else if (propValue instanceof RegExp) return "regexp";
			}
			return propType;
		}
		function getPostfixForTypeWarning(value) {
			var type = getPreciseType(value);
			switch (type) {
				case "array":
				case "object": return "an " + type;
				case "boolean":
				case "date":
				case "regexp": return "a " + type;
				default: return type;
			}
		}
		function getClassName(propValue) {
			if (!propValue.constructor || !propValue.constructor.name) return ANONYMOUS;
			return propValue.constructor.name;
		}
		ReactPropTypes.checkPropTypes = checkPropTypes;
		ReactPropTypes.resetWarningCache = checkPropTypes.resetWarningCache;
		ReactPropTypes.PropTypes = ReactPropTypes;
		return ReactPropTypes;
	};
}));
//#endregion
//#region node_modules/.pnpm/dom-helpers@5.2.1/node_modules/dom-helpers/esm/hasClass.js
var import_prop_types = /* @__PURE__ */ __toESM((/* @__PURE__ */ __commonJSMin(((exports, module) => {
	var ReactIs = require_react_is();
	module.exports = require_factoryWithTypeCheckers()(ReactIs.isElement, true);
})))());
/**
* Checks if a given element has a CSS class.
* 
* @param element the element
* @param className the CSS class name
*/
function hasClass(element, className) {
	if (element.classList) return !!className && element.classList.contains(className);
	return (" " + (element.className.baseVal || element.className) + " ").indexOf(" " + className + " ") !== -1;
}
//#endregion
//#region node_modules/.pnpm/dom-helpers@5.2.1/node_modules/dom-helpers/esm/addClass.js
/**
* Adds a CSS class to a given element.
* 
* @param element the element
* @param className the CSS class name
*/
function addClass(element, className) {
	if (element.classList) element.classList.add(className);
	else if (!hasClass(element, className)) if (typeof element.className === "string") element.className = element.className + " " + className;
	else element.setAttribute("class", (element.className && element.className.baseVal || "") + " " + className);
}
//#endregion
//#region node_modules/.pnpm/dom-helpers@5.2.1/node_modules/dom-helpers/esm/removeClass.js
function replaceClassName(origClass, classToRemove) {
	return origClass.replace(new RegExp("(^|\\s)" + classToRemove + "(?:\\s|$)", "g"), "$1").replace(/\s+/g, " ").replace(/^\s*|\s*$/g, "");
}
/**
* Removes a CSS class from a given element.
* 
* @param element the element
* @param className the CSS class name
*/
function removeClass$1(element, className) {
	if (element.classList) element.classList.remove(className);
	else if (typeof element.className === "string") element.className = replaceClassName(element.className, className);
	else element.setAttribute("class", replaceClassName(element.className && element.className.baseVal || "", className));
}
//#endregion
//#region node_modules/.pnpm/react-transition-group@4.4.5_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/react-transition-group/esm/config.js
var config_default = { disabled: false };
//#endregion
//#region node_modules/.pnpm/react-transition-group@4.4.5_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/react-transition-group/esm/utils/PropTypes.js
var timeoutsShape = import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.shape({
	enter: import_prop_types.default.number,
	exit: import_prop_types.default.number,
	appear: import_prop_types.default.number
}).isRequired]);
var classNamesShape = import_prop_types.default.oneOfType([
	import_prop_types.default.string,
	import_prop_types.default.shape({
		enter: import_prop_types.default.string,
		exit: import_prop_types.default.string,
		active: import_prop_types.default.string
	}),
	import_prop_types.default.shape({
		enter: import_prop_types.default.string,
		enterDone: import_prop_types.default.string,
		enterActive: import_prop_types.default.string,
		exit: import_prop_types.default.string,
		exitDone: import_prop_types.default.string,
		exitActive: import_prop_types.default.string
	})
]);
//#endregion
//#region node_modules/.pnpm/react-transition-group@4.4.5_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/react-transition-group/esm/TransitionGroupContext.js
var TransitionGroupContext_default = import_react.createContext(null);
//#endregion
//#region node_modules/.pnpm/react-transition-group@4.4.5_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/react-transition-group/esm/utils/reflow.js
var forceReflow = function forceReflow(node) {
	return node.scrollTop;
};
//#endregion
//#region node_modules/.pnpm/react-transition-group@4.4.5_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/react-transition-group/esm/Transition.js
var UNMOUNTED = "unmounted";
var EXITED = "exited";
var ENTERING = "entering";
var ENTERED = "entered";
var EXITING = "exiting";
/**
* The Transition component lets you describe a transition from one component
* state to another _over time_ with a simple declarative API. Most commonly
* it's used to animate the mounting and unmounting of a component, but can also
* be used to describe in-place transition states as well.
*
* ---
*
* **Note**: `Transition` is a platform-agnostic base component. If you're using
* transitions in CSS, you'll probably want to use
* [`CSSTransition`](https://reactcommunity.org/react-transition-group/css-transition)
* instead. It inherits all the features of `Transition`, but contains
* additional features necessary to play nice with CSS transitions (hence the
* name of the component).
*
* ---
*
* By default the `Transition` component does not alter the behavior of the
* component it renders, it only tracks "enter" and "exit" states for the
* components. It's up to you to give meaning and effect to those states. For
* example we can add styles to a component when it enters or exits:
*
* ```jsx
* import { Transition } from 'react-transition-group';
*
* const duration = 300;
*
* const defaultStyle = {
*   transition: `opacity ${duration}ms ease-in-out`,
*   opacity: 0,
* }
*
* const transitionStyles = {
*   entering: { opacity: 1 },
*   entered:  { opacity: 1 },
*   exiting:  { opacity: 0 },
*   exited:  { opacity: 0 },
* };
*
* const Fade = ({ in: inProp }) => (
*   <Transition in={inProp} timeout={duration}>
*     {state => (
*       <div style={{
*         ...defaultStyle,
*         ...transitionStyles[state]
*       }}>
*         I'm a fade Transition!
*       </div>
*     )}
*   </Transition>
* );
* ```
*
* There are 4 main states a Transition can be in:
*  - `'entering'`
*  - `'entered'`
*  - `'exiting'`
*  - `'exited'`
*
* Transition state is toggled via the `in` prop. When `true` the component
* begins the "Enter" stage. During this stage, the component will shift from
* its current transition state, to `'entering'` for the duration of the
* transition and then to the `'entered'` stage once it's complete. Let's take
* the following example (we'll use the
* [useState](https://reactjs.org/docs/hooks-reference.html#usestate) hook):
*
* ```jsx
* function App() {
*   const [inProp, setInProp] = useState(false);
*   return (
*     <div>
*       <Transition in={inProp} timeout={500}>
*         {state => (
*           // ...
*         )}
*       </Transition>
*       <button onClick={() => setInProp(true)}>
*         Click to Enter
*       </button>
*     </div>
*   );
* }
* ```
*
* When the button is clicked the component will shift to the `'entering'` state
* and stay there for 500ms (the value of `timeout`) before it finally switches
* to `'entered'`.
*
* When `in` is `false` the same thing happens except the state moves from
* `'exiting'` to `'exited'`.
*/
var Transition = /*#__PURE__*/ function(_React$Component) {
	_inheritsLoose(Transition, _React$Component);
	function Transition(props, context) {
		var _this = _React$Component.call(this, props, context) || this;
		var parentGroup = context;
		var appear = parentGroup && !parentGroup.isMounting ? props.enter : props.appear;
		var initialStatus;
		_this.appearStatus = null;
		if (props.in) if (appear) {
			initialStatus = EXITED;
			_this.appearStatus = ENTERING;
		} else initialStatus = ENTERED;
		else if (props.unmountOnExit || props.mountOnEnter) initialStatus = UNMOUNTED;
		else initialStatus = EXITED;
		_this.state = { status: initialStatus };
		_this.nextCallback = null;
		return _this;
	}
	Transition.getDerivedStateFromProps = function getDerivedStateFromProps(_ref, prevState) {
		if (_ref.in && prevState.status === "unmounted") return { status: EXITED };
		return null;
	};
	var _proto = Transition.prototype;
	_proto.componentDidMount = function componentDidMount() {
		this.updateStatus(true, this.appearStatus);
	};
	_proto.componentDidUpdate = function componentDidUpdate(prevProps) {
		var nextStatus = null;
		if (prevProps !== this.props) {
			var status = this.state.status;
			if (this.props.in) {
				if (status !== "entering" && status !== "entered") nextStatus = ENTERING;
			} else if (status === "entering" || status === "entered") nextStatus = EXITING;
		}
		this.updateStatus(false, nextStatus);
	};
	_proto.componentWillUnmount = function componentWillUnmount() {
		this.cancelNextCallback();
	};
	_proto.getTimeouts = function getTimeouts() {
		var timeout = this.props.timeout;
		var exit = enter = appear = timeout, enter, appear;
		if (timeout != null && typeof timeout !== "number") {
			exit = timeout.exit;
			enter = timeout.enter;
			appear = timeout.appear !== void 0 ? timeout.appear : enter;
		}
		return {
			exit,
			enter,
			appear
		};
	};
	_proto.updateStatus = function updateStatus(mounting, nextStatus) {
		if (mounting === void 0) mounting = false;
		if (nextStatus !== null) {
			this.cancelNextCallback();
			if (nextStatus === "entering") {
				if (this.props.unmountOnExit || this.props.mountOnEnter) {
					var node = this.props.nodeRef ? this.props.nodeRef.current : import_react_dom.default.findDOMNode(this);
					if (node) forceReflow(node);
				}
				this.performEnter(mounting);
			} else this.performExit();
		} else if (this.props.unmountOnExit && this.state.status === "exited") this.setState({ status: UNMOUNTED });
	};
	_proto.performEnter = function performEnter(mounting) {
		var _this2 = this;
		var enter = this.props.enter;
		var appearing = this.context ? this.context.isMounting : mounting;
		var _ref2 = this.props.nodeRef ? [appearing] : [import_react_dom.default.findDOMNode(this), appearing], maybeNode = _ref2[0], maybeAppearing = _ref2[1];
		var timeouts = this.getTimeouts();
		var enterTimeout = appearing ? timeouts.appear : timeouts.enter;
		if (!mounting && !enter || config_default.disabled) {
			this.safeSetState({ status: ENTERED }, function() {
				_this2.props.onEntered(maybeNode);
			});
			return;
		}
		this.props.onEnter(maybeNode, maybeAppearing);
		this.safeSetState({ status: ENTERING }, function() {
			_this2.props.onEntering(maybeNode, maybeAppearing);
			_this2.onTransitionEnd(enterTimeout, function() {
				_this2.safeSetState({ status: ENTERED }, function() {
					_this2.props.onEntered(maybeNode, maybeAppearing);
				});
			});
		});
	};
	_proto.performExit = function performExit() {
		var _this3 = this;
		var exit = this.props.exit;
		var timeouts = this.getTimeouts();
		var maybeNode = this.props.nodeRef ? void 0 : import_react_dom.default.findDOMNode(this);
		if (!exit || config_default.disabled) {
			this.safeSetState({ status: EXITED }, function() {
				_this3.props.onExited(maybeNode);
			});
			return;
		}
		this.props.onExit(maybeNode);
		this.safeSetState({ status: EXITING }, function() {
			_this3.props.onExiting(maybeNode);
			_this3.onTransitionEnd(timeouts.exit, function() {
				_this3.safeSetState({ status: EXITED }, function() {
					_this3.props.onExited(maybeNode);
				});
			});
		});
	};
	_proto.cancelNextCallback = function cancelNextCallback() {
		if (this.nextCallback !== null) {
			this.nextCallback.cancel();
			this.nextCallback = null;
		}
	};
	_proto.safeSetState = function safeSetState(nextState, callback) {
		callback = this.setNextCallback(callback);
		this.setState(nextState, callback);
	};
	_proto.setNextCallback = function setNextCallback(callback) {
		var _this4 = this;
		var active = true;
		this.nextCallback = function(event) {
			if (active) {
				active = false;
				_this4.nextCallback = null;
				callback(event);
			}
		};
		this.nextCallback.cancel = function() {
			active = false;
		};
		return this.nextCallback;
	};
	_proto.onTransitionEnd = function onTransitionEnd(timeout, handler) {
		this.setNextCallback(handler);
		var node = this.props.nodeRef ? this.props.nodeRef.current : import_react_dom.default.findDOMNode(this);
		var doesNotHaveTimeoutOrListener = timeout == null && !this.props.addEndListener;
		if (!node || doesNotHaveTimeoutOrListener) {
			setTimeout(this.nextCallback, 0);
			return;
		}
		if (this.props.addEndListener) {
			var _ref3 = this.props.nodeRef ? [this.nextCallback] : [node, this.nextCallback], maybeNode = _ref3[0], maybeNextCallback = _ref3[1];
			this.props.addEndListener(maybeNode, maybeNextCallback);
		}
		if (timeout != null) setTimeout(this.nextCallback, timeout);
	};
	_proto.render = function render() {
		var status = this.state.status;
		if (status === "unmounted") return null;
		var _this$props = this.props, children = _this$props.children;
		_this$props.in;
		_this$props.mountOnEnter;
		_this$props.unmountOnExit;
		_this$props.appear;
		_this$props.enter;
		_this$props.exit;
		_this$props.timeout;
		_this$props.addEndListener;
		_this$props.onEnter;
		_this$props.onEntering;
		_this$props.onEntered;
		_this$props.onExit;
		_this$props.onExiting;
		_this$props.onExited;
		_this$props.nodeRef;
		var childProps = _objectWithoutPropertiesLoose(_this$props, [
			"children",
			"in",
			"mountOnEnter",
			"unmountOnExit",
			"appear",
			"enter",
			"exit",
			"timeout",
			"addEndListener",
			"onEnter",
			"onEntering",
			"onEntered",
			"onExit",
			"onExiting",
			"onExited",
			"nodeRef"
		]);
		return /*#__PURE__*/ import_react.createElement(TransitionGroupContext_default.Provider, { value: null }, typeof children === "function" ? children(status, childProps) : import_react.cloneElement(import_react.Children.only(children), childProps));
	};
	return Transition;
}(import_react.Component);
Transition.contextType = TransitionGroupContext_default;
Transition.propTypes = {
	/**
	* A React reference to DOM element that need to transition:
	* https://stackoverflow.com/a/51127130/4671932
	*
	*   - When `nodeRef` prop is used, `node` is not passed to callback functions
	*      (e.g. `onEnter`) because user already has direct access to the node.
	*   - When changing `key` prop of `Transition` in a `TransitionGroup` a new
	*     `nodeRef` need to be provided to `Transition` with changed `key` prop
	*     (see
	*     [test/CSSTransition-test.js](https://github.com/reactjs/react-transition-group/blob/13435f897b3ab71f6e19d724f145596f5910581c/test/CSSTransition-test.js#L362-L437)).
	*/
	nodeRef: import_prop_types.default.shape({ current: typeof Element === "undefined" ? import_prop_types.default.any : function(propValue, key, componentName, location, propFullName, secret) {
		var value = propValue[key];
		return import_prop_types.default.instanceOf(value && "ownerDocument" in value ? value.ownerDocument.defaultView.Element : Element)(propValue, key, componentName, location, propFullName, secret);
	} }),
	/**
	* A `function` child can be used instead of a React element. This function is
	* called with the current transition status (`'entering'`, `'entered'`,
	* `'exiting'`, `'exited'`), which can be used to apply context
	* specific props to a component.
	*
	* ```jsx
	* <Transition in={this.state.in} timeout={150}>
	*   {state => (
	*     <MyComponent className={`fade fade-${state}`} />
	*   )}
	* </Transition>
	* ```
	*/
	children: import_prop_types.default.oneOfType([import_prop_types.default.func.isRequired, import_prop_types.default.element.isRequired]).isRequired,
	/**
	* Show the component; triggers the enter or exit states
	*/
	in: import_prop_types.default.bool,
	/**
	* By default the child component is mounted immediately along with
	* the parent `Transition` component. If you want to "lazy mount" the component on the
	* first `in={true}` you can set `mountOnEnter`. After the first enter transition the component will stay
	* mounted, even on "exited", unless you also specify `unmountOnExit`.
	*/
	mountOnEnter: import_prop_types.default.bool,
	/**
	* By default the child component stays mounted after it reaches the `'exited'` state.
	* Set `unmountOnExit` if you'd prefer to unmount the component after it finishes exiting.
	*/
	unmountOnExit: import_prop_types.default.bool,
	/**
	* By default the child component does not perform the enter transition when
	* it first mounts, regardless of the value of `in`. If you want this
	* behavior, set both `appear` and `in` to `true`.
	*
	* > **Note**: there are no special appear states like `appearing`/`appeared`, this prop
	* > only adds an additional enter transition. However, in the
	* > `<CSSTransition>` component that first enter transition does result in
	* > additional `.appear-*` classes, that way you can choose to style it
	* > differently.
	*/
	appear: import_prop_types.default.bool,
	/**
	* Enable or disable enter transitions.
	*/
	enter: import_prop_types.default.bool,
	/**
	* Enable or disable exit transitions.
	*/
	exit: import_prop_types.default.bool,
	/**
	* The duration of the transition, in milliseconds.
	* Required unless `addEndListener` is provided.
	*
	* You may specify a single timeout for all transitions:
	*
	* ```jsx
	* timeout={500}
	* ```
	*
	* or individually:
	*
	* ```jsx
	* timeout={{
	*  appear: 500,
	*  enter: 300,
	*  exit: 500,
	* }}
	* ```
	*
	* - `appear` defaults to the value of `enter`
	* - `enter` defaults to `0`
	* - `exit` defaults to `0`
	*
	* @type {number | { enter?: number, exit?: number, appear?: number }}
	*/
	timeout: function timeout(props) {
		var pt = timeoutsShape;
		if (!props.addEndListener) pt = pt.isRequired;
		for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) args[_key - 1] = arguments[_key];
		return pt.apply(void 0, [props].concat(args));
	},
	/**
	* Add a custom transition end trigger. Called with the transitioning
	* DOM node and a `done` callback. Allows for more fine grained transition end
	* logic. Timeouts are still used as a fallback if provided.
	*
	* **Note**: when `nodeRef` prop is passed, `node` is not passed.
	*
	* ```jsx
	* addEndListener={(node, done) => {
	*   // use the css transitionend event to mark the finish of a transition
	*   node.addEventListener('transitionend', done, false);
	* }}
	* ```
	*/
	addEndListener: import_prop_types.default.func,
	/**
	* Callback fired before the "entering" status is applied. An extra parameter
	* `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
	*
	* **Note**: when `nodeRef` prop is passed, `node` is not passed.
	*
	* @type Function(node: HtmlElement, isAppearing: bool) -> void
	*/
	onEnter: import_prop_types.default.func,
	/**
	* Callback fired after the "entering" status is applied. An extra parameter
	* `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
	*
	* **Note**: when `nodeRef` prop is passed, `node` is not passed.
	*
	* @type Function(node: HtmlElement, isAppearing: bool)
	*/
	onEntering: import_prop_types.default.func,
	/**
	* Callback fired after the "entered" status is applied. An extra parameter
	* `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
	*
	* **Note**: when `nodeRef` prop is passed, `node` is not passed.
	*
	* @type Function(node: HtmlElement, isAppearing: bool) -> void
	*/
	onEntered: import_prop_types.default.func,
	/**
	* Callback fired before the "exiting" status is applied.
	*
	* **Note**: when `nodeRef` prop is passed, `node` is not passed.
	*
	* @type Function(node: HtmlElement) -> void
	*/
	onExit: import_prop_types.default.func,
	/**
	* Callback fired after the "exiting" status is applied.
	*
	* **Note**: when `nodeRef` prop is passed, `node` is not passed.
	*
	* @type Function(node: HtmlElement) -> void
	*/
	onExiting: import_prop_types.default.func,
	/**
	* Callback fired after the "exited" status is applied.
	*
	* **Note**: when `nodeRef` prop is passed, `node` is not passed
	*
	* @type Function(node: HtmlElement) -> void
	*/
	onExited: import_prop_types.default.func
};
function noop$3() {}
Transition.defaultProps = {
	in: false,
	mountOnEnter: false,
	unmountOnExit: false,
	appear: false,
	enter: true,
	exit: true,
	onEnter: noop$3,
	onEntering: noop$3,
	onEntered: noop$3,
	onExit: noop$3,
	onExiting: noop$3,
	onExited: noop$3
};
Transition.UNMOUNTED = UNMOUNTED;
Transition.EXITED = EXITED;
Transition.ENTERING = ENTERING;
Transition.ENTERED = ENTERED;
Transition.EXITING = EXITING;
//#endregion
//#region node_modules/.pnpm/react-transition-group@4.4.5_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/react-transition-group/esm/CSSTransition.js
var _addClass = function addClass$1(node, classes) {
	return node && classes && classes.split(" ").forEach(function(c) {
		return addClass(node, c);
	});
};
var removeClass = function removeClass(node, classes) {
	return node && classes && classes.split(" ").forEach(function(c) {
		return removeClass$1(node, c);
	});
};
/**
* A transition component inspired by the excellent
* [ng-animate](https://docs.angularjs.org/api/ngAnimate) library, you should
* use it if you're using CSS transitions or animations. It's built upon the
* [`Transition`](https://reactcommunity.org/react-transition-group/transition)
* component, so it inherits all of its props.
*
* `CSSTransition` applies a pair of class names during the `appear`, `enter`,
* and `exit` states of the transition. The first class is applied and then a
* second `*-active` class in order to activate the CSS transition. After the
* transition, matching `*-done` class names are applied to persist the
* transition state.
*
* ```jsx
* function App() {
*   const [inProp, setInProp] = useState(false);
*   return (
*     <div>
*       <CSSTransition in={inProp} timeout={200} classNames="my-node">
*         <div>
*           {"I'll receive my-node-* classes"}
*         </div>
*       </CSSTransition>
*       <button type="button" onClick={() => setInProp(true)}>
*         Click to Enter
*       </button>
*     </div>
*   );
* }
* ```
*
* When the `in` prop is set to `true`, the child component will first receive
* the class `example-enter`, then the `example-enter-active` will be added in
* the next tick. `CSSTransition` [forces a
* reflow](https://github.com/reactjs/react-transition-group/blob/5007303e729a74be66a21c3e2205e4916821524b/src/CSSTransition.js#L208-L215)
* between before adding the `example-enter-active`. This is an important trick
* because it allows us to transition between `example-enter` and
* `example-enter-active` even though they were added immediately one after
* another. Most notably, this is what makes it possible for us to animate
* _appearance_.
*
* ```css
* .my-node-enter {
*   opacity: 0;
* }
* .my-node-enter-active {
*   opacity: 1;
*   transition: opacity 200ms;
* }
* .my-node-exit {
*   opacity: 1;
* }
* .my-node-exit-active {
*   opacity: 0;
*   transition: opacity 200ms;
* }
* ```
*
* `*-active` classes represent which styles you want to animate **to**, so it's
* important to add `transition` declaration only to them, otherwise transitions
* might not behave as intended! This might not be obvious when the transitions
* are symmetrical, i.e. when `*-enter-active` is the same as `*-exit`, like in
* the example above (minus `transition`), but it becomes apparent in more
* complex transitions.
*
* **Note**: If you're using the
* [`appear`](http://reactcommunity.org/react-transition-group/transition#Transition-prop-appear)
* prop, make sure to define styles for `.appear-*` classes as well.
*/
var CSSTransition = /*#__PURE__*/ function(_React$Component) {
	_inheritsLoose(CSSTransition, _React$Component);
	function CSSTransition() {
		var _this;
		for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
		_this = _React$Component.call.apply(_React$Component, [this].concat(args)) || this;
		_this.appliedClasses = {
			appear: {},
			enter: {},
			exit: {}
		};
		_this.onEnter = function(maybeNode, maybeAppearing) {
			var _this$resolveArgument = _this.resolveArguments(maybeNode, maybeAppearing), node = _this$resolveArgument[0], appearing = _this$resolveArgument[1];
			_this.removeClasses(node, "exit");
			_this.addClass(node, appearing ? "appear" : "enter", "base");
			if (_this.props.onEnter) _this.props.onEnter(maybeNode, maybeAppearing);
		};
		_this.onEntering = function(maybeNode, maybeAppearing) {
			var _this$resolveArgument2 = _this.resolveArguments(maybeNode, maybeAppearing), node = _this$resolveArgument2[0];
			var type = _this$resolveArgument2[1] ? "appear" : "enter";
			_this.addClass(node, type, "active");
			if (_this.props.onEntering) _this.props.onEntering(maybeNode, maybeAppearing);
		};
		_this.onEntered = function(maybeNode, maybeAppearing) {
			var _this$resolveArgument3 = _this.resolveArguments(maybeNode, maybeAppearing), node = _this$resolveArgument3[0];
			var type = _this$resolveArgument3[1] ? "appear" : "enter";
			_this.removeClasses(node, type);
			_this.addClass(node, type, "done");
			if (_this.props.onEntered) _this.props.onEntered(maybeNode, maybeAppearing);
		};
		_this.onExit = function(maybeNode) {
			var node = _this.resolveArguments(maybeNode)[0];
			_this.removeClasses(node, "appear");
			_this.removeClasses(node, "enter");
			_this.addClass(node, "exit", "base");
			if (_this.props.onExit) _this.props.onExit(maybeNode);
		};
		_this.onExiting = function(maybeNode) {
			var node = _this.resolveArguments(maybeNode)[0];
			_this.addClass(node, "exit", "active");
			if (_this.props.onExiting) _this.props.onExiting(maybeNode);
		};
		_this.onExited = function(maybeNode) {
			var node = _this.resolveArguments(maybeNode)[0];
			_this.removeClasses(node, "exit");
			_this.addClass(node, "exit", "done");
			if (_this.props.onExited) _this.props.onExited(maybeNode);
		};
		_this.resolveArguments = function(maybeNode, maybeAppearing) {
			return _this.props.nodeRef ? [_this.props.nodeRef.current, maybeNode] : [maybeNode, maybeAppearing];
		};
		_this.getClassNames = function(type) {
			var classNames = _this.props.classNames;
			var isStringClassNames = typeof classNames === "string";
			var prefix = isStringClassNames && classNames ? classNames + "-" : "";
			var baseClassName = isStringClassNames ? "" + prefix + type : classNames[type];
			return {
				baseClassName,
				activeClassName: isStringClassNames ? baseClassName + "-active" : classNames[type + "Active"],
				doneClassName: isStringClassNames ? baseClassName + "-done" : classNames[type + "Done"]
			};
		};
		return _this;
	}
	var _proto = CSSTransition.prototype;
	_proto.addClass = function addClass(node, type, phase) {
		var className = this.getClassNames(type)[phase + "ClassName"];
		var doneClassName = this.getClassNames("enter").doneClassName;
		if (type === "appear" && phase === "done" && doneClassName) className += " " + doneClassName;
		if (phase === "active") {
			if (node) forceReflow(node);
		}
		if (className) {
			this.appliedClasses[type][phase] = className;
			_addClass(node, className);
		}
	};
	_proto.removeClasses = function removeClasses(node, type) {
		var _this$appliedClasses$ = this.appliedClasses[type], baseClassName = _this$appliedClasses$.base, activeClassName = _this$appliedClasses$.active, doneClassName = _this$appliedClasses$.done;
		this.appliedClasses[type] = {};
		if (baseClassName) removeClass(node, baseClassName);
		if (activeClassName) removeClass(node, activeClassName);
		if (doneClassName) removeClass(node, doneClassName);
	};
	_proto.render = function render() {
		var _this$props = this.props;
		_this$props.classNames;
		var props = _objectWithoutPropertiesLoose(_this$props, ["classNames"]);
		return /*#__PURE__*/ import_react.createElement(Transition, _extends({}, props, {
			onEnter: this.onEnter,
			onEntered: this.onEntered,
			onEntering: this.onEntering,
			onExit: this.onExit,
			onExiting: this.onExiting,
			onExited: this.onExited
		}));
	};
	return CSSTransition;
}(import_react.Component);
CSSTransition.defaultProps = { classNames: "" };
CSSTransition.propTypes = _extends({}, Transition.propTypes, {
	/**
	* The animation classNames applied to the component as it appears, enters,
	* exits or has finished the transition. A single name can be provided, which
	* will be suffixed for each stage, e.g. `classNames="fade"` applies:
	*
	* - `fade-appear`, `fade-appear-active`, `fade-appear-done`
	* - `fade-enter`, `fade-enter-active`, `fade-enter-done`
	* - `fade-exit`, `fade-exit-active`, `fade-exit-done`
	*
	* A few details to note about how these classes are applied:
	*
	* 1. They are _joined_ with the ones that are already defined on the child
	*    component, so if you want to add some base styles, you can use
	*    `className` without worrying that it will be overridden.
	*
	* 2. If the transition component mounts with `in={false}`, no classes are
	*    applied yet. You might be expecting `*-exit-done`, but if you think
	*    about it, a component cannot finish exiting if it hasn't entered yet.
	*
	* 2. `fade-appear-done` and `fade-enter-done` will _both_ be applied. This
	*    allows you to define different behavior for when appearing is done and
	*    when regular entering is done, using selectors like
	*    `.fade-enter-done:not(.fade-appear-done)`. For example, you could apply
	*    an epic entrance animation when element first appears in the DOM using
	*    [Animate.css](https://daneden.github.io/animate.css/). Otherwise you can
	*    simply use `fade-enter-done` for defining both cases.
	*
	* Each individual classNames can also be specified independently like:
	*
	* ```js
	* classNames={{
	*  appear: 'my-appear',
	*  appearActive: 'my-active-appear',
	*  appearDone: 'my-done-appear',
	*  enter: 'my-enter',
	*  enterActive: 'my-active-enter',
	*  enterDone: 'my-done-enter',
	*  exit: 'my-exit',
	*  exitActive: 'my-active-exit',
	*  exitDone: 'my-done-exit',
	* }}
	* ```
	*
	* If you want to set these classes using CSS Modules:
	*
	* ```js
	* import styles from './styles.css';
	* ```
	*
	* you might want to use camelCase in your CSS file, that way could simply
	* spread them instead of listing them one by one:
	*
	* ```js
	* classNames={{ ...styles }}
	* ```
	*
	* @type {string | {
	*  appear?: string,
	*  appearActive?: string,
	*  appearDone?: string,
	*  enter?: string,
	*  enterActive?: string,
	*  enterDone?: string,
	*  exit?: string,
	*  exitActive?: string,
	*  exitDone?: string,
	* }}
	*/
	classNames: classNamesShape,
	/**
	* A `<Transition>` callback fired immediately after the 'enter' or 'appear' class is
	* applied.
	*
	* **Note**: when `nodeRef` prop is passed, `node` is not passed.
	*
	* @type Function(node: HtmlElement, isAppearing: bool)
	*/
	onEnter: import_prop_types.default.func,
	/**
	* A `<Transition>` callback fired immediately after the 'enter-active' or
	* 'appear-active' class is applied.
	*
	* **Note**: when `nodeRef` prop is passed, `node` is not passed.
	*
	* @type Function(node: HtmlElement, isAppearing: bool)
	*/
	onEntering: import_prop_types.default.func,
	/**
	* A `<Transition>` callback fired immediately after the 'enter' or
	* 'appear' classes are **removed** and the `done` class is added to the DOM node.
	*
	* **Note**: when `nodeRef` prop is passed, `node` is not passed.
	*
	* @type Function(node: HtmlElement, isAppearing: bool)
	*/
	onEntered: import_prop_types.default.func,
	/**
	* A `<Transition>` callback fired immediately after the 'exit' class is
	* applied.
	*
	* **Note**: when `nodeRef` prop is passed, `node` is not passed
	*
	* @type Function(node: HtmlElement)
	*/
	onExit: import_prop_types.default.func,
	/**
	* A `<Transition>` callback fired immediately after the 'exit-active' is applied.
	*
	* **Note**: when `nodeRef` prop is passed, `node` is not passed
	*
	* @type Function(node: HtmlElement)
	*/
	onExiting: import_prop_types.default.func,
	/**
	* A `<Transition>` callback fired immediately after the 'exit' classes
	* are **removed** and the `exit-done` class is added to the DOM node.
	*
	* **Note**: when `nodeRef` prop is passed, `node` is not passed
	*
	* @type Function(node: HtmlElement)
	*/
	onExited: import_prop_types.default.func
});
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/assertThisInitialized.js
function _assertThisInitialized(e) {
	if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
	return e;
}
//#endregion
//#region node_modules/.pnpm/react-transition-group@4.4.5_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/react-transition-group/esm/utils/ChildMapping.js
/**
* Given `this.props.children`, return an object mapping key to child.
*
* @param {*} children `this.props.children`
* @return {object} Mapping of key to child
*/
function getChildMapping(children, mapFn) {
	var mapper = function mapper(child) {
		return mapFn && (0, import_react.isValidElement)(child) ? mapFn(child) : child;
	};
	var result = Object.create(null);
	if (children) import_react.Children.map(children, function(c) {
		return c;
	}).forEach(function(child) {
		result[child.key] = mapper(child);
	});
	return result;
}
/**
* When you're adding or removing children some may be added or removed in the
* same render pass. We want to show *both* since we want to simultaneously
* animate elements in and out. This function takes a previous set of keys
* and a new set of keys and merges them with its best guess of the correct
* ordering. In the future we may expose some of the utilities in
* ReactMultiChild to make this easy, but for now React itself does not
* directly have this concept of the union of prevChildren and nextChildren
* so we implement it here.
*
* @param {object} prev prev children as returned from
* `ReactTransitionChildMapping.getChildMapping()`.
* @param {object} next next children as returned from
* `ReactTransitionChildMapping.getChildMapping()`.
* @return {object} a key set that contains all keys in `prev` and all keys
* in `next` in a reasonable order.
*/
function mergeChildMappings(prev, next) {
	prev = prev || {};
	next = next || {};
	function getValueForKey(key) {
		return key in next ? next[key] : prev[key];
	}
	var nextKeysPending = Object.create(null);
	var pendingKeys = [];
	for (var prevKey in prev) if (prevKey in next) {
		if (pendingKeys.length) {
			nextKeysPending[prevKey] = pendingKeys;
			pendingKeys = [];
		}
	} else pendingKeys.push(prevKey);
	var i;
	var childMapping = {};
	for (var nextKey in next) {
		if (nextKeysPending[nextKey]) for (i = 0; i < nextKeysPending[nextKey].length; i++) {
			var pendingNextKey = nextKeysPending[nextKey][i];
			childMapping[nextKeysPending[nextKey][i]] = getValueForKey(pendingNextKey);
		}
		childMapping[nextKey] = getValueForKey(nextKey);
	}
	for (i = 0; i < pendingKeys.length; i++) childMapping[pendingKeys[i]] = getValueForKey(pendingKeys[i]);
	return childMapping;
}
function getProp(child, prop, props) {
	return props[prop] != null ? props[prop] : child.props[prop];
}
function getInitialChildMapping(props, onExited) {
	return getChildMapping(props.children, function(child) {
		return (0, import_react.cloneElement)(child, {
			onExited: onExited.bind(null, child),
			in: true,
			appear: getProp(child, "appear", props),
			enter: getProp(child, "enter", props),
			exit: getProp(child, "exit", props)
		});
	});
}
function getNextChildMapping(nextProps, prevChildMapping, onExited) {
	var nextChildMapping = getChildMapping(nextProps.children);
	var children = mergeChildMappings(prevChildMapping, nextChildMapping);
	Object.keys(children).forEach(function(key) {
		var child = children[key];
		if (!(0, import_react.isValidElement)(child)) return;
		var hasPrev = key in prevChildMapping;
		var hasNext = key in nextChildMapping;
		var prevChild = prevChildMapping[key];
		var isLeaving = (0, import_react.isValidElement)(prevChild) && !prevChild.props.in;
		if (hasNext && (!hasPrev || isLeaving)) children[key] = (0, import_react.cloneElement)(child, {
			onExited: onExited.bind(null, child),
			in: true,
			exit: getProp(child, "exit", nextProps),
			enter: getProp(child, "enter", nextProps)
		});
		else if (!hasNext && hasPrev && !isLeaving) children[key] = (0, import_react.cloneElement)(child, { in: false });
		else if (hasNext && hasPrev && (0, import_react.isValidElement)(prevChild)) children[key] = (0, import_react.cloneElement)(child, {
			onExited: onExited.bind(null, child),
			in: prevChild.props.in,
			exit: getProp(child, "exit", nextProps),
			enter: getProp(child, "enter", nextProps)
		});
	});
	return children;
}
//#endregion
//#region node_modules/.pnpm/react-transition-group@4.4.5_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/react-transition-group/esm/TransitionGroup.js
var values = Object.values || function(obj) {
	return Object.keys(obj).map(function(k) {
		return obj[k];
	});
};
var defaultProps$1 = {
	component: "div",
	childFactory: function childFactory(child) {
		return child;
	}
};
/**
* The `<TransitionGroup>` component manages a set of transition components
* (`<Transition>` and `<CSSTransition>`) in a list. Like with the transition
* components, `<TransitionGroup>` is a state machine for managing the mounting
* and unmounting of components over time.
*
* Consider the example below. As items are removed or added to the TodoList the
* `in` prop is toggled automatically by the `<TransitionGroup>`.
*
* Note that `<TransitionGroup>`  does not define any animation behavior!
* Exactly _how_ a list item animates is up to the individual transition
* component. This means you can mix and match animations across different list
* items.
*/
var TransitionGroup = /*#__PURE__*/ function(_React$Component) {
	_inheritsLoose(TransitionGroup, _React$Component);
	function TransitionGroup(props, context) {
		var _this = _React$Component.call(this, props, context) || this;
		_this.state = {
			contextValue: { isMounting: true },
			handleExited: _this.handleExited.bind(_assertThisInitialized(_this)),
			firstRender: true
		};
		return _this;
	}
	var _proto = TransitionGroup.prototype;
	_proto.componentDidMount = function componentDidMount() {
		this.mounted = true;
		this.setState({ contextValue: { isMounting: false } });
	};
	_proto.componentWillUnmount = function componentWillUnmount() {
		this.mounted = false;
	};
	TransitionGroup.getDerivedStateFromProps = function getDerivedStateFromProps(nextProps, _ref) {
		var prevChildMapping = _ref.children, handleExited = _ref.handleExited;
		return {
			children: _ref.firstRender ? getInitialChildMapping(nextProps, handleExited) : getNextChildMapping(nextProps, prevChildMapping, handleExited),
			firstRender: false
		};
	};
	_proto.handleExited = function handleExited(child, node) {
		var currentChildMapping = getChildMapping(this.props.children);
		if (child.key in currentChildMapping) return;
		if (child.props.onExited) child.props.onExited(node);
		if (this.mounted) this.setState(function(state) {
			var children = _extends({}, state.children);
			delete children[child.key];
			return { children };
		});
	};
	_proto.render = function render() {
		var _this$props = this.props, Component = _this$props.component, childFactory = _this$props.childFactory, props = _objectWithoutPropertiesLoose(_this$props, ["component", "childFactory"]);
		var contextValue = this.state.contextValue;
		var children = values(this.state.children).map(childFactory);
		delete props.appear;
		delete props.enter;
		delete props.exit;
		if (Component === null) return /*#__PURE__*/ import_react.createElement(TransitionGroupContext_default.Provider, { value: contextValue }, children);
		return /*#__PURE__*/ import_react.createElement(TransitionGroupContext_default.Provider, { value: contextValue }, /*#__PURE__*/ import_react.createElement(Component, props, children));
	};
	return TransitionGroup;
}(import_react.Component);
TransitionGroup.propTypes = {
	/**
	* `<TransitionGroup>` renders a `<div>` by default. You can change this
	* behavior by providing a `component` prop.
	* If you use React v16+ and would like to avoid a wrapping `<div>` element
	* you can pass in `component={null}`. This is useful if the wrapping div
	* borks your css styles.
	*/
	component: import_prop_types.default.any,
	/**
	* A set of `<Transition>` components, that are toggled `in` and out as they
	* leave. the `<TransitionGroup>` will inject specific transition props, so
	* remember to spread them through if you are wrapping the `<Transition>` as
	* with our `<Fade>` example.
	*
	* While this component is meant for multiple `Transition` or `CSSTransition`
	* children, sometimes you may want to have a single transition child with
	* content that you want to be transitioned out and in when you change it
	* (e.g. routes, images etc.) In that case you can change the `key` prop of
	* the transition child as you change its content, this will cause
	* `TransitionGroup` to transition the child out and back in.
	*/
	children: import_prop_types.default.node,
	/**
	* A convenience prop that enables or disables appear animations
	* for all children. Note that specifying this will override any defaults set
	* on individual children Transitions.
	*/
	appear: import_prop_types.default.bool,
	/**
	* A convenience prop that enables or disables enter animations
	* for all children. Note that specifying this will override any defaults set
	* on individual children Transitions.
	*/
	enter: import_prop_types.default.bool,
	/**
	* A convenience prop that enables or disables exit animations
	* for all children. Note that specifying this will override any defaults set
	* on individual children Transitions.
	*/
	exit: import_prop_types.default.bool,
	/**
	* You may need to apply reactive updates to a child as it is exiting.
	* This is generally done by using `cloneElement` however in the case of an exiting
	* child the element has already been removed and not accessible to the consumer.
	*
	* If you do need to update a child as it leaves you can provide a `childFactory`
	* to wrap every child, even the ones that are leaving.
	*
	* @type Function(child: ReactElement) -> ReactElement
	*/
	childFactory: import_prop_types.default.func
};
TransitionGroup.defaultProps = defaultProps$1;
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/transition.js
function getCSSTransitionClassNames(b) {
	return {
		appear: modsClassName(b({ appear: true })),
		appearActive: modsClassName(b({ appear: "active" })),
		appearDone: modsClassName(b({ appear: "done" })),
		enter: modsClassName(b({ enter: true })),
		enterActive: modsClassName(b({ enter: "active" })),
		enterDone: modsClassName(b({ enter: "done" })),
		exit: modsClassName(b({ exit: true })),
		exitActive: modsClassName(b({ exit: "active" })),
		exitDone: modsClassName(b({ exit: "done" }))
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Disclosure/constants.js
var b$110 = block("disclosure");
var DisclosureQa = {
	SUMMARY: "disclosure-summary",
	DETAILS: "disclosure-details"
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Disclosure/DisclosureDetails/DisclosureDetails.js
function DisclosureDetails({ children, qa, className }) {
	const containerRef = import_react.useRef(null);
	const { ariaControls, ariaLabelledby, keepMounted, expanded } = useDisclosureAttributes();
	return (0, import_jsx_runtime.jsx)(CSSTransition, {
		nodeRef: containerRef,
		in: expanded,
		addEndListener: (done) => containerRef.current?.addEventListener("animationend", done),
		classNames: getCSSTransitionClassNames(b$110),
		mountOnEnter: !keepMounted,
		unmountOnExit: !keepMounted,
		appear: true,
		children: (0, import_jsx_runtime.jsx)("div", {
			ref: containerRef,
			id: ariaControls,
			role: "region",
			"aria-labelledby": ariaLabelledby,
			className: b$110("content", { visible: expanded }, className),
			"data-qa": qa || DisclosureQa.DETAILS,
			children
		})
	});
}
DisclosureDetails.displayName = "DisclosureDetails";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ArrowToggle/ArrowToggle.js
var b$109 = block("arrow-toggle");
function ArrowToggle(rawProps) {
	const { size = 16, direction = "bottom", className, qa } = useDefaultProps("ArrowToggle", rawProps);
	return (0, import_jsx_runtime.jsx)("span", {
		style: {
			width: size,
			height: size
		},
		className: b$109({ direction }, className),
		"data-qa": qa,
		children: (0, import_jsx_runtime.jsx)(Icon, {
			data: ChevronDown,
			size
		})
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/warn.js
var didWarn = /* @__PURE__ */ new Map();
function warnOnce(msg) {
	if (!msg || didWarn.has(msg)) return;
	console.error(msg);
	didWarn.set(msg, true);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Disclosure/DisclosureSummary/DisclosureSummary.js
var ComponentSizeToIconSizeMap = {
	m: 14,
	l: 16,
	xl: 20
};
function warnAboutPhysicalValues$2() {
	warnOnce("[Disclosure] Physical values (left, right) of \"arrowPosition\" property are deprecated. Use logical values (start, end) instead.");
}
function DisclosureSummary({ children: renderFunction, qa }) {
	const handleToggle = useToggleDisclosure();
	const { ariaControls, ariaLabelledby: id, expanded, disabled, onSummaryKeyDown: onKeyDown } = useDisclosureAttributes();
	const props = {
		onClick: handleToggle,
		ariaControls,
		id,
		expanded,
		disabled,
		qa,
		onKeyDown
	};
	return renderFunction(props, (0, import_jsx_runtime.jsx)(DefaultDisclosureSummary, { ...props }));
}
var DefaultDisclosureSummary = import_react.forwardRef(function DefaultDisclosureSummary({ onClick, ariaControls, id, expanded, disabled, qa, onKeyDown, className }, ref) {
	const { size, summary, arrowPosition } = useDisclosureAttributes();
	let arrowMod = arrowPosition;
	if (arrowMod === "left") {
		warnAboutPhysicalValues$2();
		arrowMod = "start";
	}
	if (arrowMod === "right") {
		warnAboutPhysicalValues$2();
		arrowMod = "end";
	}
	return (0, import_jsx_runtime.jsxs)("button", {
		type: "button",
		"aria-expanded": expanded,
		className: b$110("trigger", {
			disabled,
			arrow: arrowMod
		}, className),
		"aria-controls": ariaControls,
		id,
		onClick,
		disabled,
		"data-qa": qa || DisclosureQa.SUMMARY,
		onKeyDown,
		ref,
		children: [(0, import_jsx_runtime.jsx)(ArrowToggle, {
			size: ComponentSizeToIconSizeMap[size],
			direction: expanded ? "top" : "bottom"
		}), summary]
	});
});
DisclosureSummary.displayName = "DisclosureSummary";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Disclosure/Disclosure.js
var isDisclosureSummaryComponent = isOfType(DisclosureSummary);
var Disclosure = import_react.forwardRef(function Disclosure(rawProps, ref) {
	const { size = "m", disabled = false, defaultExpanded = false, arrowPosition = "start", summary = "", className, keepMounted = true, children, onUpdate = () => {}, onSummaryKeyDown, expanded, qa } = useDefaultProps("Disclosure", rawProps);
	const [summaryContent, detailsContent] = prepareChildren$3(children, { disclosureQa: qa });
	return (0, import_jsx_runtime.jsx)(DisclosureProvider, {
		disabled,
		defaultExpanded,
		expanded,
		keepMounted,
		size,
		summary,
		arrowPosition,
		onUpdate,
		onSummaryKeyDown,
		children: (0, import_jsx_runtime.jsxs)("section", {
			ref,
			className: b$110({ size }, className),
			"data-qa": qa,
			children: [summaryContent, detailsContent]
		})
	});
});
function prepareChildren$3(children, { disclosureQa }) {
	const items = import_react.Children.toArray(children);
	let summary, details;
	const content = [];
	for (const item of items) {
		if (isDisclosureSummaryComponent(item)) {
			if (summary) throw new Error("Only one <Disclosure.Summary> component is allowed");
			summary = item;
			continue;
		}
		content.push(item);
	}
	if (content.length > 0) details = (0, import_jsx_runtime.jsx)(DisclosureDetails, {
		qa: disclosureQa && `${disclosureQa}-details`,
		children: content
	});
	if (!summary) summary = (0, import_jsx_runtime.jsx)(DisclosureSummary, {
		qa: disclosureQa && `${disclosureQa}-summary`,
		children: (props) => (0, import_jsx_runtime.jsx)(DefaultDisclosureSummary, { ...props })
	});
	return [summary, details];
}
Disclosure.Summary = DisclosureSummary;
Disclosure.Details = DisclosureDetails;
Disclosure.displayName = "Disclosure";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Accordion/constants.js
var accordionBlock = block("accordion");
var accordionItemBlock = block("accordion-item");
var accordionSummaryBlock = block("accordion-summary");
var accordionSummaryTriggerBlock = accordionSummaryBlock("trigger");
var accordionDetailsBlock = accordionItemBlock("details");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Accordion/AccordionSummary/AccordionSummary.js
/**
* Marker component for detecting AccordionSummary in AccordionItem.
* The actual rendering is done by AccordionSummaryContent inside Disclosure.Summary.
*/
function AccordionSummary(_props) {
	return null;
}
function AccordionSummaryContent(props) {
	const { children, disclosureProps, defaultSummary } = props;
	const { registerSummary, unregisterSummary, getSummaryRefs, arrowPosition, size, ariaLevel } = useAccordion();
	const summaryId = useUniqId();
	const [buttonElement, setButtonElement] = import_react.useState(null);
	import_react.useEffect(() => {
		if (buttonElement) {
			const summaryRef = {
				element: buttonElement,
				disabled: buttonElement.disabled
			};
			registerSummary(summaryId, summaryRef);
		}
		return () => {
			unregisterSummary(summaryId);
		};
	}, [
		summaryId,
		buttonElement,
		registerSummary,
		unregisterSummary
	]);
	const handleKeyDown = (e) => {
		if (e.key === KeyCode$1.ARROW_DOWN || e.key === KeyCode$1.ARROW_UP || e.key === KeyCode$1.HOME || e.key === KeyCode$1.END) {
			e.preventDefault();
			const summaryRefs = getSummaryRefs();
			if (summaryRefs.length === 0) return;
			if (e.key === KeyCode$1.HOME) {
				summaryRefs[0]?.element.focus();
				return;
			}
			if (e.key === KeyCode$1.END) {
				summaryRefs[summaryRefs.length - 1]?.element.focus();
				return;
			}
			const currentIndex = summaryRefs.findIndex((ref) => ref.element === e.currentTarget);
			if (currentIndex === -1) return;
			const nextIndex = e.key === KeyCode$1.ARROW_DOWN ? (currentIndex + 1) % summaryRefs.length : (currentIndex - 1 + summaryRefs.length) % summaryRefs.length;
			summaryRefs?.[nextIndex]?.element.focus();
		}
	};
	const enhancedProps = {
		...disclosureProps,
		onKeyDown: (e) => {
			handleKeyDown(e);
			if (disclosureProps.onKeyDown) disclosureProps.onKeyDown(e);
		},
		ref: (element) => {
			setButtonElement(element);
		}
	};
	return (0, import_jsx_runtime.jsx)("div", {
		role: "heading",
		"aria-level": ariaLevel,
		className: accordionSummaryBlock({
			size,
			arrow_position: arrowPosition
		}),
		children: children(enhancedProps, defaultSummary)
	});
}
var isAccordionSummaryComponent = isOfType(AccordionSummary);
AccordionSummary.displayName = "AccordionSummary";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Accordion/AccordionItem/AccordionItem.js
var AccordionItem = import_react.forwardRef(function AccordionItem(props, _ref) {
	const { children, expanded, defaultExpanded, disabled, keepMounted, value, summary, onUpdate, qa, className } = props;
	const attributes = useAccordion();
	const { id, isExpanded, handleUpdate } = useAccordionItemState({
		expanded,
		defaultExpanded,
		value,
		onUpdate
	});
	const [preparedSummary, details] = import_react.useMemo(() => {
		return prepareChildren$2(children, qa);
	}, [children, qa]);
	return (0, import_jsx_runtime.jsxs)(Disclosure, {
		arrowPosition: attributes?.arrowPosition,
		keepMounted,
		onUpdate: handleUpdate,
		expanded: defaultExpanded ? void 0 : isExpanded,
		defaultExpanded,
		summary,
		disabled,
		qa,
		className: accordionItemBlock({
			size: attributes?.size,
			view: attributes?.view,
			disabled
		}, className),
		children: [preparedSummary, (0, import_jsx_runtime.jsx)(Disclosure.Details, {
			className: accordionDetailsBlock,
			children: details
		})]
	}, value ?? id);
});
function useAccordionItemState({ expanded, defaultExpanded, value, onUpdate }) {
	const id = useUniqId();
	const { items, updateItems } = useAccordion();
	const isControlledItem = expanded !== void 0;
	const hasDefaultState = defaultExpanded !== void 0;
	return {
		id,
		isExpanded: import_react.useMemo(() => {
			if (isControlledItem) return expanded;
			if (hasDefaultState) return false;
			if (Array.isArray(items)) return items.includes(value ?? id);
			return items === (value ?? id);
		}, [
			isControlledItem,
			expanded,
			hasDefaultState,
			items,
			value,
			id
		]),
		handleUpdate: import_react.useCallback((next) => {
			onUpdate?.(next);
			if (!isControlledItem && !hasDefaultState) updateItems(value ?? id);
		}, [
			onUpdate,
			isControlledItem,
			hasDefaultState,
			updateItems,
			value,
			id
		])
	};
}
function prepareChildren$2(children, qa) {
	const items = import_react.Children.toArray(children);
	let accordionSummaryElement;
	let details;
	const content = [];
	for (const item of items) {
		if (isAccordionSummaryComponent(item)) {
			if (accordionSummaryElement) throw new Error("Only one <Accordion.Summary> component is allowed");
			accordionSummaryElement = item;
			continue;
		}
		content.push(item);
	}
	if (content.length > 0) details = (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: content });
	const summaryQa = qa ? `${qa}-summary` : void 0;
	const summaryChildren = accordionSummaryElement?.props?.children ?? ((props) => (0, import_jsx_runtime.jsx)(DefaultDisclosureSummary, {
		...props,
		qa: summaryQa,
		className: accordionSummaryTriggerBlock
	}));
	return [(0, import_jsx_runtime.jsx)(Disclosure.Summary, {
		qa: accordionSummaryElement?.props?.qa ?? summaryQa,
		children: (disclosureProps, defaultSummary) => (0, import_jsx_runtime.jsx)(AccordionSummaryContent, {
			disclosureProps,
			defaultSummary,
			children: summaryChildren
		})
	}), details];
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/i18n/i18n.js
var { lang, fallbackLang } = getConfig();
var i18n$1 = new I18N({
	lang,
	fallbackLang
});
subscribeConfigure((config) => {
	i18n$1.setLang(config.lang);
	i18n$1.setFallbackLang(config.fallbackLang);
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/i18n/addComponentKeysets.js
/**
* Add component's keysets data
* @param data - keysets data by languages
* @param componentName - name of the component
* @returns function to get keys' translations for current language
* @example
* ```
* import {addComponentKeysets} from '@gravity-ui/uikit/i18n';
* import en from './en.json';
* import ru from './ru.json';
*
* const t = addComponentKeysets({en, ru}, 'Alert');
*
* console.log(t('label_close')); // 'Close'
* ```
*/
function addComponentKeysets(data, componentName) {
	Object.entries(data).forEach(([lang, keys]) => i18n$1.registerKeyset(lang, componentName, keys));
	const translateFunction = i18n$1.keyset(componentName);
	const useTranslation = () => {
		const langConfig = useLang();
		const { lang = "en", fallbackLang = "en" } = i18n$1;
		return { t: import_react.useCallback((...params) => {
			i18n$1.setLang(langConfig.lang);
			i18n$1.setFallbackLang(langConfig.fallbackLang);
			const result = translateFunction(...params);
			i18n$1.setLang(lang);
			i18n$1.setFallbackLang(fallbackLang);
			return result;
		}, [
			fallbackLang,
			lang,
			langConfig.fallbackLang,
			langConfig.lang
		]) };
	};
	Object.assign(translateFunction, {
		useTranslation,
		Translation: ({ children }) => {
			return children(useTranslation());
		}
	});
	return translateFunction;
}
var i18n_default$21 = addComponentKeysets({
	en: { label: "Accordion" },
	ru: { label: "Аккордеон" }
}, "Accordion");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Accordion/Accordion.js
var Accordion = import_react.forwardRef(function Accordion(rawProps, ref) {
	const props = useDefaultProps("Accordion", rawProps);
	const { t } = i18n_default$21.useTranslation();
	const { size = "m", view = "solid", multiple = false, className, arrowPosition = "end", qa, defaultValue, onUpdate = () => {}, children, ariaLevel = 3, value, ariaLabel = t("label") } = props;
	return (0, import_jsx_runtime.jsx)(AccordionProvider, {
		size,
		view,
		multiple,
		arrowPosition,
		onUpdate,
		defaultValue,
		value,
		ariaLevel,
		children: (0, import_jsx_runtime.jsx)("div", {
			className: accordionBlock({
				size,
				view
			}, className),
			"data-qa": qa,
			ref,
			role: "region",
			"aria-label": ariaLabel,
			children
		})
	});
});
Accordion.Item = AccordionItem;
Accordion.Summary = AccordionSummary;
Accordion.displayName = "Accordion";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/filterDOMProps.js
var DOMPropNames = /* @__PURE__ */ new Set(["id"]);
var labelablePropNames = /* @__PURE__ */ new Set([
	"aria-label",
	"aria-labelledby",
	"aria-describedby",
	"aria-details"
]);
var propRe = /^(data-.*)$/;
function filterDOMProps(props, options = {}) {
	const { labelable, propNames } = options;
	const filteredProps = {};
	for (const prop in props) if (Object.prototype.hasOwnProperty.call(props, prop) && (DOMPropNames.has(prop) || labelable && labelablePropNames.has(prop) || propNames?.has(prop) || propRe.test(prop))) filteredProps[prop] = props[prop];
	return filteredProps;
}
var defsByPlatform = {
	pc: {
		NormalizeMap: {
			arrowup: "up",
			arrowdown: "down",
			arrowleft: "left",
			arrowright: "right",
			esc: "escape",
			return: "enter",
			mod: "ctrl",
			control: "ctrl",
			opt: "alt",
			option: "alt",
			cmd: "ctrl",
			command: "ctrl"
		},
		Priority: {
			shift: 200,
			alt: 300,
			ctrl: 400
		},
		DisplayName: {
			up: "↑",
			down: "↓",
			left: "←",
			right: "→",
			escape: "Esc",
			plus: "＋",
			minus: "－",
			enter: "Enter",
			ctrl: "Ctrl",
			alt: "Alt",
			shift: "Shift",
			tab: "Tab",
			backspace: "Backspace"
		}
	},
	mac: {
		NormalizeMap: {
			arrowup: "up",
			arrowdown: "down",
			arrowleft: "left",
			arrowright: "right",
			esc: "escape",
			enter: "return",
			mod: "command",
			ctrl: "control",
			alt: "option",
			opt: "option",
			cmd: "command"
		},
		Priority: {
			command: 100,
			shift: 200,
			option: 300,
			control: 400
		},
		DisplayName: {
			up: "▲",
			down: "▼",
			left: "◀",
			right: "▶",
			escape: "esc",
			plus: "＋",
			minus: "－",
			return: "⏎",
			command: "⌘",
			option: "⌥",
			control: "⌃",
			shift: "⇧",
			backspace: "⌫",
			tab: "⇥"
		}
	}
};
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseFindIndex.js
var require__baseFindIndex = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* The base implementation of `_.findIndex` and `_.findLastIndex` without
	* support for iteratee shorthands.
	*
	* @private
	* @param {Array} array The array to inspect.
	* @param {Function} predicate The function invoked per iteration.
	* @param {number} fromIndex The index to search from.
	* @param {boolean} [fromRight] Specify iterating from right to left.
	* @returns {number} Returns the index of the matched value, else `-1`.
	*/
	function baseFindIndex(array, predicate, fromIndex, fromRight) {
		var length = array.length, index = fromIndex + (fromRight ? 1 : -1);
		while (fromRight ? index-- : ++index < length) if (predicate(array[index], index, array)) return index;
		return -1;
	}
	module.exports = baseFindIndex;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseIsNaN.js
var require__baseIsNaN = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* The base implementation of `_.isNaN` without support for number objects.
	*
	* @private
	* @param {*} value The value to check.
	* @returns {boolean} Returns `true` if `value` is `NaN`, else `false`.
	*/
	function baseIsNaN(value) {
		return value !== value;
	}
	module.exports = baseIsNaN;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_strictIndexOf.js
var require__strictIndexOf = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* A specialized version of `_.indexOf` which performs strict equality
	* comparisons of values, i.e. `===`.
	*
	* @private
	* @param {Array} array The array to inspect.
	* @param {*} value The value to search for.
	* @param {number} fromIndex The index to search from.
	* @returns {number} Returns the index of the matched value, else `-1`.
	*/
	function strictIndexOf(array, value, fromIndex) {
		var index = fromIndex - 1, length = array.length;
		while (++index < length) if (array[index] === value) return index;
		return -1;
	}
	module.exports = strictIndexOf;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseIndexOf.js
var require__baseIndexOf = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseFindIndex = require__baseFindIndex();
	var baseIsNaN = require__baseIsNaN();
	var strictIndexOf = require__strictIndexOf();
	/**
	* The base implementation of `_.indexOf` without `fromIndex` bounds checks.
	*
	* @private
	* @param {Array} array The array to inspect.
	* @param {*} value The value to search for.
	* @param {number} fromIndex The index to search from.
	* @returns {number} Returns the index of the matched value, else `-1`.
	*/
	function baseIndexOf(array, value, fromIndex) {
		return value === value ? strictIndexOf(array, value, fromIndex) : baseFindIndex(array, baseIsNaN, fromIndex);
	}
	module.exports = baseIndexOf;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_arrayIncludes.js
var require__arrayIncludes = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseIndexOf = require__baseIndexOf();
	/**
	* A specialized version of `_.includes` for arrays without support for
	* specifying an index to search from.
	*
	* @private
	* @param {Array} [array] The array to inspect.
	* @param {*} target The value to search for.
	* @returns {boolean} Returns `true` if `target` is found, else `false`.
	*/
	function arrayIncludes(array, value) {
		return !!(array == null ? 0 : array.length) && baseIndexOf(array, value, 0) > -1;
	}
	module.exports = arrayIncludes;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_arrayIncludesWith.js
var require__arrayIncludesWith = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* This function is like `arrayIncludes` except that it accepts a comparator.
	*
	* @private
	* @param {Array} [array] The array to inspect.
	* @param {*} target The value to search for.
	* @param {Function} comparator The comparator invoked per element.
	* @returns {boolean} Returns `true` if `target` is found, else `false`.
	*/
	function arrayIncludesWith(array, value, comparator) {
		var index = -1, length = array == null ? 0 : array.length;
		while (++index < length) if (comparator(value, array[index])) return true;
		return false;
	}
	module.exports = arrayIncludesWith;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/noop.js
var require_noop = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* This method returns `undefined`.
	*
	* @static
	* @memberOf _
	* @since 2.3.0
	* @category Util
	* @example
	*
	* _.times(2, _.noop);
	* // => [undefined, undefined]
	*/
	function noop() {}
	module.exports = noop;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_createSet.js
var require__createSet = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Set = require__Set();
	var noop = require_noop();
	var setToArray = require__setToArray();
	module.exports = !(Set && 1 / setToArray(new Set([, -0]))[1] == Infinity) ? noop : function(values) {
		return new Set(values);
	};
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseUniq.js
var require__baseUniq = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SetCache = require__SetCache();
	var arrayIncludes = require__arrayIncludes();
	var arrayIncludesWith = require__arrayIncludesWith();
	var cacheHas = require__cacheHas();
	var createSet = require__createSet();
	var setToArray = require__setToArray();
	/** Used as the size to enable large array optimizations. */
	var LARGE_ARRAY_SIZE = 200;
	/**
	* The base implementation of `_.uniqBy` without support for iteratee shorthands.
	*
	* @private
	* @param {Array} array The array to inspect.
	* @param {Function} [iteratee] The iteratee invoked per element.
	* @param {Function} [comparator] The comparator invoked per element.
	* @returns {Array} Returns the new duplicate free array.
	*/
	function baseUniq(array, iteratee, comparator) {
		var index = -1, includes = arrayIncludes, length = array.length, isCommon = true, result = [], seen = result;
		if (comparator) {
			isCommon = false;
			includes = arrayIncludesWith;
		} else if (length >= LARGE_ARRAY_SIZE) {
			var set = iteratee ? null : createSet(array);
			if (set) return setToArray(set);
			isCommon = false;
			includes = cacheHas;
			seen = new SetCache();
		} else seen = iteratee ? [] : result;
		outer: while (++index < length) {
			var value = array[index], computed = iteratee ? iteratee(value) : value;
			value = comparator || value !== 0 ? value : 0;
			if (isCommon && computed === computed) {
				var seenIndex = seen.length;
				while (seenIndex--) if (seen[seenIndex] === computed) continue outer;
				if (iteratee) seen.push(computed);
				result.push(value);
			} else if (!includes(seen, computed, comparator)) {
				if (seen !== result) seen.push(computed);
				result.push(value);
			}
		}
		return result;
	}
	module.exports = baseUniq;
}));
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Hotkey/utils.js
var import_uniqBy = /* @__PURE__ */ __toESM((/* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseIteratee = require__baseIteratee();
	var baseUniq = require__baseUniq();
	/**
	* This method is like `_.uniq` except that it accepts `iteratee` which is
	* invoked for each element in `array` to generate the criterion by which
	* uniqueness is computed. The order of result values is determined by the
	* order they occur in the array. The iteratee is invoked with one argument:
	* (value).
	*
	* @static
	* @memberOf _
	* @since 4.0.0
	* @category Array
	* @param {Array} array The array to inspect.
	* @param {Function} [iteratee=_.identity] The iteratee invoked per element.
	* @returns {Array} Returns the new duplicate free array.
	* @example
	*
	* _.uniqBy([2.1, 1.2, 2.3], Math.floor);
	* // => [2.1, 1.2]
	*
	* // The `_.property` iteratee shorthand.
	* _.uniqBy([{ 'x': 1 }, { 'x': 2 }, { 'x': 1 }], 'x');
	* // => [{ 'x': 1 }, { 'x': 2 }]
	*/
	function uniqBy(array, iteratee) {
		return array && array.length ? baseUniq(array, baseIteratee(iteratee, 2)) : [];
	}
	module.exports = uniqBy;
})))(), 1);
var isMac$1 = () => typeof navigator !== "undefined" ? /Mac|iP(hone|[oa]d)/.test(navigator.platform) : false;
function split(val, separator) {
	return val.trim().split(separator).filter(Boolean);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Hotkey/parse.js
var GROUPS_SEPARATOR = /\s/;
var KEYS_SEPARATOR = "+";
function parseKeyGroups(defs, value) {
	return split(value, GROUPS_SEPARATOR).map((keys) => (0, import_uniqBy.default)(split(keys, KEYS_SEPARATOR).map(keyParser(defs)).sort((a, b) => b.priority - a.priority), (key) => key.id).map(renderKey));
}
function keyParser(defs) {
	return function(raw) {
		const keyId = getKeyId(defs, raw);
		return {
			raw,
			id: keyId,
			priority: defs.Priority[keyId] ?? 0,
			displayName: defs.DisplayName[keyId]
		};
	};
}
function getKeyId(defs, val) {
	val = val.toLowerCase();
	return defs.NormalizeMap[val] ?? val;
}
function renderKey(key) {
	return key.displayName ?? (0, import_capitalize.default)(key.id);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Hotkey/Hotkey.js
var b$108 = block("hotkey");
var Spaces = {
	BetweenGroups: String.fromCharCode(160),
	BetweenKeys: String.fromCharCode(8239)
};
var Hotkey = import_react.forwardRef(function Hotkey(rawProps, ref) {
	const { value, platform, view = "light", qa, style, className, ...restProps } = useDefaultProps("Hotkey", rawProps);
	const groups = parseHotkeys(value, { platform });
	const content = [];
	let hasGroups = false;
	groups.forEach((keys, groupIdx) => {
		if (keys.length === 0) return;
		if (hasGroups) content.push(Spaces.BetweenGroups);
		else hasGroups = true;
		keys.forEach((key, keyIdx) => {
			if (!(keyIdx === 0)) content.push(Spaces.BetweenKeys, (0, import_jsx_runtime.jsx)("span", {
				className: b$108("plus"),
				children: "+"
			}, `${key}_${groupIdx}_${keyIdx}_plus`), Spaces.BetweenKeys);
			content.push((0, import_jsx_runtime.jsx)("kbd", { children: key }, `${key}_${groupIdx}_${keyIdx}`));
		});
	});
	if (content.length === 0) return null;
	return (0, import_jsx_runtime.jsx)("kbd", {
		...filterDOMProps(restProps, { labelable: true }),
		ref,
		style,
		"data-qa": qa,
		className: b$108({ view }, className),
		children: content
	});
});
function parseHotkeys(value, opts) {
	const defs = defsByPlatform[opts.platform ?? (isMac$1() ? "mac" : "pc")];
	return parseKeyGroups(defs, value);
}
//#endregion
//#region node_modules/.pnpm/@floating-ui+utils@0.2.11/node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs
function hasWindow() {
	return typeof window !== "undefined";
}
function getNodeName(node) {
	if (isNode(node)) return (node.nodeName || "").toLowerCase();
	return "#document";
}
function getWindow(node) {
	var _node$ownerDocument;
	return (node == null || (_node$ownerDocument = node.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
}
function getDocumentElement$1(node) {
	var _ref;
	return (_ref = (isNode(node) ? node.ownerDocument : node.document) || window.document) == null ? void 0 : _ref.documentElement;
}
function isNode(value) {
	if (!hasWindow()) return false;
	return value instanceof Node || value instanceof getWindow(value).Node;
}
function isElement$2(value) {
	if (!hasWindow()) return false;
	return value instanceof Element || value instanceof getWindow(value).Element;
}
function isHTMLElement(value) {
	if (!hasWindow()) return false;
	return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
}
function isShadowRoot(value) {
	if (!hasWindow() || typeof ShadowRoot === "undefined") return false;
	return value instanceof ShadowRoot || value instanceof getWindow(value).ShadowRoot;
}
function isOverflowElement(element) {
	const { overflow, overflowX, overflowY, display } = getComputedStyle$1(element);
	return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && display !== "inline" && display !== "contents";
}
function isTableElement(element) {
	return /^(table|td|th)$/.test(getNodeName(element));
}
function isTopLayer(element) {
	try {
		if (element.matches(":popover-open")) return true;
	} catch (_e) {}
	try {
		return element.matches(":modal");
	} catch (_e) {
		return false;
	}
}
var willChangeRe = /transform|translate|scale|rotate|perspective|filter/;
var containRe = /paint|layout|strict|content/;
var isNotNone = (value) => !!value && value !== "none";
var isWebKitValue;
function isContainingBlock(elementOrCss) {
	const css = isElement$2(elementOrCss) ? getComputedStyle$1(elementOrCss) : elementOrCss;
	return isNotNone(css.transform) || isNotNone(css.translate) || isNotNone(css.scale) || isNotNone(css.rotate) || isNotNone(css.perspective) || !isWebKit() && (isNotNone(css.backdropFilter) || isNotNone(css.filter)) || willChangeRe.test(css.willChange || "") || containRe.test(css.contain || "");
}
function getContainingBlock(element) {
	let currentNode = getParentNode(element);
	while (isHTMLElement(currentNode) && !isLastTraversableNode(currentNode)) {
		if (isContainingBlock(currentNode)) return currentNode;
		else if (isTopLayer(currentNode)) return null;
		currentNode = getParentNode(currentNode);
	}
	return null;
}
function isWebKit() {
	if (isWebKitValue == null) isWebKitValue = typeof CSS !== "undefined" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none");
	return isWebKitValue;
}
function isLastTraversableNode(node) {
	return /^(html|body|#document)$/.test(getNodeName(node));
}
function getComputedStyle$1(element) {
	return getWindow(element).getComputedStyle(element);
}
function getNodeScroll(element) {
	if (isElement$2(element)) return {
		scrollLeft: element.scrollLeft,
		scrollTop: element.scrollTop
	};
	return {
		scrollLeft: element.scrollX,
		scrollTop: element.scrollY
	};
}
function getParentNode(node) {
	if (getNodeName(node) === "html") return node;
	const result = node.assignedSlot || node.parentNode || isShadowRoot(node) && node.host || getDocumentElement$1(node);
	return isShadowRoot(result) ? result.host : result;
}
function getNearestOverflowAncestor(node) {
	const parentNode = getParentNode(node);
	if (isLastTraversableNode(parentNode)) return node.ownerDocument ? node.ownerDocument.body : node.body;
	if (isHTMLElement(parentNode) && isOverflowElement(parentNode)) return parentNode;
	return getNearestOverflowAncestor(parentNode);
}
function getOverflowAncestors(node, list, traverseIframes) {
	var _node$ownerDocument2;
	if (list === void 0) list = [];
	if (traverseIframes === void 0) traverseIframes = true;
	const scrollableAncestor = getNearestOverflowAncestor(node);
	const isBody = scrollableAncestor === ((_node$ownerDocument2 = node.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
	const win = getWindow(scrollableAncestor);
	if (isBody) {
		const frameElement = getFrameElement(win);
		return list.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
	} else return list.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
}
function getFrameElement(win) {
	return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
}
//#endregion
//#region node_modules/.pnpm/@floating-ui+utils@0.2.11/node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs
/**
* Custom positioning reference element.
* @see https://floating-ui.com/docs/virtual-elements
*/
var sides = [
	"top",
	"right",
	"bottom",
	"left"
];
var alignments = ["start", "end"];
var placements = /*#__PURE__*/ sides.reduce((acc, side) => acc.concat(side, side + "-" + alignments[0], side + "-" + alignments[1]), []);
var min = Math.min;
var max = Math.max;
var round$1 = Math.round;
var floor = Math.floor;
var createCoords = (v) => ({
	x: v,
	y: v
});
var oppositeSideMap = {
	left: "right",
	right: "left",
	bottom: "top",
	top: "bottom"
};
function clamp(start, value, end) {
	return max(start, min(value, end));
}
function evaluate(value, param) {
	return typeof value === "function" ? value(param) : value;
}
function getSide(placement) {
	return placement.split("-")[0];
}
function getAlignment(placement) {
	return placement.split("-")[1];
}
function getOppositeAxis(axis) {
	return axis === "x" ? "y" : "x";
}
function getAxisLength(axis) {
	return axis === "y" ? "height" : "width";
}
function getSideAxis(placement) {
	const firstChar = placement[0];
	return firstChar === "t" || firstChar === "b" ? "y" : "x";
}
function getAlignmentAxis(placement) {
	return getOppositeAxis(getSideAxis(placement));
}
function getAlignmentSides(placement, rects, rtl) {
	if (rtl === void 0) rtl = false;
	const alignment = getAlignment(placement);
	const alignmentAxis = getAlignmentAxis(placement);
	const length = getAxisLength(alignmentAxis);
	let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
	if (rects.reference[length] > rects.floating[length]) mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
	return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
}
function getExpandedPlacements(placement) {
	const oppositePlacement = getOppositePlacement(placement);
	return [
		getOppositeAlignmentPlacement(placement),
		oppositePlacement,
		getOppositeAlignmentPlacement(oppositePlacement)
	];
}
function getOppositeAlignmentPlacement(placement) {
	return placement.includes("start") ? placement.replace("start", "end") : placement.replace("end", "start");
}
var lrPlacement = ["left", "right"];
var rlPlacement = ["right", "left"];
var tbPlacement = ["top", "bottom"];
var btPlacement = ["bottom", "top"];
function getSideList(side, isStart, rtl) {
	switch (side) {
		case "top":
		case "bottom":
			if (rtl) return isStart ? rlPlacement : lrPlacement;
			return isStart ? lrPlacement : rlPlacement;
		case "left":
		case "right": return isStart ? tbPlacement : btPlacement;
		default: return [];
	}
}
function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
	const alignment = getAlignment(placement);
	let list = getSideList(getSide(placement), direction === "start", rtl);
	if (alignment) {
		list = list.map((side) => side + "-" + alignment);
		if (flipAlignment) list = list.concat(list.map(getOppositeAlignmentPlacement));
	}
	return list;
}
function getOppositePlacement(placement) {
	const side = getSide(placement);
	return oppositeSideMap[side] + placement.slice(side.length);
}
function expandPaddingObject(padding) {
	return {
		top: 0,
		right: 0,
		bottom: 0,
		left: 0,
		...padding
	};
}
function getPaddingObject(padding) {
	return typeof padding !== "number" ? expandPaddingObject(padding) : {
		top: padding,
		right: padding,
		bottom: padding,
		left: padding
	};
}
function rectToClientRect(rect) {
	const { x, y, width, height } = rect;
	return {
		width,
		height,
		top: y,
		left: x,
		right: x + width,
		bottom: y + height,
		x,
		y
	};
}
/*!
* tabbable 6.5.0
* @license MIT, https://github.com/focus-trap/tabbable/blob/master/LICENSE
*/
var candidateSelector = /* #__PURE__ */ [
	"input:not([inert]):not([inert] *)",
	"select:not([inert]):not([inert] *)",
	"textarea:not([inert]):not([inert] *)",
	"a[href]:not([inert]):not([inert] *)",
	"area[href]:not([inert]):not([inert] *)",
	"button:not([inert]):not([inert] *)",
	"[tabindex]:not(slot):not([inert]):not([inert] *)",
	"audio[controls]:not([inert]):not([inert] *)",
	"video[controls]:not([inert]):not([inert] *)",
	"[contenteditable]:not([contenteditable=\"false\"]):not([inert]):not([inert] *)",
	"details>summary:first-of-type:not([inert]):not([inert] *)",
	"details:not([inert]):not([inert] *)"
].join(",");
var NoElement = typeof Element === "undefined";
var matches = NoElement ? function() {} : Element.prototype.matches || Element.prototype.msMatchesSelector || Element.prototype.webkitMatchesSelector;
var getRootNode = !NoElement && Element.prototype.getRootNode ? function(element) {
	var _element$getRootNode;
	return element === null || element === void 0 ? void 0 : (_element$getRootNode = element.getRootNode) === null || _element$getRootNode === void 0 ? void 0 : _element$getRootNode.call(element);
} : function(element) {
	return element === null || element === void 0 ? void 0 : element.ownerDocument;
};
/**
* Determines if a node is inert or in an inert ancestor.
* @param {Node} [node]
* @param {boolean} [lookUp] If true and `node` is not inert, looks up at ancestors to
*  see if any of them are inert. If false, only `node` itself is considered.
* @returns {boolean} True if inert itself or by way of being in an inert ancestor.
*  False if `node` is falsy.
*/
var _isInert = function isInert(node, lookUp) {
	var _node$getAttribute;
	if (lookUp === void 0) lookUp = true;
	var inertAtt = node === null || node === void 0 ? void 0 : (_node$getAttribute = node.getAttribute) === null || _node$getAttribute === void 0 ? void 0 : _node$getAttribute.call(node, "inert");
	return inertAtt === "" || inertAtt === "true" || lookUp && node && (typeof node.closest === "function" ? node.closest("[inert]") : _isInert(node.parentNode));
};
/**
* Determines if a node's content is editable.
* @param {Element} [node]
* @returns True if it's content-editable; false if it's not or `node` is falsy.
*/
var isContentEditable = function isContentEditable(node) {
	var _node$getAttribute2;
	var attValue = node === null || node === void 0 ? void 0 : (_node$getAttribute2 = node.getAttribute) === null || _node$getAttribute2 === void 0 ? void 0 : _node$getAttribute2.call(node, "contenteditable");
	return attValue === "" || attValue === "true";
};
/**
* @param {Element} el container to check in
* @param {boolean} includeContainer add container to check
* @param {(node: Element) => boolean} filter filter candidates
* @returns {Element[]}
*/
var getCandidates = function getCandidates(el, includeContainer, filter) {
	if (_isInert(el)) return [];
	var candidates = Array.prototype.slice.apply(el.querySelectorAll(candidateSelector));
	if (includeContainer && matches.call(el, candidateSelector)) candidates.unshift(el);
	candidates = candidates.filter(filter);
	return candidates;
};
/**
* @callback GetShadowRoot
* @param {Element} element to check for shadow root
* @returns {ShadowRoot|boolean} ShadowRoot if available or boolean indicating if a shadowRoot is attached but not available.
*/
/**
* @callback ShadowRootFilter
* @param {Element} shadowHostNode the element which contains shadow content
* @returns {boolean} true if a shadow root could potentially contain valid candidates.
*/
/**
* @typedef {Object} CandidateScope
* @property {Element} scopeParent contains inner candidates
* @property {Element[]} candidates list of candidates found in the scope parent
*/
/**
* @typedef {Object} IterativeOptions
* @property {GetShadowRoot|boolean} getShadowRoot true if shadow support is enabled; falsy if not;
*  if a function, implies shadow support is enabled and either returns the shadow root of an element
*  or a boolean stating if it has an undisclosed shadow root
* @property {(node: Element) => boolean} filter filter candidates
* @property {boolean} flatten if true then result will flatten any CandidateScope into the returned list
* @property {ShadowRootFilter} shadowRootFilter filter shadow roots;
*/
/**
* @param {Element[]} elements list of element containers to match candidates from
* @param {boolean} includeContainer add container list to check
* @param {IterativeOptions} options
* @returns {Array.<Element|CandidateScope>}
*/
var _getCandidatesIteratively = function getCandidatesIteratively(elements, includeContainer, options) {
	var candidates = [];
	var elementsToCheck = Array.from(elements);
	while (elementsToCheck.length) {
		var element = elementsToCheck.shift();
		if (_isInert(element, false)) continue;
		if (element.tagName === "SLOT") {
			var assigned = element.assignedElements();
			var nestedCandidates = _getCandidatesIteratively(assigned.length ? assigned : element.children, true, options);
			if (options.flatten) candidates.push.apply(candidates, nestedCandidates);
			else candidates.push({
				scopeParent: element,
				candidates: nestedCandidates
			});
		} else {
			if (matches.call(element, candidateSelector) && options.filter(element) && (includeContainer || !elements.includes(element))) candidates.push(element);
			var shadowRoot = element.shadowRoot || typeof options.getShadowRoot === "function" && options.getShadowRoot(element);
			var validShadowRoot = !_isInert(shadowRoot, false) && (!options.shadowRootFilter || options.shadowRootFilter(element));
			if (shadowRoot && validShadowRoot) {
				var _nestedCandidates = _getCandidatesIteratively(shadowRoot === true ? element.children : shadowRoot.children, true, options);
				if (options.flatten) candidates.push.apply(candidates, _nestedCandidates);
				else candidates.push({
					scopeParent: element,
					candidates: _nestedCandidates
				});
			} else elementsToCheck.unshift.apply(elementsToCheck, element.children);
		}
	}
	return candidates;
};
/**
* @private
* Determines if the node has an explicitly specified `tabindex` attribute.
* @param {HTMLElement} node
* @returns {boolean} True if so; false if not.
*/
var hasTabIndex = function hasTabIndex(node) {
	return !isNaN(parseInt(node.getAttribute("tabindex"), 10));
};
/**
* Determine the tab index of a given node.
* @param {HTMLElement} node
* @returns {number} Tab order (negative, 0, or positive number).
* @throws {Error} If `node` is falsy.
*/
var getTabIndex = function getTabIndex(node) {
	if (!node) throw new Error("No node provided");
	if (node.tabIndex < 0) {
		if ((/^(AUDIO|VIDEO|DETAILS)$/.test(node.tagName) || isContentEditable(node)) && !hasTabIndex(node)) return 0;
	}
	return node.tabIndex;
};
/**
* Determine the tab index of a given node __for sort order purposes__.
* @param {HTMLElement} node
* @param {boolean} [isScope] True for a custom element with shadow root or slot that, by default,
*  has tabIndex -1, but needs to be sorted by document order in order for its content to be
*  inserted into the correct sort position.
* @returns {number} Tab order (negative, 0, or positive number).
*/
var getSortOrderTabIndex = function getSortOrderTabIndex(node, isScope) {
	var tabIndex = getTabIndex(node);
	if (tabIndex < 0 && isScope && !hasTabIndex(node)) return 0;
	return tabIndex;
};
var sortOrderedTabbables = function sortOrderedTabbables(a, b) {
	return a.tabIndex === b.tabIndex ? a.documentOrder - b.documentOrder : a.tabIndex - b.tabIndex;
};
var isInput = function isInput(node) {
	return node.tagName === "INPUT";
};
var isHiddenInput = function isHiddenInput(node) {
	return isInput(node) && node.type === "hidden";
};
var isDetailsWithSummary = function isDetailsWithSummary(node) {
	return node.tagName === "DETAILS" && Array.prototype.slice.apply(node.children).some(function(child) {
		return child.tagName === "SUMMARY";
	});
};
var getCheckedRadio = function getCheckedRadio(nodes, form) {
	for (var i = 0; i < nodes.length; i++) if (nodes[i].checked && nodes[i].form === form) return nodes[i];
};
var isTabbableRadio = function isTabbableRadio(node) {
	if (!node.name) return true;
	var radioScope = node.form || getRootNode(node);
	var queryRadios = function queryRadios(name) {
		return radioScope.querySelectorAll("input[type=\"radio\"][name=\"" + name + "\"]");
	};
	var radioSet;
	if (typeof window !== "undefined" && typeof window.CSS !== "undefined" && typeof window.CSS.escape === "function") radioSet = queryRadios(window.CSS.escape(node.name));
	else try {
		radioSet = queryRadios(node.name);
	} catch (err) {
		console.error("Looks like you have a radio button with a name attribute containing invalid CSS selector characters and need the CSS.escape polyfill: %s", err.message);
		return false;
	}
	var checked = getCheckedRadio(radioSet, node.form);
	return !checked || checked === node;
};
var isRadio = function isRadio(node) {
	return isInput(node) && node.type === "radio";
};
var isNonTabbableRadio = function isNonTabbableRadio(node) {
	return isRadio(node) && !isTabbableRadio(node);
};
var isNodeAttached = function isNodeAttached(node) {
	var _nodeRoot;
	var nodeRoot = node && getRootNode(node);
	var nodeRootHost = (_nodeRoot = nodeRoot) === null || _nodeRoot === void 0 ? void 0 : _nodeRoot.host;
	var attached = false;
	if (nodeRoot && nodeRoot !== node) {
		var _nodeRootHost, _nodeRootHost$ownerDo, _node$ownerDocument;
		attached = !!((_nodeRootHost = nodeRootHost) !== null && _nodeRootHost !== void 0 && (_nodeRootHost$ownerDo = _nodeRootHost.ownerDocument) !== null && _nodeRootHost$ownerDo !== void 0 && _nodeRootHost$ownerDo.contains(nodeRootHost) || node !== null && node !== void 0 && (_node$ownerDocument = node.ownerDocument) !== null && _node$ownerDocument !== void 0 && _node$ownerDocument.contains(node));
		while (!attached && nodeRootHost) {
			var _nodeRoot2, _nodeRootHost2, _nodeRootHost2$ownerD;
			nodeRoot = getRootNode(nodeRootHost);
			nodeRootHost = (_nodeRoot2 = nodeRoot) === null || _nodeRoot2 === void 0 ? void 0 : _nodeRoot2.host;
			attached = !!((_nodeRootHost2 = nodeRootHost) !== null && _nodeRootHost2 !== void 0 && (_nodeRootHost2$ownerD = _nodeRootHost2.ownerDocument) !== null && _nodeRootHost2$ownerD !== void 0 && _nodeRootHost2$ownerD.contains(nodeRootHost));
		}
	}
	return attached;
};
var isZeroArea = function isZeroArea(node) {
	var _node$getBoundingClie = node.getBoundingClientRect(), width = _node$getBoundingClie.width, height = _node$getBoundingClie.height;
	return width === 0 && height === 0;
};
var isHidden = function isHidden(node, _ref) {
	var displayCheck = _ref.displayCheck, getShadowRoot = _ref.getShadowRoot;
	if (displayCheck === "full-native") {
		if ("checkVisibility" in node) return !node.checkVisibility({
			checkOpacity: false,
			opacityProperty: false,
			contentVisibilityAuto: true,
			visibilityProperty: true,
			checkVisibilityCSS: true
		});
	}
	var visibility = getComputedStyle(node).visibility;
	if (visibility === "hidden" || visibility === "collapse") return true;
	var nodeUnderDetails = matches.call(node, "details>summary:first-of-type") ? node.parentElement : node;
	if (matches.call(nodeUnderDetails, "details:not([open]) *")) return true;
	if (!displayCheck || displayCheck === "full" || displayCheck === "full-native" || displayCheck === "legacy-full") {
		if (typeof getShadowRoot === "function") {
			var originalNode = node;
			while (node) {
				var parentElement = node.parentElement;
				var rootNode = getRootNode(node);
				if (parentElement && !parentElement.shadowRoot && getShadowRoot(parentElement) === true) return isZeroArea(node);
				else if (node.assignedSlot) node = node.assignedSlot;
				else if (!parentElement && rootNode !== node.ownerDocument) node = rootNode.host;
				else node = parentElement;
			}
			node = originalNode;
		}
		if (isNodeAttached(node)) return !node.getClientRects().length;
		if (displayCheck !== "legacy-full") return true;
	} else if (displayCheck === "non-zero-area") return isZeroArea(node);
	return false;
};
var isDisabledFromFieldset = function isDisabledFromFieldset(node) {
	if (/^(INPUT|BUTTON|SELECT|TEXTAREA)$/.test(node.tagName)) {
		var parentNode = node.parentElement;
		while (parentNode) {
			if (parentNode.tagName === "FIELDSET" && parentNode.disabled) {
				for (var i = 0; i < parentNode.children.length; i++) {
					var child = parentNode.children.item(i);
					if (child.tagName === "LEGEND") return matches.call(parentNode, "fieldset[disabled] *") ? true : !child.contains(node);
				}
				return true;
			}
			parentNode = parentNode.parentElement;
		}
	}
	return false;
};
var isNodeMatchingSelectorFocusable = function isNodeMatchingSelectorFocusable(options, node) {
	if (node.disabled || isHiddenInput(node) || isHidden(node, options) || isDetailsWithSummary(node) || isDisabledFromFieldset(node)) return false;
	return true;
};
var isNodeMatchingSelectorTabbable = function isNodeMatchingSelectorTabbable(options, node) {
	if (isNonTabbableRadio(node) || getTabIndex(node) < 0 || !isNodeMatchingSelectorFocusable(options, node)) return false;
	return true;
};
var isShadowRootTabbable = function isShadowRootTabbable(shadowHostNode) {
	var tabIndex = parseInt(shadowHostNode.getAttribute("tabindex"), 10);
	if (isNaN(tabIndex) || tabIndex >= 0) return true;
	return false;
};
/**
* @param {Array.<Element|CandidateScope>} candidates
* @returns Element[]
*/
var _sortByOrder = function sortByOrder(candidates) {
	var regularTabbables = [];
	var orderedTabbables = [];
	candidates.forEach(function(item, i) {
		var isScope = !!item.scopeParent;
		var element = isScope ? item.scopeParent : item;
		var candidateTabindex = getSortOrderTabIndex(element, isScope);
		var elements = isScope ? _sortByOrder(item.candidates) : element;
		if (candidateTabindex === 0) isScope ? regularTabbables.push.apply(regularTabbables, elements) : regularTabbables.push(element);
		else orderedTabbables.push({
			documentOrder: i,
			tabIndex: candidateTabindex,
			item,
			isScope,
			content: elements
		});
	});
	return orderedTabbables.sort(sortOrderedTabbables).reduce(function(acc, sortable) {
		sortable.isScope ? acc.push.apply(acc, sortable.content) : acc.push(sortable.content);
		return acc;
	}, []).concat(regularTabbables);
};
var tabbable = function tabbable(container, options) {
	options = options || {};
	var candidates;
	if (options.getShadowRoot) candidates = _getCandidatesIteratively([container], options.includeContainer, {
		filter: isNodeMatchingSelectorTabbable.bind(null, options),
		flatten: false,
		getShadowRoot: options.getShadowRoot,
		shadowRootFilter: isShadowRootTabbable
	});
	else candidates = getCandidates(container, options.includeContainer, isNodeMatchingSelectorTabbable.bind(null, options));
	return _sortByOrder(candidates);
};
var focusable = function focusable(container, options) {
	options = options || {};
	var candidates;
	if (options.getShadowRoot) candidates = _getCandidatesIteratively([container], options.includeContainer, {
		filter: isNodeMatchingSelectorFocusable.bind(null, options),
		flatten: true,
		getShadowRoot: options.getShadowRoot
	});
	else candidates = getCandidates(container, options.includeContainer, isNodeMatchingSelectorFocusable.bind(null, options));
	return candidates;
};
var isTabbable = function isTabbable(node, options) {
	options = options || {};
	if (!node) throw new Error("No node provided");
	if (matches.call(node, candidateSelector) === false) return false;
	return isNodeMatchingSelectorTabbable(options, node);
};
//#endregion
//#region node_modules/.pnpm/@floating-ui+react@0.27.19_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@floating-ui/react/dist/floating-ui.react.utils.mjs
function getPlatform() {
	const uaData = navigator.userAgentData;
	if (uaData != null && uaData.platform) return uaData.platform;
	return navigator.platform;
}
function getUserAgent() {
	const uaData = navigator.userAgentData;
	if (uaData && Array.isArray(uaData.brands)) return uaData.brands.map((_ref) => {
		let { brand, version } = _ref;
		return brand + "/" + version;
	}).join(" ");
	return navigator.userAgent;
}
function isSafari() {
	return /apple/i.test(navigator.vendor);
}
function isAndroid() {
	const re = /android/i;
	return re.test(getPlatform()) || re.test(getUserAgent());
}
function isMac() {
	return getPlatform().toLowerCase().startsWith("mac") && !navigator.maxTouchPoints;
}
function isJSDOM() {
	return getUserAgent().includes("jsdom/");
}
var FOCUSABLE_ATTRIBUTE$1 = "data-floating-ui-focusable";
var TYPEABLE_SELECTOR = "input:not([type='hidden']):not([disabled]),[contenteditable]:not([contenteditable='false']),textarea:not([disabled])";
var ARROW_LEFT$1 = "ArrowLeft";
var ARROW_RIGHT$1 = "ArrowRight";
var ARROW_UP$1 = "ArrowUp";
var ARROW_DOWN$1 = "ArrowDown";
function activeElement(doc) {
	let activeElement = doc.activeElement;
	while (((_activeElement = activeElement) == null || (_activeElement = _activeElement.shadowRoot) == null ? void 0 : _activeElement.activeElement) != null) {
		var _activeElement;
		activeElement = activeElement.shadowRoot.activeElement;
	}
	return activeElement;
}
function contains$1(parent, child) {
	if (!parent || !child) return false;
	const rootNode = child.getRootNode == null ? void 0 : child.getRootNode();
	if (parent.contains(child)) return true;
	if (rootNode && isShadowRoot(rootNode)) {
		let next = child;
		while (next) {
			if (parent === next) return true;
			next = next.parentNode || next.host;
		}
	}
	return false;
}
function getTarget$2(event) {
	if ("composedPath" in event) return event.composedPath()[0];
	return event.target;
}
function isEventTargetWithin(event, node) {
	if (node == null) return false;
	if ("composedPath" in event) return event.composedPath().includes(node);
	const e = event;
	return e.target != null && node.contains(e.target);
}
function isRootElement(element) {
	return element.matches("html,body");
}
function getDocument$1(node) {
	return (node == null ? void 0 : node.ownerDocument) || document;
}
function isTypeableElement(element) {
	return isHTMLElement(element) && element.matches(TYPEABLE_SELECTOR);
}
function isTypeableCombobox(element) {
	if (!element) return false;
	return element.getAttribute("role") === "combobox" && isTypeableElement(element);
}
function matchesFocusVisible(element) {
	if (!element || isJSDOM()) return true;
	try {
		return element.matches(":focus-visible");
	} catch (_e) {
		return true;
	}
}
function getFloatingFocusElement(floatingElement) {
	if (!floatingElement) return null;
	return floatingElement.hasAttribute(FOCUSABLE_ATTRIBUTE$1) ? floatingElement : floatingElement.querySelector("[data-floating-ui-focusable]") || floatingElement;
}
function getNodeChildren$1(nodes, id, onlyOpenChildren) {
	if (onlyOpenChildren === void 0) onlyOpenChildren = true;
	return nodes.filter((node) => {
		var _node$context;
		return node.parentId === id && (!onlyOpenChildren || ((_node$context = node.context) == null ? void 0 : _node$context.open));
	}).flatMap((child) => [child, ...getNodeChildren$1(nodes, child.id, onlyOpenChildren)]);
}
function getDeepestNode(nodes, id) {
	let deepestNodeId;
	let maxDepth = -1;
	function findDeepest(nodeId, depth) {
		if (depth > maxDepth) {
			deepestNodeId = nodeId;
			maxDepth = depth;
		}
		getNodeChildren$1(nodes, nodeId).forEach((child) => {
			findDeepest(child.id, depth + 1);
		});
	}
	findDeepest(id, 0);
	return nodes.find((node) => node.id === deepestNodeId);
}
function getNodeAncestors(nodes, id) {
	var _nodes$find;
	let allAncestors = [];
	let currentParentId = (_nodes$find = nodes.find((node) => node.id === id)) == null ? void 0 : _nodes$find.parentId;
	while (currentParentId) {
		const currentNode = nodes.find((node) => node.id === currentParentId);
		currentParentId = currentNode == null ? void 0 : currentNode.parentId;
		if (currentNode) allAncestors = allAncestors.concat(currentNode);
	}
	return allAncestors;
}
function stopEvent(event) {
	event.preventDefault();
	event.stopPropagation();
}
function isReactEvent(event) {
	return "nativeEvent" in event;
}
function isVirtualClick(event) {
	if (event.mozInputSource === 0 && event.isTrusted) return true;
	if (isAndroid() && event.pointerType) return event.type === "click" && event.buttons === 1;
	return event.detail === 0 && !event.pointerType;
}
function isVirtualPointerEvent(event) {
	if (isJSDOM()) return false;
	return !isAndroid() && event.width === 0 && event.height === 0 || isAndroid() && event.width === 1 && event.height === 1 && event.pressure === 0 && event.detail === 0 && event.pointerType === "mouse" || event.width < 1 && event.height < 1 && event.pressure === 0 && event.detail === 0 && event.pointerType === "touch";
}
function isMouseLikePointerType(pointerType, strict) {
	const values = ["mouse", "pen"];
	if (!strict) values.push("", void 0);
	return values.includes(pointerType);
}
var index$1 = typeof document !== "undefined" ? import_react.useLayoutEffect : function noop() {};
var SafeReact$1 = { ...import_react };
function useLatestRef$1(value) {
	const ref = import_react.useRef(value);
	index$1(() => {
		ref.current = value;
	});
	return ref;
}
var useSafeInsertionEffect = SafeReact$1.useInsertionEffect || ((fn) => fn());
function useEffectEvent(callback) {
	const ref = import_react.useRef(() => {
		throw new Error("Cannot call an event handler while rendering.");
	});
	useSafeInsertionEffect(() => {
		ref.current = callback;
	});
	return import_react.useCallback(function() {
		for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
		return ref.current == null ? void 0 : ref.current(...args);
	}, []);
}
function isDifferentGridRow(index, cols, prevRow) {
	return Math.floor(index / cols) !== prevRow;
}
function isIndexOutOfListBounds(listRef, index) {
	return index < 0 || index >= listRef.current.length;
}
function getMinListIndex(listRef, disabledIndices) {
	return findNonDisabledListIndex(listRef, { disabledIndices });
}
function getMaxListIndex(listRef, disabledIndices) {
	return findNonDisabledListIndex(listRef, {
		decrement: true,
		startingIndex: listRef.current.length,
		disabledIndices
	});
}
function findNonDisabledListIndex(listRef, _temp) {
	let { startingIndex = -1, decrement = false, disabledIndices, amount = 1 } = _temp === void 0 ? {} : _temp;
	let index = startingIndex;
	do
		index += decrement ? -amount : amount;
	while (index >= 0 && index <= listRef.current.length - 1 && isListIndexDisabled(listRef, index, disabledIndices));
	return index;
}
function getGridNavigatedIndex(listRef, _ref) {
	let { event, orientation, loop, rtl, cols, disabledIndices, minIndex, maxIndex, prevIndex, stopEvent: stop = false } = _ref;
	let nextIndex = prevIndex;
	if (event.key === ARROW_UP$1) {
		stop && stopEvent(event);
		if (prevIndex === -1) nextIndex = maxIndex;
		else {
			nextIndex = findNonDisabledListIndex(listRef, {
				startingIndex: nextIndex,
				amount: cols,
				decrement: true,
				disabledIndices
			});
			if (loop && (prevIndex - cols < minIndex || nextIndex < 0)) {
				const col = prevIndex % cols;
				const maxCol = maxIndex % cols;
				const offset = maxIndex - (maxCol - col);
				if (maxCol === col) nextIndex = maxIndex;
				else nextIndex = maxCol > col ? offset : offset - cols;
			}
		}
		if (isIndexOutOfListBounds(listRef, nextIndex)) nextIndex = prevIndex;
	}
	if (event.key === ARROW_DOWN$1) {
		stop && stopEvent(event);
		if (prevIndex === -1) nextIndex = minIndex;
		else {
			nextIndex = findNonDisabledListIndex(listRef, {
				startingIndex: prevIndex,
				amount: cols,
				disabledIndices
			});
			if (loop && prevIndex + cols > maxIndex) nextIndex = findNonDisabledListIndex(listRef, {
				startingIndex: prevIndex % cols - cols,
				amount: cols,
				disabledIndices
			});
		}
		if (isIndexOutOfListBounds(listRef, nextIndex)) nextIndex = prevIndex;
	}
	if (orientation === "both") {
		const prevRow = floor(prevIndex / cols);
		if (event.key === (rtl ? ARROW_LEFT$1 : ARROW_RIGHT$1)) {
			stop && stopEvent(event);
			if (prevIndex % cols !== cols - 1) {
				nextIndex = findNonDisabledListIndex(listRef, {
					startingIndex: prevIndex,
					disabledIndices
				});
				if (loop && isDifferentGridRow(nextIndex, cols, prevRow)) nextIndex = findNonDisabledListIndex(listRef, {
					startingIndex: prevIndex - prevIndex % cols - 1,
					disabledIndices
				});
			} else if (loop) nextIndex = findNonDisabledListIndex(listRef, {
				startingIndex: prevIndex - prevIndex % cols - 1,
				disabledIndices
			});
			if (isDifferentGridRow(nextIndex, cols, prevRow)) nextIndex = prevIndex;
		}
		if (event.key === (rtl ? ARROW_RIGHT$1 : ARROW_LEFT$1)) {
			stop && stopEvent(event);
			if (prevIndex % cols !== 0) {
				nextIndex = findNonDisabledListIndex(listRef, {
					startingIndex: prevIndex,
					decrement: true,
					disabledIndices
				});
				if (loop && isDifferentGridRow(nextIndex, cols, prevRow)) nextIndex = findNonDisabledListIndex(listRef, {
					startingIndex: prevIndex + (cols - prevIndex % cols),
					decrement: true,
					disabledIndices
				});
			} else if (loop) nextIndex = findNonDisabledListIndex(listRef, {
				startingIndex: prevIndex + (cols - prevIndex % cols),
				decrement: true,
				disabledIndices
			});
			if (isDifferentGridRow(nextIndex, cols, prevRow)) nextIndex = prevIndex;
		}
		const lastRow = floor(maxIndex / cols) === prevRow;
		if (isIndexOutOfListBounds(listRef, nextIndex)) if (loop && lastRow) nextIndex = event.key === (rtl ? ARROW_RIGHT$1 : ARROW_LEFT$1) ? maxIndex : findNonDisabledListIndex(listRef, {
			startingIndex: prevIndex - prevIndex % cols - 1,
			disabledIndices
		});
		else nextIndex = prevIndex;
	}
	return nextIndex;
}
/** For each cell index, gets the item index that occupies that cell */
function createGridCellMap(sizes, cols, dense) {
	const cellMap = [];
	let startIndex = 0;
	sizes.forEach((_ref2, index) => {
		let { width, height } = _ref2;
		if (width > cols) throw new Error("[Floating UI]: Invalid grid - item width at index " + index + " is greater than grid columns");
		let itemPlaced = false;
		if (dense) startIndex = 0;
		while (!itemPlaced) {
			const targetCells = [];
			for (let i = 0; i < width; i++) for (let j = 0; j < height; j++) targetCells.push(startIndex + i + j * cols);
			if (startIndex % cols + width <= cols && targetCells.every((cell) => cellMap[cell] == null)) {
				targetCells.forEach((cell) => {
					cellMap[cell] = index;
				});
				itemPlaced = true;
			} else startIndex++;
		}
	});
	return [...cellMap];
}
/** Gets cell index of an item's corner or -1 when index is -1. */
function getGridCellIndexOfCorner(index, sizes, cellMap, cols, corner) {
	if (index === -1) return -1;
	const firstCellIndex = cellMap.indexOf(index);
	const sizeItem = sizes[index];
	switch (corner) {
		case "tl": return firstCellIndex;
		case "tr":
			if (!sizeItem) return firstCellIndex;
			return firstCellIndex + sizeItem.width - 1;
		case "bl":
			if (!sizeItem) return firstCellIndex;
			return firstCellIndex + (sizeItem.height - 1) * cols;
		case "br": return cellMap.lastIndexOf(index);
	}
}
/** Gets all cell indices that correspond to the specified indices */
function getGridCellIndices(indices, cellMap) {
	return cellMap.flatMap((index, cellIndex) => indices.includes(index) ? [cellIndex] : []);
}
function isListIndexDisabled(listRef, index, disabledIndices) {
	if (typeof disabledIndices === "function") return disabledIndices(index);
	else if (disabledIndices) return disabledIndices.includes(index);
	const element = listRef.current[index];
	return element == null || element.hasAttribute("disabled") || element.getAttribute("aria-disabled") === "true";
}
var getTabbableOptions = () => ({
	getShadowRoot: true,
	displayCheck: typeof ResizeObserver === "function" && ResizeObserver.toString().includes("[native code]") ? "full" : "none"
});
function getTabbableIn(container, dir) {
	const list = tabbable(container, getTabbableOptions());
	const len = list.length;
	if (len === 0) return;
	const active = activeElement(getDocument$1(container));
	const index = list.indexOf(active);
	return list[index === -1 ? dir === 1 ? 0 : len - 1 : index + dir];
}
function getNextTabbable(referenceElement) {
	return getTabbableIn(getDocument$1(referenceElement).body, 1) || referenceElement;
}
function getPreviousTabbable(referenceElement) {
	return getTabbableIn(getDocument$1(referenceElement).body, -1) || referenceElement;
}
function isOutsideEvent(event, container) {
	const containerElement = container || event.currentTarget;
	const relatedTarget = event.relatedTarget;
	return !relatedTarget || !contains$1(containerElement, relatedTarget);
}
function disableFocusInside(container) {
	tabbable(container, getTabbableOptions()).forEach((element) => {
		element.dataset.tabindex = element.getAttribute("tabindex") || "";
		element.setAttribute("tabindex", "-1");
	});
}
function enableFocusInside(container) {
	container.querySelectorAll("[data-tabindex]").forEach((element) => {
		const tabindex = element.dataset.tabindex;
		delete element.dataset.tabindex;
		if (tabindex) element.setAttribute("tabindex", tabindex);
		else element.removeAttribute("tabindex");
	});
}
//#endregion
//#region node_modules/.pnpm/@floating-ui+core@1.7.5/node_modules/@floating-ui/core/dist/floating-ui.core.mjs
function computeCoordsFromPlacement(_ref, placement, rtl) {
	let { reference, floating } = _ref;
	const sideAxis = getSideAxis(placement);
	const alignmentAxis = getAlignmentAxis(placement);
	const alignLength = getAxisLength(alignmentAxis);
	const side = getSide(placement);
	const isVertical = sideAxis === "y";
	const commonX = reference.x + reference.width / 2 - floating.width / 2;
	const commonY = reference.y + reference.height / 2 - floating.height / 2;
	const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
	let coords;
	switch (side) {
		case "top":
			coords = {
				x: commonX,
				y: reference.y - floating.height
			};
			break;
		case "bottom":
			coords = {
				x: commonX,
				y: reference.y + reference.height
			};
			break;
		case "right":
			coords = {
				x: reference.x + reference.width,
				y: commonY
			};
			break;
		case "left":
			coords = {
				x: reference.x - floating.width,
				y: commonY
			};
			break;
		default: coords = {
			x: reference.x,
			y: reference.y
		};
	}
	switch (getAlignment(placement)) {
		case "start":
			coords[alignmentAxis] -= commonAlign * (rtl && isVertical ? -1 : 1);
			break;
		case "end":
			coords[alignmentAxis] += commonAlign * (rtl && isVertical ? -1 : 1);
			break;
	}
	return coords;
}
/**
* Resolves with an object of overflow side offsets that determine how much the
* element is overflowing a given clipping boundary on each side.
* - positive = overflowing the boundary by that number of pixels
* - negative = how many pixels left before it will overflow
* - 0 = lies flush with the boundary
* @see https://floating-ui.com/docs/detectOverflow
*/
async function detectOverflow(state, options) {
	var _await$platform$isEle;
	if (options === void 0) options = {};
	const { x, y, platform, rects, elements, strategy } = state;
	const { boundary = "clippingAncestors", rootBoundary = "viewport", elementContext = "floating", altBoundary = false, padding = 0 } = evaluate(options, state);
	const paddingObject = getPaddingObject(padding);
	const element = elements[altBoundary ? elementContext === "floating" ? "reference" : "floating" : elementContext];
	const clippingClientRect = rectToClientRect(await platform.getClippingRect({
		element: ((_await$platform$isEle = await (platform.isElement == null ? void 0 : platform.isElement(element))) != null ? _await$platform$isEle : true) ? element : element.contextElement || await (platform.getDocumentElement == null ? void 0 : platform.getDocumentElement(elements.floating)),
		boundary,
		rootBoundary,
		strategy
	}));
	const rect = elementContext === "floating" ? {
		x,
		y,
		width: rects.floating.width,
		height: rects.floating.height
	} : rects.reference;
	const offsetParent = await (platform.getOffsetParent == null ? void 0 : platform.getOffsetParent(elements.floating));
	const offsetScale = await (platform.isElement == null ? void 0 : platform.isElement(offsetParent)) ? await (platform.getScale == null ? void 0 : platform.getScale(offsetParent)) || {
		x: 1,
		y: 1
	} : {
		x: 1,
		y: 1
	};
	const elementClientRect = rectToClientRect(platform.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform.convertOffsetParentRelativeRectToViewportRelativeRect({
		elements,
		rect,
		offsetParent,
		strategy
	}) : rect);
	return {
		top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
		bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
		left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
		right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
	};
}
var MAX_RESET_COUNT = 50;
/**
* Computes the `x` and `y` coordinates that will place the floating element
* next to a given reference element.
*
* This export does not have any `platform` interface logic. You will need to
* write one for the platform you are using Floating UI with.
*/
var computePosition$1 = async (reference, floating, config) => {
	const { placement = "bottom", strategy = "absolute", middleware = [], platform } = config;
	const platformWithDetectOverflow = platform.detectOverflow ? platform : {
		...platform,
		detectOverflow
	};
	const rtl = await (platform.isRTL == null ? void 0 : platform.isRTL(floating));
	let rects = await platform.getElementRects({
		reference,
		floating,
		strategy
	});
	let { x, y } = computeCoordsFromPlacement(rects, placement, rtl);
	let statefulPlacement = placement;
	let resetCount = 0;
	const middlewareData = {};
	for (let i = 0; i < middleware.length; i++) {
		const currentMiddleware = middleware[i];
		if (!currentMiddleware) continue;
		const { name, fn } = currentMiddleware;
		const { x: nextX, y: nextY, data, reset } = await fn({
			x,
			y,
			initialPlacement: placement,
			placement: statefulPlacement,
			strategy,
			middlewareData,
			rects,
			platform: platformWithDetectOverflow,
			elements: {
				reference,
				floating
			}
		});
		x = nextX != null ? nextX : x;
		y = nextY != null ? nextY : y;
		middlewareData[name] = {
			...middlewareData[name],
			...data
		};
		if (reset && resetCount < MAX_RESET_COUNT) {
			resetCount++;
			if (typeof reset === "object") {
				if (reset.placement) statefulPlacement = reset.placement;
				if (reset.rects) rects = reset.rects === true ? await platform.getElementRects({
					reference,
					floating,
					strategy
				}) : reset.rects;
				({x, y} = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
			}
			i = -1;
		}
	}
	return {
		x,
		y,
		placement: statefulPlacement,
		strategy,
		middlewareData
	};
};
/**
* Provides data to position an inner element of the floating element so that it
* appears centered to the reference element.
* @see https://floating-ui.com/docs/arrow
*/
var arrow$3 = (options) => ({
	name: "arrow",
	options,
	async fn(state) {
		const { x, y, placement, rects, platform, elements, middlewareData } = state;
		const { element, padding = 0 } = evaluate(options, state) || {};
		if (element == null) return {};
		const paddingObject = getPaddingObject(padding);
		const coords = {
			x,
			y
		};
		const axis = getAlignmentAxis(placement);
		const length = getAxisLength(axis);
		const arrowDimensions = await platform.getDimensions(element);
		const isYAxis = axis === "y";
		const minProp = isYAxis ? "top" : "left";
		const maxProp = isYAxis ? "bottom" : "right";
		const clientProp = isYAxis ? "clientHeight" : "clientWidth";
		const endDiff = rects.reference[length] + rects.reference[axis] - coords[axis] - rects.floating[length];
		const startDiff = coords[axis] - rects.reference[axis];
		const arrowOffsetParent = await (platform.getOffsetParent == null ? void 0 : platform.getOffsetParent(element));
		let clientSize = arrowOffsetParent ? arrowOffsetParent[clientProp] : 0;
		if (!clientSize || !await (platform.isElement == null ? void 0 : platform.isElement(arrowOffsetParent))) clientSize = elements.floating[clientProp] || rects.floating[length];
		const centerToReference = endDiff / 2 - startDiff / 2;
		const largestPossiblePadding = clientSize / 2 - arrowDimensions[length] / 2 - 1;
		const minPadding = min(paddingObject[minProp], largestPossiblePadding);
		const maxPadding = min(paddingObject[maxProp], largestPossiblePadding);
		const min$1 = minPadding;
		const max = clientSize - arrowDimensions[length] - maxPadding;
		const center = clientSize / 2 - arrowDimensions[length] / 2 + centerToReference;
		const offset = clamp(min$1, center, max);
		const shouldAddOffset = !middlewareData.arrow && getAlignment(placement) != null && center !== offset && rects.reference[length] / 2 - (center < min$1 ? minPadding : maxPadding) - arrowDimensions[length] / 2 < 0;
		const alignmentOffset = shouldAddOffset ? center < min$1 ? center - min$1 : center - max : 0;
		return {
			[axis]: coords[axis] + alignmentOffset,
			data: {
				[axis]: offset,
				centerOffset: center - offset - alignmentOffset,
				...shouldAddOffset && { alignmentOffset }
			},
			reset: shouldAddOffset
		};
	}
});
function getPlacementList(alignment, autoAlignment, allowedPlacements) {
	return (alignment ? [...allowedPlacements.filter((placement) => getAlignment(placement) === alignment), ...allowedPlacements.filter((placement) => getAlignment(placement) !== alignment)] : allowedPlacements.filter((placement) => getSide(placement) === placement)).filter((placement) => {
		if (alignment) return getAlignment(placement) === alignment || (autoAlignment ? getOppositeAlignmentPlacement(placement) !== placement : false);
		return true;
	});
}
/**
* Optimizes the visibility of the floating element by choosing the placement
* that has the most space available automatically, without needing to specify a
* preferred placement. Alternative to `flip`.
* @see https://floating-ui.com/docs/autoPlacement
*/
var autoPlacement$2 = function(options) {
	if (options === void 0) options = {};
	return {
		name: "autoPlacement",
		options,
		async fn(state) {
			var _middlewareData$autoP, _middlewareData$autoP2, _placementsThatFitOnE;
			const { rects, middlewareData, placement, platform, elements } = state;
			const { crossAxis = false, alignment, allowedPlacements = placements, autoAlignment = true, ...detectOverflowOptions } = evaluate(options, state);
			const placements$1 = alignment !== void 0 || allowedPlacements === placements ? getPlacementList(alignment || null, autoAlignment, allowedPlacements) : allowedPlacements;
			const overflow = await platform.detectOverflow(state, detectOverflowOptions);
			const currentIndex = ((_middlewareData$autoP = middlewareData.autoPlacement) == null ? void 0 : _middlewareData$autoP.index) || 0;
			const currentPlacement = placements$1[currentIndex];
			if (currentPlacement == null) return {};
			const alignmentSides = getAlignmentSides(currentPlacement, rects, await (platform.isRTL == null ? void 0 : platform.isRTL(elements.floating)));
			if (placement !== currentPlacement) return { reset: { placement: placements$1[0] } };
			const currentOverflows = [
				overflow[getSide(currentPlacement)],
				overflow[alignmentSides[0]],
				overflow[alignmentSides[1]]
			];
			const allOverflows = [...((_middlewareData$autoP2 = middlewareData.autoPlacement) == null ? void 0 : _middlewareData$autoP2.overflows) || [], {
				placement: currentPlacement,
				overflows: currentOverflows
			}];
			const nextPlacement = placements$1[currentIndex + 1];
			if (nextPlacement) return {
				data: {
					index: currentIndex + 1,
					overflows: allOverflows
				},
				reset: { placement: nextPlacement }
			};
			const placementsSortedByMostSpace = allOverflows.map((d) => {
				const alignment = getAlignment(d.placement);
				return [
					d.placement,
					alignment && crossAxis ? d.overflows.slice(0, 2).reduce((acc, v) => acc + v, 0) : d.overflows[0],
					d.overflows
				];
			}).sort((a, b) => a[1] - b[1]);
			const resetPlacement = ((_placementsThatFitOnE = placementsSortedByMostSpace.filter((d) => d[2].slice(0, getAlignment(d[0]) ? 2 : 3).every((v) => v <= 0))[0]) == null ? void 0 : _placementsThatFitOnE[0]) || placementsSortedByMostSpace[0][0];
			if (resetPlacement !== placement) return {
				data: {
					index: currentIndex + 1,
					overflows: allOverflows
				},
				reset: { placement: resetPlacement }
			};
			return {};
		}
	};
};
/**
* Optimizes the visibility of the floating element by flipping the `placement`
* in order to keep it in view when the preferred placement(s) will overflow the
* clipping boundary. Alternative to `autoPlacement`.
* @see https://floating-ui.com/docs/flip
*/
var flip$2 = function(options) {
	if (options === void 0) options = {};
	return {
		name: "flip",
		options,
		async fn(state) {
			var _middlewareData$arrow, _middlewareData$flip;
			const { placement, middlewareData, rects, initialPlacement, platform, elements } = state;
			const { mainAxis: checkMainAxis = true, crossAxis: checkCrossAxis = true, fallbackPlacements: specifiedFallbackPlacements, fallbackStrategy = "bestFit", fallbackAxisSideDirection = "none", flipAlignment = true, ...detectOverflowOptions } = evaluate(options, state);
			if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) return {};
			const side = getSide(placement);
			const initialSideAxis = getSideAxis(initialPlacement);
			const isBasePlacement = getSide(initialPlacement) === initialPlacement;
			const rtl = await (platform.isRTL == null ? void 0 : platform.isRTL(elements.floating));
			const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
			const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
			if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
			const placements = [initialPlacement, ...fallbackPlacements];
			const overflow = await platform.detectOverflow(state, detectOverflowOptions);
			const overflows = [];
			let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
			if (checkMainAxis) overflows.push(overflow[side]);
			if (checkCrossAxis) {
				const sides = getAlignmentSides(placement, rects, rtl);
				overflows.push(overflow[sides[0]], overflow[sides[1]]);
			}
			overflowsData = [...overflowsData, {
				placement,
				overflows
			}];
			if (!overflows.every((side) => side <= 0)) {
				var _middlewareData$flip2, _overflowsData$filter;
				const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
				const nextPlacement = placements[nextIndex];
				if (nextPlacement) {
					if (!(checkCrossAxis === "alignment" ? initialSideAxis !== getSideAxis(nextPlacement) : false) || overflowsData.every((d) => getSideAxis(d.placement) === initialSideAxis ? d.overflows[0] > 0 : true)) return {
						data: {
							index: nextIndex,
							overflows: overflowsData
						},
						reset: { placement: nextPlacement }
					};
				}
				let resetPlacement = (_overflowsData$filter = overflowsData.filter((d) => d.overflows[0] <= 0).sort((a, b) => a.overflows[1] - b.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
				if (!resetPlacement) switch (fallbackStrategy) {
					case "bestFit": {
						var _overflowsData$filter2;
						const placement = (_overflowsData$filter2 = overflowsData.filter((d) => {
							if (hasFallbackAxisSideDirection) {
								const currentSideAxis = getSideAxis(d.placement);
								return currentSideAxis === initialSideAxis || currentSideAxis === "y";
							}
							return true;
						}).map((d) => [d.placement, d.overflows.filter((overflow) => overflow > 0).reduce((acc, overflow) => acc + overflow, 0)]).sort((a, b) => a[1] - b[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
						if (placement) resetPlacement = placement;
						break;
					}
					case "initialPlacement":
						resetPlacement = initialPlacement;
						break;
				}
				if (placement !== resetPlacement) return { reset: { placement: resetPlacement } };
			}
			return {};
		}
	};
};
var originSides = /*#__PURE__*/ new Set(["left", "top"]);
async function convertValueToCoords(state, options) {
	const { placement, platform, elements } = state;
	const rtl = await (platform.isRTL == null ? void 0 : platform.isRTL(elements.floating));
	const side = getSide(placement);
	const alignment = getAlignment(placement);
	const isVertical = getSideAxis(placement) === "y";
	const mainAxisMulti = originSides.has(side) ? -1 : 1;
	const crossAxisMulti = rtl && isVertical ? -1 : 1;
	const rawValue = evaluate(options, state);
	let { mainAxis, crossAxis, alignmentAxis } = typeof rawValue === "number" ? {
		mainAxis: rawValue,
		crossAxis: 0,
		alignmentAxis: null
	} : {
		mainAxis: rawValue.mainAxis || 0,
		crossAxis: rawValue.crossAxis || 0,
		alignmentAxis: rawValue.alignmentAxis
	};
	if (alignment && typeof alignmentAxis === "number") crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
	return isVertical ? {
		x: crossAxis * crossAxisMulti,
		y: mainAxis * mainAxisMulti
	} : {
		x: mainAxis * mainAxisMulti,
		y: crossAxis * crossAxisMulti
	};
}
/**
* Modifies the placement by translating the floating element along the
* specified axes.
* A number (shorthand for `mainAxis` or distance), or an axes configuration
* object may be passed.
* @see https://floating-ui.com/docs/offset
*/
var offset$3 = function(options) {
	if (options === void 0) options = 0;
	return {
		name: "offset",
		options,
		async fn(state) {
			var _middlewareData$offse, _middlewareData$arrow;
			const { x, y, placement, middlewareData } = state;
			const diffCoords = await convertValueToCoords(state, options);
			if (placement === ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse.placement) && (_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) return {};
			return {
				x: x + diffCoords.x,
				y: y + diffCoords.y,
				data: {
					...diffCoords,
					placement
				}
			};
		}
	};
};
/**
* Optimizes the visibility of the floating element by shifting it in order to
* keep it in view when it will overflow the clipping boundary.
* @see https://floating-ui.com/docs/shift
*/
var shift$3 = function(options) {
	if (options === void 0) options = {};
	return {
		name: "shift",
		options,
		async fn(state) {
			const { x, y, placement, platform } = state;
			const { mainAxis: checkMainAxis = true, crossAxis: checkCrossAxis = false, limiter = { fn: (_ref) => {
				let { x, y } = _ref;
				return {
					x,
					y
				};
			} }, ...detectOverflowOptions } = evaluate(options, state);
			const coords = {
				x,
				y
			};
			const overflow = await platform.detectOverflow(state, detectOverflowOptions);
			const crossAxis = getSideAxis(getSide(placement));
			const mainAxis = getOppositeAxis(crossAxis);
			let mainAxisCoord = coords[mainAxis];
			let crossAxisCoord = coords[crossAxis];
			if (checkMainAxis) {
				const minSide = mainAxis === "y" ? "top" : "left";
				const maxSide = mainAxis === "y" ? "bottom" : "right";
				const min = mainAxisCoord + overflow[minSide];
				const max = mainAxisCoord - overflow[maxSide];
				mainAxisCoord = clamp(min, mainAxisCoord, max);
			}
			if (checkCrossAxis) {
				const minSide = crossAxis === "y" ? "top" : "left";
				const maxSide = crossAxis === "y" ? "bottom" : "right";
				const min = crossAxisCoord + overflow[minSide];
				const max = crossAxisCoord - overflow[maxSide];
				crossAxisCoord = clamp(min, crossAxisCoord, max);
			}
			const limitedCoords = limiter.fn({
				...state,
				[mainAxis]: mainAxisCoord,
				[crossAxis]: crossAxisCoord
			});
			return {
				...limitedCoords,
				data: {
					x: limitedCoords.x - x,
					y: limitedCoords.y - y,
					enabled: {
						[mainAxis]: checkMainAxis,
						[crossAxis]: checkCrossAxis
					}
				}
			};
		}
	};
};
/**
* Built-in `limiter` that will stop `shift()` at a certain point.
*/
var limitShift$2 = function(options) {
	if (options === void 0) options = {};
	return {
		options,
		fn(state) {
			const { x, y, placement, rects, middlewareData } = state;
			const { offset = 0, mainAxis: checkMainAxis = true, crossAxis: checkCrossAxis = true } = evaluate(options, state);
			const coords = {
				x,
				y
			};
			const crossAxis = getSideAxis(placement);
			const mainAxis = getOppositeAxis(crossAxis);
			let mainAxisCoord = coords[mainAxis];
			let crossAxisCoord = coords[crossAxis];
			const rawOffset = evaluate(offset, state);
			const computedOffset = typeof rawOffset === "number" ? {
				mainAxis: rawOffset,
				crossAxis: 0
			} : {
				mainAxis: 0,
				crossAxis: 0,
				...rawOffset
			};
			if (checkMainAxis) {
				const len = mainAxis === "y" ? "height" : "width";
				const limitMin = rects.reference[mainAxis] - rects.floating[len] + computedOffset.mainAxis;
				const limitMax = rects.reference[mainAxis] + rects.reference[len] - computedOffset.mainAxis;
				if (mainAxisCoord < limitMin) mainAxisCoord = limitMin;
				else if (mainAxisCoord > limitMax) mainAxisCoord = limitMax;
			}
			if (checkCrossAxis) {
				var _middlewareData$offse, _middlewareData$offse2;
				const len = mainAxis === "y" ? "width" : "height";
				const isOriginSide = originSides.has(getSide(placement));
				const limitMin = rects.reference[crossAxis] - rects.floating[len] + (isOriginSide ? ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse[crossAxis]) || 0 : 0) + (isOriginSide ? 0 : computedOffset.crossAxis);
				const limitMax = rects.reference[crossAxis] + rects.reference[len] + (isOriginSide ? 0 : ((_middlewareData$offse2 = middlewareData.offset) == null ? void 0 : _middlewareData$offse2[crossAxis]) || 0) - (isOriginSide ? computedOffset.crossAxis : 0);
				if (crossAxisCoord < limitMin) crossAxisCoord = limitMin;
				else if (crossAxisCoord > limitMax) crossAxisCoord = limitMax;
			}
			return {
				[mainAxis]: mainAxisCoord,
				[crossAxis]: crossAxisCoord
			};
		}
	};
};
/**
* Provides data that allows you to change the size of the floating element —
* for instance, prevent it from overflowing the clipping boundary or match the
* width of the reference element.
* @see https://floating-ui.com/docs/size
*/
var size$3 = function(options) {
	if (options === void 0) options = {};
	return {
		name: "size",
		options,
		async fn(state) {
			var _state$middlewareData, _state$middlewareData2;
			const { placement, rects, platform, elements } = state;
			const { apply = () => {}, ...detectOverflowOptions } = evaluate(options, state);
			const overflow = await platform.detectOverflow(state, detectOverflowOptions);
			const side = getSide(placement);
			const alignment = getAlignment(placement);
			const isYAxis = getSideAxis(placement) === "y";
			const { width, height } = rects.floating;
			let heightSide;
			let widthSide;
			if (side === "top" || side === "bottom") {
				heightSide = side;
				widthSide = alignment === (await (platform.isRTL == null ? void 0 : platform.isRTL(elements.floating)) ? "start" : "end") ? "left" : "right";
			} else {
				widthSide = side;
				heightSide = alignment === "end" ? "top" : "bottom";
			}
			const maximumClippingHeight = height - overflow.top - overflow.bottom;
			const maximumClippingWidth = width - overflow.left - overflow.right;
			const overflowAvailableHeight = min(height - overflow[heightSide], maximumClippingHeight);
			const overflowAvailableWidth = min(width - overflow[widthSide], maximumClippingWidth);
			const noShift = !state.middlewareData.shift;
			let availableHeight = overflowAvailableHeight;
			let availableWidth = overflowAvailableWidth;
			if ((_state$middlewareData = state.middlewareData.shift) != null && _state$middlewareData.enabled.x) availableWidth = maximumClippingWidth;
			if ((_state$middlewareData2 = state.middlewareData.shift) != null && _state$middlewareData2.enabled.y) availableHeight = maximumClippingHeight;
			if (noShift && !alignment) {
				const xMin = max(overflow.left, 0);
				const xMax = max(overflow.right, 0);
				const yMin = max(overflow.top, 0);
				const yMax = max(overflow.bottom, 0);
				if (isYAxis) availableWidth = width - 2 * (xMin !== 0 || xMax !== 0 ? xMin + xMax : max(overflow.left, overflow.right));
				else availableHeight = height - 2 * (yMin !== 0 || yMax !== 0 ? yMin + yMax : max(overflow.top, overflow.bottom));
			}
			await apply({
				...state,
				availableWidth,
				availableHeight
			});
			const nextDimensions = await platform.getDimensions(elements.floating);
			if (width !== nextDimensions.width || height !== nextDimensions.height) return { reset: { rects: true } };
			return {};
		}
	};
};
//#endregion
//#region node_modules/.pnpm/@floating-ui+dom@1.7.6/node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
function getCssDimensions(element) {
	const css = getComputedStyle$1(element);
	let width = parseFloat(css.width) || 0;
	let height = parseFloat(css.height) || 0;
	const hasOffset = isHTMLElement(element);
	const offsetWidth = hasOffset ? element.offsetWidth : width;
	const offsetHeight = hasOffset ? element.offsetHeight : height;
	const shouldFallback = round$1(width) !== offsetWidth || round$1(height) !== offsetHeight;
	if (shouldFallback) {
		width = offsetWidth;
		height = offsetHeight;
	}
	return {
		width,
		height,
		$: shouldFallback
	};
}
function unwrapElement(element) {
	return !isElement$2(element) ? element.contextElement : element;
}
function getScale(element) {
	const domElement = unwrapElement(element);
	if (!isHTMLElement(domElement)) return createCoords(1);
	const rect = domElement.getBoundingClientRect();
	const { width, height, $ } = getCssDimensions(domElement);
	let x = ($ ? round$1(rect.width) : rect.width) / width;
	let y = ($ ? round$1(rect.height) : rect.height) / height;
	if (!x || !Number.isFinite(x)) x = 1;
	if (!y || !Number.isFinite(y)) y = 1;
	return {
		x,
		y
	};
}
var noOffsets = /*#__PURE__*/ createCoords(0);
function getVisualOffsets(element) {
	const win = getWindow(element);
	if (!isWebKit() || !win.visualViewport) return noOffsets;
	return {
		x: win.visualViewport.offsetLeft,
		y: win.visualViewport.offsetTop
	};
}
function shouldAddVisualOffsets(element, isFixed, floatingOffsetParent) {
	if (isFixed === void 0) isFixed = false;
	if (!floatingOffsetParent || isFixed && floatingOffsetParent !== getWindow(element)) return false;
	return isFixed;
}
function getBoundingClientRect(element, includeScale, isFixedStrategy, offsetParent) {
	if (includeScale === void 0) includeScale = false;
	if (isFixedStrategy === void 0) isFixedStrategy = false;
	const clientRect = element.getBoundingClientRect();
	const domElement = unwrapElement(element);
	let scale = createCoords(1);
	if (includeScale) if (offsetParent) {
		if (isElement$2(offsetParent)) scale = getScale(offsetParent);
	} else scale = getScale(element);
	const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
	let x = (clientRect.left + visualOffsets.x) / scale.x;
	let y = (clientRect.top + visualOffsets.y) / scale.y;
	let width = clientRect.width / scale.x;
	let height = clientRect.height / scale.y;
	if (domElement) {
		const win = getWindow(domElement);
		const offsetWin = offsetParent && isElement$2(offsetParent) ? getWindow(offsetParent) : offsetParent;
		let currentWin = win;
		let currentIFrame = getFrameElement(currentWin);
		while (currentIFrame && offsetParent && offsetWin !== currentWin) {
			const iframeScale = getScale(currentIFrame);
			const iframeRect = currentIFrame.getBoundingClientRect();
			const css = getComputedStyle$1(currentIFrame);
			const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css.paddingLeft)) * iframeScale.x;
			const top = iframeRect.top + (currentIFrame.clientTop + parseFloat(css.paddingTop)) * iframeScale.y;
			x *= iframeScale.x;
			y *= iframeScale.y;
			width *= iframeScale.x;
			height *= iframeScale.y;
			x += left;
			y += top;
			currentWin = getWindow(currentIFrame);
			currentIFrame = getFrameElement(currentWin);
		}
	}
	return rectToClientRect({
		width,
		height,
		x,
		y
	});
}
function getWindowScrollBarX(element, rect) {
	const leftScroll = getNodeScroll(element).scrollLeft;
	if (!rect) return getBoundingClientRect(getDocumentElement$1(element)).left + leftScroll;
	return rect.left + leftScroll;
}
function getHTMLOffset(documentElement, scroll) {
	const htmlRect = documentElement.getBoundingClientRect();
	return {
		x: htmlRect.left + scroll.scrollLeft - getWindowScrollBarX(documentElement, htmlRect),
		y: htmlRect.top + scroll.scrollTop
	};
}
function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
	let { elements, rect, offsetParent, strategy } = _ref;
	const isFixed = strategy === "fixed";
	const documentElement = getDocumentElement$1(offsetParent);
	const topLayer = elements ? isTopLayer(elements.floating) : false;
	if (offsetParent === documentElement || topLayer && isFixed) return rect;
	let scroll = {
		scrollLeft: 0,
		scrollTop: 0
	};
	let scale = createCoords(1);
	const offsets = createCoords(0);
	const isOffsetParentAnElement = isHTMLElement(offsetParent);
	if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
		if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) scroll = getNodeScroll(offsetParent);
		if (isOffsetParentAnElement) {
			const offsetRect = getBoundingClientRect(offsetParent);
			scale = getScale(offsetParent);
			offsets.x = offsetRect.x + offsetParent.clientLeft;
			offsets.y = offsetRect.y + offsetParent.clientTop;
		}
	}
	const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
	return {
		width: rect.width * scale.x,
		height: rect.height * scale.y,
		x: rect.x * scale.x - scroll.scrollLeft * scale.x + offsets.x + htmlOffset.x,
		y: rect.y * scale.y - scroll.scrollTop * scale.y + offsets.y + htmlOffset.y
	};
}
function getClientRects(element) {
	return Array.from(element.getClientRects());
}
function getDocumentRect(element) {
	const html = getDocumentElement$1(element);
	const scroll = getNodeScroll(element);
	const body = element.ownerDocument.body;
	const width = max(html.scrollWidth, html.clientWidth, body.scrollWidth, body.clientWidth);
	const height = max(html.scrollHeight, html.clientHeight, body.scrollHeight, body.clientHeight);
	let x = -scroll.scrollLeft + getWindowScrollBarX(element);
	const y = -scroll.scrollTop;
	if (getComputedStyle$1(body).direction === "rtl") x += max(html.clientWidth, body.clientWidth) - width;
	return {
		width,
		height,
		x,
		y
	};
}
var SCROLLBAR_MAX = 25;
function getViewportRect(element, strategy) {
	const win = getWindow(element);
	const html = getDocumentElement$1(element);
	const visualViewport = win.visualViewport;
	let width = html.clientWidth;
	let height = html.clientHeight;
	let x = 0;
	let y = 0;
	if (visualViewport) {
		width = visualViewport.width;
		height = visualViewport.height;
		const visualViewportBased = isWebKit();
		if (!visualViewportBased || visualViewportBased && strategy === "fixed") {
			x = visualViewport.offsetLeft;
			y = visualViewport.offsetTop;
		}
	}
	const windowScrollbarX = getWindowScrollBarX(html);
	if (windowScrollbarX <= 0) {
		const doc = html.ownerDocument;
		const body = doc.body;
		const bodyStyles = getComputedStyle(body);
		const bodyMarginInline = doc.compatMode === "CSS1Compat" ? parseFloat(bodyStyles.marginLeft) + parseFloat(bodyStyles.marginRight) || 0 : 0;
		const clippingStableScrollbarWidth = Math.abs(html.clientWidth - body.clientWidth - bodyMarginInline);
		if (clippingStableScrollbarWidth <= SCROLLBAR_MAX) width -= clippingStableScrollbarWidth;
	} else if (windowScrollbarX <= SCROLLBAR_MAX) width += windowScrollbarX;
	return {
		width,
		height,
		x,
		y
	};
}
function getInnerBoundingClientRect(element, strategy) {
	const clientRect = getBoundingClientRect(element, true, strategy === "fixed");
	const top = clientRect.top + element.clientTop;
	const left = clientRect.left + element.clientLeft;
	const scale = isHTMLElement(element) ? getScale(element) : createCoords(1);
	return {
		width: element.clientWidth * scale.x,
		height: element.clientHeight * scale.y,
		x: left * scale.x,
		y: top * scale.y
	};
}
function getClientRectFromClippingAncestor(element, clippingAncestor, strategy) {
	let rect;
	if (clippingAncestor === "viewport") rect = getViewportRect(element, strategy);
	else if (clippingAncestor === "document") rect = getDocumentRect(getDocumentElement$1(element));
	else if (isElement$2(clippingAncestor)) rect = getInnerBoundingClientRect(clippingAncestor, strategy);
	else {
		const visualOffsets = getVisualOffsets(element);
		rect = {
			x: clippingAncestor.x - visualOffsets.x,
			y: clippingAncestor.y - visualOffsets.y,
			width: clippingAncestor.width,
			height: clippingAncestor.height
		};
	}
	return rectToClientRect(rect);
}
function hasFixedPositionAncestor(element, stopNode) {
	const parentNode = getParentNode(element);
	if (parentNode === stopNode || !isElement$2(parentNode) || isLastTraversableNode(parentNode)) return false;
	return getComputedStyle$1(parentNode).position === "fixed" || hasFixedPositionAncestor(parentNode, stopNode);
}
function getClippingElementAncestors(element, cache) {
	const cachedResult = cache.get(element);
	if (cachedResult) return cachedResult;
	let result = getOverflowAncestors(element, [], false).filter((el) => isElement$2(el) && getNodeName(el) !== "body");
	let currentContainingBlockComputedStyle = null;
	const elementIsFixed = getComputedStyle$1(element).position === "fixed";
	let currentNode = elementIsFixed ? getParentNode(element) : element;
	while (isElement$2(currentNode) && !isLastTraversableNode(currentNode)) {
		const computedStyle = getComputedStyle$1(currentNode);
		const currentNodeIsContaining = isContainingBlock(currentNode);
		if (!currentNodeIsContaining && computedStyle.position === "fixed") currentContainingBlockComputedStyle = null;
		if (elementIsFixed ? !currentNodeIsContaining && !currentContainingBlockComputedStyle : !currentNodeIsContaining && computedStyle.position === "static" && !!currentContainingBlockComputedStyle && (currentContainingBlockComputedStyle.position === "absolute" || currentContainingBlockComputedStyle.position === "fixed") || isOverflowElement(currentNode) && !currentNodeIsContaining && hasFixedPositionAncestor(element, currentNode)) result = result.filter((ancestor) => ancestor !== currentNode);
		else currentContainingBlockComputedStyle = computedStyle;
		currentNode = getParentNode(currentNode);
	}
	cache.set(element, result);
	return result;
}
function getClippingRect(_ref) {
	let { element, boundary, rootBoundary, strategy } = _ref;
	const clippingAncestors = [...boundary === "clippingAncestors" ? isTopLayer(element) ? [] : getClippingElementAncestors(element, this._c) : [].concat(boundary), rootBoundary];
	const firstRect = getClientRectFromClippingAncestor(element, clippingAncestors[0], strategy);
	let top = firstRect.top;
	let right = firstRect.right;
	let bottom = firstRect.bottom;
	let left = firstRect.left;
	for (let i = 1; i < clippingAncestors.length; i++) {
		const rect = getClientRectFromClippingAncestor(element, clippingAncestors[i], strategy);
		top = max(rect.top, top);
		right = min(rect.right, right);
		bottom = min(rect.bottom, bottom);
		left = max(rect.left, left);
	}
	return {
		width: right - left,
		height: bottom - top,
		x: left,
		y: top
	};
}
function getDimensions(element) {
	const { width, height } = getCssDimensions(element);
	return {
		width,
		height
	};
}
function getRectRelativeToOffsetParent(element, offsetParent, strategy) {
	const isOffsetParentAnElement = isHTMLElement(offsetParent);
	const documentElement = getDocumentElement$1(offsetParent);
	const isFixed = strategy === "fixed";
	const rect = getBoundingClientRect(element, true, isFixed, offsetParent);
	let scroll = {
		scrollLeft: 0,
		scrollTop: 0
	};
	const offsets = createCoords(0);
	function setLeftRTLScrollbarOffset() {
		offsets.x = getWindowScrollBarX(documentElement);
	}
	if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
		if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) scroll = getNodeScroll(offsetParent);
		if (isOffsetParentAnElement) {
			const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
			offsets.x = offsetRect.x + offsetParent.clientLeft;
			offsets.y = offsetRect.y + offsetParent.clientTop;
		} else if (documentElement) setLeftRTLScrollbarOffset();
	}
	if (isFixed && !isOffsetParentAnElement && documentElement) setLeftRTLScrollbarOffset();
	const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
	return {
		x: rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x,
		y: rect.top + scroll.scrollTop - offsets.y - htmlOffset.y,
		width: rect.width,
		height: rect.height
	};
}
function isStaticPositioned(element) {
	return getComputedStyle$1(element).position === "static";
}
function getTrueOffsetParent(element, polyfill) {
	if (!isHTMLElement(element) || getComputedStyle$1(element).position === "fixed") return null;
	if (polyfill) return polyfill(element);
	let rawOffsetParent = element.offsetParent;
	if (getDocumentElement$1(element) === rawOffsetParent) rawOffsetParent = rawOffsetParent.ownerDocument.body;
	return rawOffsetParent;
}
function getOffsetParent(element, polyfill) {
	const win = getWindow(element);
	if (isTopLayer(element)) return win;
	if (!isHTMLElement(element)) {
		let svgOffsetParent = getParentNode(element);
		while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
			if (isElement$2(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) return svgOffsetParent;
			svgOffsetParent = getParentNode(svgOffsetParent);
		}
		return win;
	}
	let offsetParent = getTrueOffsetParent(element, polyfill);
	while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) offsetParent = getTrueOffsetParent(offsetParent, polyfill);
	if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) return win;
	return offsetParent || getContainingBlock(element) || win;
}
var getElementRects = async function(data) {
	const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
	const getDimensionsFn = this.getDimensions;
	const floatingDimensions = await getDimensionsFn(data.floating);
	return {
		reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
		floating: {
			x: 0,
			y: 0,
			width: floatingDimensions.width,
			height: floatingDimensions.height
		}
	};
};
function isRTL(element) {
	return getComputedStyle$1(element).direction === "rtl";
}
var platform = {
	convertOffsetParentRelativeRectToViewportRelativeRect,
	getDocumentElement: getDocumentElement$1,
	getClippingRect,
	getOffsetParent,
	getElementRects,
	getClientRects,
	getDimensions,
	getScale,
	isElement: isElement$2,
	isRTL
};
function rectsAreEqual(a, b) {
	return a.x === b.x && a.y === b.y && a.width === b.width && a.height === b.height;
}
function observeMove(element, onMove) {
	let io = null;
	let timeoutId;
	const root = getDocumentElement$1(element);
	function cleanup() {
		var _io;
		clearTimeout(timeoutId);
		(_io = io) == null || _io.disconnect();
		io = null;
	}
	function refresh(skip, threshold) {
		if (skip === void 0) skip = false;
		if (threshold === void 0) threshold = 1;
		cleanup();
		const elementRectForRootMargin = element.getBoundingClientRect();
		const { left, top, width, height } = elementRectForRootMargin;
		if (!skip) onMove();
		if (!width || !height) return;
		const insetTop = floor(top);
		const insetRight = floor(root.clientWidth - (left + width));
		const insetBottom = floor(root.clientHeight - (top + height));
		const insetLeft = floor(left);
		const options = {
			rootMargin: -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px",
			threshold: max(0, min(1, threshold)) || 1
		};
		let isFirstUpdate = true;
		function handleObserve(entries) {
			const ratio = entries[0].intersectionRatio;
			if (ratio !== threshold) {
				if (!isFirstUpdate) return refresh();
				if (!ratio) timeoutId = setTimeout(() => {
					refresh(false, 1e-7);
				}, 1e3);
				else refresh(false, ratio);
			}
			if (ratio === 1 && !rectsAreEqual(elementRectForRootMargin, element.getBoundingClientRect())) refresh();
			isFirstUpdate = false;
		}
		try {
			io = new IntersectionObserver(handleObserve, {
				...options,
				root: root.ownerDocument
			});
		} catch (_e) {
			io = new IntersectionObserver(handleObserve, options);
		}
		io.observe(element);
	}
	refresh(true);
	return cleanup;
}
/**
* Automatically updates the position of the floating element when necessary.
* Should only be called when the floating element is mounted on the DOM or
* visible on the screen.
* @returns cleanup function that should be invoked when the floating element is
* removed from the DOM or hidden from the screen.
* @see https://floating-ui.com/docs/autoUpdate
*/
function autoUpdate(reference, floating, update, options) {
	if (options === void 0) options = {};
	const { ancestorScroll = true, ancestorResize = true, elementResize = typeof ResizeObserver === "function", layoutShift = typeof IntersectionObserver === "function", animationFrame = false } = options;
	const referenceEl = unwrapElement(reference);
	const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...floating ? getOverflowAncestors(floating) : []] : [];
	ancestors.forEach((ancestor) => {
		ancestorScroll && ancestor.addEventListener("scroll", update, { passive: true });
		ancestorResize && ancestor.addEventListener("resize", update);
	});
	const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update) : null;
	let reobserveFrame = -1;
	let resizeObserver = null;
	if (elementResize) {
		resizeObserver = new ResizeObserver((_ref) => {
			let [firstEntry] = _ref;
			if (firstEntry && firstEntry.target === referenceEl && resizeObserver && floating) {
				resizeObserver.unobserve(floating);
				cancelAnimationFrame(reobserveFrame);
				reobserveFrame = requestAnimationFrame(() => {
					var _resizeObserver;
					(_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
				});
			}
			update();
		});
		if (referenceEl && !animationFrame) resizeObserver.observe(referenceEl);
		if (floating) resizeObserver.observe(floating);
	}
	let frameId;
	let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
	if (animationFrame) frameLoop();
	function frameLoop() {
		const nextRefRect = getBoundingClientRect(reference);
		if (prevRefRect && !rectsAreEqual(prevRefRect, nextRefRect)) update();
		prevRefRect = nextRefRect;
		frameId = requestAnimationFrame(frameLoop);
	}
	update();
	return () => {
		var _resizeObserver2;
		ancestors.forEach((ancestor) => {
			ancestorScroll && ancestor.removeEventListener("scroll", update);
			ancestorResize && ancestor.removeEventListener("resize", update);
		});
		cleanupIo?.();
		(_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
		resizeObserver = null;
		if (animationFrame) cancelAnimationFrame(frameId);
	};
}
/**
* Modifies the placement by translating the floating element along the
* specified axes.
* A number (shorthand for `mainAxis` or distance), or an axes configuration
* object may be passed.
* @see https://floating-ui.com/docs/offset
*/
var offset$2 = offset$3;
/**
* Optimizes the visibility of the floating element by choosing the placement
* that has the most space available automatically, without needing to specify a
* preferred placement. Alternative to `flip`.
* @see https://floating-ui.com/docs/autoPlacement
*/
var autoPlacement$1 = autoPlacement$2;
/**
* Optimizes the visibility of the floating element by shifting it in order to
* keep it in view when it will overflow the clipping boundary.
* @see https://floating-ui.com/docs/shift
*/
var shift$2 = shift$3;
/**
* Optimizes the visibility of the floating element by flipping the `placement`
* in order to keep it in view when the preferred placement(s) will overflow the
* clipping boundary. Alternative to `autoPlacement`.
* @see https://floating-ui.com/docs/flip
*/
var flip$1 = flip$2;
/**
* Provides data that allows you to change the size of the floating element —
* for instance, prevent it from overflowing the clipping boundary or match the
* width of the reference element.
* @see https://floating-ui.com/docs/size
*/
var size$2 = size$3;
/**
* Provides data to position an inner element of the floating element so that it
* appears centered to the reference element.
* @see https://floating-ui.com/docs/arrow
*/
var arrow$2 = arrow$3;
/**
* Built-in `limiter` that will stop `shift()` at a certain point.
*/
var limitShift$1 = limitShift$2;
/**
* Computes the `x` and `y` coordinates that will place the floating element
* next to a given reference element.
*/
var computePosition = (reference, floating, options) => {
	const cache = /* @__PURE__ */ new Map();
	const mergedOptions = {
		platform,
		...options
	};
	const platformWithCache = {
		...mergedOptions.platform,
		_c: cache
	};
	return computePosition$1(reference, floating, {
		...mergedOptions,
		platform: platformWithCache
	});
};
//#endregion
//#region node_modules/.pnpm/@floating-ui+react-dom@2.1.8_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@floating-ui/react-dom/dist/floating-ui.react-dom.mjs
var index = typeof document !== "undefined" ? import_react.useLayoutEffect : function noop() {};
function deepEqual(a, b) {
	if (a === b) return true;
	if (typeof a !== typeof b) return false;
	if (typeof a === "function" && a.toString() === b.toString()) return true;
	let length;
	let i;
	let keys;
	if (a && b && typeof a === "object") {
		if (Array.isArray(a)) {
			length = a.length;
			if (length !== b.length) return false;
			for (i = length; i-- !== 0;) if (!deepEqual(a[i], b[i])) return false;
			return true;
		}
		keys = Object.keys(a);
		length = keys.length;
		if (length !== Object.keys(b).length) return false;
		for (i = length; i-- !== 0;) if (!{}.hasOwnProperty.call(b, keys[i])) return false;
		for (i = length; i-- !== 0;) {
			const key = keys[i];
			if (key === "_owner" && a.$$typeof) continue;
			if (!deepEqual(a[key], b[key])) return false;
		}
		return true;
	}
	return a !== a && b !== b;
}
function getDPR(element) {
	if (typeof window === "undefined") return 1;
	return (element.ownerDocument.defaultView || window).devicePixelRatio || 1;
}
function roundByDPR$1(element, value) {
	const dpr = getDPR(element);
	return Math.round(value * dpr) / dpr;
}
function useLatestRef(value) {
	const ref = import_react.useRef(value);
	index(() => {
		ref.current = value;
	});
	return ref;
}
/**
* Provides data to position a floating element.
* @see https://floating-ui.com/docs/useFloating
*/
function useFloating$1(options) {
	if (options === void 0) options = {};
	const { placement = "bottom", strategy = "absolute", middleware = [], platform, elements: { reference: externalReference, floating: externalFloating } = {}, transform = true, whileElementsMounted, open } = options;
	const [data, setData] = import_react.useState({
		x: 0,
		y: 0,
		strategy,
		placement,
		middlewareData: {},
		isPositioned: false
	});
	const [latestMiddleware, setLatestMiddleware] = import_react.useState(middleware);
	if (!deepEqual(latestMiddleware, middleware)) setLatestMiddleware(middleware);
	const [_reference, _setReference] = import_react.useState(null);
	const [_floating, _setFloating] = import_react.useState(null);
	const setReference = import_react.useCallback((node) => {
		if (node !== referenceRef.current) {
			referenceRef.current = node;
			_setReference(node);
		}
	}, []);
	const setFloating = import_react.useCallback((node) => {
		if (node !== floatingRef.current) {
			floatingRef.current = node;
			_setFloating(node);
		}
	}, []);
	const referenceEl = externalReference || _reference;
	const floatingEl = externalFloating || _floating;
	const referenceRef = import_react.useRef(null);
	const floatingRef = import_react.useRef(null);
	const dataRef = import_react.useRef(data);
	const hasWhileElementsMounted = whileElementsMounted != null;
	const whileElementsMountedRef = useLatestRef(whileElementsMounted);
	const platformRef = useLatestRef(platform);
	const openRef = useLatestRef(open);
	const update = import_react.useCallback(() => {
		if (!referenceRef.current || !floatingRef.current) return;
		const config = {
			placement,
			strategy,
			middleware: latestMiddleware
		};
		if (platformRef.current) config.platform = platformRef.current;
		computePosition(referenceRef.current, floatingRef.current, config).then((data) => {
			const fullData = {
				...data,
				isPositioned: openRef.current !== false
			};
			if (isMountedRef.current && !deepEqual(dataRef.current, fullData)) {
				dataRef.current = fullData;
				import_react_dom.flushSync(() => {
					setData(fullData);
				});
			}
		});
	}, [
		latestMiddleware,
		placement,
		strategy,
		platformRef,
		openRef
	]);
	index(() => {
		if (open === false && dataRef.current.isPositioned) {
			dataRef.current.isPositioned = false;
			setData((data) => ({
				...data,
				isPositioned: false
			}));
		}
	}, [open]);
	const isMountedRef = import_react.useRef(false);
	index(() => {
		isMountedRef.current = true;
		return () => {
			isMountedRef.current = false;
		};
	}, []);
	index(() => {
		if (referenceEl) referenceRef.current = referenceEl;
		if (floatingEl) floatingRef.current = floatingEl;
		if (referenceEl && floatingEl) {
			if (whileElementsMountedRef.current) return whileElementsMountedRef.current(referenceEl, floatingEl, update);
			update();
		}
	}, [
		referenceEl,
		floatingEl,
		update,
		whileElementsMountedRef,
		hasWhileElementsMounted
	]);
	const refs = import_react.useMemo(() => ({
		reference: referenceRef,
		floating: floatingRef,
		setReference,
		setFloating
	}), [setReference, setFloating]);
	const elements = import_react.useMemo(() => ({
		reference: referenceEl,
		floating: floatingEl
	}), [referenceEl, floatingEl]);
	const floatingStyles = import_react.useMemo(() => {
		const initialStyles = {
			position: strategy,
			left: 0,
			top: 0
		};
		if (!elements.floating) return initialStyles;
		const x = roundByDPR$1(elements.floating, data.x);
		const y = roundByDPR$1(elements.floating, data.y);
		if (transform) return {
			...initialStyles,
			transform: "translate(" + x + "px, " + y + "px)",
			...getDPR(elements.floating) >= 1.5 && { willChange: "transform" }
		};
		return {
			position: strategy,
			left: x,
			top: y
		};
	}, [
		strategy,
		transform,
		elements.floating,
		data.x,
		data.y
	]);
	return import_react.useMemo(() => ({
		...data,
		update,
		refs,
		elements,
		floatingStyles
	}), [
		data,
		update,
		refs,
		elements,
		floatingStyles
	]);
}
/**
* Provides data to position an inner element of the floating element so that it
* appears centered to the reference element.
* This wraps the core `arrow` middleware to allow React refs as the element.
* @see https://floating-ui.com/docs/arrow
*/
var arrow$1 = (options) => {
	function isRef(value) {
		return {}.hasOwnProperty.call(value, "current");
	}
	return {
		name: "arrow",
		options,
		fn(state) {
			const { element, padding } = typeof options === "function" ? options(state) : options;
			if (element && isRef(element)) {
				if (element.current != null) return arrow$2({
					element: element.current,
					padding
				}).fn(state);
				return {};
			}
			if (element) return arrow$2({
				element,
				padding
			}).fn(state);
			return {};
		}
	};
};
/**
* Modifies the placement by translating the floating element along the
* specified axes.
* A number (shorthand for `mainAxis` or distance), or an axes configuration
* object may be passed.
* @see https://floating-ui.com/docs/offset
*/
var offset$1 = (options, deps) => {
	const result = offset$2(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
/**
* Optimizes the visibility of the floating element by shifting it in order to
* keep it in view when it will overflow the clipping boundary.
* @see https://floating-ui.com/docs/shift
*/
var shift$1 = (options, deps) => {
	const result = shift$2(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
/**
* Built-in `limiter` that will stop `shift()` at a certain point.
*/
var limitShift = (options, deps) => {
	return {
		fn: limitShift$1(options).fn,
		options: [options, deps]
	};
};
/**
* Optimizes the visibility of the floating element by flipping the `placement`
* in order to keep it in view when the preferred placement(s) will overflow the
* clipping boundary. Alternative to `autoPlacement`.
* @see https://floating-ui.com/docs/flip
*/
var flip = (options, deps) => {
	const result = flip$1(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
/**
* Provides data that allows you to change the size of the floating element —
* for instance, prevent it from overflowing the clipping boundary or match the
* width of the reference element.
* @see https://floating-ui.com/docs/size
*/
var size$1 = (options, deps) => {
	const result = size$2(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
/**
* Optimizes the visibility of the floating element by choosing the placement
* that has the most space available automatically, without needing to specify a
* preferred placement. Alternative to `flip`.
* @see https://floating-ui.com/docs/autoPlacement
*/
var autoPlacement = (options, deps) => {
	const result = autoPlacement$1(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
/**
* Provides data to position an inner element of the floating element so that it
* appears centered to the reference element.
* This wraps the core `arrow` middleware to allow React refs as the element.
* @see https://floating-ui.com/docs/arrow
*/
var arrow = (options, deps) => {
	const result = arrow$1(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
//#endregion
//#region node_modules/.pnpm/@floating-ui+react@0.27.19_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@floating-ui/react/dist/floating-ui.react.mjs
/**
* Merges an array of refs into a single memoized callback ref or `null`.
* @see https://floating-ui.com/docs/react-utils#usemergerefs
*/
function useMergeRefs(refs) {
	const cleanupRef = import_react.useRef(void 0);
	const refEffect = import_react.useCallback((instance) => {
		const cleanups = refs.map((ref) => {
			if (ref == null) return;
			if (typeof ref === "function") {
				const refCallback = ref;
				const refCleanup = refCallback(instance);
				return typeof refCleanup === "function" ? refCleanup : () => {
					refCallback(null);
				};
			}
			ref.current = instance;
			return () => {
				ref.current = null;
			};
		});
		return () => {
			cleanups.forEach((refCleanup) => refCleanup == null ? void 0 : refCleanup());
		};
	}, refs);
	return import_react.useMemo(() => {
		if (refs.every((ref) => ref == null)) return null;
		return (value) => {
			if (cleanupRef.current) {
				cleanupRef.current();
				cleanupRef.current = void 0;
			}
			if (value != null) cleanupRef.current = refEffect(value);
		};
	}, refs);
}
function sortByDocumentPosition(a, b) {
	const position = a.compareDocumentPosition(b);
	if (position & Node.DOCUMENT_POSITION_FOLLOWING || position & Node.DOCUMENT_POSITION_CONTAINED_BY) return -1;
	if (position & Node.DOCUMENT_POSITION_PRECEDING || position & Node.DOCUMENT_POSITION_CONTAINS) return 1;
	return 0;
}
var FloatingListContext = /*#__PURE__*/ import_react.createContext({
	register: () => {},
	unregister: () => {},
	map: /*#__PURE__*/ new Map(),
	elementsRef: { current: [] }
});
/**
* Provides context for a list of items within the floating element.
* @see https://floating-ui.com/docs/FloatingList
*/
function FloatingList(props) {
	const { children, elementsRef, labelsRef } = props;
	const [nodes, setNodes] = import_react.useState(() => /* @__PURE__ */ new Set());
	const register = import_react.useCallback((node) => {
		setNodes((prevSet) => new Set(prevSet).add(node));
	}, []);
	const unregister = import_react.useCallback((node) => {
		setNodes((prevSet) => {
			const set = new Set(prevSet);
			set.delete(node);
			return set;
		});
	}, []);
	const map = import_react.useMemo(() => {
		const newMap = /* @__PURE__ */ new Map();
		Array.from(nodes.keys()).sort(sortByDocumentPosition).forEach((node, index) => {
			newMap.set(node, index);
		});
		return newMap;
	}, [nodes]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(FloatingListContext.Provider, {
		value: import_react.useMemo(() => ({
			register,
			unregister,
			map,
			elementsRef,
			labelsRef
		}), [
			register,
			unregister,
			map,
			elementsRef,
			labelsRef
		]),
		children
	});
}
/**
* Used to register a list item and its index (DOM position) in the
* `FloatingList`.
* @see https://floating-ui.com/docs/FloatingList#uselistitem
*/
function useListItem(props) {
	if (props === void 0) props = {};
	const { label } = props;
	const { register, unregister, map, elementsRef, labelsRef } = import_react.useContext(FloatingListContext);
	const [index, setIndex] = import_react.useState(null);
	const componentRef = import_react.useRef(null);
	const ref = import_react.useCallback((node) => {
		componentRef.current = node;
		if (index !== null) {
			elementsRef.current[index] = node;
			if (labelsRef) {
				var _node$textContent;
				const isLabelDefined = label !== void 0;
				labelsRef.current[index] = isLabelDefined ? label : (_node$textContent = node == null ? void 0 : node.textContent) != null ? _node$textContent : null;
			}
		}
	}, [
		index,
		elementsRef,
		labelsRef,
		label
	]);
	index$1(() => {
		const node = componentRef.current;
		if (node) {
			register(node);
			return () => {
				unregister(node);
			};
		}
	}, [register, unregister]);
	index$1(() => {
		const index = componentRef.current ? map.get(componentRef.current) : null;
		if (index != null) setIndex(index);
	}, [map]);
	return import_react.useMemo(() => ({
		ref,
		index: index == null ? -1 : index
	}), [index, ref]);
}
var FOCUSABLE_ATTRIBUTE = "data-floating-ui-focusable";
var ACTIVE_KEY = "active";
var SELECTED_KEY = "selected";
var ARROW_LEFT = "ArrowLeft";
var ARROW_RIGHT = "ArrowRight";
var ARROW_UP = "ArrowUp";
var ARROW_DOWN = "ArrowDown";
function renderJsx(render, computedProps) {
	if (typeof render === "function") return render(computedProps);
	if (render) return /*#__PURE__*/ import_react.cloneElement(render, computedProps);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("div", { ...computedProps });
}
var CompositeContext = /*#__PURE__*/ import_react.createContext({
	activeIndex: 0,
	onNavigate: () => {}
});
var horizontalKeys = [ARROW_LEFT, ARROW_RIGHT];
var verticalKeys = [ARROW_UP, ARROW_DOWN];
var allKeys = [...horizontalKeys, ...verticalKeys];
/**
* Creates a single tab stop whose items are navigated by arrow keys, which
* provides list navigation outside of floating element contexts.
*
* This is useful to enable navigation of a list of items that aren’t part of a
* floating element. A menubar is an example of a composite, with each reference
* element being an item.
* @see https://floating-ui.com/docs/Composite
*/
var Composite = /*#__PURE__*/ import_react.forwardRef(function Composite(props, forwardedRef) {
	const { render, orientation = "both", loop = true, rtl = false, cols = 1, disabledIndices, activeIndex: externalActiveIndex, onNavigate: externalSetActiveIndex, itemSizes, dense = false, ...domProps } = props;
	const [internalActiveIndex, internalSetActiveIndex] = import_react.useState(0);
	const activeIndex = externalActiveIndex != null ? externalActiveIndex : internalActiveIndex;
	const onNavigate = useEffectEvent(externalSetActiveIndex != null ? externalSetActiveIndex : internalSetActiveIndex);
	const elementsRef = import_react.useRef([]);
	const renderElementProps = render && typeof render !== "function" ? render.props : {};
	const contextValue = import_react.useMemo(() => ({
		activeIndex,
		onNavigate
	}), [activeIndex, onNavigate]);
	const isGrid = cols > 1;
	function handleKeyDown(event) {
		if (!allKeys.includes(event.key)) return;
		let nextIndex = activeIndex;
		const minIndex = getMinListIndex(elementsRef, disabledIndices);
		const maxIndex = getMaxListIndex(elementsRef, disabledIndices);
		const horizontalEndKey = rtl ? ARROW_LEFT : ARROW_RIGHT;
		const horizontalStartKey = rtl ? ARROW_RIGHT : ARROW_LEFT;
		if (isGrid) {
			const sizes = itemSizes || Array.from({ length: elementsRef.current.length }, () => ({
				width: 1,
				height: 1
			}));
			const cellMap = createGridCellMap(sizes, cols, dense);
			const minGridIndex = cellMap.findIndex((index) => index != null && !isListIndexDisabled(elementsRef, index, disabledIndices));
			const maxGridIndex = cellMap.reduce((foundIndex, index, cellIndex) => index != null && !isListIndexDisabled(elementsRef, index, disabledIndices) ? cellIndex : foundIndex, -1);
			const maybeNextIndex = cellMap[getGridNavigatedIndex({ current: cellMap.map((itemIndex) => itemIndex ? elementsRef.current[itemIndex] : null) }, {
				event,
				orientation,
				loop,
				rtl,
				cols,
				disabledIndices: getGridCellIndices([...(typeof disabledIndices !== "function" ? disabledIndices : null) || elementsRef.current.map((_, index) => isListIndexDisabled(elementsRef, index, disabledIndices) ? index : void 0), void 0], cellMap),
				minIndex: minGridIndex,
				maxIndex: maxGridIndex,
				prevIndex: getGridCellIndexOfCorner(activeIndex > maxIndex ? minIndex : activeIndex, sizes, cellMap, cols, event.key === ARROW_DOWN ? "bl" : event.key === horizontalEndKey ? "tr" : "tl")
			})];
			if (maybeNextIndex != null) nextIndex = maybeNextIndex;
		}
		const toEndKeys = {
			horizontal: [horizontalEndKey],
			vertical: [ARROW_DOWN],
			both: [horizontalEndKey, ARROW_DOWN]
		}[orientation];
		const toStartKeys = {
			horizontal: [horizontalStartKey],
			vertical: [ARROW_UP],
			both: [horizontalStartKey, ARROW_UP]
		}[orientation];
		const preventedKeys = isGrid ? allKeys : {
			horizontal: horizontalKeys,
			vertical: verticalKeys,
			both: allKeys
		}[orientation];
		if (nextIndex === activeIndex && [...toEndKeys, ...toStartKeys].includes(event.key)) if (loop && nextIndex === maxIndex && toEndKeys.includes(event.key)) nextIndex = minIndex;
		else if (loop && nextIndex === minIndex && toStartKeys.includes(event.key)) nextIndex = maxIndex;
		else nextIndex = findNonDisabledListIndex(elementsRef, {
			startingIndex: nextIndex,
			decrement: toStartKeys.includes(event.key),
			disabledIndices
		});
		if (nextIndex !== activeIndex && !isIndexOutOfListBounds(elementsRef, nextIndex)) {
			var _elementsRef$current$;
			event.stopPropagation();
			if (preventedKeys.includes(event.key)) event.preventDefault();
			onNavigate(nextIndex);
			(_elementsRef$current$ = elementsRef.current[nextIndex]) == null || _elementsRef$current$.focus();
		}
	}
	const computedProps = {
		...domProps,
		...renderElementProps,
		ref: forwardedRef,
		"aria-orientation": orientation === "both" ? void 0 : orientation,
		onKeyDown(e) {
			domProps.onKeyDown == null || domProps.onKeyDown(e);
			renderElementProps.onKeyDown == null || renderElementProps.onKeyDown(e);
			handleKeyDown(e);
		}
	};
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CompositeContext.Provider, {
		value: contextValue,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(FloatingList, {
			elementsRef,
			children: renderJsx(render, computedProps)
		})
	});
});
/**
* @see https://floating-ui.com/docs/Composite
*/
var CompositeItem = /*#__PURE__*/ import_react.forwardRef(function CompositeItem(props, forwardedRef) {
	const { render, ...domProps } = props;
	const renderElementProps = render && typeof render !== "function" ? render.props : {};
	const { activeIndex, onNavigate } = import_react.useContext(CompositeContext);
	const { ref, index } = useListItem();
	const mergedRef = useMergeRefs([
		ref,
		forwardedRef,
		renderElementProps.ref
	]);
	const isActive = activeIndex === index;
	return renderJsx(render, {
		...domProps,
		...renderElementProps,
		ref: mergedRef,
		tabIndex: isActive ? 0 : -1,
		"data-active": isActive ? "" : void 0,
		onFocus(e) {
			domProps.onFocus == null || domProps.onFocus(e);
			renderElementProps.onFocus == null || renderElementProps.onFocus(e);
			onNavigate(index);
		}
	});
});
var SafeReact = { ...import_react };
var serverHandoffComplete = false;
var count = 0;
var genId = () => "floating-ui-" + Math.random().toString(36).slice(2, 6) + count++;
function useFloatingId() {
	const [id, setId] = import_react.useState(() => serverHandoffComplete ? genId() : void 0);
	index$1(() => {
		if (id == null) setId(genId());
	}, []);
	import_react.useEffect(() => {
		serverHandoffComplete = true;
	}, []);
	return id;
}
/**
* Uses React 18's built-in `useId()` when available, or falls back to a
* slightly less performant (requiring a double render) implementation for
* earlier React versions.
* @see https://floating-ui.com/docs/react-utils#useid
*/
var useId = SafeReact.useId || useFloatingId;
var devMessageSet = /*#__PURE__*/ new Set();
function warn() {
	var _devMessageSet;
	for (var _len = arguments.length, messages = new Array(_len), _key = 0; _key < _len; _key++) messages[_key] = arguments[_key];
	const message = "Floating UI: " + messages.join(" ");
	if (!((_devMessageSet = devMessageSet) != null && _devMessageSet.has(message))) {
		var _devMessageSet2;
		(_devMessageSet2 = devMessageSet) == null || _devMessageSet2.add(message);
		console.warn(message);
	}
}
function error$1() {
	var _devMessageSet3;
	for (var _len2 = arguments.length, messages = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) messages[_key2] = arguments[_key2];
	const message = "Floating UI: " + messages.join(" ");
	if (!((_devMessageSet3 = devMessageSet) != null && _devMessageSet3.has(message))) {
		var _devMessageSet4;
		(_devMessageSet4 = devMessageSet) == null || _devMessageSet4.add(message);
		console.error(message);
	}
}
function createEventEmitter() {
	const map = /* @__PURE__ */ new Map();
	return {
		emit(event, data) {
			var _map$get;
			(_map$get = map.get(event)) == null || _map$get.forEach((listener) => listener(data));
		},
		on(event, listener) {
			if (!map.has(event)) map.set(event, /* @__PURE__ */ new Set());
			map.get(event).add(listener);
		},
		off(event, listener) {
			var _map$get2;
			(_map$get2 = map.get(event)) == null || _map$get2.delete(listener);
		}
	};
}
var FloatingNodeContext = /*#__PURE__*/ import_react.createContext(null);
var FloatingTreeContext = /*#__PURE__*/ import_react.createContext(null);
/**
* Returns the parent node id for nested floating elements, if available.
* Returns `null` for top-level floating elements.
*/
var useFloatingParentNodeId = () => {
	var _React$useContext;
	return ((_React$useContext = import_react.useContext(FloatingNodeContext)) == null ? void 0 : _React$useContext.id) || null;
};
/**
* Returns the nearest floating tree context, if available.
*/
var useFloatingTree = () => import_react.useContext(FloatingTreeContext);
/**
* Registers a node into the `FloatingTree`, returning its id.
* @see https://floating-ui.com/docs/FloatingTree
*/
function useFloatingNodeId(customParentId) {
	const id = useId();
	const tree = useFloatingTree();
	const reactParentId = useFloatingParentNodeId();
	const parentId = customParentId || reactParentId;
	index$1(() => {
		if (!id) return;
		const node = {
			id,
			parentId
		};
		tree?.addNode(node);
		return () => {
			tree?.removeNode(node);
		};
	}, [
		tree,
		id,
		parentId
	]);
	return id;
}
/**
* Provides parent node context for nested floating elements.
* @see https://floating-ui.com/docs/FloatingTree
*/
function FloatingNode(props) {
	const { children, id } = props;
	const parentId = useFloatingParentNodeId();
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(FloatingNodeContext.Provider, {
		value: import_react.useMemo(() => ({
			id,
			parentId
		}), [id, parentId]),
		children
	});
}
/**
* Provides context for nested floating elements when they are not children of
* each other on the DOM.
* This is not necessary in all cases, except when there must be explicit communication between parent and child floating elements. It is necessary for:
* - The `bubbles` option in the `useDismiss()` Hook
* - Nested virtual list navigation
* - Nested floating elements that each open on hover
* - Custom communication between parent and child floating elements
* @see https://floating-ui.com/docs/FloatingTree
*/
function FloatingTree(props) {
	const { children } = props;
	const nodesRef = import_react.useRef([]);
	const addNode = import_react.useCallback((node) => {
		nodesRef.current = [...nodesRef.current, node];
	}, []);
	const removeNode = import_react.useCallback((node) => {
		nodesRef.current = nodesRef.current.filter((n) => n !== node);
	}, []);
	const [events] = import_react.useState(() => createEventEmitter());
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(FloatingTreeContext.Provider, {
		value: import_react.useMemo(() => ({
			nodesRef,
			addNode,
			removeNode,
			events
		}), [
			addNode,
			removeNode,
			events
		]),
		children
	});
}
function createAttribute(name) {
	return "data-floating-ui-" + name;
}
function clearTimeoutIfSet(timeoutRef) {
	if (timeoutRef.current !== -1) {
		clearTimeout(timeoutRef.current);
		timeoutRef.current = -1;
	}
}
var safePolygonIdentifier = /*#__PURE__*/ createAttribute("safe-polygon");
function getDelay(value, prop, pointerType) {
	if (pointerType && !isMouseLikePointerType(pointerType)) return 0;
	if (typeof value === "number") return value;
	if (typeof value === "function") {
		const result = value();
		if (typeof result === "number") return result;
		return result == null ? void 0 : result[prop];
	}
	return value == null ? void 0 : value[prop];
}
function getRestMs(value) {
	if (typeof value === "function") return value();
	return value;
}
/**
* Opens the floating element while hovering over the reference element, like
* CSS `:hover`.
* @see https://floating-ui.com/docs/useHover
*/
function useHover$1(context, props) {
	if (props === void 0) props = {};
	const { open, onOpenChange, dataRef, events, elements } = context;
	const { enabled = true, delay = 0, handleClose = null, mouseOnly = false, restMs = 0, move = true } = props;
	const tree = useFloatingTree();
	const parentId = useFloatingParentNodeId();
	const handleCloseRef = useLatestRef$1(handleClose);
	const delayRef = useLatestRef$1(delay);
	const openRef = useLatestRef$1(open);
	const restMsRef = useLatestRef$1(restMs);
	const pointerTypeRef = import_react.useRef();
	const timeoutRef = import_react.useRef(-1);
	const handlerRef = import_react.useRef();
	const restTimeoutRef = import_react.useRef(-1);
	const blockMouseMoveRef = import_react.useRef(true);
	const performedPointerEventsMutationRef = import_react.useRef(false);
	const unbindMouseMoveRef = import_react.useRef(() => {});
	const restTimeoutPendingRef = import_react.useRef(false);
	const isHoverOpen = useEffectEvent(() => {
		var _dataRef$current$open;
		const type = (_dataRef$current$open = dataRef.current.openEvent) == null ? void 0 : _dataRef$current$open.type;
		return (type == null ? void 0 : type.includes("mouse")) && type !== "mousedown";
	});
	import_react.useEffect(() => {
		if (!enabled) return;
		function onOpenChange(_ref) {
			let { open } = _ref;
			if (!open) {
				clearTimeoutIfSet(timeoutRef);
				clearTimeoutIfSet(restTimeoutRef);
				blockMouseMoveRef.current = true;
				restTimeoutPendingRef.current = false;
			}
		}
		events.on("openchange", onOpenChange);
		return () => {
			events.off("openchange", onOpenChange);
		};
	}, [enabled, events]);
	import_react.useEffect(() => {
		if (!enabled) return;
		if (!handleCloseRef.current) return;
		if (!open) return;
		function onLeave(event) {
			if (isHoverOpen()) onOpenChange(false, event, "hover");
		}
		const html = getDocument$1(elements.floating).documentElement;
		html.addEventListener("mouseleave", onLeave);
		return () => {
			html.removeEventListener("mouseleave", onLeave);
		};
	}, [
		elements.floating,
		open,
		onOpenChange,
		enabled,
		handleCloseRef,
		isHoverOpen
	]);
	const closeWithDelay = import_react.useCallback(function(event, runElseBranch, reason) {
		if (runElseBranch === void 0) runElseBranch = true;
		if (reason === void 0) reason = "hover";
		const closeDelay = getDelay(delayRef.current, "close", pointerTypeRef.current);
		if (closeDelay && !handlerRef.current) {
			clearTimeoutIfSet(timeoutRef);
			timeoutRef.current = window.setTimeout(() => onOpenChange(false, event, reason), closeDelay);
		} else if (runElseBranch) {
			clearTimeoutIfSet(timeoutRef);
			onOpenChange(false, event, reason);
		}
	}, [delayRef, onOpenChange]);
	const cleanupMouseMoveHandler = useEffectEvent(() => {
		unbindMouseMoveRef.current();
		handlerRef.current = void 0;
	});
	const clearPointerEvents = useEffectEvent(() => {
		if (performedPointerEventsMutationRef.current) {
			const body = getDocument$1(elements.floating).body;
			body.style.pointerEvents = "";
			body.removeAttribute(safePolygonIdentifier);
			performedPointerEventsMutationRef.current = false;
		}
	});
	const isClickLikeOpenEvent = useEffectEvent(() => {
		return dataRef.current.openEvent ? ["click", "mousedown"].includes(dataRef.current.openEvent.type) : false;
	});
	import_react.useEffect(() => {
		if (!enabled) return;
		function onReferenceMouseEnter(event) {
			clearTimeoutIfSet(timeoutRef);
			blockMouseMoveRef.current = false;
			if (mouseOnly && !isMouseLikePointerType(pointerTypeRef.current) || getRestMs(restMsRef.current) > 0 && !getDelay(delayRef.current, "open")) return;
			const openDelay = getDelay(delayRef.current, "open", pointerTypeRef.current);
			if (openDelay) timeoutRef.current = window.setTimeout(() => {
				if (!openRef.current) onOpenChange(true, event, "hover");
			}, openDelay);
			else if (!open) onOpenChange(true, event, "hover");
		}
		function onReferenceMouseLeave(event) {
			if (isClickLikeOpenEvent()) {
				clearPointerEvents();
				return;
			}
			unbindMouseMoveRef.current();
			const doc = getDocument$1(elements.floating);
			clearTimeoutIfSet(restTimeoutRef);
			restTimeoutPendingRef.current = false;
			if (handleCloseRef.current && dataRef.current.floatingContext) {
				if (!open) clearTimeoutIfSet(timeoutRef);
				handlerRef.current = handleCloseRef.current({
					...dataRef.current.floatingContext,
					tree,
					x: event.clientX,
					y: event.clientY,
					onClose() {
						clearPointerEvents();
						cleanupMouseMoveHandler();
						if (!isClickLikeOpenEvent()) closeWithDelay(event, true, "safe-polygon");
					}
				});
				const handler = handlerRef.current;
				doc.addEventListener("mousemove", handler);
				unbindMouseMoveRef.current = () => {
					doc.removeEventListener("mousemove", handler);
				};
				return;
			}
			if (pointerTypeRef.current === "touch" ? !contains$1(elements.floating, event.relatedTarget) : true) closeWithDelay(event);
		}
		function onScrollMouseLeave(event) {
			if (isClickLikeOpenEvent()) return;
			if (!dataRef.current.floatingContext) return;
			handleCloseRef.current == null || handleCloseRef.current({
				...dataRef.current.floatingContext,
				tree,
				x: event.clientX,
				y: event.clientY,
				onClose() {
					clearPointerEvents();
					cleanupMouseMoveHandler();
					if (!isClickLikeOpenEvent()) closeWithDelay(event);
				}
			})(event);
		}
		function onFloatingMouseEnter() {
			clearTimeoutIfSet(timeoutRef);
		}
		function onFloatingMouseLeave(event) {
			if (!isClickLikeOpenEvent()) closeWithDelay(event, false);
		}
		if (isElement$2(elements.domReference)) {
			const reference = elements.domReference;
			const floating = elements.floating;
			if (open) reference.addEventListener("mouseleave", onScrollMouseLeave);
			if (move) reference.addEventListener("mousemove", onReferenceMouseEnter, { once: true });
			reference.addEventListener("mouseenter", onReferenceMouseEnter);
			reference.addEventListener("mouseleave", onReferenceMouseLeave);
			if (floating) {
				floating.addEventListener("mouseleave", onScrollMouseLeave);
				floating.addEventListener("mouseenter", onFloatingMouseEnter);
				floating.addEventListener("mouseleave", onFloatingMouseLeave);
			}
			return () => {
				if (open) reference.removeEventListener("mouseleave", onScrollMouseLeave);
				if (move) reference.removeEventListener("mousemove", onReferenceMouseEnter);
				reference.removeEventListener("mouseenter", onReferenceMouseEnter);
				reference.removeEventListener("mouseleave", onReferenceMouseLeave);
				if (floating) {
					floating.removeEventListener("mouseleave", onScrollMouseLeave);
					floating.removeEventListener("mouseenter", onFloatingMouseEnter);
					floating.removeEventListener("mouseleave", onFloatingMouseLeave);
				}
			};
		}
	}, [
		elements,
		enabled,
		context,
		mouseOnly,
		move,
		closeWithDelay,
		cleanupMouseMoveHandler,
		clearPointerEvents,
		onOpenChange,
		open,
		openRef,
		tree,
		delayRef,
		handleCloseRef,
		dataRef,
		isClickLikeOpenEvent,
		restMsRef
	]);
	index$1(() => {
		var _handleCloseRef$curre;
		if (!enabled) return;
		if (open && (_handleCloseRef$curre = handleCloseRef.current) != null && (_handleCloseRef$curre = _handleCloseRef$curre.__options) != null && _handleCloseRef$curre.blockPointerEvents && isHoverOpen()) {
			performedPointerEventsMutationRef.current = true;
			const floatingEl = elements.floating;
			if (isElement$2(elements.domReference) && floatingEl) {
				var _tree$nodesRef$curren;
				const body = getDocument$1(elements.floating).body;
				body.setAttribute(safePolygonIdentifier, "");
				const ref = elements.domReference;
				const parentFloating = tree == null || (_tree$nodesRef$curren = tree.nodesRef.current.find((node) => node.id === parentId)) == null || (_tree$nodesRef$curren = _tree$nodesRef$curren.context) == null ? void 0 : _tree$nodesRef$curren.elements.floating;
				if (parentFloating) parentFloating.style.pointerEvents = "";
				body.style.pointerEvents = "none";
				ref.style.pointerEvents = "auto";
				floatingEl.style.pointerEvents = "auto";
				return () => {
					body.style.pointerEvents = "";
					ref.style.pointerEvents = "";
					floatingEl.style.pointerEvents = "";
				};
			}
		}
	}, [
		enabled,
		open,
		parentId,
		elements,
		tree,
		handleCloseRef,
		isHoverOpen
	]);
	index$1(() => {
		if (!open) {
			pointerTypeRef.current = void 0;
			restTimeoutPendingRef.current = false;
			cleanupMouseMoveHandler();
			clearPointerEvents();
		}
	}, [
		open,
		cleanupMouseMoveHandler,
		clearPointerEvents
	]);
	import_react.useEffect(() => {
		return () => {
			cleanupMouseMoveHandler();
			clearTimeoutIfSet(timeoutRef);
			clearTimeoutIfSet(restTimeoutRef);
			clearPointerEvents();
		};
	}, [
		enabled,
		elements.domReference,
		cleanupMouseMoveHandler,
		clearPointerEvents
	]);
	const reference = import_react.useMemo(() => {
		function setPointerRef(event) {
			pointerTypeRef.current = event.pointerType;
		}
		return {
			onPointerDown: setPointerRef,
			onPointerEnter: setPointerRef,
			onMouseMove(event) {
				const { nativeEvent } = event;
				function handleMouseMove() {
					if (!blockMouseMoveRef.current && !openRef.current) onOpenChange(true, nativeEvent, "hover");
				}
				if (mouseOnly && !isMouseLikePointerType(pointerTypeRef.current)) return;
				if (open || getRestMs(restMsRef.current) === 0) return;
				if (restTimeoutPendingRef.current && event.movementX ** 2 + event.movementY ** 2 < 2) return;
				clearTimeoutIfSet(restTimeoutRef);
				if (pointerTypeRef.current === "touch") handleMouseMove();
				else {
					restTimeoutPendingRef.current = true;
					restTimeoutRef.current = window.setTimeout(handleMouseMove, getRestMs(restMsRef.current));
				}
			}
		};
	}, [
		mouseOnly,
		onOpenChange,
		open,
		openRef,
		restMsRef
	]);
	return import_react.useMemo(() => enabled ? { reference } : {}, [enabled, reference]);
}
var rafId = 0;
function enqueueFocus(el, options) {
	if (options === void 0) options = {};
	const { preventScroll = false, cancelPrevious = true, sync = false } = options;
	cancelPrevious && cancelAnimationFrame(rafId);
	const exec = () => el == null ? void 0 : el.focus({ preventScroll });
	if (sync) exec();
	else rafId = requestAnimationFrame(exec);
}
function contains(parent, child) {
	if (!parent || !child) return false;
	const rootNode = child.getRootNode == null ? void 0 : child.getRootNode();
	if (parent.contains(child)) return true;
	if (rootNode && isShadowRoot(rootNode)) {
		let next = child;
		while (next) {
			if (parent === next) return true;
			next = next.parentNode || next.host;
		}
	}
	return false;
}
function getTarget$1(event) {
	if ("composedPath" in event) return event.composedPath()[0];
	return event.target;
}
function getDocument(node) {
	return (node == null ? void 0 : node.ownerDocument) || document;
}
var counters = {
	inert: /*#__PURE__*/ new WeakMap(),
	"aria-hidden": /*#__PURE__*/ new WeakMap(),
	none: /*#__PURE__*/ new WeakMap()
};
function getCounterMap(control) {
	if (control === "inert") return counters.inert;
	if (control === "aria-hidden") return counters["aria-hidden"];
	return counters.none;
}
var uncontrolledElementsSet = /*#__PURE__*/ new WeakSet();
var markerMap = {};
var lockCount$1 = 0;
var supportsInert = () => typeof HTMLElement !== "undefined" && "inert" in HTMLElement.prototype;
function unwrapHost(node) {
	if (!node) return null;
	return isShadowRoot(node) ? node.host : unwrapHost(node.parentNode);
}
var correctElements = (parent, targets) => targets.map((target) => {
	if (parent.contains(target)) return target;
	const correctedTarget = unwrapHost(target);
	if (parent.contains(correctedTarget)) return correctedTarget;
	return null;
}).filter((x) => x != null);
function applyAttributeToOthers(uncorrectedAvoidElements, body, ariaHidden, inert) {
	const markerName = "data-floating-ui-inert";
	const controlAttribute = inert ? "inert" : ariaHidden ? "aria-hidden" : null;
	const avoidElements = correctElements(body, uncorrectedAvoidElements);
	const elementsToKeep = /* @__PURE__ */ new Set();
	const elementsToStop = new Set(avoidElements);
	const hiddenElements = [];
	if (!markerMap[markerName]) markerMap[markerName] = /* @__PURE__ */ new WeakMap();
	const markerCounter = markerMap[markerName];
	avoidElements.forEach(keep);
	deep(body);
	elementsToKeep.clear();
	function keep(el) {
		if (!el || elementsToKeep.has(el)) return;
		elementsToKeep.add(el);
		el.parentNode && keep(el.parentNode);
	}
	function deep(parent) {
		if (!parent || elementsToStop.has(parent)) return;
		[].forEach.call(parent.children, (node) => {
			if (getNodeName(node) === "script") return;
			if (elementsToKeep.has(node)) deep(node);
			else {
				const attr = controlAttribute ? node.getAttribute(controlAttribute) : null;
				const alreadyHidden = attr !== null && attr !== "false";
				const counterMap = getCounterMap(controlAttribute);
				const counterValue = (counterMap.get(node) || 0) + 1;
				const markerValue = (markerCounter.get(node) || 0) + 1;
				counterMap.set(node, counterValue);
				markerCounter.set(node, markerValue);
				hiddenElements.push(node);
				if (counterValue === 1 && alreadyHidden) uncontrolledElementsSet.add(node);
				if (markerValue === 1) node.setAttribute(markerName, "");
				if (!alreadyHidden && controlAttribute) node.setAttribute(controlAttribute, controlAttribute === "inert" ? "" : "true");
			}
		});
	}
	lockCount$1++;
	return () => {
		hiddenElements.forEach((element) => {
			const counterMap = getCounterMap(controlAttribute);
			const counterValue = (counterMap.get(element) || 0) - 1;
			const markerValue = (markerCounter.get(element) || 0) - 1;
			counterMap.set(element, counterValue);
			markerCounter.set(element, markerValue);
			if (!counterValue) {
				if (!uncontrolledElementsSet.has(element) && controlAttribute) element.removeAttribute(controlAttribute);
				uncontrolledElementsSet.delete(element);
			}
			if (!markerValue) element.removeAttribute(markerName);
		});
		lockCount$1--;
		if (!lockCount$1) {
			counters.inert = /* @__PURE__ */ new WeakMap();
			counters["aria-hidden"] = /* @__PURE__ */ new WeakMap();
			counters.none = /* @__PURE__ */ new WeakMap();
			uncontrolledElementsSet = /* @__PURE__ */ new WeakSet();
			markerMap = {};
		}
	};
}
function markOthers(avoidElements, ariaHidden, inert) {
	if (ariaHidden === void 0) ariaHidden = false;
	if (inert === void 0) inert = false;
	const body = getDocument(avoidElements[0]).body;
	return applyAttributeToOthers(avoidElements.concat(Array.from(body.querySelectorAll("[aria-live],[role=\"status\"],output"))), body, ariaHidden, inert);
}
var HIDDEN_STYLES = {
	border: 0,
	clip: "rect(0 0 0 0)",
	height: "1px",
	margin: "-1px",
	overflow: "hidden",
	padding: 0,
	position: "fixed",
	whiteSpace: "nowrap",
	width: "1px",
	top: 0,
	left: 0
};
var FocusGuard = /*#__PURE__*/ import_react.forwardRef(function FocusGuard(props, ref) {
	const [role, setRole] = import_react.useState();
	index$1(() => {
		if (isSafari()) setRole("button");
	}, []);
	const restProps = {
		ref,
		tabIndex: 0,
		role,
		"aria-hidden": role ? void 0 : true,
		[createAttribute("focus-guard")]: "",
		style: HIDDEN_STYLES
	};
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("span", {
		...props,
		...restProps
	});
});
var HIDDEN_OWNER_STYLES = {
	clipPath: "inset(50%)",
	position: "fixed",
	top: 0,
	left: 0
};
var PortalContext = /*#__PURE__*/ import_react.createContext(null);
var attr = /*#__PURE__*/ createAttribute("portal");
/**
* @see https://floating-ui.com/docs/FloatingPortal#usefloatingportalnode
*/
function useFloatingPortalNode(props) {
	if (props === void 0) props = {};
	const { id, root } = props;
	const uniqueId = useId();
	const portalContext = usePortalContext();
	const [portalNode, setPortalNode] = import_react.useState(null);
	const portalNodeRef = import_react.useRef(null);
	index$1(() => {
		return () => {
			portalNode?.remove();
			queueMicrotask(() => {
				portalNodeRef.current = null;
			});
		};
	}, [portalNode]);
	index$1(() => {
		if (!uniqueId) return;
		if (portalNodeRef.current) return;
		const existingIdRoot = id ? document.getElementById(id) : null;
		if (!existingIdRoot) return;
		const subRoot = document.createElement("div");
		subRoot.id = uniqueId;
		subRoot.setAttribute(attr, "");
		existingIdRoot.appendChild(subRoot);
		portalNodeRef.current = subRoot;
		setPortalNode(subRoot);
	}, [id, uniqueId]);
	index$1(() => {
		if (root === null) return;
		if (!uniqueId) return;
		if (portalNodeRef.current) return;
		let container = root || (portalContext == null ? void 0 : portalContext.portalNode);
		if (container && !isNode(container)) container = container.current;
		container = container || document.body;
		let idWrapper = null;
		if (id) {
			idWrapper = document.createElement("div");
			idWrapper.id = id;
			container.appendChild(idWrapper);
		}
		const subRoot = document.createElement("div");
		subRoot.id = uniqueId;
		subRoot.setAttribute(attr, "");
		container = idWrapper || container;
		container.appendChild(subRoot);
		portalNodeRef.current = subRoot;
		setPortalNode(subRoot);
	}, [
		id,
		root,
		uniqueId,
		portalContext
	]);
	return portalNode;
}
/**
* Portals the floating element into a given container element — by default,
* outside of the app root and into the body.
* This is necessary to ensure the floating element can appear outside any
* potential parent containers that cause clipping (such as `overflow: hidden`),
* while retaining its location in the React tree.
* @see https://floating-ui.com/docs/FloatingPortal
*/
function FloatingPortal(props) {
	const { children, id, root, preserveTabOrder = true } = props;
	const portalNode = useFloatingPortalNode({
		id,
		root
	});
	const [focusManagerState, setFocusManagerState] = import_react.useState(null);
	const beforeOutsideRef = import_react.useRef(null);
	const afterOutsideRef = import_react.useRef(null);
	const beforeInsideRef = import_react.useRef(null);
	const afterInsideRef = import_react.useRef(null);
	const modal = focusManagerState == null ? void 0 : focusManagerState.modal;
	const open = focusManagerState == null ? void 0 : focusManagerState.open;
	const shouldRenderGuards = !!focusManagerState && !focusManagerState.modal && focusManagerState.open && preserveTabOrder && !!(root || portalNode);
	import_react.useEffect(() => {
		if (!portalNode || !preserveTabOrder || modal) return;
		function onFocus(event) {
			if (portalNode && isOutsideEvent(event)) (event.type === "focusin" ? enableFocusInside : disableFocusInside)(portalNode);
		}
		portalNode.addEventListener("focusin", onFocus, true);
		portalNode.addEventListener("focusout", onFocus, true);
		return () => {
			portalNode.removeEventListener("focusin", onFocus, true);
			portalNode.removeEventListener("focusout", onFocus, true);
		};
	}, [
		portalNode,
		preserveTabOrder,
		modal
	]);
	import_react.useEffect(() => {
		if (!portalNode) return;
		if (open) return;
		enableFocusInside(portalNode);
	}, [open, portalNode]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(PortalContext.Provider, {
		value: import_react.useMemo(() => ({
			preserveTabOrder,
			beforeOutsideRef,
			afterOutsideRef,
			beforeInsideRef,
			afterInsideRef,
			portalNode,
			setFocusManagerState
		}), [preserveTabOrder, portalNode]),
		children: [
			shouldRenderGuards && portalNode && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(FocusGuard, {
				"data-type": "outside",
				ref: beforeOutsideRef,
				onFocus: (event) => {
					if (isOutsideEvent(event, portalNode)) {
						var _beforeInsideRef$curr;
						(_beforeInsideRef$curr = beforeInsideRef.current) == null || _beforeInsideRef$curr.focus();
					} else getPreviousTabbable(focusManagerState ? focusManagerState.domReference : null)?.focus();
				}
			}),
			shouldRenderGuards && portalNode && /*#__PURE__*/ (0, import_jsx_runtime.jsx)("span", {
				"aria-owns": portalNode.id,
				style: HIDDEN_OWNER_STYLES
			}),
			portalNode && /*#__PURE__*/ import_react_dom.createPortal(children, portalNode),
			shouldRenderGuards && portalNode && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(FocusGuard, {
				"data-type": "outside",
				ref: afterOutsideRef,
				onFocus: (event) => {
					if (isOutsideEvent(event, portalNode)) {
						var _afterInsideRef$curre;
						(_afterInsideRef$curre = afterInsideRef.current) == null || _afterInsideRef$curre.focus();
					} else {
						getNextTabbable(focusManagerState ? focusManagerState.domReference : null)?.focus();
						focusManagerState != null && focusManagerState.closeOnFocusOut && focusManagerState?.onOpenChange(false, event.nativeEvent, "focus-out");
					}
				}
			})
		]
	});
}
var usePortalContext = () => import_react.useContext(PortalContext);
function useLiteMergeRefs(refs) {
	return import_react.useMemo(() => {
		return (value) => {
			refs.forEach((ref) => {
				if (ref) ref.current = value;
			});
		};
	}, refs);
}
var LIST_LIMIT = 20;
var previouslyFocusedElements = [];
function clearDisconnectedPreviouslyFocusedElements() {
	previouslyFocusedElements = previouslyFocusedElements.filter((elementRef) => {
		var _elementRef$deref;
		return (_elementRef$deref = elementRef.deref()) == null ? void 0 : _elementRef$deref.isConnected;
	});
}
function addPreviouslyFocusedElement(element) {
	clearDisconnectedPreviouslyFocusedElements();
	if (element && getNodeName(element) !== "body") {
		previouslyFocusedElements.push(new WeakRef(element));
		if (previouslyFocusedElements.length > LIST_LIMIT) previouslyFocusedElements = previouslyFocusedElements.slice(-20);
	}
}
function getPreviouslyFocusedElement() {
	clearDisconnectedPreviouslyFocusedElements();
	const elementRef = previouslyFocusedElements[previouslyFocusedElements.length - 1];
	return elementRef == null ? void 0 : elementRef.deref();
}
function getFirstTabbableElement(container) {
	const tabbableOptions = getTabbableOptions();
	if (isTabbable(container, tabbableOptions)) return container;
	return tabbable(container, tabbableOptions)[0] || container;
}
function handleTabIndex(floatingFocusElement, orderRef) {
	var _floatingFocusElement;
	if (!orderRef.current.includes("floating") && !((_floatingFocusElement = floatingFocusElement.getAttribute("role")) != null && _floatingFocusElement.includes("dialog"))) return;
	const options = getTabbableOptions();
	const tabbableContent = focusable(floatingFocusElement, options).filter((element) => {
		const dataTabIndex = element.getAttribute("data-tabindex") || "";
		return isTabbable(element, options) || element.hasAttribute("data-tabindex") && !dataTabIndex.startsWith("-");
	});
	const tabIndex = floatingFocusElement.getAttribute("tabindex");
	if (orderRef.current.includes("floating") || tabbableContent.length === 0) {
		if (tabIndex !== "0") floatingFocusElement.setAttribute("tabindex", "0");
	} else if (tabIndex !== "-1" || floatingFocusElement.hasAttribute("data-tabindex") && floatingFocusElement.getAttribute("data-tabindex") !== "-1") {
		floatingFocusElement.setAttribute("tabindex", "-1");
		floatingFocusElement.setAttribute("data-tabindex", "-1");
	}
}
var VisuallyHiddenDismiss = /*#__PURE__*/ import_react.forwardRef(function VisuallyHiddenDismiss(props, ref) {
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("button", {
		...props,
		type: "button",
		ref,
		tabIndex: -1,
		style: HIDDEN_STYLES
	});
});
/**
* Provides focus management for the floating element.
* @see https://floating-ui.com/docs/FloatingFocusManager
*/
function FloatingFocusManager(props) {
	const { context, children, disabled = false, order = ["content"], guards: _guards = true, initialFocus = 0, returnFocus = true, restoreFocus = false, modal = true, visuallyHiddenDismiss = false, closeOnFocusOut = true, outsideElementsInert = false, getInsideElements: _getInsideElements = () => [] } = props;
	const { open, onOpenChange, events, dataRef, elements: { domReference, floating } } = context;
	const getNodeId = useEffectEvent(() => {
		var _dataRef$current$floa;
		return (_dataRef$current$floa = dataRef.current.floatingContext) == null ? void 0 : _dataRef$current$floa.nodeId;
	});
	const getInsideElements = useEffectEvent(_getInsideElements);
	const ignoreInitialFocus = typeof initialFocus === "number" && initialFocus < 0;
	const isUntrappedTypeableCombobox = isTypeableCombobox(domReference) && ignoreInitialFocus;
	const inertSupported = supportsInert();
	const guards = inertSupported ? _guards : true;
	const useInert = !guards || inertSupported && outsideElementsInert;
	const orderRef = useLatestRef$1(order);
	const initialFocusRef = useLatestRef$1(initialFocus);
	const returnFocusRef = useLatestRef$1(returnFocus);
	const tree = useFloatingTree();
	const portalContext = usePortalContext();
	const startDismissButtonRef = import_react.useRef(null);
	const endDismissButtonRef = import_react.useRef(null);
	const preventReturnFocusRef = import_react.useRef(false);
	const isPointerDownRef = import_react.useRef(false);
	const tabbableIndexRef = import_react.useRef(-1);
	const blurTimeoutRef = import_react.useRef(-1);
	const isInsidePortal = portalContext != null;
	const floatingFocusElement = getFloatingFocusElement(floating);
	const getTabbableContent = useEffectEvent(function(container) {
		if (container === void 0) container = floatingFocusElement;
		return container ? tabbable(container, getTabbableOptions()) : [];
	});
	const getTabbableElements = useEffectEvent((container) => {
		const content = getTabbableContent(container);
		return orderRef.current.map((type) => {
			if (domReference && type === "reference") return domReference;
			if (floatingFocusElement && type === "floating") return floatingFocusElement;
			return content;
		}).filter(Boolean).flat();
	});
	import_react.useEffect(() => {
		if (disabled) return;
		if (!modal) return;
		function onKeyDown(event) {
			if (event.key === "Tab") {
				if (contains$1(floatingFocusElement, activeElement(getDocument$1(floatingFocusElement))) && getTabbableContent().length === 0 && !isUntrappedTypeableCombobox) stopEvent(event);
				const els = getTabbableElements();
				const target = getTarget$2(event);
				if (orderRef.current[0] === "reference" && target === domReference) {
					stopEvent(event);
					if (event.shiftKey) enqueueFocus(els[els.length - 1]);
					else enqueueFocus(els[1]);
				}
				if (orderRef.current[1] === "floating" && target === floatingFocusElement && event.shiftKey) {
					stopEvent(event);
					enqueueFocus(els[0]);
				}
			}
		}
		const doc = getDocument$1(floatingFocusElement);
		doc.addEventListener("keydown", onKeyDown);
		return () => {
			doc.removeEventListener("keydown", onKeyDown);
		};
	}, [
		disabled,
		domReference,
		floatingFocusElement,
		modal,
		orderRef,
		isUntrappedTypeableCombobox,
		getTabbableContent,
		getTabbableElements
	]);
	import_react.useEffect(() => {
		if (disabled) return;
		if (!floating) return;
		function handleFocusIn(event) {
			const target = getTarget$2(event);
			const tabbableIndex = getTabbableContent().indexOf(target);
			if (tabbableIndex !== -1) tabbableIndexRef.current = tabbableIndex;
		}
		floating.addEventListener("focusin", handleFocusIn);
		return () => {
			floating.removeEventListener("focusin", handleFocusIn);
		};
	}, [
		disabled,
		floating,
		getTabbableContent
	]);
	import_react.useEffect(() => {
		if (disabled) return;
		if (!closeOnFocusOut) return;
		function handlePointerDown() {
			isPointerDownRef.current = true;
			setTimeout(() => {
				isPointerDownRef.current = false;
			});
		}
		function handleFocusOutside(event) {
			const relatedTarget = event.relatedTarget;
			const currentTarget = event.currentTarget;
			const target = getTarget$2(event);
			queueMicrotask(() => {
				const nodeId = getNodeId();
				const movedToUnrelatedNode = !(contains$1(domReference, relatedTarget) || contains$1(floating, relatedTarget) || contains$1(relatedTarget, floating) || contains$1(portalContext == null ? void 0 : portalContext.portalNode, relatedTarget) || relatedTarget != null && relatedTarget.hasAttribute(createAttribute("focus-guard")) || tree && (getNodeChildren$1(tree.nodesRef.current, nodeId).find((node) => {
					var _node$context, _node$context2;
					return contains$1((_node$context = node.context) == null ? void 0 : _node$context.elements.floating, relatedTarget) || contains$1((_node$context2 = node.context) == null ? void 0 : _node$context2.elements.domReference, relatedTarget);
				}) || getNodeAncestors(tree.nodesRef.current, nodeId).find((node) => {
					var _node$context3, _node$context4, _node$context5;
					return [(_node$context3 = node.context) == null ? void 0 : _node$context3.elements.floating, getFloatingFocusElement((_node$context4 = node.context) == null ? void 0 : _node$context4.elements.floating)].includes(relatedTarget) || ((_node$context5 = node.context) == null ? void 0 : _node$context5.elements.domReference) === relatedTarget;
				})));
				if (currentTarget === domReference && floatingFocusElement) handleTabIndex(floatingFocusElement, orderRef);
				if (restoreFocus && currentTarget !== domReference && !(target != null && target.isConnected) && activeElement(getDocument$1(floatingFocusElement)) === getDocument$1(floatingFocusElement).body) {
					if (isHTMLElement(floatingFocusElement)) floatingFocusElement.focus();
					const prevTabbableIndex = tabbableIndexRef.current;
					const tabbableContent = getTabbableContent();
					const nodeToFocus = tabbableContent[prevTabbableIndex] || tabbableContent[tabbableContent.length - 1] || floatingFocusElement;
					if (isHTMLElement(nodeToFocus)) nodeToFocus.focus();
				}
				if (dataRef.current.insideReactTree) {
					dataRef.current.insideReactTree = false;
					return;
				}
				if ((isUntrappedTypeableCombobox ? true : !modal) && relatedTarget && movedToUnrelatedNode && !isPointerDownRef.current && relatedTarget !== getPreviouslyFocusedElement()) {
					preventReturnFocusRef.current = true;
					onOpenChange(false, event, "focus-out");
				}
			});
		}
		const shouldHandleBlurCapture = Boolean(!tree && portalContext);
		function markInsideReactTree() {
			clearTimeoutIfSet(blurTimeoutRef);
			dataRef.current.insideReactTree = true;
			blurTimeoutRef.current = window.setTimeout(() => {
				dataRef.current.insideReactTree = false;
			});
		}
		if (floating && isHTMLElement(domReference)) {
			domReference.addEventListener("focusout", handleFocusOutside);
			domReference.addEventListener("pointerdown", handlePointerDown);
			floating.addEventListener("focusout", handleFocusOutside);
			if (shouldHandleBlurCapture) floating.addEventListener("focusout", markInsideReactTree, true);
			return () => {
				domReference.removeEventListener("focusout", handleFocusOutside);
				domReference.removeEventListener("pointerdown", handlePointerDown);
				floating.removeEventListener("focusout", handleFocusOutside);
				if (shouldHandleBlurCapture) floating.removeEventListener("focusout", markInsideReactTree, true);
			};
		}
	}, [
		disabled,
		domReference,
		floating,
		floatingFocusElement,
		modal,
		tree,
		portalContext,
		onOpenChange,
		closeOnFocusOut,
		restoreFocus,
		getTabbableContent,
		isUntrappedTypeableCombobox,
		getNodeId,
		orderRef,
		dataRef
	]);
	const beforeGuardRef = import_react.useRef(null);
	const afterGuardRef = import_react.useRef(null);
	const mergedBeforeGuardRef = useLiteMergeRefs([beforeGuardRef, portalContext == null ? void 0 : portalContext.beforeInsideRef]);
	const mergedAfterGuardRef = useLiteMergeRefs([afterGuardRef, portalContext == null ? void 0 : portalContext.afterInsideRef]);
	import_react.useEffect(() => {
		var _portalContext$portal, _ancestors$find;
		if (disabled) return;
		if (!floating) return;
		const portalNodes = Array.from((portalContext == null || (_portalContext$portal = portalContext.portalNode) == null ? void 0 : _portalContext$portal.querySelectorAll("[" + createAttribute("portal") + "]")) || []);
		const rootAncestorComboboxDomReference = (_ancestors$find = (tree ? getNodeAncestors(tree.nodesRef.current, getNodeId()) : []).find((node) => {
			var _node$context6;
			return isTypeableCombobox(((_node$context6 = node.context) == null ? void 0 : _node$context6.elements.domReference) || null);
		})) == null || (_ancestors$find = _ancestors$find.context) == null ? void 0 : _ancestors$find.elements.domReference;
		const insideElements = [
			floating,
			rootAncestorComboboxDomReference,
			...portalNodes,
			...getInsideElements(),
			startDismissButtonRef.current,
			endDismissButtonRef.current,
			beforeGuardRef.current,
			afterGuardRef.current,
			portalContext == null ? void 0 : portalContext.beforeOutsideRef.current,
			portalContext == null ? void 0 : portalContext.afterOutsideRef.current,
			orderRef.current.includes("reference") || isUntrappedTypeableCombobox ? domReference : null
		].filter((x) => x != null);
		const cleanup = modal || isUntrappedTypeableCombobox ? markOthers(insideElements, !useInert, useInert) : markOthers(insideElements);
		return () => {
			cleanup();
		};
	}, [
		disabled,
		domReference,
		floating,
		modal,
		orderRef,
		portalContext,
		isUntrappedTypeableCombobox,
		guards,
		useInert,
		tree,
		getNodeId,
		getInsideElements
	]);
	index$1(() => {
		if (disabled || !isHTMLElement(floatingFocusElement)) return;
		const previouslyFocusedElement = activeElement(getDocument$1(floatingFocusElement));
		queueMicrotask(() => {
			const focusableElements = getTabbableElements(floatingFocusElement);
			const initialFocusValue = initialFocusRef.current;
			const elToFocus = (typeof initialFocusValue === "number" ? focusableElements[initialFocusValue] : initialFocusValue.current) || floatingFocusElement;
			const focusAlreadyInsideFloatingEl = contains$1(floatingFocusElement, previouslyFocusedElement);
			if (!ignoreInitialFocus && !focusAlreadyInsideFloatingEl && open) enqueueFocus(elToFocus, { preventScroll: elToFocus === floatingFocusElement });
		});
	}, [
		disabled,
		open,
		floatingFocusElement,
		ignoreInitialFocus,
		getTabbableElements,
		initialFocusRef
	]);
	index$1(() => {
		if (disabled || !floatingFocusElement) return;
		const doc = getDocument$1(floatingFocusElement);
		addPreviouslyFocusedElement(activeElement(doc));
		function onOpenChange(_ref) {
			let { reason, event, nested } = _ref;
			if (["hover", "safe-polygon"].includes(reason) && event.type === "mouseleave") preventReturnFocusRef.current = true;
			if (reason !== "outside-press") return;
			if (nested) preventReturnFocusRef.current = false;
			else if (isVirtualClick(event) || isVirtualPointerEvent(event)) preventReturnFocusRef.current = false;
			else {
				let isPreventScrollSupported = false;
				document.createElement("div").focus({ get preventScroll() {
					isPreventScrollSupported = true;
					return false;
				} });
				if (isPreventScrollSupported) preventReturnFocusRef.current = false;
				else preventReturnFocusRef.current = true;
			}
		}
		events.on("openchange", onOpenChange);
		const fallbackEl = doc.createElement("span");
		fallbackEl.setAttribute("tabindex", "-1");
		fallbackEl.setAttribute("aria-hidden", "true");
		Object.assign(fallbackEl.style, HIDDEN_STYLES);
		if (isInsidePortal && domReference) domReference.insertAdjacentElement("afterend", fallbackEl);
		function getReturnElement() {
			if (typeof returnFocusRef.current === "boolean") {
				const el = domReference || getPreviouslyFocusedElement();
				return el && el.isConnected ? el : fallbackEl;
			}
			return returnFocusRef.current.current || fallbackEl;
		}
		return () => {
			events.off("openchange", onOpenChange);
			const activeEl = activeElement(doc);
			const isFocusInsideFloatingTree = contains$1(floating, activeEl) || tree && getNodeChildren$1(tree.nodesRef.current, getNodeId(), false).some((node) => {
				var _node$context7;
				return contains$1((_node$context7 = node.context) == null ? void 0 : _node$context7.elements.floating, activeEl);
			});
			const returnElement = getReturnElement();
			queueMicrotask(() => {
				const tabbableReturnElement = getFirstTabbableElement(returnElement);
				if (returnFocusRef.current && !preventReturnFocusRef.current && isHTMLElement(tabbableReturnElement) && (tabbableReturnElement !== activeEl && activeEl !== doc.body ? isFocusInsideFloatingTree : true)) tabbableReturnElement.focus({ preventScroll: true });
				fallbackEl.remove();
			});
		};
	}, [
		disabled,
		floating,
		floatingFocusElement,
		returnFocusRef,
		dataRef,
		events,
		tree,
		isInsidePortal,
		domReference,
		getNodeId
	]);
	import_react.useEffect(() => {
		queueMicrotask(() => {
			preventReturnFocusRef.current = false;
		});
		return () => {
			queueMicrotask(clearDisconnectedPreviouslyFocusedElements);
		};
	}, [disabled]);
	index$1(() => {
		if (disabled) return;
		if (!portalContext) return;
		portalContext.setFocusManagerState({
			modal,
			closeOnFocusOut,
			open,
			onOpenChange,
			domReference
		});
		return () => {
			portalContext.setFocusManagerState(null);
		};
	}, [
		disabled,
		portalContext,
		modal,
		open,
		onOpenChange,
		closeOnFocusOut,
		domReference
	]);
	index$1(() => {
		if (disabled) return;
		if (!floatingFocusElement) return;
		handleTabIndex(floatingFocusElement, orderRef);
	}, [
		disabled,
		floatingFocusElement,
		orderRef
	]);
	function renderDismissButton(location) {
		if (disabled || !visuallyHiddenDismiss || !modal) return null;
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(VisuallyHiddenDismiss, {
			ref: location === "start" ? startDismissButtonRef : endDismissButtonRef,
			onClick: (event) => onOpenChange(false, event.nativeEvent),
			children: typeof visuallyHiddenDismiss === "string" ? visuallyHiddenDismiss : "Dismiss"
		});
	}
	const shouldRenderGuards = !disabled && guards && (modal ? !isUntrappedTypeableCombobox : true) && (isInsidePortal || modal);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		shouldRenderGuards && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(FocusGuard, {
			"data-type": "inside",
			ref: mergedBeforeGuardRef,
			onFocus: (event) => {
				if (modal) {
					const els = getTabbableElements();
					enqueueFocus(order[0] === "reference" ? els[0] : els[els.length - 1]);
				} else if (portalContext != null && portalContext.preserveTabOrder && portalContext.portalNode) {
					preventReturnFocusRef.current = false;
					if (isOutsideEvent(event, portalContext.portalNode)) getNextTabbable(domReference)?.focus();
					else {
						var _portalContext$before;
						(_portalContext$before = portalContext.beforeOutsideRef.current) == null || _portalContext$before.focus();
					}
				}
			}
		}),
		!isUntrappedTypeableCombobox && renderDismissButton("start"),
		children,
		renderDismissButton("end"),
		shouldRenderGuards && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(FocusGuard, {
			"data-type": "inside",
			ref: mergedAfterGuardRef,
			onFocus: (event) => {
				if (modal) enqueueFocus(getTabbableElements()[0]);
				else if (portalContext != null && portalContext.preserveTabOrder && portalContext.portalNode) {
					if (closeOnFocusOut) preventReturnFocusRef.current = true;
					if (isOutsideEvent(event, portalContext.portalNode)) getPreviousTabbable(domReference)?.focus();
					else {
						var _portalContext$afterO;
						(_portalContext$afterO = portalContext.afterOutsideRef.current) == null || _portalContext$afterO.focus();
					}
				}
			}
		})
	] });
}
var lockCount = 0;
var scrollbarProperty = "--floating-ui-scrollbar-width";
function enableScrollLock() {
	const platform = getPlatform();
	const isIOS = /iP(hone|ad|od)|iOS/.test(platform) || platform === "MacIntel" && navigator.maxTouchPoints > 1;
	const bodyStyle = document.body.style;
	const paddingProp = Math.round(document.documentElement.getBoundingClientRect().left) + document.documentElement.scrollLeft ? "paddingLeft" : "paddingRight";
	const scrollbarWidth = window.innerWidth - document.documentElement.clientWidth;
	const scrollX = bodyStyle.left ? parseFloat(bodyStyle.left) : window.scrollX;
	const scrollY = bodyStyle.top ? parseFloat(bodyStyle.top) : window.scrollY;
	bodyStyle.overflow = "hidden";
	bodyStyle.setProperty(scrollbarProperty, scrollbarWidth + "px");
	if (scrollbarWidth) bodyStyle[paddingProp] = scrollbarWidth + "px";
	if (isIOS) {
		var _window$visualViewpor, _window$visualViewpor2;
		const offsetLeft = ((_window$visualViewpor = window.visualViewport) == null ? void 0 : _window$visualViewpor.offsetLeft) || 0;
		const offsetTop = ((_window$visualViewpor2 = window.visualViewport) == null ? void 0 : _window$visualViewpor2.offsetTop) || 0;
		Object.assign(bodyStyle, {
			position: "fixed",
			top: -(scrollY - Math.floor(offsetTop)) + "px",
			left: -(scrollX - Math.floor(offsetLeft)) + "px",
			right: "0"
		});
	}
	return () => {
		Object.assign(bodyStyle, {
			overflow: "",
			[paddingProp]: ""
		});
		bodyStyle.removeProperty(scrollbarProperty);
		if (isIOS) {
			Object.assign(bodyStyle, {
				position: "",
				top: "",
				left: "",
				right: ""
			});
			window.scrollTo(scrollX, scrollY);
		}
	};
}
var cleanup = () => {};
/**
* Provides base styling for a fixed overlay element to dim content or block
* pointer events behind a floating element.
* It's a regular `<div>`, so it can be styled via any CSS solution you prefer.
* @see https://floating-ui.com/docs/FloatingOverlay
*/
var FloatingOverlay = /*#__PURE__*/ import_react.forwardRef(function FloatingOverlay(props, ref) {
	const { lockScroll = false, ...rest } = props;
	index$1(() => {
		if (!lockScroll) return;
		lockCount++;
		if (lockCount === 1) cleanup = enableScrollLock();
		return () => {
			lockCount--;
			if (lockCount === 0) cleanup();
		};
	}, [lockScroll]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("div", {
		ref,
		...rest,
		style: {
			position: "fixed",
			overflow: "auto",
			top: 0,
			right: 0,
			bottom: 0,
			left: 0,
			...rest.style
		}
	});
});
function isButtonTarget(event) {
	return isHTMLElement(event.target) && event.target.tagName === "BUTTON";
}
function isAnchorTarget(event) {
	return isHTMLElement(event.target) && event.target.tagName === "A";
}
function isSpaceIgnored(element) {
	return isTypeableElement(element);
}
/**
* Opens or closes the floating element when clicking the reference element.
* @see https://floating-ui.com/docs/useClick
*/
function useClick(context, props) {
	if (props === void 0) props = {};
	const { open, onOpenChange, dataRef, elements: { domReference } } = context;
	const { enabled = true, event: eventOption = "click", toggle = true, ignoreMouse = false, keyboardHandlers = true, stickIfOpen = true } = props;
	const pointerTypeRef = import_react.useRef();
	const didKeyDownRef = import_react.useRef(false);
	const reference = import_react.useMemo(() => ({
		onPointerDown(event) {
			pointerTypeRef.current = event.pointerType;
		},
		onMouseDown(event) {
			const pointerType = pointerTypeRef.current;
			if (event.button !== 0) return;
			if (eventOption === "click") return;
			if (isMouseLikePointerType(pointerType, true) && ignoreMouse) return;
			if (open && toggle && (dataRef.current.openEvent && stickIfOpen ? dataRef.current.openEvent.type === "mousedown" : true)) onOpenChange(false, event.nativeEvent, "click");
			else {
				event.preventDefault();
				onOpenChange(true, event.nativeEvent, "click");
			}
		},
		onClick(event) {
			const pointerType = pointerTypeRef.current;
			if (eventOption === "mousedown" && pointerTypeRef.current) {
				pointerTypeRef.current = void 0;
				return;
			}
			if (isMouseLikePointerType(pointerType, true) && ignoreMouse) return;
			if (open && toggle && (dataRef.current.openEvent && stickIfOpen ? dataRef.current.openEvent.type === "click" : true)) onOpenChange(false, event.nativeEvent, "click");
			else onOpenChange(true, event.nativeEvent, "click");
		},
		onKeyDown(event) {
			pointerTypeRef.current = void 0;
			if (event.defaultPrevented || !keyboardHandlers || isButtonTarget(event)) return;
			if (event.key === " " && !isSpaceIgnored(domReference)) {
				event.preventDefault();
				didKeyDownRef.current = true;
			}
			if (isAnchorTarget(event)) return;
			if (event.key === "Enter") if (open && toggle) onOpenChange(false, event.nativeEvent, "click");
			else onOpenChange(true, event.nativeEvent, "click");
		},
		onKeyUp(event) {
			if (event.defaultPrevented || !keyboardHandlers || isButtonTarget(event) || isSpaceIgnored(domReference)) return;
			if (event.key === " " && didKeyDownRef.current) {
				didKeyDownRef.current = false;
				if (open && toggle) onOpenChange(false, event.nativeEvent, "click");
				else onOpenChange(true, event.nativeEvent, "click");
			}
		}
	}), [
		dataRef,
		domReference,
		eventOption,
		ignoreMouse,
		keyboardHandlers,
		onOpenChange,
		open,
		stickIfOpen,
		toggle
	]);
	return import_react.useMemo(() => enabled ? { reference } : {}, [enabled, reference]);
}
var bubbleHandlerKeys = {
	pointerdown: "onPointerDown",
	mousedown: "onMouseDown",
	click: "onClick"
};
var captureHandlerKeys = {
	pointerdown: "onPointerDownCapture",
	mousedown: "onMouseDownCapture",
	click: "onClickCapture"
};
var normalizeProp = (normalizable) => {
	var _normalizable$escapeK, _normalizable$outside;
	return {
		escapeKey: typeof normalizable === "boolean" ? normalizable : (_normalizable$escapeK = normalizable == null ? void 0 : normalizable.escapeKey) != null ? _normalizable$escapeK : false,
		outsidePress: typeof normalizable === "boolean" ? normalizable : (_normalizable$outside = normalizable == null ? void 0 : normalizable.outsidePress) != null ? _normalizable$outside : true
	};
};
/**
* Closes the floating element when a dismissal is requested — by default, when
* the user presses the `escape` key or outside of the floating element.
* @see https://floating-ui.com/docs/useDismiss
*/
function useDismiss(context, props) {
	if (props === void 0) props = {};
	const { open, onOpenChange, elements, dataRef } = context;
	const { enabled = true, escapeKey = true, outsidePress: unstable_outsidePress = true, outsidePressEvent = "pointerdown", referencePress = false, referencePressEvent = "pointerdown", ancestorScroll = false, bubbles, capture } = props;
	const tree = useFloatingTree();
	const outsidePressFn = useEffectEvent(typeof unstable_outsidePress === "function" ? unstable_outsidePress : () => false);
	const outsidePress = typeof unstable_outsidePress === "function" ? outsidePressFn : unstable_outsidePress;
	const endedOrStartedInsideRef = import_react.useRef(false);
	const { escapeKey: escapeKeyBubbles, outsidePress: outsidePressBubbles } = normalizeProp(bubbles);
	const { escapeKey: escapeKeyCapture, outsidePress: outsidePressCapture } = normalizeProp(capture);
	const isComposingRef = import_react.useRef(false);
	const closeOnEscapeKeyDown = useEffectEvent((event) => {
		var _dataRef$current$floa;
		if (!open || !enabled || !escapeKey || event.key !== "Escape") return;
		if (isComposingRef.current) return;
		const nodeId = (_dataRef$current$floa = dataRef.current.floatingContext) == null ? void 0 : _dataRef$current$floa.nodeId;
		const children = tree ? getNodeChildren$1(tree.nodesRef.current, nodeId) : [];
		if (!escapeKeyBubbles) {
			event.stopPropagation();
			if (children.length > 0) {
				let shouldDismiss = true;
				children.forEach((child) => {
					var _child$context;
					if ((_child$context = child.context) != null && _child$context.open && !child.context.dataRef.current.__escapeKeyBubbles) {
						shouldDismiss = false;
						return;
					}
				});
				if (!shouldDismiss) return;
			}
		}
		onOpenChange(false, isReactEvent(event) ? event.nativeEvent : event, "escape-key");
	});
	const closeOnEscapeKeyDownCapture = useEffectEvent((event) => {
		var _getTarget2;
		const callback = () => {
			var _getTarget;
			closeOnEscapeKeyDown(event);
			(_getTarget = getTarget$2(event)) == null || _getTarget.removeEventListener("keydown", callback);
		};
		(_getTarget2 = getTarget$2(event)) == null || _getTarget2.addEventListener("keydown", callback);
	});
	const closeOnPressOutside = useEffectEvent((event) => {
		var _dataRef$current$floa2;
		const insideReactTree = dataRef.current.insideReactTree;
		dataRef.current.insideReactTree = false;
		const endedOrStartedInside = endedOrStartedInsideRef.current;
		endedOrStartedInsideRef.current = false;
		if (outsidePressEvent === "click" && endedOrStartedInside) return;
		if (insideReactTree) return;
		if (typeof outsidePress === "function" && !outsidePress(event)) return;
		const target = getTarget$2(event);
		const inertSelector = "[" + createAttribute("inert") + "]";
		const markers = getDocument$1(elements.floating).querySelectorAll(inertSelector);
		let targetRootAncestor = isElement$2(target) ? target : null;
		while (targetRootAncestor && !isLastTraversableNode(targetRootAncestor)) {
			const nextParent = getParentNode(targetRootAncestor);
			if (isLastTraversableNode(nextParent) || !isElement$2(nextParent)) break;
			targetRootAncestor = nextParent;
		}
		if (markers.length && isElement$2(target) && !isRootElement(target) && !contains$1(target, elements.floating) && Array.from(markers).every((marker) => !contains$1(targetRootAncestor, marker))) return;
		if (isHTMLElement(target) && floating) {
			const lastTraversableNode = isLastTraversableNode(target);
			const style = getComputedStyle$1(target);
			const scrollRe = /auto|scroll/;
			const isScrollableX = lastTraversableNode || scrollRe.test(style.overflowX);
			const isScrollableY = lastTraversableNode || scrollRe.test(style.overflowY);
			const canScrollX = isScrollableX && target.clientWidth > 0 && target.scrollWidth > target.clientWidth;
			const canScrollY = isScrollableY && target.clientHeight > 0 && target.scrollHeight > target.clientHeight;
			const isRTL = style.direction === "rtl";
			const pressedVerticalScrollbar = canScrollY && (isRTL ? event.offsetX <= target.offsetWidth - target.clientWidth : event.offsetX > target.clientWidth);
			const pressedHorizontalScrollbar = canScrollX && event.offsetY > target.clientHeight;
			if (pressedVerticalScrollbar || pressedHorizontalScrollbar) return;
		}
		const nodeId = (_dataRef$current$floa2 = dataRef.current.floatingContext) == null ? void 0 : _dataRef$current$floa2.nodeId;
		const targetIsInsideChildren = tree && getNodeChildren$1(tree.nodesRef.current, nodeId).some((node) => {
			var _node$context;
			return isEventTargetWithin(event, (_node$context = node.context) == null ? void 0 : _node$context.elements.floating);
		});
		if (isEventTargetWithin(event, elements.floating) || isEventTargetWithin(event, elements.domReference) || targetIsInsideChildren) return;
		const children = tree ? getNodeChildren$1(tree.nodesRef.current, nodeId) : [];
		if (children.length > 0) {
			let shouldDismiss = true;
			children.forEach((child) => {
				var _child$context2;
				if ((_child$context2 = child.context) != null && _child$context2.open && !child.context.dataRef.current.__outsidePressBubbles) {
					shouldDismiss = false;
					return;
				}
			});
			if (!shouldDismiss) return;
		}
		onOpenChange(false, event, "outside-press");
	});
	const closeOnPressOutsideCapture = useEffectEvent((event) => {
		var _getTarget4;
		const callback = () => {
			var _getTarget3;
			closeOnPressOutside(event);
			(_getTarget3 = getTarget$2(event)) == null || _getTarget3.removeEventListener(outsidePressEvent, callback);
		};
		(_getTarget4 = getTarget$2(event)) == null || _getTarget4.addEventListener(outsidePressEvent, callback);
	});
	import_react.useEffect(() => {
		if (!open || !enabled) return;
		dataRef.current.__escapeKeyBubbles = escapeKeyBubbles;
		dataRef.current.__outsidePressBubbles = outsidePressBubbles;
		let compositionTimeout = -1;
		function onScroll(event) {
			onOpenChange(false, event, "ancestor-scroll");
		}
		function handleCompositionStart() {
			window.clearTimeout(compositionTimeout);
			isComposingRef.current = true;
		}
		function handleCompositionEnd() {
			compositionTimeout = window.setTimeout(() => {
				isComposingRef.current = false;
			}, isWebKit() ? 5 : 0);
		}
		const doc = getDocument$1(elements.floating);
		if (escapeKey) {
			doc.addEventListener("keydown", escapeKeyCapture ? closeOnEscapeKeyDownCapture : closeOnEscapeKeyDown, escapeKeyCapture);
			doc.addEventListener("compositionstart", handleCompositionStart);
			doc.addEventListener("compositionend", handleCompositionEnd);
		}
		outsidePress && doc.addEventListener(outsidePressEvent, outsidePressCapture ? closeOnPressOutsideCapture : closeOnPressOutside, outsidePressCapture);
		let ancestors = [];
		if (ancestorScroll) {
			if (isElement$2(elements.domReference)) ancestors = getOverflowAncestors(elements.domReference);
			if (isElement$2(elements.floating)) ancestors = ancestors.concat(getOverflowAncestors(elements.floating));
			if (!isElement$2(elements.reference) && elements.reference && elements.reference.contextElement) ancestors = ancestors.concat(getOverflowAncestors(elements.reference.contextElement));
		}
		ancestors = ancestors.filter((ancestor) => {
			var _doc$defaultView;
			return ancestor !== ((_doc$defaultView = doc.defaultView) == null ? void 0 : _doc$defaultView.visualViewport);
		});
		ancestors.forEach((ancestor) => {
			ancestor.addEventListener("scroll", onScroll, { passive: true });
		});
		return () => {
			if (escapeKey) {
				doc.removeEventListener("keydown", escapeKeyCapture ? closeOnEscapeKeyDownCapture : closeOnEscapeKeyDown, escapeKeyCapture);
				doc.removeEventListener("compositionstart", handleCompositionStart);
				doc.removeEventListener("compositionend", handleCompositionEnd);
			}
			outsidePress && doc.removeEventListener(outsidePressEvent, outsidePressCapture ? closeOnPressOutsideCapture : closeOnPressOutside, outsidePressCapture);
			ancestors.forEach((ancestor) => {
				ancestor.removeEventListener("scroll", onScroll);
			});
			window.clearTimeout(compositionTimeout);
		};
	}, [
		dataRef,
		elements,
		escapeKey,
		outsidePress,
		outsidePressEvent,
		open,
		onOpenChange,
		ancestorScroll,
		enabled,
		escapeKeyBubbles,
		outsidePressBubbles,
		closeOnEscapeKeyDown,
		escapeKeyCapture,
		closeOnEscapeKeyDownCapture,
		closeOnPressOutside,
		outsidePressCapture,
		closeOnPressOutsideCapture
	]);
	import_react.useEffect(() => {
		dataRef.current.insideReactTree = false;
	}, [
		dataRef,
		outsidePress,
		outsidePressEvent
	]);
	const reference = import_react.useMemo(() => ({
		onKeyDown: closeOnEscapeKeyDown,
		...referencePress && {
			[bubbleHandlerKeys[referencePressEvent]]: (event) => {
				onOpenChange(false, event.nativeEvent, "reference-press");
			},
			...referencePressEvent !== "click" && { onClick(event) {
				onOpenChange(false, event.nativeEvent, "reference-press");
			} }
		}
	}), [
		closeOnEscapeKeyDown,
		onOpenChange,
		referencePress,
		referencePressEvent
	]);
	const floating = import_react.useMemo(() => {
		function setMouseDownOrUpInside(event) {
			if (event.button !== 0) return;
			endedOrStartedInsideRef.current = true;
		}
		return {
			onKeyDown: closeOnEscapeKeyDown,
			onMouseDown: setMouseDownOrUpInside,
			onMouseUp: setMouseDownOrUpInside,
			[captureHandlerKeys[outsidePressEvent]]: () => {
				dataRef.current.insideReactTree = true;
			}
		};
	}, [
		closeOnEscapeKeyDown,
		outsidePressEvent,
		dataRef
	]);
	return import_react.useMemo(() => enabled ? {
		reference,
		floating
	} : {}, [
		enabled,
		reference,
		floating
	]);
}
function useFloatingRootContext(options) {
	const { open = false, onOpenChange: onOpenChangeProp, elements: elementsProp } = options;
	const floatingId = useId();
	const dataRef = import_react.useRef({});
	const [events] = import_react.useState(() => createEventEmitter());
	const nested = useFloatingParentNodeId() != null;
	{
		const optionDomReference = elementsProp.reference;
		if (optionDomReference && !isElement$2(optionDomReference)) error$1("Cannot pass a virtual element to the `elements.reference` option,", "as it must be a real DOM element. Use `refs.setPositionReference()`", "instead.");
	}
	const [positionReference, setPositionReference] = import_react.useState(elementsProp.reference);
	const onOpenChange = useEffectEvent((open, event, reason) => {
		dataRef.current.openEvent = open ? event : void 0;
		events.emit("openchange", {
			open,
			event,
			reason,
			nested
		});
		onOpenChangeProp?.(open, event, reason);
	});
	const refs = import_react.useMemo(() => ({ setPositionReference }), []);
	const elements = import_react.useMemo(() => ({
		reference: positionReference || elementsProp.reference || null,
		floating: elementsProp.floating || null,
		domReference: elementsProp.reference
	}), [
		positionReference,
		elementsProp.reference,
		elementsProp.floating
	]);
	return import_react.useMemo(() => ({
		dataRef,
		open,
		onOpenChange,
		elements,
		events,
		floatingId,
		refs
	}), [
		open,
		onOpenChange,
		elements,
		events,
		floatingId,
		refs
	]);
}
/**
* Provides data to position a floating element and context to add interactions.
* @see https://floating-ui.com/docs/useFloating
*/
function useFloating(options) {
	if (options === void 0) options = {};
	const { nodeId } = options;
	const internalRootContext = useFloatingRootContext({
		...options,
		elements: {
			reference: null,
			floating: null,
			...options.elements
		}
	});
	const rootContext = options.rootContext || internalRootContext;
	const computedElements = rootContext.elements;
	const [_domReference, setDomReference] = import_react.useState(null);
	const [positionReference, _setPositionReference] = import_react.useState(null);
	const domReference = (computedElements == null ? void 0 : computedElements.domReference) || _domReference;
	const domReferenceRef = import_react.useRef(null);
	const tree = useFloatingTree();
	index$1(() => {
		if (domReference) domReferenceRef.current = domReference;
	}, [domReference]);
	const position = useFloating$1({
		...options,
		elements: {
			...computedElements,
			...positionReference && { reference: positionReference }
		}
	});
	const setPositionReference = import_react.useCallback((node) => {
		const computedPositionReference = isElement$2(node) ? {
			getBoundingClientRect: () => node.getBoundingClientRect(),
			getClientRects: () => node.getClientRects(),
			contextElement: node
		} : node;
		_setPositionReference(computedPositionReference);
		position.refs.setReference(computedPositionReference);
	}, [position.refs]);
	const setReference = import_react.useCallback((node) => {
		if (isElement$2(node) || node === null) {
			domReferenceRef.current = node;
			setDomReference(node);
		}
		if (isElement$2(position.refs.reference.current) || position.refs.reference.current === null || node !== null && !isElement$2(node)) position.refs.setReference(node);
	}, [position.refs]);
	const refs = import_react.useMemo(() => ({
		...position.refs,
		setReference,
		setPositionReference,
		domReference: domReferenceRef
	}), [
		position.refs,
		setReference,
		setPositionReference
	]);
	const elements = import_react.useMemo(() => ({
		...position.elements,
		domReference
	}), [position.elements, domReference]);
	const context = import_react.useMemo(() => ({
		...position,
		...rootContext,
		refs,
		elements,
		nodeId
	}), [
		position,
		refs,
		elements,
		nodeId,
		rootContext
	]);
	index$1(() => {
		rootContext.dataRef.current.floatingContext = context;
		const node = tree == null ? void 0 : tree.nodesRef.current.find((node) => node.id === nodeId);
		if (node) node.context = context;
	});
	return import_react.useMemo(() => ({
		...position,
		context,
		refs,
		elements
	}), [
		position,
		refs,
		elements,
		context
	]);
}
function isMacSafari() {
	return isMac() && isSafari();
}
/**
* Opens the floating element while the reference element has focus, like CSS
* `:focus`.
* @see https://floating-ui.com/docs/useFocus
*/
function useFocus(context, props) {
	if (props === void 0) props = {};
	const { open, onOpenChange, events, dataRef, elements } = context;
	const { enabled = true, visibleOnly = true } = props;
	const blockFocusRef = import_react.useRef(false);
	const timeoutRef = import_react.useRef(-1);
	const keyboardModalityRef = import_react.useRef(true);
	import_react.useEffect(() => {
		if (!enabled) return;
		const win = getWindow(elements.domReference);
		function onBlur() {
			if (!open && isHTMLElement(elements.domReference) && elements.domReference === activeElement(getDocument$1(elements.domReference))) blockFocusRef.current = true;
		}
		function onKeyDown() {
			keyboardModalityRef.current = true;
		}
		function onPointerDown() {
			keyboardModalityRef.current = false;
		}
		win.addEventListener("blur", onBlur);
		if (isMacSafari()) {
			win.addEventListener("keydown", onKeyDown, true);
			win.addEventListener("pointerdown", onPointerDown, true);
		}
		return () => {
			win.removeEventListener("blur", onBlur);
			if (isMacSafari()) {
				win.removeEventListener("keydown", onKeyDown, true);
				win.removeEventListener("pointerdown", onPointerDown, true);
			}
		};
	}, [
		elements.domReference,
		open,
		enabled
	]);
	import_react.useEffect(() => {
		if (!enabled) return;
		function onOpenChange(_ref) {
			let { reason } = _ref;
			if (reason === "reference-press" || reason === "escape-key") blockFocusRef.current = true;
		}
		events.on("openchange", onOpenChange);
		return () => {
			events.off("openchange", onOpenChange);
		};
	}, [events, enabled]);
	import_react.useEffect(() => {
		return () => {
			clearTimeoutIfSet(timeoutRef);
		};
	}, []);
	const reference = import_react.useMemo(() => ({
		onMouseLeave() {
			blockFocusRef.current = false;
		},
		onFocus(event) {
			if (blockFocusRef.current) return;
			const target = getTarget$2(event.nativeEvent);
			if (visibleOnly && isElement$2(target)) {
				if (isMacSafari() && !event.relatedTarget) {
					if (!keyboardModalityRef.current && !isTypeableElement(target)) return;
				} else if (!matchesFocusVisible(target)) return;
			}
			onOpenChange(true, event.nativeEvent, "focus");
		},
		onBlur(event) {
			blockFocusRef.current = false;
			const relatedTarget = event.relatedTarget;
			const nativeEvent = event.nativeEvent;
			const movedToFocusGuard = isElement$2(relatedTarget) && relatedTarget.hasAttribute(createAttribute("focus-guard")) && relatedTarget.getAttribute("data-type") === "outside";
			timeoutRef.current = window.setTimeout(() => {
				var _dataRef$current$floa;
				const activeEl = activeElement(elements.domReference ? elements.domReference.ownerDocument : document);
				if (!relatedTarget && activeEl === elements.domReference) return;
				if (contains$1((_dataRef$current$floa = dataRef.current.floatingContext) == null ? void 0 : _dataRef$current$floa.refs.floating.current, activeEl) || contains$1(elements.domReference, activeEl) || movedToFocusGuard) return;
				onOpenChange(false, nativeEvent, "focus");
			});
		}
	}), [
		dataRef,
		elements.domReference,
		onOpenChange,
		visibleOnly
	]);
	return import_react.useMemo(() => enabled ? { reference } : {}, [enabled, reference]);
}
function mergeProps$1(userProps, propsList, elementKey) {
	const map = /* @__PURE__ */ new Map();
	const isItem = elementKey === "item";
	let domUserProps = userProps;
	if (isItem && userProps) {
		const { [ACTIVE_KEY]: _, [SELECTED_KEY]: __, ...validProps } = userProps;
		domUserProps = validProps;
	}
	return {
		...elementKey === "floating" && {
			tabIndex: -1,
			[FOCUSABLE_ATTRIBUTE]: ""
		},
		...domUserProps,
		...propsList.map((value) => {
			const propsOrGetProps = value ? value[elementKey] : null;
			if (typeof propsOrGetProps === "function") return userProps ? propsOrGetProps(userProps) : null;
			return propsOrGetProps;
		}).concat(userProps).reduce((acc, props) => {
			if (!props) return acc;
			Object.entries(props).forEach((_ref) => {
				let [key, value] = _ref;
				if (isItem && [ACTIVE_KEY, SELECTED_KEY].includes(key)) return;
				if (key.indexOf("on") === 0) {
					if (!map.has(key)) map.set(key, []);
					if (typeof value === "function") {
						var _map$get;
						(_map$get = map.get(key)) == null || _map$get.push(value);
						acc[key] = function() {
							var _map$get2;
							for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
							return (_map$get2 = map.get(key)) == null ? void 0 : _map$get2.map((fn) => fn(...args)).find((val) => val !== void 0);
						};
					}
				} else acc[key] = value;
			});
			return acc;
		}, {})
	};
}
/**
* Merges an array of interaction hooks' props into prop getters, allowing
* event handler functions to be composed together without overwriting one
* another.
* @see https://floating-ui.com/docs/useInteractions
*/
function useInteractions(propsList) {
	if (propsList === void 0) propsList = [];
	const referenceDeps = propsList.map((key) => key == null ? void 0 : key.reference);
	const floatingDeps = propsList.map((key) => key == null ? void 0 : key.floating);
	const itemDeps = propsList.map((key) => key == null ? void 0 : key.item);
	const getReferenceProps = import_react.useCallback((userProps) => mergeProps$1(userProps, propsList, "reference"), referenceDeps);
	const getFloatingProps = import_react.useCallback((userProps) => mergeProps$1(userProps, propsList, "floating"), floatingDeps);
	const getItemProps = import_react.useCallback((userProps) => mergeProps$1(userProps, propsList, "item"), itemDeps);
	return import_react.useMemo(() => ({
		getReferenceProps,
		getFloatingProps,
		getItemProps
	}), [
		getReferenceProps,
		getFloatingProps,
		getItemProps
	]);
}
var ESCAPE = "Escape";
function doSwitch(orientation, vertical, horizontal) {
	switch (orientation) {
		case "vertical": return vertical;
		case "horizontal": return horizontal;
		default: return vertical || horizontal;
	}
}
function isMainOrientationKey(key, orientation) {
	return doSwitch(orientation, key === ARROW_UP || key === ARROW_DOWN, key === ARROW_LEFT || key === ARROW_RIGHT);
}
function isMainOrientationToEndKey(key, orientation, rtl) {
	return doSwitch(orientation, key === ARROW_DOWN, rtl ? key === ARROW_LEFT : key === ARROW_RIGHT) || key === "Enter" || key === " " || key === "";
}
function isCrossOrientationOpenKey(key, orientation, rtl) {
	return doSwitch(orientation, rtl ? key === ARROW_LEFT : key === ARROW_RIGHT, key === ARROW_DOWN);
}
function isCrossOrientationCloseKey(key, orientation, rtl, cols) {
	const vertical = rtl ? key === ARROW_RIGHT : key === ARROW_LEFT;
	const horizontal = key === ARROW_UP;
	if (orientation === "both" || orientation === "horizontal" && cols && cols > 1) return key === ESCAPE;
	return doSwitch(orientation, vertical, horizontal);
}
/**
* Adds arrow key-based navigation of a list of items, either using real DOM
* focus or virtual focus.
* @see https://floating-ui.com/docs/useListNavigation
*/
function useListNavigation(context, props) {
	const { open, onOpenChange, elements, floatingId } = context;
	const { listRef, activeIndex, onNavigate: unstable_onNavigate = () => {}, enabled = true, selectedIndex = null, allowEscape = false, loop = false, nested = false, rtl = false, virtual = false, focusItemOnOpen = "auto", focusItemOnHover = true, openOnArrowKeyDown = true, disabledIndices = void 0, orientation = "vertical", parentOrientation, cols = 1, scrollItemIntoView = true, virtualItemRef, itemSizes, dense = false } = props;
	if (allowEscape) {
		if (!loop) warn("`useListNavigation` looping must be enabled to allow escaping.");
		if (!virtual) warn("`useListNavigation` must be virtual to allow escaping.");
	}
	if (orientation === "vertical" && cols > 1) warn("In grid list navigation mode (`cols` > 1), the `orientation` should", "be either \"horizontal\" or \"both\".");
	const floatingFocusElementRef = useLatestRef$1(getFloatingFocusElement(elements.floating));
	const parentId = useFloatingParentNodeId();
	const tree = useFloatingTree();
	index$1(() => {
		context.dataRef.current.orientation = orientation;
	}, [context, orientation]);
	const onNavigate = useEffectEvent(() => {
		unstable_onNavigate(indexRef.current === -1 ? null : indexRef.current);
	});
	const typeableComboboxReference = isTypeableCombobox(elements.domReference);
	const focusItemOnOpenRef = import_react.useRef(focusItemOnOpen);
	const indexRef = import_react.useRef(selectedIndex != null ? selectedIndex : -1);
	const keyRef = import_react.useRef(null);
	const isPointerModalityRef = import_react.useRef(true);
	const previousOnNavigateRef = import_react.useRef(onNavigate);
	const previousMountedRef = import_react.useRef(!!elements.floating);
	const previousOpenRef = import_react.useRef(open);
	const forceSyncFocusRef = import_react.useRef(false);
	const forceScrollIntoViewRef = import_react.useRef(false);
	const disabledIndicesRef = useLatestRef$1(disabledIndices);
	const latestOpenRef = useLatestRef$1(open);
	const scrollItemIntoViewRef = useLatestRef$1(scrollItemIntoView);
	const selectedIndexRef = useLatestRef$1(selectedIndex);
	const [activeId, setActiveId] = import_react.useState();
	const [virtualId, setVirtualId] = import_react.useState();
	const focusItem = useEffectEvent(() => {
		function runFocus(item) {
			if (virtual) {
				var _item$id;
				if ((_item$id = item.id) != null && _item$id.endsWith("-fui-option")) item.id = floatingId + "-" + Math.random().toString(16).slice(2, 10);
				setActiveId(item.id);
				tree?.events.emit("virtualfocus", item);
				if (virtualItemRef) virtualItemRef.current = item;
			} else enqueueFocus(item, {
				sync: forceSyncFocusRef.current,
				preventScroll: true
			});
		}
		const initialItem = listRef.current[indexRef.current];
		const forceScrollIntoView = forceScrollIntoViewRef.current;
		if (initialItem) runFocus(initialItem);
		(forceSyncFocusRef.current ? (v) => v() : requestAnimationFrame)(() => {
			const waitedItem = listRef.current[indexRef.current] || initialItem;
			if (!waitedItem) return;
			if (!initialItem) runFocus(waitedItem);
			const scrollIntoViewOptions = scrollItemIntoViewRef.current;
			if (scrollIntoViewOptions && item && (forceScrollIntoView || !isPointerModalityRef.current)) waitedItem.scrollIntoView == null || waitedItem.scrollIntoView(typeof scrollIntoViewOptions === "boolean" ? {
				block: "nearest",
				inline: "nearest"
			} : scrollIntoViewOptions);
		});
	});
	index$1(() => {
		if (!enabled) return;
		if (open && elements.floating) {
			if (focusItemOnOpenRef.current && selectedIndex != null) {
				forceScrollIntoViewRef.current = true;
				indexRef.current = selectedIndex;
				onNavigate();
			}
		} else if (previousMountedRef.current) {
			indexRef.current = -1;
			previousOnNavigateRef.current();
		}
	}, [
		enabled,
		open,
		elements.floating,
		selectedIndex,
		onNavigate
	]);
	index$1(() => {
		if (!enabled) return;
		if (!open) return;
		if (!elements.floating) return;
		if (activeIndex == null) {
			forceSyncFocusRef.current = false;
			if (selectedIndexRef.current != null) return;
			if (previousMountedRef.current) {
				indexRef.current = -1;
				focusItem();
			}
			if ((!previousOpenRef.current || !previousMountedRef.current) && focusItemOnOpenRef.current && (keyRef.current != null || focusItemOnOpenRef.current === true && keyRef.current == null)) {
				let runs = 0;
				const waitForListPopulated = () => {
					if (listRef.current[0] == null) {
						if (runs < 2) (runs ? requestAnimationFrame : queueMicrotask)(waitForListPopulated);
						runs++;
					} else {
						indexRef.current = keyRef.current == null || isMainOrientationToEndKey(keyRef.current, orientation, rtl) || nested ? getMinListIndex(listRef, disabledIndicesRef.current) : getMaxListIndex(listRef, disabledIndicesRef.current);
						keyRef.current = null;
						onNavigate();
					}
				};
				waitForListPopulated();
			}
		} else if (!isIndexOutOfListBounds(listRef, activeIndex)) {
			indexRef.current = activeIndex;
			focusItem();
			forceScrollIntoViewRef.current = false;
		}
	}, [
		enabled,
		open,
		elements.floating,
		activeIndex,
		selectedIndexRef,
		nested,
		listRef,
		orientation,
		rtl,
		onNavigate,
		focusItem,
		disabledIndicesRef
	]);
	index$1(() => {
		var _nodes$find;
		if (!enabled || elements.floating || !tree || virtual || !previousMountedRef.current) return;
		const nodes = tree.nodesRef.current;
		const parent = (_nodes$find = nodes.find((node) => node.id === parentId)) == null || (_nodes$find = _nodes$find.context) == null ? void 0 : _nodes$find.elements.floating;
		const activeEl = activeElement(getDocument$1(elements.floating));
		const treeContainsActiveEl = nodes.some((node) => node.context && contains$1(node.context.elements.floating, activeEl));
		if (parent && !treeContainsActiveEl && isPointerModalityRef.current) parent.focus({ preventScroll: true });
	}, [
		enabled,
		elements.floating,
		tree,
		parentId,
		virtual
	]);
	index$1(() => {
		if (!enabled) return;
		if (!tree) return;
		if (!virtual) return;
		if (parentId) return;
		function handleVirtualFocus(item) {
			setVirtualId(item.id);
			if (virtualItemRef) virtualItemRef.current = item;
		}
		tree.events.on("virtualfocus", handleVirtualFocus);
		return () => {
			tree.events.off("virtualfocus", handleVirtualFocus);
		};
	}, [
		enabled,
		tree,
		virtual,
		parentId,
		virtualItemRef
	]);
	index$1(() => {
		previousOnNavigateRef.current = onNavigate;
		previousOpenRef.current = open;
		previousMountedRef.current = !!elements.floating;
	});
	index$1(() => {
		if (!open) {
			keyRef.current = null;
			focusItemOnOpenRef.current = focusItemOnOpen;
		}
	}, [open, focusItemOnOpen]);
	const hasActiveIndex = activeIndex != null;
	const item = import_react.useMemo(() => {
		function syncCurrentTarget(currentTarget) {
			if (!latestOpenRef.current) return;
			const index = listRef.current.indexOf(currentTarget);
			if (index !== -1 && indexRef.current !== index) {
				indexRef.current = index;
				onNavigate();
			}
		}
		return {
			onFocus(_ref) {
				let { currentTarget } = _ref;
				forceSyncFocusRef.current = true;
				syncCurrentTarget(currentTarget);
			},
			onClick: (_ref2) => {
				let { currentTarget } = _ref2;
				return currentTarget.focus({ preventScroll: true });
			},
			onMouseMove(_ref3) {
				let { currentTarget } = _ref3;
				forceSyncFocusRef.current = true;
				forceScrollIntoViewRef.current = false;
				if (focusItemOnHover) syncCurrentTarget(currentTarget);
			},
			onPointerLeave(_ref4) {
				let { pointerType } = _ref4;
				if (!isPointerModalityRef.current || pointerType === "touch") return;
				forceSyncFocusRef.current = true;
				if (!focusItemOnHover) return;
				indexRef.current = -1;
				onNavigate();
				if (!virtual) {
					var _floatingFocusElement;
					(_floatingFocusElement = floatingFocusElementRef.current) == null || _floatingFocusElement.focus({ preventScroll: true });
				}
			}
		};
	}, [
		latestOpenRef,
		floatingFocusElementRef,
		focusItemOnHover,
		listRef,
		onNavigate,
		virtual
	]);
	const getParentOrientation = import_react.useCallback(() => {
		var _tree$nodesRef$curren;
		return parentOrientation != null ? parentOrientation : tree == null || (_tree$nodesRef$curren = tree.nodesRef.current.find((node) => node.id === parentId)) == null || (_tree$nodesRef$curren = _tree$nodesRef$curren.context) == null || (_tree$nodesRef$curren = _tree$nodesRef$curren.dataRef) == null ? void 0 : _tree$nodesRef$curren.current.orientation;
	}, [
		parentId,
		tree,
		parentOrientation
	]);
	const commonOnKeyDown = useEffectEvent((event) => {
		isPointerModalityRef.current = false;
		forceSyncFocusRef.current = true;
		if (event.which === 229) return;
		if (!latestOpenRef.current && event.currentTarget === floatingFocusElementRef.current) return;
		if (nested && isCrossOrientationCloseKey(event.key, orientation, rtl, cols)) {
			if (!isMainOrientationKey(event.key, getParentOrientation())) stopEvent(event);
			onOpenChange(false, event.nativeEvent, "list-navigation");
			if (isHTMLElement(elements.domReference)) if (virtual) tree?.events.emit("virtualfocus", elements.domReference);
			else elements.domReference.focus();
			return;
		}
		const currentIndex = indexRef.current;
		const minIndex = getMinListIndex(listRef, disabledIndices);
		const maxIndex = getMaxListIndex(listRef, disabledIndices);
		if (!typeableComboboxReference) {
			if (event.key === "Home") {
				stopEvent(event);
				indexRef.current = minIndex;
				onNavigate();
			}
			if (event.key === "End") {
				stopEvent(event);
				indexRef.current = maxIndex;
				onNavigate();
			}
		}
		if (cols > 1) {
			const sizes = itemSizes || Array.from({ length: listRef.current.length }, () => ({
				width: 1,
				height: 1
			}));
			const cellMap = createGridCellMap(sizes, cols, dense);
			const minGridIndex = cellMap.findIndex((index) => index != null && !isListIndexDisabled(listRef, index, disabledIndices));
			const maxGridIndex = cellMap.reduce((foundIndex, index, cellIndex) => index != null && !isListIndexDisabled(listRef, index, disabledIndices) ? cellIndex : foundIndex, -1);
			const index = cellMap[getGridNavigatedIndex({ current: cellMap.map((itemIndex) => itemIndex != null ? listRef.current[itemIndex] : null) }, {
				event,
				orientation,
				loop,
				rtl,
				cols,
				disabledIndices: getGridCellIndices([...(typeof disabledIndices !== "function" ? disabledIndices : null) || listRef.current.map((_, index) => isListIndexDisabled(listRef, index, disabledIndices) ? index : void 0), void 0], cellMap),
				minIndex: minGridIndex,
				maxIndex: maxGridIndex,
				prevIndex: getGridCellIndexOfCorner(indexRef.current > maxIndex ? minIndex : indexRef.current, sizes, cellMap, cols, event.key === ARROW_DOWN ? "bl" : event.key === (rtl ? ARROW_LEFT : ARROW_RIGHT) ? "tr" : "tl"),
				stopEvent: true
			})];
			if (index != null) {
				indexRef.current = index;
				onNavigate();
			}
			if (orientation === "both") return;
		}
		if (isMainOrientationKey(event.key, orientation)) {
			stopEvent(event);
			if (open && !virtual && activeElement(event.currentTarget.ownerDocument) === event.currentTarget) {
				indexRef.current = isMainOrientationToEndKey(event.key, orientation, rtl) ? minIndex : maxIndex;
				onNavigate();
				return;
			}
			if (isMainOrientationToEndKey(event.key, orientation, rtl)) if (loop) indexRef.current = currentIndex >= maxIndex ? allowEscape && currentIndex !== listRef.current.length ? -1 : minIndex : findNonDisabledListIndex(listRef, {
				startingIndex: currentIndex,
				disabledIndices
			});
			else indexRef.current = Math.min(maxIndex, findNonDisabledListIndex(listRef, {
				startingIndex: currentIndex,
				disabledIndices
			}));
			else if (loop) indexRef.current = currentIndex <= minIndex ? allowEscape && currentIndex !== -1 ? listRef.current.length : maxIndex : findNonDisabledListIndex(listRef, {
				startingIndex: currentIndex,
				decrement: true,
				disabledIndices
			});
			else indexRef.current = Math.max(minIndex, findNonDisabledListIndex(listRef, {
				startingIndex: currentIndex,
				decrement: true,
				disabledIndices
			}));
			if (isIndexOutOfListBounds(listRef, indexRef.current)) indexRef.current = -1;
			onNavigate();
		}
	});
	const ariaActiveDescendantProp = import_react.useMemo(() => {
		return virtual && open && hasActiveIndex && { "aria-activedescendant": virtualId || activeId };
	}, [
		virtual,
		open,
		hasActiveIndex,
		virtualId,
		activeId
	]);
	const floating = import_react.useMemo(() => {
		return {
			"aria-orientation": orientation === "both" ? void 0 : orientation,
			...!typeableComboboxReference ? ariaActiveDescendantProp : {},
			onKeyDown: commonOnKeyDown,
			onPointerMove() {
				isPointerModalityRef.current = true;
			}
		};
	}, [
		ariaActiveDescendantProp,
		commonOnKeyDown,
		orientation,
		typeableComboboxReference
	]);
	const reference = import_react.useMemo(() => {
		function checkVirtualMouse(event) {
			if (focusItemOnOpen === "auto" && isVirtualClick(event.nativeEvent)) focusItemOnOpenRef.current = true;
		}
		function checkVirtualPointer(event) {
			focusItemOnOpenRef.current = focusItemOnOpen;
			if (focusItemOnOpen === "auto" && isVirtualPointerEvent(event.nativeEvent)) focusItemOnOpenRef.current = true;
		}
		return {
			...ariaActiveDescendantProp,
			onKeyDown(event) {
				isPointerModalityRef.current = false;
				const isArrowKey = event.key.startsWith("Arrow");
				const isHomeOrEndKey = ["Home", "End"].includes(event.key);
				const isMoveKey = isArrowKey || isHomeOrEndKey;
				const isCrossOpenKey = isCrossOrientationOpenKey(event.key, orientation, rtl);
				const isCrossCloseKey = isCrossOrientationCloseKey(event.key, orientation, rtl, cols);
				const isParentCrossOpenKey = isCrossOrientationOpenKey(event.key, getParentOrientation(), rtl);
				const isMainKey = isMainOrientationKey(event.key, orientation);
				const isNavigationKey = (nested ? isParentCrossOpenKey : isMainKey) || event.key === "Enter" || event.key.trim() === "";
				if (virtual && open) {
					const rootNode = tree == null ? void 0 : tree.nodesRef.current.find((node) => node.parentId == null);
					const deepestNode = tree && rootNode ? getDeepestNode(tree.nodesRef.current, rootNode.id) : null;
					if (isMoveKey && deepestNode && virtualItemRef) {
						const eventObject = new KeyboardEvent("keydown", {
							key: event.key,
							bubbles: true
						});
						if (isCrossOpenKey || isCrossCloseKey) {
							var _deepestNode$context, _deepestNode$context2;
							const isCurrentTarget = ((_deepestNode$context = deepestNode.context) == null ? void 0 : _deepestNode$context.elements.domReference) === event.currentTarget;
							const dispatchItem = isCrossCloseKey && !isCurrentTarget ? (_deepestNode$context2 = deepestNode.context) == null ? void 0 : _deepestNode$context2.elements.domReference : isCrossOpenKey ? listRef.current.find((item) => (item == null ? void 0 : item.id) === activeId) : null;
							if (dispatchItem) {
								stopEvent(event);
								dispatchItem.dispatchEvent(eventObject);
								setVirtualId(void 0);
							}
						}
						if ((isMainKey || isHomeOrEndKey) && deepestNode.context) {
							if (deepestNode.context.open && deepestNode.parentId && event.currentTarget !== deepestNode.context.elements.domReference) {
								var _deepestNode$context$;
								stopEvent(event);
								(_deepestNode$context$ = deepestNode.context.elements.domReference) == null || _deepestNode$context$.dispatchEvent(eventObject);
								return;
							}
						}
					}
					return commonOnKeyDown(event);
				}
				if (!open && !openOnArrowKeyDown && isArrowKey) return;
				if (isNavigationKey) {
					const isParentMainKey = isMainOrientationKey(event.key, getParentOrientation());
					keyRef.current = nested && isParentMainKey ? null : event.key;
				}
				if (nested) {
					if (isParentCrossOpenKey) {
						stopEvent(event);
						if (open) {
							indexRef.current = getMinListIndex(listRef, disabledIndicesRef.current);
							onNavigate();
						} else onOpenChange(true, event.nativeEvent, "list-navigation");
					}
					return;
				}
				if (isMainKey) {
					if (selectedIndex != null) indexRef.current = selectedIndex;
					stopEvent(event);
					if (!open && openOnArrowKeyDown) onOpenChange(true, event.nativeEvent, "list-navigation");
					else commonOnKeyDown(event);
					if (open) onNavigate();
				}
			},
			onFocus() {
				if (open && !virtual) {
					indexRef.current = -1;
					onNavigate();
				}
			},
			onPointerDown: checkVirtualPointer,
			onPointerEnter: checkVirtualPointer,
			onMouseDown: checkVirtualMouse,
			onClick: checkVirtualMouse
		};
	}, [
		activeId,
		ariaActiveDescendantProp,
		cols,
		commonOnKeyDown,
		disabledIndicesRef,
		focusItemOnOpen,
		listRef,
		nested,
		onNavigate,
		onOpenChange,
		open,
		openOnArrowKeyDown,
		orientation,
		getParentOrientation,
		rtl,
		selectedIndex,
		tree,
		virtual,
		virtualItemRef
	]);
	return import_react.useMemo(() => enabled ? {
		reference,
		floating,
		item
	} : {}, [
		enabled,
		reference,
		floating,
		item
	]);
}
var componentRoleToAriaRoleMap = /*#__PURE__*/ new Map([
	["select", "listbox"],
	["combobox", "listbox"],
	["label", false]
]);
/**
* Adds base screen reader props to the reference and floating elements for a
* given floating element `role`.
* @see https://floating-ui.com/docs/useRole
*/
function useRole(context, props) {
	var _elements$domReferenc, _componentRoleToAriaR;
	if (props === void 0) props = {};
	const { open, elements, floatingId: defaultFloatingId } = context;
	const { enabled = true, role = "dialog" } = props;
	const defaultReferenceId = useId();
	const referenceId = ((_elements$domReferenc = elements.domReference) == null ? void 0 : _elements$domReferenc.id) || defaultReferenceId;
	const floatingId = import_react.useMemo(() => {
		var _getFloatingFocusElem;
		return ((_getFloatingFocusElem = getFloatingFocusElement(elements.floating)) == null ? void 0 : _getFloatingFocusElem.id) || defaultFloatingId;
	}, [elements.floating, defaultFloatingId]);
	const ariaRole = (_componentRoleToAriaR = componentRoleToAriaRoleMap.get(role)) != null ? _componentRoleToAriaR : role;
	const isNested = useFloatingParentNodeId() != null;
	const reference = import_react.useMemo(() => {
		if (ariaRole === "tooltip" || role === "label") return { ["aria-" + (role === "label" ? "labelledby" : "describedby")]: open ? floatingId : void 0 };
		return {
			"aria-expanded": open ? "true" : "false",
			"aria-haspopup": ariaRole === "alertdialog" ? "dialog" : ariaRole,
			"aria-controls": open ? floatingId : void 0,
			...ariaRole === "listbox" && { role: "combobox" },
			...ariaRole === "menu" && { id: referenceId },
			...ariaRole === "menu" && isNested && { role: "menuitem" },
			...role === "select" && { "aria-autocomplete": "none" },
			...role === "combobox" && { "aria-autocomplete": "list" }
		};
	}, [
		ariaRole,
		floatingId,
		isNested,
		open,
		referenceId,
		role
	]);
	const floating = import_react.useMemo(() => {
		const floatingProps = {
			id: floatingId,
			...ariaRole && { role: ariaRole }
		};
		if (ariaRole === "tooltip" || role === "label") return floatingProps;
		return {
			...floatingProps,
			...ariaRole === "menu" && { "aria-labelledby": referenceId }
		};
	}, [
		ariaRole,
		floatingId,
		referenceId,
		role
	]);
	const item = import_react.useCallback((_ref) => {
		let { active, selected } = _ref;
		const commonProps = {
			role: "option",
			...active && { id: floatingId + "-fui-option" }
		};
		switch (role) {
			case "select":
			case "combobox": return {
				...commonProps,
				"aria-selected": selected
			};
		}
		return {};
	}, [floatingId, role]);
	return import_react.useMemo(() => enabled ? {
		reference,
		floating,
		item
	} : {}, [
		enabled,
		reference,
		floating,
		item
	]);
}
function useDelayUnmount(open, durationMs) {
	const [isMounted, setIsMounted] = import_react.useState(open);
	if (open && !isMounted) setIsMounted(true);
	import_react.useEffect(() => {
		if (!open && isMounted) {
			const timeout = setTimeout(() => setIsMounted(false), durationMs);
			return () => clearTimeout(timeout);
		}
	}, [
		open,
		isMounted,
		durationMs
	]);
	return isMounted;
}
/**
* Provides a status string to apply CSS transitions to a floating element,
* correctly handling placement-aware transitions.
* @see https://floating-ui.com/docs/useTransition#usetransitionstatus
*/
function useTransitionStatus(context, props) {
	if (props === void 0) props = {};
	const { open, elements: { floating } } = context;
	const { duration = 250 } = props;
	const closeDuration = (typeof duration === "number" ? duration : duration.close) || 0;
	const [status, setStatus] = import_react.useState("unmounted");
	const isMounted = useDelayUnmount(open, closeDuration);
	if (!isMounted && status === "close") setStatus("unmounted");
	index$1(() => {
		if (!floating) return;
		if (open) {
			setStatus("initial");
			const frame = requestAnimationFrame(() => {
				import_react_dom.flushSync(() => {
					setStatus("open");
				});
			});
			return () => {
				cancelAnimationFrame(frame);
			};
		}
		setStatus("close");
	}, [open, floating]);
	return {
		isMounted,
		status
	};
}
function getNodeChildren(nodes, id, onlyOpenChildren) {
	if (onlyOpenChildren === void 0) onlyOpenChildren = true;
	return nodes.filter((node) => {
		var _node$context;
		return node.parentId === id && (!onlyOpenChildren || ((_node$context = node.context) == null ? void 0 : _node$context.open));
	}).flatMap((child) => [child, ...getNodeChildren(nodes, child.id, onlyOpenChildren)]);
}
function isPointInPolygon(point, polygon) {
	const [x, y] = point;
	let isInside = false;
	const length = polygon.length;
	for (let i = 0, j = length - 1; i < length; j = i++) {
		const [xi, yi] = polygon[i] || [0, 0];
		const [xj, yj] = polygon[j] || [0, 0];
		if (yi >= y !== yj >= y && x <= (xj - xi) * (y - yi) / (yj - yi) + xi) isInside = !isInside;
	}
	return isInside;
}
function isInside(point, rect) {
	return point[0] >= rect.x && point[0] <= rect.x + rect.width && point[1] >= rect.y && point[1] <= rect.y + rect.height;
}
/**
* Generates a safe polygon area that the user can traverse without closing the
* floating element once leaving the reference element.
* @see https://floating-ui.com/docs/useHover#safepolygon
*/
function safePolygon(options) {
	if (options === void 0) options = {};
	const { buffer = .5, blockPointerEvents = false, requireIntent = true } = options;
	const timeoutRef = { current: -1 };
	let hasLanded = false;
	let lastX = null;
	let lastY = null;
	let lastCursorTime = typeof performance !== "undefined" ? performance.now() : 0;
	function getCursorSpeed(x, y) {
		const currentTime = performance.now();
		const elapsedTime = currentTime - lastCursorTime;
		if (lastX === null || lastY === null || elapsedTime === 0) {
			lastX = x;
			lastY = y;
			lastCursorTime = currentTime;
			return null;
		}
		const deltaX = x - lastX;
		const deltaY = y - lastY;
		const speed = Math.sqrt(deltaX * deltaX + deltaY * deltaY) / elapsedTime;
		lastX = x;
		lastY = y;
		lastCursorTime = currentTime;
		return speed;
	}
	const fn = (_ref) => {
		let { x, y, placement, elements, onClose, nodeId, tree } = _ref;
		return function onMouseMove(event) {
			function close() {
				clearTimeoutIfSet(timeoutRef);
				onClose();
			}
			clearTimeoutIfSet(timeoutRef);
			if (!elements.domReference || !elements.floating || placement == null || x == null || y == null) return;
			const { clientX, clientY } = event;
			const clientPoint = [clientX, clientY];
			const target = getTarget$1(event);
			const isLeave = event.type === "mouseleave";
			const isOverFloatingEl = contains(elements.floating, target);
			const isOverReferenceEl = contains(elements.domReference, target);
			const refRect = elements.domReference.getBoundingClientRect();
			const rect = elements.floating.getBoundingClientRect();
			const side = placement.split("-")[0];
			const cursorLeaveFromRight = x > rect.right - rect.width / 2;
			const cursorLeaveFromBottom = y > rect.bottom - rect.height / 2;
			const isOverReferenceRect = isInside(clientPoint, refRect);
			const isFloatingWider = rect.width > refRect.width;
			const isFloatingTaller = rect.height > refRect.height;
			const left = (isFloatingWider ? refRect : rect).left;
			const right = (isFloatingWider ? refRect : rect).right;
			const top = (isFloatingTaller ? refRect : rect).top;
			const bottom = (isFloatingTaller ? refRect : rect).bottom;
			if (isOverFloatingEl) {
				hasLanded = true;
				if (!isLeave) return;
			}
			if (isOverReferenceEl) hasLanded = false;
			if (isOverReferenceEl && !isLeave) {
				hasLanded = true;
				return;
			}
			if (isLeave && isElement$2(event.relatedTarget) && contains(elements.floating, event.relatedTarget)) return;
			if (tree && getNodeChildren(tree.nodesRef.current, nodeId).length) return;
			if (side === "top" && y >= refRect.bottom - 1 || side === "bottom" && y <= refRect.top + 1 || side === "left" && x >= refRect.right - 1 || side === "right" && x <= refRect.left + 1) return close();
			let rectPoly = [];
			switch (side) {
				case "top":
					rectPoly = [
						[left, refRect.top + 1],
						[left, rect.bottom - 1],
						[right, rect.bottom - 1],
						[right, refRect.top + 1]
					];
					break;
				case "bottom":
					rectPoly = [
						[left, rect.top + 1],
						[left, refRect.bottom - 1],
						[right, refRect.bottom - 1],
						[right, rect.top + 1]
					];
					break;
				case "left":
					rectPoly = [
						[rect.right - 1, bottom],
						[rect.right - 1, top],
						[refRect.left + 1, top],
						[refRect.left + 1, bottom]
					];
					break;
				case "right":
					rectPoly = [
						[refRect.right - 1, bottom],
						[refRect.right - 1, top],
						[rect.left + 1, top],
						[rect.left + 1, bottom]
					];
					break;
			}
			function getPolygon(_ref2) {
				let [x, y] = _ref2;
				switch (side) {
					case "top": return [
						[isFloatingWider ? x + buffer / 2 : cursorLeaveFromRight ? x + buffer * 4 : x - buffer * 4, y + buffer + 1],
						[isFloatingWider ? x - buffer / 2 : cursorLeaveFromRight ? x + buffer * 4 : x - buffer * 4, y + buffer + 1],
						...[[rect.left, cursorLeaveFromRight ? rect.bottom - buffer : isFloatingWider ? rect.bottom - buffer : rect.top], [rect.right, cursorLeaveFromRight ? isFloatingWider ? rect.bottom - buffer : rect.top : rect.bottom - buffer]]
					];
					case "bottom": return [
						[isFloatingWider ? x + buffer / 2 : cursorLeaveFromRight ? x + buffer * 4 : x - buffer * 4, y - buffer],
						[isFloatingWider ? x - buffer / 2 : cursorLeaveFromRight ? x + buffer * 4 : x - buffer * 4, y - buffer],
						...[[rect.left, cursorLeaveFromRight ? rect.top + buffer : isFloatingWider ? rect.top + buffer : rect.bottom], [rect.right, cursorLeaveFromRight ? isFloatingWider ? rect.top + buffer : rect.bottom : rect.top + buffer]]
					];
					case "left": {
						const cursorPointOne = [x + buffer + 1, isFloatingTaller ? y + buffer / 2 : cursorLeaveFromBottom ? y + buffer * 4 : y - buffer * 4];
						const cursorPointTwo = [x + buffer + 1, isFloatingTaller ? y - buffer / 2 : cursorLeaveFromBottom ? y + buffer * 4 : y - buffer * 4];
						return [
							...[[cursorLeaveFromBottom ? rect.right - buffer : isFloatingTaller ? rect.right - buffer : rect.left, rect.top], [cursorLeaveFromBottom ? isFloatingTaller ? rect.right - buffer : rect.left : rect.right - buffer, rect.bottom]],
							cursorPointOne,
							cursorPointTwo
						];
					}
					case "right": return [
						[x - buffer, isFloatingTaller ? y + buffer / 2 : cursorLeaveFromBottom ? y + buffer * 4 : y - buffer * 4],
						[x - buffer, isFloatingTaller ? y - buffer / 2 : cursorLeaveFromBottom ? y + buffer * 4 : y - buffer * 4],
						...[[cursorLeaveFromBottom ? rect.left + buffer : isFloatingTaller ? rect.left + buffer : rect.right, rect.top], [cursorLeaveFromBottom ? isFloatingTaller ? rect.left + buffer : rect.right : rect.left + buffer, rect.bottom]]
					];
				}
			}
			if (isPointInPolygon([clientX, clientY], rectPoly)) return;
			if (hasLanded && !isOverReferenceRect) return close();
			if (!isLeave && requireIntent) {
				const cursorSpeed = getCursorSpeed(event.clientX, event.clientY);
				if (cursorSpeed !== null && cursorSpeed < .1) return close();
			}
			if (!isPointInPolygon([clientX, clientY], getPolygon([x, y]))) close();
			else if (!hasLanded && requireIntent) timeoutRef.current = window.setTimeout(close, 40);
		};
	};
	fn.__options = { blockPointerEvents };
	return fn;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Popup/constants.js
var AUTO_PLACEMENTS = [
	"auto",
	"auto-start",
	"auto-end"
];
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Popup/utils.js
function getOffsetOptions(offsetProp, hasArrow) {
	let offset = offsetProp;
	if (hasArrow) if (typeof offset === "number") offset += 8;
	else offset = {
		...offset,
		mainAxis: (offset.mainAxis ?? 0) + 8
	};
	return { offset };
}
function isAutoPlacement(placement) {
	return Boolean(placement && AUTO_PLACEMENTS.includes(placement));
}
function getPlacementOptions(placementProp, disablePortal) {
	let placement;
	let middleware;
	if (Array.isArray(placementProp)) {
		placement = placementProp[0];
		middleware = flip({
			padding: 4,
			altBoundary: disablePortal,
			fallbackPlacements: placementProp.slice(1)
		});
	} else if (isAutoPlacement(placementProp)) {
		let alignment;
		if (placementProp === "auto-start") alignment = "start";
		else if (placementProp === "auto-end") alignment = "end";
		placement = void 0;
		middleware = autoPlacement({
			padding: 4,
			altBoundary: disablePortal,
			alignment
		});
	} else {
		let fallbackAxisSideDirection;
		if (placementProp?.startsWith("top") || placementProp?.startsWith("left")) fallbackAxisSideDirection = "start";
		else fallbackAxisSideDirection = "end";
		placement = placementProp;
		middleware = flip({
			padding: 4,
			altBoundary: disablePortal,
			fallbackAxisSideDirection
		});
	}
	return {
		placement,
		middleware
	};
}
var arrowStylesMiddleware = () => ({
	name: "arrowStyles",
	fn({ middlewareData }) {
		if (!middlewareData.arrow) return {};
		return { data: {
			left: middlewareData.arrow.x,
			top: middlewareData.arrow.y
		} };
	}
});
function roundByDPR(value) {
	const dpr = typeof window !== "undefined" ? window.devicePixelRatio || 1 : 1;
	return Math.round(value * dpr) / dpr;
}
var roundByDPRMiddleware = () => ({
	name: "roundByDPR",
	fn({ x, y }) {
		return {
			x: roundByDPR(x),
			y: roundByDPR(y)
		};
	}
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Portal/Portal.js
var b$107 = block("portal");
function Portal(rawProps) {
	const { container, children, disablePortal } = useDefaultProps("Portal", rawProps);
	const defaultContainer = usePortalContainer();
	const { scoped } = useThemeContext();
	const containerNode = container ?? defaultContainer;
	if (disablePortal) return (0, import_jsx_runtime.jsx)(import_react.Fragment, { children });
	if (containerNode) return (0, import_jsx_runtime.jsx)(FloatingPortal, {
		root: containerNode,
		children: scoped ? (0, import_jsx_runtime.jsx)(ThemeProvider, {
			rootClassName: b$107("theme-wrapper"),
			scoped: true,
			children
		}) : children
	});
	return null;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/getElementRef.js
function getElementRef(element) {
	{
		const getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
		if (getter && "isReactWarning" in getter && getter.isReactWarning) return element.ref;
	}
	return element.props.ref ?? element.ref;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Tooltip/Tooltip.js
var b$106 = block("tooltip");
var DEFAULT_OPEN_DELAY$2 = 1e3;
var DEFAULT_CLOSE_DELAY$2 = 0;
var DEFAULT_REST$1 = 0;
var DEFAULT_PLACEMENT$2 = "bottom";
var DEFAULT_OFFSET = 4;
function Tooltip(rawProps) {
	const { children, open, onOpenChange, strategy, placement: placementProp = DEFAULT_PLACEMENT$2, offset: offsetProp = DEFAULT_OFFSET, disabled, content, trigger = "all", role: roleProp = "tooltip", openDelay = DEFAULT_OPEN_DELAY$2, closeDelay = DEFAULT_CLOSE_DELAY$2, rest = DEFAULT_REST$1, container, disablePortal, className, style, qa, ...restProps } = useDefaultProps("Tooltip", rawProps);
	const [anchorElement, setAnchorElement] = import_react.useState(null);
	const { placement, middleware: placementMiddleware } = getPlacementOptions(placementProp, false);
	const [isOpen, setIsOpen] = useControlledState(open, false, onOpenChange);
	const { refs, floatingStyles, context } = useFloating({
		open: isOpen,
		onOpenChange: setIsOpen,
		strategy,
		placement,
		middleware: [
			offset$1(offsetProp),
			shift$1({
				padding: 4,
				limiter: limitShift()
			}),
			placementMiddleware
		],
		whileElementsMounted: autoUpdate,
		elements: { reference: anchorElement }
	});
	const { getReferenceProps, getFloatingProps } = useInteractions([
		useHover$1(context, {
			enabled: trigger === "all",
			delay: {
				open: openDelay,
				close: closeDelay
			},
			restMs: rest,
			move: false
		}),
		useFocus(context),
		useRole(context, { role: roleProp }),
		useDismiss(context, {
			outsidePress: false,
			ancestorScroll: true
		})
	]);
	const anchorRef = useForkRef(setAnchorElement, import_react.isValidElement(children) ? getElementRef(children) : void 0);
	const anchorProps = import_react.isValidElement(children) ? getReferenceProps(children.props) : getReferenceProps();
	const anchorNode = import_react.isValidElement(children) ? import_react.cloneElement(children, {
		...anchorProps,
		ref: anchorRef
	}) : children(anchorProps, anchorRef);
	return (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [anchorNode, isOpen && !disabled ? (0, import_jsx_runtime.jsx)(Portal, {
		container,
		disablePortal,
		children: (0, import_jsx_runtime.jsx)("div", {
			ref: refs.setFloating,
			style: {
				position: "absolute",
				top: 0,
				left: 0,
				zIndex: 1e4,
				width: "max-content",
				...floatingStyles
			},
			...getFloatingProps(),
			children: (0, import_jsx_runtime.jsx)("div", {
				className: b$106(null, className),
				style,
				"data-qa": qa,
				...filterDOMProps(restProps, { labelable: true }),
				children: content
			})
		})
	}) : null] });
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ActionTooltip/ActionTooltip.js
var b$105 = block("action-tooltip");
var DEFAULT_OPEN_DELAY$1 = 500;
var DEFAULT_CLOSE_DELAY$1 = 0;
function ActionTooltip(rawProps) {
	const { title, description, hotkey, openDelay = DEFAULT_OPEN_DELAY$1, closeDelay = DEFAULT_CLOSE_DELAY$1, className, ...restProps } = useDefaultProps("ActionTooltip", rawProps);
	const content = import_react.useMemo(() => (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [(0, import_jsx_runtime.jsxs)("div", {
		className: b$105("heading"),
		children: [(0, import_jsx_runtime.jsx)("div", {
			className: b$105("title"),
			children: title
		}), hotkey && (0, import_jsx_runtime.jsx)(Hotkey, {
			view: "dark",
			value: hotkey,
			className: b$105("hotkey")
		})]
	}), description && (0, import_jsx_runtime.jsx)("div", {
		className: b$105("description"),
		children: description
	})] }), [
		title,
		description,
		hotkey
	]);
	return (0, import_jsx_runtime.jsx)(Tooltip, {
		...restProps,
		role: "label",
		content,
		openDelay,
		closeDelay,
		className: b$105(null, className)
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/event-broker/EventBroker.js
var EventBroker = class {
	subscriptions = [];
	componentPrefix;
	constructor(prefix) {
		this.componentPrefix = prefix;
	}
	subscribe(subscription) {
		this.subscriptions.push(subscription);
	}
	unsubscribe(subscription) {
		const index = this.subscriptions.indexOf(subscription);
		if (index > -1) this.subscriptions.splice(index, 1);
	}
	publish({ componentId, ...restData }) {
		this.subscriptions.forEach((fn) => fn({
			...restData,
			componentId: this.componentPrefix ? `${this.componentPrefix}${componentId}` : componentId
		}));
	}
	withEventPublisher(componentId, qa) {
		return (eventBrokerData) => {
			this.publish({
				...eventBrokerData,
				componentId,
				qa
			});
		};
	}
};
var eventBroker = new EventBroker("g-");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/event-broker/useEventBroker.js
function useEventBroker(subscription, broker = eventBroker) {
	import_react.useEffect(() => {
		broker.subscribe(subscription);
		return () => broker.unsubscribe(subscription);
	}, [broker, subscription]);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Button/ButtonIcon.js
var b$104 = block("button");
function warnAboutPhysicalValues$1() {
	warnOnce("[Button.Icon] Physical values (left, right) of \"side\" property are deprecated. Use logical values (start, end) instead.");
}
var ButtonIcon = ({ side, className, children }) => {
	return (0, import_jsx_runtime.jsx)("span", {
		className: b$104("icon", { side: getIconSide(side) }, className),
		children: (0, import_jsx_runtime.jsx)("span", {
			className: b$104("icon-inner"),
			children
		})
	});
};
ButtonIcon.displayName = "Button.Icon";
function getIconSide(side) {
	let sideMod = side;
	if (sideMod === "left") {
		warnAboutPhysicalValues$1();
		sideMod = "start";
	}
	if (sideMod === "right") {
		warnAboutPhysicalValues$1();
		sideMod = "end";
	}
	return sideMod;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Button/Button.js
function isButtonComponentProps(p) {
	return p.component !== void 0;
}
var b$103 = block("button");
var _Button = import_react.forwardRef(function Button(rawProps, ref) {
	const props = useDefaultProps("Button", rawProps);
	const { view = "normal", size = "m", pin = "round-round", selected, disabled = false, loading = false, width, children, extraProps, qa, onClickCapture, component: _component, ...rest } = props;
	const commonProps = {
		onClickCapture: import_react.useCallback((event) => {
			eventBroker.publish({
				componentId: "Button",
				eventId: "click",
				domEvent: event,
				meta: {
					content: event.currentTarget.textContent,
					view
				}
			});
			if (onClickCapture) onClickCapture(event);
		}, [view, onClickCapture]),
		className: b$103({
			view,
			size,
			pin,
			selected,
			disabled: disabled || loading,
			loading,
			width
		}, rest.className),
		"data-qa": qa,
		tabIndex: rest.tabIndex ?? extraProps?.tabIndex ?? (disabled ? void 0 : 0)
	};
	if (isButtonComponentProps(props)) return import_react.createElement(props.component, {
		role: "button",
		...rest,
		...extraProps,
		...commonProps,
		ref,
		"aria-disabled": disabled ?? void 0
	}, prepareChildren$1(children));
	if (typeof props.href !== "undefined") return (0, import_jsx_runtime.jsx)("a", {
		...rest,
		...extraProps,
		...commonProps,
		ref,
		rel: props.target === "_blank" && !rest.rel ? "noopener noreferrer" : rest.rel,
		"aria-disabled": disabled ?? void 0,
		children: prepareChildren$1(children)
	});
	return (0, import_jsx_runtime.jsx)("button", {
		...rest,
		...extraProps,
		...commonProps,
		ref,
		type: props.type || "button",
		disabled: disabled || loading,
		"aria-pressed": selected,
		children: prepareChildren$1(children)
	});
});
var Button = Object.assign(_Button, { Icon: ButtonIcon });
var isButtonIconComponent = isOfType(ButtonIcon);
var isSpan = isOfType("span");
var buttonIconClassRe = RegExp(`^${b$103("icon")}($|\\s+\\w)`);
function prepareChildren$1(children) {
	const items = import_react.Children.toArray(children);
	if (items.length === 1) {
		const onlyItem = items[0];
		if (isButtonIconComponent(onlyItem) || isSpan(onlyItem) && buttonIconClassRe.test(onlyItem.props.className || "")) return onlyItem;
		else if (isIcon(onlyItem) || isSvg(onlyItem)) return (0, import_jsx_runtime.jsx)(Button.Icon, { children: onlyItem }, "icon");
		else return (0, import_jsx_runtime.jsx)("span", {
			className: b$103("text"),
			children: onlyItem
		}, "text");
	} else {
		let startIcon, endIcon, text;
		const content = [];
		for (const item of items) {
			const isIconElement = isIcon(item) || isSvg(item);
			const isButtonIconElement = isButtonIconComponent(item);
			const isRenderedButtonIconElement = isSpan(item) && buttonIconClassRe.test(item.props.className || "");
			if (isIconElement || isButtonIconElement || isRenderedButtonIconElement) {
				if (!startIcon && content.length === 0) {
					const key = "icon-start";
					const side = "start";
					if (isIconElement) startIcon = (0, import_jsx_runtime.jsx)(Button.Icon, {
						side,
						children: item
					}, key);
					else if (isButtonIconElement) startIcon = import_react.cloneElement(item, { side });
					else startIcon = import_react.cloneElement(item, { className: b$103("icon", { side: getIconSide(side) }, item.props.className) });
				} else if (!endIcon && content.length !== 0) {
					const key = "icon-end";
					const side = "end";
					if (isIconElement) endIcon = (0, import_jsx_runtime.jsx)(Button.Icon, {
						side,
						children: item
					}, key);
					else if (isButtonIconElement) endIcon = import_react.cloneElement(item, { side });
					else endIcon = import_react.cloneElement(item, { className: b$103("icon", { side: getIconSide(side) }, item.props.className) });
				}
			} else content.push(item);
		}
		if (content.length > 0) text = (0, import_jsx_runtime.jsx)("span", {
			className: b$103("text"),
			children: content
		}, "text");
		return [
			startIcon,
			endIcon,
			text
		];
	}
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Text/text/text.js
var b$102 = block("text");
var TEXT_VARIANTS = [
	"display-4",
	"display-3",
	"display-2",
	"display-1",
	"header-2",
	"header-1",
	"subheader-3",
	"subheader-2",
	"subheader-1",
	"body-3",
	"body-2",
	"body-1",
	"body-short",
	"caption-2",
	"caption-1",
	"code-3",
	"code-inline-3",
	"code-2",
	"code-inline-2",
	"code-1",
	"code-inline-1"
];
/**
* Utility to generate text classes.
*
* **Hint:** Hover on props in your editor to read jsdoc
*
* ---
* ```jsx
* // "text text_display1 some-class"
* text({variant: 'display-1'}, 'some-class')`
*```
*/
var text = ({ variant = "body-1", ellipsis, ellipsisLines, whiteSpace, wordBreak }, className) => b$102({
	variant,
	ellipsis,
	ws: whiteSpace,
	wb: wordBreak,
	"ellipsis-lines": ellipsisLines
}, className);
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Text/Text.js
/**
* A component for working with typography.
*
* Storybook: https://preview.gravity-ui.com/uikit/?path=/story/components-text--default
*
* **Hint:** Hover on props in your editor to read jsdoc
*
* Provides a convenient API for working with mixins of typography and text colors. Just point at the prop in you favorite code editor and read the accompanying documentation via `jsdoc` on where to apply this or that font or color.
*
* ```jsx
* import {Text} from '@gravity-ui/uikit';
*
* <Text variant="body-1" color="inherit" ellipsis>some test</Text>
* ```
*
* You can also use a more flexible way of setting the style. "Gravity UI" also provide `text` utility function.
*
*```jsx
* import {text} from '@gravity-ui/uikit';
*
* // textStyles = 'text text_variant_display-1 some-class-name'
* const textStyles = text({variant: 'display-1'}, 'some-class-name');
*
* <span className={textStyles}>some text</span>
* ```
*/
var Text = import_react.forwardRef(function Text(rawProps, ref) {
	const { as, children, variant, className, ellipsis, color, whiteSpace, wordBreak, ellipsisLines, style: outerStyle, qa, ...rest } = useDefaultProps("Text", rawProps);
	const Tag = as || "span";
	const style = { ...outerStyle };
	if (typeof ellipsisLines === "number") style.WebkitLineClamp = ellipsisLines;
	return (0, import_jsx_runtime.jsx)(Tag, {
		ref,
		className: text({
			variant,
			ellipsis,
			whiteSpace,
			wordBreak,
			ellipsisLines: typeof ellipsisLines === "number"
		}, color ? colorText({ color }, className) : className),
		style,
		"data-qa": qa,
		...rest,
		children
	});
});
Text.displayName = "Text";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/DropdownMenu.classname.js
var cnDropdownMenu = block("dropdown-menu");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/DropdownMenuContext.js
var DropdownMenuContext = import_react.createContext({
	toggle() {},
	data: void 0
});
DropdownMenuContext.displayName = "DropdownMenu.Context";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Menu/MenuGroup.js
var b$101 = block("menu");
var MenuGroup = import_react.forwardRef(function MenuGroup({ label, children, style, className, qa }, ref) {
	const labelId = useUniqId();
	return (0, import_jsx_runtime.jsx)("li", {
		ref,
		className: b$101("list-group-item"),
		children: (0, import_jsx_runtime.jsxs)("div", {
			style,
			className: b$101("group", className),
			"data-qa": qa,
			children: [label && (0, import_jsx_runtime.jsx)("div", {
				id: labelId,
				className: b$101("group-label"),
				children: label
			}), (0, import_jsx_runtime.jsx)("ul", {
				role: "group",
				"aria-labelledby": labelId,
				className: b$101("group-list"),
				children
			})]
		})
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Menu/MenuItem.js
var b$100 = block("menu");
var MenuItem$1 = import_react.forwardRef(function MenuItem({ iconStart, iconEnd, title, disabled, active, selected, href, target, rel, onClick, style, className, contentClassName, theme, extraProps, children, qa }, ref) {
	const { onKeyDown } = useActionHandlers(onClick);
	const handleClickCapture = import_react.useCallback((event) => {
		eventBroker.publish({
			componentId: "MenuItem",
			eventId: "click",
			domEvent: event
		});
	}, []);
	const defaultProps = {
		role: "menuitem",
		onKeyDown: onClick && !disabled ? onKeyDown : void 0
	};
	const commonProps = {
		title,
		onClick: disabled ? void 0 : onClick,
		onClickCapture: disabled ? void 0 : handleClickCapture,
		style,
		tabIndex: disabled ? -1 : 0,
		className: b$100("item", {
			disabled,
			active,
			selected,
			theme,
			interactive: Boolean(onClick) || Boolean(href)
		}, className),
		"data-qa": qa
	};
	const content = [
		iconStart && (0, import_jsx_runtime.jsx)("div", {
			className: b$100("item-icon"),
			children: iconStart
		}, "icon-start"),
		(0, import_jsx_runtime.jsx)("div", {
			className: b$100("item-content", contentClassName),
			children
		}, "content"),
		iconEnd && (0, import_jsx_runtime.jsx)("div", {
			className: b$100("item-icon-end"),
			children: iconEnd
		}, "icon-end")
	];
	let item;
	if (href) item = (0, import_jsx_runtime.jsx)("a", {
		...defaultProps,
		...extraProps,
		...commonProps,
		href,
		target,
		rel,
		children: content
	});
	else item = (0, import_jsx_runtime.jsx)("div", {
		...defaultProps,
		...extraProps,
		...commonProps,
		children: content
	});
	return (0, import_jsx_runtime.jsx)("li", {
		ref,
		className: b$100("list-item"),
		role: "none",
		children: item
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Menu/Menu.js
var b$99 = block("menu");
var Menu$1 = import_react.forwardRef(function Menu(rawProps, ref) {
	const { size = "m", children, style, className, qa, ...restProps } = useDefaultProps("Menu", rawProps);
	return (0, import_jsx_runtime.jsx)("ul", {
		...filterDOMProps(restProps, { labelable: true }),
		ref,
		role: "menu",
		style,
		className: b$99({ size }, className),
		"data-qa": qa,
		children
	});
});
Menu$1.Item = MenuItem$1;
Menu$1.Group = MenuGroup;
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/usePrevious/usePrevious.js
function usePrevious$1(value) {
	const ref = import_react.useRef();
	import_react.useEffect(() => {
		ref.current = value;
	});
	return ref.current;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useFloatingTransition/useFloatingTransition.js
function useFloatingTransition({ context, duration, onTransitionIn, onTransitionInComplete, onTransitionOut, onTransitionOutComplete }) {
	const { isMounted, status } = useTransitionStatus(context, { duration });
	const previousStatus = usePrevious$1(status);
	const openDuration = (typeof duration === "number" ? duration : duration.open) ?? 0;
	const timerIdRef = import_react.useRef(null);
	import_react.useEffect(() => {
		if (status === "open" && previousStatus === "initial") {
			onTransitionIn?.();
			timerIdRef.current = setTimeout(() => {
				onTransitionInComplete?.();
				timerIdRef.current = null;
			}, openDuration);
		}
		if (status === "close" && previousStatus === "open") {
			if (timerIdRef.current) {
				clearTimeout(timerIdRef.current);
				timerIdRef.current = null;
			}
			onTransitionOut?.();
		}
		if (status === "unmounted" && previousStatus === "close") onTransitionOutComplete?.();
	}, [
		status,
		previousStatus,
		openDuration,
		onTransitionIn,
		onTransitionInComplete,
		onTransitionOut,
		onTransitionOutComplete
	]);
	import_react.useEffect(() => () => {
		if (timerIdRef.current) {
			clearTimeout(timerIdRef.current);
			timerIdRef.current = null;
		}
	}, []);
	return {
		isMounted,
		status
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/layer-manager/LayerManager.js
var LayerManager = class {
	stack = [];
	mouseDownLayerTarget;
	add(config) {
		this.stack.push(config);
		if (this.stack.length === 1) this.addListeners();
		this.notifyLayersChange();
	}
	remove(config) {
		const index = this.stack.indexOf(config);
		this.stack.splice(index, 1);
		if (this.stack.length === 0) this.removeListeners();
		this.notifyLayersChange();
	}
	getLayersCount() {
		return this.stack.length;
	}
	getLayers() {
		return this.stack.map(({ type }) => ({ type }));
	}
	addListeners() {
		document.addEventListener("keydown", this.handleDocumentKeyDown);
		document.addEventListener("click", this.handleDocumentClick, true);
		document.addEventListener("mousedown", this.handleDocumentMouseDown, true);
	}
	removeListeners() {
		document.removeEventListener("keydown", this.handleDocumentKeyDown);
		document.removeEventListener("click", this.handleDocumentClick, true);
		document.removeEventListener("mousedown", this.handleDocumentMouseDown, true);
	}
	notifyLayersChange() {
		eventBroker.publish({
			componentId: "LayerManager",
			eventId: "layerschange",
			meta: {
				/**
				* @deprecated use layers
				*/
				layersCount: this.getLayersCount(),
				layers: this.getLayers()
			}
		});
	}
	handleDocumentKeyDown = (event) => {
		if (event.code === KeyCode$1.ESCAPE) {
			const topLayer = this.getTopLayer();
			if (!topLayer.disableEscapeKeyDown) {
				topLayer.onEscapeKeyDown?.(event);
				topLayer.onClose?.(event, "escapeKeyDown");
			}
		}
		if (event.code === KeyCode$1.ENTER) this.getTopLayer().onEnterKeyDown?.(event);
	};
	handleDocumentClick = (event) => {
		if (this.isToastClick(event)) return;
		let layer;
		let mouseDownTarget = null;
		if (this.mouseDownLayerTarget) {
			layer = this.mouseDownLayerTarget.layer;
			mouseDownTarget = this.mouseDownLayerTarget.target;
			this.mouseDownLayerTarget = void 0;
			if (!this.stack.includes(layer)) return;
		} else layer = this.getTopLayer();
		if (!layer.disableOutsideClick && this.isOutsideClick(layer, event, mouseDownTarget)) {
			layer.onOutsideClick?.(event);
			layer.onClose?.(event, "outsideClick");
		}
	};
	handleDocumentMouseDown = (event) => {
		const layer = this.getTopLayer();
		if (layer) this.mouseDownLayerTarget = {
			layer,
			target: event.target
		};
	};
	getTopLayer() {
		return this.stack[this.stack.length - 1];
	}
	isOutsideClick(layer, event, mouseDownTarget = null) {
		const contentElements = layer.contentRefs || [];
		const { target } = event;
		const composedPath = typeof event.composedPath === "function" ? event.composedPath() : [];
		if (contentElements.length > 0) return !contentElements.some((el) => el?.current?.contains?.(target) || el?.current?.contains?.(mouseDownTarget) || composedPath.includes(el?.current));
		return false;
	}
	isToastClick(event) {
		return (typeof event.composedPath === "function" ? event.composedPath() : []).some((el) => {
			return Boolean(el?.dataset?.toast);
		});
	}
};
var layerManager = new LayerManager();
var getLayersCount = () => {
	return layerManager.getLayersCount();
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/layer-manager/useLayer.js
function useLayer({ open, disableEscapeKeyDown, disableOutsideClick, onEscapeKeyDown, onEnterKeyDown, onOutsideClick, onClose, contentRefs, enabled = true, type }) {
	const layerConfigRef = import_react.useRef({
		disableEscapeKeyDown,
		disableOutsideClick,
		onEscapeKeyDown,
		onEnterKeyDown,
		onOutsideClick,
		onClose,
		contentRefs,
		type
	});
	import_react.useEffect(() => {
		Object.assign(layerConfigRef.current, {
			disableEscapeKeyDown,
			disableOutsideClick,
			onEscapeKeyDown,
			onEnterKeyDown,
			onOutsideClick,
			onClose,
			contentRefs,
			enabled
		});
	}, [
		disableEscapeKeyDown,
		disableOutsideClick,
		onEscapeKeyDown,
		onEnterKeyDown,
		onOutsideClick,
		onClose,
		contentRefs,
		enabled
	]);
	import_react.useEffect(() => {
		if (open && enabled) {
			const layerConfig = layerConfigRef.current;
			layerManager.add(layerConfig);
			return () => {
				layerManager.remove(layerConfig);
			};
		}
	}, [open, enabled]);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Popup/PopupArrow.js
var b$98 = block("popup");
var PopupArrow = import_react.forwardRef(function PopupArrow({ styles }, ref) {
	return (0, import_jsx_runtime.jsx)("div", {
		ref,
		className: b$98("arrow"),
		style: styles,
		children: (0, import_jsx_runtime.jsxs)("div", {
			className: b$98("arrow-content"),
			children: [(0, import_jsx_runtime.jsx)("div", {
				className: b$98("arrow-circle-wrapper"),
				children: (0, import_jsx_runtime.jsx)("div", { className: b$98("arrow-circle", { left: true }) })
			}), (0, import_jsx_runtime.jsx)("div", {
				className: b$98("arrow-circle-wrapper"),
				children: (0, import_jsx_runtime.jsx)("div", { className: b$98("arrow-circle", { right: true }) })
			})]
		})
	});
});
var i18n_default$20 = addComponentKeysets({
	en: { close: "Close" },
	ru: { close: "Закрыть" }
}, "Popup");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Popup/Popup.js
var b$97 = block("popup");
function PopupComponent(rawProps) {
	const { keepMounted = false, hasArrow = false, open = false, onOpenChange, strategy, placement: placementProp, offset: offsetProp = 4, anchorElement, anchorRef, floatingMiddlewares, floatingContext, floatingInteractions, floatingRef, floatingStyles: floatingStylesProp, floatingClassName, modal = false, initialFocus: initialFocusProp, returnFocus = true, focusOrder, disableVisuallyHiddenDismiss = !modal, onClose, onEscapeKeyDown, onOutsideClick, disableEscapeKeyDown = false, disableOutsideClick = false, disableFocusOut = false, style, className, children, container, disablePortal = false, disableLayer = false, disableTransition = false, qa, role: roleProp, zIndex = 1e3, onTransitionIn, onTransitionOut, onTransitionInComplete, onTransitionOutComplete, ...restProps } = useDefaultProps("Popup", rawProps);
	useLayer({
		open,
		type: "popup",
		enabled: !disableLayer
	});
	const contentRef = import_react.useRef(null);
	const [arrowElement, setArrowElement] = import_react.useState(null);
	const { offset } = getOffsetOptions(offsetProp, hasArrow);
	const { placement, middleware: placementMiddleware } = getPlacementOptions(placementProp, disablePortal);
	const { t } = i18n_default$20.useTranslation();
	const handleOpenChange = import_react.useCallback((isOpen, event, reason) => {
		onOpenChange?.(isOpen, event, reason);
		if (isOpen || !event) return;
		let closeReason;
		if (reason === "escape-key") closeReason = "escapeKeyDown";
		else if (reason === "outside-press") closeReason = "outsideClick";
		else closeReason = reason;
		if (closeReason === "escapeKeyDown" && onEscapeKeyDown) onEscapeKeyDown(event);
		if (closeReason === "outsideClick" && onOutsideClick) onOutsideClick(event);
		onClose?.(event, closeReason);
	}, [
		onOpenChange,
		onClose,
		onEscapeKeyDown,
		onOutsideClick
	]);
	const floatingNodeId = useFloatingNodeId();
	const { refs, elements, floatingStyles, placement: finalPlacement, middlewareData, context, update, isPositioned } = useFloating({
		rootContext: floatingContext,
		nodeId: floatingNodeId,
		strategy,
		placement,
		open,
		onOpenChange: handleOpenChange,
		middleware: floatingMiddlewares ?? [
			offset$1(offset),
			shift$1({
				padding: 4,
				limiter: limitShift({ offset: 4 + (hasArrow ? 18 : 0) }),
				altBoundary: disablePortal
			}),
			placementMiddleware,
			hasArrow && arrow({
				element: arrowElement,
				padding: 4
			}),
			hasArrow && arrowStylesMiddleware(),
			roundByDPRMiddleware()
		]
	});
	import_react.useEffect(() => {
		const element = anchorElement === void 0 ? anchorRef?.current : anchorElement;
		if (element !== void 0 && element !== refs.reference.current) refs.setReference(element);
	}, [
		anchorElement,
		anchorRef,
		refs
	]);
	const role = useRole(context, {
		enabled: Boolean(roleProp || modal),
		role: roleProp ?? (modal ? "dialog" : void 0)
	});
	const dismiss = useDismiss(context, {
		enabled: !disableOutsideClick || !disableEscapeKeyDown,
		outsidePress: !disableOutsideClick,
		escapeKey: !disableEscapeKeyDown
	});
	const { getFloatingProps } = useInteractions(floatingInteractions ?? [role, dismiss]);
	const { isMounted, status } = useFloatingTransition({
		context,
		duration: disableTransition ? 0 : 100,
		onTransitionIn,
		onTransitionInComplete,
		onTransitionOut,
		onTransitionOutComplete
	});
	import_react.useEffect(() => {
		if (isMounted && elements.reference && elements.floating) return autoUpdate(elements.reference, elements.floating, update);
	}, [
		isMounted,
		elements,
		update
	]);
	const handleFloatingRef = useForkRef(refs.setFloating, floatingRef);
	let initialFocus = initialFocusProp;
	if (typeof initialFocus === "undefined") if (modal) initialFocus = refs.floating;
	else initialFocus = -1;
	return (0, import_jsx_runtime.jsx)(FloatingNode, {
		id: floatingNodeId,
		children: isMounted || keepMounted ? (0, import_jsx_runtime.jsx)(Portal, {
			container,
			disablePortal,
			children: (0, import_jsx_runtime.jsx)(FloatingFocusManager, {
				context,
				disabled: !isPositioned,
				modal,
				initialFocus,
				returnFocus,
				closeOnFocusOut: !disableFocusOut,
				visuallyHiddenDismiss: disableVisuallyHiddenDismiss ? false : t("close"),
				guards: modal || !disablePortal,
				order: focusOrder,
				children: (0, import_jsx_runtime.jsx)("div", {
					ref: handleFloatingRef,
					className: floatingClassName,
					style: {
						position: "absolute",
						top: 0,
						left: 0,
						zIndex,
						width: "max-content",
						pointerEvents: isMounted ? "auto" : "none",
						outline: "none",
						...floatingStyles,
						...floatingStylesProp
					},
					"data-floating-ui-placement": finalPlacement,
					"data-floating-ui-status": status === "open" && !isPositioned ? "initial" : status,
					"aria-modal": modal && isMounted ? true : void 0,
					...getFloatingProps(),
					children: (0, import_jsx_runtime.jsxs)("div", {
						ref: contentRef,
						className: b$97({
							open: isMounted,
							"disable-transition": disableTransition
						}, className),
						style,
						"data-qa": qa,
						...filterDOMProps(restProps),
						children: [hasArrow && (0, import_jsx_runtime.jsx)(PopupArrow, {
							ref: setArrowElement,
							styles: middlewareData.arrowStyles
						}), children]
					})
				})
			})
		}) : null
	});
}
function Popup(props) {
	if (useFloatingParentNodeId() === null) return (0, import_jsx_runtime.jsx)(FloatingTree, { children: (0, import_jsx_runtime.jsx)(PopupComponent, { ...props }) });
	return (0, import_jsx_runtime.jsx)(PopupComponent, { ...props });
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/DropdownMenuNavigationContext.js
var rootMenuPath = [];
var DropdownMenuNavigationContext = import_react.createContext({
	activeMenuPath: rootMenuPath,
	setActiveMenuPath: () => {},
	anchorRef: { current: null }
});
var DropdownMenuNavigationContextProvider = ({ anchorRef, children, disabled }) => {
	const [activeMenuPath, setActiveMenuPath] = import_react.useState(rootMenuPath);
	import_react.useEffect(() => {
		if (disabled) setActiveMenuPath(rootMenuPath);
	}, [disabled]);
	const contextValue = import_react.useMemo(() => ({
		activeMenuPath,
		setActiveMenuPath,
		anchorRef
	}), [activeMenuPath, anchorRef]);
	return (0, import_jsx_runtime.jsx)(DropdownMenuNavigationContext.Provider, {
		value: contextValue,
		children
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/constants.js
var dropdownMenuSeparator = {
	text: "",
	action: () => {},
	path: []
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/utils/isSeparator.js
function isSeparator(item) {
	return item === dropdownMenuSeparator;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/utils/shouldSkipItemNavigation.js
function shouldSkipItemNavigation(item) {
	return item.disabled || isSeparator(item);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/utils/stringifyNavigationPath.js
function stringifyNavigationPath(path) {
	return path?.join(" ") ?? "";
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/DropdownMenuPopup.js
var DropdownMenuPopup = ({ items, open, anchorRef, onClose, size, menuProps, children, popupProps, path = [] }) => {
	const { toggle, data } = import_react.useContext(DropdownMenuContext);
	const { activeMenuPath, setActiveMenuPath, anchorRef: navigationAnchorRef } = import_react.useContext(DropdownMenuNavigationContext);
	const isSubmenu = path.length > 0;
	const activateParent = import_react.useCallback(() => {
		setActiveMenuPath(path.slice(0, path.length - 1));
	}, [setActiveMenuPath, path]);
	const handleMouseEnter = import_react.useCallback(() => {
		setActiveMenuPath(path);
	}, [path, setActiveMenuPath]);
	const handleMouseLeave = import_react.useCallback(() => {
		activateParent();
	}, [activateParent]);
	const handleSelect = import_react.useCallback((activeItem, event) => {
		if (activeItem.items && activeItem.path) setActiveMenuPath(activeItem.path);
		else {
			activeItem.action?.(event, data);
			toggle(false);
		}
	}, [
		data,
		setActiveMenuPath,
		toggle
	]);
	const handleKeydown = import_react.useCallback((activeItemIndex, event) => {
		switch (event.key) {
			case KeyCode$1.ESCAPE:
				if (isSubmenu) {
					event.stopPropagation();
					activateParent?.();
				}
				return false;
			case KeyCode$1.ENTER:
			case KeyCode$1.SPACEBAR: {
				const activeItem = items[activeItemIndex];
				const isSubmenuToggleActive = activeItem?.items;
				if (isSubmenu || isSubmenuToggleActive) {
					event.stopPropagation();
					event.preventDefault();
				}
				if (activeItem) handleSelect(activeItem, event);
				return false;
			}
		}
		return true;
	}, [
		activateParent,
		handleSelect,
		isSubmenu,
		items
	]);
	const isNavigationActive = open && stringifyNavigationPath(path) === stringifyNavigationPath(activeMenuPath);
	const { activeItemIndex, setActiveItemIndex, reset: resetNavigation } = useListNavigation$1({
		items,
		skip: shouldSkipItemNavigation,
		anchorRef: navigationAnchorRef,
		onAnchorKeyDown: handleKeydown,
		disabled: !isNavigationActive,
		initialValue: isSubmenu ? 0 : -1
	});
	import_react.useEffect(() => {
		if (!open) resetNavigation();
	}, [open, resetNavigation]);
	return (0, import_jsx_runtime.jsx)(Popup, {
		open,
		anchorRef,
		onClose,
		placement: "bottom-start",
		...popupProps,
		children: (0, import_jsx_runtime.jsx)("div", {
			onMouseEnter: handleMouseEnter,
			onMouseLeave: handleMouseLeave,
			className: cnDropdownMenu("popup-content"),
			children: children || (0, import_jsx_runtime.jsx)(Menu$1, {
				className: cnDropdownMenu("menu"),
				size,
				...menuProps,
				children: items.map((item, index) => {
					const isActive = isNavigationActive && activeItemIndex === index;
					const activate = () => setActiveItemIndex(index);
					const isActiveParent = open && !isActive && activeMenuPath.length !== 0 && stringifyNavigationPath(item.path) === stringifyNavigationPath(activeMenuPath.slice(0, item.path.length));
					const extraProps = {
						...item.extraProps,
						onClick: activate,
						onMouseEnter: activate
					};
					return (0, import_jsx_runtime.jsx)(DropdownMenuItem, {
						size,
						className: cnDropdownMenu("menu-item", {
							separator: isSeparator(item),
							"active-parent": isActiveParent,
							"with-submenu": Boolean(item.items?.length)
						}, item.className),
						selected: isActive,
						popupProps,
						closeMenu: onClose,
						...item,
						extraProps
					}, index);
				})
			})
		})
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/utils/isSubmenuOpen.js
function isSubmenuOpen(path, activeMenuPath) {
	return path?.every((item, index) => item === activeMenuPath?.[index]) ?? false;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/hooks/useSubmenu.js
function useSubmenu({ items, path }) {
	const { activeMenuPath, setActiveMenuPath } = import_react.useContext(DropdownMenuNavigationContext);
	const hasSubmenu = Boolean(path) && Boolean(items?.length);
	const closeSubmenu = import_react.useCallback(() => {
		if (!path) return;
		setActiveMenuPath(path.slice(0, path.length - 1));
	}, [path, setActiveMenuPath]);
	const openSubmenu = import_react.useCallback(() => {
		if (!path) return;
		setActiveMenuPath(path);
	}, [path, setActiveMenuPath]);
	return {
		hasSubmenu,
		isSubmenuOpen: isSubmenuOpen(path, activeMenuPath),
		openSubmenu,
		closeSubmenu
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/DropdownMenuItem.js
var DropdownMenuItem = ({ text, action, items: subMenuItems, popupProps, closeMenu, children, path, size, ...props }) => {
	const { toggle, data } = import_react.useContext(DropdownMenuContext);
	const menuItemRef = import_react.useRef(null);
	const direction = useDirection();
	const { hasSubmenu, isSubmenuOpen, closeSubmenu, openSubmenu } = useSubmenu({
		items: subMenuItems,
		path
	});
	const handleCloseMenu = import_react.useCallback(() => {
		const close = () => {
			if (closeMenu) closeMenu();
			else toggle(false);
		};
		if (hasSubmenu) {
			closeSubmenu();
			requestAnimationFrame(close);
		} else close();
	}, [
		closeMenu,
		closeSubmenu,
		hasSubmenu,
		toggle
	]);
	const handleMenuItemClick = import_react.useCallback((event) => {
		props.extraProps?.onClick?.(event);
		if (hasSubmenu) {
			if (isSubmenuOpen) closeSubmenu();
			else openSubmenu();
			return;
		}
		action?.(event, data);
		handleCloseMenu();
	}, [
		action,
		data,
		handleCloseMenu,
		hasSubmenu,
		isSubmenuOpen,
		openSubmenu,
		closeSubmenu,
		props.extraProps
	]);
	const extraProps = import_react.useMemo(() => {
		return {
			...props.extraProps,
			onMouseEnter: (event) => {
				props.extraProps?.onMouseEnter?.(event);
				if (hasSubmenu) openSubmenu();
			},
			onMouseLeave: (event) => {
				props.extraProps?.onMouseLeave?.(event);
				if (hasSubmenu) closeSubmenu();
			}
		};
	}, [
		props.extraProps,
		closeSubmenu,
		hasSubmenu,
		openSubmenu
	]);
	const subMenuPlacement = import_react.useMemo(() => direction === "rtl" ? ["left-start", "right-start"] : ["right-start", "left-start"], [direction]);
	const iconEnd = import_react.useMemo(() => hasSubmenu ? (0, import_jsx_runtime.jsx)(Icon, {
		data: direction === "rtl" ? ChevronLeft : ChevronRight,
		size: 10,
		className: cnDropdownMenu("sub-menu-arrow")
	}) : props.iconEnd, [
		hasSubmenu,
		direction,
		props.iconEnd
	]);
	return (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [(0, import_jsx_runtime.jsx)(Menu$1.Item, {
		ref: menuItemRef,
		...props,
		extraProps,
		onClick: handleMenuItemClick,
		iconEnd,
		children: text || children
	}), hasSubmenu && subMenuItems && (0, import_jsx_runtime.jsx)(DropdownMenuPopup, {
		popupProps: {
			...popupProps,
			className: cnDropdownMenu("sub-menu", popupProps?.className),
			placement: subMenuPlacement
		},
		size,
		items: subMenuItems,
		path,
		open: isSubmenuOpen,
		anchorRef: menuItemRef,
		onClose: handleCloseMenu
	})] });
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useMatchMedia/useMatchMedia.js
function useMatchMedia({ media }) {
	const mql = import_react.useMemo(() => typeof window === "undefined" ? null : window.matchMedia(media), [media]);
	const [matches, setMatches] = import_react.useState(Boolean(mql?.matches));
	import_react.useEffect(() => {
		const handleChange = (event) => {
			setMatches(event.matches);
		};
		setMatches(Boolean(mql?.matches));
		mql?.addEventListener("change", handleChange);
		return () => mql?.removeEventListener("change", handleChange);
	}, [mql]);
	return matches;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useAnimateHeight/useAnimateHeight.js
function useAnimateHeight({ ref, enabled: enabledProp }) {
	const isPrefersReducedMotion = useMatchMedia({ media: "(prefers-reduced-motion: reduce)" });
	const enabled = enabledProp && !isPrefersReducedMotion;
	const previousHeight = import_react.useRef(null);
	const isTransitioningHeight = import_react.useRef(false);
	const overflowY = import_react.useRef("");
	const animationFrame = import_react.useRef(-1);
	import_react.useEffect(() => {
		let mutationObserver;
		const node = ref?.current;
		if (node && enabled) {
			mutationObserver = new MutationObserver((mutations) => {
				if (!mutations.length || !isTransitioningHeight.current) return;
				previousHeight.current = calculateNodeHeight(node);
				isTransitioningHeight.current = false;
				node.style.height = "";
				node.style.overflowY = overflowY.current;
				cancelAnimationFrame(animationFrame.current);
			});
			mutationObserver.observe(node, {
				childList: true,
				subtree: true
			});
		}
		return () => {
			mutationObserver?.disconnect();
		};
	}, [ref, enabled]);
	import_react.useEffect(() => {
		if (!enabled) {
			previousHeight.current = null;
			isTransitioningHeight.current = false;
		}
	}, [enabled]);
	const handleResize = import_react.useCallback((resizeInfo) => {
		const node = ref?.current;
		if (!node || isTransitioningHeight.current || !enabled) return;
		const contentHeight = calculateNodeHeight(node);
		if (!previousHeight.current && !overflowY.current) {
			previousHeight.current = contentHeight;
			overflowY.current = node.style.overflowY;
			return;
		}
		if (previousHeight.current && contentHeight === previousHeight.current) return;
		resizeInfo.observer?.unobserve(node);
		node.style.height = `${previousHeight.current}px`;
		isTransitioningHeight.current = true;
		node.style.overflowY = "clip";
		const handleTransitionEnd = (event) => {
			if (event.propertyName !== "height") return;
			node.removeEventListener("transitionend", handleTransitionEnd);
			setTimeout(() => {
				node.style.height = "";
				node.style.overflowY = overflowY.current;
				isTransitioningHeight.current = false;
			}, 0);
		};
		node.addEventListener("transitionend", handleTransitionEnd);
		animationFrame.current = requestAnimationFrame(() => {
			resizeInfo.observer?.observe(node);
			node.style.height = `${contentHeight}px`;
			previousHeight.current = contentHeight;
		});
	}, [ref, enabled]);
	useResizeObserver({
		ref: enabled ? ref : void 0,
		onResize: handleResize
	});
}
function calculateNodeHeight(node) {
	const computedStyle = window.getComputedStyle(node, null);
	if (computedStyle.getPropertyValue("box-sizing") === "border-box") return node.clientHeight;
	const paddingTop = parseInt(computedStyle.getPropertyValue("padding-top"), 10);
	const paddingBottom = parseInt(computedStyle.getPropertyValue("padding-bottom"), 10);
	return node.clientHeight - paddingTop - paddingBottom;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useBoolean/useBoolean.js
function useBoolean(initialState) {
	const [value, setValue] = import_react.useState(initialState);
	return [
		value,
		import_react.useCallback(() => setValue(true), []),
		import_react.useCallback(() => setValue(false), []),
		import_react.useCallback(() => setValue((val) => !val), [])
	];
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useFormResetHandler/index.js
function useFormResetHandler({ initialValue, onReset }) {
	const [formElement, setFormElement] = import_react.useState(null);
	const resetValue = import_react.useRef(initialValue);
	import_react.useEffect(() => {
		if (!formElement) return;
		const handleReset = () => {
			onReset(resetValue.current);
		};
		formElement.addEventListener("reset", handleReset);
		return () => {
			formElement.removeEventListener("reset", handleReset);
		};
	}, [formElement, onReset]);
	return import_react.useCallback((node) => {
		setFormElement(node?.form ?? null);
	}, []);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useCheckbox/useCheckbox.js
function useCheckbox({ name, value, id, defaultChecked, checked, indeterminate, onUpdate, onChange, controlRef, controlProps, onFocus, onBlur, disabled }) {
	const innerControlRef = import_react.useRef(null);
	const [isChecked, setCheckedState] = useControlledState(checked, defaultChecked ?? false, onUpdate);
	const inputChecked = indeterminate ? false : checked;
	const handleRef = useForkRef(controlRef, innerControlRef, useFormResetHandler({
		initialValue: isChecked,
		onReset: setCheckedState
	}));
	useLayoutEffect$4(() => {
		if (innerControlRef.current) innerControlRef.current.indeterminate = Boolean(indeterminate);
	}, [indeterminate]);
	const handleChange = (event) => {
		setCheckedState(event.target.checked);
		if (onChange) onChange(event);
	};
	const handleClickCapture = import_react.useCallback((event) => {
		eventBroker.publish({
			componentId: "Checkbox",
			eventId: "click",
			domEvent: event,
			meta: { checked: event.target.checked }
		});
	}, []);
	return {
		checked: isChecked,
		inputProps: {
			...controlProps,
			name,
			value,
			id,
			onFocus,
			onBlur,
			disabled,
			type: "checkbox",
			onChange: handleChange,
			onClickCapture: handleClickCapture,
			defaultChecked,
			checked: inputChecked,
			ref: handleRef
		}
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useHover/useHover.js
function useHover() {
	const [isHovering, setIsHovering] = import_react.useState(false);
	return [
		import_react.useCallback(() => {
			setIsHovering(true);
		}, []),
		import_react.useCallback(() => {
			setIsHovering(false);
		}, []),
		isHovering
	];
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useCloseOnTimeout/useCloseOnTimeout.js
/**
* Invokes callback after given amount of time unless mouse is on the element
* @param onClose
* @param timeout
* @returns mouse event handlers
*/
function useCloseOnTimeout({ onClose, timeout }) {
	const [onMouseOver, onMouseLeave, isHovering] = useHover();
	useTimeout(onClose, isHovering ? null : timeout);
	return {
		onMouseOver,
		onMouseLeave
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/typeCheckers.js
var isFunction = (value) => typeof value === "function";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useStateWithCallback/useStateWithCallback.js
function useStateWithCallback(initialValue, callback) {
	const [state, setState] = import_react.useState(initialValue);
	return [state, import_react.useCallback((nextValue) => {
		if (isFunction(nextValue)) setState((previousState) => {
			const newState = nextValue(previousState);
			callback?.(newState);
			return newState;
		});
		else {
			callback?.(nextValue);
			setState(nextValue);
		}
	}, [callback])];
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useConditionallyControlledState/useConditionallyControlledState.js
function useConditionallyControlledState(property, setProperty, initialState, isControlled = property !== void 0 && setProperty !== void 0) {
	const state = useStateWithCallback(property || initialState, setProperty);
	if (isControlled) return [property, setProperty];
	return state;
}
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/throttle.js
var require_throttle = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var debounce = require_debounce();
	var isObject = require_isObject();
	/** Error message constants. */
	var FUNC_ERROR_TEXT = "Expected a function";
	/**
	* Creates a throttled function that only invokes `func` at most once per
	* every `wait` milliseconds. The throttled function comes with a `cancel`
	* method to cancel delayed `func` invocations and a `flush` method to
	* immediately invoke them. Provide `options` to indicate whether `func`
	* should be invoked on the leading and/or trailing edge of the `wait`
	* timeout. The `func` is invoked with the last arguments provided to the
	* throttled function. Subsequent calls to the throttled function return the
	* result of the last `func` invocation.
	*
	* **Note:** If `leading` and `trailing` options are `true`, `func` is
	* invoked on the trailing edge of the timeout only if the throttled function
	* is invoked more than once during the `wait` timeout.
	*
	* If `wait` is `0` and `leading` is `false`, `func` invocation is deferred
	* until to the next tick, similar to `setTimeout` with a timeout of `0`.
	*
	* See [David Corbacho's article](https://css-tricks.com/debouncing-throttling-explained-examples/)
	* for details over the differences between `_.throttle` and `_.debounce`.
	*
	* @static
	* @memberOf _
	* @since 0.1.0
	* @category Function
	* @param {Function} func The function to throttle.
	* @param {number} [wait=0] The number of milliseconds to throttle invocations to.
	* @param {Object} [options={}] The options object.
	* @param {boolean} [options.leading=true]
	*  Specify invoking on the leading edge of the timeout.
	* @param {boolean} [options.trailing=true]
	*  Specify invoking on the trailing edge of the timeout.
	* @returns {Function} Returns the new throttled function.
	* @example
	*
	* // Avoid excessively updating the position while scrolling.
	* jQuery(window).on('scroll', _.throttle(updatePosition, 100));
	*
	* // Invoke `renewToken` when the click event is fired, but not more than once every 5 minutes.
	* var throttled = _.throttle(renewToken, 300000, { 'trailing': false });
	* jQuery(element).on('click', throttled);
	*
	* // Cancel the trailing throttled invocation.
	* jQuery(window).on('popstate', throttled.cancel);
	*/
	function throttle(func, wait, options) {
		var leading = true, trailing = true;
		if (typeof func != "function") throw new TypeError(FUNC_ERROR_TEXT);
		if (isObject(options)) {
			leading = "leading" in options ? !!options.leading : leading;
			trailing = "trailing" in options ? !!options.trailing : trailing;
		}
		return debounce(func, wait, {
			"leading": leading,
			"maxWait": wait,
			"trailing": trailing
		});
	}
	module.exports = throttle;
}));
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useElementSize/useElementSize.js
var import_throttle = /* @__PURE__ */ __toESM(require_throttle(), 1);
var RESIZE_THROTTLE = 16;
var ROUND_PRECISION = 2;
function useElementSize(ref, key) {
	const [size, setSize] = import_react.useState({
		width: 0,
		height: 0
	});
	useLayoutEffect$4(() => {
		const element = ref?.current;
		if (!element) return;
		setSize({
			width: (0, import_round.default)(element.offsetWidth, ROUND_PRECISION),
			height: (0, import_round.default)(element.offsetHeight, ROUND_PRECISION)
		});
		const handleResize = (entries) => {
			if (!Array.isArray(entries)) return;
			const entry = entries[0];
			if (entry.borderBoxSize) {
				const borderBoxSize = entry.borderBoxSize[0] ? entry.borderBoxSize[0] : entry.borderBoxSize;
				setSize({
					width: (0, import_round.default)(borderBoxSize.inlineSize, ROUND_PRECISION),
					height: (0, import_round.default)(borderBoxSize.blockSize, ROUND_PRECISION)
				});
			} else {
				const target = entry.target;
				setSize({
					width: (0, import_round.default)(target.offsetWidth, ROUND_PRECISION),
					height: (0, import_round.default)(target.offsetHeight, ROUND_PRECISION)
				});
			}
		};
		const observer = new ResizeObserver((0, import_throttle.default)(handleResize, RESIZE_THROTTLE));
		observer.observe(element);
		return () => {
			observer.disconnect();
		};
	}, [ref, key]);
	return size;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useRadio/useRadio.js
function useRadio({ name, value, checked, defaultChecked, disabled, controlRef, controlProps, onUpdate, onChange, onFocus, onBlur, id }) {
	const controlId = useUniqId();
	const innerControlRef = import_react.useRef(null);
	const [isChecked, setCheckedState] = useControlledState(checked, defaultChecked ?? false, onUpdate);
	const handleRef = useForkRef(controlRef, innerControlRef, useFormResetHandler({
		initialValue: isChecked,
		onReset: setCheckedState
	}));
	const handleChange = (event) => {
		setCheckedState(event.target.checked);
		if (onChange) onChange(event);
	};
	const onChangeCapture = (event) => {
		eventBroker.publish({
			componentId: "Radio",
			eventId: "click",
			domEvent: event
		});
	};
	return {
		checked: isChecked,
		inputProps: {
			...controlProps,
			name: name || controlId,
			value,
			id,
			onFocus,
			onBlur,
			disabled,
			type: "radio",
			onChange: handleChange,
			onChangeCapture,
			checked,
			defaultChecked,
			ref: handleRef
		}
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useRadioGroup/useRadioGroup.js
function useRadioGroup(props) {
	const { name, value, defaultValue, options = [], disabled, onUpdate, onChange, onFocus, onBlur } = props;
	const controlId = useUniqId();
	const [currentValue, setValueState] = useControlledState(value, defaultValue ?? null, onUpdate);
	const fieldRef = useFormResetHandler({
		initialValue: currentValue,
		onReset: setValueState
	});
	const { focusWithinProps } = useFocusWithin({
		onFocusWithin: onFocus,
		onBlurWithin: onBlur
	});
	const handleChange = import_react.useCallback((event) => {
		setValueState(event.target.value);
		if (onChange) onChange(event);
	}, [onChange, setValueState]);
	const contextProps = import_react.useMemo(() => ({
		name: name || controlId,
		currentValue,
		disabled: Boolean(disabled),
		ref: fieldRef,
		onChange: handleChange
	}), [
		controlId,
		currentValue,
		disabled,
		fieldRef,
		handleChange,
		name
	]);
	return {
		containerProps: {
			...filterDOMProps(props, { labelable: true }),
			...focusWithinProps,
			role: "radiogroup",
			"aria-disabled": disabled
		},
		optionsProps: options.map((option) => ({
			name: name || controlId,
			value: option.value,
			content: option.content,
			title: option.title,
			checked: currentValue === String(option.value),
			disabled: disabled || option.disabled,
			onChange: handleChange,
			ref: fieldRef
		})),
		contextProps
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/hooks/private/useRadioGroup/RadioGroupContext.js
var RadioGroupContext = import_react.createContext(null);
RadioGroupContext.displayName = "RadioGroup.Context";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/hooks/usePopupVisibility.js
function usePopupVisibility(visible, onChangeVisibility, disabled) {
	const [isPopupShown, setPopupShown] = useConditionallyControlledState(visible, onChangeVisibility, false);
	const togglePopup = import_react.useCallback((open) => {
		setPopupShown((isShown) => {
			if (typeof open === "boolean") return open;
			return !isShown;
		});
	}, [setPopupShown]);
	const closePopup = import_react.useCallback(() => {
		setPopupShown(false);
	}, [setPopupShown]);
	import_react.useEffect(() => {
		if (disabled && isPopupShown) closePopup();
	}, [
		closePopup,
		disabled,
		isPopupShown
	]);
	return {
		isPopupShown,
		togglePopup,
		closePopup
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/hooks/useScrollHandler.js
function useScrollHandler(onScroll, anchorRef, disabled) {
	import_react.useEffect(() => {
		if (disabled) return;
		const handleScroll = (event) => {
			if (event.target.contains(anchorRef.current)) onScroll(event);
		};
		document.addEventListener("scroll", handleScroll, true);
		return () => {
			document.removeEventListener("scroll", handleScroll, true);
		};
	}, [
		anchorRef,
		onScroll,
		disabled
	]);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/utils/toItemList.js
function toItemList(items, separator, path = [], startIndex = 0) {
	const updatedItems = [];
	let addedGroup = false;
	let index = startIndex;
	for (const item of items) if (Array.isArray(item)) {
		const groupItems = toItemList(item, separator, path, index);
		if (updatedItems.length !== 0 && groupItems.length !== 0) updatedItems.push(separator);
		updatedItems.push(...groupItems);
		index += groupItems.length;
		addedGroup = true;
	} else {
		if (item.hidden) continue;
		if (addedGroup) updatedItems.push(separator);
		const updatedItem = {
			...item,
			path: [...path, index++]
		};
		if (item.items) updatedItem.items = toItemList(item.items, separator, updatedItem.path);
		updatedItems.push(updatedItem);
		addedGroup = false;
	}
	return updatedItems;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DropdownMenu/DropdownMenu.js
var DropdownMenu = (rawProps) => {
	const { items = [], size = "m", icon = (0, import_jsx_runtime.jsx)(Icon, { data: Ellipsis }), open, onOpenToggle, hideOnScroll = true, data, disabled, switcher, renderSwitcher, switcherWrapperClassName, defaultSwitcherProps, defaultSwitcherClassName, onSwitcherClick, menuProps, popupProps, children } = useDefaultProps("DropdownMenu", rawProps);
	const anchorRef = import_react.useRef(null);
	const { isPopupShown, togglePopup, closePopup } = usePopupVisibility(open, onOpenToggle, disabled);
	useScrollHandler(closePopup, anchorRef, !isPopupShown || !hideOnScroll);
	const contextValue = import_react.useMemo(() => ({
		toggle: togglePopup,
		data
	}), [data, togglePopup]);
	const itemsList = import_react.useMemo(() => toItemList(items, dropdownMenuSeparator), [items]);
	const handleSwitcherClick = import_react.useCallback((event) => {
		if (disabled) return;
		onSwitcherClick?.(event);
		togglePopup();
	}, [
		disabled,
		onSwitcherClick,
		togglePopup
	]);
	const { onKeyDown: handleSwitcherKeyDown } = useActionHandlers(handleSwitcherClick);
	const switcherProps = import_react.useMemo(() => ({
		onClick: handleSwitcherClick,
		onKeyDown: handleSwitcherKeyDown
	}), [handleSwitcherClick, handleSwitcherKeyDown]);
	return (0, import_jsx_runtime.jsxs)(DropdownMenuContext.Provider, {
		value: contextValue,
		children: [(0, import_jsx_runtime.jsx)("div", {
			ref: anchorRef,
			className: cnDropdownMenu("switcher-wrapper", switcherWrapperClassName),
			...renderSwitcher ? {} : switcherProps,
			children: renderSwitcher?.(switcherProps) || switcher || (0, import_jsx_runtime.jsx)(Button, {
				view: "flat",
				size,
				...defaultSwitcherProps,
				className: cnDropdownMenu("switcher-button", defaultSwitcherClassName),
				disabled,
				children: icon
			})
		}), (0, import_jsx_runtime.jsx)(DropdownMenuNavigationContextProvider, {
			anchorRef,
			disabled: !isPopupShown,
			children: (0, import_jsx_runtime.jsx)(DropdownMenuPopup, {
				items: itemsList,
				open: isPopupShown,
				size,
				menuProps,
				anchorRef,
				onClose: closePopup,
				popupProps,
				children
			})
		})]
	});
};
var DropdownMenuExport = Object.assign(DropdownMenu, { Item: DropdownMenuItem });
var i18n_default$19 = addComponentKeysets({
	en: {
		label_close: "Close",
		label_more: "Show more"
	},
	ru: {
		label_close: "Закрыть",
		label_more: "Показать больше"
	}
}, "ActionsPanel");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ActionsPanel/components/hooks/useDropdownActions.js
var useDropdownActions = ({ buttonActions, restActions, visibilityMap }) => {
	const actions = [...buttonActions.filter((action) => !visibilityMap[action.id]), ...restActions];
	const groups = (0, import_groupBy.default)(actions, (action) => action.dropdown.group);
	const usedGroups = /* @__PURE__ */ new Set();
	const dropdownItems = [];
	for (const action of actions) {
		const group = action.dropdown.group;
		if (typeof group === "undefined") {
			dropdownItems.push(action.dropdown.item);
			continue;
		}
		if (usedGroups.has(group)) continue;
		usedGroups.add(group);
		dropdownItems.push(groups[group].map((groupedAction) => groupedAction.dropdown.item));
	}
	return dropdownItems;
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ActionsPanel/components/hooks/useObserveIntersection.js
var OBSERVER_TARGET_ATTR = "data-observer-id";
var GAP = 4;
var useObserveIntersection = (updateObserveKey) => {
	const direction = useDirection();
	const parentRef = import_react.useRef(null);
	const [visibilityMap, setVisibilityMap] = import_react.useState({});
	const [offset, setOffset] = import_react.useState(0);
	const handleIntersection = import_react.useCallback((entries) => {
		const updatedEntries = {};
		let newOffest = 0;
		let lastVisibleEntry;
		let firstInvisible;
		entries.forEach((entry) => {
			const targetId = entry.target.getAttribute(OBSERVER_TARGET_ATTR);
			if (!targetId) return;
			if (entry.isIntersecting) {
				lastVisibleEntry = entry;
				updatedEntries[targetId] = true;
			} else {
				if (!firstInvisible) firstInvisible = entry;
				updatedEntries[targetId] = false;
			}
		});
		const parentRect = parentRef.current?.getBoundingClientRect();
		if (parentRect && firstInvisible) {
			const rect = firstInvisible.target.getBoundingClientRect();
			newOffest = direction === "ltr" ? rect.left - parentRect.left : parentRect.right - rect.right;
		} else if (parentRect && lastVisibleEntry) {
			const rect = lastVisibleEntry.target.getBoundingClientRect();
			newOffest = direction === "ltr" ? rect.right - parentRect.left + GAP : parentRect.right - rect.left + GAP;
		}
		setVisibilityMap((prev) => ({
			...prev,
			...updatedEntries
		}));
		setOffset(newOffest);
	}, [direction]);
	import_react.useEffect(() => {
		setVisibilityMap({});
		const observer = new IntersectionObserver(handleIntersection, {
			root: parentRef.current,
			threshold: 1
		});
		Array.from(parentRef.current?.children || []).forEach((item) => {
			if (item.hasAttribute("data-observer-id")) observer.observe(item);
		});
		return () => observer.disconnect();
	}, [handleIntersection, updateObserveKey]);
	return {
		parentRef,
		visibilityMap,
		offset
	};
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ActionsPanel/components/hooks/useCollapseActions.js
var DEFAULT_MAX_BUTTON_ACTIONS = 4;
var useCollapseActions = (actions, maxRowActions) => {
	const maxActions = Math.max(0, typeof maxRowActions === "undefined" ? DEFAULT_MAX_BUTTON_ACTIONS : maxRowActions);
	const allActionsCollapsed = import_react.useMemo(() => {
		return actions.every((action) => action.collapsed);
	}, [actions]);
	const updateObserveKey = import_react.useMemo(() => actions.map(({ id }) => id).join("/") + maxActions, [actions, maxActions]);
	const [buttonActions, restActions] = import_react.useMemo(() => {
		const buttonItems = [];
		const restItems = [];
		actions.forEach((action) => {
			if (buttonItems.length < maxActions && !action.collapsed) buttonItems.push(action);
			else restItems.push(action);
		});
		return [buttonItems, restItems];
	}, [actions, maxActions]);
	const { parentRef, visibilityMap, offset } = useObserveIntersection(updateObserveKey);
	const dropdownItems = useDropdownActions({
		buttonActions,
		restActions,
		visibilityMap
	});
	const isDefaultOffset = allActionsCollapsed || maxActions === 0;
	const showDropdown = (Object.keys(visibilityMap).length > 0 || isDefaultOffset) && dropdownItems.length > 0;
	return {
		buttonActions,
		dropdownItems,
		parentRef,
		offset: isDefaultOffset ? 0 : offset,
		visibilityMap,
		showDropdown
	};
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ActionsPanel/components/CollapseActions.js
var b$96 = block("actions-panel-collapse");
var CollapseActions = ({ actions, maxRowActions }) => {
	const { buttonActions, dropdownItems, parentRef, offset, visibilityMap, showDropdown } = useCollapseActions(actions, maxRowActions);
	const { t } = i18n_default$19.useTranslation();
	return (0, import_jsx_runtime.jsxs)("div", {
		className: b$96(),
		children: [(0, import_jsx_runtime.jsx)("div", {
			className: b$96("container"),
			ref: parentRef,
			children: buttonActions.map((action) => {
				const { id } = action;
				const attr = { [OBSERVER_TARGET_ATTR]: id };
				const invisible = visibilityMap[id] === false;
				const node = Array.isArray(action.dropdown.item.items) ? (0, import_jsx_runtime.jsx)(DropdownMenuExport, {
					size: "s",
					items: action.dropdown.item.items,
					renderSwitcher: ({ onClick }) => (0, import_jsx_runtime.jsx)(Button, {
						view: "flat-contrast",
						size: "m",
						...action.button.props,
						onClick
					}),
					onSwitcherClick: action.button.props.onClick
				}) : (0, import_jsx_runtime.jsx)(Button, {
					view: "flat-contrast",
					size: "m",
					...action.button.props
				});
				return (0, import_react.createElement)("div", {
					className: b$96("button-action-wrapper", { invisible }),
					...attr,
					key: id
				}, node);
			})
		}), showDropdown && (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [(0, import_jsx_runtime.jsx)("div", { className: b$96("menu-placeholder") }), (0, import_jsx_runtime.jsx)("div", {
			className: b$96("menu-wrapper"),
			style: { insetInlineStart: offset },
			children: (0, import_jsx_runtime.jsx)(DropdownMenuExport, {
				size: "s",
				items: dropdownItems,
				renderSwitcher: ({ onClick }) => (0, import_jsx_runtime.jsx)(Button, {
					view: "flat-contrast",
					size: "m",
					"aria-label": t("label_more"),
					onClick,
					children: (0, import_jsx_runtime.jsx)(Icon, { data: Ellipsis })
				})
			})
		})] })]
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ActionsPanel/ActionsPanel.js
var b$95 = block("actions-panel");
var ActionsPanel = (rawProps) => {
	const { className, actions, onClose, renderNote, noteClassName, qa, maxRowActions } = useDefaultProps("ActionsPanel", rawProps);
	const { t } = i18n_default$19.useTranslation();
	return (0, import_jsx_runtime.jsxs)("div", {
		className: b$95(null, className),
		"data-qa": qa,
		children: [
			typeof renderNote === "function" && (0, import_jsx_runtime.jsx)(Text, {
				className: b$95("note-wrapper", noteClassName),
				as: "div",
				color: "light-primary",
				variant: "subheader-2",
				ellipsis: true,
				children: renderNote()
			}),
			(0, import_jsx_runtime.jsx)(CollapseActions, {
				actions,
				maxRowActions
			}),
			typeof onClose === "function" && (0, import_jsx_runtime.jsx)(Button, {
				view: "flat-contrast",
				size: "m",
				onClick: onClose,
				className: b$95("button-close"),
				"aria-label": t("label_close"),
				children: (0, import_jsx_runtime.jsx)(Icon, { data: Xmark }, "icon")
			})
		]
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/utils/index.js
var mediaByOrder = {
	xs: 0,
	s: 1,
	m: 2,
	l: 3,
	xl: 4,
	xxl: 5,
	xxxl: 6
};
var isMediaActiveFactory = (activeType) => (toCheck) => {
	return activeType in mediaByOrder ? mediaByOrder[activeType] - mediaByOrder[toCheck] >= 0 : false;
};
var mediaOrder = [
	"xs",
	"s",
	"m",
	"l",
	"xl",
	"xxl",
	"xxxl"
];
var getClosestMediaPropsFactory = (currentActive) => (medias = {}) => {
	if (!currentActive) return;
	let candidate = currentActive;
	while (candidate) {
		if (typeof medias[candidate] !== "undefined") return medias[candidate];
		candidate = mediaOrder[mediaByOrder[candidate] - 1];
	}
};
var makeCssMod = (space) => {
	return space in CSS_SIZE_EXCEPTION ? CSS_SIZE_EXCEPTION[space] : String(space);
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/hooks/useLayoutContext.js
/**
* Quick access to theme and helpers to work with media queries
* ---
* Storybook - https://preview.gravity-ui.com/uikit/?path=/docs/layout--playground#uselayoutcontext
*/
var useLayoutContext = () => {
	const { activeMediaQuery, theme } = import_react.useContext(LayoutContext);
	const { isMediaActive, getClosestMediaProps } = import_react.useMemo(() => ({
		isMediaActive: isMediaActiveFactory(activeMediaQuery),
		getClosestMediaProps: getClosestMediaPropsFactory(activeMediaQuery)
	}), [activeMediaQuery]);
	return {
		theme,
		activeMediaQuery,
		isMediaActive,
		getClosestMediaProps
	};
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/Col/Col.js
var b$94 = block("col");
/**
* How many columns of you 12-th column layout will take content.
* Must be used as a child of `Row` component.
*
* By default, component takes all available space.
* If you want to specify static size use `size` prop.
*
* ```tsx
* <Col size="6">some content</Col>
* ```
* If you want responsive column use provide media sizes.
*
* ```tsx
* <Col size={[12, {m: 6}]}>some content</Col>
* ```
* ---
*
* Note: you can use empty <Col/> component for spacing:
*
* ```tsx
* <Row>
*   <Col size="4">col 1</Col>
*   <Col/>
*   <Col size="4">col 2</Col>
* </Row>
* ```
* ---
* Storybook - https://preview.gravity-ui.com/uikit/?path=/docs/components-layout--docs#col
*/
var Col = ({ size, children, style, className, qa, ...mediaConfigProp }) => {
	const { getClosestMediaProps } = useLayoutContext();
	let mediaConfig;
	let defaultSizeMod;
	if (Array.isArray(size)) [defaultSizeMod, mediaConfig] = size;
	else if (typeof size === "object") mediaConfig = size || mediaConfigProp;
	else {
		defaultSizeMod = size;
		mediaConfig = mediaConfigProp;
	}
	const sizeModValue = getClosestMediaProps(mediaConfig);
	return (0, import_jsx_runtime.jsx)("div", {
		style,
		className: b$94({ size: typeof sizeModValue === "undefined" ? defaultSizeMod : makeCssMod(sizeModValue) }, className),
		"data-qa": qa,
		children
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/Row/Row.js
var b$93 = block("row");
/**
* Defines the margins between columns (`<Col />`).
*
* Required to use with `<Col />` component
*
* ```tsx
* import {Row, Col} from '@gravity-ui/uikit';
*
* <Row space="5">
*  <Col>col</Col>
*  <Col>col</Col>
* </Row>
* ```
* ---
* Storybook - https://preview.gravity-ui.com/uikit/?path=/docs/components-layout--docs#row
*/
var Row = ({ children, style, className, space, spaceRow, qa }) => {
	const { getClosestMediaProps } = useLayoutContext();
	let s;
	let sr;
	if (typeof space === "object") {
		const res = getClosestMediaProps(space);
		if (typeof res !== "undefined") s = makeCssMod(res);
	} else if (typeof space !== "undefined") s = makeCssMod(space);
	if (typeof spaceRow === "object") {
		const res = getClosestMediaProps(spaceRow);
		if (typeof res !== "undefined") sr = makeCssMod(res);
	} else if (typeof spaceRow !== "undefined") sr = String(spaceRow);
	return (0, import_jsx_runtime.jsx)("div", {
		style,
		className: b$93({
			s,
			sr
		}, className),
		"data-qa": qa,
		children
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/spacing/spacing.js
var b$92 = block("s");
/**
* Utility to generate predefined css classes to describe position between components
* ---
* Storybook - https://preview.gravity-ui.com/uikit/?path=/docs/layout--playground#spacing-utility
*/
var spacing = (props, className) => {
	const classes = [];
	for (const key in props) if (Object.prototype.hasOwnProperty.call(props, key)) {
		const value = props[key];
		if (typeof value !== "undefined") classes.push(b$92(`${key}_${makeCssMod(value)}`));
	}
	if (className) classes.push(className);
	return classes.join(" ");
};
var sp = spacing;
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/Box/Box.js
var import_debounce = /* @__PURE__ */ __toESM(require_debounce(), 1);
var b$91 = block("box");
/**
* Basic block to build other components and for standalone usage as a smart block with build in support of most usable css properties and shortcut `spacing` properties.
* ```tsx
* <Box width={300} height={200} spacing={{mb: 2}}>
*      some content
* </Box>
* <Box width={300} height={200} >
*      some content
* </Box>
* ```
*/
var Box = import_react.forwardRef(function Box({ as, children, qa, className, width, height, minWidth, minHeight, maxHeight, maxWidth, position, style: outerStyle, spacing, overflow, ...props }, ref) {
	const Tag = as || "div";
	const style = {
		width,
		height,
		minWidth,
		minHeight,
		maxHeight,
		maxWidth,
		position,
		...outerStyle
	};
	return (0, import_jsx_runtime.jsx)(Tag, {
		...props,
		"data-qa": qa,
		style,
		ref,
		className: b$91({ overflow }, spacing ? sp(spacing, className) : className),
		children
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/Flex/Flex.js
var b$90 = block("flex");
/**
* Flexbox model utility component.
*
* ```tsx
* import {Flex, Button} from '@gravity-ui/uikit';
*
* <Flex
*  // take value from theme depends of current media query
*  space
* >
*  <Button>
*      Button 1
*  </Button>
*  <Button>
*      Button 2
*  </Button>
* </Flex>
* ```
*
* Use build in media goods via props
*
* ```tsx
* <Flex
*  // space dynamically changes instead of current media query
*  space={{s: '1', m: '5'}}
*  // `flex-direction: column` will be applied to `l`, 'xl', 'xxl' and `xxxl` media queries
*  direction={{'s': 'column', 'm': 'row'}}
* >
*  {...}
* </Flex>
* ```
* ---
* Storybook - https://preview.gravity-ui.com/uikit/?path=/docs/layout--playground#flex
*/
var Flex = import_react.forwardRef(function Flex(props, ref) {
	const { as: propsAs, direction, grow, basis, children, style, alignContent, alignItems, alignSelf, justifyContent, justifyItems, justifySelf, shrink, wrap, inline, gap, gapRow, className, space, centerContent, ...restProps } = props;
	const as = propsAs || "div";
	const { getClosestMediaProps, theme: { spaceBaseSize } } = useLayoutContext();
	const applyMediaProps = (property) => typeof property === "object" && property !== null ? getClosestMediaProps(property) : property;
	const gapSpaceSize = applyMediaProps(gap);
	const columnGap = typeof gapSpaceSize === "undefined" ? void 0 : spaceBaseSize * Number(gapSpaceSize);
	const gapRowSpaceSize = applyMediaProps(gapRow) || gapSpaceSize;
	const rowGap = typeof gapRowSpaceSize === "undefined" ? void 0 : spaceBaseSize * Number(gapRowSpaceSize);
	const spaceSize = applyMediaProps(space);
	const s = typeof gap === "undefined" && typeof gapRow === "undefined" && typeof spaceSize !== "undefined" ? makeCssMod(spaceSize) : void 0;
	return (0, import_jsx_runtime.jsx)(Box, {
		as,
		className: b$90({
			"center-content": centerContent,
			inline,
			s
		}, className),
		ref,
		style: {
			flexDirection: applyMediaProps(direction),
			flexGrow: grow === true ? 1 : grow,
			flexWrap: wrap === true ? "wrap" : wrap,
			flexBasis: basis,
			flexShrink: shrink,
			columnGap,
			rowGap,
			alignContent: applyMediaProps(alignContent),
			alignItems: applyMediaProps(alignItems),
			alignSelf: applyMediaProps(alignSelf),
			justifyContent: applyMediaProps(justifyContent),
			justifyItems: applyMediaProps(justifyItems),
			justifySelf: applyMediaProps(justifySelf),
			...style
		},
		...restProps,
		children: space ? import_react.Children.map(children, (child) => child ? (0, import_jsx_runtime.jsx)("div", {
			className: b$90("wr"),
			children: child
		}) : child) : children
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/Container/useContainerThemeProps.js
var pickContainerProps = ({ gutters, spaceRow, space } = {}) => {
	const res = {};
	if (gutters) res.gutters = gutters;
	if (spaceRow || space) res.spaceRow = spaceRow || space;
	return res;
};
var useContainerThemeProps = () => {
	const { theme, getClosestMediaProps } = useLayoutContext();
	return {
		getClosestMediaProps,
		containerThemeProps: import_react.useMemo(() => ({
			...pickContainerProps(theme.components?.container),
			...pickContainerProps(getClosestMediaProps(theme.components?.container?.media))
		}), [getClosestMediaProps, theme]),
		breakpoints: theme.breakpoints
	};
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/layout/Container/Container.js
var b$89 = block("container");
/**
* Center you content in horizontal direction.
*
* > In most cases must be one on the page.
*
* ```tsx
* import {Container, Row, Col} from '@gravity-ui/uikit';
*
* <Container masWidth="m">
*   <Row>
*     <Col>
*       Col 1
*    </Col>
*    <Col>
*       Col 2
*    </Col>
*  </Row>
* </Container>
* ```
* ---
* Storybook - https://preview.gravity-ui.com/uikit/?path=/docs/layout--playground#container
*/
var Container = ({ children, style: propsStyle, as: Tag = "div", className, maxWidth, gutters, spaceRow, qa }) => {
	const { getClosestMediaProps, containerThemeProps, breakpoints } = useContainerThemeProps();
	const style = {
		...maxWidth ? { maxWidth: breakpoints[maxWidth] } : {},
		...propsStyle
	};
	let sr;
	if (typeof spaceRow === "object") {
		const propsCandidate = getClosestMediaProps(spaceRow);
		if (propsCandidate) sr = makeCssMod(propsCandidate);
	} else if (typeof spaceRow !== "undefined") sr = makeCssMod(spaceRow);
	return (0, import_jsx_runtime.jsx)(Tag, {
		style,
		className: b$89({ sr }, gutters === false ? className : sp({ px: gutters ?? containerThemeProps.gutters }, className)),
		"data-qa": qa,
		children
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Card/Card.js
var b$88 = block("card");
var roles = {
	selection: "radio",
	action: "button",
	container: void 0
};
var Card = import_react.forwardRef(function Card(rawProps, ref) {
	const { type = "container", theme, view, size = "m", children, className, onClick, disabled, selected, ...restProps } = useDefaultProps("Card", rawProps);
	const isTypeAction = type === "action";
	const isTypeSelection = type === "selection";
	const isTypeContainer = type === "container";
	const isClickable = (isTypeAction || isTypeSelection) && Boolean(onClick) && !disabled;
	const defaultTheme = isTypeContainer ? "normal" : void 0;
	const defaultView = isTypeContainer || isTypeSelection ? "outlined" : void 0;
	const handleClick = isClickable ? onClick : void 0;
	const { onKeyDown } = useActionHandlers(onClick);
	return (0, import_jsx_runtime.jsx)(Box, {
		ref,
		role: roles[type],
		className: b$88({
			theme: theme || defaultTheme,
			view: view || defaultView,
			type,
			selected,
			size,
			disabled,
			clickable: isClickable
		}, className),
		onClick: handleClick,
		onKeyDown: isClickable ? onKeyDown : void 0,
		tabIndex: isClickable ? 0 : void 0,
		"aria-checked": isTypeSelection ? selected : void 0,
		...restProps,
		children
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Alert/AlertContext.js
var AlertContext = import_react.createContext(null);
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Alert/useAlertContext.js
var useAlertContext = () => {
	const context = import_react.useContext(AlertContext);
	if (!context) throw new Error("Alert: `useAlertContext` hook is used out of \"AlertContext\"");
	return context;
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Alert/AlertAction.js
function alertSizeToButtonSize(alertSize) {
	switch (alertSize) {
		case "s":
		case "m": return "m";
		default: return "l";
	}
}
var AlertAction = (props) => {
	const { view, size } = useAlertContext();
	return (0, import_jsx_runtime.jsx)(Button, {
		view: view === "filled" ? "normal-contrast" : void 0,
		size: alertSizeToButtonSize(size),
		...props
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Alert/constants.js
var bAlert = block("alert");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Alert/AlertActions.js
var AlertActions = ({ items, children, className }) => {
	const { actionsLayout } = useAlertContext();
	return (0, import_jsx_runtime.jsx)("div", {
		className: bAlert("actions", { [actionsLayout]: true }, className),
		children: items?.map(({ handler, text }, i) => (0, import_jsx_runtime.jsx)(AlertAction, {
			onClick: handler,
			children: text
		}, i)) || children
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Alert/AlertContextProvider.js
var AlertContextProvider = ({ layout, actionsLayout, view, size, children }) => {
	return (0, import_jsx_runtime.jsx)(AlertContext.Provider, {
		value: {
			layout,
			actionsLayout,
			view,
			size
		},
		children
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Alert/AlertIcon.js
var typeToIcon = {
	danger: {
		filled: CircleXmarkFill,
		outlined: CircleXmark
	},
	info: {
		filled: CircleInfoFill,
		outlined: CircleInfo
	},
	success: {
		filled: CircleCheckFill,
		outlined: CircleCheck
	},
	warning: {
		filled: TriangleExclamationFill,
		outlined: TriangleExclamation
	},
	utility: {
		filled: ThunderboltFill,
		outlined: Thunderbolt
	},
	normal: null,
	clear: null
};
var AlertIcon = ({ className, theme, align, view = "filled", size = 18 }) => {
	const iconByTheme = typeToIcon[theme];
	if (!iconByTheme) return null;
	let color;
	if (theme === "success") color = "positive";
	else if (theme !== "normal" && theme !== "clear") color = theme;
	return (0, import_jsx_runtime.jsx)("div", {
		className: bAlert("icon-wrapper", { align }, colorText({ color }, className)),
		children: (0, import_jsx_runtime.jsx)(Icon, {
			className: bAlert("icon"),
			data: iconByTheme[view],
			size
		})
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Alert/AlertTitle.js
var AlertTitle = ({ text, className }) => {
	return (0, import_jsx_runtime.jsx)("div", {
		className: bAlert("title", className),
		children: text
	});
};
var i18n_default$18 = addComponentKeysets({
	en: { label_close: "Close" },
	ru: { label_close: "Закрыть" }
}, "Alert");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Alert/Alert.js
function alertSizeToIconSize(alertSize) {
	switch (alertSize) {
		case "s": return 16;
		case "m": return 18;
		default: return 22;
	}
}
var Alert = (rawProps) => {
	const props = useDefaultProps("Alert", rawProps);
	const { theme = "normal", view = "filled", size = "m", layout = "vertical", actionsLayout = layout === "vertical" ? "horizontal" : "vertical", message, className, corners, style, onClose, align = "baseline", qa } = props;
	const commonProps = {
		style,
		className: bAlert({
			corners,
			size,
			align
		}, className),
		qa
	};
	const { t } = i18n_default$18.useTranslation();
	const content = (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [
		typeof props.icon === "undefined" ? (0, import_jsx_runtime.jsx)(Alert.Icon, {
			theme,
			view,
			align,
			size: alertSizeToIconSize(size)
		}) : props.icon && (0, import_jsx_runtime.jsx)("div", {
			className: bAlert("icon-wrapper", { align }),
			children: props.icon
		}),
		(0, import_jsx_runtime.jsxs)("div", {
			className: bAlert("main", { layout }),
			children: [(0, import_jsx_runtime.jsxs)("div", { children: [typeof props.title === "string" ? (0, import_jsx_runtime.jsx)(Alert.Title, { text: props.title }) : props.title, message ? (0, import_jsx_runtime.jsx)("div", {
				className: bAlert("message", { "with-top-margin": Boolean(props.title) }),
				children: message
			}) : null] }), Array.isArray(props.actions) ? (0, import_jsx_runtime.jsx)(Alert.Actions, { items: props.actions }) : props.actions]
		}),
		onClose && (0, import_jsx_runtime.jsx)(Button, {
			view: "flat",
			className: bAlert("close-btn"),
			onClick: onClose,
			"aria-label": t("label_close"),
			size,
			children: (0, import_jsx_runtime.jsx)(Icon, {
				data: Xmark,
				size: 18,
				className: colorText({ color: "secondary" })
			})
		})
	] });
	return (0, import_jsx_runtime.jsx)(AlertContextProvider, {
		layout,
		view,
		size,
		actionsLayout,
		children: theme === "clear" ? (0, import_jsx_runtime.jsx)("div", {
			...commonProps,
			children: content
		}) : (0, import_jsx_runtime.jsx)(Card, {
			...commonProps,
			theme,
			view,
			children: content
		})
	});
};
Alert.Icon = AlertIcon;
Alert.Title = AlertTitle;
Alert.Actions = AlertActions;
Alert.Action = AlertAction;
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Avatar/constants.js
var AVATAR_SIZES = {
	"3xs": 16,
	"2xs": 20,
	xs: 24,
	s: 28,
	m: 32,
	l: 42,
	xl: 50
};
var DEFAULT_AVATAR_SIZE = "m";
var bAvatar = block("avatar");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Avatar/AvatarIcon/AvatarIcon.js
var avatarSizeToIconSize = {
	"3xs": 10,
	"2xs": 12,
	xs: 14,
	s: 16,
	m: 16,
	l: 20,
	xl: 24
};
var AvatarIcon = ({ icon, color, size }) => {
	const style = { color };
	return (0, import_jsx_runtime.jsx)("div", {
		className: bAvatar("icon"),
		style,
		children: (0, import_jsx_runtime.jsx)(Icon, {
			data: icon,
			size: avatarSizeToIconSize[size]
		})
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Avatar/AvatarImage/AvatarImage.js
var AvatarImage = ({ imgUrl, fallbackImgUrl, sizes, srcSet, alt, loading, withImageBorder, size }) => {
	const [isErrored, setIsErrored] = import_react.useState(false);
	const handleError = import_react.useCallback(() => {
		setIsErrored(true);
	}, []);
	import_react.useEffect(() => {
		setIsErrored(false);
	}, [imgUrl]);
	return (0, import_jsx_runtime.jsx)("img", {
		className: bAvatar("image", { "with-border": withImageBorder }),
		loading,
		width: AVATAR_SIZES[size],
		height: AVATAR_SIZES[size],
		src: fallbackImgUrl && isErrored ? fallbackImgUrl : imgUrl,
		sizes,
		srcSet,
		alt,
		onError: handleError
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Avatar/AvatarText/utils.js
var getAvatarDisplayText = (text, size) => {
	if (size === "3xs") return text[0].toUpperCase();
	const words = text.split(/[^\p{L}]+/u).filter((word) => word.length > 0);
	if (words.length <= 1) return text.slice(0, 2).toUpperCase();
	return [words[0][0], words[1][0]].filter(Boolean).join("").toUpperCase();
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Avatar/AvatarText/AvatarText.js
var AvatarText = ({ text, color, size }) => {
	const style = { color };
	const displayText = getAvatarDisplayText(text, size);
	return (0, import_jsx_runtime.jsx)("div", {
		className: bAvatar("text"),
		style,
		children: displayText
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Avatar/Avatar.js
var Avatar = import_react.forwardRef((rawProps, ref) => {
	const props = useDefaultProps("Avatar", rawProps);
	const { size = "m", theme = "normal", view = "filled", shape = "circle", backgroundColor, borderColor, title, className, style: styleProp, qa } = props;
	const style = {
		backgroundColor,
		color: borderColor,
		...styleProp
	};
	const renderContent = () => {
		if ("imgUrl" in props && props.imgUrl) return (0, import_jsx_runtime.jsx)(AvatarImage, {
			imgUrl: props.imgUrl,
			fallbackImgUrl: props.fallbackImgUrl,
			sizes: props.sizes,
			srcSet: props.srcSet,
			alt: props.alt || title,
			loading: props.loading,
			withImageBorder: props.withImageBorder,
			size
		});
		if ("icon" in props && props.icon) return (0, import_jsx_runtime.jsx)(AvatarIcon, {
			icon: props.icon,
			color: props.color,
			size
		});
		if ("text" in props && props.text) return (0, import_jsx_runtime.jsx)(AvatarText, {
			text: props.text,
			color: props.color,
			size
		});
		return null;
	};
	return (0, import_jsx_runtime.jsx)("div", {
		className: bAvatar({
			size,
			theme,
			view,
			shape,
			"with-border": Boolean(borderColor)
		}, className),
		title,
		role: "img",
		style,
		"data-qa": qa,
		ref,
		...filterDOMProps(props, { labelable: true }),
		children: renderContent()
	});
});
Avatar.displayName = "Avatar";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/AvatarStack/AvatarStackItem.js
var b$87 = block("avatar-stack");
var AvatarStackItem = ({ children, style }) => {
	return (0, import_jsx_runtime.jsx)("li", {
		className: b$87("item"),
		style,
		children
	});
};
AvatarStackItem.displayName = "AvatarStack.Item";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/AvatarStack/constants.js
var COMPONENT_SIZE_TO_MORE_ICON_SIZE = {
	xl: 24,
	l: 20,
	m: 16,
	s: 16,
	xs: 14,
	"2xs": 12,
	"3xs": 10
};
var i18n_default$17 = addComponentKeysets({
	en: { more: [
		"and {{count}} more",
		"and {{count}} more",
		"and {{count}} more"
	] },
	ru: { more: [
		"И eщё {{count}}",
		"И eщё {{count}}",
		"И eщё {{count}}"
	] }
}, "AvatarStack");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/AvatarStack/AvatarStackMore.js
var b$86 = block("avatar-stack");
/**
* Badge for displaying count of remaining avatars
*/
var AvatarStackMore = import_react.forwardRef(({ className, count, "aria-label": ariaLabel, borderColor = "var(--g-color-line-generic-solid)", size = "m", variant = "counter" }, ref) => {
	const { t } = i18n_default$17.useTranslation();
	return (0, import_jsx_runtime.jsx)("div", {
		ref,
		className: b$86("more", {
			size,
			"has-border": Boolean(borderColor)
		}, className),
		"aria-label": ariaLabel || t("more", { count }),
		style: { borderColor },
		children: variant === "counter" ? `+${count}` : (0, import_jsx_runtime.jsx)(Icon, {
			size: COMPONENT_SIZE_TO_MORE_ICON_SIZE[size],
			data: Ellipsis
		})
	});
});
AvatarStackMore.displayName = "AvatarStack.More";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/AvatarStack/AvatarStackMoreButton.js
var b$85 = block("avatar-stack");
var AvatarStackMoreButton = import_react.forwardRef(({ className, badgeClassName, size = "m", onClick, count, "aria-label": ariaLabel, borderColor, variant }, ref) => {
	return (0, import_jsx_runtime.jsx)("button", {
		ref,
		type: "button",
		className: b$85("more-button", { size }, className),
		onClick,
		children: (0, import_jsx_runtime.jsx)(AvatarStackMore, {
			className: badgeClassName,
			size,
			count,
			"aria-label": ariaLabel,
			borderColor,
			variant
		})
	});
});
AvatarStackMoreButton.displayName = "AvatarStack.MoreButton";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/AvatarStack/AvatarStack.js
var b$84 = block("avatar-stack");
var DEFAULT_MORE_BUTTON_STYLE = { zIndex: 0 };
var AvatarStackComponent = import_react.forwardRef((rawProps, ref) => {
	const { max = 3, total, overlapSize = "s", size, children, className, renderMore, moreVariant } = useDefaultProps("AvatarStack", rawProps);
	const visibleItems = [];
	/** All avatars amount */
	const normalizedTotal = total ? Math.max(total, max) : import_react.Children.count(children);
	/** Amount avatars to be visible (doesn't include badge with remaining avatars) */
	let normalizedMax = max < 1 ? 1 : max;
	normalizedMax = normalizedTotal - normalizedMax > 1 ? normalizedMax : normalizedTotal;
	/** Remaining avatars */
	const moreItems = normalizedTotal - normalizedMax;
	import_react.Children.forEach(children, (child) => {
		if (!import_react.isValidElement(child)) return;
		const item = (0, import_jsx_runtime.jsx)(AvatarStackItem, {
			style: { zIndex: normalizedMax - visibleItems.length },
			children: child
		}, visibleItems.length);
		if (visibleItems.length < normalizedMax) visibleItems.push(item);
	});
	const hasMoreButton = moreItems > 0;
	return (0, import_jsx_runtime.jsxs)("ul", {
		className: b$84({ "overlap-size": overlapSize }, className),
		role: "list",
		ref,
		children: [visibleItems, hasMoreButton ? (0, import_jsx_runtime.jsx)(AvatarStackItem, {
			style: DEFAULT_MORE_BUTTON_STYLE,
			children: renderMore ? renderMore({ count: moreItems }) : (0, import_jsx_runtime.jsx)(AvatarStackMore, {
				count: moreItems,
				size,
				variant: moreVariant
			})
		}, "more-button") : null]
	});
});
AvatarStackComponent.displayName = "AvatarStack";
var AvatarStack = Object.assign(AvatarStackComponent, {
	More: AvatarStackMore,
	MoreButton: AvatarStackMoreButton
});
var i18n_default$16 = addComponentKeysets({
	en: {
		breadcrumbs: "Breadcrumbs",
		label_more: "Show more"
	},
	ru: {
		breadcrumbs: "Навигация",
		label_more: "Показать больше"
	}
}, "lab/Breadcrumbs");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Breadcrumbs/utils.js
var b$83 = block("breadcrumbs");
/**
* This is not a full hash, but a stable string that will change when:
*  - the text changes at any level of nesting
*  - the component type changes
*  - primitive props change (className, title, href, etc.)
*  - elements are added, removed, or reordered
*/
function getReactNodeHash(children) {
	const parts = [];
	import_react.Children.forEach(children, (child) => {
		if (child === null || child === void 0 || typeof child === "boolean") return;
		if (typeof child === "string" || typeof child === "number") {
			parts.push(String(child));
			return;
		}
		if (import_react.isValidElement(child)) {
			const type = child.type;
			const typeName = typeof type === "string" ? type : type?.displayName || type?.name || "";
			parts.push(typeName);
			if (child.key !== null && child.key !== void 0) parts.push(String(child.key));
			const { children: nested, ...rest } = child.props;
			for (const [k, v] of Object.entries(rest)) if (typeof v === "string" || typeof v === "number" || typeof v === "boolean") parts.push(`${k}:${v}`);
			if (nested) parts.push(getReactNodeHash(nested));
		}
	});
	return parts.join("|");
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Breadcrumbs/BreadcrumbsDropdownMenu.js
var menuContext = import_react.createContext({
	isMenu: false,
	activeIndex: null,
	getItemProps: (props = {}) => props,
	listItemsRef: { current: [] },
	popupStyle: void 0
});
function BreadcrumbsDropdownMenu({ children, disabled, popupPlacement, popupStyle }) {
	const [reference, setReference] = import_react.useState(null);
	const [floating, setFloating] = import_react.useState(null);
	const [activeIndex, setActiveIndex] = import_react.useState(null);
	const [open, setOpen] = import_react.useState(false);
	const context = useFloatingRootContext({
		open,
		onOpenChange: setOpen,
		elements: {
			reference,
			floating
		}
	});
	const listItemsRef = import_react.useRef([]);
	const listNavigation = useListNavigation(context, {
		enabled: !disabled,
		listRef: listItemsRef,
		activeIndex,
		onNavigate: setActiveIndex,
		loop: true
	});
	const dismiss = useDismiss(context, { enabled: !disabled });
	const interactions = [
		useClick(context, { enabled: !disabled }),
		dismiss,
		listNavigation,
		useRole(context, { role: "menu" })
	];
	const { getReferenceProps, getItemProps } = useInteractions(interactions);
	const { t } = i18n_default$16.useTranslation();
	return (0, import_jsx_runtime.jsxs)("div", {
		className: b$83("menu"),
		children: [(0, import_jsx_runtime.jsx)(Button, {
			ref: setReference,
			...getReferenceProps(),
			title: t("label_more"),
			"aria-label": t("label_more"),
			size: "s",
			view: "flat",
			disabled,
			children: (0, import_jsx_runtime.jsx)(Button.Icon, { children: "..." })
		}), (0, import_jsx_runtime.jsx)(Popup, {
			open,
			floatingContext: context,
			floatingRef: setFloating,
			floatingInteractions: interactions,
			placement: popupPlacement,
			className: b$83("menu-popup"),
			children: (0, import_jsx_runtime.jsx)(menuContext.Provider, {
				value: {
					isMenu: true,
					getItemProps,
					listItemsRef,
					activeIndex,
					popupStyle
				},
				children
			})
		})]
	});
}
function useMenuContext() {
	return import_react.useContext(menuContext);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/lab/ListItemView/ListItemView.js
var b$82 = block("lab-list-item-view");
var ListItemView$1 = import_react.forwardRef(ListItemViewComponent);
function ListItemViewComponent(props, ref) {
	const { size, active, selected, hovered, disabled, onClick, selectionStyle, className, style, collapsed, onCollapseChange, children, isContainer = false, component: Component = "div", componentProps, collapsible: _collapsible, description, draggable: _draggable, startContent: _startContent, endContent: _endContent, nestedLevel: _nestedLevel } = props;
	const containerRef = import_react.useRef(null);
	return (0, import_jsx_runtime.jsx)(Component, {
		ref: useForkRef(containerRef, ref),
		...componentProps,
		...filterDOMProps(props),
		className: b$82({
			size,
			selected: selected && selectionStyle === "highlight",
			disabled,
			active,
			hovered: typeof hovered === "boolean" && (hovered ? "yes" : "no"),
			"is-container": isContainer,
			"has-description": Boolean(description)
		}, componentProps?.className ?? className),
		style: componentProps?.style ?? style,
		onClick: (e) => {
			if (disabled) {
				e.preventDefault();
				return;
			}
			const target = e.target;
			if (target instanceof Element && containerRef.current && focusable(containerRef.current).some((el) => el.contains(target))) return;
			if (typeof onClick === "function" || typeof componentProps?.onClick === "function") {
				onClick?.(e);
				componentProps?.onClick?.(e);
			} else if (typeof onCollapseChange === "function") onCollapseChange(!collapsed);
		},
		children: isContainer ? children : (0, import_jsx_runtime.jsx)(ListItemViewContent$1, {
			...props,
			children
		})
	});
}
function ListItemViewContent$1({ selected, disabled, selectionStyle, draggable, nestedLevel, collapsible, collapsed, onCollapseChange, startContent, children, description, endContent }) {
	return (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [
		draggable ? (0, import_jsx_runtime.jsx)(Slot, { name: "drag-handle" }) : null,
		nestedLevel ? (0, import_jsx_runtime.jsx)(Slot, {
			name: "spacer",
			style: { "--_--nested-level": nestedLevel }
		}) : null,
		collapsible ? (0, import_jsx_runtime.jsx)(Slot, {
			name: "collapsed-toggle",
			children: (0, import_jsx_runtime.jsx)(Button, {
				className: b$82("collapsible"),
				view: "flat",
				tabIndex: -1,
				disabled,
				onClick: () => {
					onCollapseChange?.(!collapsed);
				},
				"aria-hidden": "true",
				children: (0, import_jsx_runtime.jsx)(Button.Icon, { children: (0, import_jsx_runtime.jsx)(ArrowToggle, { className: b$82("arrow", { direction: collapsed ? "bottom" : "top" }) }) })
			})
		}) : null,
		selectionStyle === "check" && (0, import_jsx_runtime.jsx)(Slot, {
			name: "checked",
			children: (0, import_jsx_runtime.jsx)("div", {
				className: b$82("checked"),
				children: selected ? (0, import_jsx_runtime.jsx)(Icon, {
					data: Check,
					className: b$82("icon")
				}) : null
			})
		}),
		startContent ? (0, import_jsx_runtime.jsx)(Slot, {
			name: "start-content",
			children: startContent
		}) : null,
		(0, import_jsx_runtime.jsx)(Slot, {
			name: "content",
			children
		}),
		description ? (0, import_jsx_runtime.jsx)(Slot, {
			name: "description",
			children: description
		}) : null,
		endContent ? (0, import_jsx_runtime.jsx)(Slot, {
			name: "end-content",
			children: endContent
		}) : null
	] });
}
function Slot({ name, children, className, style }) {
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$82("slot", { name }, className),
		style,
		children
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Breadcrumbs/BreadcrumbsItem.js
function BreadcrumbsItem(props, ref) {
	const domProps = filterDOMProps(props, { labelable: true });
	const { href, hrefLang, target, rel, download, ping, referrerPolicy, children, disabled: disabledProp, __disabled: disabledInner, __current: current, __onAction: onAction, __index: index, ...restProps } = props;
	const disabled = disabledInner || disabledProp;
	const handleAction = (event) => {
		if (disabled) {
			event.preventDefault();
			return;
		}
		if (typeof restProps.onClick === "function") restProps.onClick(event);
		if (typeof onAction === "function") onAction();
	};
	const linkProps = {
		onClick: handleAction,
		"aria-disabled": disabled ? true : void 0
	};
	if (href) {
		linkProps.href = href;
		linkProps.hrefLang = hrefLang;
		linkProps.target = target;
		linkProps.rel = target === "_blank" && !rel ? "noopener noreferrer" : rel;
		linkProps.download = download;
		linkProps.ping = ping;
		linkProps.referrerPolicy = referrerPolicy;
		linkProps.tabIndex = disabled ? -1 : void 0;
	} else {
		linkProps.role = "link";
		linkProps.tabIndex = disabled ? void 0 : 0;
		linkProps.onKeyDown = (event) => {
			if (disabled) {
				event.preventDefault();
				return;
			}
			if (typeof restProps.onKeyDown === "function") restProps.onKeyDown(event);
			if (event.key === KeyCode$1.ENTER) {
				if (typeof onAction === "function") onAction();
			}
		};
	}
	if (current) linkProps["aria-current"] = props["aria-current"] ?? "page";
	const Element = href ? "a" : "span";
	const { isMenu, getItemProps, listItemsRef, activeIndex, popupStyle } = useMenuContext();
	if (isMenu) {
		const active = !disabled && activeIndex === index;
		return (0, import_jsx_runtime.jsx)(ListItemView$1, {
			ref: (node) => {
				listItemsRef.current[index ?? 0] = node;
			},
			nestedLevel: popupStyle === "staircase" ? index : void 0,
			active,
			size: "m",
			className: b$83("menu-link", props.className),
			component: Element,
			componentProps: getItemProps({
				...restProps,
				...domProps,
				...linkProps,
				role: "menuitem",
				tabIndex: active ? 0 : -1
			}),
			disabled,
			children
		});
	}
	return (0, import_jsx_runtime.jsx)(Element, {
		...restProps,
		...domProps,
		...linkProps,
		ref,
		className: b$83("link", {
			"is-current": current,
			"is-disabled": disabled && !current
		}, props.className),
		children
	});
}
BreadcrumbsItem.displayName = "Breadcrumbs.Item";
var _BreadcrumbsItem = import_react.forwardRef(BreadcrumbsItem);
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Breadcrumbs/BreadcrumbsSeparator.js
function BreadcrumbsSeparator({ separator }) {
	return (0, import_jsx_runtime.jsx)("div", {
		"aria-hidden": true,
		className: b$83("divider"),
		children: separator ?? "/"
	});
}
BreadcrumbsSeparator.displayName = "Breadcrumbs.Separator";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Breadcrumbs/Breadcrumbs.js
var Breadcrumbs = import_react.forwardRef(function Breadcrumbs(rawProps, ref) {
	const props = useDefaultProps("Breadcrumbs", rawProps);
	const listRef = import_react.useRef(null);
	const containerRef = useForkRef(ref, listRef);
	const menuRef = import_react.useRef(null);
	const endContentRef = import_react.useRef(null);
	const items = [];
	import_react.Children.forEach(props.children, (child, index) => {
		if (import_react.isValidElement(child)) {
			if (child.key === void 0 || child.key === null) child = import_react.cloneElement(child, { key: index });
			items.push(child);
		}
	});
	const { calculated, recalculate, visibleCount: visibleItemsCount } = useCollapseChildren({
		containerRef: listRef,
		preservedRefs: [menuRef, endContentRef],
		direction: "start",
		minCount: 1,
		maxCount: typeof props.maxItems === "number" && props.maxItems < items.length ? props.maxItems - 1 : void 0,
		childSelector: `.${b$83("item")}`,
		getChildWidth: (child) => {
			const width = child.getBoundingClientRect().width;
			const maxWidth = child.dataset.current ? 200 : Infinity;
			return Math.min(maxWidth, width);
		}
	});
	useResizeObserver({
		ref: endContentRef,
		onResize: recalculate
	});
	const childrenHash = getReactNodeHash(props.children);
	const separatorHash = getReactNodeHash(props.separator);
	import_react.useEffect(() => {
		recalculate();
	}, [
		childrenHash,
		separatorHash,
		props.itemComponent
	]);
	let contents = items;
	if (items.length > visibleItemsCount) {
		contents = [];
		const breadcrumbs = [...items];
		let endItems = visibleItemsCount;
		if (props.showRoot && visibleItemsCount > 1) {
			const rootItem = breadcrumbs.shift();
			if (rootItem) contents.push(rootItem);
			endItems--;
		}
		const hiddenItems = breadcrumbs.slice(0, -endItems);
		const menuItem = (0, import_jsx_runtime.jsx)(BreadcrumbsDropdownMenu, {
			disabled: props.disabled,
			popupPlacement: props.popupPlacement,
			popupStyle: props.popupStyle,
			"data-breadcrumbs-menu-item": true,
			children: hiddenItems.map((child, index) => {
				const Component = props.itemComponent ?? _BreadcrumbsItem;
				const key = child.key ?? index;
				const handleAction = () => {
					if (typeof props.onAction === "function") props.onAction(key);
				};
				const innerProps = {
					__index: index,
					__disabled: props.disabled || child.props.disabled,
					__onAction: handleAction
				};
				return (0, import_react.createElement)(Component, {
					...child.props,
					key,
					...innerProps
				}, child.props.children);
			})
		});
		contents.push(menuItem);
		contents.push(...breadcrumbs.slice(-endItems));
	}
	return (0, import_jsx_runtime.jsxs)("ol", {
		ref: containerRef,
		...filterDOMProps(props, { labelable: true }),
		"data-qa": props.qa,
		className: b$83(null, props.className),
		style: props.style,
		children: [contents.map((child, index) => {
			const key = child.key ?? index;
			const isCurrent = index === contents.length - 1;
			const { "data-breadcrumbs-menu-item": isMenu, ...childProps } = child.props;
			let item;
			if (isMenu) item = child;
			else {
				const Component = props.itemComponent ?? _BreadcrumbsItem;
				const handleAction = () => {
					if (typeof props.onAction === "function") props.onAction(key);
				};
				const innerProps = {
					__current: isCurrent,
					__disabled: props.disabled || childProps.disabled,
					__onAction: handleAction
				};
				item = (0, import_react.createElement)(Component, {
					...childProps,
					key,
					...innerProps
				}, childProps.children);
			}
			return (0, import_jsx_runtime.jsxs)("li", {
				ref: isMenu ? menuRef : void 0,
				className: b$83("item", {
					calculating: isCurrent && !calculated,
					current: isCurrent
				}),
				"data-current": isCurrent ? isCurrent : void 0,
				children: [item, isCurrent ? null : (0, import_jsx_runtime.jsx)(BreadcrumbsSeparator, { separator: props.separator })]
			}, isMenu ? "menu" : `item-${key}`);
		}), props.endContent && (0, import_jsx_runtime.jsx)("li", {
			ref: endContentRef,
			className: b$83("item"),
			children: props.endContent
		})]
	});
});
Breadcrumbs.Item = _BreadcrumbsItem;
Breadcrumbs.displayName = "Breadcrumbs";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ControlLabel/ControlLabel.js
var b$81 = block("control-label");
/**
* Wrap with label for `<Checkbox/>`, `<Radio/>`, `<Switch/>`
*/
var ControlLabel = import_react.forwardRef(({ children, className, labelClassName, title, style, disabled = false, control, size = "m", qa }, ref) => {
	const clonedControl = import_react.cloneElement(control, { className: b$81("indicator", control.props.className) });
	return (0, import_jsx_runtime.jsxs)("label", {
		ref,
		title,
		style,
		className: b$81({
			size,
			disabled
		}, className),
		"data-qa": qa,
		children: [clonedControl, children ? (0, import_jsx_runtime.jsx)("span", {
			className: b$81("text", labelClassName),
			children
		}) : null]
	});
});
ControlLabel.displayName = "ControlLabel";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Checkbox/CheckboxDashIcon.js
function CheckboxDashIcon(props) {
	return (0, import_jsx_runtime.jsx)("svg", {
		xmlns: "http://www.w3.org/2000/svg",
		viewBox: "0 0 17 17",
		width: "16",
		height: "16",
		fill: "currentColor",
		...props,
		children: (0, import_jsx_runtime.jsx)("path", { d: "M4 7h9v3H4z" })
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Checkbox/CheckboxTickIcon.js
function CheckboxTickIcon(props) {
	return (0, import_jsx_runtime.jsx)("svg", {
		xmlns: "http://www.w3.org/2000/svg",
		viewBox: "0 0 12 10",
		width: "16",
		height: "16",
		fill: "currentColor",
		...props,
		children: (0, import_jsx_runtime.jsx)("path", { d: "M.49 5.385l1.644-1.644 4.385 4.385L4.874 9.77.49 5.385zm4.384 1.096L10.356 1 12 2.644 6.519 8.126 4.874 6.48v.001z" })
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Checkbox/Checkbox.js
var b$80 = block("checkbox");
var Checkbox = import_react.forwardRef(function Checkbox(rawProps, ref) {
	const props = useDefaultProps("Checkbox", rawProps);
	const { size = "m", indeterminate, disabled = false, content, children, title, style, className, qa } = props;
	const { checked, inputProps } = useCheckbox(props);
	const text = content || children;
	const control = (0, import_jsx_runtime.jsxs)("span", {
		className: b$80("indicator"),
		children: [
			(0, import_jsx_runtime.jsx)("span", {
				className: b$80("icon"),
				"aria-hidden": true,
				children: indeterminate ? (0, import_jsx_runtime.jsx)(CheckboxDashIcon, { className: b$80("icon-svg", { type: "dash" }) }) : (0, import_jsx_runtime.jsx)(CheckboxTickIcon, { className: b$80("icon-svg", { type: "tick" }) })
			}),
			(0, import_jsx_runtime.jsx)("input", {
				...inputProps,
				className: b$80("control")
			}),
			(0, import_jsx_runtime.jsx)("span", { className: b$80("outline") })
		]
	});
	return (0, import_jsx_runtime.jsx)(ControlLabel, {
		ref,
		title,
		style,
		size,
		disabled,
		className: b$80({
			size,
			disabled,
			indeterminate,
			checked
		}, className),
		qa,
		control,
		children: text
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ClipboardIcon/ClipboardIcon.js
function ClipboardIcon(rawProps) {
	const { status, ...rest } = useDefaultProps("ClipboardIcon", rawProps);
	if (status === "error") return (0, import_jsx_runtime.jsx)(Icon, {
		data: CopyXmark,
		...rest
	});
	if (status === "success") return (0, import_jsx_runtime.jsx)(Icon, {
		data: CopyCheck,
		...rest
	});
	return (0, import_jsx_runtime.jsx)(Icon, {
		data: Copy,
		...rest
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/CopyToClipboard/copyText.js
/**
* Copies text to the browser's clipboard.
* Uses the modern `navigator.clipboard` API, with a fallback to `document.execCommand` when unavailable.
* @param {string} text Text to copy to the clipboard
* @returns {Promise<void>} Promise that resolves on successful copy or rejects on error
* @throws {Error} Throws an error if neither `navigator.clipboard` nor `document` is available
* @example
* ```typescript
* // Copy simple text
* copyText('Hello, World!')
*   .then(() => console.log('Text copied'))
*   .catch(error => console.error('Copy error:', error));
*
* // Using with async/await
* try {
*   await copyText('Text to copy');
*   console.log('Successfully copied');
* } catch (error) {
*   console.error('Failed to copy:', error);
* }
* ```
*/
async function copyText(text) {
	if (typeof navigator !== "undefined" && navigator.clipboard?.writeText) try {
		return await navigator.clipboard.writeText(text);
	} catch (error) {
		return typeof document === "undefined" ? Promise.reject(error) : copyTextFallback(text);
	}
	if (typeof document !== "undefined") return copyTextFallback(text);
	return Promise.reject(/* @__PURE__ */ new Error("Neither navigator.clipboard nor document is available"));
}
async function copyTextFallback(text) {
	const activeElement = document.activeElement;
	try {
		const textarea = document.createElement("textarea");
		textarea.setAttribute("aria-hidden", "true");
		textarea.style.all = "unset";
		textarea.style.position = "absolute";
		textarea.style.left = "1000%";
		textarea.style.whiteSpace = "pre";
		textarea.style.userSelect = "text";
		textarea.value = text;
		document.body.append(textarea);
		textarea.select();
		if (!document.execCommand("copy")) throw new Error("Failed to copy text with document.execCommand(\"copy\")");
		document.body.removeChild(textarea);
	} finally {
		if (activeElement && "focus" in activeElement && typeof activeElement.focus === "function") activeElement.focus();
	}
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/CopyToClipboard/CopyToClipboard.js
var INITIAL_STATUS = "pending";
function CopyToClipboard(rawProps) {
	const { children, text, timeout, onCopy } = useDefaultProps("CopyToClipboard", rawProps);
	const textRef = import_react.useRef("");
	const [status, setStatus] = import_react.useState(INITIAL_STATUS);
	const timerIdRef = import_react.useRef();
	const content = typeof children === "function" ? children(status) : children;
	const contentOnClick = content.props?.onClick;
	const handleCopy = import_react.useCallback((copyText, result) => {
		setStatus(result ? "success" : "error");
		window.clearTimeout(timerIdRef.current);
		timerIdRef.current = window.setTimeout(() => setStatus(INITIAL_STATUS), timeout);
		onCopy?.(copyText, result);
	}, [onCopy, timeout]);
	const onClickWithCopy = import_react.useCallback((event) => {
		contentOnClick?.(event);
		const currentText = typeof text === "function" ? text() : text;
		textRef.current = currentText;
		function copy(result) {
			if (currentText === textRef.current) handleCopy(currentText, result);
		}
		copyText(currentText).then(() => {
			copy(true);
		}, () => {
			copy(false);
		});
	}, [
		contentOnClick,
		handleCopy,
		text
	]);
	import_react.useEffect(() => () => window.clearTimeout(timerIdRef.current), []);
	if (!import_react.isValidElement(content)) throw new Error("Content must be a valid react element");
	return import_react.cloneElement(content, { onClick: onClickWithCopy });
}
var i18n_default$15 = addComponentKeysets({
	en: {
		startCopy: "Copy",
		endCopy: "Copied"
	},
	ru: {
		startCopy: "Копировать",
		endCopy: "Скопировано"
	}
}, "ClipboardButton");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/ClipboardButton/ClipboardButton.js
var b$79 = block("clipboard-button");
var DEFAULT_TIMEOUT$1 = 1200;
var ButtonSizeToIconSize = {
	xs: 12,
	s: 16,
	m: 16,
	l: 16,
	xl: 20
};
var ClipboardButtonComponent = (props) => {
	const { t } = i18n_default$15.useTranslation();
	const { size = "m", hasTooltip = true, tooltipInitialText = t("startCopy"), tooltipSuccessText = t("endCopy"), status, view = "flat", children, iconPosition = "start", closeDelay, icon, ...rest } = props;
	const buttonIcon = (0, import_jsx_runtime.jsx)(Button.Icon, {
		className: b$79("icon"),
		children: icon ?? (0, import_jsx_runtime.jsx)(ClipboardIcon, {
			size: ButtonSizeToIconSize[size],
			status
		})
	});
	return (0, import_jsx_runtime.jsx)(ActionTooltip, {
		title: status === "success" ? tooltipSuccessText : tooltipInitialText,
		disabled: !hasTooltip,
		closeDelay,
		children: (0, import_jsx_runtime.jsxs)(Button, {
			view,
			size,
			"aria-label": tooltipInitialText,
			...rest,
			children: [
				iconPosition === "start" ? buttonIcon : null,
				children,
				iconPosition === "end" ? buttonIcon : null
			]
		})
	});
};
function ClipboardButton(rawProps) {
	const { text, timeout = DEFAULT_TIMEOUT$1, onCopy, hasTooltip = true, onMouseEnter, onFocus, ...buttonProps } = useDefaultProps("ClipboardButton", rawProps);
	const timerIdRef = import_react.useRef();
	const [tooltipCloseDelay, setTooltipCloseDelay] = import_react.useState(void 0);
	const [tooltipDisabled, setTooltipDisabled] = import_react.useState(false);
	import_react.useEffect(() => window.clearTimeout(timerIdRef.current), []);
	const handleCopy = import_react.useCallback((text, result) => {
		onCopy?.(text, result);
		setTooltipDisabled(false);
		setTooltipCloseDelay(timeout);
		window.clearTimeout(timerIdRef.current);
		timerIdRef.current = window.setTimeout(() => {
			setTooltipDisabled(true);
		}, timeout);
	}, [onCopy, timeout]);
	const resetTooltip = import_react.useCallback(() => {
		if (tooltipDisabled) {
			setTooltipDisabled(false);
			setTooltipCloseDelay(void 0);
		}
	}, [tooltipDisabled]);
	const handleMouseEnter = import_react.useCallback((event) => {
		onMouseEnter?.(event);
		resetTooltip();
	}, [onMouseEnter, resetTooltip]);
	const handleFocus = import_react.useCallback((event) => {
		onFocus?.(event);
		resetTooltip();
	}, [onFocus, resetTooltip]);
	return (0, import_jsx_runtime.jsx)(CopyToClipboard, {
		text,
		timeout,
		onCopy: handleCopy,
		children: (status) => (0, import_jsx_runtime.jsx)(ClipboardButtonComponent, {
			...buttonProps,
			closeDelay: tooltipCloseDelay,
			hasTooltip: hasTooltip && !tooltipDisabled,
			status,
			onMouseEnter: handleMouseEnter,
			onFocus: handleFocus
		})
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DefinitionList/components/DefinitionListContext.js
var DefinitionListAttributesContext = import_react.createContext(void 0);
function DefinitionListProvider({ direction, contentMaxWidth, nameMaxWidth, children }) {
	const keyStyle = nameMaxWidth ? {
		maxWidth: nameMaxWidth,
		width: nameMaxWidth
	} : {};
	const valueStyle = typeof contentMaxWidth === "number" ? {
		width: contentMaxWidth,
		maxWidth: contentMaxWidth
	} : {};
	return (0, import_jsx_runtime.jsx)(DefinitionListAttributesContext.Provider, {
		value: {
			keyStyle,
			valueStyle,
			direction
		},
		children
	});
}
function useDefinitionListAttributes() {
	const state = import_react.useContext(DefinitionListAttributesContext);
	if (state === void 0) throw new Error("useDefinitionListAttributes must be used within DefinitionListProvider");
	return state;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DefinitionList/constants.js
var b$78 = block("definition-list");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DefinitionList/utils.js
function isUnbreakableOver(limit) {
	return function(value) {
		return value.split(/\s+/).some((line) => line.length > limit);
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DefinitionList/components/DefinitionContent.js
function DefinitionContent({ copyText, children }) {
	const definitionContent = children ?? "—";
	return copyText ? (0, import_jsx_runtime.jsxs)("div", {
		className: b$78("copy-container"),
		children: [definitionContent, (0, import_jsx_runtime.jsx)(ClipboardButton, {
			size: "s",
			text: copyText,
			className: b$78("copy-button"),
			view: "flat-secondary"
		})]
	}) : definitionContent;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Popover/Popover.js
var b$77 = block("popover");
var DEFAULT_OPEN_DELAY = 500;
var DEFAULT_CLOSE_DELAY = 250;
var DEFAULT_REST = 0;
function Popover(rawProps) {
	const { children, open, onOpenChange, toggle = true, disabled, content, trigger = "all", openDelay = DEFAULT_OPEN_DELAY, closeDelay = DEFAULT_CLOSE_DELAY, rest = DEFAULT_REST, enableSafePolygon, className, ...restProps } = useDefaultProps("Popover", rawProps);
	const [anchorElement, setAnchorElement] = import_react.useState(null);
	const [floatingElement, setFloatingElement] = import_react.useState(null);
	const [isOpen, setIsOpen] = useControlledState(open, false, onOpenChange);
	const context = useFloatingRootContext({
		open: isOpen && !disabled,
		onOpenChange: setIsOpen,
		elements: {
			reference: anchorElement,
			floating: floatingElement
		}
	});
	const isHoverEnabled = trigger === "all";
	const interactions = [
		useHover$1(context, {
			enabled: isHoverEnabled,
			delay: {
				open: openDelay,
				close: closeDelay
			},
			restMs: rest,
			move: false,
			handleClose: enableSafePolygon ? safePolygon() : void 0
		}),
		useClick(context, {
			toggle,
			ignoreMouse: isHoverEnabled
		}),
		useRole(context, { role: "dialog" }),
		useDismiss(context)
	];
	const { getReferenceProps } = useInteractions(interactions);
	const anchorRef = useForkRef(setAnchorElement, import_react.isValidElement(children) ? getElementRef(children) : void 0);
	const anchorProps = import_react.isValidElement(children) ? getReferenceProps(children.props) : getReferenceProps();
	const anchorNode = import_react.isValidElement(children) ? import_react.cloneElement(children, {
		...anchorProps,
		ref: anchorRef
	}) : children(anchorProps, anchorRef);
	return (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [anchorNode, (0, import_jsx_runtime.jsx)(Popup, {
		...restProps,
		open: context.open,
		floatingContext: context,
		floatingRef: setFloatingElement,
		floatingInteractions: interactions,
		className: b$77(null, className),
		children: content
	})] });
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/HelpMark/constants.js
var ICON_SIZE_MAP = {
	s: 14,
	m: 16,
	l: 18,
	xl: 20
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/HelpMark/HelpMark.js
var b$76 = block("help-mark");
var HelpMark = import_react.forwardRef(function HelpMark(rawProps, ref) {
	const { children, qa, className, iconSize = "m", popoverProps, ...restProps } = useDefaultProps("HelpMark", rawProps);
	return (0, import_jsx_runtime.jsx)(Popover, {
		content: (0, import_jsx_runtime.jsx)("div", {
			className: b$76("popover"),
			children
		}),
		hasArrow: true,
		...popoverProps,
		children: (0, import_jsx_runtime.jsx)("button", {
			...restProps,
			ref,
			type: "button",
			className: b$76({ size: iconSize }, className),
			"data-qa": qa,
			children: (0, import_jsx_runtime.jsx)(Icon, {
				data: CircleQuestion,
				size: ICON_SIZE_MAP[iconSize],
				className: b$76("icon")
			})
		})
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DefinitionList/i18n/index.js
var i18n_default$14 = addComponentKeysets({
	en: { label_note: "Note" },
	ru: { label_note: "Справка" }
}, "DefinitionList");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DefinitionList/components/TermContent.js
function NoteElement({ note }) {
	const { t } = i18n_default$14.useTranslation();
	if (!note) return null;
	if (typeof note === "string") return (0, import_jsx_runtime.jsx)(HelpMark, {
		className: b$78("note"),
		popoverProps: { placement: ["bottom", "top"] },
		"aria-label": t("label_note"),
		children: note
	});
	if (typeof note === "object") return (0, import_jsx_runtime.jsx)(HelpMark, {
		...note,
		className: b$78("note", note.className),
		popoverProps: {
			placement: ["bottom", "top"],
			...note.popoverProps
		},
		"aria-label": t("label_note")
	});
	return null;
}
function TermContent({ note, name, direction }) {
	const noteElement = note ? (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: ["\xA0", (0, import_jsx_runtime.jsx)(NoteElement, { note })] }) : null;
	return (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [(0, import_jsx_runtime.jsxs)("div", {
		className: b$78("term-wrapper"),
		children: [name, noteElement]
	}), direction === "horizontal" && (0, import_jsx_runtime.jsx)("div", { className: b$78("dots") })] });
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DefinitionList/components/DefinitionListItem.js
function DefinitionListItem({ name, children, copyText, note, qa }) {
	const { direction, keyStyle, valueStyle } = useDefinitionListAttributes();
	return (0, import_jsx_runtime.jsxs)("div", {
		className: b$78("item"),
		"data-qa": qa,
		children: [(0, import_jsx_runtime.jsx)("dt", {
			className: b$78("term-container"),
			style: keyStyle,
			children: (0, import_jsx_runtime.jsx)(TermContent, {
				direction,
				name,
				note
			})
		}), (0, import_jsx_runtime.jsx)("dd", {
			className: b$78("definition"),
			style: {
				...valueStyle,
				lineBreak: typeof children === "string" && isUnbreakableOver(20)(children) ? "anywhere" : void 0
			},
			children: (0, import_jsx_runtime.jsx)(DefinitionContent, {
				copyText,
				children
			})
		})]
	});
}
DefinitionListItem.displayName = "DefinitionListItem";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/DefinitionList/DefinitionList.js
function DefinitionList(rawProps) {
	const { responsive, direction = "horizontal", nameMaxWidth, contentMaxWidth, className, children, qa, ...restProps } = useDefaultProps("DefinitionList", rawProps);
	const normalizedChildren = prepareChildren(children);
	const withCopy = normalizedChildren.some((item) => item.props.copyText);
	return (0, import_jsx_runtime.jsx)(DefinitionListProvider, {
		direction,
		nameMaxWidth,
		contentMaxWidth,
		children: (0, import_jsx_runtime.jsx)("dl", {
			...filterDOMProps(restProps, { labelable: true }),
			className: b$78({
				responsive,
				vertical: direction === "vertical",
				"with-copy": withCopy
			}, className),
			"data-qa": qa,
			children: normalizedChildren
		})
	});
}
var isDefinitionListItem = isOfType(DefinitionListItem);
function prepareChildren(children) {
	const items = import_react.Children.toArray(children);
	const normalizedItems = [];
	for (const item of items) if (isDefinitionListItem(item)) normalizedItems.push(item);
	else warnOnce("[DefinitionList] Only <DefinitionList.Item> components is allowed as children");
	return normalizedItems;
}
DefinitionList.Item = DefinitionListItem;
DefinitionList.displayName = "DefinitionList";
var i18n_default$13 = addComponentKeysets({
	en: { close: "Close" },
	ru: { close: "Закрыть" }
}, "Modal");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Modal/Modal.js
var b$75 = block("modal");
var TRANSITION_DURATION$1 = 150;
function ModalComponent(rawProps) {
	const { open = false, onOpenChange, keepMounted = false, disableBodyScrollLock = false, disableEscapeKeyDown, disableOutsideClick, initialFocus, returnFocus, disableVisuallyHiddenDismiss, onEscapeKeyDown, onOutsideClick, onClose, onEnterKeyDown, onTransitionIn, onTransitionInComplete, onTransitionOut, onTransitionOutComplete, children, style, contentOverflow = "visible", className, contentClassName, container, disablePortal, qa, floatingRef, disableHeightTransition = false, ...restProps } = useDefaultProps("Modal", rawProps);
	useLayer({
		open,
		type: "modal"
	});
	const mobileModals = import_react.useContext(MobileContext).__experimentalMobileModals ?? false;
	const mobile = useMobile() && mobileModals;
	const overlayRef = import_react.useRef(null);
	const [isVisible, setIsVisible] = import_react.useState(false);
	const handleOpenChange = import_react.useCallback((isOpen, event, reason) => {
		onOpenChange?.(isOpen, event, reason);
		if (isOpen || !event) return;
		let closeReason;
		if (reason === "escape-key") closeReason = "escapeKeyDown";
		else if (reason === "outside-press") closeReason = "outsideClick";
		else closeReason = reason;
		if (closeReason === "escapeKeyDown" && onEscapeKeyDown) onEscapeKeyDown(event);
		if (closeReason === "outsideClick" && onOutsideClick) onOutsideClick(event);
		onClose?.(event, closeReason);
	}, [
		onOpenChange,
		onEscapeKeyDown,
		onOutsideClick,
		onClose
	]);
	const floatingNodeId = useFloatingNodeId();
	const { refs, elements, context } = useFloating({
		nodeId: floatingNodeId,
		open,
		onOpenChange: handleOpenChange
	});
	const handleFloatingRef = useForkRef(refs.setFloating, floatingRef);
	const handleTransitionInComplete = import_react.useCallback(() => {
		setIsVisible(true);
		onTransitionInComplete?.();
	}, [onTransitionInComplete]);
	const handleTransitionOutComplete = import_react.useCallback(() => {
		setIsVisible(false);
		onTransitionOutComplete?.();
	}, [onTransitionOutComplete]);
	const { getFloatingProps } = useInteractions([useDismiss(context, {
		enabled: !disableOutsideClick || !disableEscapeKeyDown,
		outsidePress: (event) => {
			if (disableOutsideClick) return false;
			if (event.target.closest(`.${b$75()}`) !== overlayRef.current) return false;
			return true;
		},
		escapeKey: !disableEscapeKeyDown
	}), useRole(context, { role: "dialog" })]);
	const { isMounted, status } = useFloatingTransition({
		context,
		duration: TRANSITION_DURATION$1,
		onTransitionIn,
		onTransitionInComplete: handleTransitionInComplete,
		onTransitionOut,
		onTransitionOutComplete: handleTransitionOutComplete
	});
	useAnimateHeight({
		ref: refs.floating,
		enabled: status === "open" && !disableHeightTransition
	});
	const handleKeyDown = import_react.useCallback((event) => {
		if (!onEnterKeyDown || event.key !== KeyCode$1.ENTER || event.defaultPrevented) return;
		const floatingElement = elements.floating;
		if (!floatingElement) return;
		const pathElements = event.nativeEvent.composedPath();
		const index = pathElements.indexOf(floatingElement);
		const nestedElements = index < 0 ? pathElements : pathElements.slice(0, index);
		const nestedFloatingElementIndex = nestedElements.findIndex((el) => el?.hasAttribute("data-floating-ui-focusable"));
		if (nestedFloatingElementIndex < 0) {
			onEnterKeyDown(event.nativeEvent);
			return;
		}
		if (!nestedElements.slice(0, nestedFloatingElementIndex).some((el) => isTabbable(el))) onEnterKeyDown(event.nativeEvent);
	}, [elements.floating, onEnterKeyDown]);
	const { t } = i18n_default$13.useTranslation();
	return (0, import_jsx_runtime.jsx)(FloatingNode, {
		id: floatingNodeId,
		children: isMounted || keepMounted ? (0, import_jsx_runtime.jsx)(Portal, {
			container,
			disablePortal,
			children: (0, import_jsx_runtime.jsx)(FloatingOverlay, {
				ref: overlayRef,
				style: {
					...style,
					...mobile ? { overflow: "hidden" } : {}
				},
				className: b$75({
					open,
					mobile
				}, className),
				"data-qa": qa,
				"data-floating-ui-status": status,
				lockScroll: !disableBodyScrollLock,
				children: (0, import_jsx_runtime.jsx)(FloatingFocusManager, {
					context,
					disabled: !isMounted || !isVisible,
					modal: isMounted,
					initialFocus: initialFocus ?? refs.floating,
					returnFocus,
					visuallyHiddenDismiss: disableVisuallyHiddenDismiss ? false : t("close"),
					restoreFocus: true,
					children: (0, import_jsx_runtime.jsx)("div", {
						className: b$75("content-aligner"),
						children: (0, import_jsx_runtime.jsx)("div", {
							...filterDOMProps(restProps, { labelable: true }),
							className: b$75("content", { "has-scroll": mobile ? true : contentOverflow === "auto" }, contentClassName),
							ref: handleFloatingRef,
							...getFloatingProps({ onKeyDown: handleKeyDown }),
							children
						})
					})
				})
			})
		}) : null
	});
}
function Modal(props) {
	if (useFloatingParentNodeId() === null) return (0, import_jsx_runtime.jsx)(FloatingTree, { children: (0, import_jsx_runtime.jsx)(ModalComponent, { ...props }) });
	return (0, import_jsx_runtime.jsx)(ModalComponent, { ...props });
}
var i18n_default$12 = addComponentKeysets({
	en: { close: "Close dialog" },
	ru: { close: "Закрыть диалоговое окно" }
}, "Dialog");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Dialog/ButtonClose/ButtonClose.js
var b$74 = block("dialog-btn-close");
function ButtonClose({ mobile, onClose }) {
	const { t } = i18n_default$12.useTranslation();
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$74({ mobile }),
		children: (0, import_jsx_runtime.jsx)(Button, {
			view: "flat",
			size: mobile ? "xl" : "l",
			className: b$74("btn"),
			onClick: (event) => onClose(event, { isOutsideClick: false }),
			"aria-label": t("close"),
			children: (0, import_jsx_runtime.jsx)(Icon, {
				data: Xmark,
				size: 20
			})
		})
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Dialog/DialogPrivateContext.js
var DialogPrivateContext = import_react.createContext({
	mobile: false,
	disableHeightTransition: false
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Dialog/DialogBody/DialogBody.js
var b$73 = block("dialog-body");
function DialogBody(props) {
	const { className, hasBorders = false } = props;
	const contentRef = import_react.useRef(null);
	const { disableHeightTransition } = import_react.useContext(DialogPrivateContext);
	useAnimateHeight({
		ref: contentRef,
		enabled: !disableHeightTransition
	});
	return (0, import_jsx_runtime.jsx)("div", {
		ref: contentRef,
		className: b$73({ "has-borders": hasBorders }, className),
		children: props.children
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Dialog/DialogDivider/DialogDivider.js
var b$72 = block("dialog-divider");
function DialogDivider({ className }) {
	return (0, import_jsx_runtime.jsx)("div", { className: b$72(null, className) });
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Dialog/DialogFooter/DialogFooter.js
var b$71 = block("dialog-footer");
function getButtonView(preset) {
	switch (preset) {
		case "default": return "action";
		case "success": return "action";
		case "danger": return "action";
		default: return "action";
	}
}
function DialogFooter(props) {
	const { onClickButtonCancel, onClickButtonApply, loading, textButtonCancel, textButtonApply, propsButtonCancel, propsButtonApply, preset = "default", children, errorText, showError = false, renderButtons, className } = props;
	const { mobile, initialFocusRef, initialFocusAction, onTooltipEscapeKeyDown } = import_react.useContext(DialogPrivateContext);
	const errorTooltipRef = import_react.useRef(null);
	const apllyBtnRef = useForkRef(errorTooltipRef, initialFocusAction === "apply" ? initialFocusRef : null);
	const cancelBtnRef = useForkRef(initialFocusAction === "cancel" ? initialFocusRef : null);
	const buttonCancel = (0, import_jsx_runtime.jsx)("div", {
		className: b$71("button", { action: "cancel" }),
		children: (0, import_jsx_runtime.jsx)(Button, {
			ref: cancelBtnRef,
			view: textButtonApply ? "flat" : "normal",
			size: mobile ? "xl" : "l",
			width: "max",
			onClick: onClickButtonCancel,
			disabled: loading,
			...propsButtonCancel,
			children: textButtonCancel
		})
	});
	const handleOpenChange = import_react.useCallback((isOpen, event, reason) => {
		if (!isOpen && event && reason === "escape-key") onTooltipEscapeKeyDown?.(event);
	}, [onTooltipEscapeKeyDown]);
	const buttonApply = (0, import_jsx_runtime.jsxs)("div", {
		className: b$71("button", { action: "apply" }),
		children: [(0, import_jsx_runtime.jsx)(Button, {
			ref: apllyBtnRef,
			type: "submit",
			view: getButtonView(preset),
			size: mobile ? "xl" : "l",
			width: "max",
			onClick: onClickButtonApply,
			loading,
			className: b$71("button-apply", { preset }),
			...propsButtonApply,
			children: textButtonApply
		}), errorText && (0, import_jsx_runtime.jsx)(Popup, {
			open: showError,
			onOpenChange: handleOpenChange,
			anchorRef: errorTooltipRef,
			placement: "top",
			disablePortal: true,
			hasArrow: true,
			children: (0, import_jsx_runtime.jsx)("div", {
				className: b$71("error"),
				children: errorText
			})
		})]
	});
	const buttons = (() => {
		if (renderButtons) return renderButtons(buttonApply, buttonCancel);
		if (!textButtonCancel && !textButtonApply) return null;
		return (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [textButtonCancel && buttonCancel, textButtonApply && buttonApply] });
	})();
	if (!buttons && !children) return null;
	return (0, import_jsx_runtime.jsxs)("div", {
		className: b$71({ mobile }, className),
		children: [children && (0, import_jsx_runtime.jsx)("div", {
			className: b$71("children"),
			children
		}), buttons && (0, import_jsx_runtime.jsx)("div", {
			className: b$71("bts-wrapper"),
			children: buttons
		})]
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Dialog/DialogHeader/DialogHeader.js
var b$70 = block("dialog-header");
function DialogHeader(props) {
	const { caption = "", as = "div", insertBefore, insertAfter, className, id } = props;
	const { mobile } = import_react.useContext(DialogPrivateContext);
	return (0, import_jsx_runtime.jsxs)("div", {
		className: b$70({ mobile }, className),
		children: [
			insertBefore,
			(0, import_jsx_runtime.jsx)(Text, {
				as,
				variant: mobile ? "header-1" : "subheader-3",
				className: b$70("caption"),
				id,
				children: caption
			}),
			insertAfter
		]
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Dialog/Dialog.js
var b$69 = block("dialog");
function Dialog(rawProps) {
	const { container, children, open, disableBodyScrollLock = false, disableEscapeKeyDown = false, disableOutsideClick = false, initialFocus, returnFocus, keepMounted = false, size, maxWidth, fullWidth, contentOverflow = "visible", className, modalClassName, hasCloseButton = true, disableHeightTransition = false, onEscapeKeyDown, onEnterKeyDown, onOpenChange, onOutsideClick, onClose, onTransitionIn, onTransitionInComplete, onTransitionOut, onTransitionOutComplete, qa, ...restProps } = useDefaultProps("Dialog", rawProps);
	const mobileModals = import_react.useContext(MobileContext).__experimentalMobileModals ?? false;
	const mobile = useMobile() && mobileModals;
	const handleCloseButtonClick = import_react.useCallback((event) => {
		onClose(event.nativeEvent, "closeButtonClick");
	}, [onClose]);
	const footerAutoFocusRef = import_react.useRef(null);
	const privateContextProps = import_react.useMemo(() => {
		const result = {
			onTooltipEscapeKeyDown: (event) => {
				onOpenChange?.(false, event, "escape-key");
				onEscapeKeyDown?.(event);
				onClose?.(event, "escapeKeyDown");
			},
			disableHeightTransition: disableHeightTransition || !open || mobile,
			mobile
		};
		if (typeof initialFocus === "string") {
			result.initialFocusRef = footerAutoFocusRef;
			result.initialFocusAction = initialFocus;
		}
		return result;
	}, [
		initialFocus,
		onEscapeKeyDown,
		onClose,
		onOpenChange,
		open,
		disableHeightTransition,
		mobile
	]);
	let initialFocusValue;
	if (typeof initialFocus === "string") initialFocusValue = footerAutoFocusRef;
	else initialFocusValue = initialFocus;
	return (0, import_jsx_runtime.jsx)(Modal, {
		...filterDOMProps(restProps, { labelable: true }),
		open,
		contentOverflow: mobile ? "auto" : contentOverflow,
		disableBodyScrollLock,
		disableEscapeKeyDown,
		disableOutsideClick,
		disableVisuallyHiddenDismiss: hasCloseButton,
		initialFocus: initialFocusValue,
		returnFocus,
		keepMounted,
		onEscapeKeyDown,
		onOutsideClick,
		onClose,
		onEnterKeyDown,
		onTransitionIn,
		onTransitionInComplete,
		onTransitionOut,
		onTransitionOutComplete,
		className: b$69("modal", modalClassName),
		container,
		qa,
		disableHeightTransition: true,
		children: (0, import_jsx_runtime.jsxs)("div", {
			className: b$69({
				size,
				mobile,
				"max-width": maxWidth,
				"full-width": mobile ? true : fullWidth,
				"has-close": hasCloseButton,
				"has-scroll": mobile ? true : contentOverflow === "auto"
			}, className),
			children: [hasCloseButton && (0, import_jsx_runtime.jsx)(ButtonClose, {
				mobile,
				onClose: handleCloseButtonClick
			}), (0, import_jsx_runtime.jsx)(DialogPrivateContext.Provider, {
				value: privateContextProps,
				children
			})]
		})
	});
}
Dialog.Footer = DialogFooter;
Dialog.Header = DialogHeader;
Dialog.Body = DialogBody;
Dialog.Divider = DialogDivider;
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Divider/Divider.js
var b$68 = block("divider");
var Divider = import_react.forwardRef(function Divider(rawProps, ref) {
	const { orientation = "horizontal", className, style, qa, children, align = "start", ...restProps } = useDefaultProps("Divider", rawProps);
	return (0, import_jsx_runtime.jsx)("div", {
		...filterDOMProps(restProps, { labelable: true }),
		className: b$68({
			orientation,
			align
		}, className),
		ref,
		style,
		"data-qa": qa,
		role: "separator",
		"aria-orientation": orientation === "vertical" ? "vertical" : void 0,
		children
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Drawer/hooks/useInitialFocus.js
function useInitialFocus({ open, initialFocus, floatingRef }) {
	import_react.useEffect(() => {
		if (!open || initialFocus === void 0) return;
		const timerId = setTimeout(() => {
			const floatingEl = floatingRef.current;
			if (!floatingEl) return;
			let elToFocus = null;
			if (typeof initialFocus === "number") elToFocus = tabbable(floatingEl)[initialFocus] ?? null;
			else elToFocus = initialFocus.current;
			elToFocus?.focus({ preventScroll: true });
		}, 0);
		return () => clearTimeout(timerId);
	}, [
		open,
		initialFocus,
		floatingRef
	]);
}
var i18n_default$11 = addComponentKeysets({
	en: { close: "Close" },
	ru: { close: "Закрыть" }
}, "Drawer");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Drawer/hooks/useResizeHandlers.js
function useResizeHandlers({ onStart, onMove, onEnd, arrangement = "horizontal" }) {
	const startRef = import_react.useRef(0);
	const handleMove = import_react.useCallback((evt) => {
		const current = arrangement === "horizontal" ? evt.clientX : evt.clientY;
		onMove(startRef.current - current);
	}, [arrangement, onMove]);
	const handleUp = import_react.useCallback((evt) => {
		window.removeEventListener("pointermove", handleMove);
		onEnd(startRef.current - (arrangement === "horizontal" ? evt.clientX : evt.clientY), evt);
	}, [
		arrangement,
		handleMove,
		onEnd
	]);
	const onPointerDown = import_react.useCallback((e) => {
		e.preventDefault();
		const axisPos = arrangement === "horizontal" ? e.clientX : e.clientY;
		startRef.current = axisPos;
		onStart();
		window.addEventListener("pointermove", handleMove);
		window.addEventListener("pointerup", handleUp, { once: true });
	}, [
		onStart,
		arrangement,
		handleMove,
		handleUp
	]);
	import_react.useEffect(() => {
		return () => {
			window.removeEventListener("pointermove", handleMove);
			window.removeEventListener("pointerup", handleUp);
		};
	}, [handleMove, handleUp]);
	return { onPointerDown };
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Drawer/hooks/useResizableDrawerItem.js
function useResizableDrawerItem(params) {
	const { placement = "left", size, minSize = 200, maxSize = 800, onResizeStart, onResizeEnd, onResize, overlayRef } = params;
	const isHorizontal = ["left", "right"].includes(placement);
	const getOverlayMaxSize = import_react.useCallback(() => {
		return isHorizontal ? overlayRef.current?.clientWidth ?? 0 : overlayRef.current?.clientHeight ?? 0;
	}, [isHorizontal, overlayRef]);
	const [isResizing, setIsResizing] = import_react.useState(false);
	const [resizeDelta, setResizeDelta] = import_react.useState(0);
	const [internalSize, setInternalSize] = import_react.useState(size ?? 400);
	import_react.useEffect(() => {
		if (size !== void 0 && !isResizing) setInternalSize(size);
	}, [size, isResizing]);
	const getClampedSize = import_react.useCallback((curSize) => Math.min(Math.max(curSize, minSize), maxSize), [minSize, maxSize]);
	const getResizedSize = import_react.useCallback((delta) => {
		const signedDelta = ["right", "bottom"].includes(placement) ? delta : -delta;
		const overlaySize = getOverlayMaxSize();
		const newSize = Math.min(internalSize + signedDelta, overlaySize);
		return getClampedSize(newSize);
	}, [
		internalSize,
		placement,
		getClampedSize,
		getOverlayMaxSize
	]);
	const onStart = import_react.useCallback(() => {
		setIsResizing(true);
		setResizeDelta(0);
		onResizeStart?.(getResizedSize(0));
	}, [onResizeStart, getResizedSize]);
	const onMove = import_react.useCallback((delta) => {
		setResizeDelta(delta);
		onResize?.(getResizedSize(delta));
	}, [getResizedSize, onResize]);
	const onEnd = import_react.useCallback((delta) => {
		const newSize = getResizedSize(delta);
		setIsResizing(false);
		setInternalSize(newSize);
		onResizeEnd?.(newSize);
	}, [getResizedSize, onResizeEnd]);
	const displaySize = isResizing ? getResizedSize(resizeDelta) : getClampedSize(size ?? internalSize);
	const { onPointerDown: onResizerPointerDown } = useResizeHandlers({
		onStart,
		onMove,
		onEnd,
		arrangement: isHorizontal ? "horizontal" : "vertical"
	});
	const handleCommonResize = import_react.useCallback(() => {
		const overlaySize = getOverlayMaxSize();
		if (overlaySize >= internalSize) return;
		setInternalSize(Math.max(minSize, overlaySize));
		onResize?.(overlaySize);
		onResizeEnd?.(overlaySize);
	}, [
		getOverlayMaxSize,
		minSize,
		internalSize,
		onResize,
		onResizeEnd
	]);
	import_react.useEffect(() => {
		window.addEventListener("resize", handleCommonResize);
		return () => {
			window.removeEventListener("resize", handleCommonResize);
		};
	}, [handleCommonResize]);
	return {
		currentSize: displaySize,
		onResizerPointerDown
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Drawer/components/DrawerItem.js
var b$67 = block("drawer");
var DrawerItem = import_react.forwardRef(function DrawerItem(props, ref) {
	const { open, children, placement = "left", className, resizable, size, minSize, maxSize, onResizeStart, onResizeEnd, onResize, style = {}, contentOverflow = "visible", overlayRef, ...restProps } = props;
	const { currentSize, onResizerPointerDown } = useResizableDrawerItem({
		placement,
		size: typeof size === "number" ? size : void 0,
		minSize,
		maxSize,
		onResizeStart,
		onResizeEnd,
		onResize,
		overlayRef
	});
	const isHorizontalArrangement = ["left", "right"].includes(placement);
	const isVerticalArrangement = !isHorizontalArrangement;
	const width = isHorizontalArrangement && (resizable || size !== "auto") ? `${currentSize}px` : void 0;
	const height = isVerticalArrangement && (resizable || size !== "auto") ? `${currentSize}px` : void 0;
	const resizerElement = resizable ? (0, import_jsx_runtime.jsx)("div", {
		className: b$67("resizer"),
		onPointerDown: open && resizable ? onResizerPointerDown : void 0,
		children: (0, import_jsx_runtime.jsx)("div", { className: b$67("resizer-handle") })
	}) : null;
	return (0, import_jsx_runtime.jsxs)("div", {
		ref,
		className: b$67("item", {
			resizable,
			hidden: !open
		}, className),
		style: {
			...style,
			width,
			height
		},
		...restProps,
		children: [resizerElement, (0, import_jsx_runtime.jsx)("div", {
			className: b$67("item-content", { "has-scroll": contentOverflow === "auto" }),
			children
		})]
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Drawer/components/Drawer.js
var b$66 = block("drawer");
var Drawer = (rawProps) => {
	const { open, onOpenChange, placement = "left", children, contentClassName, resizable = false, size, minSize, maxSize, onResizeStart, onResizeEnd, onResize, className, style, qa, disableEscapeKeyDown, initialFocus, returnFocus, disableBodyScrollLock = false, contentOverflow = "visible", disableVisuallyHiddenDismiss, onTransitionIn, onTransitionInComplete, onTransitionOut, onTransitionOutComplete, floatingRef, disablePortal, keepMounted = false, container, hideVeil = false, disableTransition = false, ...restProps } = useDefaultProps("Drawer", rawProps);
	const floatingNodeId = useFloatingNodeId();
	const disableOutsideClick = hideVeil || restProps.disableOutsideClick;
	const { refs, context } = useFloating({
		nodeId: floatingNodeId,
		open,
		onOpenChange
	});
	const overlayRef = import_react.useRef(null);
	const { isMounted, status } = useFloatingTransition({
		context,
		duration: disableTransition ? 0 : 300,
		onTransitionIn,
		onTransitionInComplete,
		onTransitionOut,
		onTransitionOutComplete
	});
	useInitialFocus({
		open,
		initialFocus,
		floatingRef: refs.floating
	});
	const { getFloatingProps } = useInteractions([useDismiss(context, {
		enabled: !disableEscapeKeyDown || !disableOutsideClick,
		outsidePress: (event) => {
			if (disableOutsideClick) return false;
			if (!(event.target.closest(`.${b$66()}`) === overlayRef.current)) return false;
			return true;
		},
		escapeKey: !disableEscapeKeyDown
	}), useRole(context, { role: "dialog" })]);
	const handleFloatingRef = useForkRef(refs.setFloating, floatingRef);
	const composedStyle = import_react.useMemo(() => ({
		position: disablePortal ? "absolute" : "fixed",
		"--_--animation-duration": disableTransition ? "0ms" : `300ms`,
		...style
	}), [
		disablePortal,
		disableTransition,
		style
	]);
	return (0, import_jsx_runtime.jsx)(FloatingNode, {
		id: floatingNodeId,
		children: isMounted || keepMounted ? (0, import_jsx_runtime.jsx)(Portal, {
			container,
			disablePortal,
			children: (0, import_jsx_runtime.jsx)(FloatingOverlay, {
				style: composedStyle,
				ref: overlayRef,
				className: b$66({
					open,
					placement,
					"hide-veil": hideVeil
				}, className),
				"data-qa": qa,
				"data-floating-ui-status": status,
				lockScroll: !disableBodyScrollLock,
				children: (0, import_jsx_runtime.jsx)(FloatingFocusManager, {
					context,
					disabled: !isMounted,
					modal: isMounted,
					initialFocus: refs.floating,
					returnFocus,
					visuallyHiddenDismiss: disableVisuallyHiddenDismiss ? false : i18n_default$11("close"),
					restoreFocus: true,
					children: (0, import_jsx_runtime.jsx)(DrawerItem, {
						ref: handleFloatingRef,
						open,
						placement,
						className: contentClassName,
						contentOverflow,
						resizable,
						size,
						onResize,
						onResizeStart,
						onResizeEnd,
						minSize,
						maxSize,
						overlayRef,
						...filterDOMProps(restProps, { labelable: true }),
						...getFloatingProps(),
						children
					})
				})
			})
		}) : null
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/FilePreview/FilePreviewActions/DesktopActionsMenu/DesktopActionsMenu.js
var cn$2 = block("file-preview-actions-desktop");
var DesktopActionsMenu = ({ actions, hoverabelPanelClassName }) => {
	const id = useUniqId();
	return (0, import_jsx_runtime.jsx)("div", {
		className: cn$2(null, hoverabelPanelClassName),
		children: actions.map(({ title, icon, onClick, disabled, tooltipExtraProps, ...props }, index) => {
			const extraProps = props.href ? {
				...props.extraProps,
				href: props.href
			} : props.extraProps;
			return (0, import_jsx_runtime.jsx)(ActionTooltip, {
				title,
				...tooltipExtraProps,
				children: (0, import_jsx_runtime.jsx)(Button, {
					onClick,
					"aria-describedby": `${id}-${index}`,
					view: "raised",
					pin: "circle-circle",
					disabled,
					size: "s",
					className: cn$2("action-button"),
					"aria-label": title,
					...extraProps,
					children: (0, import_jsx_runtime.jsx)("div", {
						className: cn$2("action-icon-wrapper"),
						children: icon
					})
				})
			}, `${id}-${index}`);
		})
	});
};
//#endregion
//#region node_modules/.pnpm/redux@5.0.1/node_modules/redux/dist/redux.mjs
var symbol_observable_default = /* @__PURE__ */ (() => typeof Symbol === "function" && Symbol.observable || "@@observable")();
var randomString = () => Math.random().toString(36).substring(7).split("").join(".");
var actionTypes_default = {
	INIT: `@@redux/INIT${/* @__PURE__ */ randomString()}`,
	REPLACE: `@@redux/REPLACE${/* @__PURE__ */ randomString()}`,
	PROBE_UNKNOWN_ACTION: () => `@@redux/PROBE_UNKNOWN_ACTION${randomString()}`
};
function isPlainObject$1(obj) {
	if (typeof obj !== "object" || obj === null) return false;
	let proto = obj;
	while (Object.getPrototypeOf(proto) !== null) proto = Object.getPrototypeOf(proto);
	return Object.getPrototypeOf(obj) === proto || Object.getPrototypeOf(obj) === null;
}
function miniKindOf(val) {
	if (val === void 0) return "undefined";
	if (val === null) return "null";
	const type = typeof val;
	switch (type) {
		case "boolean":
		case "string":
		case "number":
		case "symbol":
		case "function": return type;
	}
	if (Array.isArray(val)) return "array";
	if (isDate(val)) return "date";
	if (isError(val)) return "error";
	const constructorName = ctorName(val);
	switch (constructorName) {
		case "Symbol":
		case "Promise":
		case "WeakMap":
		case "WeakSet":
		case "Map":
		case "Set": return constructorName;
	}
	return Object.prototype.toString.call(val).slice(8, -1).toLowerCase().replace(/\s/g, "");
}
function ctorName(val) {
	return typeof val.constructor === "function" ? val.constructor.name : null;
}
function isError(val) {
	return val instanceof Error || typeof val.message === "string" && val.constructor && typeof val.constructor.stackTraceLimit === "number";
}
function isDate(val) {
	if (val instanceof Date) return true;
	return typeof val.toDateString === "function" && typeof val.getDate === "function" && typeof val.setDate === "function";
}
function kindOf(val) {
	let typeOfVal = typeof val;
	typeOfVal = miniKindOf(val);
	return typeOfVal;
}
function createStore$1(reducer, preloadedState, enhancer) {
	if (typeof reducer !== "function") throw new Error(`Expected the root reducer to be a function. Instead, received: '${kindOf(reducer)}'`);
	if (typeof preloadedState === "function" && typeof enhancer === "function" || typeof enhancer === "function" && typeof arguments[3] === "function") throw new Error("It looks like you are passing several store enhancers to createStore(). This is not supported. Instead, compose them together to a single function. See https://redux.js.org/tutorials/fundamentals/part-4-store#creating-a-store-with-enhancers for an example.");
	if (typeof preloadedState === "function" && typeof enhancer === "undefined") {
		enhancer = preloadedState;
		preloadedState = void 0;
	}
	if (typeof enhancer !== "undefined") {
		if (typeof enhancer !== "function") throw new Error(`Expected the enhancer to be a function. Instead, received: '${kindOf(enhancer)}'`);
		return enhancer(createStore$1)(reducer, preloadedState);
	}
	let currentReducer = reducer;
	let currentState = preloadedState;
	let currentListeners = /* @__PURE__ */ new Map();
	let nextListeners = currentListeners;
	let listenerIdCounter = 0;
	let isDispatching = false;
	function ensureCanMutateNextListeners() {
		if (nextListeners === currentListeners) {
			nextListeners = /* @__PURE__ */ new Map();
			currentListeners.forEach((listener, key) => {
				nextListeners.set(key, listener);
			});
		}
	}
	function getState() {
		if (isDispatching) throw new Error("You may not call store.getState() while the reducer is executing. The reducer has already received the state as an argument. Pass it down from the top reducer instead of reading it from the store.");
		return currentState;
	}
	function subscribe(listener) {
		if (typeof listener !== "function") throw new Error(`Expected the listener to be a function. Instead, received: '${kindOf(listener)}'`);
		if (isDispatching) throw new Error("You may not call store.subscribe() while the reducer is executing. If you would like to be notified after the store has been updated, subscribe from a component and invoke store.getState() in the callback to access the latest state. See https://redux.js.org/api/store#subscribelistener for more details.");
		let isSubscribed = true;
		ensureCanMutateNextListeners();
		const listenerId = listenerIdCounter++;
		nextListeners.set(listenerId, listener);
		return function unsubscribe() {
			if (!isSubscribed) return;
			if (isDispatching) throw new Error("You may not unsubscribe from a store listener while the reducer is executing. See https://redux.js.org/api/store#subscribelistener for more details.");
			isSubscribed = false;
			ensureCanMutateNextListeners();
			nextListeners.delete(listenerId);
			currentListeners = null;
		};
	}
	function dispatch(action) {
		if (!isPlainObject$1(action)) throw new Error(`Actions must be plain objects. Instead, the actual type was: '${kindOf(action)}'. You may need to add middleware to your store setup to handle dispatching other values, such as 'redux-thunk' to handle dispatching functions. See https://redux.js.org/tutorials/fundamentals/part-4-store#middleware and https://redux.js.org/tutorials/fundamentals/part-6-async-logic#using-the-redux-thunk-middleware for examples.`);
		if (typeof action.type === "undefined") throw new Error("Actions may not have an undefined \"type\" property. You may have misspelled an action type string constant.");
		if (typeof action.type !== "string") throw new Error(`Action "type" property must be a string. Instead, the actual type was: '${kindOf(action.type)}'. Value was: '${action.type}' (stringified)`);
		if (isDispatching) throw new Error("Reducers may not dispatch actions.");
		try {
			isDispatching = true;
			currentState = currentReducer(currentState, action);
		} finally {
			isDispatching = false;
		}
		(currentListeners = nextListeners).forEach((listener) => {
			listener();
		});
		return action;
	}
	function replaceReducer(nextReducer) {
		if (typeof nextReducer !== "function") throw new Error(`Expected the nextReducer to be a function. Instead, received: '${kindOf(nextReducer)}`);
		currentReducer = nextReducer;
		dispatch({ type: actionTypes_default.REPLACE });
	}
	function observable() {
		const outerSubscribe = subscribe;
		return {
			/**
			* The minimal observable subscription method.
			* @param observer Any object that can be used as an observer.
			* The observer object should have a `next` method.
			* @returns An object with an `unsubscribe` method that can
			* be used to unsubscribe the observable from the store, and prevent further
			* emission of values from the observable.
			*/
			subscribe(observer) {
				if (typeof observer !== "object" || observer === null) throw new Error(`Expected the observer to be an object. Instead, received: '${kindOf(observer)}'`);
				function observeState() {
					const observerAsObserver = observer;
					if (observerAsObserver.next) observerAsObserver.next(getState());
				}
				observeState();
				return { unsubscribe: outerSubscribe(observeState) };
			},
			[symbol_observable_default]() {
				return this;
			}
		};
	}
	dispatch({ type: actionTypes_default.INIT });
	return {
		dispatch,
		subscribe,
		getState,
		replaceReducer,
		[symbol_observable_default]: observable
	};
}
function bindActionCreator(actionCreator, dispatch) {
	return function(...args) {
		return dispatch(actionCreator.apply(this, args));
	};
}
function bindActionCreators$1(actionCreators, dispatch) {
	if (typeof actionCreators === "function") return bindActionCreator(actionCreators, dispatch);
	if (typeof actionCreators !== "object" || actionCreators === null) throw new Error(`bindActionCreators expected an object or a function, but instead received: '${kindOf(actionCreators)}'. Did you write "import ActionCreators from" instead of "import * as ActionCreators from"?`);
	const boundActionCreators = {};
	for (const key in actionCreators) {
		const actionCreator = actionCreators[key];
		if (typeof actionCreator === "function") boundActionCreators[key] = bindActionCreator(actionCreator, dispatch);
	}
	return boundActionCreators;
}
function compose(...funcs) {
	if (funcs.length === 0) return (arg) => arg;
	if (funcs.length === 1) return funcs[0];
	return funcs.reduce((a, b) => (...args) => a(b(...args)));
}
function applyMiddleware(...middlewares) {
	return (createStore2) => (reducer, preloadedState) => {
		const store = createStore2(reducer, preloadedState);
		let dispatch = () => {
			throw new Error("Dispatching while constructing your middleware is not allowed. Other middleware would not be applied to this dispatch.");
		};
		const middlewareAPI = {
			getState: store.getState,
			dispatch: (action, ...args) => dispatch(action, ...args)
		};
		dispatch = compose(...middlewares.map((middleware) => middleware(middlewareAPI)))(store.dispatch);
		return {
			...store,
			dispatch
		};
	};
}
//#endregion
//#region node_modules/.pnpm/use-sync-external-store@1.6.0_react@19.2.7/node_modules/use-sync-external-store/cjs/use-sync-external-store-with-selector.development.js
/**
* @license React
* use-sync-external-store-with-selector.development.js
*
* Copyright (c) Meta Platforms, Inc. and affiliates.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_use_sync_external_store_with_selector_development = /* @__PURE__ */ __commonJSMin(((exports) => {
	(function() {
		function is(x, y) {
			return x === y && (0 !== x || 1 / x === 1 / y) || x !== x && y !== y;
		}
		"undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
		var React = require_react(), objectIs = "function" === typeof Object.is ? Object.is : is, useSyncExternalStore = React.useSyncExternalStore, useRef = React.useRef, useEffect = React.useEffect, useMemo = React.useMemo, useDebugValue = React.useDebugValue;
		exports.useSyncExternalStoreWithSelector = function(subscribe, getSnapshot, getServerSnapshot, selector, isEqual) {
			var instRef = useRef(null);
			if (null === instRef.current) {
				var inst = {
					hasValue: !1,
					value: null
				};
				instRef.current = inst;
			} else inst = instRef.current;
			instRef = useMemo(function() {
				function memoizedSelector(nextSnapshot) {
					if (!hasMemo) {
						hasMemo = !0;
						memoizedSnapshot = nextSnapshot;
						nextSnapshot = selector(nextSnapshot);
						if (void 0 !== isEqual && inst.hasValue) {
							var currentSelection = inst.value;
							if (isEqual(currentSelection, nextSnapshot)) return memoizedSelection = currentSelection;
						}
						return memoizedSelection = nextSnapshot;
					}
					currentSelection = memoizedSelection;
					if (objectIs(memoizedSnapshot, nextSnapshot)) return currentSelection;
					var nextSelection = selector(nextSnapshot);
					if (void 0 !== isEqual && isEqual(currentSelection, nextSelection)) return memoizedSnapshot = nextSnapshot, currentSelection;
					memoizedSnapshot = nextSnapshot;
					return memoizedSelection = nextSelection;
				}
				var hasMemo = !1, memoizedSnapshot, memoizedSelection, maybeGetServerSnapshot = void 0 === getServerSnapshot ? null : getServerSnapshot;
				return [function() {
					return memoizedSelector(getSnapshot());
				}, null === maybeGetServerSnapshot ? void 0 : function() {
					return memoizedSelector(maybeGetServerSnapshot());
				}];
			}, [
				getSnapshot,
				getServerSnapshot,
				selector,
				isEqual
			]);
			var value = useSyncExternalStore(subscribe, instRef[0], instRef[1]);
			useEffect(function() {
				inst.hasValue = !0;
				inst.value = value;
			}, [value]);
			useDebugValue(value);
			return value;
		};
		"undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
	})();
}));
(/* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_use_sync_external_store_with_selector_development();
})))();
var IS_REACT_19 = /* @__PURE__ */ "19.2.7".startsWith("19");
var REACT_ELEMENT_TYPE = /* @__PURE__ */ Symbol.for(IS_REACT_19 ? "react.transitional.element" : "react.element");
var REACT_PORTAL_TYPE = /* @__PURE__ */ Symbol.for("react.portal");
var REACT_FRAGMENT_TYPE = /* @__PURE__ */ Symbol.for("react.fragment");
var REACT_STRICT_MODE_TYPE = /* @__PURE__ */ Symbol.for("react.strict_mode");
var REACT_PROFILER_TYPE = /* @__PURE__ */ Symbol.for("react.profiler");
var REACT_CONSUMER_TYPE = /* @__PURE__ */ Symbol.for("react.consumer");
var REACT_CONTEXT_TYPE = /* @__PURE__ */ Symbol.for("react.context");
var REACT_FORWARD_REF_TYPE = /* @__PURE__ */ Symbol.for("react.forward_ref");
var REACT_SUSPENSE_TYPE = /* @__PURE__ */ Symbol.for("react.suspense");
var REACT_SUSPENSE_LIST_TYPE = /* @__PURE__ */ Symbol.for("react.suspense_list");
var REACT_MEMO_TYPE = /* @__PURE__ */ Symbol.for("react.memo");
var REACT_LAZY_TYPE = /* @__PURE__ */ Symbol.for("react.lazy");
var REACT_OFFSCREEN_TYPE = /* @__PURE__ */ Symbol.for("react.offscreen");
var REACT_CLIENT_REFERENCE = /* @__PURE__ */ Symbol.for("react.client.reference");
var ForwardRef = REACT_FORWARD_REF_TYPE;
var Memo = REACT_MEMO_TYPE;
function isValidElementType(type) {
	return typeof type === "string" || typeof type === "function" || type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || type === REACT_OFFSCREEN_TYPE || typeof type === "object" && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_CONSUMER_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_CLIENT_REFERENCE || type.getModuleId !== void 0) ? true : false;
}
function typeOf(object) {
	if (typeof object === "object" && object !== null) {
		const { $$typeof } = object;
		switch ($$typeof) {
			case REACT_ELEMENT_TYPE: switch (object = object.type, object) {
				case REACT_FRAGMENT_TYPE:
				case REACT_PROFILER_TYPE:
				case REACT_STRICT_MODE_TYPE:
				case REACT_SUSPENSE_TYPE:
				case REACT_SUSPENSE_LIST_TYPE: return object;
				default: switch (object = object && object.$$typeof, object) {
					case REACT_CONTEXT_TYPE:
					case REACT_FORWARD_REF_TYPE:
					case REACT_LAZY_TYPE:
					case REACT_MEMO_TYPE: return object;
					case REACT_CONSUMER_TYPE: return object;
					default: return $$typeof;
				}
			}
			case REACT_PORTAL_TYPE: return $$typeof;
		}
	}
}
function isContextConsumer(object) {
	return IS_REACT_19 ? typeOf(object) === REACT_CONSUMER_TYPE : typeOf(object) === REACT_CONTEXT_TYPE;
}
function isMemo(object) {
	return typeOf(object) === REACT_MEMO_TYPE;
}
function warning$2(message) {
	if (typeof console !== "undefined" && typeof console.error === "function") console.error(message);
	try {
		throw new Error(message);
	} catch (e) {}
}
function verify(selector, methodName) {
	if (!selector) throw new Error(`Unexpected value for ${methodName} in connect.`);
	else if (methodName === "mapStateToProps" || methodName === "mapDispatchToProps") {
		if (!Object.prototype.hasOwnProperty.call(selector, "dependsOnOwnProps")) warning$2(`The selector for ${methodName} of connect did not specify a value for dependsOnOwnProps.`);
	}
}
function verifySubselectors(mapStateToProps, mapDispatchToProps, mergeProps) {
	verify(mapStateToProps, "mapStateToProps");
	verify(mapDispatchToProps, "mapDispatchToProps");
	verify(mergeProps, "mergeProps");
}
function pureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, { areStatesEqual, areOwnPropsEqual, areStatePropsEqual }) {
	let hasRunAtLeastOnce = false;
	let state;
	let ownProps;
	let stateProps;
	let dispatchProps;
	let mergedProps;
	function handleFirstCall(firstState, firstOwnProps) {
		state = firstState;
		ownProps = firstOwnProps;
		stateProps = mapStateToProps(state, ownProps);
		dispatchProps = mapDispatchToProps(dispatch, ownProps);
		mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
		hasRunAtLeastOnce = true;
		return mergedProps;
	}
	function handleNewPropsAndNewState() {
		stateProps = mapStateToProps(state, ownProps);
		if (mapDispatchToProps.dependsOnOwnProps) dispatchProps = mapDispatchToProps(dispatch, ownProps);
		mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
		return mergedProps;
	}
	function handleNewProps() {
		if (mapStateToProps.dependsOnOwnProps) stateProps = mapStateToProps(state, ownProps);
		if (mapDispatchToProps.dependsOnOwnProps) dispatchProps = mapDispatchToProps(dispatch, ownProps);
		mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
		return mergedProps;
	}
	function handleNewState() {
		const nextStateProps = mapStateToProps(state, ownProps);
		const statePropsChanged = !areStatePropsEqual(nextStateProps, stateProps);
		stateProps = nextStateProps;
		if (statePropsChanged) mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
		return mergedProps;
	}
	function handleSubsequentCalls(nextState, nextOwnProps) {
		const propsChanged = !areOwnPropsEqual(nextOwnProps, ownProps);
		const stateChanged = !areStatesEqual(nextState, state, nextOwnProps, ownProps);
		state = nextState;
		ownProps = nextOwnProps;
		if (propsChanged && stateChanged) return handleNewPropsAndNewState();
		if (propsChanged) return handleNewProps();
		if (stateChanged) return handleNewState();
		return mergedProps;
	}
	return function pureFinalPropsSelector(nextState, nextOwnProps) {
		return hasRunAtLeastOnce ? handleSubsequentCalls(nextState, nextOwnProps) : handleFirstCall(nextState, nextOwnProps);
	};
}
function finalPropsSelectorFactory(dispatch, { initMapStateToProps, initMapDispatchToProps, initMergeProps, ...options }) {
	const mapStateToProps = initMapStateToProps(dispatch, options);
	const mapDispatchToProps = initMapDispatchToProps(dispatch, options);
	const mergeProps = initMergeProps(dispatch, options);
	verifySubselectors(mapStateToProps, mapDispatchToProps, mergeProps);
	return pureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, options);
}
function bindActionCreators(actionCreators, dispatch) {
	const boundActionCreators = {};
	for (const key in actionCreators) {
		const actionCreator = actionCreators[key];
		if (typeof actionCreator === "function") boundActionCreators[key] = (...args) => dispatch(actionCreator(...args));
	}
	return boundActionCreators;
}
function isPlainObject(obj) {
	if (typeof obj !== "object" || obj === null) return false;
	const proto = Object.getPrototypeOf(obj);
	if (proto === null) return true;
	let baseProto = proto;
	while (Object.getPrototypeOf(baseProto) !== null) baseProto = Object.getPrototypeOf(baseProto);
	return proto === baseProto;
}
function verifyPlainObject(value, displayName, methodName) {
	if (!isPlainObject(value)) warning$2(`${methodName}() in ${displayName} must return a plain object. Instead received ${value}.`);
}
function wrapMapToPropsConstant(getConstant) {
	return function initConstantSelector(dispatch) {
		const constant = getConstant(dispatch);
		function constantSelector() {
			return constant;
		}
		constantSelector.dependsOnOwnProps = false;
		return constantSelector;
	};
}
function getDependsOnOwnProps(mapToProps) {
	return mapToProps.dependsOnOwnProps ? Boolean(mapToProps.dependsOnOwnProps) : mapToProps.length !== 1;
}
function wrapMapToPropsFunc(mapToProps, methodName) {
	return function initProxySelector(dispatch, { displayName }) {
		const proxy = function mapToPropsProxy(stateOrDispatch, ownProps) {
			return proxy.dependsOnOwnProps ? proxy.mapToProps(stateOrDispatch, ownProps) : proxy.mapToProps(stateOrDispatch, void 0);
		};
		proxy.dependsOnOwnProps = true;
		proxy.mapToProps = function detectFactoryAndVerify(stateOrDispatch, ownProps) {
			proxy.mapToProps = mapToProps;
			proxy.dependsOnOwnProps = getDependsOnOwnProps(mapToProps);
			let props = proxy(stateOrDispatch, ownProps);
			if (typeof props === "function") {
				proxy.mapToProps = props;
				proxy.dependsOnOwnProps = getDependsOnOwnProps(props);
				props = proxy(stateOrDispatch, ownProps);
			}
			verifyPlainObject(props, displayName, methodName);
			return props;
		};
		return proxy;
	};
}
function createInvalidArgFactory(arg, name) {
	return (dispatch, options) => {
		throw new Error(`Invalid value of type ${typeof arg} for ${name} argument when connecting component ${options.wrappedComponentName}.`);
	};
}
function mapDispatchToPropsFactory(mapDispatchToProps) {
	return mapDispatchToProps && typeof mapDispatchToProps === "object" ? wrapMapToPropsConstant((dispatch) => bindActionCreators(mapDispatchToProps, dispatch)) : !mapDispatchToProps ? wrapMapToPropsConstant((dispatch) => ({ dispatch })) : typeof mapDispatchToProps === "function" ? wrapMapToPropsFunc(mapDispatchToProps, "mapDispatchToProps") : createInvalidArgFactory(mapDispatchToProps, "mapDispatchToProps");
}
function mapStateToPropsFactory(mapStateToProps) {
	return !mapStateToProps ? wrapMapToPropsConstant(() => ({})) : typeof mapStateToProps === "function" ? wrapMapToPropsFunc(mapStateToProps, "mapStateToProps") : createInvalidArgFactory(mapStateToProps, "mapStateToProps");
}
function defaultMergeProps(stateProps, dispatchProps, ownProps) {
	return {
		...ownProps,
		...stateProps,
		...dispatchProps
	};
}
function wrapMergePropsFunc(mergeProps) {
	return function initMergePropsProxy(dispatch, { displayName, areMergedPropsEqual }) {
		let hasRunOnce = false;
		let mergedProps;
		return function mergePropsProxy(stateProps, dispatchProps, ownProps) {
			const nextMergedProps = mergeProps(stateProps, dispatchProps, ownProps);
			if (hasRunOnce) {
				if (!areMergedPropsEqual(nextMergedProps, mergedProps)) mergedProps = nextMergedProps;
			} else {
				hasRunOnce = true;
				mergedProps = nextMergedProps;
				verifyPlainObject(mergedProps, displayName, "mergeProps");
			}
			return mergedProps;
		};
	};
}
function mergePropsFactory(mergeProps) {
	return !mergeProps ? () => defaultMergeProps : typeof mergeProps === "function" ? wrapMergePropsFunc(mergeProps) : createInvalidArgFactory(mergeProps, "mergeProps");
}
function defaultNoopBatch(callback) {
	callback();
}
function createListenerCollection() {
	let first = null;
	let last = null;
	return {
		clear() {
			first = null;
			last = null;
		},
		notify() {
			defaultNoopBatch(() => {
				let listener = first;
				while (listener) {
					listener.callback();
					listener = listener.next;
				}
			});
		},
		get() {
			const listeners = [];
			let listener = first;
			while (listener) {
				listeners.push(listener);
				listener = listener.next;
			}
			return listeners;
		},
		subscribe(callback) {
			let isSubscribed = true;
			const listener = last = {
				callback,
				next: null,
				prev: last
			};
			if (listener.prev) listener.prev.next = listener;
			else first = listener;
			return function unsubscribe() {
				if (!isSubscribed || first === null) return;
				isSubscribed = false;
				if (listener.next) listener.next.prev = listener.prev;
				else last = listener.prev;
				if (listener.prev) listener.prev.next = listener.next;
				else first = listener.next;
			};
		}
	};
}
var nullListeners = {
	notify() {},
	get: () => []
};
function createSubscription(store, parentSub) {
	let unsubscribe;
	let listeners = nullListeners;
	let subscriptionsAmount = 0;
	let selfSubscribed = false;
	function addNestedSub(listener) {
		trySubscribe();
		const cleanupListener = listeners.subscribe(listener);
		let removed = false;
		return () => {
			if (!removed) {
				removed = true;
				cleanupListener();
				tryUnsubscribe();
			}
		};
	}
	function notifyNestedSubs() {
		listeners.notify();
	}
	function handleChangeWrapper() {
		if (subscription.onStateChange) subscription.onStateChange();
	}
	function isSubscribed() {
		return selfSubscribed;
	}
	function trySubscribe() {
		subscriptionsAmount++;
		if (!unsubscribe) {
			unsubscribe = parentSub ? parentSub.addNestedSub(handleChangeWrapper) : store.subscribe(handleChangeWrapper);
			listeners = createListenerCollection();
		}
	}
	function tryUnsubscribe() {
		subscriptionsAmount--;
		if (unsubscribe && subscriptionsAmount === 0) {
			unsubscribe();
			unsubscribe = void 0;
			listeners.clear();
			listeners = nullListeners;
		}
	}
	function trySubscribeSelf() {
		if (!selfSubscribed) {
			selfSubscribed = true;
			trySubscribe();
		}
	}
	function tryUnsubscribeSelf() {
		if (selfSubscribed) {
			selfSubscribed = false;
			tryUnsubscribe();
		}
	}
	const subscription = {
		addNestedSub,
		notifyNestedSubs,
		handleChangeWrapper,
		isSubscribed,
		trySubscribe: trySubscribeSelf,
		tryUnsubscribe: tryUnsubscribeSelf,
		getListeners: () => listeners
	};
	return subscription;
}
var canUseDOM = () => !!(typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined");
var isDOM = /* @__PURE__ */ canUseDOM();
var isRunningInReactNative = () => typeof navigator !== "undefined" && navigator.product === "ReactNative";
var isReactNative = /* @__PURE__ */ isRunningInReactNative();
var getUseIsomorphicLayoutEffect = () => isDOM || isReactNative ? import_react.useLayoutEffect : import_react.useEffect;
var useIsomorphicLayoutEffect$1 = /* @__PURE__ */ getUseIsomorphicLayoutEffect();
function is(x, y) {
	if (x === y) return x !== 0 || y !== 0 || 1 / x === 1 / y;
	else return x !== x && y !== y;
}
function shallowEqual(objA, objB) {
	if (is(objA, objB)) return true;
	if (typeof objA !== "object" || objA === null || typeof objB !== "object" || objB === null) return false;
	const keysA = Object.keys(objA);
	const keysB = Object.keys(objB);
	if (keysA.length !== keysB.length) return false;
	for (let i = 0; i < keysA.length; i++) if (!Object.prototype.hasOwnProperty.call(objB, keysA[i]) || !is(objA[keysA[i]], objB[keysA[i]])) return false;
	return true;
}
var REACT_STATICS = {
	childContextTypes: true,
	contextType: true,
	contextTypes: true,
	defaultProps: true,
	displayName: true,
	getDefaultProps: true,
	getDerivedStateFromError: true,
	getDerivedStateFromProps: true,
	mixins: true,
	propTypes: true,
	type: true
};
var KNOWN_STATICS = {
	name: true,
	length: true,
	prototype: true,
	caller: true,
	callee: true,
	arguments: true,
	arity: true
};
var FORWARD_REF_STATICS = {
	$$typeof: true,
	render: true,
	defaultProps: true,
	displayName: true,
	propTypes: true
};
var MEMO_STATICS = {
	$$typeof: true,
	compare: true,
	defaultProps: true,
	displayName: true,
	propTypes: true,
	type: true
};
var TYPE_STATICS = {
	[ForwardRef]: FORWARD_REF_STATICS,
	[Memo]: MEMO_STATICS
};
function getStatics(component) {
	if (isMemo(component)) return MEMO_STATICS;
	return TYPE_STATICS[component["$$typeof"]] || REACT_STATICS;
}
var defineProperty = Object.defineProperty;
var getOwnPropertyNames = Object.getOwnPropertyNames;
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
var getPrototypeOf = Object.getPrototypeOf;
var objectPrototype = Object.prototype;
function hoistNonReactStatics(targetComponent, sourceComponent) {
	if (typeof sourceComponent !== "string") {
		if (objectPrototype) {
			const inheritedComponent = getPrototypeOf(sourceComponent);
			if (inheritedComponent && inheritedComponent !== objectPrototype) hoistNonReactStatics(targetComponent, inheritedComponent);
		}
		let keys = getOwnPropertyNames(sourceComponent);
		if (getOwnPropertySymbols) keys = keys.concat(getOwnPropertySymbols(sourceComponent));
		const targetStatics = getStatics(targetComponent);
		const sourceStatics = getStatics(sourceComponent);
		for (let i = 0; i < keys.length; ++i) {
			const key = keys[i];
			if (!KNOWN_STATICS[key] && !(sourceStatics && sourceStatics[key]) && !(targetStatics && targetStatics[key])) {
				const descriptor = getOwnPropertyDescriptor(sourceComponent, key);
				try {
					defineProperty(targetComponent, key, descriptor);
				} catch (e) {}
			}
		}
	}
	return targetComponent;
}
var ContextKey = /* @__PURE__ */ Symbol.for(`react-redux-context`);
var gT = typeof globalThis !== "undefined" ? globalThis : {};
function getContext() {
	if (!import_react.createContext) return {};
	const contextMap = gT[ContextKey] ??= /* @__PURE__ */ new Map();
	let realContext = contextMap.get(import_react.createContext);
	if (!realContext) {
		realContext = import_react.createContext(null);
		realContext.displayName = "ReactRedux";
		contextMap.set(import_react.createContext, realContext);
	}
	return realContext;
}
var ReactReduxContext = /* @__PURE__ */ getContext();
var NO_SUBSCRIPTION_ARRAY = [null, null];
var stringifyComponent = (Comp) => {
	try {
		return JSON.stringify(Comp);
	} catch (err) {
		return String(Comp);
	}
};
function useIsomorphicLayoutEffectWithArgs(effectFunc, effectArgs, dependencies) {
	useIsomorphicLayoutEffect$1(() => effectFunc(...effectArgs), dependencies);
}
function captureWrapperProps(lastWrapperProps, lastChildProps, renderIsScheduled, wrapperProps, childPropsFromStoreUpdate, notifyNestedSubs) {
	lastWrapperProps.current = wrapperProps;
	renderIsScheduled.current = false;
	if (childPropsFromStoreUpdate.current) {
		childPropsFromStoreUpdate.current = null;
		notifyNestedSubs();
	}
}
function subscribeUpdates(shouldHandleStateChanges, store, subscription, childPropsSelector, lastWrapperProps, lastChildProps, renderIsScheduled, isMounted, childPropsFromStoreUpdate, notifyNestedSubs, additionalSubscribeListener) {
	if (!shouldHandleStateChanges) return () => {};
	let didUnsubscribe = false;
	let lastThrownError = null;
	const checkForUpdates = () => {
		if (didUnsubscribe || !isMounted.current) return;
		const latestStoreState = store.getState();
		let newChildProps, error;
		try {
			newChildProps = childPropsSelector(latestStoreState, lastWrapperProps.current);
		} catch (e) {
			error = e;
			lastThrownError = e;
		}
		if (!error) lastThrownError = null;
		if (newChildProps === lastChildProps.current) {
			if (!renderIsScheduled.current) notifyNestedSubs();
		} else {
			lastChildProps.current = newChildProps;
			childPropsFromStoreUpdate.current = newChildProps;
			renderIsScheduled.current = true;
			additionalSubscribeListener();
		}
	};
	subscription.onStateChange = checkForUpdates;
	subscription.trySubscribe();
	checkForUpdates();
	const unsubscribeWrapper = () => {
		didUnsubscribe = true;
		subscription.tryUnsubscribe();
		subscription.onStateChange = null;
		if (lastThrownError) throw lastThrownError;
	};
	return unsubscribeWrapper;
}
function strictEqual(a, b) {
	return a === b;
}
var hasWarnedAboutDeprecatedPureOption = false;
function _connect(mapStateToProps, mapDispatchToProps, mergeProps, { pure, areStatesEqual = strictEqual, areOwnPropsEqual = shallowEqual, areStatePropsEqual = shallowEqual, areMergedPropsEqual = shallowEqual, forwardRef = false, context = ReactReduxContext } = {}) {
	if (pure !== void 0 && !hasWarnedAboutDeprecatedPureOption) {
		hasWarnedAboutDeprecatedPureOption = true;
		warning$2("The `pure` option has been removed. `connect` is now always a \"pure/memoized\" component");
	}
	const Context = context;
	const initMapStateToProps = mapStateToPropsFactory(mapStateToProps);
	const initMapDispatchToProps = mapDispatchToPropsFactory(mapDispatchToProps);
	const initMergeProps = mergePropsFactory(mergeProps);
	const shouldHandleStateChanges = Boolean(mapStateToProps);
	const wrapWithConnect = (WrappedComponent) => {
		if (!/* @__PURE__ */ isValidElementType(WrappedComponent)) throw new Error(`You must pass a component to the function returned by connect. Instead received ${stringifyComponent(WrappedComponent)}`);
		const wrappedComponentName = WrappedComponent.displayName || WrappedComponent.name || "Component";
		const displayName = `Connect(${wrappedComponentName})`;
		const selectorFactoryOptions = {
			shouldHandleStateChanges,
			displayName,
			wrappedComponentName,
			WrappedComponent,
			initMapStateToProps,
			initMapDispatchToProps,
			initMergeProps,
			areStatesEqual,
			areStatePropsEqual,
			areOwnPropsEqual,
			areMergedPropsEqual
		};
		function ConnectFunction(props) {
			const [propsContext, reactReduxForwardedRef, wrapperProps] = import_react.useMemo(() => {
				const { reactReduxForwardedRef: reactReduxForwardedRef2, ...wrapperProps2 } = props;
				return [
					props.context,
					reactReduxForwardedRef2,
					wrapperProps2
				];
			}, [props]);
			const ContextToUse = import_react.useMemo(() => {
				let ResultContext = Context;
				if (propsContext?.Consumer) {
					if (!/* @__PURE__ */ isContextConsumer(/* @__PURE__ */ import_react.createElement(propsContext.Consumer, null))) throw new Error("You must pass a valid React context consumer as `props.context`");
					ResultContext = propsContext;
				}
				return ResultContext;
			}, [propsContext, Context]);
			const contextValue = import_react.useContext(ContextToUse);
			const didStoreComeFromProps = Boolean(props.store) && Boolean(props.store.getState) && Boolean(props.store.dispatch);
			const didStoreComeFromContext = Boolean(contextValue) && Boolean(contextValue.store);
			if (!didStoreComeFromProps && !didStoreComeFromContext) throw new Error(`Could not find "store" in the context of "${displayName}". Either wrap the root component in a <Provider>, or pass a custom React context provider to <Provider> and the corresponding React context consumer to ${displayName} in connect options.`);
			const store = didStoreComeFromProps ? props.store : contextValue.store;
			const getServerState = didStoreComeFromContext ? contextValue.getServerState : store.getState;
			const childPropsSelector = import_react.useMemo(() => {
				return finalPropsSelectorFactory(store.dispatch, selectorFactoryOptions);
			}, [store]);
			const [subscription, notifyNestedSubs] = import_react.useMemo(() => {
				if (!shouldHandleStateChanges) return NO_SUBSCRIPTION_ARRAY;
				const subscription2 = createSubscription(store, didStoreComeFromProps ? void 0 : contextValue.subscription);
				return [subscription2, subscription2.notifyNestedSubs.bind(subscription2)];
			}, [
				store,
				didStoreComeFromProps,
				contextValue
			]);
			const overriddenContextValue = import_react.useMemo(() => {
				if (didStoreComeFromProps) return contextValue;
				return {
					...contextValue,
					subscription
				};
			}, [
				didStoreComeFromProps,
				contextValue,
				subscription
			]);
			const lastChildProps = import_react.useRef(void 0);
			const lastWrapperProps = import_react.useRef(wrapperProps);
			const childPropsFromStoreUpdate = import_react.useRef(void 0);
			const renderIsScheduled = import_react.useRef(false);
			const isMounted = import_react.useRef(false);
			const latestSubscriptionCallbackError = import_react.useRef(void 0);
			useIsomorphicLayoutEffect$1(() => {
				isMounted.current = true;
				return () => {
					isMounted.current = false;
				};
			}, []);
			const actualChildPropsSelector = import_react.useMemo(() => {
				const selector = () => {
					if (childPropsFromStoreUpdate.current && wrapperProps === lastWrapperProps.current) return childPropsFromStoreUpdate.current;
					return childPropsSelector(store.getState(), wrapperProps);
				};
				return selector;
			}, [store, wrapperProps]);
			const subscribeForReact = import_react.useMemo(() => {
				const subscribe = (reactListener) => {
					if (!subscription) return () => {};
					return subscribeUpdates(shouldHandleStateChanges, store, subscription, childPropsSelector, lastWrapperProps, lastChildProps, renderIsScheduled, isMounted, childPropsFromStoreUpdate, notifyNestedSubs, reactListener);
				};
				return subscribe;
			}, [subscription]);
			useIsomorphicLayoutEffectWithArgs(captureWrapperProps, [
				lastWrapperProps,
				lastChildProps,
				renderIsScheduled,
				wrapperProps,
				childPropsFromStoreUpdate,
				notifyNestedSubs
			]);
			let actualChildProps;
			try {
				actualChildProps = import_react.useSyncExternalStore(subscribeForReact, actualChildPropsSelector, getServerState ? () => childPropsSelector(getServerState(), wrapperProps) : actualChildPropsSelector);
			} catch (err) {
				if (latestSubscriptionCallbackError.current) err.message += `
The error may be correlated with this previous error:
${latestSubscriptionCallbackError.current.stack}

`;
				throw err;
			}
			useIsomorphicLayoutEffect$1(() => {
				latestSubscriptionCallbackError.current = void 0;
				childPropsFromStoreUpdate.current = void 0;
				lastChildProps.current = actualChildProps;
			});
			const renderedWrappedComponent = import_react.useMemo(() => {
				return /* @__PURE__ */ import_react.createElement(WrappedComponent, {
					...actualChildProps,
					ref: reactReduxForwardedRef
				});
			}, [
				reactReduxForwardedRef,
				WrappedComponent,
				actualChildProps
			]);
			return import_react.useMemo(() => {
				if (shouldHandleStateChanges) return /* @__PURE__ */ import_react.createElement(ContextToUse.Provider, { value: overriddenContextValue }, renderedWrappedComponent);
				return renderedWrappedComponent;
			}, [
				ContextToUse,
				renderedWrappedComponent,
				overriddenContextValue
			]);
		}
		const Connect = import_react.memo(ConnectFunction);
		Connect.WrappedComponent = WrappedComponent;
		Connect.displayName = ConnectFunction.displayName = displayName;
		if (forwardRef) {
			const forwarded = import_react.forwardRef(function forwardConnectRef(props, ref) {
				return /* @__PURE__ */ import_react.createElement(Connect, {
					...props,
					reactReduxForwardedRef: ref
				});
			});
			forwarded.displayName = displayName;
			forwarded.WrappedComponent = WrappedComponent;
			return /* @__PURE__ */ hoistNonReactStatics(forwarded, WrappedComponent);
		}
		return /* @__PURE__ */ hoistNonReactStatics(Connect, WrappedComponent);
	};
	return wrapWithConnect;
}
var connect = _connect;
function Provider(providerProps) {
	const { children, context, serverState, store } = providerProps;
	const contextValue = import_react.useMemo(() => {
		const subscription = createSubscription(store);
		const baseContextValue = {
			store,
			subscription,
			getServerState: serverState ? () => serverState : void 0
		};
		{
			const { identityFunctionCheck = "once", stabilityCheck = "once" } = providerProps;
			return /* @__PURE__ */ Object.assign(baseContextValue, {
				stabilityCheck,
				identityFunctionCheck
			});
		}
	}, [store, serverState]);
	const previousState = import_react.useMemo(() => store.getState(), [store]);
	useIsomorphicLayoutEffect$1(() => {
		const { subscription } = contextValue;
		subscription.onStateChange = subscription.notifyNestedSubs;
		subscription.trySubscribe();
		if (previousState !== store.getState()) subscription.notifyNestedSubs();
		return () => {
			subscription.tryUnsubscribe();
			subscription.onStateChange = void 0;
		};
	}, [contextValue, previousState]);
	const Context = context || ReactReduxContext;
	return /* @__PURE__ */ import_react.createElement(Context.Provider, { value: contextValue }, children);
}
var Provider_default = Provider;
//#endregion
//#region node_modules/.pnpm/tiny-invariant@1.3.3/node_modules/tiny-invariant/dist/esm/tiny-invariant.js
var prefix$2 = "Invariant failed";
function invariant$1(condition, message) {
	if (condition) return;
	var provided = typeof message === "function" ? message() : message;
	var value = provided ? "".concat(prefix$2, ": ").concat(provided) : prefix$2;
	throw new Error(value);
}
//#endregion
//#region node_modules/.pnpm/css-box-model@1.2.1/node_modules/css-box-model/dist/css-box-model.esm.js
var getRect = function getRect(_ref) {
	var top = _ref.top, right = _ref.right, bottom = _ref.bottom, left = _ref.left;
	return {
		top,
		right,
		bottom,
		left,
		width: right - left,
		height: bottom - top,
		x: left,
		y: top,
		center: {
			x: (right + left) / 2,
			y: (bottom + top) / 2
		}
	};
};
var expand = function expand(target, expandBy) {
	return {
		top: target.top - expandBy.top,
		left: target.left - expandBy.left,
		bottom: target.bottom + expandBy.bottom,
		right: target.right + expandBy.right
	};
};
var shrink = function shrink(target, shrinkBy) {
	return {
		top: target.top + shrinkBy.top,
		left: target.left + shrinkBy.left,
		bottom: target.bottom - shrinkBy.bottom,
		right: target.right - shrinkBy.right
	};
};
var shift = function shift(target, shiftBy) {
	return {
		top: target.top + shiftBy.y,
		left: target.left + shiftBy.x,
		bottom: target.bottom + shiftBy.y,
		right: target.right + shiftBy.x
	};
};
var noSpacing$1 = {
	top: 0,
	right: 0,
	bottom: 0,
	left: 0
};
var createBox = function createBox(_ref2) {
	var borderBox = _ref2.borderBox, _ref2$margin = _ref2.margin, margin = _ref2$margin === void 0 ? noSpacing$1 : _ref2$margin, _ref2$border = _ref2.border, border = _ref2$border === void 0 ? noSpacing$1 : _ref2$border, _ref2$padding = _ref2.padding, padding = _ref2$padding === void 0 ? noSpacing$1 : _ref2$padding;
	var marginBox = getRect(expand(borderBox, margin));
	var paddingBox = getRect(shrink(borderBox, border));
	var contentBox = getRect(shrink(paddingBox, padding));
	return {
		marginBox,
		borderBox: getRect(borderBox),
		paddingBox,
		contentBox,
		margin,
		border,
		padding
	};
};
var parse = function parse(raw) {
	var value = raw.slice(0, -2);
	if (raw.slice(-2) !== "px") return 0;
	var result = Number(value);
	isNaN(result) && invariant$1(false, "Could not parse value [raw: " + raw + ", without suffix: " + value + "]");
	return result;
};
var getWindowScroll$1 = function getWindowScroll() {
	return {
		x: window.pageXOffset,
		y: window.pageYOffset
	};
};
var offset = function offset(original, change) {
	var borderBox = original.borderBox, border = original.border, margin = original.margin, padding = original.padding;
	return createBox({
		borderBox: shift(borderBox, change),
		border,
		margin,
		padding
	});
};
var withScroll = function withScroll(original, scroll) {
	if (scroll === void 0) scroll = getWindowScroll$1();
	return offset(original, scroll);
};
var calculateBox = function calculateBox(borderBox, styles) {
	return createBox({
		borderBox,
		margin: {
			top: parse(styles.marginTop),
			right: parse(styles.marginRight),
			bottom: parse(styles.marginBottom),
			left: parse(styles.marginLeft)
		},
		padding: {
			top: parse(styles.paddingTop),
			right: parse(styles.paddingRight),
			bottom: parse(styles.paddingBottom),
			left: parse(styles.paddingLeft)
		},
		border: {
			top: parse(styles.borderTopWidth),
			right: parse(styles.borderRightWidth),
			bottom: parse(styles.borderBottomWidth),
			left: parse(styles.borderLeftWidth)
		}
	});
};
var getBox = function getBox(el) {
	return calculateBox(el.getBoundingClientRect(), window.getComputedStyle(el));
};
//#endregion
//#region node_modules/.pnpm/raf-schd@4.0.3/node_modules/raf-schd/dist/raf-schd.esm.js
var rafSchd = function rafSchd(fn) {
	var lastArgs = [];
	var frameId = null;
	var wrapperFn = function wrapperFn() {
		for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
		lastArgs = args;
		if (frameId) return;
		frameId = requestAnimationFrame(function() {
			frameId = null;
			fn.apply(void 0, lastArgs);
		});
	};
	wrapperFn.cancel = function() {
		if (!frameId) return;
		cancelAnimationFrame(frameId);
		frameId = null;
	};
	return wrapperFn;
};
//#endregion
//#region node_modules/.pnpm/@hello-pangea+dnd@18.0.1_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@hello-pangea/dnd/dist/dnd.esm.js
var spacesAndTabs = /[ \t]{2,}/g;
var lineStartWithSpaces = /^[ \t]*/gm;
var clean$2 = (value) => value.replace(spacesAndTabs, " ").replace(lineStartWithSpaces, "").trim();
var getDevMessage = (message) => clean$2(`
  %c@hello-pangea/dnd

  %c${clean$2(message)}

  %c👷‍ This is a development only message. It will be removed in production builds.
`);
var getFormattedMessage = (message) => [
	getDevMessage(message),
	"color: #00C584; font-size: 1.2em; font-weight: bold;",
	"line-height: 1.5",
	"color: #723874;"
];
var isDisabledFlag = "__@hello-pangea/dnd-disable-dev-warnings";
function log(type, message) {
	if (typeof window !== "undefined" && window[isDisabledFlag]) return;
	console[type](...getFormattedMessage(message));
}
var warning$1 = log.bind(null, "warn");
var error = log.bind(null, "error");
function noop$2() {}
function getOptions(shared, fromBinding) {
	return {
		...shared,
		...fromBinding
	};
}
function bindEvents(el, bindings, sharedOptions) {
	const unbindings = bindings.map((binding) => {
		const options = getOptions(sharedOptions, binding.options);
		el.addEventListener(binding.eventName, binding.fn, options);
		return function unbind() {
			el.removeEventListener(binding.eventName, binding.fn, options);
		};
	});
	return function unbindAll() {
		unbindings.forEach((unbind) => {
			unbind();
		});
	};
}
var prefix$1 = "Invariant failed";
var RbdInvariant = class extends Error {};
RbdInvariant.prototype.toString = function toString() {
	return this.message;
};
function invariant(condition, message) {
	throw new RbdInvariant(`${prefix$1}: ${message || ""}`);
}
var ErrorBoundary = class extends import_react.Component {
	constructor(...args) {
		super(...args);
		this.callbacks = null;
		this.unbind = noop$2;
		this.onWindowError = (event) => {
			const callbacks = this.getCallbacks();
			if (callbacks.isDragging()) {
				callbacks.tryAbort();
				warning$1(`
        An error was caught by our window 'error' event listener while a drag was occurring.
        The active drag has been aborted.
      `);
			}
			const err = event.error;
			if (err instanceof RbdInvariant) {
				event.preventDefault();
				error(err.message);
			}
		};
		this.getCallbacks = () => {
			if (!this.callbacks) throw new Error("Unable to find AppCallbacks in <ErrorBoundary/>");
			return this.callbacks;
		};
		this.setCallbacks = (callbacks) => {
			this.callbacks = callbacks;
		};
	}
	componentDidMount() {
		this.unbind = bindEvents(window, [{
			eventName: "error",
			fn: this.onWindowError
		}]);
	}
	componentDidCatch(err) {
		if (err instanceof RbdInvariant) {
			error(err.message);
			this.setState({});
			return;
		}
		throw err;
	}
	componentWillUnmount() {
		this.unbind();
	}
	render() {
		return this.props.children(this.setCallbacks);
	}
};
var dragHandleUsageInstructions = `
  Press space bar to start a drag.
  When dragging you can use the arrow keys to move the item around and escape to cancel.
  Some screen readers may require you to be in focus mode or to use your pass through key
`;
var position = (index) => index + 1;
var onDragStart = (start) => `
  You have lifted an item in position ${position(start.source.index)}
`;
var withLocation = (source, destination) => {
	const isInHomeList = source.droppableId === destination.droppableId;
	const startPosition = position(source.index);
	const endPosition = position(destination.index);
	if (isInHomeList) return `
      You have moved the item from position ${startPosition}
      to position ${endPosition}
    `;
	return `
    You have moved the item from position ${startPosition}
    in list ${source.droppableId}
    to list ${destination.droppableId}
    in position ${endPosition}
  `;
};
var withCombine = (id, source, combine) => {
	if (source.droppableId === combine.droppableId) return `
      The item ${id}
      has been combined with ${combine.draggableId}`;
	return `
      The item ${id}
      in list ${source.droppableId}
      has been combined with ${combine.draggableId}
      in list ${combine.droppableId}
    `;
};
var onDragUpdate = (update) => {
	const location = update.destination;
	if (location) return withLocation(update.source, location);
	const combine = update.combine;
	if (combine) return withCombine(update.draggableId, update.source, combine);
	return "You are over an area that cannot be dropped on";
};
var returnedToStart = (source) => `
  The item has returned to its starting position
  of ${position(source.index)}
`;
var onDragEnd = (result) => {
	if (result.reason === "CANCEL") return `
      Movement cancelled.
      ${returnedToStart(result.source)}
    `;
	const location = result.destination;
	const combine = result.combine;
	if (location) return `
      You have dropped the item.
      ${withLocation(result.source, location)}
    `;
	if (combine) return `
      You have dropped the item.
      ${withCombine(result.draggableId, result.source, combine)}
    `;
	return `
    The item has been dropped while not over a drop area.
    ${returnedToStart(result.source)}
  `;
};
var preset = {
	dragHandleUsageInstructions,
	onDragStart,
	onDragUpdate,
	onDragEnd
};
function isEqual$2$1(first, second) {
	if (first === second) return true;
	if (Number.isNaN(first) && Number.isNaN(second)) return true;
	return false;
}
function areInputsEqual$1(newInputs, lastInputs) {
	if (newInputs.length !== lastInputs.length) return false;
	for (let i = 0; i < newInputs.length; i++) if (!isEqual$2$1(newInputs[i], lastInputs[i])) return false;
	return true;
}
function useMemo$1(getResult, inputs) {
	const initial = (0, import_react.useState)(() => ({
		inputs,
		result: getResult()
	}))[0];
	const isFirstRun = (0, import_react.useRef)(true);
	const committed = (0, import_react.useRef)(initial);
	const cache = isFirstRun.current || Boolean(inputs && committed.current.inputs && areInputsEqual$1(inputs, committed.current.inputs)) ? committed.current : {
		inputs,
		result: getResult()
	};
	(0, import_react.useEffect)(() => {
		isFirstRun.current = false;
		committed.current = cache;
	}, [cache]);
	return cache.result;
}
function useCallback(callback, inputs) {
	return useMemo$1(() => callback, inputs);
}
var origin = {
	x: 0,
	y: 0
};
var add = (point1, point2) => ({
	x: point1.x + point2.x,
	y: point1.y + point2.y
});
var subtract = (point1, point2) => ({
	x: point1.x - point2.x,
	y: point1.y - point2.y
});
var isEqual$1$1 = (point1, point2) => point1.x === point2.x && point1.y === point2.y;
var negate = (point) => ({
	x: point.x !== 0 ? -point.x : 0,
	y: point.y !== 0 ? -point.y : 0
});
var patch = (line, value, otherValue = 0) => {
	if (line === "x") return {
		x: value,
		y: otherValue
	};
	return {
		x: otherValue,
		y: value
	};
};
var distance = (point1, point2) => Math.sqrt((point2.x - point1.x) ** 2 + (point2.y - point1.y) ** 2);
var closest$1 = (target, points) => Math.min(...points.map((point) => distance(target, point)));
var apply = (fn) => (point) => ({
	x: fn(point.x),
	y: fn(point.y)
});
var executeClip = (frame, subject) => {
	const result = getRect({
		top: Math.max(subject.top, frame.top),
		right: Math.min(subject.right, frame.right),
		bottom: Math.min(subject.bottom, frame.bottom),
		left: Math.max(subject.left, frame.left)
	});
	if (result.width <= 0 || result.height <= 0) return null;
	return result;
};
var offsetByPosition = (spacing, point) => ({
	top: spacing.top + point.y,
	left: spacing.left + point.x,
	bottom: spacing.bottom + point.y,
	right: spacing.right + point.x
});
var getCorners = (spacing) => [
	{
		x: spacing.left,
		y: spacing.top
	},
	{
		x: spacing.right,
		y: spacing.top
	},
	{
		x: spacing.left,
		y: spacing.bottom
	},
	{
		x: spacing.right,
		y: spacing.bottom
	}
];
var noSpacing = {
	top: 0,
	right: 0,
	bottom: 0,
	left: 0
};
var scroll$1 = (target, frame) => {
	if (!frame) return target;
	return offsetByPosition(target, frame.scroll.diff.displacement);
};
var increase = (target, axis, withPlaceholder) => {
	if (withPlaceholder && withPlaceholder.increasedBy) return {
		...target,
		[axis.end]: target[axis.end] + withPlaceholder.increasedBy[axis.line]
	};
	return target;
};
var clip = (target, frame) => {
	if (frame && frame.shouldClipSubject) return executeClip(frame.pageMarginBox, target);
	return getRect(target);
};
var getSubject = ({ page, withPlaceholder, axis, frame }) => {
	return {
		page,
		withPlaceholder,
		active: clip(increase(scroll$1(page.marginBox, frame), axis, withPlaceholder), frame)
	};
};
var scrollDroppable = (droppable, newScroll) => {
	!droppable.frame && invariant();
	const scrollable = droppable.frame;
	const scrollDiff = subtract(newScroll, scrollable.scroll.initial);
	const scrollDisplacement = negate(scrollDiff);
	const frame = {
		...scrollable,
		scroll: {
			initial: scrollable.scroll.initial,
			current: newScroll,
			diff: {
				value: scrollDiff,
				displacement: scrollDisplacement
			},
			max: scrollable.scroll.max
		}
	};
	const subject = getSubject({
		page: droppable.subject.page,
		withPlaceholder: droppable.subject.withPlaceholder,
		axis: droppable.axis,
		frame
	});
	return {
		...droppable,
		frame,
		subject
	};
};
function memoizeOne$1(resultFn, isEqual = areInputsEqual$1) {
	let cache = null;
	function memoized(...newArgs) {
		if (cache && cache.lastThis === this && isEqual(newArgs, cache.lastArgs)) return cache.lastResult;
		const lastResult = resultFn.apply(this, newArgs);
		cache = {
			lastResult,
			lastArgs: newArgs,
			lastThis: this
		};
		return lastResult;
	}
	memoized.clear = function clear() {
		cache = null;
	};
	return memoized;
}
var toDroppableMap = memoizeOne$1((droppables) => droppables.reduce((previous, current) => {
	previous[current.descriptor.id] = current;
	return previous;
}, {}));
var toDraggableMap = memoizeOne$1((draggables) => draggables.reduce((previous, current) => {
	previous[current.descriptor.id] = current;
	return previous;
}, {}));
var toDroppableList = memoizeOne$1((droppables) => Object.values(droppables));
var toDraggableList = memoizeOne$1((draggables) => Object.values(draggables));
var getDraggablesInsideDroppable = memoizeOne$1((droppableId, draggables) => {
	return toDraggableList(draggables).filter((draggable) => droppableId === draggable.descriptor.droppableId).sort((a, b) => a.descriptor.index - b.descriptor.index);
});
function tryGetDestination(impact) {
	if (impact.at && impact.at.type === "REORDER") return impact.at.destination;
	return null;
}
function tryGetCombine(impact) {
	if (impact.at && impact.at.type === "COMBINE") return impact.at.combine;
	return null;
}
var removeDraggableFromList = memoizeOne$1((remove, list) => list.filter((item) => item.descriptor.id !== remove.descriptor.id));
var moveToNextCombine = ({ isMovingForward, draggable, destination, insideDestination, previousImpact }) => {
	if (!destination.isCombineEnabled) return null;
	if (!tryGetDestination(previousImpact)) return null;
	function getImpact(target) {
		const at = {
			type: "COMBINE",
			combine: {
				draggableId: target,
				droppableId: destination.descriptor.id
			}
		};
		return {
			...previousImpact,
			at
		};
	}
	const all = previousImpact.displaced.all;
	const closestId = all.length ? all[0] : null;
	if (isMovingForward) return closestId ? getImpact(closestId) : null;
	const withoutDraggable = removeDraggableFromList(draggable, insideDestination);
	if (!closestId) {
		if (!withoutDraggable.length) return null;
		const last = withoutDraggable[withoutDraggable.length - 1];
		return getImpact(last.descriptor.id);
	}
	const indexOfClosest = withoutDraggable.findIndex((d) => d.descriptor.id === closestId);
	!(indexOfClosest !== -1) && invariant(false, "Could not find displaced item in set");
	const proposedIndex = indexOfClosest - 1;
	if (proposedIndex < 0) return null;
	const before = withoutDraggable[proposedIndex];
	return getImpact(before.descriptor.id);
};
var isHomeOf = (draggable, destination) => draggable.descriptor.droppableId === destination.descriptor.id;
var noDisplacedBy = {
	point: origin,
	value: 0
};
var emptyGroups = {
	invisible: {},
	visible: {},
	all: []
};
var noImpact = {
	displaced: emptyGroups,
	displacedBy: noDisplacedBy,
	at: null
};
var isWithin = (lowerBound, upperBound) => (value) => lowerBound <= value && value <= upperBound;
var isPartiallyVisibleThroughFrame = (frame) => {
	const isWithinVertical = isWithin(frame.top, frame.bottom);
	const isWithinHorizontal = isWithin(frame.left, frame.right);
	return (subject) => {
		if (isWithinVertical(subject.top) && isWithinVertical(subject.bottom) && isWithinHorizontal(subject.left) && isWithinHorizontal(subject.right)) return true;
		const isPartiallyVisibleVertically = isWithinVertical(subject.top) || isWithinVertical(subject.bottom);
		const isPartiallyVisibleHorizontally = isWithinHorizontal(subject.left) || isWithinHorizontal(subject.right);
		if (isPartiallyVisibleVertically && isPartiallyVisibleHorizontally) return true;
		const isBiggerVertically = subject.top < frame.top && subject.bottom > frame.bottom;
		const isBiggerHorizontally = subject.left < frame.left && subject.right > frame.right;
		if (isBiggerVertically && isBiggerHorizontally) return true;
		return isBiggerVertically && isPartiallyVisibleHorizontally || isBiggerHorizontally && isPartiallyVisibleVertically;
	};
};
var isTotallyVisibleThroughFrame = (frame) => {
	const isWithinVertical = isWithin(frame.top, frame.bottom);
	const isWithinHorizontal = isWithin(frame.left, frame.right);
	return (subject) => {
		return isWithinVertical(subject.top) && isWithinVertical(subject.bottom) && isWithinHorizontal(subject.left) && isWithinHorizontal(subject.right);
	};
};
var vertical = {
	direction: "vertical",
	line: "y",
	crossAxisLine: "x",
	start: "top",
	end: "bottom",
	size: "height",
	crossAxisStart: "left",
	crossAxisEnd: "right",
	crossAxisSize: "width"
};
var horizontal = {
	direction: "horizontal",
	line: "x",
	crossAxisLine: "y",
	start: "left",
	end: "right",
	size: "width",
	crossAxisStart: "top",
	crossAxisEnd: "bottom",
	crossAxisSize: "height"
};
var isTotallyVisibleThroughFrameOnAxis = (axis) => (frame) => {
	const isWithinVertical = isWithin(frame.top, frame.bottom);
	const isWithinHorizontal = isWithin(frame.left, frame.right);
	return (subject) => {
		if (axis === vertical) return isWithinVertical(subject.top) && isWithinVertical(subject.bottom);
		return isWithinHorizontal(subject.left) && isWithinHorizontal(subject.right);
	};
};
var getDroppableDisplaced = (target, destination) => {
	return offsetByPosition(target, destination.frame ? destination.frame.scroll.diff.displacement : origin);
};
var isVisibleInDroppable = (target, destination, isVisibleThroughFrameFn) => {
	if (!destination.subject.active) return false;
	return isVisibleThroughFrameFn(destination.subject.active)(target);
};
var isVisibleInViewport = (target, viewport, isVisibleThroughFrameFn) => isVisibleThroughFrameFn(viewport)(target);
var isVisible$1 = ({ target: toBeDisplaced, destination, viewport, withDroppableDisplacement, isVisibleThroughFrameFn }) => {
	const displacedTarget = withDroppableDisplacement ? getDroppableDisplaced(toBeDisplaced, destination) : toBeDisplaced;
	return isVisibleInDroppable(displacedTarget, destination, isVisibleThroughFrameFn) && isVisibleInViewport(displacedTarget, viewport, isVisibleThroughFrameFn);
};
var isPartiallyVisible = (args) => isVisible$1({
	...args,
	isVisibleThroughFrameFn: isPartiallyVisibleThroughFrame
});
var isTotallyVisible = (args) => isVisible$1({
	...args,
	isVisibleThroughFrameFn: isTotallyVisibleThroughFrame
});
var isTotallyVisibleOnAxis = (args) => isVisible$1({
	...args,
	isVisibleThroughFrameFn: isTotallyVisibleThroughFrameOnAxis(args.destination.axis)
});
var getShouldAnimate = (id, last, forceShouldAnimate) => {
	if (typeof forceShouldAnimate === "boolean") return forceShouldAnimate;
	if (!last) return true;
	const { invisible, visible } = last;
	if (invisible[id]) return false;
	const previous = visible[id];
	return previous ? previous.shouldAnimate : true;
};
function getTarget(draggable, displacedBy) {
	const marginBox = draggable.page.marginBox;
	return getRect(expand(marginBox, {
		top: displacedBy.point.y,
		right: 0,
		bottom: 0,
		left: displacedBy.point.x
	}));
}
function getDisplacementGroups({ afterDragging, destination, displacedBy, viewport, forceShouldAnimate, last }) {
	return afterDragging.reduce(function process(groups, draggable) {
		const target = getTarget(draggable, displacedBy);
		const id = draggable.descriptor.id;
		groups.all.push(id);
		if (!isPartiallyVisible({
			target,
			destination,
			viewport,
			withDroppableDisplacement: true
		})) {
			groups.invisible[draggable.descriptor.id] = true;
			return groups;
		}
		const displacement = {
			draggableId: id,
			shouldAnimate: getShouldAnimate(id, last, forceShouldAnimate)
		};
		groups.visible[id] = displacement;
		return groups;
	}, {
		all: [],
		visible: {},
		invisible: {}
	});
}
function getIndexOfLastItem(draggables, options) {
	if (!draggables.length) return 0;
	const indexOfLastItem = draggables[draggables.length - 1].descriptor.index;
	return options.inHomeList ? indexOfLastItem : indexOfLastItem + 1;
}
function goAtEnd({ insideDestination, inHomeList, displacedBy, destination }) {
	const newIndex = getIndexOfLastItem(insideDestination, { inHomeList });
	return {
		displaced: emptyGroups,
		displacedBy,
		at: {
			type: "REORDER",
			destination: {
				droppableId: destination.descriptor.id,
				index: newIndex
			}
		}
	};
}
function calculateReorderImpact({ draggable, insideDestination, destination, viewport, displacedBy, last, index, forceShouldAnimate }) {
	const inHomeList = isHomeOf(draggable, destination);
	if (index == null) return goAtEnd({
		insideDestination,
		inHomeList,
		displacedBy,
		destination
	});
	const match = insideDestination.find((item) => item.descriptor.index === index);
	if (!match) return goAtEnd({
		insideDestination,
		inHomeList,
		displacedBy,
		destination
	});
	const withoutDragging = removeDraggableFromList(draggable, insideDestination);
	const sliceFrom = insideDestination.indexOf(match);
	return {
		displaced: getDisplacementGroups({
			afterDragging: withoutDragging.slice(sliceFrom),
			destination,
			displacedBy,
			last,
			viewport: viewport.frame,
			forceShouldAnimate
		}),
		displacedBy,
		at: {
			type: "REORDER",
			destination: {
				droppableId: destination.descriptor.id,
				index
			}
		}
	};
}
function didStartAfterCritical(draggableId, afterCritical) {
	return Boolean(afterCritical.effected[draggableId]);
}
var fromCombine = ({ isMovingForward, destination, draggables, combine, afterCritical }) => {
	if (!destination.isCombineEnabled) return null;
	const combineId = combine.draggableId;
	const combineWithIndex = draggables[combineId].descriptor.index;
	if (didStartAfterCritical(combineId, afterCritical)) {
		if (isMovingForward) return combineWithIndex;
		return combineWithIndex - 1;
	}
	if (isMovingForward) return combineWithIndex + 1;
	return combineWithIndex;
};
var fromReorder = ({ isMovingForward, isInHomeList, insideDestination, location }) => {
	if (!insideDestination.length) return null;
	const currentIndex = location.index;
	const proposedIndex = isMovingForward ? currentIndex + 1 : currentIndex - 1;
	const firstIndex = insideDestination[0].descriptor.index;
	const lastIndex = insideDestination[insideDestination.length - 1].descriptor.index;
	const upperBound = isInHomeList ? lastIndex : lastIndex + 1;
	if (proposedIndex < firstIndex) return null;
	if (proposedIndex > upperBound) return null;
	return proposedIndex;
};
var moveToNextIndex = ({ isMovingForward, isInHomeList, draggable, draggables, destination, insideDestination, previousImpact, viewport, afterCritical }) => {
	const wasAt = previousImpact.at;
	!wasAt && invariant(false, "Cannot move in direction without previous impact location");
	if (wasAt.type === "REORDER") {
		const newIndex = fromReorder({
			isMovingForward,
			isInHomeList,
			location: wasAt.destination,
			insideDestination
		});
		if (newIndex == null) return null;
		return calculateReorderImpact({
			draggable,
			insideDestination,
			destination,
			viewport,
			last: previousImpact.displaced,
			displacedBy: previousImpact.displacedBy,
			index: newIndex
		});
	}
	const newIndex = fromCombine({
		isMovingForward,
		destination,
		displaced: previousImpact.displaced,
		draggables,
		combine: wasAt.combine,
		afterCritical
	});
	if (newIndex == null) return null;
	return calculateReorderImpact({
		draggable,
		insideDestination,
		destination,
		viewport,
		last: previousImpact.displaced,
		displacedBy: previousImpact.displacedBy,
		index: newIndex
	});
};
var getCombinedItemDisplacement = ({ displaced, afterCritical, combineWith, displacedBy }) => {
	const isDisplaced = Boolean(displaced.visible[combineWith] || displaced.invisible[combineWith]);
	if (didStartAfterCritical(combineWith, afterCritical)) return isDisplaced ? origin : negate(displacedBy.point);
	return isDisplaced ? displacedBy.point : origin;
};
var whenCombining = ({ afterCritical, impact, draggables }) => {
	const combine = tryGetCombine(impact);
	!combine && invariant();
	const combineWith = combine.draggableId;
	const center = draggables[combineWith].page.borderBox.center;
	return add(center, getCombinedItemDisplacement({
		displaced: impact.displaced,
		afterCritical,
		combineWith,
		displacedBy: impact.displacedBy
	}));
};
var distanceFromStartToBorderBoxCenter = (axis, box) => box.margin[axis.start] + box.borderBox[axis.size] / 2;
var distanceFromEndToBorderBoxCenter = (axis, box) => box.margin[axis.end] + box.borderBox[axis.size] / 2;
var getCrossAxisBorderBoxCenter = (axis, target, isMoving) => target[axis.crossAxisStart] + isMoving.margin[axis.crossAxisStart] + isMoving.borderBox[axis.crossAxisSize] / 2;
var goAfter = ({ axis, moveRelativeTo, isMoving }) => patch(axis.line, moveRelativeTo.marginBox[axis.end] + distanceFromStartToBorderBoxCenter(axis, isMoving), getCrossAxisBorderBoxCenter(axis, moveRelativeTo.marginBox, isMoving));
var goBefore = ({ axis, moveRelativeTo, isMoving }) => patch(axis.line, moveRelativeTo.marginBox[axis.start] - distanceFromEndToBorderBoxCenter(axis, isMoving), getCrossAxisBorderBoxCenter(axis, moveRelativeTo.marginBox, isMoving));
var goIntoStart = ({ axis, moveInto, isMoving }) => patch(axis.line, moveInto.contentBox[axis.start] + distanceFromStartToBorderBoxCenter(axis, isMoving), getCrossAxisBorderBoxCenter(axis, moveInto.contentBox, isMoving));
var whenReordering = ({ impact, draggable, draggables, droppable, afterCritical }) => {
	const insideDestination = getDraggablesInsideDroppable(droppable.descriptor.id, draggables);
	const draggablePage = draggable.page;
	const axis = droppable.axis;
	if (!insideDestination.length) return goIntoStart({
		axis,
		moveInto: droppable.page,
		isMoving: draggablePage
	});
	const { displaced, displacedBy } = impact;
	const closestAfter = displaced.all[0];
	if (closestAfter) {
		const closest = draggables[closestAfter];
		if (didStartAfterCritical(closestAfter, afterCritical)) return goBefore({
			axis,
			moveRelativeTo: closest.page,
			isMoving: draggablePage
		});
		return goBefore({
			axis,
			moveRelativeTo: offset(closest.page, displacedBy.point),
			isMoving: draggablePage
		});
	}
	const last = insideDestination[insideDestination.length - 1];
	if (last.descriptor.id === draggable.descriptor.id) return draggablePage.borderBox.center;
	if (didStartAfterCritical(last.descriptor.id, afterCritical)) return goAfter({
		axis,
		moveRelativeTo: offset(last.page, negate(afterCritical.displacedBy.point)),
		isMoving: draggablePage
	});
	return goAfter({
		axis,
		moveRelativeTo: last.page,
		isMoving: draggablePage
	});
};
var withDroppableDisplacement = (droppable, point) => {
	const frame = droppable.frame;
	if (!frame) return point;
	return add(point, frame.scroll.diff.displacement);
};
var getResultWithoutDroppableDisplacement = ({ impact, draggable, droppable, draggables, afterCritical }) => {
	const original = draggable.page.borderBox.center;
	const at = impact.at;
	if (!droppable) return original;
	if (!at) return original;
	if (at.type === "REORDER") return whenReordering({
		impact,
		draggable,
		draggables,
		droppable,
		afterCritical
	});
	return whenCombining({
		impact,
		draggables,
		afterCritical
	});
};
var getPageBorderBoxCenterFromImpact = (args) => {
	const withoutDisplacement = getResultWithoutDroppableDisplacement(args);
	const droppable = args.droppable;
	return droppable ? withDroppableDisplacement(droppable, withoutDisplacement) : withoutDisplacement;
};
var scrollViewport = (viewport, newScroll) => {
	const diff = subtract(newScroll, viewport.scroll.initial);
	const displacement = negate(diff);
	return {
		frame: getRect({
			top: newScroll.y,
			bottom: newScroll.y + viewport.frame.height,
			left: newScroll.x,
			right: newScroll.x + viewport.frame.width
		}),
		scroll: {
			initial: viewport.scroll.initial,
			max: viewport.scroll.max,
			current: newScroll,
			diff: {
				value: diff,
				displacement
			}
		}
	};
};
function getDraggables$1(ids, draggables) {
	return ids.map((id) => draggables[id]);
}
function tryGetVisible(id, groups) {
	for (let i = 0; i < groups.length; i++) {
		const displacement = groups[i].visible[id];
		if (displacement) return displacement;
	}
	return null;
}
var speculativelyIncrease = ({ impact, viewport, destination, draggables, maxScrollChange }) => {
	const scrolledViewport = scrollViewport(viewport, add(viewport.scroll.current, maxScrollChange));
	const scrolledDroppable = destination.frame ? scrollDroppable(destination, add(destination.frame.scroll.current, maxScrollChange)) : destination;
	const last = impact.displaced;
	const withViewportScroll = getDisplacementGroups({
		afterDragging: getDraggables$1(last.all, draggables),
		destination,
		displacedBy: impact.displacedBy,
		viewport: scrolledViewport.frame,
		last,
		forceShouldAnimate: false
	});
	const withDroppableScroll = getDisplacementGroups({
		afterDragging: getDraggables$1(last.all, draggables),
		destination: scrolledDroppable,
		displacedBy: impact.displacedBy,
		viewport: viewport.frame,
		last,
		forceShouldAnimate: false
	});
	const invisible = {};
	const visible = {};
	const groups = [
		last,
		withViewportScroll,
		withDroppableScroll
	];
	last.all.forEach((id) => {
		const displacement = tryGetVisible(id, groups);
		if (displacement) {
			visible[id] = displacement;
			return;
		}
		invisible[id] = true;
	});
	return {
		...impact,
		displaced: {
			all: last.all,
			invisible,
			visible
		}
	};
};
var withViewportDisplacement = (viewport, point) => add(viewport.scroll.diff.displacement, point);
var getClientFromPageBorderBoxCenter = ({ pageBorderBoxCenter, draggable, viewport }) => {
	const offset = subtract(withViewportDisplacement(viewport, pageBorderBoxCenter), draggable.page.borderBox.center);
	return add(draggable.client.borderBox.center, offset);
};
var isTotallyVisibleInNewLocation = ({ draggable, destination, newPageBorderBoxCenter, viewport, withDroppableDisplacement, onlyOnMainAxis = false }) => {
	const changeNeeded = subtract(newPageBorderBoxCenter, draggable.page.borderBox.center);
	const args = {
		target: offsetByPosition(draggable.page.borderBox, changeNeeded),
		destination,
		withDroppableDisplacement,
		viewport
	};
	return onlyOnMainAxis ? isTotallyVisibleOnAxis(args) : isTotallyVisible(args);
};
var moveToNextPlace = ({ isMovingForward, draggable, destination, draggables, previousImpact, viewport, previousPageBorderBoxCenter, previousClientSelection, afterCritical }) => {
	if (!destination.isEnabled) return null;
	const insideDestination = getDraggablesInsideDroppable(destination.descriptor.id, draggables);
	const isInHomeList = isHomeOf(draggable, destination);
	const impact = moveToNextCombine({
		isMovingForward,
		draggable,
		destination,
		insideDestination,
		previousImpact
	}) || moveToNextIndex({
		isMovingForward,
		isInHomeList,
		draggable,
		draggables,
		destination,
		insideDestination,
		previousImpact,
		viewport,
		afterCritical
	});
	if (!impact) return null;
	const pageBorderBoxCenter = getPageBorderBoxCenterFromImpact({
		impact,
		draggable,
		droppable: destination,
		draggables,
		afterCritical
	});
	if (isTotallyVisibleInNewLocation({
		draggable,
		destination,
		newPageBorderBoxCenter: pageBorderBoxCenter,
		viewport: viewport.frame,
		withDroppableDisplacement: false,
		onlyOnMainAxis: true
	})) return {
		clientSelection: getClientFromPageBorderBoxCenter({
			pageBorderBoxCenter,
			draggable,
			viewport
		}),
		impact,
		scrollJumpRequest: null
	};
	const distance = subtract(pageBorderBoxCenter, previousPageBorderBoxCenter);
	return {
		clientSelection: previousClientSelection,
		impact: speculativelyIncrease({
			impact,
			viewport,
			destination,
			draggables,
			maxScrollChange: distance
		}),
		scrollJumpRequest: distance
	};
};
var getKnownActive = (droppable) => {
	const rect = droppable.subject.active;
	!rect && invariant(false, "Cannot get clipped area from droppable");
	return rect;
};
var getBestCrossAxisDroppable = ({ isMovingForward, pageBorderBoxCenter, source, droppables, viewport }) => {
	const active = source.subject.active;
	if (!active) return null;
	const axis = source.axis;
	const isBetweenSourceClipped = isWithin(active[axis.start], active[axis.end]);
	const candidates = toDroppableList(droppables).filter((droppable) => droppable !== source).filter((droppable) => droppable.isEnabled).filter((droppable) => Boolean(droppable.subject.active)).filter((droppable) => isPartiallyVisibleThroughFrame(viewport.frame)(getKnownActive(droppable))).filter((droppable) => {
		const activeOfTarget = getKnownActive(droppable);
		if (isMovingForward) return active[axis.crossAxisEnd] < activeOfTarget[axis.crossAxisEnd];
		return activeOfTarget[axis.crossAxisStart] < active[axis.crossAxisStart];
	}).filter((droppable) => {
		const activeOfTarget = getKnownActive(droppable);
		const isBetweenDestinationClipped = isWithin(activeOfTarget[axis.start], activeOfTarget[axis.end]);
		return isBetweenSourceClipped(activeOfTarget[axis.start]) || isBetweenSourceClipped(activeOfTarget[axis.end]) || isBetweenDestinationClipped(active[axis.start]) || isBetweenDestinationClipped(active[axis.end]);
	}).sort((a, b) => {
		const first = getKnownActive(a)[axis.crossAxisStart];
		const second = getKnownActive(b)[axis.crossAxisStart];
		if (isMovingForward) return first - second;
		return second - first;
	}).filter((droppable, index, array) => getKnownActive(droppable)[axis.crossAxisStart] === getKnownActive(array[0])[axis.crossAxisStart]);
	if (!candidates.length) return null;
	if (candidates.length === 1) return candidates[0];
	const contains = candidates.filter((droppable) => {
		return isWithin(getKnownActive(droppable)[axis.start], getKnownActive(droppable)[axis.end])(pageBorderBoxCenter[axis.line]);
	});
	if (contains.length === 1) return contains[0];
	if (contains.length > 1) return contains.sort((a, b) => getKnownActive(a)[axis.start] - getKnownActive(b)[axis.start])[0];
	return candidates.sort((a, b) => {
		const first = closest$1(pageBorderBoxCenter, getCorners(getKnownActive(a)));
		const second = closest$1(pageBorderBoxCenter, getCorners(getKnownActive(b)));
		if (first !== second) return first - second;
		return getKnownActive(a)[axis.start] - getKnownActive(b)[axis.start];
	})[0];
};
var getCurrentPageBorderBoxCenter = (draggable, afterCritical) => {
	const original = draggable.page.borderBox.center;
	return didStartAfterCritical(draggable.descriptor.id, afterCritical) ? subtract(original, afterCritical.displacedBy.point) : original;
};
var getCurrentPageBorderBox = (draggable, afterCritical) => {
	const original = draggable.page.borderBox;
	return didStartAfterCritical(draggable.descriptor.id, afterCritical) ? offsetByPosition(original, negate(afterCritical.displacedBy.point)) : original;
};
var getClosestDraggable = ({ pageBorderBoxCenter, viewport, destination, insideDestination, afterCritical }) => {
	return insideDestination.filter((draggable) => isTotallyVisible({
		target: getCurrentPageBorderBox(draggable, afterCritical),
		destination,
		viewport: viewport.frame,
		withDroppableDisplacement: true
	})).sort((a, b) => {
		const distanceToA = distance(pageBorderBoxCenter, withDroppableDisplacement(destination, getCurrentPageBorderBoxCenter(a, afterCritical)));
		const distanceToB = distance(pageBorderBoxCenter, withDroppableDisplacement(destination, getCurrentPageBorderBoxCenter(b, afterCritical)));
		if (distanceToA < distanceToB) return -1;
		if (distanceToB < distanceToA) return 1;
		return a.descriptor.index - b.descriptor.index;
	})[0] || null;
};
var getDisplacedBy = memoizeOne$1(function getDisplacedBy(axis, displaceBy) {
	const displacement = displaceBy[axis.line];
	return {
		value: displacement,
		point: patch(axis.line, displacement)
	};
});
var getRequiredGrowthForPlaceholder = (droppable, placeholderSize, draggables) => {
	const axis = droppable.axis;
	if (droppable.descriptor.mode === "virtual") return patch(axis.line, placeholderSize[axis.line]);
	const availableSpace = droppable.subject.page.contentBox[axis.size];
	const needsToGrowBy = getDraggablesInsideDroppable(droppable.descriptor.id, draggables).reduce((sum, dimension) => sum + dimension.client.marginBox[axis.size], 0) + placeholderSize[axis.line] - availableSpace;
	if (needsToGrowBy <= 0) return null;
	return patch(axis.line, needsToGrowBy);
};
var withMaxScroll = (frame, max) => ({
	...frame,
	scroll: {
		...frame.scroll,
		max
	}
});
var addPlaceholder = (droppable, draggable, draggables) => {
	const frame = droppable.frame;
	isHomeOf(draggable, droppable) && invariant(false, "Should not add placeholder space to home list");
	droppable.subject.withPlaceholder && invariant(false, "Cannot add placeholder size to a subject when it already has one");
	const placeholderSize = getDisplacedBy(droppable.axis, draggable.displaceBy).point;
	const requiredGrowth = getRequiredGrowthForPlaceholder(droppable, placeholderSize, draggables);
	const added = {
		placeholderSize,
		increasedBy: requiredGrowth,
		oldFrameMaxScroll: droppable.frame ? droppable.frame.scroll.max : null
	};
	if (!frame) {
		const subject = getSubject({
			page: droppable.subject.page,
			withPlaceholder: added,
			axis: droppable.axis,
			frame: droppable.frame
		});
		return {
			...droppable,
			subject
		};
	}
	const newFrame = withMaxScroll(frame, requiredGrowth ? add(frame.scroll.max, requiredGrowth) : frame.scroll.max);
	const subject = getSubject({
		page: droppable.subject.page,
		withPlaceholder: added,
		axis: droppable.axis,
		frame: newFrame
	});
	return {
		...droppable,
		subject,
		frame: newFrame
	};
};
var removePlaceholder = (droppable) => {
	const added = droppable.subject.withPlaceholder;
	!added && invariant(false, "Cannot remove placeholder form subject when there was none");
	const frame = droppable.frame;
	if (!frame) {
		const subject = getSubject({
			page: droppable.subject.page,
			axis: droppable.axis,
			frame: null,
			withPlaceholder: null
		});
		return {
			...droppable,
			subject
		};
	}
	const oldMaxScroll = added.oldFrameMaxScroll;
	!oldMaxScroll && invariant(false, "Expected droppable with frame to have old max frame scroll when removing placeholder");
	const newFrame = withMaxScroll(frame, oldMaxScroll);
	const subject = getSubject({
		page: droppable.subject.page,
		axis: droppable.axis,
		frame: newFrame,
		withPlaceholder: null
	});
	return {
		...droppable,
		subject,
		frame: newFrame
	};
};
var moveToNewDroppable = ({ previousPageBorderBoxCenter, moveRelativeTo, insideDestination, draggable, draggables, destination, viewport, afterCritical }) => {
	if (!moveRelativeTo) {
		if (insideDestination.length) return null;
		const proposed = {
			displaced: emptyGroups,
			displacedBy: noDisplacedBy,
			at: {
				type: "REORDER",
				destination: {
					droppableId: destination.descriptor.id,
					index: 0
				}
			}
		};
		const proposedPageBorderBoxCenter = getPageBorderBoxCenterFromImpact({
			impact: proposed,
			draggable,
			droppable: destination,
			draggables,
			afterCritical
		});
		return isTotallyVisibleInNewLocation({
			draggable,
			destination: isHomeOf(draggable, destination) ? destination : addPlaceholder(destination, draggable, draggables),
			newPageBorderBoxCenter: proposedPageBorderBoxCenter,
			viewport: viewport.frame,
			withDroppableDisplacement: false,
			onlyOnMainAxis: true
		}) ? proposed : null;
	}
	const isGoingBeforeTarget = Boolean(previousPageBorderBoxCenter[destination.axis.line] <= moveRelativeTo.page.borderBox.center[destination.axis.line]);
	const proposedIndex = (() => {
		const relativeTo = moveRelativeTo.descriptor.index;
		if (moveRelativeTo.descriptor.id === draggable.descriptor.id) return relativeTo;
		if (isGoingBeforeTarget) return relativeTo;
		return relativeTo + 1;
	})();
	return calculateReorderImpact({
		draggable,
		insideDestination,
		destination,
		viewport,
		displacedBy: getDisplacedBy(destination.axis, draggable.displaceBy),
		last: emptyGroups,
		index: proposedIndex
	});
};
var moveCrossAxis = ({ isMovingForward, previousPageBorderBoxCenter, draggable, isOver, draggables, droppables, viewport, afterCritical }) => {
	const destination = getBestCrossAxisDroppable({
		isMovingForward,
		pageBorderBoxCenter: previousPageBorderBoxCenter,
		source: isOver,
		droppables,
		viewport
	});
	if (!destination) return null;
	const insideDestination = getDraggablesInsideDroppable(destination.descriptor.id, draggables);
	const impact = moveToNewDroppable({
		previousPageBorderBoxCenter,
		destination,
		draggable,
		draggables,
		moveRelativeTo: getClosestDraggable({
			pageBorderBoxCenter: previousPageBorderBoxCenter,
			viewport,
			destination,
			insideDestination,
			afterCritical
		}),
		insideDestination,
		viewport,
		afterCritical
	});
	if (!impact) return null;
	return {
		clientSelection: getClientFromPageBorderBoxCenter({
			pageBorderBoxCenter: getPageBorderBoxCenterFromImpact({
				impact,
				draggable,
				droppable: destination,
				draggables,
				afterCritical
			}),
			draggable,
			viewport
		}),
		impact,
		scrollJumpRequest: null
	};
};
var whatIsDraggedOver = (impact) => {
	const at = impact.at;
	if (!at) return null;
	if (at.type === "REORDER") return at.destination.droppableId;
	return at.combine.droppableId;
};
var getDroppableOver$1 = (impact, droppables) => {
	const id = whatIsDraggedOver(impact);
	return id ? droppables[id] : null;
};
var moveInDirection = ({ state, type }) => {
	const isActuallyOver = getDroppableOver$1(state.impact, state.dimensions.droppables);
	const isMainAxisMovementAllowed = Boolean(isActuallyOver);
	const home = state.dimensions.droppables[state.critical.droppable.id];
	const isOver = isActuallyOver || home;
	const direction = isOver.axis.direction;
	const isMovingOnMainAxis = direction === "vertical" && (type === "MOVE_UP" || type === "MOVE_DOWN") || direction === "horizontal" && (type === "MOVE_LEFT" || type === "MOVE_RIGHT");
	if (isMovingOnMainAxis && !isMainAxisMovementAllowed) return null;
	const isMovingForward = type === "MOVE_DOWN" || type === "MOVE_RIGHT";
	const draggable = state.dimensions.draggables[state.critical.draggable.id];
	const previousPageBorderBoxCenter = state.current.page.borderBoxCenter;
	const { draggables, droppables } = state.dimensions;
	return isMovingOnMainAxis ? moveToNextPlace({
		isMovingForward,
		previousPageBorderBoxCenter,
		draggable,
		destination: isOver,
		draggables,
		viewport: state.viewport,
		previousClientSelection: state.current.client.selection,
		previousImpact: state.impact,
		afterCritical: state.afterCritical
	}) : moveCrossAxis({
		isMovingForward,
		previousPageBorderBoxCenter,
		draggable,
		isOver,
		draggables,
		droppables,
		viewport: state.viewport,
		afterCritical: state.afterCritical
	});
};
function isMovementAllowed(state) {
	return state.phase === "DRAGGING" || state.phase === "COLLECTING";
}
function isPositionInFrame(frame) {
	const isWithinVertical = isWithin(frame.top, frame.bottom);
	const isWithinHorizontal = isWithin(frame.left, frame.right);
	return function run(point) {
		return isWithinVertical(point.y) && isWithinHorizontal(point.x);
	};
}
function getHasOverlap(first, second) {
	return first.left < second.right && first.right > second.left && first.top < second.bottom && first.bottom > second.top;
}
function getFurthestAway({ pageBorderBox, draggable, candidates }) {
	const startCenter = draggable.page.borderBox.center;
	const sorted = candidates.map((candidate) => {
		const axis = candidate.axis;
		const target = patch(candidate.axis.line, pageBorderBox.center[axis.line], candidate.page.borderBox.center[axis.crossAxisLine]);
		return {
			id: candidate.descriptor.id,
			distance: distance(startCenter, target)
		};
	}).sort((a, b) => b.distance - a.distance);
	return sorted[0] ? sorted[0].id : null;
}
function getDroppableOver({ pageBorderBox, draggable, droppables }) {
	const candidates = toDroppableList(droppables).filter((item) => {
		if (!item.isEnabled) return false;
		const active = item.subject.active;
		if (!active) return false;
		if (!getHasOverlap(pageBorderBox, active)) return false;
		if (isPositionInFrame(active)(pageBorderBox.center)) return true;
		const axis = item.axis;
		const childCenter = active.center[axis.crossAxisLine];
		const crossAxisStart = pageBorderBox[axis.crossAxisStart];
		const crossAxisEnd = pageBorderBox[axis.crossAxisEnd];
		const isContained = isWithin(active[axis.crossAxisStart], active[axis.crossAxisEnd]);
		const isStartContained = isContained(crossAxisStart);
		const isEndContained = isContained(crossAxisEnd);
		if (!isStartContained && !isEndContained) return true;
		if (isStartContained) return crossAxisStart < childCenter;
		return crossAxisEnd > childCenter;
	});
	if (!candidates.length) return null;
	if (candidates.length === 1) return candidates[0].descriptor.id;
	return getFurthestAway({
		pageBorderBox,
		draggable,
		candidates
	});
}
var offsetRectByPosition = (rect, point) => getRect(offsetByPosition(rect, point));
var withDroppableScroll = (droppable, area) => {
	const frame = droppable.frame;
	if (!frame) return area;
	return offsetRectByPosition(area, frame.scroll.diff.value);
};
function getIsDisplaced({ displaced, id }) {
	return Boolean(displaced.visible[id] || displaced.invisible[id]);
}
function atIndex({ draggable, closest, inHomeList }) {
	if (!closest) return null;
	if (!inHomeList) return closest.descriptor.index;
	if (closest.descriptor.index > draggable.descriptor.index) return closest.descriptor.index - 1;
	return closest.descriptor.index;
}
var getReorderImpact = ({ pageBorderBoxWithDroppableScroll: targetRect, draggable, destination, insideDestination, last, viewport, afterCritical }) => {
	const axis = destination.axis;
	const displacedBy = getDisplacedBy(destination.axis, draggable.displaceBy);
	const displacement = displacedBy.value;
	const targetStart = targetRect[axis.start];
	const targetEnd = targetRect[axis.end];
	return calculateReorderImpact({
		draggable,
		insideDestination,
		destination,
		viewport,
		last,
		displacedBy,
		index: atIndex({
			draggable,
			closest: removeDraggableFromList(draggable, insideDestination).find((child) => {
				const id = child.descriptor.id;
				const childCenter = child.page.borderBox.center[axis.line];
				const didStartAfterCritical$1 = didStartAfterCritical(id, afterCritical);
				const isDisplaced = getIsDisplaced({
					displaced: last,
					id
				});
				if (didStartAfterCritical$1) {
					if (isDisplaced) return targetEnd <= childCenter;
					return targetStart < childCenter - displacement;
				}
				if (isDisplaced) return targetEnd <= childCenter + displacement;
				return targetStart < childCenter;
			}) || null,
			inHomeList: isHomeOf(draggable, destination)
		})
	});
};
var combineThresholdDivisor = 4;
var getCombineImpact = ({ draggable, pageBorderBoxWithDroppableScroll: targetRect, previousImpact, destination, insideDestination, afterCritical }) => {
	if (!destination.isCombineEnabled) return null;
	const axis = destination.axis;
	const displacedBy = getDisplacedBy(destination.axis, draggable.displaceBy);
	const displacement = displacedBy.value;
	const targetStart = targetRect[axis.start];
	const targetEnd = targetRect[axis.end];
	const combineWith = removeDraggableFromList(draggable, insideDestination).find((child) => {
		const id = child.descriptor.id;
		const childRect = child.page.borderBox;
		const threshold = childRect[axis.size] / combineThresholdDivisor;
		const didStartAfterCritical$1 = didStartAfterCritical(id, afterCritical);
		const isDisplaced = getIsDisplaced({
			displaced: previousImpact.displaced,
			id
		});
		if (didStartAfterCritical$1) {
			if (isDisplaced) return targetEnd > childRect[axis.start] + threshold && targetEnd < childRect[axis.end] - threshold;
			return targetStart > childRect[axis.start] - displacement + threshold && targetStart < childRect[axis.end] - displacement - threshold;
		}
		if (isDisplaced) return targetEnd > childRect[axis.start] + displacement + threshold && targetEnd < childRect[axis.end] + displacement - threshold;
		return targetStart > childRect[axis.start] + threshold && targetStart < childRect[axis.end] - threshold;
	});
	if (!combineWith) return null;
	return {
		displacedBy,
		displaced: previousImpact.displaced,
		at: {
			type: "COMBINE",
			combine: {
				draggableId: combineWith.descriptor.id,
				droppableId: destination.descriptor.id
			}
		}
	};
};
var getDragImpact = ({ pageOffset, draggable, draggables, droppables, previousImpact, viewport, afterCritical }) => {
	const pageBorderBox = offsetRectByPosition(draggable.page.borderBox, pageOffset);
	const destinationId = getDroppableOver({
		pageBorderBox,
		draggable,
		droppables
	});
	if (!destinationId) return noImpact;
	const destination = droppables[destinationId];
	const insideDestination = getDraggablesInsideDroppable(destination.descriptor.id, draggables);
	const pageBorderBoxWithDroppableScroll = withDroppableScroll(destination, pageBorderBox);
	return getCombineImpact({
		pageBorderBoxWithDroppableScroll,
		draggable,
		previousImpact,
		destination,
		insideDestination,
		afterCritical
	}) || getReorderImpact({
		pageBorderBoxWithDroppableScroll,
		draggable,
		destination,
		insideDestination,
		last: previousImpact.displaced,
		viewport,
		afterCritical
	});
};
var patchDroppableMap = (droppables, updated) => ({
	...droppables,
	[updated.descriptor.id]: updated
});
var clearUnusedPlaceholder = ({ previousImpact, impact, droppables }) => {
	const last = whatIsDraggedOver(previousImpact);
	const now = whatIsDraggedOver(impact);
	if (!last) return droppables;
	if (last === now) return droppables;
	const lastDroppable = droppables[last];
	if (!lastDroppable.subject.withPlaceholder) return droppables;
	return patchDroppableMap(droppables, removePlaceholder(lastDroppable));
};
var recomputePlaceholders = ({ draggable, draggables, droppables, previousImpact, impact }) => {
	const cleaned = clearUnusedPlaceholder({
		previousImpact,
		impact,
		droppables
	});
	const isOver = whatIsDraggedOver(impact);
	if (!isOver) return cleaned;
	const droppable = droppables[isOver];
	if (isHomeOf(draggable, droppable)) return cleaned;
	if (droppable.subject.withPlaceholder) return cleaned;
	return patchDroppableMap(cleaned, addPlaceholder(droppable, draggable, draggables));
};
var update = ({ state, clientSelection: forcedClientSelection, dimensions: forcedDimensions, viewport: forcedViewport, impact: forcedImpact, scrollJumpRequest }) => {
	const viewport = forcedViewport || state.viewport;
	const dimensions = forcedDimensions || state.dimensions;
	const clientSelection = forcedClientSelection || state.current.client.selection;
	const offset = subtract(clientSelection, state.initial.client.selection);
	const client = {
		offset,
		selection: clientSelection,
		borderBoxCenter: add(state.initial.client.borderBoxCenter, offset)
	};
	const page = {
		selection: add(client.selection, viewport.scroll.current),
		borderBoxCenter: add(client.borderBoxCenter, viewport.scroll.current),
		offset: add(client.offset, viewport.scroll.diff.value)
	};
	const current = {
		client,
		page
	};
	if (state.phase === "COLLECTING") return {
		...state,
		dimensions,
		viewport,
		current
	};
	const draggable = dimensions.draggables[state.critical.draggable.id];
	const newImpact = forcedImpact || getDragImpact({
		pageOffset: page.offset,
		draggable,
		draggables: dimensions.draggables,
		droppables: dimensions.droppables,
		previousImpact: state.impact,
		viewport,
		afterCritical: state.afterCritical
	});
	const withUpdatedPlaceholders = recomputePlaceholders({
		draggable,
		impact: newImpact,
		previousImpact: state.impact,
		draggables: dimensions.draggables,
		droppables: dimensions.droppables
	});
	return {
		...state,
		current,
		dimensions: {
			draggables: dimensions.draggables,
			droppables: withUpdatedPlaceholders
		},
		impact: newImpact,
		viewport,
		scrollJumpRequest: scrollJumpRequest || null,
		forceShouldAnimate: scrollJumpRequest ? false : null
	};
};
function getDraggables(ids, draggables) {
	return ids.map((id) => draggables[id]);
}
var recompute = ({ impact, viewport, draggables, destination, forceShouldAnimate }) => {
	const last = impact.displaced;
	const displaced = getDisplacementGroups({
		afterDragging: getDraggables(last.all, draggables),
		destination,
		displacedBy: impact.displacedBy,
		viewport: viewport.frame,
		forceShouldAnimate,
		last
	});
	return {
		...impact,
		displaced
	};
};
var getClientBorderBoxCenter = ({ impact, draggable, droppable, draggables, viewport, afterCritical }) => {
	return getClientFromPageBorderBoxCenter({
		pageBorderBoxCenter: getPageBorderBoxCenterFromImpact({
			impact,
			draggable,
			draggables,
			droppable,
			afterCritical
		}),
		draggable,
		viewport
	});
};
var refreshSnap = ({ state, dimensions: forcedDimensions, viewport: forcedViewport }) => {
	!(state.movementMode === "SNAP") && invariant();
	const needsVisibilityCheck = state.impact;
	const viewport = forcedViewport || state.viewport;
	const dimensions = forcedDimensions || state.dimensions;
	const { draggables, droppables } = dimensions;
	const draggable = draggables[state.critical.draggable.id];
	const isOver = whatIsDraggedOver(needsVisibilityCheck);
	!isOver && invariant(false, "Must be over a destination in SNAP movement mode");
	const destination = droppables[isOver];
	const impact = recompute({
		impact: needsVisibilityCheck,
		viewport,
		destination,
		draggables
	});
	return update({
		impact,
		clientSelection: getClientBorderBoxCenter({
			impact,
			draggable,
			droppable: destination,
			draggables,
			viewport,
			afterCritical: state.afterCritical
		}),
		state,
		dimensions,
		viewport
	});
};
var getHomeLocation = (descriptor) => ({
	index: descriptor.index,
	droppableId: descriptor.droppableId
});
var getLiftEffect = ({ draggable, home, draggables, viewport }) => {
	const displacedBy = getDisplacedBy(home.axis, draggable.displaceBy);
	const insideHome = getDraggablesInsideDroppable(home.descriptor.id, draggables);
	const rawIndex = insideHome.indexOf(draggable);
	!(rawIndex !== -1) && invariant(false, "Expected draggable to be inside home list");
	const afterDragging = insideHome.slice(rawIndex + 1);
	const effected = afterDragging.reduce((previous, item) => {
		previous[item.descriptor.id] = true;
		return previous;
	}, {});
	const afterCritical = {
		inVirtualList: home.descriptor.mode === "virtual",
		displacedBy,
		effected
	};
	return {
		impact: {
			displaced: getDisplacementGroups({
				afterDragging,
				destination: home,
				displacedBy,
				last: null,
				viewport: viewport.frame,
				forceShouldAnimate: false
			}),
			displacedBy,
			at: {
				type: "REORDER",
				destination: getHomeLocation(draggable.descriptor)
			}
		},
		afterCritical
	};
};
var patchDimensionMap = (dimensions, updated) => ({
	draggables: dimensions.draggables,
	droppables: patchDroppableMap(dimensions.droppables, updated)
});
var offsetDraggable = ({ draggable, offset: offset$1, initialWindowScroll }) => {
	const client = offset(draggable.client, offset$1);
	const page = withScroll(client, initialWindowScroll);
	return {
		...draggable,
		placeholder: {
			...draggable.placeholder,
			client
		},
		client,
		page
	};
};
var getFrame = (droppable) => {
	const frame = droppable.frame;
	!frame && invariant(false, "Expected Droppable to have a frame");
	return frame;
};
var adjustAdditionsForScrollChanges = ({ additions, updatedDroppables, viewport }) => {
	const windowScrollChange = viewport.scroll.diff.value;
	return additions.map((draggable) => {
		const modified = updatedDroppables[draggable.descriptor.droppableId];
		const droppableScrollChange = getFrame(modified).scroll.diff.value;
		return offsetDraggable({
			draggable,
			offset: add(windowScrollChange, droppableScrollChange),
			initialWindowScroll: viewport.scroll.initial
		});
	});
};
var publishWhileDraggingInVirtual = ({ state, published }) => {
	const withScrollChange = published.modified.map((update) => {
		const existing = state.dimensions.droppables[update.droppableId];
		return scrollDroppable(existing, update.scroll);
	});
	const droppables = {
		...state.dimensions.droppables,
		...toDroppableMap(withScrollChange)
	};
	const updatedAdditions = toDraggableMap(adjustAdditionsForScrollChanges({
		additions: published.additions,
		updatedDroppables: droppables,
		viewport: state.viewport
	}));
	const draggables = {
		...state.dimensions.draggables,
		...updatedAdditions
	};
	published.removals.forEach((id) => {
		delete draggables[id];
	});
	const dimensions = {
		droppables,
		draggables
	};
	const wasOverId = whatIsDraggedOver(state.impact);
	const wasOver = wasOverId ? dimensions.droppables[wasOverId] : null;
	const draggable = dimensions.draggables[state.critical.draggable.id];
	const home = dimensions.droppables[state.critical.droppable.id];
	const { impact: onLiftImpact, afterCritical } = getLiftEffect({
		draggable,
		home,
		draggables,
		viewport: state.viewport
	});
	const previousImpact = wasOver && wasOver.isCombineEnabled ? state.impact : onLiftImpact;
	const impact = getDragImpact({
		pageOffset: state.current.page.offset,
		draggable: dimensions.draggables[state.critical.draggable.id],
		draggables: dimensions.draggables,
		droppables: dimensions.droppables,
		previousImpact,
		viewport: state.viewport,
		afterCritical
	});
	const draggingState = {
		...state,
		phase: "DRAGGING",
		impact,
		onLiftImpact,
		dimensions,
		afterCritical,
		forceShouldAnimate: false
	};
	if (state.phase === "COLLECTING") return draggingState;
	return {
		...draggingState,
		phase: "DROP_PENDING",
		reason: state.reason,
		isWaiting: false
	};
};
var isSnapping = (state) => state.movementMode === "SNAP";
var postDroppableChange = (state, updated, isEnabledChanging) => {
	const dimensions = patchDimensionMap(state.dimensions, updated);
	if (!isSnapping(state) || isEnabledChanging) return update({
		state,
		dimensions
	});
	return refreshSnap({
		state,
		dimensions
	});
};
function removeScrollJumpRequest(state) {
	if (state.isDragging && state.movementMode === "SNAP") return {
		...state,
		scrollJumpRequest: null
	};
	return state;
}
var idle$2 = {
	phase: "IDLE",
	completed: null,
	shouldFlush: false
};
var reducer = (state = idle$2, action) => {
	if (action.type === "FLUSH") return {
		...idle$2,
		shouldFlush: true
	};
	if (action.type === "INITIAL_PUBLISH") {
		!(state.phase === "IDLE") && invariant(false, "INITIAL_PUBLISH must come after a IDLE phase");
		const { critical, clientSelection, viewport, dimensions, movementMode } = action.payload;
		const draggable = dimensions.draggables[critical.draggable.id];
		const home = dimensions.droppables[critical.droppable.id];
		const client = {
			selection: clientSelection,
			borderBoxCenter: draggable.client.borderBox.center,
			offset: origin
		};
		const initial = {
			client,
			page: {
				selection: add(client.selection, viewport.scroll.initial),
				borderBoxCenter: add(client.selection, viewport.scroll.initial),
				offset: add(client.selection, viewport.scroll.diff.value)
			}
		};
		const isWindowScrollAllowed = toDroppableList(dimensions.droppables).every((item) => !item.isFixedOnPage);
		const { impact, afterCritical } = getLiftEffect({
			draggable,
			home,
			draggables: dimensions.draggables,
			viewport
		});
		return {
			phase: "DRAGGING",
			isDragging: true,
			critical,
			movementMode,
			dimensions,
			initial,
			current: initial,
			isWindowScrollAllowed,
			impact,
			afterCritical,
			onLiftImpact: impact,
			viewport,
			scrollJumpRequest: null,
			forceShouldAnimate: null
		};
	}
	if (action.type === "COLLECTION_STARTING") {
		if (state.phase === "COLLECTING" || state.phase === "DROP_PENDING") return state;
		!(state.phase === "DRAGGING") && invariant(false, `Collection cannot start from phase ${state.phase}`);
		return {
			...state,
			phase: "COLLECTING"
		};
	}
	if (action.type === "PUBLISH_WHILE_DRAGGING") {
		!(state.phase === "COLLECTING" || state.phase === "DROP_PENDING") && invariant(false, `Unexpected ${action.type} received in phase ${state.phase}`);
		return publishWhileDraggingInVirtual({
			state,
			published: action.payload
		});
	}
	if (action.type === "MOVE") {
		if (state.phase === "DROP_PENDING") return state;
		!isMovementAllowed(state) && invariant(false, `${action.type} not permitted in phase ${state.phase}`);
		const { client: clientSelection } = action.payload;
		if (isEqual$1$1(clientSelection, state.current.client.selection)) return state;
		return update({
			state,
			clientSelection,
			impact: isSnapping(state) ? state.impact : null
		});
	}
	if (action.type === "UPDATE_DROPPABLE_SCROLL") {
		if (state.phase === "DROP_PENDING") return removeScrollJumpRequest(state);
		if (state.phase === "COLLECTING") return removeScrollJumpRequest(state);
		!isMovementAllowed(state) && invariant(false, `${action.type} not permitted in phase ${state.phase}`);
		const { id, newScroll } = action.payload;
		const target = state.dimensions.droppables[id];
		if (!target) return state;
		return postDroppableChange(state, scrollDroppable(target, newScroll), false);
	}
	if (action.type === "UPDATE_DROPPABLE_IS_ENABLED") {
		if (state.phase === "DROP_PENDING") return state;
		!isMovementAllowed(state) && invariant(false, `Attempting to move in an unsupported phase ${state.phase}`);
		const { id, isEnabled } = action.payload;
		const target = state.dimensions.droppables[id];
		!target && invariant(false, `Cannot find Droppable[id: ${id}] to toggle its enabled state`);
		!(target.isEnabled !== isEnabled) && invariant(false, `Trying to set droppable isEnabled to ${String(isEnabled)}
      but it is already ${String(target.isEnabled)}`);
		return postDroppableChange(state, {
			...target,
			isEnabled
		}, true);
	}
	if (action.type === "UPDATE_DROPPABLE_IS_COMBINE_ENABLED") {
		if (state.phase === "DROP_PENDING") return state;
		!isMovementAllowed(state) && invariant(false, `Attempting to move in an unsupported phase ${state.phase}`);
		const { id, isCombineEnabled } = action.payload;
		const target = state.dimensions.droppables[id];
		!target && invariant(false, `Cannot find Droppable[id: ${id}] to toggle its isCombineEnabled state`);
		!(target.isCombineEnabled !== isCombineEnabled) && invariant(false, `Trying to set droppable isCombineEnabled to ${String(isCombineEnabled)}
      but it is already ${String(target.isCombineEnabled)}`);
		return postDroppableChange(state, {
			...target,
			isCombineEnabled
		}, true);
	}
	if (action.type === "MOVE_BY_WINDOW_SCROLL") {
		if (state.phase === "DROP_PENDING" || state.phase === "DROP_ANIMATING") return state;
		!isMovementAllowed(state) && invariant(false, `Cannot move by window in phase ${state.phase}`);
		!state.isWindowScrollAllowed && invariant(false, "Window scrolling is currently not supported for fixed lists");
		const newScroll = action.payload.newScroll;
		if (isEqual$1$1(state.viewport.scroll.current, newScroll)) return removeScrollJumpRequest(state);
		const viewport = scrollViewport(state.viewport, newScroll);
		if (isSnapping(state)) return refreshSnap({
			state,
			viewport
		});
		return update({
			state,
			viewport
		});
	}
	if (action.type === "UPDATE_VIEWPORT_MAX_SCROLL") {
		if (!isMovementAllowed(state)) return state;
		const maxScroll = action.payload.maxScroll;
		if (isEqual$1$1(maxScroll, state.viewport.scroll.max)) return state;
		const withMaxScroll = {
			...state.viewport,
			scroll: {
				...state.viewport.scroll,
				max: maxScroll
			}
		};
		return {
			...state,
			viewport: withMaxScroll
		};
	}
	if (action.type === "MOVE_UP" || action.type === "MOVE_DOWN" || action.type === "MOVE_LEFT" || action.type === "MOVE_RIGHT") {
		if (state.phase === "COLLECTING" || state.phase === "DROP_PENDING") return state;
		!(state.phase === "DRAGGING") && invariant(false, `${action.type} received while not in DRAGGING phase`);
		const result = moveInDirection({
			state,
			type: action.type
		});
		if (!result) return state;
		return update({
			state,
			impact: result.impact,
			clientSelection: result.clientSelection,
			scrollJumpRequest: result.scrollJumpRequest
		});
	}
	if (action.type === "DROP_PENDING") {
		const reason = action.payload.reason;
		!(state.phase === "COLLECTING") && invariant(false, "Can only move into the DROP_PENDING phase from the COLLECTING phase");
		return {
			...state,
			phase: "DROP_PENDING",
			isWaiting: true,
			reason
		};
	}
	if (action.type === "DROP_ANIMATE") {
		const { completed, dropDuration, newHomeClientOffset } = action.payload;
		!(state.phase === "DRAGGING" || state.phase === "DROP_PENDING") && invariant(false, `Cannot animate drop from phase ${state.phase}`);
		return {
			phase: "DROP_ANIMATING",
			completed,
			dropDuration,
			newHomeClientOffset,
			dimensions: state.dimensions
		};
	}
	if (action.type === "DROP_COMPLETE") {
		const { completed } = action.payload;
		return {
			phase: "IDLE",
			completed,
			shouldFlush: false
		};
	}
	return state;
};
function guard(action, predicate) {
	return action instanceof Object && "type" in action && action.type === predicate;
}
var beforeInitialCapture = (args) => ({
	type: "BEFORE_INITIAL_CAPTURE",
	payload: args
});
var lift$1 = (args) => ({
	type: "LIFT",
	payload: args
});
var initialPublish = (args) => ({
	type: "INITIAL_PUBLISH",
	payload: args
});
var publishWhileDragging = (args) => ({
	type: "PUBLISH_WHILE_DRAGGING",
	payload: args
});
var collectionStarting = () => ({
	type: "COLLECTION_STARTING",
	payload: null
});
var updateDroppableScroll = (args) => ({
	type: "UPDATE_DROPPABLE_SCROLL",
	payload: args
});
var updateDroppableIsEnabled = (args) => ({
	type: "UPDATE_DROPPABLE_IS_ENABLED",
	payload: args
});
var updateDroppableIsCombineEnabled = (args) => ({
	type: "UPDATE_DROPPABLE_IS_COMBINE_ENABLED",
	payload: args
});
var move = (args) => ({
	type: "MOVE",
	payload: args
});
var moveByWindowScroll = (args) => ({
	type: "MOVE_BY_WINDOW_SCROLL",
	payload: args
});
var updateViewportMaxScroll = (args) => ({
	type: "UPDATE_VIEWPORT_MAX_SCROLL",
	payload: args
});
var moveUp = () => ({
	type: "MOVE_UP",
	payload: null
});
var moveDown = () => ({
	type: "MOVE_DOWN",
	payload: null
});
var moveRight = () => ({
	type: "MOVE_RIGHT",
	payload: null
});
var moveLeft = () => ({
	type: "MOVE_LEFT",
	payload: null
});
var flush = () => ({
	type: "FLUSH",
	payload: null
});
var animateDrop = (args) => ({
	type: "DROP_ANIMATE",
	payload: args
});
var completeDrop = (args) => ({
	type: "DROP_COMPLETE",
	payload: args
});
var drop = (args) => ({
	type: "DROP",
	payload: args
});
var dropPending = (args) => ({
	type: "DROP_PENDING",
	payload: args
});
var dropAnimationFinished = () => ({
	type: "DROP_ANIMATION_FINISHED",
	payload: null
});
function checkIndexes(insideDestination) {
	if (insideDestination.length <= 1) return;
	const indexes = insideDestination.map((d) => d.descriptor.index);
	const errors = {};
	for (let i = 1; i < indexes.length; i++) {
		const current = indexes[i];
		if (current !== indexes[i - 1] + 1) errors[current] = true;
	}
	if (!Object.keys(errors).length) return;
	warning$1(`
    Detected non-consecutive <Draggable /> indexes.

    (This can cause unexpected bugs)

    ${indexes.map((index) => {
		return Boolean(errors[index]) ? `[🔥${index}]` : `${index}`;
	}).join(", ")}
  `);
}
function validateDimensions(critical, dimensions) {
	checkIndexes(getDraggablesInsideDroppable(critical.droppable.id, dimensions.draggables));
}
var lift = (marshal) => ({ getState, dispatch }) => (next) => (action) => {
	if (!guard(action, "LIFT")) {
		next(action);
		return;
	}
	const { id, clientSelection, movementMode } = action.payload;
	const initial = getState();
	if (initial.phase === "DROP_ANIMATING") dispatch(completeDrop({ completed: initial.completed }));
	!(getState().phase === "IDLE") && invariant(false, "Unexpected phase to start a drag");
	dispatch(flush());
	dispatch(beforeInitialCapture({
		draggableId: id,
		movementMode
	}));
	const request = {
		draggableId: id,
		scrollOptions: { shouldPublishImmediately: movementMode === "SNAP" }
	};
	const { critical, dimensions, viewport } = marshal.startPublishing(request);
	validateDimensions(critical, dimensions);
	dispatch(initialPublish({
		critical,
		dimensions,
		clientSelection,
		movementMode,
		viewport
	}));
};
var style$1 = (marshal) => () => (next) => (action) => {
	if (guard(action, "INITIAL_PUBLISH")) marshal.dragging();
	if (guard(action, "DROP_ANIMATE")) marshal.dropping(action.payload.completed.result.reason);
	if (guard(action, "FLUSH") || guard(action, "DROP_COMPLETE")) marshal.resting();
	next(action);
};
var curves = {
	outOfTheWay: "cubic-bezier(0.2, 0, 0, 1)",
	drop: "cubic-bezier(.2,1,.1,1)"
};
var combine = {
	opacity: {
		drop: 0,
		combining: .7
	},
	scale: { drop: .75 }
};
var timings = {
	outOfTheWay: .2,
	minDropTime: .33,
	maxDropTime: .55
};
var outOfTheWayTiming = `${timings.outOfTheWay}s ${curves.outOfTheWay}`;
var transitions = {
	fluid: `opacity ${outOfTheWayTiming}`,
	snap: `transform ${outOfTheWayTiming}, opacity ${outOfTheWayTiming}`,
	drop: (duration) => {
		const timing = `${duration}s ${curves.drop}`;
		return `transform ${timing}, opacity ${timing}`;
	},
	outOfTheWay: `transform ${outOfTheWayTiming}`,
	placeholder: `height ${outOfTheWayTiming}, width ${outOfTheWayTiming}, margin ${outOfTheWayTiming}`
};
var moveTo = (offset) => isEqual$1$1(offset, origin) ? void 0 : `translate(${offset.x}px, ${offset.y}px)`;
var transforms = {
	moveTo,
	drop: (offset, isCombining) => {
		const translate = moveTo(offset);
		if (!translate) return;
		if (!isCombining) return translate;
		return `${translate} scale(${combine.scale.drop})`;
	}
};
var { minDropTime, maxDropTime } = timings;
var dropTimeRange = maxDropTime - minDropTime;
var maxDropTimeAtDistance = 1500;
var cancelDropModifier = .6;
var getDropDuration = ({ current, destination, reason }) => {
	const distance$1 = distance(current, destination);
	if (distance$1 <= 0) return minDropTime;
	if (distance$1 >= maxDropTimeAtDistance) return maxDropTime;
	const duration = minDropTime + dropTimeRange * (distance$1 / maxDropTimeAtDistance);
	const withDuration = reason === "CANCEL" ? duration * cancelDropModifier : duration;
	return Number(withDuration.toFixed(2));
};
var getNewHomeClientOffset = ({ impact, draggable, dimensions, viewport, afterCritical }) => {
	const { draggables, droppables } = dimensions;
	const droppableId = whatIsDraggedOver(impact);
	const destination = droppableId ? droppables[droppableId] : null;
	const home = droppables[draggable.descriptor.droppableId];
	return subtract(getClientBorderBoxCenter({
		impact,
		draggable,
		draggables,
		afterCritical,
		droppable: destination || home,
		viewport
	}), draggable.client.borderBox.center);
};
var getDropImpact = ({ draggables, reason, lastImpact, home, viewport, onLiftImpact }) => {
	if (!lastImpact.at || reason !== "DROP") return {
		impact: recompute({
			draggables,
			impact: onLiftImpact,
			destination: home,
			viewport,
			forceShouldAnimate: true
		}),
		didDropInsideDroppable: false
	};
	if (lastImpact.at.type === "REORDER") return {
		impact: lastImpact,
		didDropInsideDroppable: true
	};
	return {
		impact: {
			...lastImpact,
			displaced: emptyGroups
		},
		didDropInsideDroppable: true
	};
};
var dropMiddleware = ({ getState, dispatch }) => (next) => (action) => {
	if (!guard(action, "DROP")) {
		next(action);
		return;
	}
	const state = getState();
	const reason = action.payload.reason;
	if (state.phase === "COLLECTING") {
		dispatch(dropPending({ reason }));
		return;
	}
	if (state.phase === "IDLE") return;
	state.phase === "DROP_PENDING" && state.isWaiting && invariant(false, "A DROP action occurred while DROP_PENDING and still waiting");
	!(state.phase === "DRAGGING" || state.phase === "DROP_PENDING") && invariant(false, `Cannot drop in phase: ${state.phase}`);
	const critical = state.critical;
	const dimensions = state.dimensions;
	const draggable = dimensions.draggables[state.critical.draggable.id];
	const { impact, didDropInsideDroppable } = getDropImpact({
		reason,
		lastImpact: state.impact,
		afterCritical: state.afterCritical,
		onLiftImpact: state.onLiftImpact,
		home: state.dimensions.droppables[state.critical.droppable.id],
		viewport: state.viewport,
		draggables: state.dimensions.draggables
	});
	const destination = didDropInsideDroppable ? tryGetDestination(impact) : null;
	const combine = didDropInsideDroppable ? tryGetCombine(impact) : null;
	const source = {
		index: critical.draggable.index,
		droppableId: critical.droppable.id
	};
	const result = {
		draggableId: draggable.descriptor.id,
		type: draggable.descriptor.type,
		source,
		reason,
		mode: state.movementMode,
		destination,
		combine
	};
	const newHomeClientOffset = getNewHomeClientOffset({
		impact,
		draggable,
		dimensions,
		viewport: state.viewport,
		afterCritical: state.afterCritical
	});
	const completed = {
		critical: state.critical,
		afterCritical: state.afterCritical,
		result,
		impact
	};
	if (!(!isEqual$1$1(state.current.client.offset, newHomeClientOffset) || Boolean(result.combine))) {
		dispatch(completeDrop({ completed }));
		return;
	}
	dispatch(animateDrop({
		newHomeClientOffset,
		dropDuration: getDropDuration({
			current: state.current.client.offset,
			destination: newHomeClientOffset,
			reason
		}),
		completed
	}));
};
var getWindowScroll = () => ({
	x: window.pageXOffset,
	y: window.pageYOffset
});
function getWindowScrollBinding(update) {
	return {
		eventName: "scroll",
		options: {
			passive: true,
			capture: false
		},
		fn: (event) => {
			if (event.target !== window && event.target !== window.document) return;
			update();
		}
	};
}
function getScrollListener({ onWindowScroll }) {
	function updateScroll() {
		onWindowScroll(getWindowScroll());
	}
	const scheduled = rafSchd(updateScroll);
	const binding = getWindowScrollBinding(scheduled);
	let unbind = noop$2;
	function isActive() {
		return unbind !== noop$2;
	}
	function start() {
		isActive() && invariant(false, "Cannot start scroll listener when already active");
		unbind = bindEvents(window, [binding]);
	}
	function stop() {
		!isActive() && invariant(false, "Cannot stop scroll listener when not active");
		scheduled.cancel();
		unbind();
		unbind = noop$2;
	}
	return {
		start,
		stop,
		isActive
	};
}
var shouldStop$1 = (action) => guard(action, "DROP_COMPLETE") || guard(action, "DROP_ANIMATE") || guard(action, "FLUSH");
var scrollListener = (store) => {
	const listener = getScrollListener({ onWindowScroll: (newScroll) => {
		store.dispatch(moveByWindowScroll({ newScroll }));
	} });
	return (next) => (action) => {
		if (!listener.isActive() && guard(action, "INITIAL_PUBLISH")) listener.start();
		if (listener.isActive() && shouldStop$1(action)) listener.stop();
		next(action);
	};
};
var getExpiringAnnounce = (announce) => {
	let wasCalled = false;
	let isExpired = false;
	const timeoutId = setTimeout(() => {
		isExpired = true;
	});
	const result = (message) => {
		if (wasCalled) {
			warning$1("Announcement already made. Not making a second announcement");
			return;
		}
		if (isExpired) {
			warning$1(`
        Announcements cannot be made asynchronously.
        Default message has already been announced.
      `);
			return;
		}
		wasCalled = true;
		announce(message);
		clearTimeout(timeoutId);
	};
	result.wasCalled = () => wasCalled;
	return result;
};
var getAsyncMarshal = () => {
	const entries = [];
	const execute = (timerId) => {
		const index = entries.findIndex((item) => item.timerId === timerId);
		!(index !== -1) && invariant(false, "Could not find timer");
		const [entry] = entries.splice(index, 1);
		entry.callback();
	};
	const add = (fn) => {
		const timerId = setTimeout(() => execute(timerId));
		const entry = {
			timerId,
			callback: fn
		};
		entries.push(entry);
	};
	const flush = () => {
		if (!entries.length) return;
		const shallow = [...entries];
		entries.length = 0;
		shallow.forEach((entry) => {
			clearTimeout(entry.timerId);
			entry.callback();
		});
	};
	return {
		add,
		flush
	};
};
var areLocationsEqual = (first, second) => {
	if (first == null && second == null) return true;
	if (first == null || second == null) return false;
	return first.droppableId === second.droppableId && first.index === second.index;
};
var isCombineEqual = (first, second) => {
	if (first == null && second == null) return true;
	if (first == null || second == null) return false;
	return first.draggableId === second.draggableId && first.droppableId === second.droppableId;
};
var isCriticalEqual = (first, second) => {
	if (first === second) return true;
	const isDraggableEqual = first.draggable.id === second.draggable.id && first.draggable.droppableId === second.draggable.droppableId && first.draggable.type === second.draggable.type && first.draggable.index === second.draggable.index;
	const isDroppableEqual = first.droppable.id === second.droppable.id && first.droppable.type === second.droppable.type;
	return isDraggableEqual && isDroppableEqual;
};
var withTimings = (key, fn) => {
	fn();
};
var getDragStart = (critical, mode) => ({
	draggableId: critical.draggable.id,
	type: critical.droppable.type,
	source: {
		droppableId: critical.droppable.id,
		index: critical.draggable.index
	},
	mode
});
function execute(responder, data, announce, getDefaultMessage) {
	if (!responder) {
		announce(getDefaultMessage(data));
		return;
	}
	const willExpire = getExpiringAnnounce(announce);
	responder(data, { announce: willExpire });
	if (!willExpire.wasCalled()) announce(getDefaultMessage(data));
}
var getPublisher = (getResponders, announce) => {
	const asyncMarshal = getAsyncMarshal();
	let dragging = null;
	const beforeCapture = (draggableId, mode) => {
		dragging && invariant(false, "Cannot fire onBeforeCapture as a drag start has already been published");
		withTimings("onBeforeCapture", () => {
			const fn = getResponders().onBeforeCapture;
			if (fn) fn({
				draggableId,
				mode
			});
		});
	};
	const beforeStart = (critical, mode) => {
		dragging && invariant(false, "Cannot fire onBeforeDragStart as a drag start has already been published");
		withTimings("onBeforeDragStart", () => {
			const fn = getResponders().onBeforeDragStart;
			if (fn) fn(getDragStart(critical, mode));
		});
	};
	const start = (critical, mode) => {
		dragging && invariant(false, "Cannot fire onBeforeDragStart as a drag start has already been published");
		const data = getDragStart(critical, mode);
		dragging = {
			mode,
			lastCritical: critical,
			lastLocation: data.source,
			lastCombine: null
		};
		asyncMarshal.add(() => {
			withTimings("onDragStart", () => execute(getResponders().onDragStart, data, announce, preset.onDragStart));
		});
	};
	const update = (critical, impact) => {
		const location = tryGetDestination(impact);
		const combine = tryGetCombine(impact);
		!dragging && invariant(false, "Cannot fire onDragMove when onDragStart has not been called");
		const hasCriticalChanged = !isCriticalEqual(critical, dragging.lastCritical);
		if (hasCriticalChanged) dragging.lastCritical = critical;
		const hasLocationChanged = !areLocationsEqual(dragging.lastLocation, location);
		if (hasLocationChanged) dragging.lastLocation = location;
		const hasGroupingChanged = !isCombineEqual(dragging.lastCombine, combine);
		if (hasGroupingChanged) dragging.lastCombine = combine;
		if (!hasCriticalChanged && !hasLocationChanged && !hasGroupingChanged) return;
		const data = {
			...getDragStart(critical, dragging.mode),
			combine,
			destination: location
		};
		asyncMarshal.add(() => {
			withTimings("onDragUpdate", () => execute(getResponders().onDragUpdate, data, announce, preset.onDragUpdate));
		});
	};
	const flush = () => {
		!dragging && invariant(false, "Can only flush responders while dragging");
		asyncMarshal.flush();
	};
	const drop = (result) => {
		!dragging && invariant(false, "Cannot fire onDragEnd when there is no matching onDragStart");
		dragging = null;
		withTimings("onDragEnd", () => execute(getResponders().onDragEnd, result, announce, preset.onDragEnd));
	};
	const abort = () => {
		if (!dragging) return;
		const result = {
			...getDragStart(dragging.lastCritical, dragging.mode),
			combine: null,
			destination: null,
			reason: "CANCEL"
		};
		drop(result);
	};
	return {
		beforeCapture,
		beforeStart,
		start,
		update,
		flush,
		drop,
		abort
	};
};
var responders = (getResponders, announce) => {
	const publisher = getPublisher(getResponders, announce);
	return (store) => (next) => (action) => {
		if (guard(action, "BEFORE_INITIAL_CAPTURE")) {
			publisher.beforeCapture(action.payload.draggableId, action.payload.movementMode);
			return;
		}
		if (guard(action, "INITIAL_PUBLISH")) {
			const critical = action.payload.critical;
			publisher.beforeStart(critical, action.payload.movementMode);
			next(action);
			publisher.start(critical, action.payload.movementMode);
			return;
		}
		if (guard(action, "DROP_COMPLETE")) {
			const result = action.payload.completed.result;
			publisher.flush();
			next(action);
			publisher.drop(result);
			return;
		}
		next(action);
		if (guard(action, "FLUSH")) {
			publisher.abort();
			return;
		}
		const state = store.getState();
		if (state.phase === "DRAGGING") publisher.update(state.critical, state.impact);
	};
};
var dropAnimationFinishMiddleware = (store) => (next) => (action) => {
	if (!guard(action, "DROP_ANIMATION_FINISHED")) {
		next(action);
		return;
	}
	const state = store.getState();
	!(state.phase === "DROP_ANIMATING") && invariant(false, "Cannot finish a drop animating when no drop is occurring");
	store.dispatch(completeDrop({ completed: state.completed }));
};
var dropAnimationFlushOnScrollMiddleware = (store) => {
	let unbind = null;
	let frameId = null;
	function clear() {
		if (frameId) {
			cancelAnimationFrame(frameId);
			frameId = null;
		}
		if (unbind) {
			unbind();
			unbind = null;
		}
	}
	return (next) => (action) => {
		if (guard(action, "FLUSH") || guard(action, "DROP_COMPLETE") || guard(action, "DROP_ANIMATION_FINISHED")) clear();
		next(action);
		if (!guard(action, "DROP_ANIMATE")) return;
		const binding = {
			eventName: "scroll",
			options: {
				capture: true,
				passive: false,
				once: true
			},
			fn: function flushDropAnimation() {
				if (store.getState().phase === "DROP_ANIMATING") store.dispatch(dropAnimationFinished());
			}
		};
		frameId = requestAnimationFrame(() => {
			frameId = null;
			unbind = bindEvents(window, [binding]);
		});
	};
};
var dimensionMarshalStopper = (marshal) => () => (next) => (action) => {
	if (guard(action, "DROP_COMPLETE") || guard(action, "FLUSH") || guard(action, "DROP_ANIMATE")) marshal.stopPublishing();
	next(action);
};
var focus = (marshal) => {
	let isWatching = false;
	return () => (next) => (action) => {
		if (guard(action, "INITIAL_PUBLISH")) {
			isWatching = true;
			marshal.tryRecordFocus(action.payload.critical.draggable.id);
			next(action);
			marshal.tryRestoreFocusRecorded();
			return;
		}
		next(action);
		if (!isWatching) return;
		if (guard(action, "FLUSH")) {
			isWatching = false;
			marshal.tryRestoreFocusRecorded();
			return;
		}
		if (guard(action, "DROP_COMPLETE")) {
			isWatching = false;
			const result = action.payload.completed.result;
			if (result.combine) marshal.tryShiftRecord(result.draggableId, result.combine.draggableId);
			marshal.tryRestoreFocusRecorded();
		}
	};
};
var shouldStop = (action) => guard(action, "DROP_COMPLETE") || guard(action, "DROP_ANIMATE") || guard(action, "FLUSH");
var autoScroll = (autoScroller) => (store) => (next) => (action) => {
	if (shouldStop(action)) {
		autoScroller.stop();
		next(action);
		return;
	}
	if (guard(action, "INITIAL_PUBLISH")) {
		next(action);
		const state = store.getState();
		!(state.phase === "DRAGGING") && invariant(false, "Expected phase to be DRAGGING after INITIAL_PUBLISH");
		autoScroller.start(state);
		return;
	}
	next(action);
	autoScroller.scroll(store.getState());
};
var pendingDrop = (store) => (next) => (action) => {
	next(action);
	if (!guard(action, "PUBLISH_WHILE_DRAGGING")) return;
	const postActionState = store.getState();
	if (postActionState.phase !== "DROP_PENDING") return;
	if (postActionState.isWaiting) return;
	store.dispatch(drop({ reason: postActionState.reason }));
};
var composeEnhancers = typeof window !== "undefined" && window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ ? window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__({ name: "@hello-pangea/dnd" }) : compose;
var createStore = ({ dimensionMarshal, focusMarshal, styleMarshal, getResponders, announce, autoScroller }) => createStore$1(reducer, composeEnhancers(applyMiddleware(style$1(styleMarshal), dimensionMarshalStopper(dimensionMarshal), lift(dimensionMarshal), dropMiddleware, dropAnimationFinishMiddleware, dropAnimationFlushOnScrollMiddleware, pendingDrop, autoScroll(autoScroller), scrollListener, focus(focusMarshal), responders(getResponders, announce))));
var clean$1 = () => ({
	additions: {},
	removals: {},
	modified: {}
});
function createPublisher({ registry, callbacks }) {
	let staging = clean$1();
	let frameId = null;
	const collect = () => {
		if (frameId) return;
		callbacks.collectionStarting();
		frameId = requestAnimationFrame(() => {
			frameId = null;
			const { additions, removals, modified } = staging;
			const added = Object.keys(additions).map((id) => registry.draggable.getById(id).getDimension(origin)).sort((a, b) => a.descriptor.index - b.descriptor.index);
			const updated = Object.keys(modified).map((id) => {
				return {
					droppableId: id,
					scroll: registry.droppable.getById(id).callbacks.getScrollWhileDragging()
				};
			});
			const result = {
				additions: added,
				removals: Object.keys(removals),
				modified: updated
			};
			staging = clean$1();
			callbacks.publish(result);
		});
	};
	const add = (entry) => {
		const id = entry.descriptor.id;
		staging.additions[id] = entry;
		staging.modified[entry.descriptor.droppableId] = true;
		if (staging.removals[id]) delete staging.removals[id];
		collect();
	};
	const remove = (entry) => {
		const descriptor = entry.descriptor;
		staging.removals[descriptor.id] = true;
		staging.modified[descriptor.droppableId] = true;
		if (staging.additions[descriptor.id]) delete staging.additions[descriptor.id];
		collect();
	};
	const stop = () => {
		if (!frameId) return;
		cancelAnimationFrame(frameId);
		frameId = null;
		staging = clean$1();
	};
	return {
		add,
		remove,
		stop
	};
}
var getMaxScroll = ({ scrollHeight, scrollWidth, height, width }) => {
	const maxScroll = subtract({
		x: scrollWidth,
		y: scrollHeight
	}, {
		x: width,
		y: height
	});
	return {
		x: Math.max(0, maxScroll.x),
		y: Math.max(0, maxScroll.y)
	};
};
var getDocumentElement = () => {
	const doc = document.documentElement;
	!doc && invariant(false, "Cannot find document.documentElement");
	return doc;
};
var getMaxWindowScroll = () => {
	const doc = getDocumentElement();
	return getMaxScroll({
		scrollHeight: doc.scrollHeight,
		scrollWidth: doc.scrollWidth,
		width: doc.clientWidth,
		height: doc.clientHeight
	});
};
var getViewport = () => {
	const scroll = getWindowScroll();
	const maxScroll = getMaxWindowScroll();
	const top = scroll.y;
	const left = scroll.x;
	const doc = getDocumentElement();
	const width = doc.clientWidth;
	const height = doc.clientHeight;
	return {
		frame: getRect({
			top,
			left,
			right: left + width,
			bottom: top + height
		}),
		scroll: {
			initial: scroll,
			current: scroll,
			max: maxScroll,
			diff: {
				value: origin,
				displacement: origin
			}
		}
	};
};
var getInitialPublish = ({ critical, scrollOptions, registry }) => {
	const viewport = getViewport();
	const windowScroll = viewport.scroll.current;
	const home = critical.droppable;
	const droppables = registry.droppable.getAllByType(home.type).map((entry) => entry.callbacks.getDimensionAndWatchScroll(windowScroll, scrollOptions));
	return {
		dimensions: {
			draggables: toDraggableMap(registry.draggable.getAllByType(critical.draggable.type).map((entry) => entry.getDimension(windowScroll))),
			droppables: toDroppableMap(droppables)
		},
		critical,
		viewport
	};
};
function shouldPublishUpdate(registry, dragging, entry) {
	if (entry.descriptor.id === dragging.id) return false;
	if (entry.descriptor.type !== dragging.type) return false;
	if (registry.droppable.getById(entry.descriptor.droppableId).descriptor.mode !== "virtual") {
		warning$1(`
      You are attempting to add or remove a Draggable [id: ${entry.descriptor.id}]
      while a drag is occurring. This is only supported for virtual lists.

      See https://github.com/hello-pangea/dnd/blob/main/docs/patterns/virtual-lists.md
    `);
		return false;
	}
	return true;
}
var createDimensionMarshal = (registry, callbacks) => {
	let collection = null;
	const publisher = createPublisher({
		callbacks: {
			publish: callbacks.publishWhileDragging,
			collectionStarting: callbacks.collectionStarting
		},
		registry
	});
	const updateDroppableIsEnabled = (id, isEnabled) => {
		!registry.droppable.exists(id) && invariant(false, `Cannot update is enabled flag of Droppable ${id} as it is not registered`);
		if (!collection) return;
		callbacks.updateDroppableIsEnabled({
			id,
			isEnabled
		});
	};
	const updateDroppableIsCombineEnabled = (id, isCombineEnabled) => {
		if (!collection) return;
		!registry.droppable.exists(id) && invariant(false, `Cannot update isCombineEnabled flag of Droppable ${id} as it is not registered`);
		callbacks.updateDroppableIsCombineEnabled({
			id,
			isCombineEnabled
		});
	};
	const updateDroppableScroll = (id, newScroll) => {
		if (!collection) return;
		!registry.droppable.exists(id) && invariant(false, `Cannot update the scroll on Droppable ${id} as it is not registered`);
		callbacks.updateDroppableScroll({
			id,
			newScroll
		});
	};
	const scrollDroppable = (id, change) => {
		if (!collection) return;
		registry.droppable.getById(id).callbacks.scroll(change);
	};
	const stopPublishing = () => {
		if (!collection) return;
		publisher.stop();
		const home = collection.critical.droppable;
		registry.droppable.getAllByType(home.type).forEach((entry) => entry.callbacks.dragStopped());
		collection.unsubscribe();
		collection = null;
	};
	const subscriber = (event) => {
		!collection && invariant(false, "Should only be subscribed when a collection is occurring");
		const dragging = collection.critical.draggable;
		if (event.type === "ADDITION") {
			if (shouldPublishUpdate(registry, dragging, event.value)) publisher.add(event.value);
		}
		if (event.type === "REMOVAL") {
			if (shouldPublishUpdate(registry, dragging, event.value)) publisher.remove(event.value);
		}
	};
	const startPublishing = (request) => {
		collection && invariant(false, "Cannot start capturing critical dimensions as there is already a collection");
		const entry = registry.draggable.getById(request.draggableId);
		const home = registry.droppable.getById(entry.descriptor.droppableId);
		const critical = {
			draggable: entry.descriptor,
			droppable: home.descriptor
		};
		collection = {
			critical,
			unsubscribe: registry.subscribe(subscriber)
		};
		return getInitialPublish({
			critical,
			registry,
			scrollOptions: request.scrollOptions
		});
	};
	return {
		updateDroppableIsEnabled,
		updateDroppableIsCombineEnabled,
		scrollDroppable,
		updateDroppableScroll,
		startPublishing,
		stopPublishing
	};
};
var canStartDrag = (state, id) => {
	if (state.phase === "IDLE") return true;
	if (state.phase !== "DROP_ANIMATING") return false;
	if (state.completed.result.draggableId === id) return false;
	return state.completed.result.reason === "DROP";
};
var scrollWindow = (change) => {
	window.scrollBy(change.x, change.y);
};
var getScrollableDroppables = memoizeOne$1((droppables) => toDroppableList(droppables).filter((droppable) => {
	if (!droppable.isEnabled) return false;
	if (!droppable.frame) return false;
	return true;
}));
var getScrollableDroppableOver = (target, droppables) => {
	return getScrollableDroppables(droppables).find((droppable) => {
		!droppable.frame && invariant(false, "Invalid result");
		return isPositionInFrame(droppable.frame.pageMarginBox)(target);
	}) || null;
};
var getBestScrollableDroppable = ({ center, destination, droppables }) => {
	if (destination) {
		const dimension = droppables[destination];
		if (!dimension.frame) return null;
		return dimension;
	}
	return getScrollableDroppableOver(center, droppables);
};
var defaultAutoScrollerOptions = {
	startFromPercentage: .25,
	maxScrollAtPercentage: .05,
	maxPixelScroll: 28,
	ease: (percentage) => percentage ** 2,
	durationDampening: {
		stopDampeningAt: 1200,
		accelerateAt: 360
	},
	disabled: false
};
var getDistanceThresholds = (container, axis, getAutoScrollerOptions = () => defaultAutoScrollerOptions) => {
	const autoScrollerOptions = getAutoScrollerOptions();
	return {
		startScrollingFrom: container[axis.size] * autoScrollerOptions.startFromPercentage,
		maxScrollValueAt: container[axis.size] * autoScrollerOptions.maxScrollAtPercentage
	};
};
var getPercentage = ({ startOfRange, endOfRange, current }) => {
	const range = endOfRange - startOfRange;
	if (range === 0) {
		warning$1(`
      Detected distance range of 0 in the fluid auto scroller
      This is unexpected and would cause a divide by 0 issue.
      Not allowing an auto scroll
    `);
		return 0;
	}
	return (current - startOfRange) / range;
};
var minScroll = 1;
var getValueFromDistance = (distanceToEdge, thresholds, getAutoScrollerOptions = () => defaultAutoScrollerOptions) => {
	const autoScrollerOptions = getAutoScrollerOptions();
	if (distanceToEdge > thresholds.startScrollingFrom) return 0;
	if (distanceToEdge <= thresholds.maxScrollValueAt) return autoScrollerOptions.maxPixelScroll;
	if (distanceToEdge === thresholds.startScrollingFrom) return minScroll;
	const percentageFromStartScrollingFrom = 1 - getPercentage({
		startOfRange: thresholds.maxScrollValueAt,
		endOfRange: thresholds.startScrollingFrom,
		current: distanceToEdge
	});
	const scroll = autoScrollerOptions.maxPixelScroll * autoScrollerOptions.ease(percentageFromStartScrollingFrom);
	return Math.ceil(scroll);
};
var dampenValueByTime = (proposedScroll, dragStartTime, getAutoScrollerOptions) => {
	const autoScrollerOptions = getAutoScrollerOptions();
	const accelerateAt = autoScrollerOptions.durationDampening.accelerateAt;
	const stopAt = autoScrollerOptions.durationDampening.stopDampeningAt;
	const startOfRange = dragStartTime;
	const endOfRange = stopAt;
	const runTime = Date.now() - startOfRange;
	if (runTime >= stopAt) return proposedScroll;
	if (runTime < accelerateAt) return minScroll;
	const betweenAccelerateAtAndStopAtPercentage = getPercentage({
		startOfRange: accelerateAt,
		endOfRange,
		current: runTime
	});
	const scroll = proposedScroll * autoScrollerOptions.ease(betweenAccelerateAtAndStopAtPercentage);
	return Math.ceil(scroll);
};
var getValue = ({ distanceToEdge, thresholds, dragStartTime, shouldUseTimeDampening, getAutoScrollerOptions }) => {
	const scroll = getValueFromDistance(distanceToEdge, thresholds, getAutoScrollerOptions);
	if (scroll === 0) return 0;
	if (!shouldUseTimeDampening) return scroll;
	return Math.max(dampenValueByTime(scroll, dragStartTime, getAutoScrollerOptions), minScroll);
};
var getScrollOnAxis = ({ container, distanceToEdges, dragStartTime, axis, shouldUseTimeDampening, getAutoScrollerOptions }) => {
	const thresholds = getDistanceThresholds(container, axis, getAutoScrollerOptions);
	if (distanceToEdges[axis.end] < distanceToEdges[axis.start]) return getValue({
		distanceToEdge: distanceToEdges[axis.end],
		thresholds,
		dragStartTime,
		shouldUseTimeDampening,
		getAutoScrollerOptions
	});
	return -1 * getValue({
		distanceToEdge: distanceToEdges[axis.start],
		thresholds,
		dragStartTime,
		shouldUseTimeDampening,
		getAutoScrollerOptions
	});
};
var adjustForSizeLimits = ({ container, subject, proposedScroll }) => {
	const isTooBigVertically = subject.height > container.height;
	const isTooBigHorizontally = subject.width > container.width;
	if (!isTooBigHorizontally && !isTooBigVertically) return proposedScroll;
	if (isTooBigHorizontally && isTooBigVertically) return null;
	return {
		x: isTooBigHorizontally ? 0 : proposedScroll.x,
		y: isTooBigVertically ? 0 : proposedScroll.y
	};
};
var clean = apply((value) => value === 0 ? 0 : value);
var getScroll$1 = ({ dragStartTime, container, subject, center, shouldUseTimeDampening, getAutoScrollerOptions }) => {
	const distanceToEdges = {
		top: center.y - container.top,
		right: container.right - center.x,
		bottom: container.bottom - center.y,
		left: center.x - container.left
	};
	const y = getScrollOnAxis({
		container,
		distanceToEdges,
		dragStartTime,
		axis: vertical,
		shouldUseTimeDampening,
		getAutoScrollerOptions
	});
	const required = clean({
		x: getScrollOnAxis({
			container,
			distanceToEdges,
			dragStartTime,
			axis: horizontal,
			shouldUseTimeDampening,
			getAutoScrollerOptions
		}),
		y
	});
	if (isEqual$1$1(required, origin)) return null;
	const limited = adjustForSizeLimits({
		container,
		subject,
		proposedScroll: required
	});
	if (!limited) return null;
	return isEqual$1$1(limited, origin) ? null : limited;
};
var smallestSigned = apply((value) => {
	if (value === 0) return 0;
	return value > 0 ? 1 : -1;
});
var getOverlap = (() => {
	const getRemainder = (target, max) => {
		if (target < 0) return target;
		if (target > max) return target - max;
		return 0;
	};
	return ({ current, max, change }) => {
		const targetScroll = add(current, change);
		const overlap = {
			x: getRemainder(targetScroll.x, max.x),
			y: getRemainder(targetScroll.y, max.y)
		};
		if (isEqual$1$1(overlap, origin)) return null;
		return overlap;
	};
})();
var canPartiallyScroll = ({ max: rawMax, current, change }) => {
	const max = {
		x: Math.max(current.x, rawMax.x),
		y: Math.max(current.y, rawMax.y)
	};
	const smallestChange = smallestSigned(change);
	const overlap = getOverlap({
		max,
		current,
		change: smallestChange
	});
	if (!overlap) return true;
	if (smallestChange.x !== 0 && overlap.x === 0) return true;
	if (smallestChange.y !== 0 && overlap.y === 0) return true;
	return false;
};
var canScrollWindow = (viewport, change) => canPartiallyScroll({
	current: viewport.scroll.current,
	max: viewport.scroll.max,
	change
});
var getWindowOverlap = (viewport, change) => {
	if (!canScrollWindow(viewport, change)) return null;
	const max = viewport.scroll.max;
	const current = viewport.scroll.current;
	return getOverlap({
		current,
		max,
		change
	});
};
var canScrollDroppable = (droppable, change) => {
	const frame = droppable.frame;
	if (!frame) return false;
	return canPartiallyScroll({
		current: frame.scroll.current,
		max: frame.scroll.max,
		change
	});
};
var getDroppableOverlap = (droppable, change) => {
	const frame = droppable.frame;
	if (!frame) return null;
	if (!canScrollDroppable(droppable, change)) return null;
	return getOverlap({
		current: frame.scroll.current,
		max: frame.scroll.max,
		change
	});
};
var getWindowScrollChange = ({ viewport, subject, center, dragStartTime, shouldUseTimeDampening, getAutoScrollerOptions }) => {
	const scroll = getScroll$1({
		dragStartTime,
		container: viewport.frame,
		subject,
		center,
		shouldUseTimeDampening,
		getAutoScrollerOptions
	});
	return scroll && canScrollWindow(viewport, scroll) ? scroll : null;
};
var getDroppableScrollChange = ({ droppable, subject, center, dragStartTime, shouldUseTimeDampening, getAutoScrollerOptions }) => {
	const frame = droppable.frame;
	if (!frame) return null;
	const scroll = getScroll$1({
		dragStartTime,
		container: frame.pageMarginBox,
		subject,
		center,
		shouldUseTimeDampening,
		getAutoScrollerOptions
	});
	return scroll && canScrollDroppable(droppable, scroll) ? scroll : null;
};
var scroll = ({ state, dragStartTime, shouldUseTimeDampening, scrollWindow, scrollDroppable, getAutoScrollerOptions }) => {
	const center = state.current.page.borderBoxCenter;
	const subject = state.dimensions.draggables[state.critical.draggable.id].page.marginBox;
	if (state.isWindowScrollAllowed) {
		const viewport = state.viewport;
		const change = getWindowScrollChange({
			dragStartTime,
			viewport,
			subject,
			center,
			shouldUseTimeDampening,
			getAutoScrollerOptions
		});
		if (change) {
			scrollWindow(change);
			return;
		}
	}
	const droppable = getBestScrollableDroppable({
		center,
		destination: whatIsDraggedOver(state.impact),
		droppables: state.dimensions.droppables
	});
	if (!droppable) return;
	const change = getDroppableScrollChange({
		dragStartTime,
		droppable,
		subject,
		center,
		shouldUseTimeDampening,
		getAutoScrollerOptions
	});
	if (change) scrollDroppable(droppable.descriptor.id, change);
};
var createFluidScroller = ({ scrollWindow, scrollDroppable, getAutoScrollerOptions = () => defaultAutoScrollerOptions }) => {
	const scheduleWindowScroll = rafSchd(scrollWindow);
	const scheduleDroppableScroll = rafSchd(scrollDroppable);
	let dragging = null;
	const tryScroll = (state) => {
		!dragging && invariant(false, "Cannot fluid scroll if not dragging");
		const { shouldUseTimeDampening, dragStartTime } = dragging;
		scroll({
			state,
			scrollWindow: scheduleWindowScroll,
			scrollDroppable: scheduleDroppableScroll,
			dragStartTime,
			shouldUseTimeDampening,
			getAutoScrollerOptions
		});
	};
	const start$1 = (state) => {
		dragging && invariant(false, "Cannot start auto scrolling when already started");
		const dragStartTime = Date.now();
		let wasScrollNeeded = false;
		const fakeScrollCallback = () => {
			wasScrollNeeded = true;
		};
		scroll({
			state,
			dragStartTime: 0,
			shouldUseTimeDampening: false,
			scrollWindow: fakeScrollCallback,
			scrollDroppable: fakeScrollCallback,
			getAutoScrollerOptions
		});
		dragging = {
			dragStartTime,
			shouldUseTimeDampening: wasScrollNeeded
		};
		if (wasScrollNeeded) tryScroll(state);
	};
	const stop = () => {
		if (!dragging) return;
		scheduleWindowScroll.cancel();
		scheduleDroppableScroll.cancel();
		dragging = null;
	};
	return {
		start: start$1,
		stop,
		scroll: tryScroll
	};
};
var createJumpScroller = ({ move, scrollDroppable, scrollWindow }) => {
	const moveByOffset = (state, offset) => {
		move({ client: add(state.current.client.selection, offset) });
	};
	const scrollDroppableAsMuchAsItCan = (droppable, change) => {
		if (!canScrollDroppable(droppable, change)) return change;
		const overlap = getDroppableOverlap(droppable, change);
		if (!overlap) {
			scrollDroppable(droppable.descriptor.id, change);
			return null;
		}
		const whatTheDroppableCanScroll = subtract(change, overlap);
		scrollDroppable(droppable.descriptor.id, whatTheDroppableCanScroll);
		return subtract(change, whatTheDroppableCanScroll);
	};
	const scrollWindowAsMuchAsItCan = (isWindowScrollAllowed, viewport, change) => {
		if (!isWindowScrollAllowed) return change;
		if (!canScrollWindow(viewport, change)) return change;
		const overlap = getWindowOverlap(viewport, change);
		if (!overlap) {
			scrollWindow(change);
			return null;
		}
		const whatTheWindowCanScroll = subtract(change, overlap);
		scrollWindow(whatTheWindowCanScroll);
		return subtract(change, whatTheWindowCanScroll);
	};
	const jumpScroller = (state) => {
		const request = state.scrollJumpRequest;
		if (!request) return;
		const destination = whatIsDraggedOver(state.impact);
		!destination && invariant(false, "Cannot perform a jump scroll when there is no destination");
		const droppableRemainder = scrollDroppableAsMuchAsItCan(state.dimensions.droppables[destination], request);
		if (!droppableRemainder) return;
		const viewport = state.viewport;
		const windowRemainder = scrollWindowAsMuchAsItCan(state.isWindowScrollAllowed, viewport, droppableRemainder);
		if (!windowRemainder) return;
		moveByOffset(state, windowRemainder);
	};
	return jumpScroller;
};
var createAutoScroller = ({ scrollDroppable, scrollWindow, move, getAutoScrollerOptions }) => {
	const fluidScroller = createFluidScroller({
		scrollWindow,
		scrollDroppable,
		getAutoScrollerOptions
	});
	const jumpScroll = createJumpScroller({
		move,
		scrollWindow,
		scrollDroppable
	});
	const scroll = (state) => {
		if (getAutoScrollerOptions().disabled || state.phase !== "DRAGGING") return;
		if (state.movementMode === "FLUID") {
			fluidScroller.scroll(state);
			return;
		}
		if (!state.scrollJumpRequest) return;
		jumpScroll(state);
	};
	return {
		scroll,
		start: fluidScroller.start,
		stop: fluidScroller.stop
	};
};
var prefix = "data-rfd";
var dragHandle = (() => {
	const base = `${prefix}-drag-handle`;
	return {
		base,
		draggableId: `${base}-draggable-id`,
		contextId: `${base}-context-id`
	};
})();
var draggable = (() => {
	const base = `${prefix}-draggable`;
	return {
		base,
		contextId: `${base}-context-id`,
		id: `${base}-id`
	};
})();
var droppable = (() => {
	const base = `${prefix}-droppable`;
	return {
		base,
		contextId: `${base}-context-id`,
		id: `${base}-id`
	};
})();
var scrollContainer = { contextId: `${prefix}-scroll-container-context-id` };
var makeGetSelector = (context) => (attribute) => `[${attribute}="${context}"]`;
var getStyles = (rules, property) => rules.map((rule) => {
	const value = rule.styles[property];
	if (!value) return "";
	return `${rule.selector} { ${value} }`;
}).join(" ");
var noPointerEvents = "pointer-events: none;";
var getStyles$1 = (contextId) => {
	const getSelector = makeGetSelector(contextId);
	const dragHandle$1 = (() => {
		const grabCursor = `
      cursor: -webkit-grab;
      cursor: grab;
    `;
		return {
			selector: getSelector(dragHandle.contextId),
			styles: {
				always: `
          -webkit-touch-callout: none;
          -webkit-tap-highlight-color: rgba(0,0,0,0);
          touch-action: manipulation;
        `,
				resting: grabCursor,
				dragging: noPointerEvents,
				dropAnimating: grabCursor
			}
		};
	})();
	const rules = [
		(() => {
			const transition = `
      transition: ${transitions.outOfTheWay};
    `;
			return {
				selector: getSelector(draggable.contextId),
				styles: {
					dragging: transition,
					dropAnimating: transition,
					userCancel: transition
				}
			};
		})(),
		dragHandle$1,
		{
			selector: getSelector(droppable.contextId),
			styles: { always: `overflow-anchor: none;` }
		},
		{
			selector: "body",
			styles: { dragging: `
        cursor: grabbing;
        cursor: -webkit-grabbing;
        user-select: none;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        overflow-anchor: none;
      ` }
		}
	];
	return {
		always: getStyles(rules, "always"),
		resting: getStyles(rules, "resting"),
		dragging: getStyles(rules, "dragging"),
		dropAnimating: getStyles(rules, "dropAnimating"),
		userCancel: getStyles(rules, "userCancel")
	};
};
var useIsomorphicLayoutEffect = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined" ? import_react.useLayoutEffect : import_react.useEffect;
var getHead = () => {
	const head = document.querySelector("head");
	!head && invariant(false, "Cannot find the head to append a style to");
	return head;
};
var createStyleEl = (nonce) => {
	const el = document.createElement("style");
	if (nonce) el.setAttribute("nonce", nonce);
	el.type = "text/css";
	return el;
};
function useStyleMarshal(contextId, nonce) {
	const styles = useMemo$1(() => getStyles$1(contextId), [contextId]);
	const alwaysRef = (0, import_react.useRef)(null);
	const dynamicRef = (0, import_react.useRef)(null);
	const setDynamicStyle = useCallback(memoizeOne$1((proposed) => {
		const el = dynamicRef.current;
		!el && invariant(false, "Cannot set dynamic style element if it is not set");
		el.textContent = proposed;
	}), []);
	const setAlwaysStyle = useCallback((proposed) => {
		const el = alwaysRef.current;
		!el && invariant(false, "Cannot set dynamic style element if it is not set");
		el.textContent = proposed;
	}, []);
	useIsomorphicLayoutEffect(() => {
		!(!alwaysRef.current && !dynamicRef.current) && invariant(false, "style elements already mounted");
		const always = createStyleEl(nonce);
		const dynamic = createStyleEl(nonce);
		alwaysRef.current = always;
		dynamicRef.current = dynamic;
		always.setAttribute(`${prefix}-always`, contextId);
		dynamic.setAttribute(`${prefix}-dynamic`, contextId);
		getHead().appendChild(always);
		getHead().appendChild(dynamic);
		setAlwaysStyle(styles.always);
		setDynamicStyle(styles.resting);
		return () => {
			const remove = (ref) => {
				const current = ref.current;
				!current && invariant(false, "Cannot unmount ref as it is not set");
				getHead().removeChild(current);
				ref.current = null;
			};
			remove(alwaysRef);
			remove(dynamicRef);
		};
	}, [
		nonce,
		setAlwaysStyle,
		setDynamicStyle,
		styles.always,
		styles.resting,
		contextId
	]);
	const dragging = useCallback(() => setDynamicStyle(styles.dragging), [setDynamicStyle, styles.dragging]);
	const dropping = useCallback((reason) => {
		if (reason === "DROP") {
			setDynamicStyle(styles.dropAnimating);
			return;
		}
		setDynamicStyle(styles.userCancel);
	}, [
		setDynamicStyle,
		styles.dropAnimating,
		styles.userCancel
	]);
	const resting = useCallback(() => {
		if (!dynamicRef.current) return;
		setDynamicStyle(styles.resting);
	}, [setDynamicStyle, styles.resting]);
	return useMemo$1(() => ({
		dragging,
		dropping,
		resting
	}), [
		dragging,
		dropping,
		resting
	]);
}
function querySelectorAll(parentNode, selector) {
	return Array.from(parentNode.querySelectorAll(selector));
}
var getWindowFromEl = (el) => {
	if (el && el.ownerDocument && el.ownerDocument.defaultView) return el.ownerDocument.defaultView;
	return window;
};
function isHtmlElement(el) {
	return el instanceof getWindowFromEl(el).HTMLElement;
}
function findDragHandle(contextId, draggableId) {
	const selector = `[${dragHandle.contextId}="${contextId}"]`;
	const possible = querySelectorAll(document, selector);
	if (!possible.length) {
		warning$1(`Unable to find any drag handles in the context "${contextId}"`);
		return null;
	}
	const handle = possible.find((el) => {
		return el.getAttribute(dragHandle.draggableId) === draggableId;
	});
	if (!handle) {
		warning$1(`Unable to find drag handle with id "${draggableId}" as no handle with a matching id was found`);
		return null;
	}
	if (!isHtmlElement(handle)) {
		warning$1("drag handle needs to be a HTMLElement");
		return null;
	}
	return handle;
}
function useFocusMarshal(contextId) {
	const entriesRef = (0, import_react.useRef)({});
	const recordRef = (0, import_react.useRef)(null);
	const restoreFocusFrameRef = (0, import_react.useRef)(null);
	const isMountedRef = (0, import_react.useRef)(false);
	const register = useCallback(function register(id, focus) {
		const entry = {
			id,
			focus
		};
		entriesRef.current[id] = entry;
		return function unregister() {
			const entries = entriesRef.current;
			if (entries[id] !== entry) delete entries[id];
		};
	}, []);
	const tryGiveFocus = useCallback(function tryGiveFocus(tryGiveFocusTo) {
		const handle = findDragHandle(contextId, tryGiveFocusTo);
		if (handle && handle !== document.activeElement) handle.focus();
	}, [contextId]);
	const tryShiftRecord = useCallback(function tryShiftRecord(previous, redirectTo) {
		if (recordRef.current === previous) recordRef.current = redirectTo;
	}, []);
	const tryRestoreFocusRecorded = useCallback(function tryRestoreFocusRecorded() {
		if (restoreFocusFrameRef.current) return;
		if (!isMountedRef.current) return;
		restoreFocusFrameRef.current = requestAnimationFrame(() => {
			restoreFocusFrameRef.current = null;
			const record = recordRef.current;
			if (record) tryGiveFocus(record);
		});
	}, [tryGiveFocus]);
	const tryRecordFocus = useCallback(function tryRecordFocus(id) {
		recordRef.current = null;
		const focused = document.activeElement;
		if (!focused) return;
		if (focused.getAttribute(dragHandle.draggableId) !== id) return;
		recordRef.current = id;
	}, []);
	useIsomorphicLayoutEffect(() => {
		isMountedRef.current = true;
		return function clearFrameOnUnmount() {
			isMountedRef.current = false;
			const frameId = restoreFocusFrameRef.current;
			if (frameId) cancelAnimationFrame(frameId);
		};
	}, []);
	return useMemo$1(() => ({
		register,
		tryRecordFocus,
		tryRestoreFocusRecorded,
		tryShiftRecord
	}), [
		register,
		tryRecordFocus,
		tryRestoreFocusRecorded,
		tryShiftRecord
	]);
}
function createRegistry() {
	const entries = {
		draggables: {},
		droppables: {}
	};
	const subscribers = [];
	function subscribe(cb) {
		subscribers.push(cb);
		return function unsubscribe() {
			const index = subscribers.indexOf(cb);
			if (index === -1) return;
			subscribers.splice(index, 1);
		};
	}
	function notify(event) {
		if (subscribers.length) subscribers.forEach((cb) => cb(event));
	}
	function findDraggableById(id) {
		return entries.draggables[id] || null;
	}
	function getDraggableById(id) {
		const entry = findDraggableById(id);
		!entry && invariant(false, `Cannot find draggable entry with id [${id}]`);
		return entry;
	}
	const draggableAPI = {
		register: (entry) => {
			entries.draggables[entry.descriptor.id] = entry;
			notify({
				type: "ADDITION",
				value: entry
			});
		},
		update: (entry, last) => {
			const current = entries.draggables[last.descriptor.id];
			if (!current) return;
			if (current.uniqueId !== entry.uniqueId) return;
			delete entries.draggables[last.descriptor.id];
			entries.draggables[entry.descriptor.id] = entry;
		},
		unregister: (entry) => {
			const draggableId = entry.descriptor.id;
			const current = findDraggableById(draggableId);
			if (!current) return;
			if (entry.uniqueId !== current.uniqueId) return;
			delete entries.draggables[draggableId];
			if (entries.droppables[entry.descriptor.droppableId]) notify({
				type: "REMOVAL",
				value: entry
			});
		},
		getById: getDraggableById,
		findById: findDraggableById,
		exists: (id) => Boolean(findDraggableById(id)),
		getAllByType: (type) => Object.values(entries.draggables).filter((entry) => entry.descriptor.type === type)
	};
	function findDroppableById(id) {
		return entries.droppables[id] || null;
	}
	function getDroppableById(id) {
		const entry = findDroppableById(id);
		!entry && invariant(false, `Cannot find droppable entry with id [${id}]`);
		return entry;
	}
	const droppableAPI = {
		register: (entry) => {
			entries.droppables[entry.descriptor.id] = entry;
		},
		unregister: (entry) => {
			const current = findDroppableById(entry.descriptor.id);
			if (!current) return;
			if (entry.uniqueId !== current.uniqueId) return;
			delete entries.droppables[entry.descriptor.id];
		},
		getById: getDroppableById,
		findById: findDroppableById,
		exists: (id) => Boolean(findDroppableById(id)),
		getAllByType: (type) => Object.values(entries.droppables).filter((entry) => entry.descriptor.type === type)
	};
	function clean() {
		entries.draggables = {};
		entries.droppables = {};
		subscribers.length = 0;
	}
	return {
		draggable: draggableAPI,
		droppable: droppableAPI,
		subscribe,
		clean
	};
}
function useRegistry() {
	const registry = useMemo$1(createRegistry, []);
	(0, import_react.useEffect)(() => {
		return function unmount() {
			registry.clean();
		};
	}, [registry]);
	return registry;
}
var StoreContext = import_react.createContext(null);
var getBodyElement = () => {
	const body = document.body;
	!body && invariant(false, "Cannot find document.body");
	return body;
};
var visuallyHidden = {
	position: "absolute",
	width: "1px",
	height: "1px",
	margin: "-1px",
	border: "0",
	padding: "0",
	overflow: "hidden",
	clip: "rect(0 0 0 0)",
	"clip-path": "inset(100%)"
};
var getId = (contextId) => `rfd-announcement-${contextId}`;
function useAnnouncer(contextId) {
	const id = useMemo$1(() => getId(contextId), [contextId]);
	const ref = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(function setup() {
		const el = document.createElement("div");
		ref.current = el;
		el.id = id;
		el.setAttribute("aria-live", "assertive");
		el.setAttribute("aria-atomic", "true");
		_extends(el.style, visuallyHidden);
		getBodyElement().appendChild(el);
		return function cleanup() {
			setTimeout(function remove() {
				const body = getBodyElement();
				if (body.contains(el)) body.removeChild(el);
				if (el === ref.current) ref.current = null;
			});
		};
	}, [id]);
	return useCallback((message) => {
		const el = ref.current;
		if (el) {
			el.textContent = message;
			return;
		}
		warning$1(`
      A screen reader message was trying to be announced but it was unable to do so.
      This can occur if you unmount your <DragDropContext /> in your onDragEnd.
      Consider calling provided.announce() before the unmount so that the instruction will
      not be lost for users relying on a screen reader.

      Message not passed to screen reader:

      "${message}"
    `);
	}, []);
}
var defaults = { separator: "::" };
function useUniqueId(prefix, options = defaults) {
	const id = import_react.useId();
	return useMemo$1(() => `${prefix}${options.separator}${id}`, [
		options.separator,
		prefix,
		id
	]);
}
function getElementId$1({ contextId, uniqueId }) {
	return `rfd-hidden-text-${contextId}-${uniqueId}`;
}
function useHiddenTextElement({ contextId, text }) {
	const uniqueId = useUniqueId("hidden-text", { separator: "-" });
	const id = useMemo$1(() => getElementId$1({
		contextId,
		uniqueId
	}), [uniqueId, contextId]);
	(0, import_react.useEffect)(function mount() {
		const el = document.createElement("div");
		el.id = id;
		el.textContent = text;
		el.style.display = "none";
		getBodyElement().appendChild(el);
		return function unmount() {
			const body = getBodyElement();
			if (body.contains(el)) body.removeChild(el);
		};
	}, [id, text]);
	return id;
}
var AppContext = import_react.createContext(null);
var peerDependencies = { react: "^18.0.0 || ^19.0.0" };
var semver = /(\d+)\.(\d+)\.(\d+)/;
var getVersion = (value) => {
	const result = semver.exec(value);
	!(result != null) && invariant(false, `Unable to parse React version ${value}`);
	return {
		major: Number(result[1]),
		minor: Number(result[2]),
		patch: Number(result[3]),
		raw: value
	};
};
var isSatisfied = (expected, actual) => {
	if (actual.major > expected.major) return true;
	if (actual.major < expected.major) return false;
	if (actual.minor > expected.minor) return true;
	if (actual.minor < expected.minor) return false;
	return actual.patch >= expected.patch;
};
var checkReactVersion = (peerDepValue, actualValue) => {
	const peerDep = getVersion(peerDepValue);
	const actual = getVersion(actualValue);
	if (isSatisfied(peerDep, actual)) return;
	warning$1(`
    React version: [${actual.raw}]
    does not satisfy expected peer dependency version: [${peerDep.raw}]

    This can result in run time bugs, and even fatal crashes
  `);
};
var suffix = `
  We expect a html5 doctype: <!doctype html>
  This is to ensure consistent browser layout and measurement

  More information: https://github.com/hello-pangea/dnd/blob/main/docs/guides/doctype.md
`;
var checkDoctype = (doc) => {
	const doctype = doc.doctype;
	if (!doctype) {
		warning$1(`
      No <!doctype html> found.

      ${suffix}
    `);
		return;
	}
	if (doctype.name.toLowerCase() !== "html") warning$1(`
      Unexpected <!doctype> found: (${doctype.name})

      ${suffix}
    `);
	if (doctype.publicId !== "") warning$1(`
      Unexpected <!doctype> publicId found: (${doctype.publicId})
      A html5 doctype does not have a publicId

      ${suffix}
    `);
};
function useDev(useHook) {
	useHook();
}
function useDevSetupWarning(fn, inputs) {
	useDev(() => {
		(0, import_react.useEffect)(() => {
			try {
				fn();
			} catch (e) {
				error(`
          A setup problem was encountered.

          > ${e.message}
        `);
			}
		}, inputs);
	});
}
function useStartupValidation() {
	useDevSetupWarning(() => {
		checkReactVersion(peerDependencies.react, "19.2.7");
		checkDoctype(document);
	}, []);
}
function usePrevious(current) {
	const ref = (0, import_react.useRef)(current);
	(0, import_react.useEffect)(() => {
		ref.current = current;
	});
	return ref;
}
function create() {
	let lock = null;
	function isClaimed() {
		return Boolean(lock);
	}
	function isActive(value) {
		return value === lock;
	}
	function claim(abandon) {
		lock && invariant(false, "Cannot claim lock as it is already claimed");
		const newLock = { abandon };
		lock = newLock;
		return newLock;
	}
	function release() {
		!lock && invariant(false, "Cannot release lock when there is no lock");
		lock = null;
	}
	function tryAbandon() {
		if (lock) {
			lock.abandon();
			release();
		}
	}
	return {
		isClaimed,
		isActive,
		claim,
		release,
		tryAbandon
	};
}
function isDragging(state) {
	if (state.phase === "IDLE" || state.phase === "DROP_ANIMATING") return false;
	return state.isDragging;
}
var tab = 9;
var enter = 13;
var escape = 27;
var space = 32;
var pageUp = 33;
var pageDown = 34;
var end = 35;
var home = 36;
var arrowLeft = 37;
var arrowUp = 38;
var arrowRight = 39;
var arrowDown = 40;
var preventedKeys = {
	[enter]: true,
	[tab]: true
};
var preventStandardKeyEvents = (event) => {
	if (preventedKeys[event.keyCode]) event.preventDefault();
};
var supportedEventName = (() => {
	const base = "visibilitychange";
	if (typeof document === "undefined") return base;
	return [
		base,
		`ms${base}`,
		`webkit${base}`,
		`moz${base}`,
		`o${base}`
	].find((eventName) => `on${eventName}` in document) || base;
})();
var primaryButton = 0;
var sloppyClickThreshold = 5;
function isSloppyClickThresholdExceeded(original, current) {
	return Math.abs(current.x - original.x) >= sloppyClickThreshold || Math.abs(current.y - original.y) >= sloppyClickThreshold;
}
var idle$1 = { type: "IDLE" };
function getCaptureBindings({ cancel, completed, getPhase, setPhase }) {
	return [
		{
			eventName: "mousemove",
			fn: (event) => {
				const { button, clientX, clientY } = event;
				if (button !== primaryButton) return;
				const point = {
					x: clientX,
					y: clientY
				};
				const phase = getPhase();
				if (phase.type === "DRAGGING") {
					event.preventDefault();
					phase.actions.move(point);
					return;
				}
				!(phase.type === "PENDING") && invariant(false, "Cannot be IDLE");
				const pending = phase.point;
				if (!isSloppyClickThresholdExceeded(pending, point)) return;
				event.preventDefault();
				setPhase({
					type: "DRAGGING",
					actions: phase.actions.fluidLift(point)
				});
			}
		},
		{
			eventName: "mouseup",
			fn: (event) => {
				const phase = getPhase();
				if (phase.type !== "DRAGGING") {
					cancel();
					return;
				}
				event.preventDefault();
				phase.actions.drop({ shouldBlockNextClick: true });
				completed();
			}
		},
		{
			eventName: "mousedown",
			fn: (event) => {
				if (getPhase().type === "DRAGGING") event.preventDefault();
				cancel();
			}
		},
		{
			eventName: "keydown",
			fn: (event) => {
				if (getPhase().type === "PENDING") {
					cancel();
					return;
				}
				if (event.keyCode === escape) {
					event.preventDefault();
					cancel();
					return;
				}
				preventStandardKeyEvents(event);
			}
		},
		{
			eventName: "resize",
			fn: cancel
		},
		{
			eventName: "scroll",
			options: {
				passive: true,
				capture: false
			},
			fn: () => {
				if (getPhase().type === "PENDING") cancel();
			}
		},
		{
			eventName: "webkitmouseforcedown",
			fn: (event) => {
				const phase = getPhase();
				!(phase.type !== "IDLE") && invariant(false, "Unexpected phase");
				if (phase.actions.shouldRespectForcePress()) {
					cancel();
					return;
				}
				event.preventDefault();
			}
		},
		{
			eventName: supportedEventName,
			fn: cancel
		}
	];
}
function useMouseSensor(api) {
	const phaseRef = (0, import_react.useRef)(idle$1);
	const unbindEventsRef = (0, import_react.useRef)(noop$2);
	const startCaptureBinding = useMemo$1(() => ({
		eventName: "mousedown",
		fn: function onMouseDown(event) {
			if (event.defaultPrevented) return;
			if (event.button !== primaryButton) return;
			if (event.ctrlKey || event.metaKey || event.shiftKey || event.altKey) return;
			const draggableId = api.findClosestDraggableId(event);
			if (!draggableId) return;
			const actions = api.tryGetLock(draggableId, stop, { sourceEvent: event });
			if (!actions) return;
			event.preventDefault();
			const point = {
				x: event.clientX,
				y: event.clientY
			};
			unbindEventsRef.current();
			startPendingDrag(actions, point);
		}
	}), [api]);
	const preventForcePressBinding = useMemo$1(() => ({
		eventName: "webkitmouseforcewillbegin",
		fn: (event) => {
			if (event.defaultPrevented) return;
			const id = api.findClosestDraggableId(event);
			if (!id) return;
			const options = api.findOptionsForDraggable(id);
			if (!options) return;
			if (options.shouldRespectForcePress) return;
			if (!api.canGetLock(id)) return;
			event.preventDefault();
		}
	}), [api]);
	const listenForCapture = useCallback(function listenForCapture() {
		unbindEventsRef.current = bindEvents(window, [preventForcePressBinding, startCaptureBinding], {
			passive: false,
			capture: true
		});
	}, [preventForcePressBinding, startCaptureBinding]);
	const stop = useCallback(() => {
		if (phaseRef.current.type === "IDLE") return;
		phaseRef.current = idle$1;
		unbindEventsRef.current();
		listenForCapture();
	}, [listenForCapture]);
	const cancel = useCallback(() => {
		const phase = phaseRef.current;
		stop();
		if (phase.type === "DRAGGING") phase.actions.cancel({ shouldBlockNextClick: true });
		if (phase.type === "PENDING") phase.actions.abort();
	}, [stop]);
	const bindCapturingEvents = useCallback(function bindCapturingEvents() {
		const options = {
			capture: true,
			passive: false
		};
		const bindings = getCaptureBindings({
			cancel,
			completed: stop,
			getPhase: () => phaseRef.current,
			setPhase: (phase) => {
				phaseRef.current = phase;
			}
		});
		unbindEventsRef.current = bindEvents(window, bindings, options);
	}, [cancel, stop]);
	const startPendingDrag = useCallback(function startPendingDrag(actions, point) {
		!(phaseRef.current.type === "IDLE") && invariant(false, "Expected to move from IDLE to PENDING drag");
		phaseRef.current = {
			type: "PENDING",
			point,
			actions
		};
		bindCapturingEvents();
	}, [bindCapturingEvents]);
	useIsomorphicLayoutEffect(function mount() {
		listenForCapture();
		return function unmount() {
			unbindEventsRef.current();
		};
	}, [listenForCapture]);
}
function noop$1() {}
var scrollJumpKeys = {
	[pageDown]: true,
	[pageUp]: true,
	[home]: true,
	[end]: true
};
function getDraggingBindings(actions, stop) {
	function cancel() {
		stop();
		actions.cancel();
	}
	function drop() {
		stop();
		actions.drop();
	}
	return [
		{
			eventName: "keydown",
			fn: (event) => {
				if (event.keyCode === escape) {
					event.preventDefault();
					cancel();
					return;
				}
				if (event.keyCode === space) {
					event.preventDefault();
					drop();
					return;
				}
				if (event.keyCode === arrowDown) {
					event.preventDefault();
					actions.moveDown();
					return;
				}
				if (event.keyCode === arrowUp) {
					event.preventDefault();
					actions.moveUp();
					return;
				}
				if (event.keyCode === arrowRight) {
					event.preventDefault();
					actions.moveRight();
					return;
				}
				if (event.keyCode === arrowLeft) {
					event.preventDefault();
					actions.moveLeft();
					return;
				}
				if (scrollJumpKeys[event.keyCode]) {
					event.preventDefault();
					return;
				}
				preventStandardKeyEvents(event);
			}
		},
		{
			eventName: "mousedown",
			fn: cancel
		},
		{
			eventName: "mouseup",
			fn: cancel
		},
		{
			eventName: "click",
			fn: cancel
		},
		{
			eventName: "touchstart",
			fn: cancel
		},
		{
			eventName: "resize",
			fn: cancel
		},
		{
			eventName: "wheel",
			fn: cancel,
			options: { passive: true }
		},
		{
			eventName: supportedEventName,
			fn: cancel
		}
	];
}
function useKeyboardSensor(api) {
	const unbindEventsRef = (0, import_react.useRef)(noop$1);
	const startCaptureBinding = useMemo$1(() => ({
		eventName: "keydown",
		fn: function onKeyDown(event) {
			if (event.defaultPrevented) return;
			if (event.keyCode !== space) return;
			const draggableId = api.findClosestDraggableId(event);
			if (!draggableId) return;
			const preDrag = api.tryGetLock(draggableId, stop, { sourceEvent: event });
			if (!preDrag) return;
			event.preventDefault();
			let isCapturing = true;
			const actions = preDrag.snapLift();
			unbindEventsRef.current();
			function stop() {
				!isCapturing && invariant(false, "Cannot stop capturing a keyboard drag when not capturing");
				isCapturing = false;
				unbindEventsRef.current();
				listenForCapture();
			}
			unbindEventsRef.current = bindEvents(window, getDraggingBindings(actions, stop), {
				capture: true,
				passive: false
			});
		}
	}), [api]);
	const listenForCapture = useCallback(function tryStartCapture() {
		unbindEventsRef.current = bindEvents(window, [startCaptureBinding], {
			passive: false,
			capture: true
		});
	}, [startCaptureBinding]);
	useIsomorphicLayoutEffect(function mount() {
		listenForCapture();
		return function unmount() {
			unbindEventsRef.current();
		};
	}, [listenForCapture]);
}
var idle = { type: "IDLE" };
var timeForLongPress = 120;
var forcePressThreshold = .15;
function getWindowBindings({ cancel, getPhase }) {
	return [
		{
			eventName: "orientationchange",
			fn: cancel
		},
		{
			eventName: "resize",
			fn: cancel
		},
		{
			eventName: "contextmenu",
			fn: (event) => {
				event.preventDefault();
			}
		},
		{
			eventName: "keydown",
			fn: (event) => {
				if (getPhase().type !== "DRAGGING") {
					cancel();
					return;
				}
				if (event.keyCode === escape) event.preventDefault();
				cancel();
			}
		},
		{
			eventName: supportedEventName,
			fn: cancel
		}
	];
}
function getHandleBindings({ cancel, completed, getPhase }) {
	return [
		{
			eventName: "touchmove",
			options: { capture: false },
			fn: (event) => {
				const phase = getPhase();
				if (phase.type !== "DRAGGING") {
					cancel();
					return;
				}
				phase.hasMoved = true;
				const { clientX, clientY } = event.touches[0];
				const point = {
					x: clientX,
					y: clientY
				};
				event.preventDefault();
				phase.actions.move(point);
			}
		},
		{
			eventName: "touchend",
			fn: (event) => {
				const phase = getPhase();
				if (phase.type !== "DRAGGING") {
					cancel();
					return;
				}
				event.preventDefault();
				phase.actions.drop({ shouldBlockNextClick: true });
				completed();
			}
		},
		{
			eventName: "touchcancel",
			fn: (event) => {
				if (getPhase().type !== "DRAGGING") {
					cancel();
					return;
				}
				event.preventDefault();
				cancel();
			}
		},
		{
			eventName: "touchforcechange",
			fn: (event) => {
				const phase = getPhase();
				!(phase.type !== "IDLE") && invariant();
				const touch = event.touches[0];
				if (!touch) return;
				if (!(touch.force >= forcePressThreshold)) return;
				const shouldRespect = phase.actions.shouldRespectForcePress();
				if (phase.type === "PENDING") {
					if (shouldRespect) cancel();
					return;
				}
				if (shouldRespect) {
					if (phase.hasMoved) {
						event.preventDefault();
						return;
					}
					cancel();
					return;
				}
				event.preventDefault();
			}
		},
		{
			eventName: supportedEventName,
			fn: cancel
		}
	];
}
function useTouchSensor(api) {
	const phaseRef = (0, import_react.useRef)(idle);
	const unbindEventsRef = (0, import_react.useRef)(noop$2);
	const getPhase = useCallback(function getPhase() {
		return phaseRef.current;
	}, []);
	const setPhase = useCallback(function setPhase(phase) {
		phaseRef.current = phase;
	}, []);
	const startCaptureBinding = useMemo$1(() => ({
		eventName: "touchstart",
		fn: function onTouchStart(event) {
			if (event.defaultPrevented) return;
			const draggableId = api.findClosestDraggableId(event);
			if (!draggableId) return;
			const actions = api.tryGetLock(draggableId, stop, { sourceEvent: event });
			if (!actions) return;
			const { clientX, clientY } = event.touches[0];
			const point = {
				x: clientX,
				y: clientY
			};
			unbindEventsRef.current();
			startPendingDrag(actions, point);
		}
	}), [api]);
	const listenForCapture = useCallback(function listenForCapture() {
		unbindEventsRef.current = bindEvents(window, [startCaptureBinding], {
			capture: true,
			passive: false
		});
	}, [startCaptureBinding]);
	const stop = useCallback(() => {
		const current = phaseRef.current;
		if (current.type === "IDLE") return;
		if (current.type === "PENDING") clearTimeout(current.longPressTimerId);
		setPhase(idle);
		unbindEventsRef.current();
		listenForCapture();
	}, [listenForCapture, setPhase]);
	const cancel = useCallback(() => {
		const phase = phaseRef.current;
		stop();
		if (phase.type === "DRAGGING") phase.actions.cancel({ shouldBlockNextClick: true });
		if (phase.type === "PENDING") phase.actions.abort();
	}, [stop]);
	const bindCapturingEvents = useCallback(function bindCapturingEvents() {
		const options = {
			capture: true,
			passive: false
		};
		const args = {
			cancel,
			completed: stop,
			getPhase
		};
		const unbindTarget = bindEvents(window, getHandleBindings(args), options);
		const unbindWindow = bindEvents(window, getWindowBindings(args), options);
		unbindEventsRef.current = function unbindAll() {
			unbindTarget();
			unbindWindow();
		};
	}, [
		cancel,
		getPhase,
		stop
	]);
	const startDragging = useCallback(function startDragging() {
		const phase = getPhase();
		!(phase.type === "PENDING") && invariant(false, `Cannot start dragging from phase ${phase.type}`);
		const actions = phase.actions.fluidLift(phase.point);
		setPhase({
			type: "DRAGGING",
			actions,
			hasMoved: false
		});
	}, [getPhase, setPhase]);
	const startPendingDrag = useCallback(function startPendingDrag(actions, point) {
		!(getPhase().type === "IDLE") && invariant(false, "Expected to move from IDLE to PENDING drag");
		const longPressTimerId = setTimeout(startDragging, timeForLongPress);
		setPhase({
			type: "PENDING",
			point,
			actions,
			longPressTimerId
		});
		bindCapturingEvents();
	}, [
		bindCapturingEvents,
		getPhase,
		setPhase,
		startDragging
	]);
	useIsomorphicLayoutEffect(function mount() {
		listenForCapture();
		return function unmount() {
			unbindEventsRef.current();
			const phase = getPhase();
			if (phase.type === "PENDING") {
				clearTimeout(phase.longPressTimerId);
				setPhase(idle);
			}
		};
	}, [
		getPhase,
		listenForCapture,
		setPhase
	]);
	useIsomorphicLayoutEffect(function webkitHack() {
		return bindEvents(window, [{
			eventName: "touchmove",
			fn: () => {},
			options: {
				capture: false,
				passive: false
			}
		}]);
	}, []);
}
function useValidateSensorHooks(sensorHooks) {
	useDev(() => {
		const previousRef = usePrevious(sensorHooks);
		useDevSetupWarning(() => {
			!(previousRef.current.length === sensorHooks.length) && invariant(false, "Cannot change the amount of sensor hooks after mounting");
		});
	});
}
var interactiveTagNames = [
	"input",
	"button",
	"textarea",
	"select",
	"option",
	"optgroup",
	"video",
	"audio"
];
function isAnInteractiveElement(parent, current) {
	if (current == null) return false;
	if (interactiveTagNames.includes(current.tagName.toLowerCase())) return true;
	const attribute = current.getAttribute("contenteditable");
	if (attribute === "true" || attribute === "") return true;
	if (current === parent) return false;
	return isAnInteractiveElement(parent, current.parentElement);
}
function isEventInInteractiveElement(draggable, event) {
	const target = event.target;
	if (!isHtmlElement(target)) return false;
	return isAnInteractiveElement(draggable, target);
}
var getBorderBoxCenterPosition = (el) => getRect(el.getBoundingClientRect()).center;
function isElement$1(el) {
	return el instanceof getWindowFromEl(el).Element;
}
var supportedMatchesName = (() => {
	const base = "matches";
	if (typeof document === "undefined") return base;
	return [
		base,
		"msMatchesSelector",
		"webkitMatchesSelector"
	].find((name) => name in Element.prototype) || base;
})();
function closestPonyfill(el, selector) {
	if (el == null) return null;
	if (el[supportedMatchesName](selector)) return el;
	return closestPonyfill(el.parentElement, selector);
}
function closest(el, selector) {
	if (el.closest) return el.closest(selector);
	return closestPonyfill(el, selector);
}
function getSelector(contextId) {
	return `[${dragHandle.contextId}="${contextId}"]`;
}
function findClosestDragHandleFromEvent(contextId, event) {
	const target = event.target;
	if (!isElement$1(target)) {
		warning$1("event.target must be a Element");
		return null;
	}
	const handle = closest(target, getSelector(contextId));
	if (!handle) return null;
	if (!isHtmlElement(handle)) {
		warning$1("drag handle must be a HTMLElement");
		return null;
	}
	return handle;
}
function tryGetClosestDraggableIdFromEvent(contextId, event) {
	const handle = findClosestDragHandleFromEvent(contextId, event);
	if (!handle) return null;
	return handle.getAttribute(dragHandle.draggableId);
}
function findDraggable(contextId, draggableId) {
	const selector = `[${draggable.contextId}="${contextId}"]`;
	const draggable$1 = querySelectorAll(document, selector).find((el) => {
		return el.getAttribute(draggable.id) === draggableId;
	});
	if (!draggable$1) return null;
	if (!isHtmlElement(draggable$1)) {
		warning$1("Draggable element is not a HTMLElement");
		return null;
	}
	return draggable$1;
}
function preventDefault(event) {
	event.preventDefault();
}
function isActive({ expected, phase, isLockActive, shouldWarn }) {
	if (!isLockActive()) {
		if (shouldWarn) warning$1(`
        Cannot perform action.
        The sensor no longer has an action lock.

        Tips:

        - Throw away your action handlers when forceStop() is called
        - Check actions.isActive() if you really need to
      `);
		return false;
	}
	if (expected !== phase) {
		if (shouldWarn) warning$1(`
        Cannot perform action.
        The actions you used belong to an outdated phase

        Current phase: ${expected}
        You called an action from outdated phase: ${phase}

        Tips:

        - Do not use preDragActions actions after calling preDragActions.lift()
      `);
		return false;
	}
	return true;
}
function canStart({ lockAPI, store, registry, draggableId }) {
	if (lockAPI.isClaimed()) return false;
	const entry = registry.draggable.findById(draggableId);
	if (!entry) {
		warning$1(`Unable to find draggable with id: ${draggableId}`);
		return false;
	}
	if (!entry.options.isEnabled) return false;
	if (!canStartDrag(store.getState(), draggableId)) return false;
	return true;
}
function tryStart({ lockAPI, contextId, store, registry, draggableId, forceSensorStop, sourceEvent }) {
	if (!canStart({
		lockAPI,
		store,
		registry,
		draggableId
	})) return null;
	const entry = registry.draggable.getById(draggableId);
	const el = findDraggable(contextId, entry.descriptor.id);
	if (!el) {
		warning$1(`Unable to find draggable element with id: ${draggableId}`);
		return null;
	}
	if (sourceEvent && !entry.options.canDragInteractiveElements && isEventInInteractiveElement(el, sourceEvent)) return null;
	const lock = lockAPI.claim(forceSensorStop || noop$2);
	let phase = "PRE_DRAG";
	function getShouldRespectForcePress() {
		return entry.options.shouldRespectForcePress;
	}
	function isLockActive() {
		return lockAPI.isActive(lock);
	}
	function tryDispatch(expected, getAction) {
		if (isActive({
			expected,
			phase,
			isLockActive,
			shouldWarn: true
		})) store.dispatch(getAction());
	}
	const tryDispatchWhenDragging = tryDispatch.bind(null, "DRAGGING");
	function lift(args) {
		function completed() {
			lockAPI.release();
			phase = "COMPLETED";
		}
		if (phase !== "PRE_DRAG") {
			completed();
			invariant(false, `Cannot lift in phase ${phase}`);
		}
		store.dispatch(lift$1(args.liftActionArgs));
		phase = "DRAGGING";
		function finish(reason, options = { shouldBlockNextClick: false }) {
			args.cleanup();
			if (options.shouldBlockNextClick) {
				const unbind = bindEvents(window, [{
					eventName: "click",
					fn: preventDefault,
					options: {
						once: true,
						passive: false,
						capture: true
					}
				}]);
				setTimeout(unbind);
			}
			completed();
			store.dispatch(drop({ reason }));
		}
		return {
			isActive: () => isActive({
				expected: "DRAGGING",
				phase,
				isLockActive,
				shouldWarn: false
			}),
			shouldRespectForcePress: getShouldRespectForcePress,
			drop: (options) => finish("DROP", options),
			cancel: (options) => finish("CANCEL", options),
			...args.actions
		};
	}
	function fluidLift(clientSelection) {
		const move$1 = rafSchd((client) => {
			tryDispatchWhenDragging(() => move({ client }));
		});
		return {
			...lift({
				liftActionArgs: {
					id: draggableId,
					clientSelection,
					movementMode: "FLUID"
				},
				cleanup: () => move$1.cancel(),
				actions: { move: move$1 }
			}),
			move: move$1
		};
	}
	function snapLift() {
		return lift({
			liftActionArgs: {
				id: draggableId,
				clientSelection: getBorderBoxCenterPosition(el),
				movementMode: "SNAP"
			},
			cleanup: noop$2,
			actions: {
				moveUp: () => tryDispatchWhenDragging(moveUp),
				moveRight: () => tryDispatchWhenDragging(moveRight),
				moveDown: () => tryDispatchWhenDragging(moveDown),
				moveLeft: () => tryDispatchWhenDragging(moveLeft)
			}
		});
	}
	function abortPreDrag() {
		if (isActive({
			expected: "PRE_DRAG",
			phase,
			isLockActive,
			shouldWarn: true
		})) lockAPI.release();
	}
	return {
		isActive: () => isActive({
			expected: "PRE_DRAG",
			phase,
			isLockActive,
			shouldWarn: false
		}),
		shouldRespectForcePress: getShouldRespectForcePress,
		fluidLift,
		snapLift,
		abort: abortPreDrag
	};
}
var defaultSensors = [
	useMouseSensor,
	useKeyboardSensor,
	useTouchSensor
];
function useSensorMarshal({ contextId, store, registry, customSensors, enableDefaultSensors }) {
	const useSensors = [...enableDefaultSensors ? defaultSensors : [], ...customSensors || []];
	const lockAPI = (0, import_react.useState)(() => create())[0];
	const tryAbandonLock = useCallback(function tryAbandonLock(previous, current) {
		if (isDragging(previous) && !isDragging(current)) lockAPI.tryAbandon();
	}, [lockAPI]);
	useIsomorphicLayoutEffect(function listenToStore() {
		let previous = store.getState();
		return store.subscribe(() => {
			const current = store.getState();
			tryAbandonLock(previous, current);
			previous = current;
		});
	}, [
		lockAPI,
		store,
		tryAbandonLock
	]);
	useIsomorphicLayoutEffect(() => {
		return lockAPI.tryAbandon;
	}, [lockAPI.tryAbandon]);
	const canGetLock = useCallback((draggableId) => {
		return canStart({
			lockAPI,
			registry,
			store,
			draggableId
		});
	}, [
		lockAPI,
		registry,
		store
	]);
	const tryGetLock = useCallback((draggableId, forceStop, options) => tryStart({
		lockAPI,
		registry,
		contextId,
		store,
		draggableId,
		forceSensorStop: forceStop || null,
		sourceEvent: options && options.sourceEvent ? options.sourceEvent : null
	}), [
		contextId,
		lockAPI,
		registry,
		store
	]);
	const findClosestDraggableId = useCallback((event) => tryGetClosestDraggableIdFromEvent(contextId, event), [contextId]);
	const findOptionsForDraggable = useCallback((id) => {
		const entry = registry.draggable.findById(id);
		return entry ? entry.options : null;
	}, [registry.draggable]);
	const tryReleaseLock = useCallback(function tryReleaseLock() {
		if (!lockAPI.isClaimed()) return;
		lockAPI.tryAbandon();
		if (store.getState().phase !== "IDLE") store.dispatch(flush());
	}, [lockAPI, store]);
	const isLockClaimed = useCallback(() => lockAPI.isClaimed(), [lockAPI]);
	const api = useMemo$1(() => ({
		canGetLock,
		tryGetLock,
		findClosestDraggableId,
		findOptionsForDraggable,
		tryReleaseLock,
		isLockClaimed
	}), [
		canGetLock,
		tryGetLock,
		findClosestDraggableId,
		findOptionsForDraggable,
		tryReleaseLock,
		isLockClaimed
	]);
	useValidateSensorHooks(useSensors);
	for (let i = 0; i < useSensors.length; i++) useSensors[i](api);
}
var createResponders = (props) => ({
	onBeforeCapture: (t) => {
		const onBeforeCapureCallback = () => {
			if (props.onBeforeCapture) props.onBeforeCapture(t);
		};
		(0, import_react_dom.flushSync)(onBeforeCapureCallback);
	},
	onBeforeDragStart: props.onBeforeDragStart,
	onDragStart: props.onDragStart,
	onDragEnd: props.onDragEnd,
	onDragUpdate: props.onDragUpdate
});
var createAutoScrollerOptions = (props) => ({
	...defaultAutoScrollerOptions,
	...props.autoScrollerOptions,
	durationDampening: {
		...defaultAutoScrollerOptions.durationDampening,
		...props.autoScrollerOptions
	}
});
function getStore(lazyRef) {
	!lazyRef.current && invariant(false, "Could not find store from lazy ref");
	return lazyRef.current;
}
function App(props) {
	const { contextId, setCallbacks, sensors, nonce, dragHandleUsageInstructions } = props;
	const lazyStoreRef = (0, import_react.useRef)(null);
	useStartupValidation();
	const lastPropsRef = usePrevious(props);
	const getResponders = useCallback(() => {
		return createResponders(lastPropsRef.current);
	}, [lastPropsRef]);
	const getAutoScrollerOptions = useCallback(() => {
		return createAutoScrollerOptions(lastPropsRef.current);
	}, [lastPropsRef]);
	const announce = useAnnouncer(contextId);
	const dragHandleUsageInstructionsId = useHiddenTextElement({
		contextId,
		text: dragHandleUsageInstructions
	});
	const styleMarshal = useStyleMarshal(contextId, nonce);
	const lazyDispatch = useCallback((action) => {
		getStore(lazyStoreRef).dispatch(action);
	}, []);
	const marshalCallbacks = useMemo$1(() => bindActionCreators$1({
		publishWhileDragging,
		updateDroppableScroll,
		updateDroppableIsEnabled,
		updateDroppableIsCombineEnabled,
		collectionStarting
	}, lazyDispatch), [lazyDispatch]);
	const registry = useRegistry();
	const dimensionMarshal = useMemo$1(() => {
		return createDimensionMarshal(registry, marshalCallbacks);
	}, [registry, marshalCallbacks]);
	const autoScroller = useMemo$1(() => createAutoScroller({
		scrollWindow,
		scrollDroppable: dimensionMarshal.scrollDroppable,
		getAutoScrollerOptions,
		...bindActionCreators$1({ move }, lazyDispatch)
	}), [
		dimensionMarshal.scrollDroppable,
		lazyDispatch,
		getAutoScrollerOptions
	]);
	const focusMarshal = useFocusMarshal(contextId);
	const store = useMemo$1(() => createStore({
		announce,
		autoScroller,
		dimensionMarshal,
		focusMarshal,
		getResponders,
		styleMarshal
	}), [
		announce,
		autoScroller,
		dimensionMarshal,
		focusMarshal,
		getResponders,
		styleMarshal
	]);
	if (lazyStoreRef.current && lazyStoreRef.current !== store) warning$1("unexpected store change");
	lazyStoreRef.current = store;
	const tryResetStore = useCallback(() => {
		const current = getStore(lazyStoreRef);
		if (current.getState().phase !== "IDLE") current.dispatch(flush());
	}, []);
	const isDragging = useCallback(() => {
		const state = getStore(lazyStoreRef).getState();
		if (state.phase === "DROP_ANIMATING") return true;
		if (state.phase === "IDLE") return false;
		return state.isDragging;
	}, []);
	setCallbacks(useMemo$1(() => ({
		isDragging,
		tryAbort: tryResetStore
	}), [isDragging, tryResetStore]));
	const getCanLift = useCallback((id) => canStartDrag(getStore(lazyStoreRef).getState(), id), []);
	const getIsMovementAllowed = useCallback(() => isMovementAllowed(getStore(lazyStoreRef).getState()), []);
	const appContext = useMemo$1(() => ({
		marshal: dimensionMarshal,
		focus: focusMarshal,
		contextId,
		canLift: getCanLift,
		isMovementAllowed: getIsMovementAllowed,
		dragHandleUsageInstructionsId,
		registry
	}), [
		contextId,
		dimensionMarshal,
		dragHandleUsageInstructionsId,
		focusMarshal,
		getCanLift,
		getIsMovementAllowed,
		registry
	]);
	useSensorMarshal({
		contextId,
		store,
		registry,
		customSensors: sensors || null,
		enableDefaultSensors: props.enableDefaultSensors !== false
	});
	(0, import_react.useEffect)(() => {
		return tryResetStore;
	}, [tryResetStore]);
	return import_react.createElement(AppContext.Provider, { value: appContext }, import_react.createElement(Provider_default, {
		context: StoreContext,
		store
	}, props.children));
}
function useUniqueContextId() {
	return import_react.useId();
}
function DragDropContext(props) {
	const contextId = useUniqueContextId();
	const dragHandleUsageInstructions = props.dragHandleUsageInstructions || preset.dragHandleUsageInstructions;
	return import_react.createElement(ErrorBoundary, null, (setCallbacks) => import_react.createElement(App, {
		nonce: props.nonce,
		contextId,
		setCallbacks,
		dragHandleUsageInstructions,
		enableDefaultSensors: props.enableDefaultSensors,
		sensors: props.sensors,
		onBeforeCapture: props.onBeforeCapture,
		onBeforeDragStart: props.onBeforeDragStart,
		onDragStart: props.onDragStart,
		onDragUpdate: props.onDragUpdate,
		onDragEnd: props.onDragEnd,
		autoScrollerOptions: props.autoScrollerOptions
	}, props.children));
}
var zIndexOptions = {
	dragging: 5e3,
	dropAnimating: 4500
};
var getDraggingTransition = (shouldAnimateDragMovement, dropping) => {
	if (dropping) return transitions.drop(dropping.duration);
	if (shouldAnimateDragMovement) return transitions.snap;
	return transitions.fluid;
};
var getDraggingOpacity = (isCombining, isDropAnimating) => {
	if (!isCombining) return;
	return isDropAnimating ? combine.opacity.drop : combine.opacity.combining;
};
var getShouldDraggingAnimate = (dragging) => {
	if (dragging.forceShouldAnimate != null) return dragging.forceShouldAnimate;
	return dragging.mode === "SNAP";
};
function getDraggingStyle(dragging) {
	const box = dragging.dimension.client;
	const { offset, combineWith, dropping } = dragging;
	const isCombining = Boolean(combineWith);
	const shouldAnimate = getShouldDraggingAnimate(dragging);
	const isDropAnimating = Boolean(dropping);
	const transform = isDropAnimating ? transforms.drop(offset, isCombining) : transforms.moveTo(offset);
	return {
		position: "fixed",
		top: box.marginBox.top,
		left: box.marginBox.left,
		boxSizing: "border-box",
		width: box.borderBox.width,
		height: box.borderBox.height,
		transition: getDraggingTransition(shouldAnimate, dropping),
		transform,
		opacity: getDraggingOpacity(isCombining, isDropAnimating),
		zIndex: isDropAnimating ? zIndexOptions.dropAnimating : zIndexOptions.dragging,
		pointerEvents: "none"
	};
}
function getSecondaryStyle(secondary) {
	return {
		transform: transforms.moveTo(secondary.offset),
		transition: secondary.shouldAnimateDisplacement ? void 0 : "none"
	};
}
function getStyle$1(mapped) {
	return mapped.type === "DRAGGING" ? getDraggingStyle(mapped) : getSecondaryStyle(mapped);
}
function getDimension$1(descriptor, el, windowScroll = origin) {
	const computedStyles = window.getComputedStyle(el);
	const client = calculateBox(el.getBoundingClientRect(), computedStyles);
	const page = withScroll(client, windowScroll);
	return {
		descriptor,
		placeholder: {
			client,
			tagName: el.tagName.toLowerCase(),
			display: computedStyles.display
		},
		displaceBy: {
			x: client.marginBox.width,
			y: client.marginBox.height
		},
		client,
		page
	};
}
function useDraggablePublisher(args) {
	const uniqueId = useUniqueId("draggable");
	const { descriptor, registry, getDraggableRef, canDragInteractiveElements, shouldRespectForcePress, isEnabled } = args;
	const options = useMemo$1(() => ({
		canDragInteractiveElements,
		shouldRespectForcePress,
		isEnabled
	}), [
		canDragInteractiveElements,
		isEnabled,
		shouldRespectForcePress
	]);
	const getDimension = useCallback((windowScroll) => {
		const el = getDraggableRef();
		!el && invariant(false, "Cannot get dimension when no ref is set");
		return getDimension$1(descriptor, el, windowScroll);
	}, [descriptor, getDraggableRef]);
	const entry = useMemo$1(() => ({
		uniqueId,
		descriptor,
		options,
		getDimension
	}), [
		descriptor,
		getDimension,
		options,
		uniqueId
	]);
	const publishedRef = (0, import_react.useRef)(entry);
	const isFirstPublishRef = (0, import_react.useRef)(true);
	useIsomorphicLayoutEffect(() => {
		registry.draggable.register(publishedRef.current);
		return () => registry.draggable.unregister(publishedRef.current);
	}, [registry.draggable]);
	useIsomorphicLayoutEffect(() => {
		if (isFirstPublishRef.current) {
			isFirstPublishRef.current = false;
			return;
		}
		const last = publishedRef.current;
		publishedRef.current = entry;
		registry.draggable.update(entry, last);
	}, [entry, registry.draggable]);
}
var DroppableContext = import_react.createContext(null);
function checkIsValidInnerRef(el) {
	!(el && isHtmlElement(el)) && invariant(false, `
    provided.innerRef has not been provided with a HTMLElement.

    You can find a guide on using the innerRef callback functions at:
    https://github.com/hello-pangea/dnd/blob/main/docs/guides/using-inner-ref.md
  `);
}
function useValidation$1(props, contextId, getRef) {
	useDevSetupWarning(() => {
		function prefix(id) {
			return `Draggable[id: ${id}]: `;
		}
		const id = props.draggableId;
		!id && invariant(false, "Draggable requires a draggableId");
		!(typeof id === "string") && invariant(false, `Draggable requires a [string] draggableId.
      Provided: [type: ${typeof id}] (value: ${id})`);
		!Number.isInteger(props.index) && invariant(false, `${prefix(id)} requires an integer index prop`);
		if (props.mapped.type === "DRAGGING") return;
		checkIsValidInnerRef(getRef());
		if (props.isEnabled) !findDragHandle(contextId, id) && invariant(false, `${prefix(id)} Unable to find drag handle`);
	});
}
function useClonePropValidation(isClone) {
	useDev(() => {
		const initialRef = (0, import_react.useRef)(isClone);
		useDevSetupWarning(() => {
			!(isClone === initialRef.current) && invariant(false, "Draggable isClone prop value changed during component life");
		}, [isClone]);
	});
}
function useRequiredContext(Context) {
	const result = (0, import_react.useContext)(Context);
	!result && invariant(false, "Could not find required context");
	return result;
}
function preventHtml5Dnd(event) {
	event.preventDefault();
}
var Draggable = (props) => {
	const ref = (0, import_react.useRef)(null);
	const setRef = useCallback((el = null) => {
		ref.current = el;
	}, []);
	const getRef = useCallback(() => ref.current, []);
	const { contextId, dragHandleUsageInstructionsId, registry } = useRequiredContext(AppContext);
	const { type, droppableId } = useRequiredContext(DroppableContext);
	const descriptor = useMemo$1(() => ({
		id: props.draggableId,
		index: props.index,
		type,
		droppableId
	}), [
		props.draggableId,
		props.index,
		type,
		droppableId
	]);
	const { children, draggableId, isEnabled, shouldRespectForcePress, canDragInteractiveElements, isClone, mapped, dropAnimationFinished: dropAnimationFinishedAction } = props;
	useValidation$1(props, contextId, getRef);
	useClonePropValidation(isClone);
	if (!isClone) useDraggablePublisher(useMemo$1(() => ({
		descriptor,
		registry,
		getDraggableRef: getRef,
		canDragInteractiveElements,
		shouldRespectForcePress,
		isEnabled
	}), [
		descriptor,
		registry,
		getRef,
		canDragInteractiveElements,
		shouldRespectForcePress,
		isEnabled
	]));
	const dragHandleProps = useMemo$1(() => isEnabled ? {
		tabIndex: 0,
		role: "button",
		"aria-describedby": dragHandleUsageInstructionsId,
		"data-rfd-drag-handle-draggable-id": draggableId,
		"data-rfd-drag-handle-context-id": contextId,
		draggable: false,
		onDragStart: preventHtml5Dnd
	} : null, [
		contextId,
		dragHandleUsageInstructionsId,
		draggableId,
		isEnabled
	]);
	const onMoveEnd = useCallback((event) => {
		if (mapped.type !== "DRAGGING") return;
		if (!mapped.dropping) return;
		if (event.propertyName !== "transform") return;
		(0, import_react_dom.flushSync)(dropAnimationFinishedAction);
	}, [dropAnimationFinishedAction, mapped]);
	const provided = useMemo$1(() => {
		const style = getStyle$1(mapped);
		const onTransitionEnd = mapped.type === "DRAGGING" && mapped.dropping ? onMoveEnd : void 0;
		return {
			innerRef: setRef,
			draggableProps: {
				"data-rfd-draggable-context-id": contextId,
				"data-rfd-draggable-id": draggableId,
				style,
				onTransitionEnd
			},
			dragHandleProps
		};
	}, [
		contextId,
		dragHandleProps,
		draggableId,
		mapped,
		onMoveEnd,
		setRef
	]);
	const rubric = useMemo$1(() => ({
		draggableId: descriptor.id,
		type: descriptor.type,
		source: {
			index: descriptor.index,
			droppableId: descriptor.droppableId
		}
	}), [
		descriptor.droppableId,
		descriptor.id,
		descriptor.index,
		descriptor.type
	]);
	return import_react.createElement(import_react.Fragment, null, children(provided, mapped.snapshot, rubric));
};
var isStrictEqual = (a, b) => a === b;
var whatIsDraggedOverFromResult = (result) => {
	const { combine, destination } = result;
	if (destination) return destination.droppableId;
	if (combine) return combine.droppableId;
	return null;
};
var getCombineWithFromResult = (result) => {
	return result.combine ? result.combine.draggableId : null;
};
var getCombineWithFromImpact = (impact) => {
	return impact.at && impact.at.type === "COMBINE" ? impact.at.combine.draggableId : null;
};
function getDraggableSelector() {
	const memoizedOffset = memoizeOne$1((x, y) => ({
		x,
		y
	}));
	const getMemoizedSnapshot = memoizeOne$1((mode, isClone, draggingOver = null, combineWith = null, dropping = null) => ({
		isDragging: true,
		isClone,
		isDropAnimating: Boolean(dropping),
		dropAnimation: dropping,
		mode,
		draggingOver,
		combineWith,
		combineTargetFor: null
	}));
	const getMemoizedProps = memoizeOne$1((offset, mode, dimension, isClone, draggingOver = null, combineWith = null, forceShouldAnimate = null) => ({ mapped: {
		type: "DRAGGING",
		dropping: null,
		draggingOver,
		combineWith,
		mode,
		offset,
		dimension,
		forceShouldAnimate,
		snapshot: getMemoizedSnapshot(mode, isClone, draggingOver, combineWith, null)
	} }));
	const selector = (state, ownProps) => {
		if (isDragging(state)) {
			if (state.critical.draggable.id !== ownProps.draggableId) return null;
			const offset = state.current.client.offset;
			const dimension = state.dimensions.draggables[ownProps.draggableId];
			const draggingOver = whatIsDraggedOver(state.impact);
			const combineWith = getCombineWithFromImpact(state.impact);
			const forceShouldAnimate = state.forceShouldAnimate;
			return getMemoizedProps(memoizedOffset(offset.x, offset.y), state.movementMode, dimension, ownProps.isClone, draggingOver, combineWith, forceShouldAnimate);
		}
		if (state.phase === "DROP_ANIMATING") {
			const completed = state.completed;
			if (completed.result.draggableId !== ownProps.draggableId) return null;
			const isClone = ownProps.isClone;
			const dimension = state.dimensions.draggables[ownProps.draggableId];
			const result = completed.result;
			const mode = result.mode;
			const draggingOver = whatIsDraggedOverFromResult(result);
			const combineWith = getCombineWithFromResult(result);
			const dropping = {
				duration: state.dropDuration,
				curve: curves.drop,
				moveTo: state.newHomeClientOffset,
				opacity: combineWith ? combine.opacity.drop : null,
				scale: combineWith ? combine.scale.drop : null
			};
			return { mapped: {
				type: "DRAGGING",
				offset: state.newHomeClientOffset,
				dimension,
				dropping,
				draggingOver,
				combineWith,
				mode,
				forceShouldAnimate: null,
				snapshot: getMemoizedSnapshot(mode, isClone, draggingOver, combineWith, dropping)
			} };
		}
		return null;
	};
	return selector;
}
function getSecondarySnapshot(combineTargetFor = null) {
	return {
		isDragging: false,
		isDropAnimating: false,
		isClone: false,
		dropAnimation: null,
		mode: null,
		draggingOver: null,
		combineTargetFor,
		combineWith: null
	};
}
var atRest = { mapped: {
	type: "SECONDARY",
	offset: origin,
	combineTargetFor: null,
	shouldAnimateDisplacement: true,
	snapshot: getSecondarySnapshot(null)
} };
function getSecondarySelector() {
	const memoizedOffset = memoizeOne$1((x, y) => ({
		x,
		y
	}));
	const getMemoizedSnapshot = memoizeOne$1(getSecondarySnapshot);
	const getMemoizedProps = memoizeOne$1((offset, combineTargetFor = null, shouldAnimateDisplacement) => ({ mapped: {
		type: "SECONDARY",
		offset,
		combineTargetFor,
		shouldAnimateDisplacement,
		snapshot: getMemoizedSnapshot(combineTargetFor)
	} }));
	const getFallback = (combineTargetFor) => {
		return combineTargetFor ? getMemoizedProps(origin, combineTargetFor, true) : null;
	};
	const getProps = (ownId, draggingId, impact, afterCritical) => {
		const visualDisplacement = impact.displaced.visible[ownId];
		const isAfterCriticalInVirtualList = Boolean(afterCritical.inVirtualList && afterCritical.effected[ownId]);
		const combine = tryGetCombine(impact);
		const combineTargetFor = combine && combine.draggableId === ownId ? draggingId : null;
		if (!visualDisplacement) {
			if (!isAfterCriticalInVirtualList) return getFallback(combineTargetFor);
			if (impact.displaced.invisible[ownId]) return null;
			const change = negate(afterCritical.displacedBy.point);
			const offset = memoizedOffset(change.x, change.y);
			return getMemoizedProps(offset, combineTargetFor, true);
		}
		if (isAfterCriticalInVirtualList) return getFallback(combineTargetFor);
		const displaceBy = impact.displacedBy.point;
		const offset = memoizedOffset(displaceBy.x, displaceBy.y);
		return getMemoizedProps(offset, combineTargetFor, visualDisplacement.shouldAnimate);
	};
	const selector = (state, ownProps) => {
		if (isDragging(state)) {
			if (state.critical.draggable.id === ownProps.draggableId) return null;
			return getProps(ownProps.draggableId, state.critical.draggable.id, state.impact, state.afterCritical);
		}
		if (state.phase === "DROP_ANIMATING") {
			const completed = state.completed;
			if (completed.result.draggableId === ownProps.draggableId) return null;
			return getProps(ownProps.draggableId, completed.result.draggableId, completed.impact, completed.afterCritical);
		}
		return null;
	};
	return selector;
}
var makeMapStateToProps$1 = () => {
	const draggingSelector = getDraggableSelector();
	const secondarySelector = getSecondarySelector();
	const selector = (state, ownProps) => draggingSelector(state, ownProps) || secondarySelector(state, ownProps) || atRest;
	return selector;
};
var ConnectedDraggable = connect(makeMapStateToProps$1, { dropAnimationFinished }, null, {
	context: StoreContext,
	areStatePropsEqual: isStrictEqual
})(Draggable);
function PrivateDraggable(props) {
	if (useRequiredContext(DroppableContext).isUsingCloneFor === props.draggableId && !props.isClone) return null;
	return import_react.createElement(ConnectedDraggable, props);
}
function PublicDraggable(props) {
	const isEnabled = typeof props.isDragDisabled === "boolean" ? !props.isDragDisabled : true;
	const canDragInteractiveElements = Boolean(props.disableInteractiveElementBlocking);
	const shouldRespectForcePress = Boolean(props.shouldRespectForcePress);
	return import_react.createElement(PrivateDraggable, _extends({}, props, {
		isClone: false,
		isEnabled,
		canDragInteractiveElements,
		shouldRespectForcePress
	}));
}
var isEqual$3 = (base) => (value) => base === value;
var isScroll = isEqual$3("scroll");
var isAuto = isEqual$3("auto");
var isVisible = isEqual$3("visible");
var isEither = (overflow, fn) => fn(overflow.overflowX) || fn(overflow.overflowY);
var isBoth = (overflow, fn) => fn(overflow.overflowX) && fn(overflow.overflowY);
var isElementScrollable = (el) => {
	const style = window.getComputedStyle(el);
	const overflow = {
		overflowX: style.overflowX,
		overflowY: style.overflowY
	};
	return isEither(overflow, isScroll) || isEither(overflow, isAuto);
};
var isBodyScrollable = () => {
	const body = getBodyElement();
	const html = document.documentElement;
	!html && invariant();
	if (!isElementScrollable(body)) return false;
	const htmlStyle = window.getComputedStyle(html);
	if (isBoth({
		overflowX: htmlStyle.overflowX,
		overflowY: htmlStyle.overflowY
	}, isVisible)) return false;
	warning$1(`
    We have detected that your <body> element might be a scroll container.
    We have found no reliable way of detecting whether the <body> element is a scroll container.
    Under most circumstances a <body> scroll bar will be on the <html> element (document.documentElement)

    Because we cannot determine if the <body> is a scroll container, and generally it is not one,
    we will be treating the <body> as *not* a scroll container

    More information: https://github.com/hello-pangea/dnd/blob/main/docs/guides/how-we-detect-scroll-containers.md
  `);
	return false;
};
var getClosestScrollable = (el) => {
	if (el == null) return null;
	if (el === document.body) return isBodyScrollable() ? el : null;
	if (el === document.documentElement) return null;
	if (!isElementScrollable(el)) return getClosestScrollable(el.parentElement);
	return el;
};
var checkForNestedScrollContainers = (scrollable) => {
	if (!scrollable) return;
	if (!getClosestScrollable(scrollable.parentElement)) return;
	warning$1(`
    Droppable: unsupported nested scroll container detected.
    A Droppable can only have one scroll parent (which can be itself)
    Nested scroll containers are currently not supported.

    We hope to support nested scroll containers soon: https://github.com/atlassian/react-beautiful-dnd/issues/131
  `);
};
var getScroll = (el) => ({
	x: el.scrollLeft,
	y: el.scrollTop
});
var getIsFixed = (el) => {
	if (!el) return false;
	if (window.getComputedStyle(el).position === "fixed") return true;
	return getIsFixed(el.parentElement);
};
var getEnv = (start) => {
	return {
		closestScrollable: getClosestScrollable(start),
		isFixedOnPage: getIsFixed(start)
	};
};
var getDroppableDimension = ({ descriptor, isEnabled, isCombineEnabled, isFixedOnPage, direction, client, page, closest }) => {
	const frame = (() => {
		if (!closest) return null;
		const { scrollSize, client: frameClient } = closest;
		const maxScroll = getMaxScroll({
			scrollHeight: scrollSize.scrollHeight,
			scrollWidth: scrollSize.scrollWidth,
			height: frameClient.paddingBox.height,
			width: frameClient.paddingBox.width
		});
		return {
			pageMarginBox: closest.page.marginBox,
			frameClient,
			scrollSize,
			shouldClipSubject: closest.shouldClipSubject,
			scroll: {
				initial: closest.scroll,
				current: closest.scroll,
				max: maxScroll,
				diff: {
					value: origin,
					displacement: origin
				}
			}
		};
	})();
	const axis = direction === "vertical" ? vertical : horizontal;
	return {
		descriptor,
		isCombineEnabled,
		isFixedOnPage,
		axis,
		isEnabled,
		client,
		page,
		frame,
		subject: getSubject({
			page,
			withPlaceholder: null,
			axis,
			frame
		})
	};
};
var getClient = (targetRef, closestScrollable) => {
	const base = getBox(targetRef);
	if (!closestScrollable) return base;
	if (targetRef !== closestScrollable) return base;
	const top = base.paddingBox.top - closestScrollable.scrollTop;
	const left = base.paddingBox.left - closestScrollable.scrollLeft;
	const bottom = top + closestScrollable.scrollHeight;
	return createBox({
		borderBox: expand({
			top,
			right: left + closestScrollable.scrollWidth,
			bottom,
			left
		}, base.border),
		margin: base.margin,
		border: base.border,
		padding: base.padding
	});
};
var getDimension = ({ ref, descriptor, env, windowScroll, direction, isDropDisabled, isCombineEnabled, shouldClipSubject }) => {
	const closestScrollable = env.closestScrollable;
	const client = getClient(ref, closestScrollable);
	const page = withScroll(client, windowScroll);
	const closest = (() => {
		if (!closestScrollable) return null;
		const frameClient = getBox(closestScrollable);
		const scrollSize = {
			scrollHeight: closestScrollable.scrollHeight,
			scrollWidth: closestScrollable.scrollWidth
		};
		return {
			client: frameClient,
			page: withScroll(frameClient, windowScroll),
			scroll: getScroll(closestScrollable),
			scrollSize,
			shouldClipSubject
		};
	})();
	return getDroppableDimension({
		descriptor,
		isEnabled: !isDropDisabled,
		isCombineEnabled,
		isFixedOnPage: env.isFixedOnPage,
		direction,
		client,
		page,
		closest
	});
};
var immediate = { passive: false };
var delayed = { passive: true };
var getListenerOptions = (options) => options.shouldPublishImmediately ? immediate : delayed;
var getClosestScrollableFromDrag = (dragging) => dragging && dragging.env.closestScrollable || null;
function useDroppablePublisher(args) {
	const whileDraggingRef = (0, import_react.useRef)(null);
	const appContext = useRequiredContext(AppContext);
	const uniqueId = useUniqueId("droppable");
	const { registry, marshal } = appContext;
	const previousRef = usePrevious(args);
	const descriptor = useMemo$1(() => ({
		id: args.droppableId,
		type: args.type,
		mode: args.mode
	}), [
		args.droppableId,
		args.mode,
		args.type
	]);
	const publishedDescriptorRef = (0, import_react.useRef)(descriptor);
	const memoizedUpdateScroll = useMemo$1(() => memoizeOne$1((x, y) => {
		!whileDraggingRef.current && invariant(false, "Can only update scroll when dragging");
		const scroll = {
			x,
			y
		};
		marshal.updateDroppableScroll(descriptor.id, scroll);
	}), [descriptor.id, marshal]);
	const getClosestScroll = useCallback(() => {
		const dragging = whileDraggingRef.current;
		if (!dragging || !dragging.env.closestScrollable) return origin;
		return getScroll(dragging.env.closestScrollable);
	}, []);
	const updateScroll = useCallback(() => {
		const scroll = getClosestScroll();
		memoizedUpdateScroll(scroll.x, scroll.y);
	}, [getClosestScroll, memoizedUpdateScroll]);
	const scheduleScrollUpdate = useMemo$1(() => rafSchd(updateScroll), [updateScroll]);
	const onClosestScroll = useCallback(() => {
		const dragging = whileDraggingRef.current;
		const closest = getClosestScrollableFromDrag(dragging);
		!(dragging && closest) && invariant(false, "Could not find scroll options while scrolling");
		if (dragging.scrollOptions.shouldPublishImmediately) {
			updateScroll();
			return;
		}
		scheduleScrollUpdate();
	}, [scheduleScrollUpdate, updateScroll]);
	const getDimensionAndWatchScroll = useCallback((windowScroll, options) => {
		whileDraggingRef.current && invariant(false, "Cannot collect a droppable while a drag is occurring");
		const previous = previousRef.current;
		const ref = previous.getDroppableRef();
		!ref && invariant(false, "Cannot collect without a droppable ref");
		const env = getEnv(ref);
		const dragging = {
			ref,
			descriptor,
			env,
			scrollOptions: options
		};
		whileDraggingRef.current = dragging;
		const dimension = getDimension({
			ref,
			descriptor,
			env,
			windowScroll,
			direction: previous.direction,
			isDropDisabled: previous.isDropDisabled,
			isCombineEnabled: previous.isCombineEnabled,
			shouldClipSubject: !previous.ignoreContainerClipping
		});
		const scrollable = env.closestScrollable;
		if (scrollable) {
			scrollable.setAttribute(scrollContainer.contextId, appContext.contextId);
			scrollable.addEventListener("scroll", onClosestScroll, getListenerOptions(dragging.scrollOptions));
			checkForNestedScrollContainers(scrollable);
		}
		return dimension;
	}, [
		appContext.contextId,
		descriptor,
		onClosestScroll,
		previousRef
	]);
	const getScrollWhileDragging = useCallback(() => {
		const dragging = whileDraggingRef.current;
		const closest = getClosestScrollableFromDrag(dragging);
		!(dragging && closest) && invariant(false, "Can only recollect Droppable client for Droppables that have a scroll container");
		return getScroll(closest);
	}, []);
	const dragStopped = useCallback(() => {
		const dragging = whileDraggingRef.current;
		!dragging && invariant(false, "Cannot stop drag when no active drag");
		const closest = getClosestScrollableFromDrag(dragging);
		whileDraggingRef.current = null;
		if (!closest) return;
		scheduleScrollUpdate.cancel();
		closest.removeAttribute(scrollContainer.contextId);
		closest.removeEventListener("scroll", onClosestScroll, getListenerOptions(dragging.scrollOptions));
	}, [onClosestScroll, scheduleScrollUpdate]);
	const scroll = useCallback((change) => {
		const dragging = whileDraggingRef.current;
		!dragging && invariant(false, "Cannot scroll when there is no drag");
		const closest = getClosestScrollableFromDrag(dragging);
		!closest && invariant(false, "Cannot scroll a droppable with no closest scrollable");
		closest.scrollTop += change.y;
		closest.scrollLeft += change.x;
	}, []);
	const callbacks = useMemo$1(() => {
		return {
			getDimensionAndWatchScroll,
			getScrollWhileDragging,
			dragStopped,
			scroll
		};
	}, [
		dragStopped,
		getDimensionAndWatchScroll,
		getScrollWhileDragging,
		scroll
	]);
	const entry = useMemo$1(() => ({
		uniqueId,
		descriptor,
		callbacks
	}), [
		callbacks,
		descriptor,
		uniqueId
	]);
	useIsomorphicLayoutEffect(() => {
		publishedDescriptorRef.current = entry.descriptor;
		registry.droppable.register(entry);
		return () => {
			if (whileDraggingRef.current) {
				warning$1("Unsupported: changing the droppableId or type of a Droppable during a drag");
				dragStopped();
			}
			registry.droppable.unregister(entry);
		};
	}, [
		callbacks,
		descriptor,
		dragStopped,
		entry,
		marshal,
		registry.droppable
	]);
	useIsomorphicLayoutEffect(() => {
		if (!whileDraggingRef.current) return;
		marshal.updateDroppableIsEnabled(publishedDescriptorRef.current.id, !args.isDropDisabled);
	}, [args.isDropDisabled, marshal]);
	useIsomorphicLayoutEffect(() => {
		if (!whileDraggingRef.current) return;
		marshal.updateDroppableIsCombineEnabled(publishedDescriptorRef.current.id, args.isCombineEnabled);
	}, [args.isCombineEnabled, marshal]);
}
function noop() {}
var empty = {
	width: 0,
	height: 0,
	margin: noSpacing
};
var getSize$1 = ({ isAnimatingOpenOnMount, placeholder, animate }) => {
	if (isAnimatingOpenOnMount) return empty;
	if (animate === "close") return empty;
	return {
		height: placeholder.client.borderBox.height,
		width: placeholder.client.borderBox.width,
		margin: placeholder.client.margin
	};
};
var getStyle$2 = ({ isAnimatingOpenOnMount, placeholder, animate }) => {
	const size = getSize$1({
		isAnimatingOpenOnMount,
		placeholder,
		animate
	});
	return {
		display: placeholder.display,
		boxSizing: "border-box",
		width: size.width,
		height: size.height,
		marginTop: size.margin.top,
		marginRight: size.margin.right,
		marginBottom: size.margin.bottom,
		marginLeft: size.margin.left,
		flexShrink: "0",
		flexGrow: "0",
		pointerEvents: "none",
		transition: animate !== "none" ? transitions.placeholder : null
	};
};
var Placeholder = (props) => {
	const animateOpenTimerRef = (0, import_react.useRef)(null);
	const tryClearAnimateOpenTimer = useCallback(() => {
		if (!animateOpenTimerRef.current) return;
		clearTimeout(animateOpenTimerRef.current);
		animateOpenTimerRef.current = null;
	}, []);
	const { animate, onTransitionEnd, onClose, contextId } = props;
	const [isAnimatingOpenOnMount, setIsAnimatingOpenOnMount] = (0, import_react.useState)(props.animate === "open");
	(0, import_react.useEffect)(() => {
		if (!isAnimatingOpenOnMount) return noop;
		if (animate !== "open") {
			tryClearAnimateOpenTimer();
			setIsAnimatingOpenOnMount(false);
			return noop;
		}
		if (animateOpenTimerRef.current) return noop;
		animateOpenTimerRef.current = setTimeout(() => {
			animateOpenTimerRef.current = null;
			setIsAnimatingOpenOnMount(false);
		});
		return tryClearAnimateOpenTimer;
	}, [
		animate,
		isAnimatingOpenOnMount,
		tryClearAnimateOpenTimer
	]);
	const onSizeChangeEnd = useCallback((event) => {
		if (event.propertyName !== "height") return;
		onTransitionEnd();
		if (animate === "close") onClose();
	}, [
		animate,
		onClose,
		onTransitionEnd
	]);
	const style = getStyle$2({
		isAnimatingOpenOnMount,
		animate: props.animate,
		placeholder: props.placeholder
	});
	return import_react.createElement(props.placeholder.tagName, {
		style,
		"data-rfd-placeholder-context-id": contextId,
		onTransitionEnd: onSizeChangeEnd,
		ref: props.innerRef
	});
};
var Placeholder$1 = import_react.memo(Placeholder);
function isBoolean(value) {
	return typeof value === "boolean";
}
function runChecks(args, checks) {
	checks.forEach((check) => check(args));
}
var shared = [
	function required({ props }) {
		!props.droppableId && invariant(false, "A Droppable requires a droppableId prop");
		!(typeof props.droppableId === "string") && invariant(false, `A Droppable requires a [string] droppableId. Provided: [${typeof props.droppableId}]`);
	},
	function boolean({ props }) {
		!isBoolean(props.isDropDisabled) && invariant(false, "isDropDisabled must be a boolean");
		!isBoolean(props.isCombineEnabled) && invariant(false, "isCombineEnabled must be a boolean");
		!isBoolean(props.ignoreContainerClipping) && invariant(false, "ignoreContainerClipping must be a boolean");
	},
	function ref({ getDroppableRef }) {
		checkIsValidInnerRef(getDroppableRef());
	}
];
var standard = [function placeholder({ props, getPlaceholderRef }) {
	if (!props.placeholder) return;
	if (getPlaceholderRef()) return;
	warning$1(`
      Droppable setup issue [droppableId: "${props.droppableId}"]:
      DroppableProvided > placeholder could not be found.

      Please be sure to add the {provided.placeholder} React Node as a child of your Droppable.
      More information: https://github.com/hello-pangea/dnd/blob/main/docs/api/droppable.md
    `);
}];
var virtual = [function hasClone({ props }) {
	!props.renderClone && invariant(false, "Must provide a clone render function (renderClone) for virtual lists");
}, function hasNoPlaceholder({ getPlaceholderRef }) {
	getPlaceholderRef() && invariant(false, "Expected virtual list to not have a placeholder");
}];
function useValidation(args) {
	useDevSetupWarning(() => {
		runChecks(args, shared);
		if (args.props.mode === "standard") runChecks(args, standard);
		if (args.props.mode === "virtual") runChecks(args, virtual);
	});
}
var AnimateInOut = class extends import_react.PureComponent {
	constructor(...args) {
		super(...args);
		this.state = {
			isVisible: Boolean(this.props.on),
			data: this.props.on,
			animate: this.props.shouldAnimate && this.props.on ? "open" : "none"
		};
		this.onClose = () => {
			if (this.state.animate !== "close") return;
			this.setState({ isVisible: false });
		};
	}
	static getDerivedStateFromProps(props, state) {
		if (!props.shouldAnimate) return {
			isVisible: Boolean(props.on),
			data: props.on,
			animate: "none"
		};
		if (props.on) return {
			isVisible: true,
			data: props.on,
			animate: "open"
		};
		if (state.isVisible) return {
			isVisible: true,
			data: state.data,
			animate: "close"
		};
		return {
			isVisible: false,
			animate: "close",
			data: null
		};
	}
	render() {
		if (!this.state.isVisible) return null;
		const provided = {
			onClose: this.onClose,
			data: this.state.data,
			animate: this.state.animate
		};
		return this.props.children(provided);
	}
};
var Droppable = (props) => {
	const appContext = (0, import_react.useContext)(AppContext);
	!appContext && invariant(false, "Could not find app context");
	const { contextId, isMovementAllowed } = appContext;
	const droppableRef = (0, import_react.useRef)(null);
	const placeholderRef = (0, import_react.useRef)(null);
	const { children, droppableId, type, mode, direction, ignoreContainerClipping, isDropDisabled, isCombineEnabled, snapshot, useClone, updateViewportMaxScroll, getContainerForClone } = props;
	const getDroppableRef = useCallback(() => droppableRef.current, []);
	const setDroppableRef = useCallback((value = null) => {
		droppableRef.current = value;
	}, []);
	const getPlaceholderRef = useCallback(() => placeholderRef.current, []);
	const setPlaceholderRef = useCallback((value = null) => {
		placeholderRef.current = value;
	}, []);
	useValidation({
		props,
		getDroppableRef,
		getPlaceholderRef
	});
	const onPlaceholderTransitionEnd = useCallback(() => {
		if (isMovementAllowed()) updateViewportMaxScroll({ maxScroll: getMaxWindowScroll() });
	}, [isMovementAllowed, updateViewportMaxScroll]);
	useDroppablePublisher({
		droppableId,
		type,
		mode,
		direction,
		isDropDisabled,
		isCombineEnabled,
		ignoreContainerClipping,
		getDroppableRef
	});
	const placeholder = useMemo$1(() => import_react.createElement(AnimateInOut, {
		on: props.placeholder,
		shouldAnimate: props.shouldAnimatePlaceholder
	}, ({ onClose, data, animate }) => import_react.createElement(Placeholder$1, {
		placeholder: data,
		onClose,
		innerRef: setPlaceholderRef,
		animate,
		contextId,
		onTransitionEnd: onPlaceholderTransitionEnd
	})), [
		contextId,
		onPlaceholderTransitionEnd,
		props.placeholder,
		props.shouldAnimatePlaceholder,
		setPlaceholderRef
	]);
	const provided = useMemo$1(() => ({
		innerRef: setDroppableRef,
		placeholder,
		droppableProps: {
			"data-rfd-droppable-id": droppableId,
			"data-rfd-droppable-context-id": contextId
		}
	}), [
		contextId,
		droppableId,
		placeholder,
		setDroppableRef
	]);
	const isUsingCloneFor = useClone ? useClone.dragging.draggableId : null;
	const droppableContext = useMemo$1(() => ({
		droppableId,
		type,
		isUsingCloneFor
	}), [
		droppableId,
		isUsingCloneFor,
		type
	]);
	function getClone() {
		if (!useClone) return null;
		const { dragging, render } = useClone;
		const node = import_react.createElement(PrivateDraggable, {
			draggableId: dragging.draggableId,
			index: dragging.source.index,
			isClone: true,
			isEnabled: true,
			shouldRespectForcePress: false,
			canDragInteractiveElements: true
		}, (draggableProvided, draggableSnapshot) => render(draggableProvided, draggableSnapshot, dragging));
		return import_react_dom.createPortal(node, getContainerForClone());
	}
	return import_react.createElement(DroppableContext.Provider, { value: droppableContext }, children(provided, snapshot), getClone());
};
function getBody() {
	!document.body && invariant(false, "document.body is not ready");
	return document.body;
}
var defaultProps = {
	mode: "standard",
	type: "DEFAULT",
	direction: "vertical",
	isDropDisabled: false,
	isCombineEnabled: false,
	ignoreContainerClipping: false,
	renderClone: null,
	getContainerForClone: getBody
};
var attachDefaultPropsToOwnProps = (ownProps) => {
	let mergedProps = { ...ownProps };
	let defaultPropKey;
	for (defaultPropKey in defaultProps) if (ownProps[defaultPropKey] === void 0) mergedProps = {
		...mergedProps,
		[defaultPropKey]: defaultProps[defaultPropKey]
	};
	return mergedProps;
};
var isMatchingType = (type, critical) => type === critical.droppable.type;
var getDraggable = (critical, dimensions) => dimensions.draggables[critical.draggable.id];
var makeMapStateToProps = () => {
	const idleWithAnimation = {
		placeholder: null,
		shouldAnimatePlaceholder: true,
		snapshot: {
			isDraggingOver: false,
			draggingOverWith: null,
			draggingFromThisWith: null,
			isUsingPlaceholder: false
		},
		useClone: null
	};
	const idleWithoutAnimation = {
		...idleWithAnimation,
		shouldAnimatePlaceholder: false
	};
	const getDraggableRubric = memoizeOne$1((descriptor) => ({
		draggableId: descriptor.id,
		type: descriptor.type,
		source: {
			index: descriptor.index,
			droppableId: descriptor.droppableId
		}
	}));
	const getMapProps = memoizeOne$1((id, isEnabled, isDraggingOverForConsumer, isDraggingOverForImpact, dragging, renderClone) => {
		const draggableId = dragging.descriptor.id;
		if (dragging.descriptor.droppableId === id) {
			const useClone = renderClone ? {
				render: renderClone,
				dragging: getDraggableRubric(dragging.descriptor)
			} : null;
			const snapshot = {
				isDraggingOver: isDraggingOverForConsumer,
				draggingOverWith: isDraggingOverForConsumer ? draggableId : null,
				draggingFromThisWith: draggableId,
				isUsingPlaceholder: true
			};
			return {
				placeholder: dragging.placeholder,
				shouldAnimatePlaceholder: false,
				snapshot,
				useClone
			};
		}
		if (!isEnabled) return idleWithoutAnimation;
		if (!isDraggingOverForImpact) return idleWithAnimation;
		const snapshot = {
			isDraggingOver: isDraggingOverForConsumer,
			draggingOverWith: draggableId,
			draggingFromThisWith: null,
			isUsingPlaceholder: true
		};
		return {
			placeholder: dragging.placeholder,
			shouldAnimatePlaceholder: true,
			snapshot,
			useClone: null
		};
	});
	const selector = (state, ownProps) => {
		const ownPropsWithDefaultProps = attachDefaultPropsToOwnProps(ownProps);
		const id = ownPropsWithDefaultProps.droppableId;
		const type = ownPropsWithDefaultProps.type;
		const isEnabled = !ownPropsWithDefaultProps.isDropDisabled;
		const renderClone = ownPropsWithDefaultProps.renderClone;
		if (isDragging(state)) {
			const critical = state.critical;
			if (!isMatchingType(type, critical)) return idleWithoutAnimation;
			const dragging = getDraggable(critical, state.dimensions);
			const isDraggingOver = whatIsDraggedOver(state.impact) === id;
			return getMapProps(id, isEnabled, isDraggingOver, isDraggingOver, dragging, renderClone);
		}
		if (state.phase === "DROP_ANIMATING") {
			const completed = state.completed;
			if (!isMatchingType(type, completed.critical)) return idleWithoutAnimation;
			const dragging = getDraggable(completed.critical, state.dimensions);
			return getMapProps(id, isEnabled, whatIsDraggedOverFromResult(completed.result) === id, whatIsDraggedOver(completed.impact) === id, dragging, renderClone);
		}
		if (state.phase === "IDLE" && state.completed && !state.shouldFlush) {
			const completed = state.completed;
			if (!isMatchingType(type, completed.critical)) return idleWithoutAnimation;
			const wasOver = whatIsDraggedOver(completed.impact) === id;
			const wasCombining = Boolean(completed.impact.at && completed.impact.at.type === "COMBINE");
			const isHome = completed.critical.droppable.id === id;
			if (wasOver) return wasCombining ? idleWithAnimation : idleWithoutAnimation;
			if (isHome) return idleWithAnimation;
			return idleWithoutAnimation;
		}
		return idleWithoutAnimation;
	};
	return selector;
};
var ConnectedDroppable = connect(makeMapStateToProps, { updateViewportMaxScroll }, (stateProps, dispatchProps, ownProps) => {
	return {
		...attachDefaultPropsToOwnProps(ownProps),
		...stateProps,
		...dispatchProps
	};
}, {
	context: StoreContext,
	areStatePropsEqual: isStrictEqual
})(Droppable);
//#endregion
//#region node_modules/.pnpm/react-virtualized-auto-sizer@1.0.26_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/react-virtualized-auto-sizer/dist/react-virtualized-auto-sizer.esm.js
/**
* Detect Element Resize.
* https://github.com/sdecima/javascript-detect-element-resize
* Sebastian Decima
*
* Forked from version 0.5.3; includes the following modifications:
* 1) Guard against unsafe 'window' and 'document' references (to support SSR).
* 2) Defer initialization code via a top-level function wrapper (to support SSR).
* 3) Avoid unnecessary reflows by not measuring size for scroll events bubbling from children.
* 4) Add nonce for style element.
* 5) Use 'export' statement over 'module.exports' assignment
**/
var windowObject;
if (typeof window !== "undefined") windowObject = window;
else if (typeof self !== "undefined") windowObject = self;
else windowObject = global;
var cancelFrame = null;
var requestFrame = null;
var TIMEOUT_DURATION = 20;
var clearTimeoutFn = windowObject.clearTimeout;
var setTimeoutFn = windowObject.setTimeout;
var cancelAnimationFrameFn = windowObject.cancelAnimationFrame || windowObject.mozCancelAnimationFrame || windowObject.webkitCancelAnimationFrame;
var requestAnimationFrameFn = windowObject.requestAnimationFrame || windowObject.mozRequestAnimationFrame || windowObject.webkitRequestAnimationFrame;
if (cancelAnimationFrameFn == null || requestAnimationFrameFn == null) {
	cancelFrame = clearTimeoutFn;
	requestFrame = function requestAnimationFrameViaSetTimeout(callback) {
		return setTimeoutFn(callback, TIMEOUT_DURATION);
	};
} else {
	cancelFrame = function cancelFrame([animationFrameID, timeoutID]) {
		cancelAnimationFrameFn(animationFrameID);
		clearTimeoutFn(timeoutID);
	};
	requestFrame = function requestAnimationFrameWithSetTimeoutFallback(callback) {
		const animationFrameID = requestAnimationFrameFn(function animationFrameCallback() {
			clearTimeoutFn(timeoutID);
			callback();
		});
		const timeoutID = setTimeoutFn(function timeoutCallback() {
			cancelAnimationFrameFn(animationFrameID);
			callback();
		}, TIMEOUT_DURATION);
		return [animationFrameID, timeoutID];
	};
}
function createDetectElementResize(nonce) {
	let animationKeyframes;
	let animationName;
	let animationStartEvent;
	let animationStyle;
	let checkTriggers;
	let resetTriggers;
	let scrollListener;
	const attachEvent = typeof document !== "undefined" && document.attachEvent;
	if (!attachEvent) {
		resetTriggers = function(element) {
			const triggers = element.__resizeTriggers__, expand = triggers.firstElementChild, contract = triggers.lastElementChild, expandChild = expand.firstElementChild;
			contract.scrollLeft = contract.scrollWidth;
			contract.scrollTop = contract.scrollHeight;
			expandChild.style.width = expand.offsetWidth + 1 + "px";
			expandChild.style.height = expand.offsetHeight + 1 + "px";
			expand.scrollLeft = expand.scrollWidth;
			expand.scrollTop = expand.scrollHeight;
		};
		checkTriggers = function(element) {
			return element.offsetWidth !== element.__resizeLast__.width || element.offsetHeight !== element.__resizeLast__.height;
		};
		scrollListener = function(e) {
			if (e.target.className && typeof e.target.className.indexOf === "function" && e.target.className.indexOf("contract-trigger") < 0 && e.target.className.indexOf("expand-trigger") < 0) return;
			const element = this;
			resetTriggers(this);
			if (this.__resizeRAF__) cancelFrame(this.__resizeRAF__);
			this.__resizeRAF__ = requestFrame(function animationFrame() {
				if (checkTriggers(element)) {
					element.__resizeLast__.width = element.offsetWidth;
					element.__resizeLast__.height = element.offsetHeight;
					element.__resizeListeners__.forEach(function forEachResizeListener(fn) {
						fn.call(element, e);
					});
				}
			});
		};
		let animation = false;
		let keyframeprefix = "";
		animationStartEvent = "animationstart";
		const domPrefixes = "Webkit Moz O ms".split(" ");
		let startEvents = "webkitAnimationStart animationstart oAnimationStart MSAnimationStart".split(" ");
		let pfx = "";
		{
			const elm = document.createElement("fakeelement");
			if (elm.style.animationName !== void 0) animation = true;
			if (animation === false) {
				for (let i = 0; i < domPrefixes.length; i++) if (elm.style[domPrefixes[i] + "AnimationName"] !== void 0) {
					pfx = domPrefixes[i];
					keyframeprefix = "-" + pfx.toLowerCase() + "-";
					animationStartEvent = startEvents[i];
					animation = true;
					break;
				}
			}
		}
		animationName = "resizeanim";
		animationKeyframes = "@" + keyframeprefix + "keyframes " + animationName + " { from { opacity: 0; } to { opacity: 0; } } ";
		animationStyle = keyframeprefix + "animation: 1ms " + animationName + "; ";
	}
	const createStyles = function(doc) {
		if (!doc.getElementById("detectElementResize")) {
			const css = (animationKeyframes ? animationKeyframes : "") + ".resize-triggers { " + (animationStyle ? animationStyle : "") + "visibility: hidden; opacity: 0; } .resize-triggers, .resize-triggers > div, .contract-trigger:before { content: \" \"; display: block; position: absolute; top: 0; left: 0; height: 100%; width: 100%; overflow: hidden; z-index: -1; } .resize-triggers > div { background: #eee; overflow: auto; } .contract-trigger:before { width: 200%; height: 200%; }", head = doc.head || doc.getElementsByTagName("head")[0], style = doc.createElement("style");
			style.id = "detectElementResize";
			style.type = "text/css";
			if (nonce != null) style.setAttribute("nonce", nonce);
			if (style.styleSheet) style.styleSheet.cssText = css;
			else style.appendChild(doc.createTextNode(css));
			head.appendChild(style);
		}
	};
	const addResizeListener = function(element, fn) {
		if (attachEvent) element.attachEvent("onresize", fn);
		else {
			if (!element.__resizeTriggers__) {
				const doc = element.ownerDocument;
				const elementStyle = windowObject.getComputedStyle(element);
				if (elementStyle && elementStyle.position === "static") element.style.position = "relative";
				createStyles(doc);
				element.__resizeLast__ = {};
				element.__resizeListeners__ = [];
				(element.__resizeTriggers__ = doc.createElement("div")).className = "resize-triggers";
				const expandTrigger = doc.createElement("div");
				expandTrigger.className = "expand-trigger";
				expandTrigger.appendChild(doc.createElement("div"));
				const contractTrigger = doc.createElement("div");
				contractTrigger.className = "contract-trigger";
				element.__resizeTriggers__.appendChild(expandTrigger);
				element.__resizeTriggers__.appendChild(contractTrigger);
				element.appendChild(element.__resizeTriggers__);
				resetTriggers(element);
				element.addEventListener("scroll", scrollListener, true);
				if (animationStartEvent) {
					element.__resizeTriggers__.__animationListener__ = function animationListener(e) {
						if (e.animationName === animationName) resetTriggers(element);
					};
					element.__resizeTriggers__.addEventListener(animationStartEvent, element.__resizeTriggers__.__animationListener__);
				}
			}
			element.__resizeListeners__.push(fn);
		}
	};
	const removeResizeListener = function(element, fn) {
		if (attachEvent) element.detachEvent("onresize", fn);
		else {
			element.__resizeListeners__.splice(element.__resizeListeners__.indexOf(fn), 1);
			if (!element.__resizeListeners__.length) {
				element.removeEventListener("scroll", scrollListener, true);
				if (element.__resizeTriggers__.__animationListener__) {
					element.__resizeTriggers__.removeEventListener(animationStartEvent, element.__resizeTriggers__.__animationListener__);
					element.__resizeTriggers__.__animationListener__ = null;
				}
				try {
					element.__resizeTriggers__ = !element.removeChild(element.__resizeTriggers__);
				} catch (e) {}
			}
		}
	};
	return {
		addResizeListener,
		removeResizeListener
	};
}
var AutoSizer = class extends import_react.Component {
	constructor(...args) {
		super(...args);
		this.state = {
			height: this.props.defaultHeight || 0,
			width: this.props.defaultWidth || 0
		};
		this._autoSizer = null;
		this._detectElementResize = null;
		this._didLogDeprecationWarning = false;
		this._parentNode = null;
		this._resizeObserver = null;
		this._timeoutId = null;
		this._onResize = () => {
			this._timeoutId = null;
			const { disableHeight, disableWidth, onResize } = this.props;
			if (this._parentNode) {
				const style = window.getComputedStyle(this._parentNode) || {};
				const paddingLeft = parseFloat(style.paddingLeft || "0");
				const paddingRight = parseFloat(style.paddingRight || "0");
				const paddingTop = parseFloat(style.paddingTop || "0");
				const paddingBottom = parseFloat(style.paddingBottom || "0");
				const rect = this._parentNode.getBoundingClientRect();
				const height = rect.height - paddingTop - paddingBottom;
				const width = rect.width - paddingLeft - paddingRight;
				if (!disableHeight && this.state.height !== height || !disableWidth && this.state.width !== width) {
					this.setState({
						height,
						width
					});
					const maybeLogDeprecationWarning = () => {
						if (!this._didLogDeprecationWarning) {
							this._didLogDeprecationWarning = true;
							console.warn("scaledWidth and scaledHeight parameters have been deprecated; use width and height instead");
						}
					};
					if (typeof onResize === "function") onResize({
						height,
						width,
						get scaledHeight() {
							maybeLogDeprecationWarning();
							return height;
						},
						get scaledWidth() {
							maybeLogDeprecationWarning();
							return width;
						}
					});
				}
			}
		};
		this._setRef = (autoSizer) => {
			this._autoSizer = autoSizer;
		};
	}
	componentDidMount() {
		const { nonce } = this.props;
		const parentNode = this._autoSizer ? this._autoSizer.parentNode : null;
		if (parentNode != null && parentNode.ownerDocument && parentNode.ownerDocument.defaultView && parentNode instanceof parentNode.ownerDocument.defaultView.HTMLElement) {
			this._parentNode = parentNode;
			const ResizeObserverInstance = parentNode.ownerDocument.defaultView.ResizeObserver;
			if (ResizeObserverInstance != null) {
				this._resizeObserver = new ResizeObserverInstance(() => {
					this._timeoutId = setTimeout(this._onResize, 0);
				});
				this._resizeObserver.observe(parentNode);
			} else {
				this._detectElementResize = createDetectElementResize(nonce);
				this._detectElementResize.addResizeListener(parentNode, this._onResize);
			}
			this._onResize();
		}
	}
	componentWillUnmount() {
		if (this._parentNode) {
			if (this._detectElementResize) this._detectElementResize.removeResizeListener(this._parentNode, this._onResize);
			if (this._timeoutId !== null) clearTimeout(this._timeoutId);
			if (this._resizeObserver) this._resizeObserver.disconnect();
		}
	}
	render() {
		const { children, defaultHeight, defaultWidth, disableHeight = false, disableWidth = false, doNotBailOutOnEmptyChildren = false, nonce, onResize, style = {}, tagName = "div", ...rest } = this.props;
		const { height, width } = this.state;
		const outerStyle = { overflow: "visible" };
		const childParams = {};
		let bailoutOnChildren = false;
		if (!disableHeight) {
			if (height === 0) bailoutOnChildren = true;
			outerStyle.height = 0;
			childParams.height = height;
			childParams.scaledHeight = height;
		}
		if (!disableWidth) {
			if (width === 0) bailoutOnChildren = true;
			outerStyle.width = 0;
			childParams.width = width;
			childParams.scaledWidth = width;
		}
		if (doNotBailOutOnEmptyChildren) bailoutOnChildren = false;
		return (0, import_react.createElement)(tagName, {
			ref: this._setRef,
			style: {
				...outerStyle,
				...style
			},
			...rest
		}, !bailoutOnChildren && children(childParams));
	}
};
//#endregion
//#region node_modules/.pnpm/memoize-one@5.2.1/node_modules/memoize-one/dist/memoize-one.esm.js
var safeIsNaN = Number.isNaN || function ponyfill(value) {
	return typeof value === "number" && value !== value;
};
function isEqual$2(first, second) {
	if (first === second) return true;
	if (safeIsNaN(first) && safeIsNaN(second)) return true;
	return false;
}
function areInputsEqual(newInputs, lastInputs) {
	if (newInputs.length !== lastInputs.length) return false;
	for (var i = 0; i < newInputs.length; i++) if (!isEqual$2(newInputs[i], lastInputs[i])) return false;
	return true;
}
function memoizeOne(resultFn, isEqual) {
	if (isEqual === void 0) isEqual = areInputsEqual;
	var lastThis;
	var lastArgs = [];
	var lastResult;
	var calledOnce = false;
	function memoized() {
		var newArgs = [];
		for (var _i = 0; _i < arguments.length; _i++) newArgs[_i] = arguments[_i];
		if (calledOnce && lastThis === this && isEqual(newArgs, lastArgs)) return lastResult;
		lastResult = resultFn.apply(this, newArgs);
		calledOnce = true;
		lastThis = this;
		lastArgs = newArgs;
		return lastResult;
	}
	return memoized;
}
//#endregion
//#region node_modules/.pnpm/react-window@1.8.11_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/react-window/dist/index.esm.js
var now = typeof performance === "object" && typeof performance.now === "function" ? function() {
	return performance.now();
} : function() {
	return Date.now();
};
function cancelTimeout(timeoutID) {
	cancelAnimationFrame(timeoutID.id);
}
function requestTimeout(callback, delay) {
	var start = now();
	function tick() {
		if (now() - start >= delay) callback.call(null);
		else timeoutID.id = requestAnimationFrame(tick);
	}
	var timeoutID = { id: requestAnimationFrame(tick) };
	return timeoutID;
}
var size = -1;
function getScrollbarSize(recalculate) {
	if (recalculate === void 0) recalculate = false;
	if (size === -1 || recalculate) {
		var div = document.createElement("div");
		var style = div.style;
		style.width = "50px";
		style.height = "50px";
		style.overflow = "scroll";
		document.body.appendChild(div);
		size = div.offsetWidth - div.clientWidth;
		document.body.removeChild(div);
	}
	return size;
}
var cachedRTLResult = null;
function getRTLOffsetType(recalculate) {
	if (recalculate === void 0) recalculate = false;
	if (cachedRTLResult === null || recalculate) {
		var outerDiv = document.createElement("div");
		var outerStyle = outerDiv.style;
		outerStyle.width = "50px";
		outerStyle.height = "50px";
		outerStyle.overflow = "scroll";
		outerStyle.direction = "rtl";
		var innerDiv = document.createElement("div");
		var innerStyle = innerDiv.style;
		innerStyle.width = "100px";
		innerStyle.height = "100px";
		outerDiv.appendChild(innerDiv);
		document.body.appendChild(outerDiv);
		if (outerDiv.scrollLeft > 0) cachedRTLResult = "positive-descending";
		else {
			outerDiv.scrollLeft = 1;
			if (outerDiv.scrollLeft === 0) cachedRTLResult = "negative";
			else cachedRTLResult = "positive-ascending";
		}
		document.body.removeChild(outerDiv);
		return cachedRTLResult;
	}
	return cachedRTLResult;
}
if (typeof window !== "undefined" && typeof window.WeakSet !== "undefined") {}
var IS_SCROLLING_DEBOUNCE_INTERVAL$1 = 150;
var defaultItemKey$1 = function defaultItemKey(index, data) {
	return index;
};
var devWarningsDirection = null;
var devWarningsTagName$1 = null;
if (typeof window !== "undefined" && typeof window.WeakSet !== "undefined") {
	devWarningsDirection = /*#__PURE__*/ new WeakSet();
	devWarningsTagName$1 = /*#__PURE__*/ new WeakSet();
}
function createListComponent(_ref) {
	var _class;
	var getItemOffset = _ref.getItemOffset, getEstimatedTotalSize = _ref.getEstimatedTotalSize, getItemSize = _ref.getItemSize, getOffsetForIndexAndAlignment = _ref.getOffsetForIndexAndAlignment, getStartIndexForOffset = _ref.getStartIndexForOffset, getStopIndexForStartIndex = _ref.getStopIndexForStartIndex, initInstanceProps = _ref.initInstanceProps, shouldResetStyleCacheOnItemSizeChange = _ref.shouldResetStyleCacheOnItemSizeChange, validateProps = _ref.validateProps;
	return _class = /*#__PURE__*/ function(_PureComponent) {
		_inheritsLoose(List, _PureComponent);
		function List(props) {
			var _this = _PureComponent.call(this, props) || this;
			_this._instanceProps = initInstanceProps(_this.props, _assertThisInitialized(_this));
			_this._outerRef = void 0;
			_this._resetIsScrollingTimeoutId = null;
			_this.state = {
				instance: _assertThisInitialized(_this),
				isScrolling: false,
				scrollDirection: "forward",
				scrollOffset: typeof _this.props.initialScrollOffset === "number" ? _this.props.initialScrollOffset : 0,
				scrollUpdateWasRequested: false
			};
			_this._callOnItemsRendered = void 0;
			_this._callOnItemsRendered = memoizeOne(function(overscanStartIndex, overscanStopIndex, visibleStartIndex, visibleStopIndex) {
				return _this.props.onItemsRendered({
					overscanStartIndex,
					overscanStopIndex,
					visibleStartIndex,
					visibleStopIndex
				});
			});
			_this._callOnScroll = void 0;
			_this._callOnScroll = memoizeOne(function(scrollDirection, scrollOffset, scrollUpdateWasRequested) {
				return _this.props.onScroll({
					scrollDirection,
					scrollOffset,
					scrollUpdateWasRequested
				});
			});
			_this._getItemStyle = void 0;
			_this._getItemStyle = function(index) {
				var _this$props = _this.props, direction = _this$props.direction, itemSize = _this$props.itemSize, layout = _this$props.layout;
				var itemStyleCache = _this._getItemStyleCache(shouldResetStyleCacheOnItemSizeChange && itemSize, shouldResetStyleCacheOnItemSizeChange && layout, shouldResetStyleCacheOnItemSizeChange && direction);
				var style;
				if (itemStyleCache.hasOwnProperty(index)) style = itemStyleCache[index];
				else {
					var _offset = getItemOffset(_this.props, index, _this._instanceProps);
					var size = getItemSize(_this.props, index, _this._instanceProps);
					var isHorizontal = direction === "horizontal" || layout === "horizontal";
					var isRtl = direction === "rtl";
					var offsetHorizontal = isHorizontal ? _offset : 0;
					itemStyleCache[index] = style = {
						position: "absolute",
						left: isRtl ? void 0 : offsetHorizontal,
						right: isRtl ? offsetHorizontal : void 0,
						top: !isHorizontal ? _offset : 0,
						height: !isHorizontal ? size : "100%",
						width: isHorizontal ? size : "100%"
					};
				}
				return style;
			};
			_this._getItemStyleCache = void 0;
			_this._getItemStyleCache = memoizeOne(function(_, __, ___) {
				return {};
			});
			_this._onScrollHorizontal = function(event) {
				var _event$currentTarget = event.currentTarget, clientWidth = _event$currentTarget.clientWidth, scrollLeft = _event$currentTarget.scrollLeft, scrollWidth = _event$currentTarget.scrollWidth;
				_this.setState(function(prevState) {
					if (prevState.scrollOffset === scrollLeft) return null;
					var direction = _this.props.direction;
					var scrollOffset = scrollLeft;
					if (direction === "rtl") switch (getRTLOffsetType()) {
						case "negative":
							scrollOffset = -scrollLeft;
							break;
						case "positive-descending":
							scrollOffset = scrollWidth - clientWidth - scrollLeft;
							break;
					}
					scrollOffset = Math.max(0, Math.min(scrollOffset, scrollWidth - clientWidth));
					return {
						isScrolling: true,
						scrollDirection: prevState.scrollOffset < scrollOffset ? "forward" : "backward",
						scrollOffset,
						scrollUpdateWasRequested: false
					};
				}, _this._resetIsScrollingDebounced);
			};
			_this._onScrollVertical = function(event) {
				var _event$currentTarget2 = event.currentTarget, clientHeight = _event$currentTarget2.clientHeight, scrollHeight = _event$currentTarget2.scrollHeight, scrollTop = _event$currentTarget2.scrollTop;
				_this.setState(function(prevState) {
					if (prevState.scrollOffset === scrollTop) return null;
					var scrollOffset = Math.max(0, Math.min(scrollTop, scrollHeight - clientHeight));
					return {
						isScrolling: true,
						scrollDirection: prevState.scrollOffset < scrollOffset ? "forward" : "backward",
						scrollOffset,
						scrollUpdateWasRequested: false
					};
				}, _this._resetIsScrollingDebounced);
			};
			_this._outerRefSetter = function(ref) {
				var outerRef = _this.props.outerRef;
				_this._outerRef = ref;
				if (typeof outerRef === "function") outerRef(ref);
				else if (outerRef != null && typeof outerRef === "object" && outerRef.hasOwnProperty("current")) outerRef.current = ref;
			};
			_this._resetIsScrollingDebounced = function() {
				if (_this._resetIsScrollingTimeoutId !== null) cancelTimeout(_this._resetIsScrollingTimeoutId);
				_this._resetIsScrollingTimeoutId = requestTimeout(_this._resetIsScrolling, IS_SCROLLING_DEBOUNCE_INTERVAL$1);
			};
			_this._resetIsScrolling = function() {
				_this._resetIsScrollingTimeoutId = null;
				_this.setState({ isScrolling: false }, function() {
					_this._getItemStyleCache(-1, null);
				});
			};
			return _this;
		}
		List.getDerivedStateFromProps = function getDerivedStateFromProps(nextProps, prevState) {
			validateSharedProps$1(nextProps, prevState);
			validateProps(nextProps);
			return null;
		};
		var _proto = List.prototype;
		_proto.scrollTo = function scrollTo(scrollOffset) {
			scrollOffset = Math.max(0, scrollOffset);
			this.setState(function(prevState) {
				if (prevState.scrollOffset === scrollOffset) return null;
				return {
					scrollDirection: prevState.scrollOffset < scrollOffset ? "forward" : "backward",
					scrollOffset,
					scrollUpdateWasRequested: true
				};
			}, this._resetIsScrollingDebounced);
		};
		_proto.scrollToItem = function scrollToItem(index, align) {
			if (align === void 0) align = "auto";
			var _this$props2 = this.props, itemCount = _this$props2.itemCount, layout = _this$props2.layout;
			var scrollOffset = this.state.scrollOffset;
			index = Math.max(0, Math.min(index, itemCount - 1));
			var scrollbarSize = 0;
			if (this._outerRef) {
				var outerRef = this._outerRef;
				if (layout === "vertical") scrollbarSize = outerRef.scrollWidth > outerRef.clientWidth ? getScrollbarSize() : 0;
				else scrollbarSize = outerRef.scrollHeight > outerRef.clientHeight ? getScrollbarSize() : 0;
			}
			this.scrollTo(getOffsetForIndexAndAlignment(this.props, index, align, scrollOffset, this._instanceProps, scrollbarSize));
		};
		_proto.componentDidMount = function componentDidMount() {
			var _this$props3 = this.props, direction = _this$props3.direction, initialScrollOffset = _this$props3.initialScrollOffset, layout = _this$props3.layout;
			if (typeof initialScrollOffset === "number" && this._outerRef != null) {
				var outerRef = this._outerRef;
				if (direction === "horizontal" || layout === "horizontal") outerRef.scrollLeft = initialScrollOffset;
				else outerRef.scrollTop = initialScrollOffset;
			}
			this._callPropsCallbacks();
		};
		_proto.componentDidUpdate = function componentDidUpdate() {
			var _this$props4 = this.props, direction = _this$props4.direction, layout = _this$props4.layout;
			var _this$state = this.state, scrollOffset = _this$state.scrollOffset;
			if (_this$state.scrollUpdateWasRequested && this._outerRef != null) {
				var outerRef = this._outerRef;
				if (direction === "horizontal" || layout === "horizontal") if (direction === "rtl") switch (getRTLOffsetType()) {
					case "negative":
						outerRef.scrollLeft = -scrollOffset;
						break;
					case "positive-ascending":
						outerRef.scrollLeft = scrollOffset;
						break;
					default:
						var clientWidth = outerRef.clientWidth;
						outerRef.scrollLeft = outerRef.scrollWidth - clientWidth - scrollOffset;
						break;
				}
				else outerRef.scrollLeft = scrollOffset;
				else outerRef.scrollTop = scrollOffset;
			}
			this._callPropsCallbacks();
		};
		_proto.componentWillUnmount = function componentWillUnmount() {
			if (this._resetIsScrollingTimeoutId !== null) cancelTimeout(this._resetIsScrollingTimeoutId);
		};
		_proto.render = function render() {
			var _this$props5 = this.props, children = _this$props5.children, className = _this$props5.className, direction = _this$props5.direction, height = _this$props5.height, innerRef = _this$props5.innerRef, innerElementType = _this$props5.innerElementType, innerTagName = _this$props5.innerTagName, itemCount = _this$props5.itemCount, itemData = _this$props5.itemData, _this$props5$itemKey = _this$props5.itemKey, itemKey = _this$props5$itemKey === void 0 ? defaultItemKey$1 : _this$props5$itemKey, layout = _this$props5.layout, outerElementType = _this$props5.outerElementType, outerTagName = _this$props5.outerTagName, style = _this$props5.style, useIsScrolling = _this$props5.useIsScrolling, width = _this$props5.width;
			var isScrolling = this.state.isScrolling;
			var isHorizontal = direction === "horizontal" || layout === "horizontal";
			var onScroll = isHorizontal ? this._onScrollHorizontal : this._onScrollVertical;
			var _this$_getRangeToRend = this._getRangeToRender(), startIndex = _this$_getRangeToRend[0], stopIndex = _this$_getRangeToRend[1];
			var items = [];
			if (itemCount > 0) for (var _index = startIndex; _index <= stopIndex; _index++) items.push((0, import_react.createElement)(children, {
				data: itemData,
				key: itemKey(_index, itemData),
				index: _index,
				isScrolling: useIsScrolling ? isScrolling : void 0,
				style: this._getItemStyle(_index)
			}));
			var estimatedTotalSize = getEstimatedTotalSize(this.props, this._instanceProps);
			return (0, import_react.createElement)(outerElementType || outerTagName || "div", {
				className,
				onScroll,
				ref: this._outerRefSetter,
				style: _extends({
					position: "relative",
					height,
					width,
					overflow: "auto",
					WebkitOverflowScrolling: "touch",
					willChange: "transform",
					direction
				}, style)
			}, (0, import_react.createElement)(innerElementType || innerTagName || "div", {
				children: items,
				ref: innerRef,
				style: {
					height: isHorizontal ? "100%" : estimatedTotalSize,
					pointerEvents: isScrolling ? "none" : void 0,
					width: isHorizontal ? estimatedTotalSize : "100%"
				}
			}));
		};
		_proto._callPropsCallbacks = function _callPropsCallbacks() {
			if (typeof this.props.onItemsRendered === "function") {
				if (this.props.itemCount > 0) {
					var _this$_getRangeToRend2 = this._getRangeToRender(), _overscanStartIndex = _this$_getRangeToRend2[0], _overscanStopIndex = _this$_getRangeToRend2[1], _visibleStartIndex = _this$_getRangeToRend2[2], _visibleStopIndex = _this$_getRangeToRend2[3];
					this._callOnItemsRendered(_overscanStartIndex, _overscanStopIndex, _visibleStartIndex, _visibleStopIndex);
				}
			}
			if (typeof this.props.onScroll === "function") {
				var _this$state2 = this.state, _scrollDirection = _this$state2.scrollDirection, _scrollOffset = _this$state2.scrollOffset, _scrollUpdateWasRequested = _this$state2.scrollUpdateWasRequested;
				this._callOnScroll(_scrollDirection, _scrollOffset, _scrollUpdateWasRequested);
			}
		};
		_proto._getRangeToRender = function _getRangeToRender() {
			var _this$props6 = this.props, itemCount = _this$props6.itemCount, overscanCount = _this$props6.overscanCount;
			var _this$state3 = this.state, isScrolling = _this$state3.isScrolling, scrollDirection = _this$state3.scrollDirection, scrollOffset = _this$state3.scrollOffset;
			if (itemCount === 0) return [
				0,
				0,
				0,
				0
			];
			var startIndex = getStartIndexForOffset(this.props, scrollOffset, this._instanceProps);
			var stopIndex = getStopIndexForStartIndex(this.props, startIndex, scrollOffset, this._instanceProps);
			var overscanBackward = !isScrolling || scrollDirection === "backward" ? Math.max(1, overscanCount) : 1;
			var overscanForward = !isScrolling || scrollDirection === "forward" ? Math.max(1, overscanCount) : 1;
			return [
				Math.max(0, startIndex - overscanBackward),
				Math.max(0, Math.min(itemCount - 1, stopIndex + overscanForward)),
				startIndex,
				stopIndex
			];
		};
		return List;
	}(import_react.PureComponent), _class.defaultProps = {
		direction: "ltr",
		itemData: void 0,
		layout: "vertical",
		overscanCount: 2,
		useIsScrolling: false
	}, _class;
}
var validateSharedProps$1 = function validateSharedProps(_ref2, _ref3) {
	var children = _ref2.children, direction = _ref2.direction, height = _ref2.height, layout = _ref2.layout, innerTagName = _ref2.innerTagName, outerTagName = _ref2.outerTagName, width = _ref2.width;
	var instance = _ref3.instance;
	if (innerTagName != null || outerTagName != null) {
		if (devWarningsTagName$1 && !devWarningsTagName$1.has(instance)) {
			devWarningsTagName$1.add(instance);
			console.warn("The innerTagName and outerTagName props have been deprecated. Please use the innerElementType and outerElementType props instead.");
		}
	}
	var isHorizontal = direction === "horizontal" || layout === "horizontal";
	switch (direction) {
		case "horizontal":
		case "vertical":
			if (devWarningsDirection && !devWarningsDirection.has(instance)) {
				devWarningsDirection.add(instance);
				console.warn("The direction prop should be either \"ltr\" (default) or \"rtl\". Please use the layout prop to specify \"vertical\" (default) or \"horizontal\" orientation.");
			}
			break;
		case "ltr":
		case "rtl": break;
		default: throw Error("An invalid \"direction\" prop has been specified. Value should be either \"ltr\" or \"rtl\". " + ("\"" + direction + "\" was specified."));
	}
	switch (layout) {
		case "horizontal":
		case "vertical": break;
		default: throw Error("An invalid \"layout\" prop has been specified. Value should be either \"horizontal\" or \"vertical\". " + ("\"" + layout + "\" was specified."));
	}
	if (children == null) throw Error("An invalid \"children\" prop has been specified. Value should be a React component. " + ("\"" + (children === null ? "null" : typeof children) + "\" was specified."));
	if (isHorizontal && typeof width !== "number") throw Error("An invalid \"width\" prop has been specified. Horizontal lists must specify a number for width. " + ("\"" + (width === null ? "null" : typeof width) + "\" was specified."));
	else if (!isHorizontal && typeof height !== "number") throw Error("An invalid \"height\" prop has been specified. Vertical lists must specify a number for height. " + ("\"" + (height === null ? "null" : typeof height) + "\" was specified."));
};
var DEFAULT_ESTIMATED_ITEM_SIZE$1 = 50;
var getItemMetadata$1 = function getItemMetadata(props, index, instanceProps) {
	var itemSize = props.itemSize;
	var itemMetadataMap = instanceProps.itemMetadataMap, lastMeasuredIndex = instanceProps.lastMeasuredIndex;
	if (index > lastMeasuredIndex) {
		var offset = 0;
		if (lastMeasuredIndex >= 0) {
			var itemMetadata = itemMetadataMap[lastMeasuredIndex];
			offset = itemMetadata.offset + itemMetadata.size;
		}
		for (var i = lastMeasuredIndex + 1; i <= index; i++) {
			var size = itemSize(i);
			itemMetadataMap[i] = {
				offset,
				size
			};
			offset += size;
		}
		instanceProps.lastMeasuredIndex = index;
	}
	return itemMetadataMap[index];
};
var findNearestItem$1 = function findNearestItem(props, instanceProps, offset) {
	var itemMetadataMap = instanceProps.itemMetadataMap, lastMeasuredIndex = instanceProps.lastMeasuredIndex;
	if ((lastMeasuredIndex > 0 ? itemMetadataMap[lastMeasuredIndex].offset : 0) >= offset) return findNearestItemBinarySearch$1(props, instanceProps, lastMeasuredIndex, 0, offset);
	else return findNearestItemExponentialSearch$1(props, instanceProps, Math.max(0, lastMeasuredIndex), offset);
};
var findNearestItemBinarySearch$1 = function findNearestItemBinarySearch(props, instanceProps, high, low, offset) {
	while (low <= high) {
		var middle = low + Math.floor((high - low) / 2);
		var currentOffset = getItemMetadata$1(props, middle, instanceProps).offset;
		if (currentOffset === offset) return middle;
		else if (currentOffset < offset) low = middle + 1;
		else if (currentOffset > offset) high = middle - 1;
	}
	if (low > 0) return low - 1;
	else return 0;
};
var findNearestItemExponentialSearch$1 = function findNearestItemExponentialSearch(props, instanceProps, index, offset) {
	var itemCount = props.itemCount;
	var interval = 1;
	while (index < itemCount && getItemMetadata$1(props, index, instanceProps).offset < offset) {
		index += interval;
		interval *= 2;
	}
	return findNearestItemBinarySearch$1(props, instanceProps, Math.min(index, itemCount - 1), Math.floor(index / 2), offset);
};
var getEstimatedTotalSize = function getEstimatedTotalSize(_ref2, _ref3) {
	var itemCount = _ref2.itemCount;
	var itemMetadataMap = _ref3.itemMetadataMap, estimatedItemSize = _ref3.estimatedItemSize, lastMeasuredIndex = _ref3.lastMeasuredIndex;
	var totalSizeOfMeasuredItems = 0;
	if (lastMeasuredIndex >= itemCount) lastMeasuredIndex = itemCount - 1;
	if (lastMeasuredIndex >= 0) {
		var itemMetadata = itemMetadataMap[lastMeasuredIndex];
		totalSizeOfMeasuredItems = itemMetadata.offset + itemMetadata.size;
	}
	var totalSizeOfUnmeasuredItems = (itemCount - lastMeasuredIndex - 1) * estimatedItemSize;
	return totalSizeOfMeasuredItems + totalSizeOfUnmeasuredItems;
};
var VariableSizeList = /*#__PURE__*/ createListComponent({
	getItemOffset: function getItemOffset(props, index, instanceProps) {
		return getItemMetadata$1(props, index, instanceProps).offset;
	},
	getItemSize: function getItemSize(props, index, instanceProps) {
		return instanceProps.itemMetadataMap[index].size;
	},
	getEstimatedTotalSize,
	getOffsetForIndexAndAlignment: function getOffsetForIndexAndAlignment(props, index, align, scrollOffset, instanceProps, scrollbarSize) {
		var direction = props.direction, height = props.height, layout = props.layout, width = props.width;
		var size = direction === "horizontal" || layout === "horizontal" ? width : height;
		var itemMetadata = getItemMetadata$1(props, index, instanceProps);
		var estimatedTotalSize = getEstimatedTotalSize(props, instanceProps);
		var maxOffset = Math.max(0, Math.min(estimatedTotalSize - size, itemMetadata.offset));
		var minOffset = Math.max(0, itemMetadata.offset - size + itemMetadata.size + scrollbarSize);
		if (align === "smart") if (scrollOffset >= minOffset - size && scrollOffset <= maxOffset + size) align = "auto";
		else align = "center";
		switch (align) {
			case "start": return maxOffset;
			case "end": return minOffset;
			case "center": return Math.round(minOffset + (maxOffset - minOffset) / 2);
			default: if (scrollOffset >= minOffset && scrollOffset <= maxOffset) return scrollOffset;
			else if (scrollOffset < minOffset) return minOffset;
			else return maxOffset;
		}
	},
	getStartIndexForOffset: function getStartIndexForOffset(props, offset, instanceProps) {
		return findNearestItem$1(props, instanceProps, offset);
	},
	getStopIndexForStartIndex: function getStopIndexForStartIndex(props, startIndex, scrollOffset, instanceProps) {
		var direction = props.direction, height = props.height, itemCount = props.itemCount, layout = props.layout, width = props.width;
		var size = direction === "horizontal" || layout === "horizontal" ? width : height;
		var itemMetadata = getItemMetadata$1(props, startIndex, instanceProps);
		var maxOffset = scrollOffset + size;
		var offset = itemMetadata.offset + itemMetadata.size;
		var stopIndex = startIndex;
		while (stopIndex < itemCount - 1 && offset < maxOffset) {
			stopIndex++;
			offset += getItemMetadata$1(props, stopIndex, instanceProps).size;
		}
		return stopIndex;
	},
	initInstanceProps: function initInstanceProps(props, instance) {
		var instanceProps = {
			itemMetadataMap: {},
			estimatedItemSize: props.estimatedItemSize || DEFAULT_ESTIMATED_ITEM_SIZE$1,
			lastMeasuredIndex: -1
		};
		instance.resetAfterIndex = function(index, shouldForceUpdate) {
			if (shouldForceUpdate === void 0) shouldForceUpdate = true;
			instanceProps.lastMeasuredIndex = Math.min(instanceProps.lastMeasuredIndex, index - 1);
			instance._getItemStyleCache(-1);
			if (shouldForceUpdate) instance.forceUpdate();
		};
		return instanceProps;
	},
	shouldResetStyleCacheOnItemSizeChange: false,
	validateProps: function validateProps(_ref5) {
		var itemSize = _ref5.itemSize;
		if (typeof itemSize !== "function") throw Error("An invalid \"itemSize\" prop has been specified. Value should be a function. " + ("\"" + (itemSize === null ? "null" : typeof itemSize) + "\" was specified."));
	}
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/common/ClearButton/i18n/index.js
var i18n_default$10 = addComponentKeysets({
	en: { "label_clear-button": "Clear" },
	ru: { "label_clear-button": "Очистить" }
}, `g-clear-button`);
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/common/ClearButton/ClearButton.js
var b$65 = block("clear-button");
var ICON_SIZE = 16;
var mapTextInputSizeToButtonSize = (textInputSize) => {
	switch (textInputSize) {
		case "s": return "xs";
		case "m": return "s";
		case "l": return "m";
		case "xl": return "l";
		default: throw new Error(`Unknown text input size "${textInputSize}"`);
	}
};
var ClearButton = (props) => {
	const { size, className, onClick } = props;
	/**
	* Turn off event onBlur on input when use clear button
	*/
	const preventDefaultHandler = (event) => {
		event.preventDefault();
	};
	const { t } = i18n_default$10.useTranslation();
	return (0, import_jsx_runtime.jsx)(Button, {
		size,
		className: b$65(null, className),
		onClick,
		onMouseDown: preventDefaultHandler,
		"aria-label": t("label_clear-button"),
		children: (0, import_jsx_runtime.jsx)(Icon, {
			data: Xmark,
			size: ICON_SIZE
		})
	});
};
var prepareAutoComplete = (autoComplete) => {
	if (typeof autoComplete === "boolean") return autoComplete ? "on" : "off";
	else return autoComplete;
};
var getInputControlState = (validationStateProp) => {
	return validationStateProp === "invalid" ? "error" : void 0;
};
var errorPropsMapper = (errorProps) => {
	const { error: errorProp, errorMessage: errorMessageProp, errorPlacement, validationState: validationStateProp } = errorProps;
	let errorMessage;
	if (typeof errorProp === "string") errorMessage = errorProp;
	if (errorMessageProp) errorMessage = errorMessageProp;
	let validationState;
	if (validationStateProp === "invalid" || Boolean(errorProp)) validationState = "invalid";
	return {
		errorMessage,
		errorPlacement,
		validationState
	};
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/common/OuterAdditionalContent/OuterAdditionalContent.js
var b$64 = block("outer-additional-content");
var OuterAdditionalContent = ({ errorMessage, note, noteId, errorMessageId }) => {
	return errorMessage || note ? (0, import_jsx_runtime.jsxs)("div", {
		className: b$64(),
		children: [errorMessage && (0, import_jsx_runtime.jsx)("div", {
			className: b$64("error"),
			id: errorMessageId,
			"data-qa": "control-error-message-qa",
			children: errorMessage
		}), note && (0, import_jsx_runtime.jsx)("div", {
			className: b$64("note"),
			id: noteId,
			children: note
		})]
	}) : null;
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/TextArea/TextAreaControl.js
var b$63 = block("text-area");
var calculateLinesByScrollHeight = (args) => {
	const { height, lineHeight } = args;
	const paddingTop = Number.isNaN(args.paddingTop) ? 0 : args.paddingTop;
	const paddingBottom = Number.isNaN(args.paddingBottom) ? 0 : args.paddingBottom;
	return (height - paddingTop - paddingBottom) / lineHeight;
};
function TextAreaControl(props) {
	const { name, id, tabIndex, autoComplete, placeholder, value, defaultValue, controlRef, controlProps, size, rows, minRows = 1, maxRows, autoFocus, disabled, readOnly, onChange, onFocus, onBlur, onKeyDown, onKeyUp, onKeyPress } = props;
	const innerControlRef = import_react.useRef(null);
	const handleRef = useForkRef(controlRef, innerControlRef);
	const textareaRows = Math.max(rows || minRows, 1);
	const innerValue = value || innerControlRef?.current?.value;
	const resizeHeight = import_react.useCallback(() => {
		const control = innerControlRef?.current;
		const parent = control?.parentElement;
		if (control && parent && !rows) {
			const controlStyles = getComputedStyle(control);
			const lineHeight = parseInt(controlStyles.getPropertyValue("line-height"), 10);
			const paddingTop = parseInt(controlStyles.getPropertyValue("padding-top"), 10);
			const paddingBottom = parseInt(controlStyles.getPropertyValue("padding-bottom"), 10);
			const linesWithCarriageReturn = (innerValue?.match(/\n/g) || []).length + 1;
			const parentHeight = parent.style.height;
			parent.style.height = `${parent.offsetHeight}px`;
			control.style.height = `${lineHeight + paddingTop + paddingBottom}px`;
			const overflow = control.style.overflow;
			control.style.overflow = "hidden";
			const linesByScrollHeight = calculateLinesByScrollHeight({
				height: control.scrollHeight,
				paddingTop,
				paddingBottom,
				lineHeight
			});
			const linesCount = Math.max(linesByScrollHeight, linesWithCarriageReturn);
			if (maxRows && maxRows < linesCount) control.style.height = `${maxRows * lineHeight + paddingTop + paddingBottom}px`;
			else if (minRows && minRows > linesCount) control.style.height = `${Math.min(minRows, maxRows || Infinity) * lineHeight + paddingTop + paddingBottom}px`;
			else control.style.height = `${control.scrollHeight}px`;
			control.style.overflow = overflow;
			parent.style.height = parentHeight;
		}
	}, [
		rows,
		maxRows,
		minRows,
		innerValue
	]);
	useResizeObserver({
		ref: rows ? void 0 : innerControlRef,
		onResize: resizeHeight
	});
	import_react.useEffect(() => {
		resizeHeight();
	}, [
		resizeHeight,
		size,
		value
	]);
	return (0, import_jsx_runtime.jsx)("textarea", {
		...controlProps,
		ref: handleRef,
		style: {
			...controlProps.style,
			height: rows ? "auto" : void 0
		},
		className: b$63("control", controlProps.className),
		name,
		id,
		tabIndex,
		placeholder,
		value,
		defaultValue,
		rows: textareaRows,
		autoFocus,
		autoComplete,
		onChange,
		onFocus,
		onBlur,
		onKeyDown,
		onKeyUp,
		onKeyPress,
		disabled: disabled ?? controlProps.disabled,
		readOnly: readOnly ?? controlProps.readOnly
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/TextArea/TextArea.js
var b$62 = block("text-area");
var TextArea = import_react.forwardRef(function TextArea(rawProps, ref) {
	const props = useDefaultProps("TextArea", rawProps);
	const { view = "normal", size = "m", pin = "round-round", name, value, defaultValue, disabled, readOnly, hasClear = false, error, errorMessage: errorMessageProp, validationState: validationStateProp, autoComplete, id: idProp, tabIndex, style, className, qa, controlProps, note, onUpdate, onChange } = props;
	const { errorMessage, validationState } = errorPropsMapper({
		error,
		errorMessage: errorMessageProp,
		validationState: validationStateProp
	});
	const [inputValue, setInputValue] = useControlledState(value, defaultValue ?? "", onUpdate);
	const innerControlRef = import_react.useRef(null);
	const fieldRef = useFormResetHandler({
		initialValue: inputValue,
		onReset: setInputValue
	});
	const handleRef = useForkRef(props.controlRef, innerControlRef, fieldRef);
	const [hasVerticalScrollbar, setHasVerticalScrollbar] = import_react.useState(false);
	const state = getInputControlState(validationState);
	const innerId = useUniqId();
	const isErrorMsgVisible = validationState === "invalid" && Boolean(errorMessage);
	const isClearControlVisible = Boolean(hasClear && !disabled && !readOnly && inputValue);
	const id = idProp || innerId;
	const errorMessageId = useUniqId();
	const noteId = useUniqId();
	const ariaDescribedBy = [
		controlProps?.["aria-describedby"],
		note ? noteId : void 0,
		isErrorMsgVisible ? errorMessageId : void 0
	].filter(Boolean).join(" ");
	const commonProps = {
		id,
		tabIndex,
		name,
		onChange(event) {
			setInputValue(event.target.value);
			if (onChange) onChange(event);
		},
		autoComplete: prepareAutoComplete(autoComplete),
		controlProps: {
			...controlProps,
			"aria-describedby": ariaDescribedBy || void 0,
			"aria-invalid": validationState === "invalid" || void 0
		}
	};
	const handleClear = (event) => {
		const control = innerControlRef.current;
		if (control) {
			control.focus();
			const syntheticEvent = Object.create(event);
			syntheticEvent.target = control;
			syntheticEvent.currentTarget = control;
			control.value = "";
			if (onChange) onChange(syntheticEvent);
		}
		setInputValue("");
	};
	import_react.useEffect(() => {
		const control = innerControlRef.current;
		if (control) {
			const currHasVerticalScrollbar = control.scrollHeight > control.clientHeight;
			if (hasVerticalScrollbar !== currHasVerticalScrollbar) setHasVerticalScrollbar(currHasVerticalScrollbar);
		}
	}, [inputValue, hasVerticalScrollbar]);
	return (0, import_jsx_runtime.jsxs)("span", {
		ref,
		style,
		className: b$62({
			view,
			size,
			disabled,
			state,
			pin: view === "clear" ? void 0 : pin,
			"has-clear": isClearControlVisible,
			"has-scrollbar": hasVerticalScrollbar
		}, className),
		"data-qa": qa,
		children: [(0, import_jsx_runtime.jsxs)("span", {
			className: b$62("content"),
			children: [(0, import_jsx_runtime.jsx)(TextAreaControl, {
				...props,
				...commonProps,
				controlRef: handleRef
			}), isClearControlVisible && (0, import_jsx_runtime.jsx)(ClearButton, {
				className: b$62("clear", { size }),
				size: mapTextInputSizeToButtonSize(size),
				onClick: handleClear
			})]
		}), (0, import_jsx_runtime.jsx)(OuterAdditionalContent, {
			errorMessage: isErrorMsgVisible ? errorMessage : null,
			errorMessageId,
			note,
			noteId
		})]
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/TextInput/AdditionalContent.js
var b$61 = block("text-input");
var AdditionalContent = import_react.forwardRef(function AdditionalContent({ placement, children, onClick }, ref) {
	if (!children) return null;
	return (0, import_jsx_runtime.jsx)("div", {
		ref,
		className: b$61("additional-content", { placement }),
		onClick,
		children
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/TextInput/TextInputControl.js
var b$60 = block("text-input");
function TextInputControl(props) {
	const { controlProps, controlRef, type, name, id, tabIndex, autoComplete, placeholder, value, defaultValue, autoFocus, disabled, readOnly, onChange, onFocus, onBlur, onKeyDown, onKeyUp, onKeyPress } = props;
	return (0, import_jsx_runtime.jsx)("input", {
		...controlProps,
		ref: controlRef,
		className: b$60("control", { type: "input" }, controlProps.className),
		type,
		name,
		id,
		tabIndex,
		placeholder,
		value,
		defaultValue,
		autoFocus,
		autoComplete,
		onChange,
		onFocus,
		onBlur,
		onKeyDown,
		onKeyUp,
		onKeyPress,
		disabled: disabled ?? controlProps.disabled,
		readOnly: readOnly ?? controlProps.readOnly
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/TextInput/TextInput.js
var b$59 = block("text-input");
var TextInput = import_react.forwardRef(function TextInput(rawProps, ref) {
	const props = useDefaultProps("TextInput", rawProps);
	const { view = "normal", size = "m", pin = "round-round", name, value, defaultValue, label, disabled, readOnly, hasClear = false, error, errorMessage: errorMessageProp, errorPlacement: errorPlacementProp = "outside", validationState: validationStateProp, autoComplete, id: idProp, tabIndex, style, className, qa, controlProps: controlPropsProp, startContent, endContent, note, onUpdate, onChange } = props;
	const direction = useDirection();
	const { errorMessage, errorPlacement, validationState } = errorPropsMapper({
		error,
		errorMessage: errorMessageProp,
		errorPlacement: errorPlacementProp,
		validationState: validationStateProp
	});
	const [inputValue, setInputValue] = useControlledState(value, defaultValue ?? "", onUpdate);
	const innerControlRef = import_react.useRef(null);
	const fieldRef = useFormResetHandler({
		initialValue: inputValue,
		onReset: setInputValue
	});
	const handleRef = useForkRef(props.controlRef, innerControlRef, fieldRef);
	const labelRef = import_react.useRef(null);
	const startContentRef = import_react.useRef(null);
	const state = getInputControlState(validationState);
	const isLabelVisible = Boolean(label);
	const isErrorMsgVisible = validationState === "invalid" && Boolean(errorMessage) && errorPlacement === "outside";
	const isErrorIconVisible = validationState === "invalid" && Boolean(errorMessage) && errorPlacement === "inside";
	const isClearControlVisible = Boolean(hasClear && !disabled && !readOnly && inputValue);
	const isStartContentVisible = Boolean(startContent);
	const isEndContentVisible = Boolean(endContent);
	const isAutoCompleteOff = isLabelVisible && !idProp && !name && typeof autoComplete === "undefined";
	const innerId = useUniqId();
	const id = isLabelVisible ? idProp || innerId : idProp;
	const labelSize = useElementSize(isLabelVisible ? labelRef : null, size);
	const startContentSize = useElementSize(isStartContentVisible ? startContentRef : null, size);
	const errorMessageId = useUniqId();
	const noteId = useUniqId();
	const ariaDescribedBy = [
		controlPropsProp?.["aria-describedby"],
		note ? noteId : void 0,
		isErrorMsgVisible ? errorMessageId : void 0
	].filter(Boolean).join(" ");
	const controlProps = {
		...controlPropsProp,
		style: {
			...controlPropsProp?.style,
			...isLabelVisible && labelSize.width ? { paddingInlineStart: `${labelSize.width}px` } : {}
		},
		"aria-invalid": validationState === "invalid" || void 0,
		"aria-describedby": ariaDescribedBy || void 0
	};
	const commonProps = {
		id,
		tabIndex,
		name,
		onChange(event) {
			setInputValue(event.target.value);
			if (onChange) onChange(event);
		},
		autoComplete: isAutoCompleteOff ? "off" : prepareAutoComplete(autoComplete),
		controlProps
	};
	const handleClear = (event) => {
		setInputValue("");
		const control = innerControlRef.current;
		if (control) {
			const syntheticEvent = Object.create(event);
			syntheticEvent.target = control;
			syntheticEvent.currentTarget = control;
			control.value = "";
			if (onChange) onChange(syntheticEvent);
		}
	};
	const handleAdditionalContentClick = (event) => {
		const needActivateInput = !event.currentTarget.contains(document.activeElement) && event.currentTarget.contains(event.target);
		const hasSelection = Boolean(document.getSelection()?.toString());
		if (needActivateInput && !hasSelection) innerControlRef.current?.focus();
	};
	return (0, import_jsx_runtime.jsxs)("span", {
		ref,
		style,
		className: b$59({
			view,
			size,
			disabled,
			state,
			pin: view === "clear" ? void 0 : pin,
			"has-clear": isClearControlVisible,
			"has-start-content": isStartContentVisible,
			"has-end-content": isEndContentVisible
		}, className),
		"data-qa": qa,
		children: [(0, import_jsx_runtime.jsxs)("span", {
			className: b$59("content"),
			children: [
				isStartContentVisible && (0, import_jsx_runtime.jsx)(AdditionalContent, {
					ref: startContentRef,
					placement: "start",
					onClick: handleAdditionalContentClick,
					children: startContent
				}),
				isLabelVisible && (0, import_jsx_runtime.jsx)("label", {
					ref: labelRef,
					style: {
						insetInlineStart: isStartContentVisible ? startContentSize.width : void 0,
						maxWidth: `calc(50% - ${startContentSize.width}px)`
					},
					className: b$59("label"),
					title: label,
					htmlFor: id,
					children: `${label}`
				}),
				(0, import_jsx_runtime.jsx)(TextInputControl, {
					...props,
					...commonProps,
					controlRef: handleRef
				}),
				isClearControlVisible && (0, import_jsx_runtime.jsx)(ClearButton, {
					size: mapTextInputSizeToButtonSize(size),
					onClick: handleClear,
					className: b$59("clear", { size })
				}),
				isErrorIconVisible && (0, import_jsx_runtime.jsx)(Popover, {
					placement: direction === "rtl" ? ["left", "bottom"] : ["right", "bottom"],
					className: b$59("error-popover"),
					hasArrow: true,
					content: (0, import_jsx_runtime.jsx)(Alert, {
						className: b$59("error-popover-content"),
						theme: "clear",
						message: (0, import_jsx_runtime.jsx)("div", {
							role: "presentation",
							children: errorMessage
						})
					}),
					children: (0, import_jsx_runtime.jsx)("span", {
						"data-qa": "control-error-icon-qa",
						children: (0, import_jsx_runtime.jsx)(Icon, {
							data: TriangleExclamation,
							className: b$59("error-icon"),
							size: size === "s" ? 12 : 16
						})
					})
				}),
				isEndContentVisible && (0, import_jsx_runtime.jsx)(AdditionalContent, {
					placement: "end",
					onClick: handleAdditionalContentClick,
					children: endContent
				})
			]
		}), (0, import_jsx_runtime.jsx)(OuterAdditionalContent, {
			note,
			errorMessage: isErrorMsgVisible ? errorMessage : null,
			noteId,
			errorMessageId
		})]
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/PasswordInput/constants.js
var PasswordInputQa = {
	revealButton: "reveal-button",
	copyButton: "copy-button"
};
var i18n = addComponentKeysets({
	en: {
		"label_show-password": "Show password",
		"label_hide-password": "Hide password"
	},
	ru: {
		"label_show-password": "Показать пароль",
		"label_hide-password": "Скрыть пароль"
	}
}, "PasswordInput");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/PasswordInput/utils.js
var getActionButtonSizeAndIconSize = (textInputSize) => {
	let actionButtonSize = "s";
	let iconSize = 16;
	switch (textInputSize) {
		case "s":
			actionButtonSize = "xs";
			iconSize = 12;
			break;
		case "l":
			actionButtonSize = "m";
			break;
		case "xl": actionButtonSize = "l";
	}
	return {
		actionButtonSize,
		iconSize
	};
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/controls/PasswordInput/PasswordInput.js
var b$58 = block("password-input");
var PasswordInput = (props) => {
	const { autoComplete, controlProps, endContent, hideCopyButton = false, hideRevealButton = false, showCopyTooltip = false, showRevealTooltip = false, size = "m" } = props;
	const [inputValue, setInputValue] = useControlledState(props.value, props.defaultValue ?? "", props.onUpdate);
	const [revealValue, setRevealValue] = useControlledState(props.revealValue, false, props.onRevealValueUpdate);
	const { actionButtonSize, iconSize } = getActionButtonSizeAndIconSize(size);
	const { t } = i18n.useTranslation();
	const additionalEndContent = (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [
		endContent,
		inputValue && !hideCopyButton && !props.disabled ? (0, import_jsx_runtime.jsx)(ClipboardButton, {
			view: "flat-secondary",
			text: inputValue,
			hasTooltip: showCopyTooltip,
			size: actionButtonSize,
			className: b$58("copy-button"),
			qa: PasswordInputQa.copyButton
		}) : null,
		hideRevealButton ? null : (0, import_jsx_runtime.jsx)(ActionTooltip, {
			disabled: !showRevealTooltip,
			title: revealValue ? t("label_hide-password") : t("label_show-password"),
			children: (0, import_jsx_runtime.jsx)(Button, {
				qa: PasswordInputQa.revealButton,
				view: "flat-secondary",
				disabled: props.disabled,
				onClick: () => setRevealValue(!revealValue),
				size: actionButtonSize,
				onMouseDown: (event) => event.preventDefault(),
				"aria-label": revealValue ? t("label_hide-password") : t("label_show-password"),
				children: (0, import_jsx_runtime.jsx)(Icon, {
					data: revealValue ? EyeSlash : Eye,
					size: iconSize
				})
			})
		})
	] });
	return (0, import_jsx_runtime.jsx)(TextInput, {
		...props,
		type: revealValue ? "text" : "password",
		endContent: additionalEndContent,
		autoComplete: autoComplete ? autoComplete : "new-password",
		controlProps: {
			...controlProps,
			className: b$58("input-control", controlProps?.className)
		},
		value: inputValue,
		onUpdate: setInputValue
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Loader/Loader.js
var b$57 = block("loader");
function Loader(rawProps) {
	const { size = "s", className, qa } = useDefaultProps("Loader", rawProps);
	return (0, import_jsx_runtime.jsxs)("div", {
		className: b$57({ size }, className),
		"data-qa": qa,
		children: [
			(0, import_jsx_runtime.jsx)("div", { className: b$57("left") }),
			(0, import_jsx_runtime.jsx)("div", { className: b$57("center") }),
			(0, import_jsx_runtime.jsx)("div", { className: b$57("right") })
		]
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/List/ListLoadingIndicator.js
var b$56 = block("list");
var ListLoadingIndicator = (props) => {
	const ref = import_react.useRef(null);
	useIntersection({
		element: ref.current,
		onIntersect: props?.onIntersect
	});
	return (0, import_jsx_runtime.jsx)("div", {
		ref,
		className: b$56("loading-indicator"),
		children: (0, import_jsx_runtime.jsx)(Loader, { qa: "list-loader" })
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/List/constants.js
var ListQa = { ACTIVE_ITEM: "list-active-item" };
var listNavigationIgnoredKeys = [KeyCode$1.TAB];
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/List/utils.js
function getElementId(id, activeItem) {
	if (typeof id !== "string" || typeof activeItem !== "number") return;
	return `${id}-item-${activeItem}`;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/List/components/ListItem.js
var b$55 = block("list");
var ROLES_WITH_ARIA_SELECTED = /* @__PURE__ */ new Set([
	"option",
	"gridcell",
	"row",
	"tab"
]);
var defaultRenderItem = (item) => String(item);
function getStyle(provided, style) {
	if (!style) return provided?.draggableProps.style;
	return {
		...provided?.draggableProps.style,
		...style
	};
}
var ListItem = class ListItem extends import_react.Component {
	static publishEvent = eventBroker.withEventPublisher("List");
	node = null;
	render() {
		const { item, height, style, sortable, sortHandleAlign, itemClassName, selected, active, role = "listitem", isDragging = false } = this.props;
		const fixedStyle = {
			height,
			...style,
			right: void 0
		};
		return (0, import_jsx_runtime.jsxs)("div", {
			role,
			"aria-selected": ROLES_WITH_ARIA_SELECTED.has(role) ? selected : void 0,
			"aria-disabled": item.disabled,
			"data-qa": active ? ListQa.ACTIVE_ITEM : void 0,
			className: b$55("item", {
				sortable,
				active,
				selected,
				inactive: item.disabled,
				"sort-handle-align": sortHandleAlign,
				dragging: isDragging
			}, itemClassName),
			...this.props.provided?.draggableProps,
			style: getStyle(this.props.provided, fixedStyle),
			onClick: item.disabled ? void 0 : this.onClick,
			onClickCapture: item.disabled ? void 0 : this.onClickCapture,
			onMouseEnter: this.onMouseEnter,
			ref: this.setRef,
			id: getElementId(this.props.listId, this.props.itemIndex),
			children: [this.renderSortIcon(), this.renderContent()]
		});
	}
	getNode = () => this.node;
	setRef = (node) => {
		this.node = node;
		this.props.provided?.innerRef(node);
	};
	renderSortIcon() {
		const { sortable } = this.props;
		return sortable ? (0, import_jsx_runtime.jsx)("div", {
			...this.props.provided?.dragHandleProps,
			className: b$55("item-sort-icon"),
			children: (0, import_jsx_runtime.jsx)(Icon, {
				data: Grip,
				size: 12
			})
		}) : null;
	}
	renderContent() {
		const { renderItem = defaultRenderItem, item, active, itemIndex } = this.props;
		return (0, import_jsx_runtime.jsx)("div", {
			className: b$55("item-content"),
			children: renderItem(item, active, itemIndex)
		});
	}
	onClick = (event) => {
		if (!this.props.onClick) return;
		this.props.onClick(this.props.item, this.props.itemIndex, false, event);
	};
	onClickCapture = (event) => {
		ListItem.publishEvent({
			domEvent: event,
			eventId: "click"
		});
	};
	onMouseEnter = () => !this.props.item.disabled && this.props.onActivate(this.props.itemIndex);
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/List/components/SimpleContainer.js
function getRefs(count) {
	return Array.from({ length: count }).reduce((acc, _, index) => {
		acc[index] = import_react.createRef();
		return acc;
	}, {});
}
var SimpleContainer = class extends import_react.Component {
	static getDerivedStateFromProps({ itemCount }, prevState) {
		if (itemCount === Object.keys(prevState.refsList).length) return prevState;
		else return { refsList: getRefs(itemCount) };
	}
	node = null;
	constructor(props) {
		super(props);
		this.state = { refsList: getRefs(props.itemCount) };
	}
	render() {
		const children = import_react.Children.map(this.props.children, (child, index) => import_react.cloneElement(child, { ref: this.state.refsList[index] }));
		return (0, import_jsx_runtime.jsx)("div", {
			ref: this.setRef,
			role: this.props.role,
			id: this.props.id,
			children
		});
	}
	scrollToItem(index) {
		const listItem = this.state.refsList[index]?.current;
		if (listItem && typeof listItem.getNode === "function") {
			const node = listItem.getNode();
			if (node) {
				if (!this.props.onScrollToItem?.(node)) node.scrollIntoView?.({ block: "nearest" });
			}
		}
	}
	setRef = (node) => {
		this.node = node;
		this.props.provided?.innerRef(node);
	};
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/List/components/VariableSizeListElementType.js
var VariableSizeListElementType = import_react.forwardRef(({ style, role, listId, ...rest }, ref) => (0, import_jsx_runtime.jsx)("div", {
	ref,
	style,
	role,
	id: listId,
	...rest
}));
VariableSizeListElementType.displayName = "VariableSizeListElementType";
var createVariableSizeListElementType = (role, listId) => {
	const Component = import_react.forwardRef((props, ref) => (0, import_jsx_runtime.jsx)(VariableSizeListElementType, {
		...props,
		role,
		ref,
		listId
	}));
	Component.displayName = "VariableSizeListElementType";
	return Component;
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/List/List.js
var b$54 = block("list");
var DEFAULT_ITEM_HEIGHT = 28;
var DEFAULT_PAGE_SIZE = 10;
var listDefaultProps = {
	items: [],
	itemClassName: "",
	filterable: true,
	sortable: false,
	virtualized: true,
	deactivateOnLeave: true
};
var reorder = (list, startIndex, endIndex) => {
	const result = Array.from(list);
	const [removed] = result.splice(startIndex, 1);
	result.splice(endIndex, 0, removed);
	return result;
};
var ListContainer$1 = import_react.forwardRef((props, ref) => {
	const { role, listId, ...restProps } = props;
	const innerElementType = import_react.useMemo(() => createVariableSizeListElementType(role, listId), [role, listId]);
	return (0, import_jsx_runtime.jsx)(VariableSizeList, {
		ref,
		direction: useDirection(),
		innerElementType,
		...restProps
	});
});
ListContainer$1.displayName = "ListContainer";
var List = class List extends import_react.Component {
	static defaultProps = listDefaultProps;
	static moveListElement(list, oldIndex, newIndex) {
		if (oldIndex !== newIndex) {
			const [item] = list.splice(oldIndex, 1);
			list.splice(newIndex, 0, item);
		}
		return list;
	}
	static findNextIndex(list, index, step) {
		const dataLength = list.length;
		let currentIndex = (index + dataLength) % dataLength;
		for (let i = 0; i < dataLength; i += 1) {
			if (list[currentIndex] && !list[currentIndex].disabled) return currentIndex;
			currentIndex = (currentIndex + dataLength + step) % dataLength;
		}
	}
	state = {
		items: this.props.items,
		activeItem: this.props.activeItemIndex,
		filter: ""
	};
	refFilter = import_react.createRef();
	refContainer = import_react.createRef();
	blurTimer = null;
	loadingItem = {
		value: "__LIST_ITEM_LOADING__",
		disabled: false
	};
	uniqId = getUniqId();
	componentDidMount() {
		this.activateItem(this.props.activeItemIndex, true);
	}
	componentDidUpdate(prevProps, prevState) {
		if (!(0, import_isEqual.default)(this.props.items, prevProps.items)) {
			const filter = this.getFilter();
			if (filter && !this.props.onFilterUpdate) this.onUpdateFilterInternal(filter);
			else this.setState({ items: this.props.items });
		}
		if (this.props.activeItemIndex !== prevProps.activeItemIndex) this.activateItem(this.props.activeItemIndex);
		if (this.props.onChangeActive && this.state.activeItem !== prevState.activeItem) this.props.onChangeActive(this.state.activeItem);
	}
	componentWillUnmount() {
		this.blurTimer = null;
	}
	render() {
		const { id, emptyPlaceholder, virtualized, className, itemsClassName, qa } = this.props;
		const { items } = this.state;
		return (0, import_jsx_runtime.jsx)(MobileContext.Consumer, { children: ({ mobile }) => (0, import_jsx_runtime.jsxs)("div", {
			className: b$54({ mobile }, className),
			"data-qa": qa,
			tabIndex: -1,
			onFocus: this.handleFocus,
			onBlur: this.handleBlur,
			onKeyDown: this.onKeyDown,
			children: [this.renderFilter(), (0, import_jsx_runtime.jsxs)("div", {
				id,
				className: b$54("items", { virtualized }, itemsClassName),
				style: this.getItemsStyle(),
				onMouseLeave: this.onMouseLeave,
				children: [this.renderItems(), items.length === 0 && Boolean(emptyPlaceholder) && (0, import_jsx_runtime.jsx)("div", {
					className: b$54("empty-placeholder"),
					children: emptyPlaceholder
				})]
			})]
		}) });
	}
	getItems() {
		return this.state.items;
	}
	getItemsWithLoading() {
		if (this.props.sortable) return this.getItems();
		return this.props.loading ? [...this.state.items, this.loadingItem] : this.getItems();
	}
	getActiveItem() {
		return typeof this.state.activeItem === "number" ? this.state.activeItem : null;
	}
	activateItem(index, scrollTo = true) {
		this.setState({ activeItem: index }, () => {
			if (typeof index === "number" && scrollTo) this.scrollToIndex(index);
		});
	}
	onKeyDown = (event) => {
		const { activeItem, pageSize } = this.state;
		if (listNavigationIgnoredKeys.includes(event.key)) return;
		const isInputTarget = event.target instanceof HTMLInputElement;
		switch (event.key) {
			case KeyCode$1.ARROW_DOWN:
				this.handleKeyMove(event, 1, -1);
				break;
			case KeyCode$1.ARROW_UP:
				this.handleKeyMove(event, -1);
				break;
			case KeyCode$1.PAGE_DOWN:
				this.handleKeyMove(event, pageSize ?? DEFAULT_PAGE_SIZE);
				break;
			case KeyCode$1.PAGE_UP:
				this.handleKeyMove(event, -(pageSize ?? DEFAULT_PAGE_SIZE));
				break;
			case KeyCode$1.HOME:
				if (isInputTarget) return;
				this.handleKeyMove(event, this.state.items.length - (activeItem || 0));
				break;
			case KeyCode$1.END:
				if (isInputTarget) return;
				this.handleKeyMove(event, -(activeItem || 0) - 1);
				break;
			case KeyCode$1.ENTER:
				if (typeof activeItem === "number" && this.props.onItemClick) this.props.onItemClick(this.state.items[activeItem], activeItem, true, event);
				break;
			default: if (this.refFilter.current) this.refFilter.current.focus();
		}
	};
	renderItemContent = (item, isItemActive, itemIndex) => {
		const { onLoadMore } = this.props;
		if ((0, import_isObject.default)(item) && "value" in item && item.value === this.loadingItem.value) return (0, import_jsx_runtime.jsx)(ListLoadingIndicator, { onIntersect: itemIndex === 0 ? void 0 : onLoadMore });
		return this.props.renderItem ? this.props.renderItem(item, isItemActive, itemIndex) : defaultRenderItem(item);
	};
	renderItem = ({ index, style, height, provided, isDragging }) => {
		const { sortHandleAlign, role } = this.props;
		const { items, activeItem } = this.state;
		const item = this.getItemsWithLoading()[index];
		const sortable = this.props.sortable && items.length > 1 && !this.getFilter();
		const active = index === activeItem || index === this.props.activeItemIndex;
		const selected = Array.isArray(this.props.selectedItemIndex) ? this.props.selectedItemIndex.includes(index) : index === this.props.selectedItemIndex;
		return (0, import_jsx_runtime.jsx)(ListItem, {
			style,
			height,
			itemIndex: index,
			item,
			sortable,
			sortHandleAlign,
			renderItem: this.renderItemContent,
			itemClassName: this.props.itemClassName,
			active,
			selected,
			onActivate: this.onItemActivate,
			onClick: this.props.onItemClick,
			role: role === "listbox" ? "option" : "listitem",
			listId: this.props.id ?? this.uniqId,
			provided,
			isDragging
		}, index);
	};
	renderVirtualizedItem = ({ index, style }) => {
		return (0, import_jsx_runtime.jsx)(PublicDraggable, {
			draggableId: String(index),
			index,
			children: (provided) => this.renderItem({
				index,
				style,
				provided
			})
		}, `item-key-${index}`);
	};
	renderFilter() {
		const { size, filterable, filter = this.state.filter, filterPlaceholder, filterClassName = "", autoFocus } = this.props;
		if (!filterable) return null;
		return (0, import_jsx_runtime.jsx)("div", {
			className: b$54("filter", filterClassName),
			children: (0, import_jsx_runtime.jsx)(TextInput, {
				controlRef: this.refFilter,
				size,
				placeholder: filterPlaceholder,
				value: filter,
				hasClear: true,
				onUpdate: this.onFilterUpdate,
				autoFocus,
				controlProps: {
					role: "combobox",
					"aria-activedescendant": getElementId(this.props.id ?? this.uniqId, this.state.activeItem),
					"aria-autocomplete": "list",
					"aria-controls": this.props.id ?? this.uniqId,
					"aria-expanded": true
				}
			})
		});
	}
	renderSimpleContainer() {
		const { sortable, role = "list" } = this.props;
		const items = this.getItemsWithLoading();
		if (sortable) return (0, import_jsx_runtime.jsx)(DragDropContext, {
			onDragStart: this.onSortStart,
			onDragEnd: this.onSortEnd,
			children: (0, import_jsx_runtime.jsx)(ConnectedDroppable, {
				droppableId: "droppable",
				renderClone: (provided, snapshot, rubric) => {
					return this.renderItem({
						index: rubric.source.index,
						provided,
						isDragging: snapshot.isDragging
					});
				},
				children: (droppableProvided) => (0, import_jsx_runtime.jsx)(SimpleContainer, {
					ref: this.refContainer,
					itemCount: items.length,
					provided: droppableProvided,
					onScrollToItem: this.props.onScrollToItem,
					role,
					id: this.props.id ?? this.uniqId,
					children: items.map((_item, index) => {
						return (0, import_jsx_runtime.jsx)(PublicDraggable, {
							draggableId: String(index),
							index,
							children: (provided, snapshot) => {
								return this.renderItem({
									index,
									isDragging: snapshot.isDragging,
									provided,
									height: this.getItemHeight(index)
								});
							}
						}, `item-key-${index}`);
					})
				})
			})
		});
		return (0, import_jsx_runtime.jsx)(SimpleContainer, {
			itemCount: items.length,
			ref: this.refContainer,
			onScrollToItem: this.props.onScrollToItem,
			role,
			id: this.props.id ?? this.uniqId,
			children: items.map((_item, index) => this.renderItem({
				index,
				height: this.getItemHeight(index)
			}))
		});
	}
	renderVirtualizedContainer() {
		const { role = "list" } = this.props;
		const items = [...this.getItemsWithLoading()];
		if (this.props.sortable) return (0, import_jsx_runtime.jsx)(DragDropContext, {
			onDragStart: this.onSortStart,
			onDragEnd: this.onSortEnd,
			children: (0, import_jsx_runtime.jsx)(ConnectedDroppable, {
				droppableId: "droppable",
				mode: "virtual",
				renderClone: (provided, snapshot, rubric) => {
					return this.renderItem({
						index: rubric.source.index,
						provided,
						isDragging: snapshot.isDragging
					});
				},
				children: (droppableProvided) => (0, import_jsx_runtime.jsx)(AutoSizer, { children: ({ width, height }) => (0, import_jsx_runtime.jsx)(ListContainer$1, {
					ref: this.refContainer,
					outerRef: droppableProvided.innerRef,
					width,
					height,
					itemSize: this.getVirtualizedItemHeight,
					itemData: items,
					itemCount: items.length,
					overscanCount: 10,
					onItemsRendered: this.onItemsRendered,
					role,
					listId: this.props.id ?? this.uniqId,
					activeItem: this.state.activeItem,
					children: this.renderVirtualizedItem
				}) })
			})
		});
		return (0, import_jsx_runtime.jsx)(AutoSizer, { children: ({ width, height }) => (0, import_jsx_runtime.jsx)(ListContainer$1, {
			ref: this.refContainer,
			width,
			height,
			itemSize: this.getVirtualizedItemHeight,
			itemData: items,
			itemCount: items.length,
			overscanCount: 10,
			onItemsRendered: this.onItemsRendered,
			role,
			listId: this.props.id ?? this.uniqId,
			activeItem: this.state.activeItem,
			children: this.renderItem
		}) });
	}
	renderItems() {
		if (this.props.virtualized) return this.renderVirtualizedContainer();
		else return this.renderSimpleContainer();
	}
	filterItem = (filter) => (item) => {
		return String(item).includes(filter);
	};
	getFilter() {
		const { filter = this.state.filter } = this.props;
		return filter;
	}
	getItemsStyle() {
		let { itemsHeight } = this.props;
		if (typeof itemsHeight === "function") itemsHeight = itemsHeight(this.state.items);
		return itemsHeight ? { height: itemsHeight } : void 0;
	}
	scrollToIndex = (index) => {
		const container = this.refContainer.current;
		if (container) container.scrollToItem(index);
	};
	deactivate = () => {
		if (!this.blurTimer) return;
		this.blurTimer = null;
		if (this.props.deactivateOnLeave) this.setState({ activeItem: void 0 });
	};
	handleKeyMove(event, step, defaultItemIndex = 0) {
		const { activeItem = defaultItemIndex } = this.state;
		event.preventDefault();
		const items = this.getItemsWithLoading();
		this.activateItem(List.findNextIndex(items, activeItem + step, Math.sign(step)));
	}
	handleFocus = () => {
		if (this.blurTimer) {
			clearTimeout(this.blurTimer);
			this.blurTimer = null;
		}
	};
	handleBlur = () => {
		if (!this.blurTimer) this.blurTimer = setTimeout(this.deactivate, 50);
	};
	onUpdateFilterInternal = (value) => {
		const { items, filterItem = this.filterItem, onFilterEnd } = this.props;
		this.setState({
			filter: value,
			items: value ? items.filter(filterItem(value)) : items
		}, () => {
			if (onFilterEnd) onFilterEnd({ items: this.state.items });
		});
	};
	onFilterUpdate = (value) => {
		if (this.props.onFilterUpdate) this.props.onFilterUpdate(value);
		else this.onUpdateFilterInternal(value);
	};
	onItemsRendered = ({ visibleStartIndex, visibleStopIndex }) => {
		this.setState({ pageSize: visibleStopIndex - visibleStartIndex });
	};
	onItemActivate = (index) => {
		if (!this.state.sorting) this.activateItem(index, false);
	};
	onMouseLeave = () => {
		this.handleBlur();
	};
	onSortStart = () => {
		this.setState({ sorting: true });
	};
	onSortEnd = (result) => {
		if (!result.destination) {
			this.setState({ sorting: false });
			return;
		}
		if (result.source.index === result.destination.index) {
			this.setState({ sorting: false });
			return;
		}
		const oldIndex = result.source.index;
		const newIndex = result.destination.index;
		if (this.props.onSortEnd) this.props.onSortEnd({
			oldIndex,
			newIndex
		});
		const nextItems = reorder(this.getItems(), oldIndex, newIndex);
		this.setState({
			activeItem: newIndex,
			items: nextItems,
			sorting: false
		});
	};
	getItemHeight = (index) => {
		const { itemHeight } = this.props;
		if (typeof itemHeight === "function") {
			const { items } = this.state;
			return itemHeight(items[index], index);
		}
		return itemHeight;
	};
	getVirtualizedItemHeight = (index) => {
		return this.getItemHeight(index) || DEFAULT_ITEM_HEIGHT;
	};
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Sheet/constants.js
var sheetBlock = block("sheet");
var SheetQa = { VEIL: "sheet-veil" };
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Sheet/utils.js
var Point = class {
	x;
	y;
	timeStamp;
	constructor(x, y) {
		this.x = x;
		this.y = y;
		this.timeStamp = Date.now();
	}
};
var VelocityTracker = class {
	pointsLen;
	points = [];
	constructor(len = 5) {
		this.pointsLen = len;
		this.clear();
	}
	clear() {
		this.points = new Array(this.pointsLen);
	}
	addMovement({ x, y }) {
		this.points.pop();
		this.points.unshift(new Point(x, y));
	}
	getYAcceleration(lastPointCount = 1) {
		const endPoint = this.points[0];
		const startPoint = this.points[lastPointCount];
		if (!endPoint || !startPoint) return 0;
		return (endPoint.y - startPoint.y) / Math.pow(endPoint.timeStamp - startPoint.timeStamp, 2);
	}
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Sheet/SheetContent.js
var TRANSITION_DURATION = "0.3s";
var HIDE_THRESHOLD = 50;
var ACCELERATION_Y_MAX = .08;
var ACCELERATION_Y_MIN = -.02;
var DEFAULT_MAX_CONTENT_HEIGHT_FROM_VIEWPORT_COEFFICIENT = .9;
var WINDOW_RESIZE_TIMEOUT = 50;
var hashHistory = [];
function warnAboutOutOfRange() {
	warnOnce("[Sheet] The value of the \"maxContentHeightCoefficient\" property must be between 0 and 1");
}
var SheetContent = class extends import_react.Component {
	static defaultProps = {
		id: "sheet",
		allowHideOnContentScroll: true
	};
	veilRef = import_react.createRef();
	sheetRef = import_react.createRef();
	sheetTopRef = import_react.createRef();
	sheetMarginBoxRef = import_react.createRef();
	sheetScrollContainerRef = import_react.createRef();
	velocityTracker = new VelocityTracker();
	observer = null;
	resizeWindowTimer = null;
	state = {
		startScrollTop: 0,
		startY: 0,
		deltaY: 0,
		prevSheetHeight: 0,
		swipeAreaTouched: false,
		contentTouched: false,
		veilTouched: false,
		isAnimating: false,
		inWindowResizeScope: false,
		delayedResize: false
	};
	componentDidMount() {
		this.addListeners();
		this.show();
		const initialHeight = this.getAvailableContentHeight(this.sheetContentHeight);
		this.setInitialStyles(initialHeight);
		this.setState({ prevSheetHeight: initialHeight });
	}
	componentDidUpdate(prevProps) {
		const { visible, location } = this.props;
		if (!prevProps.visible && visible) this.show();
		if (prevProps.visible && !visible || this.shouldClose(prevProps)) this.hide();
		if (prevProps.location.pathname !== location.pathname) hashHistory = [];
	}
	componentWillUnmount() {
		this.removeListeners();
	}
	render() {
		const { content, contentClassName, swipeAreaClassName, hideTopBar, title } = this.props;
		const { deltaY, swipeAreaTouched, contentTouched, veilTouched } = this.state;
		const veilTransitionMod = { "with-transition": !deltaY || veilTouched };
		const sheetTransitionMod = { "with-transition": veilTransitionMod["with-transition"] };
		const contentMod = { "without-scroll": deltaY > 0 && contentTouched || swipeAreaTouched };
		const marginBoxMod = { "always-full-height": this.props.alwaysFullHeight };
		return (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [(0, import_jsx_runtime.jsx)("div", {
			ref: this.veilRef,
			className: sheetBlock("veil", veilTransitionMod),
			onClick: this.onVeilClick,
			onTransitionEnd: this.onVeilTransitionEnd,
			role: "presentation",
			"data-qa": SheetQa.VEIL
		}), (0, import_jsx_runtime.jsxs)("div", {
			ref: this.sheetRef,
			className: sheetBlock("sheet", sheetTransitionMod),
			role: "dialog",
			"aria-modal": "true",
			"aria-label": title,
			children: [
				!hideTopBar && (0, import_jsx_runtime.jsx)("div", {
					ref: this.sheetTopRef,
					className: sheetBlock("sheet-top"),
					children: (0, import_jsx_runtime.jsx)("div", { className: sheetBlock("sheet-top-resizer") })
				}),
				(0, import_jsx_runtime.jsx)("div", {
					className: sheetBlock("sheet-swipe-area", swipeAreaClassName),
					onTouchStart: this.onSwipeAreaTouchStart,
					onTouchMove: this.onSwipeAriaTouchMove,
					onTouchEnd: this.onSwipeAriaTouchEnd
				}),
				(0, import_jsx_runtime.jsx)("div", {
					ref: this.sheetScrollContainerRef,
					className: sheetBlock("sheet-scroll-container", contentMod),
					onTouchStart: this.onContentTouchStart,
					onTouchMove: this.onContentTouchMove,
					onTouchEnd: this.onContentTouchEnd,
					onTransitionEnd: this.onContentTransitionEnd,
					children: (0, import_jsx_runtime.jsx)("div", {
						ref: this.sheetMarginBoxRef,
						className: sheetBlock("sheet-margin-box", marginBoxMod),
						children: (0, import_jsx_runtime.jsx)("div", {
							className: sheetBlock("sheet-margin-box-border-compensation"),
							children: (0, import_jsx_runtime.jsxs)("div", {
								className: sheetBlock("sheet-content", contentClassName),
								children: [title && (0, import_jsx_runtime.jsx)("div", {
									className: sheetBlock("sheet-content-title"),
									children: title
								}), content]
							})
						})
					})
				})
			]
		})] });
	}
	get veilOpacity() {
		return this.veilRef.current?.style.opacity || 0;
	}
	get sheetTopHeight() {
		return this.sheetTopRef.current?.getBoundingClientRect().height || 0;
	}
	get sheetHeight() {
		return this.sheetRef.current?.getBoundingClientRect().height || 0;
	}
	get sheetScrollTop() {
		return this.sheetScrollContainerRef.current?.scrollTop || 0;
	}
	get sheetContentHeight() {
		return this.sheetMarginBoxRef.current?.getBoundingClientRect().height || 0;
	}
	setInitialStyles(initialHeight) {
		if (this.sheetScrollContainerRef.current && this.sheetMarginBoxRef.current) this.sheetScrollContainerRef.current.style.height = `${initialHeight}px`;
	}
	setStyles = ({ status, deltaHeight = 0 }) => {
		if (!this.sheetRef.current || !this.veilRef.current) return;
		const visibleHeight = this.sheetHeight - deltaHeight;
		const translate = status === "showing" ? `translate3d(0, -${visibleHeight}px, 0)` : "translate3d(0, 0, 0)";
		let opacity = 0;
		if (status === "showing") opacity = deltaHeight === 0 ? 1 : visibleHeight / this.sheetHeight;
		this.veilRef.current.style.opacity = String(opacity);
		this.sheetRef.current.style.transform = translate;
		if (this.isPrefersReducedMotion) {
			this.sheetRef.current.style.opacity = String(opacity);
			this.sheetRef.current.style.transform = `translate3d(0, -${visibleHeight}px, 0)`;
		}
	};
	getAvailableContentHeight = (sheetHeight) => {
		let heightCoefficient = DEFAULT_MAX_CONTENT_HEIGHT_FROM_VIEWPORT_COEFFICIENT;
		if (typeof this.props.maxContentHeightCoefficient === "number" && this.props.maxContentHeightCoefficient >= 0 && this.props.maxContentHeightCoefficient <= 1) heightCoefficient = this.props.maxContentHeightCoefficient;
		else if (typeof this.props.maxContentHeightCoefficient === "number") warnAboutOutOfRange();
		const availableViewportHeight = window.innerHeight * heightCoefficient - this.sheetTopHeight;
		if (this.props.alwaysFullHeight) return availableViewportHeight;
		return sheetHeight >= availableViewportHeight ? availableViewportHeight : sheetHeight;
	};
	show = () => {
		this.setState({ isAnimating: true }, () => {
			this.setStyles({ status: "showing" });
			this.setHash();
		});
	};
	hide = () => {
		this.setState({ isAnimating: true }, () => {
			this.setStyles({ status: "hiding" });
			this.removeHash();
		});
	};
	onSwipeAreaTouchStart = (e) => {
		this.velocityTracker.clear();
		this.setState({
			startY: e.nativeEvent.touches[0].clientY,
			swipeAreaTouched: true
		});
	};
	onContentTouchStart = (e) => {
		if (!this.props.allowHideOnContentScroll || this.state.swipeAreaTouched) return;
		this.velocityTracker.clear();
		this.setState({
			startY: e.nativeEvent.touches[0].clientY,
			startScrollTop: this.sheetScrollTop,
			contentTouched: true
		});
	};
	onSwipeAriaTouchMove = (e) => {
		const delta = e.nativeEvent.touches[0].clientY - this.state.startY;
		this.velocityTracker.addMovement({
			x: e.nativeEvent.touches[0].clientX,
			y: e.nativeEvent.touches[0].clientY
		});
		this.setState({ deltaY: delta });
		if (delta <= 0) return;
		this.setStyles({
			status: "showing",
			deltaHeight: delta
		});
	};
	onContentTouchMove = (e) => {
		if (!this.props.allowHideOnContentScroll) return;
		if (!this.state.startY) {
			this.onContentTouchStart(e);
			return;
		}
		const { startScrollTop, swipeAreaTouched } = this.state;
		if (swipeAreaTouched || this.sheetScrollTop > 0 || startScrollTop > 0 && startScrollTop !== this.sheetScrollTop) return;
		const delta = e.nativeEvent.touches[0].clientY - this.state.startY;
		this.velocityTracker.addMovement({
			x: e.nativeEvent.touches[0].clientX,
			y: e.nativeEvent.touches[0].clientY
		});
		if (delta <= 0) {
			this.setState({ deltaY: 0 });
			return;
		}
		this.setState({ deltaY: delta });
		this.setStyles({
			status: "showing",
			deltaHeight: delta
		});
	};
	onTouchEndAction = (deltaY) => {
		const accelerationY = this.velocityTracker.getYAcceleration();
		if (this.sheetHeight <= deltaY) this.props.hideSheet();
		else if (deltaY > HIDE_THRESHOLD && accelerationY <= ACCELERATION_Y_MAX && accelerationY >= ACCELERATION_Y_MIN || accelerationY > ACCELERATION_Y_MAX) this.hide();
		else if (deltaY !== 0) this.show();
	};
	onSwipeAriaTouchEnd = () => {
		const { deltaY } = this.state;
		this.onTouchEndAction(deltaY);
		this.setState({
			startY: 0,
			deltaY: 0,
			swipeAreaTouched: false
		});
	};
	onContentTouchEnd = () => {
		const { deltaY, swipeAreaTouched } = this.state;
		if (!this.props.allowHideOnContentScroll || swipeAreaTouched) return;
		this.onTouchEndAction(deltaY);
		this.setState({
			startY: 0,
			deltaY: 0,
			contentTouched: false
		});
	};
	onVeilClick = () => {
		if (this.state.isAnimating) return;
		this.setState({ veilTouched: true });
		this.hide();
	};
	onVeilTransitionEnd = () => {
		this.setState({ isAnimating: false });
		if (this.veilOpacity === "0") {
			this.props.hideSheet();
			return;
		}
		if (this.state.delayedResize) {
			this.onResizeWindow();
			this.setState({ delayedResize: false });
		}
	};
	onContentTransitionEnd = (e) => {
		if (e.propertyName === "height") {
			if (this.sheetScrollContainerRef.current) this.sheetScrollContainerRef.current.style.transition = "none";
		}
	};
	onResizeWindow = () => {
		if (this.state.isAnimating) {
			this.setState({ delayedResize: true });
			return;
		}
		this.setState({ inWindowResizeScope: true });
		if (this.resizeWindowTimer) window.clearTimeout(this.resizeWindowTimer);
		this.resizeWindowTimer = window.setTimeout(() => {
			this.onResize();
		}, WINDOW_RESIZE_TIMEOUT);
	};
	onResize = () => {
		if (!this.sheetRef.current || !this.sheetScrollContainerRef.current) return;
		const sheetContentHeight = this.sheetContentHeight;
		if (sheetContentHeight === this.state.prevSheetHeight && !this.state.inWindowResizeScope) return;
		const availableContentHeight = this.getAvailableContentHeight(sheetContentHeight);
		this.sheetScrollContainerRef.current.style.transition = this.state.prevSheetHeight > sheetContentHeight ? `height 0s ease ${TRANSITION_DURATION}` : "none";
		this.sheetScrollContainerRef.current.style.height = `${availableContentHeight}px`;
		this.sheetRef.current.style.transform = `translate3d(0, -${availableContentHeight + this.sheetTopHeight}px, 0)`;
		this.setState({
			prevSheetHeight: sheetContentHeight,
			inWindowResizeScope: false
		});
	};
	addListeners() {
		window.addEventListener("resize", this.onResizeWindow);
		if (this.sheetMarginBoxRef.current) {
			this.observer = new ResizeObserver(() => {
				if (!this.state.inWindowResizeScope) this.onResize();
			});
			this.observer.observe(this.sheetMarginBoxRef.current);
		}
	}
	removeListeners() {
		window.removeEventListener("resize", this.onResizeWindow);
		if (this.observer) this.observer.disconnect();
	}
	setHash() {
		const { id, platform, location, history } = this.props;
		if (platform === Platform.BROWSER) return;
		const newLocation = {
			...location,
			hash: id
		};
		switch (platform) {
			case Platform.IOS:
				if (location.hash) hashHistory.push(location.hash);
				history.replace(newLocation);
				break;
			case Platform.ANDROID:
				history.push(newLocation);
				break;
		}
	}
	removeHash() {
		const { id, platform, location, history } = this.props;
		if (platform === Platform.BROWSER || location.hash !== `#${id}`) return;
		switch (platform) {
			case Platform.IOS:
				history.replace({
					...location,
					hash: hashHistory.pop() ?? ""
				});
				break;
			case Platform.ANDROID:
				history.goBack();
				break;
		}
	}
	shouldClose(prevProps) {
		const { id, platform, location, history } = this.props;
		return platform !== Platform.BROWSER && history.action === "POP" && prevProps.location.hash !== location.hash && location.hash !== `#${id}`;
	}
	get isPrefersReducedMotion() {
		return Boolean(window?.matchMedia("(prefers-reduced-motion: reduce)").matches);
	}
};
function withRouterWrapper(Component) {
	const ComponentWithRouter = (props) => {
		const { useHistory, useLocation, ...remainingProps } = props;
		return (0, import_jsx_runtime.jsx)(Component, {
			...remainingProps,
			history: useHistory(),
			location: useLocation()
		});
	};
	ComponentWithRouter.displayName = `withRouterWrapper(${Component.displayName || Component.name || "Component"})`;
	return ComponentWithRouter;
}
var SheetContentContainer = withMobile(withRouterWrapper(SheetContent));
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Sheet/Sheet.js
var Sheet = (rawProps) => {
	const { children, onClose, visible, id, title, className, contentClassName, swipeAreaClassName, allowHideOnContentScroll, hideTopBar, maxContentHeightCoefficient, alwaysFullHeight, container, disablePortal, qa } = useDefaultProps("Sheet", rawProps);
	const [open, setOpen] = import_react.useState(visible);
	const [prevVisible, setPrevVisible] = import_react.useState(visible);
	if (!prevVisible && visible) setOpen(true);
	if (visible !== prevVisible) setPrevVisible(visible);
	const hideSheet = () => {
		if (onClose) onClose();
		setOpen(false);
	};
	if (!open) return null;
	return (0, import_jsx_runtime.jsx)(Portal, {
		container,
		disablePortal,
		children: (0, import_jsx_runtime.jsx)(FloatingOverlay, {
			"data-qa": qa,
			className: sheetBlock({ "without-top-bar": hideTopBar }, className),
			lockScroll: open,
			style: { overflow: void 0 },
			children: (0, import_jsx_runtime.jsx)(SheetContentContainer, {
				id,
				content: children,
				contentClassName,
				swipeAreaClassName,
				title,
				visible,
				allowHideOnContentScroll,
				hideTopBar,
				hideSheet,
				maxContentHeightCoefficient,
				alwaysFullHeight
			})
		})
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/FilePreview/FilePreviewActions/MobileActionsMenu/MobileActionsMenu.js
var cn$1 = block("file-preview-actions-mobile");
var renderListItem = (item) => {
	return (0, import_jsx_runtime.jsxs)("div", {
		className: cn$1("list-item"),
		children: [item.icon, (0, import_jsx_runtime.jsx)(Text, {
			variant: "body-2",
			title: item.title,
			ellipsis: true,
			children: item.title
		})]
	});
};
var MobileActionsMenu = ({ actions, fileName, isCustomImage }) => {
	const [showMobileMenu, setShowMobileMenu] = import_react.useState(false);
	const handleMobileMenuClose = import_react.useCallback(() => {
		setShowMobileMenu(false);
	}, []);
	const handleItemClick = import_react.useCallback((item, _, __, event) => {
		if (event) item.onClick?.(event);
		setShowMobileMenu(false);
	}, []);
	const handleMobileButtonClick = () => {
		setShowMobileMenu(true);
	};
	const buttonView = isCustomImage ? "raised" : "flat";
	return (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [(0, import_jsx_runtime.jsx)(Button, {
		view: buttonView,
		className: cn$1("actions-menu"),
		onClick: handleMobileButtonClick,
		size: "s",
		children: (0, import_jsx_runtime.jsx)(Icon, {
			data: EllipsisVertical,
			height: 16,
			width: 16
		})
	}), (0, import_jsx_runtime.jsx)(Sheet, {
		className: cn$1("sheet"),
		visible: showMobileMenu,
		onClose: handleMobileMenuClose,
		title: fileName,
		children: (0, import_jsx_runtime.jsx)(List, {
			items: actions,
			filterable: false,
			renderItem: renderListItem,
			itemHeight: 44,
			virtualized: false,
			onItemClick: handleItemClick
		})
	})] });
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/FilePreview/FilePreviewActions/FilePreviewActions.js
var FilePreviewActions = ({ actions, fileName, hoverabelPanelClassName, isCustomImage }) => {
	if (useMobile()) return (0, import_jsx_runtime.jsx)(MobileActionsMenu, {
		actions,
		fileName,
		isCustomImage
	});
	return (0, import_jsx_runtime.jsx)(DesktopActionsMenu, {
		actions,
		hoverabelPanelClassName
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/FilePreview/types.js
var FILE_TYPES = [
	"default",
	"image",
	"video",
	"code",
	"archive",
	"music",
	"audio",
	"text",
	"pdf",
	"table"
];
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/FilePreview/utils.js
var isFilePreviewFileType = (str) => FILE_TYPES.includes(str.toLowerCase());
var APPLICATION_MIME_TO_TYPE = {
	"application/x-compressed": "archive",
	"application/x-troff-msvideo": "video",
	"application/macbinary": "code",
	"application/mac-binary": "code",
	"application/x-binary": "code",
	"application/x-macbinary": "code",
	"application/bmp": "image",
	"application/x-bmp": "image",
	"application/x-win-bitmap": "image",
	"application/vnd.msexcel": "image",
	"application/vnd.openxmlformats-officedocument.wordprocessingml.document": "text",
	"application/x-msdownload": "code",
	"application/x-gzip": "archive",
	"application/java-archive": "code",
	"application/x-java-application": "code",
	"application/x-jar": "code",
	"application/x-javascript": "code",
	"application/json": "code",
	"application/ogg": "audio",
	"application/pdf": "pdf",
	"application/octet-stream": "pdf",
	"application/x-httpd-php": "code",
	"application/php": "code",
	"application/x-php": "code",
	"application/x-httpd-php-source": "code",
	"application/msword": "text",
	"application/x-rar": "archive",
	"application/rar": "archive",
	"application/x-rar-compressed": "archive",
	"application/x-tar": "archive",
	"application/x-gzip-compressed": "archive",
	"application/xhtml+xml": "code",
	"application/excel": "table",
	"application/msexcel": "table",
	"application/x-msexcel": "table",
	"application/x-ms-excel": "table",
	"application/x-excel": "table",
	"application/x-dos_ms_excel": "table",
	"application/xls": "table",
	"application/x-xls": "table",
	"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet": "table",
	"application/vnd.ms-excel": "table",
	"application/xml": "code",
	"application/x-zip": "archive",
	"application/zip": "archive",
	"application/x-zip-compressed": "archive",
	"application/s-compressed": "archive",
	"multipart/x-zip": "archive"
};
function getFileType(arg) {
	const fileType = typeof arg === "string" ? arg : arg.type;
	if (isFilePreviewFileType(fileType)) return fileType;
	const splittedFileType = fileType.split("/")[0];
	if (isFilePreviewFileType(splittedFileType)) return splittedFileType;
	return APPLICATION_MIME_TO_TYPE[fileType] || "default";
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/FilePreview/FilePreview.js
var cn = block("file-preview");
var FILE_ICON = {
	default: FileQuestion,
	image: Picture,
	video: Filmstrip,
	code: Code,
	archive: FileZipper,
	audio: MusicNote,
	music: MusicNote,
	text: TextAlignLeft,
	pdf: LogoAcrobat,
	table: LayoutHeaderCellsLarge
};
function FilePreview(rawProps) {
	const props = useDefaultProps("FilePreview", rawProps);
	const { className, qa, file, imageSrc, description, onClick, view = "default", selected } = props;
	const actions = view === "default" && "actions" in props ? props.actions : void 0;
	const [previewSrc, setPreviewSrc] = import_react.useState(imageSrc);
	const type = getFileType(file);
	const mobile = useMobile();
	const { onKeyDown } = useActionHandlers(onClick);
	import_react.useEffect(() => {
		if (imageSrc || type !== "image") return void 0;
		try {
			const createdUrl = URL.createObjectURL(file);
			setPreviewSrc(createdUrl);
			return () => {
				URL.revokeObjectURL(createdUrl);
			};
		} catch {
			return;
		}
	}, [
		file,
		imageSrc,
		type
	]);
	const clickable = Boolean(onClick);
	const withActions = Boolean(actions?.length);
	const isPreviewString = typeof previewSrc === "string";
	const compact = view === "compact";
	return (0, import_jsx_runtime.jsxs)("div", {
		className: cn({
			mobile,
			view
		}, className),
		"data-qa": qa,
		children: [(0, import_jsx_runtime.jsxs)("div", {
			className: cn("card", {
				clickable,
				hoverable: !selected && (clickable || withActions),
				selected
			}),
			role: clickable ? "button" : void 0,
			onKeyDown: clickable ? onKeyDown : void 0,
			tabIndex: clickable ? 0 : void 0,
			onClick,
			children: [isPreviewString ? (0, import_jsx_runtime.jsx)("div", {
				className: cn("image-container"),
				children: (0, import_jsx_runtime.jsx)("img", {
					className: cn("image"),
					src: previewSrc,
					alt: file.name
				})
			}) : (0, import_jsx_runtime.jsx)("div", {
				className: cn("icon-container"),
				children: (0, import_jsx_runtime.jsx)("div", {
					className: cn("icon", { type }),
					children: (0, import_jsx_runtime.jsx)(Icon, {
						className: cn("icon-svg"),
						data: FILE_ICON[type],
						size: 20
					})
				})
			}), !compact && (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [(0, import_jsx_runtime.jsx)(Text, {
				className: cn("name"),
				color: "secondary",
				ellipsis: true,
				title: file.name,
				children: file.name
			}), Boolean(description) && (0, import_jsx_runtime.jsx)(Text, {
				className: cn("description"),
				color: "secondary",
				ellipsis: true,
				title: description,
				children: description
			})] })]
		}), actions?.length && (0, import_jsx_runtime.jsx)(FilePreviewActions, {
			hoverabelPanelClassName: cn("actions-panel"),
			fileName: file.name,
			isCustomImage: isPreviewString,
			actions
		})]
	});
}
FilePreview.displayName = "FilePreview";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Label/constants.js
var LabelQa = {
	copyButton: "copy-button",
	closeButton: "close-button",
	mainButton: "main-button"
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Label/Label.js
var b$53 = block("label");
var iconSizeMap = {
	xxs: 12,
	xs: 12,
	s: 14,
	m: 16
};
var Label = import_react.forwardRef(function Label(rawProps, ref) {
	const { type = "default", theme = "normal", size = "xs", width, title, icon, children, onCloseClick, className, disabled, copyText, closeButtonLabel, copyButtonLabel, interactive = false, value, onCopy, onClick, qa, loading = false } = useDefaultProps("Label", rawProps);
	const hasContent = Boolean(children !== "" && import_react.Children.count(children) > 0);
	const typeClose = type === "close" && hasContent;
	const typeCopy = type === "copy" && hasContent;
	const typeInfo = type === "info";
	const hasOnClick = typeof onClick === "function";
	const hasCopy = Boolean(typeCopy && copyText);
	const isInteractive = (hasOnClick || hasCopy || typeInfo || interactive) && !disabled;
	const iconSize = iconSizeMap[size];
	const startIcon = icon && (0, import_jsx_runtime.jsx)("div", {
		className: b$53("addon", {
			side: hasContent ? "start" : void 0,
			type: "icon"
		}),
		children: icon
	});
	const content = hasContent && (0, import_jsx_runtime.jsxs)("div", {
		className: b$53("text"),
		children: [(0, import_jsx_runtime.jsx)("div", {
			className: b$53("content"),
			children
		}), Boolean(value) && (0, import_jsx_runtime.jsxs)("div", {
			className: b$53("value"),
			children: [(0, import_jsx_runtime.jsx)("div", {
				className: b$53("separator"),
				children: ":"
			}), (0, import_jsx_runtime.jsx)("div", {
				className: b$53("key"),
				children: value
			})]
		})]
	});
	const renderLabel = (status) => {
		let actionButton;
		if (typeCopy) actionButton = (0, import_jsx_runtime.jsx)("button", {
			type: "button",
			"aria-label": copyButtonLabel || void 0,
			onClick: hasOnClick ? onClick : void 0,
			disabled,
			className: b$53("addon", {
				side: "end",
				type: "button",
				action: hasOnClick ? "click" : "copy"
			}),
			"data-qa": LabelQa.copyButton,
			children: (0, import_jsx_runtime.jsx)(ClipboardIcon, {
				status: status || "pending",
				size: iconSize
			})
		});
		else if (typeInfo) actionButton = (0, import_jsx_runtime.jsx)("div", {
			className: b$53("addon", {
				side: "end",
				type: "icon"
			}),
			children: (0, import_jsx_runtime.jsx)(Icon, {
				size: iconSize,
				data: CircleInfo
			})
		});
		else if (typeClose) actionButton = (0, import_jsx_runtime.jsx)("button", {
			type: "button",
			onClick: onCloseClick,
			"aria-label": closeButtonLabel || void 0,
			disabled,
			className: b$53("addon", {
				side: "end",
				type: "button",
				action: "close"
			}),
			"data-qa": LabelQa.closeButton,
			children: (0, import_jsx_runtime.jsx)(Icon, {
				size: iconSize,
				data: Xmark
			})
		});
		return (0, import_jsx_runtime.jsxs)("div", {
			ref,
			className: b$53({
				theme,
				size,
				width,
				interactive: isInteractive,
				disabled
			}, className),
			title,
			"data-qa": qa,
			children: [
				!disabled && loading && (0, import_jsx_runtime.jsx)("div", { className: b$53("animation-container") }),
				startIcon,
				hasOnClick ? (0, import_jsx_runtime.jsx)("button", {
					disabled,
					type: "button",
					onClick,
					className: b$53("main-button"),
					"data-qa": LabelQa.mainButton,
					children: content
				}) : content,
				actionButton
			]
		});
	};
	if (hasCopy && copyText) return (0, import_jsx_runtime.jsx)(CopyToClipboard, {
		text: copyText,
		onCopy,
		timeout: 1e3,
		children: (status) => renderLabel(status)
	});
	return renderLabel();
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Link/Link.js
var b$52 = block("link");
var Link = import_react.forwardRef(function Link(rawProps, ref) {
	const { view = "normal", visitable = false, underline = false, href, children, extraProps, qa, onClickCapture, ...props } = useDefaultProps("Link", rawProps);
	const handleClickCapture = import_react.useCallback((event) => {
		eventBroker.publish({
			componentId: "Link",
			eventId: "click",
			domEvent: event
		});
		if (onClickCapture) onClickCapture(event);
	}, [onClickCapture]);
	return (0, import_jsx_runtime.jsx)("a", {
		...props,
		...extraProps,
		ref,
		href,
		rel: props.target === "_blank" && !props.rel ? "noopener noreferrer" : props.rel,
		onClickCapture: handleClickCapture,
		className: b$52({
			view,
			visitable,
			underline
		}, props.className),
		"data-qa": qa,
		children
	});
});
var i18n_default$9 = addComponentKeysets({
	en: {
		label_increment: "Increment",
		label_decrement: "Decrement"
	},
	ru: {
		label_increment: "Увеличить",
		label_decrement: "Уменьшить"
	}
}, "NumberInput");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/NumberInput/utils.js
var INCREMENT_BUTTON_QA = "increment-button-qa";
var DECREMENT_BUTTON_QA = "decrement-button-qa";
var CONTROL_BUTTONS_QA = "control-buttons-qa";
function getInputPattern(withoutFraction, positiveOnly = false) {
	return `^([${positiveOnly ? "" : "\\-"}\\+]?\\d+${withoutFraction ? "" : "(?:(?:.|,)?\\d+)?"})+$`;
}
var pastedInputParsingRegex = /^([-+]?)(?:\D*)(\d*)(\.|,)?(\d*)(?:\D*)$/;
function prepareStringValue(value) {
	return value.replace(",", ".").replace(/\s/g, "").replace(/[^\d.+-]/g, "");
}
function getPossibleNumberSubstring(value, allowDecimal) {
	const preparedString = prepareStringValue(value);
	const match = pastedInputParsingRegex.exec(preparedString);
	if (!match || value.length > 0 && preparedString.length === 0) return;
	return [
		match[1],
		match[2],
		allowDecimal ? match[3] : void 0,
		match[4]
	].filter(Boolean).join("");
}
function getParsedValue(value) {
	if (value === void 0 || value === "") return {
		valid: true,
		value: null
	};
	const parsedValueOrNaN = Number(value);
	const isValidValue = !Number.isNaN(parsedValueOrNaN);
	return {
		valid: isValidValue,
		value: isValidValue ? parsedValueOrNaN : null
	};
}
function roundIfNecessary(value, allowDecimal) {
	return allowDecimal ? value : Math.floor(value);
}
function getInternalState(props) {
	const { min: externalMin, max: externalMax, step: externalStep = 1, shiftMultiplier: externalShiftMultiplier, value: externalValue, allowDecimal, defaultValue: externalDefaultValue } = props;
	const { min: rangedMin, max: rangedMax } = externalMin && externalMax && externalMin > externalMax ? {
		min: externalMax,
		max: externalMin
	} : {
		min: externalMin,
		max: externalMax
	};
	return {
		min: rangedMin !== void 0 && rangedMin >= Number.MIN_SAFE_INTEGER ? rangedMin : void 0,
		max: rangedMax !== void 0 && rangedMax <= Number.MAX_SAFE_INTEGER ? rangedMax : void 0,
		step: roundIfNecessary(Math.abs(externalStep), allowDecimal) || 1,
		shiftMultiplier: roundIfNecessary(externalShiftMultiplier, allowDecimal) || 10,
		value: externalValue ? roundIfNecessary(externalValue, allowDecimal) : externalValue,
		defaultValue: externalDefaultValue ? roundIfNecessary(externalDefaultValue, allowDecimal) : externalDefaultValue
	};
}
function clampToNearestStepValue({ value, step, min: originalMin, max = Number.MAX_SAFE_INTEGER, direction }) {
	const base = originalMin || 0;
	const min = originalMin ?? Number.MIN_SAFE_INTEGER;
	let clampedValue = toFixedNumber(value, step);
	if (clampedValue > max) clampedValue = max;
	else if (clampedValue < min) clampedValue = min;
	if (!Number.isInteger(value) || !Number.isInteger(step)) return clampedValue;
	const amountOfStepsDiff = Math.floor((clampedValue - base) / step);
	if (clampedValue - base - step * amountOfStepsDiff !== 0) {
		const smallerPossibleValue = base + amountOfStepsDiff * step;
		const greaterPossibleValue = base + (amountOfStepsDiff + 1) * step;
		const smallerValueIsPreferrable = direction ? direction === "up" : greaterPossibleValue - clampedValue > clampedValue - smallerPossibleValue;
		if ((greaterPossibleValue > max || smallerValueIsPreferrable) && smallerPossibleValue >= min) return smallerPossibleValue;
		if (greaterPossibleValue <= max) return greaterPossibleValue;
	}
	return toFixedNumber(clampedValue, step);
}
function updateCursorPosition(inputRef, eventRawValue = "", computedEventValue = "") {
	const currentSelectionEndPosition = inputRef.current?.selectionEnd ?? eventRawValue.length;
	if (eventRawValue !== computedEventValue) {
		const startingPossiblyChangedPart = eventRawValue.slice(0, currentSelectionEndPosition);
		const trailingUnchangedLength = eventRawValue.length - startingPossiblyChangedPart.length;
		const newStartingPart = computedEventValue.slice(0, computedEventValue.length - trailingUnchangedLength);
		inputRef.current?.setRangeText(newStartingPart, 0, startingPossiblyChangedPart.length, "end");
	}
}
function areStringRepresentationOfNumbersEqual(v1, v2) {
	if (v1 === v2) return true;
	const { valid: v1Valid, value: v1Value } = getParsedValue(v1);
	const { valid: v2Valid, value: v2Value } = getParsedValue(v2);
	if (v1Valid && v2Valid) return v1Value === v2Value;
	const v1OnlyNumbers = v1.replace(/\D/g, "");
	const v2OnlyNumbers = v2.replace(/\D/g, "");
	if (v1OnlyNumbers.length === v2OnlyNumbers.length && v1OnlyNumbers.length === 0) return true;
	return false;
}
function toFixedNumber(value, baseStep) {
	const stepDecimalDigits = baseStep.toString().split(".")[1]?.length || 0;
	return parseFloat(value.toFixed(stepDecimalDigits));
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/NumberInput/NumericArrows/NumericArrows.js
var b$51 = block("numeric-arrows");
function NumericArrows({ className, size, disabled, onUpClick, onDownClick, ...restProps }) {
	const commonBtnProps = {
		size: "s",
		pin: "brick-brick",
		view: "flat-secondary",
		disabled,
		tabIndex: -1,
		width: "max",
		"aria-hidden": "true"
	};
	const { t } = i18n_default$9.useTranslation();
	return (0, import_jsx_runtime.jsxs)(Flex, {
		direction: "column",
		className: b$51({ size }, className),
		qa: CONTROL_BUTTONS_QA,
		...restProps,
		children: [
			(0, import_jsx_runtime.jsx)(Button, {
				className: b$51("arrow-btn"),
				qa: INCREMENT_BUTTON_QA,
				...commonBtnProps,
				onClick: onUpClick,
				"aria-label": t("label_increment"),
				children: (0, import_jsx_runtime.jsx)(Icon, {
					data: ChevronUp,
					size: 12
				})
			}),
			(0, import_jsx_runtime.jsx)("span", { className: b$51("separator") }),
			(0, import_jsx_runtime.jsx)(Button, {
				className: b$51("arrow-btn"),
				qa: DECREMENT_BUTTON_QA,
				...commonBtnProps,
				onClick: onDownClick,
				"aria-label": t("label_decrement"),
				children: (0, import_jsx_runtime.jsx)(Icon, {
					data: ChevronDown,
					size: 12
				})
			})
		]
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/NumberInput/NumberInput.js
var b$50 = block("number-input");
function getStringValue(value) {
	return value === null ? "" : String(value);
}
var NumberInput = import_react.forwardRef(function NumberInput(rawProps, ref) {
	const { endContent, defaultValue: externalDefaultValue, ...props } = useDefaultProps("NumberInput", rawProps);
	const { value: externalValue, onChange: handleChange, onUpdate: externalOnUpdate, min: externalMin, max: externalMax, shiftMultiplier: externalShiftMultiplier = 10, step: externalStep, size = "m", view = "normal", disabled, hiddenControls, validationState, onBlur, onKeyDown, allowDecimal = false, className } = props;
	const { min, max, step: baseStep, value: internalValue, defaultValue, shiftMultiplier } = getInternalState({
		min: externalMin,
		max: externalMax,
		step: externalStep,
		shiftMultiplier: externalShiftMultiplier,
		allowDecimal,
		value: externalValue,
		defaultValue: externalDefaultValue
	});
	const [value, setValue] = useControlledState(internalValue, defaultValue ?? null, externalOnUpdate);
	const [inputValue, setInputValue] = import_react.useState(getStringValue(value));
	import_react.useEffect(() => {
		const stringPropsValue = getStringValue(value);
		if (!areStringRepresentationOfNumbersEqual(inputValue, stringPropsValue)) setInputValue(stringPropsValue);
	}, [value, inputValue]);
	const clamp = !(allowDecimal && !externalStep);
	const safeValue = value ?? 0;
	const state = getInputControlState(validationState);
	const canIncrementNumber = safeValue < (max ?? Number.MAX_SAFE_INTEGER);
	const canDecrementNumber = safeValue > (min ?? Number.MIN_SAFE_INTEGER);
	const innerControlRef = import_react.useRef(null);
	const fieldRef = useFormResetHandler({
		initialValue: value,
		onReset: setValue
	});
	const handleRef = useForkRef(props.controlRef, innerControlRef, fieldRef);
	const handleValueDelta = (e, direction) => {
		const step = e.shiftKey ? shiftMultiplier * baseStep : baseStep;
		const deltaWithSign = direction === "up" ? step : -step;
		if (direction === "up" ? canIncrementNumber : canDecrementNumber) {
			const newValue = clampToNearestStepValue({
				value: safeValue + deltaWithSign,
				step: baseStep,
				min,
				max,
				direction
			});
			setValue?.(newValue);
			setInputValue(newValue.toString());
		}
	};
	const handleKeyDown = (e) => {
		if (e.key === KeyCode$1.ARROW_DOWN) {
			e.preventDefault();
			handleValueDelta(e, "down");
		} else if (e.key === KeyCode$1.ARROW_UP) {
			e.preventDefault();
			handleValueDelta(e, "up");
		} else if (e.key === KeyCode$1.HOME) {
			e.preventDefault();
			if (min !== void 0) {
				setValue?.(min);
				setInputValue(min.toString());
			}
		} else if (e.key === KeyCode$1.END) {
			e.preventDefault();
			if (max !== void 0) {
				const newValue = clampToNearestStepValue({
					value: max,
					step: baseStep,
					min,
					max
				});
				setValue?.(newValue);
				setInputValue(newValue.toString());
			}
		}
		onKeyDown?.(e);
	};
	const handleBlur = (e) => {
		if (clamp && value !== null) {
			const clampedValue = clampToNearestStepValue({
				value,
				step: baseStep,
				min,
				max
			});
			if (value !== clampedValue) setValue?.(clampedValue);
			setInputValue(clampedValue.toString());
		}
		onBlur?.(e);
	};
	const handleUpdate = (v) => {
		setInputValue(v);
		const preparedStringValue = getPossibleNumberSubstring(v, allowDecimal);
		updateCursorPosition(innerControlRef, v, preparedStringValue);
		const { valid, value: parsedNumberValue } = getParsedValue(preparedStringValue);
		if (valid && parsedNumberValue !== value) setValue?.(parsedNumberValue);
	};
	const handleInput = (e) => {
		const preparedStringValue = getPossibleNumberSubstring(e.currentTarget.value, allowDecimal);
		updateCursorPosition(innerControlRef, e.currentTarget.value, preparedStringValue);
	};
	return (0, import_jsx_runtime.jsx)(TextInput, {
		...props,
		className: b$50({
			size,
			view,
			state
		}, className),
		controlProps: {
			...props.controlProps,
			onInput: handleInput,
			role: "spinbutton",
			inputMode: allowDecimal ? "decimal" : "numeric",
			pattern: props.controlProps?.pattern ?? getInputPattern(!allowDecimal, false),
			"aria-valuemin": props.min,
			"aria-valuemax": props.max,
			"aria-valuenow": value === null ? void 0 : value
		},
		controlRef: handleRef,
		value: inputValue,
		onChange: handleChange,
		onUpdate: handleUpdate,
		onKeyDown: handleKeyDown,
		onBlur: handleBlur,
		ref,
		endContent: (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [endContent, hiddenControls ? null : (0, import_jsx_runtime.jsx)(NumericArrows, {
			className: b$50("arrows"),
			size,
			disabled,
			onUpClick: (e) => {
				innerControlRef.current?.focus();
				handleValueDelta(e, "up");
			},
			onDownClick: (e) => {
				innerControlRef.current?.focus();
				handleValueDelta(e, "down");
			}
		})] })
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Overlay/Overlay.js
var b$49 = block("overlay");
function Overlay(rawProps) {
	const { className, background = "base", visible = false, children } = useDefaultProps("Overlay", rawProps);
	return (0, import_jsx_runtime.jsxs)("div", {
		className: b$49({ visible }, className),
		children: [(0, import_jsx_runtime.jsx)("div", { className: b$49("background", { style: background }) }), children && (0, import_jsx_runtime.jsx)("div", {
			className: b$49("children"),
			children
		})]
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/components/PaginationEllipsis/PaginationEllipsis.js
var b$48 = block("pagination-ellipsis");
var PaginationEllipsis = ({ size, className }) => {
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$48({ size }, className),
		children: "..."
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/constants.js
var PaginationQa = {
	PaginationPageSizer: "pagination-page-sizer",
	PaginationPageSizerOption: "pagination-page-sizer-option",
	PaginationInput: "pagination-input",
	PaginationPage: "pagination-page",
	PaginationButtonFirst: "pagination-button-first",
	PaginationButtonPrevious: "pagination-button-previous",
	PaginationButtonNext: "pagination-button-next"
};
var getPaginationPageQa = (pageNumber) => {
	return `${PaginationQa.PaginationPage}-${pageNumber}`;
};
var getPaginationPageSizeOptionQa = (pageSizeOption) => {
	return `${PaginationQa.PaginationPageSizerOption}-${pageSizeOption}`;
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/components/PaginationPage/PaginationPage.js
var b$47 = block("pagination-page");
var PaginationPage = ({ item, size, pageSize, className, onUpdate }) => {
	const qa = getPaginationPageQa(item.page);
	if (item.simple) return (0, import_jsx_runtime.jsx)("div", {
		"data-qa": qa,
		className: b$47("simple", { size }, className),
		children: item.page
	});
	const view = item.current ? "normal" : "flat";
	return (0, import_jsx_runtime.jsx)(Button, {
		size,
		view,
		selected: item.current,
		className,
		onClick: () => onUpdate(item.page, pageSize),
		qa,
		children: item.page
	}, view);
};
var i18n_default$8 = addComponentKeysets({
	en: {
		button_previous: "Previous",
		button_next: "Next",
		button_first: "First",
		"label_input-placeholder": "Page #",
		"label_page-of": "of",
		label_select_size: "Select page size"
	},
	ru: {
		button_previous: "Предыдущая",
		button_next: "Следующая",
		button_first: "Первая",
		"label_input-placeholder": "Стр.",
		"label_page-of": "из",
		label_select_size: "Выбрать размер страницы"
	}
}, "Pagination");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/components/PaginationPageOf/PaginationPageOf.js
var b$46 = block("pagination-page-of");
var PaginationPageOf = ({ size, className }) => {
	const { t } = i18n_default$8.useTranslation();
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$46({ size }, className),
		children: t("label_page-of")
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/components/PaginationButton/PaginationButton.js
var PaginationButton = ({ item, size, className, page, pageSize, onUpdate, compact, view }) => {
	let button = null;
	const { disabled } = item;
	const { t } = i18n_default$8.useTranslation();
	switch (item.action) {
		case "first":
			button = (0, import_jsx_runtime.jsxs)(Button, {
				size,
				view,
				className,
				onClick: () => onUpdate(1, pageSize),
				title: compact ? t("button_first") : void 0,
				disabled,
				qa: PaginationQa.PaginationButtonFirst,
				children: [(0, import_jsx_runtime.jsx)(Icon, {
					data: ChevronsLeft,
					size: "16"
				}), compact ? void 0 : t("button_first")]
			});
			break;
		case "previous":
			button = (0, import_jsx_runtime.jsxs)(Button, {
				size,
				view,
				className,
				onClick: () => onUpdate(page - 1, pageSize),
				title: compact ? t("button_previous") : void 0,
				disabled,
				qa: PaginationQa.PaginationButtonPrevious,
				children: [(0, import_jsx_runtime.jsx)(Icon, {
					data: ChevronLeft,
					size: "16"
				}), compact ? void 0 : t("button_previous")]
			});
			break;
		case "next":
			button = (0, import_jsx_runtime.jsxs)(Button, {
				size,
				view,
				className,
				onClick: () => onUpdate(page + 1, pageSize),
				title: compact ? t("button_next") : void 0,
				disabled,
				qa: PaginationQa.PaginationButtonNext,
				children: [(0, import_jsx_runtime.jsx)(Icon, {
					data: ChevronRight,
					size: "16"
				}), compact ? void 0 : t("button_next")]
			});
			break;
	}
	return button;
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/components/PaginationInput/PaginationInput.js
var b$45 = block("pagination-input");
var PaginationInput = ({ numberOfPages, size, pageSize, onUpdate, className, view }) => {
	const [value, setValue] = import_react.useState("");
	const handleUpdateValue = (inputValue) => {
		if (inputValue === "" || /^[1-9][0-9]*$/.test(inputValue)) setValue(inputValue);
	};
	const handleUpdate = (inputValue) => {
		if (!inputValue) return;
		let numValue = Number(inputValue);
		if (!Number.isInteger(numValue)) {
			setValue("");
			return;
		}
		const hasUpperLimit = numberOfPages > 0;
		if (numValue > numberOfPages) numValue = hasUpperLimit ? numberOfPages : numValue;
		else if (numValue < 1) numValue = 1;
		setValue("");
		onUpdate(numValue, pageSize);
	};
	const handleBlur = (event) => handleUpdate(event.currentTarget.value);
	const handleKeyUp = (event) => {
		if (event.key === KeyCode$1.ENTER) handleUpdate(event.currentTarget.value);
	};
	const { t } = i18n_default$8.useTranslation();
	return (0, import_jsx_runtime.jsx)(TextInput, {
		className: b$45({ size }, className),
		placeholder: t("label_input-placeholder"),
		size,
		value,
		onUpdate: handleUpdateValue,
		onBlur: handleBlur,
		onKeyUp: handleKeyUp,
		qa: PaginationQa.PaginationInput,
		view
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/EmptyOptions/EmptyOptions.js
var b$44 = block("select-empty-placeholder");
var EmptyOptions = ({ renderEmptyOptions, filter }) => {
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$44({ empty: !renderEmptyOptions }),
		children: renderEmptyOptions?.({ filter })
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/constants.js
var selectBlock = block("select");
var selectControlBlock = block("select-control");
var selectControlButtonBlock = block("select-control__button");
var selectListBlock = block("select-list");
var selectClearBlock = block("select-clear");
var SIZE_TO_ITEM_HEIGHT = {
	s: 28,
	m: 28,
	l: 32,
	xl: 36
};
var QUICK_SEARCH_TIMEOUT = 2e3;
var SelectQa = {
	LIST: "select-list",
	POPUP: "select-popup",
	SHEET: "select-sheet",
	CLEAR: "select-clear",
	FILTER_INPUT: "select-filter-input",
	COUNTER: "select-counter"
};
var FLATTEN_KEY = Symbol("flatten");
var i18n_default$7 = addComponentKeysets({
	en: {
		label_clear: "Clear",
		"label_show-error-info": "Show popup with error info",
		label_filter: "Filter"
	},
	ru: {
		label_clear: "Очистить",
		"label_show-error-info": "Показать попап с информацей об ошибке",
		label_filter: "Фильтр"
	}
}, "Select");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectClear/SelectClear.js
var SelectClear = (props) => {
	const { size, onClick, onMouseEnter, onMouseLeave, renderIcon } = props;
	const { t } = i18n_default$7.useTranslation();
	const icon = renderIcon ? renderIcon() : (0, import_jsx_runtime.jsx)(Icon, {
		className: selectClearBlock("clear"),
		data: Xmark
	});
	return (0, import_jsx_runtime.jsx)("button", {
		className: selectClearBlock({ size }),
		"aria-label": t("label_clear"),
		onClick,
		onMouseEnter,
		onMouseLeave,
		"data-qa": SelectQa.CLEAR,
		type: "button",
		children: icon
	});
};
SelectClear.displayName = "SelectClear";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectCounter/SelectCounter.js
var b$43 = block("select-counter");
function SelectCounter({ count, size, disabled }) {
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$43({ size }),
		children: (0, import_jsx_runtime.jsx)(Text, {
			variant: size === "xl" ? "body-2" : "body-1",
			color: disabled ? "hint" : "primary",
			className: b$43("text"),
			qa: SelectQa.COUNTER,
			children: count
		})
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectControl/SelectControl.js
var SelectControl = import_react.forwardRef((props, ref) => {
	const { toggleOpen, clearValue, onKeyDown, renderControl, view, size, pin, selectedOptionsContent, className, qa, label, placeholder, isErrorVisible, errorMessage, open, disabled, value, hasClear, popupId, selectId, activeIndex, renderCounter, hasCounter, title } = props;
	const showOptionsText = Boolean(selectedOptionsContent);
	const showPlaceholder = Boolean(placeholder && !showOptionsText);
	const hasValue = Array.isArray(value) && value.filter(Boolean).length > 0;
	const direction = useDirection();
	const [isDisabledButtonAnimation, setIsDisabledButtonAnimation] = import_react.useState(false);
	const controlMods = {
		open,
		size,
		pin,
		disabled,
		error: isErrorVisible,
		"has-clear": hasClear,
		"no-active": isDisabledButtonAnimation,
		"has-value": hasValue
	};
	const buttonMods = {
		open,
		size,
		view,
		pin,
		disabled,
		error: isErrorVisible
	};
	const handleControlClick = import_react.useCallback((e) => {
		if (e && e.currentTarget !== document.activeElement && "focus" in e.currentTarget) e.currentTarget.focus();
		toggleOpen();
	}, [toggleOpen]);
	const disableButtonAnimation = import_react.useCallback(() => {
		setIsDisabledButtonAnimation(true);
	}, []);
	const enableButtonAnimation = import_react.useCallback(() => {
		setIsDisabledButtonAnimation(false);
	}, []);
	const handleOnClearIconClick = import_react.useCallback(() => {
		setIsDisabledButtonAnimation(false);
		clearValue();
	}, [clearValue]);
	const renderCounterComponent = () => {
		if (!hasCounter) return null;
		const count = value.length;
		const counterComponent = (0, import_jsx_runtime.jsx)(SelectCounter, {
			count,
			size,
			disabled
		});
		return renderCounter ? renderCounter(counterComponent, {
			count,
			size,
			disabled
		}) : counterComponent;
	};
	const renderClearIcon = (args) => {
		const valueIsEmpty = value.length === 0;
		if (!hasClear || valueIsEmpty || disabled) return null;
		return (0, import_jsx_runtime.jsx)(SelectClear, {
			size,
			onClick: handleOnClearIconClick,
			onMouseEnter: disableButtonAnimation,
			onMouseLeave: enableButtonAnimation,
			renderIcon: args.renderIcon
		});
	};
	const triggerProps = {
		...filterDOMProps(props, { labelable: true }),
		id: selectId,
		role: "combobox",
		"aria-controls": open ? popupId : void 0,
		"aria-haspopup": "listbox",
		"aria-expanded": open,
		"aria-invalid": isErrorVisible || void 0,
		"aria-activedescendant": activeIndex === void 0 ? void 0 : `${popupId}-item-${activeIndex}`,
		onClick: handleControlClick,
		onKeyDown,
		disabled
	};
	const { t } = i18n_default$7.useTranslation();
	if (renderControl) return renderControl({
		onClear: clearValue,
		renderClear: renderClearIcon,
		renderCounter: renderCounterComponent,
		ref,
		open,
		disabled,
		triggerProps
	}, { value });
	return (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: (0, import_jsx_runtime.jsxs)("div", {
		className: selectControlBlock(controlMods),
		role: "group",
		children: [
			(0, import_jsx_runtime.jsxs)("button", {
				ref,
				className: selectControlButtonBlock(buttonMods, className),
				type: "button",
				"data-qa": qa,
				title,
				tabIndex: 0,
				...triggerProps,
				children: [
					label && (0, import_jsx_runtime.jsx)("span", {
						className: selectControlBlock("label"),
						children: label
					}),
					showPlaceholder && (0, import_jsx_runtime.jsx)("span", {
						className: selectControlBlock("placeholder"),
						children: placeholder
					}),
					showOptionsText && (0, import_jsx_runtime.jsx)("span", {
						className: selectControlBlock("option-text"),
						children: selectedOptionsContent
					})
				]
			}),
			renderCounterComponent(),
			renderClearIcon({}),
			errorMessage && (0, import_jsx_runtime.jsx)(Popover, {
				placement: direction === "rtl" ? ["left", "bottom"] : ["right", "bottom"],
				hasArrow: true,
				className: selectControlBlock("error-popover"),
				content: (0, import_jsx_runtime.jsx)(Alert, {
					className: selectControlBlock("error-popover-content"),
					theme: "clear",
					message: (0, import_jsx_runtime.jsx)("div", {
						role: "presentation",
						children: errorMessage
					})
				}),
				children: (0, import_jsx_runtime.jsx)("button", {
					"aria-label": t("label_show-error-info"),
					type: "button",
					className: selectControlBlock("error-icon"),
					children: (0, import_jsx_runtime.jsx)(Icon, {
						data: TriangleExclamation,
						size: size === "s" ? 12 : 16
					})
				})
			}),
			(0, import_jsx_runtime.jsx)(Icon, {
				className: selectControlBlock("chevron-icon", { disabled }),
				data: ChevronDown,
				"aria-hidden": "true"
			})
		]
	}) });
});
SelectControl.displayName = "SelectControl";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectFilter/SelectFilter.js
var b$42 = block("select-filter");
var style = { padding: "4px 4px 0" };
var SelectFilter = import_react.forwardRef((props, ref) => {
	const { onChange, onKeyDown, renderFilter, size, value, placeholder, popupId, activeIndex } = props;
	const inputRef = import_react.useRef(null);
	import_react.useImperativeHandle(ref, () => ({ focus: () => inputRef.current?.focus({ preventScroll: true }) }), []);
	const { t } = i18n_default$7.useTranslation();
	const inputProps = {
		value,
		placeholder,
		size: 1,
		onKeyDown,
		onChange: (e) => {
			onChange(e.target.value);
		},
		"aria-label": t("label_filter"),
		"aria-controls": popupId,
		"aria-activedescendant": activeIndex === void 0 ? void 0 : `${popupId}-item-${activeIndex}`
	};
	if (renderFilter) return renderFilter({
		onChange,
		onKeyDown,
		value,
		ref: inputRef,
		style,
		inputProps
	});
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$42(),
		style,
		children: (0, import_jsx_runtime.jsx)(TextInput, {
			controlRef: inputRef,
			controlProps: {
				className: b$42("input"),
				size: 1,
				"aria-label": inputProps["aria-label"],
				"aria-controls": inputProps["aria-controls"],
				"aria-activedescendant": inputProps["aria-activedescendant"]
			},
			size,
			value,
			placeholder,
			onUpdate: onChange,
			onKeyDown,
			qa: SelectQa.FILTER_INPUT
		})
	});
});
SelectFilter.displayName = "SelectFilter";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/utils.js
var isSelectGroupTitle = (option) => {
	return Boolean(option && "label" in option);
};
var getFlattenOptions = (options) => {
	const flatten = options.reduce((acc, option) => {
		if ("label" in option) {
			acc.push({
				label: option.label,
				disabled: true,
				data: option.data
			});
			acc.push(...option.options || []);
		} else acc.push(option);
		return acc;
	}, []);
	Object.defineProperty(flatten, FLATTEN_KEY, {
		enumerable: false,
		value: {}
	});
	return flatten;
};
var getPopupItemHeight = (args) => {
	const { getOptionHeight, getOptionGroupHeight, size, option, index, mobile } = args;
	let itemHeight = mobile ? 32 : SIZE_TO_ITEM_HEIGHT[size];
	if (isSelectGroupTitle(option)) {
		const marginTop = index === 0 ? 0 : 5;
		itemHeight = option.label === "" ? 0 : itemHeight;
		return getOptionGroupHeight ? getOptionGroupHeight(option, index) : itemHeight + marginTop;
	}
	return getOptionHeight ? getOptionHeight(option, index) : itemHeight;
};
var getOptionsHeight = (args) => {
	const { getOptionHeight, getOptionGroupHeight, size, options, mobile } = args;
	return options.reduce((height, option, index) => {
		return height + getPopupItemHeight({
			getOptionHeight,
			getOptionGroupHeight,
			size,
			option,
			index,
			mobile
		});
	}, 0);
};
var getOptionText = (option) => {
	if (typeof option.content === "string") return option.content;
	if (typeof option.children === "string") return option.children;
	if (option.text) return option.text;
	return option.value;
};
var getSelectedOptionsContent = (options, value, renderSelectedOption) => {
	if (value.length === 0) return null;
	const flattenSimpleOptions = options.filter((opt) => !isSelectGroupTitle(opt));
	const optionsMap = new Map(flattenSimpleOptions.map((opt) => [opt.value, opt]));
	const selectedOptions = value.map((val) => {
		return optionsMap.get(val) || { value: val };
	});
	if (renderSelectedOption) return selectedOptions.map((option, index) => {
		return (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: renderSelectedOption(option, index) }, option.value);
	});
	else return selectedOptions.map((option) => {
		return getOptionText(option);
	}).join(", ");
};
var getTypedChildrenArray = (children) => {
	return import_react.Children.toArray(children);
};
var getOptionsFromOptgroupChildren = (children) => {
	return import_react.Children.toArray(children).reduce((acc, { props }) => {
		if ("value" in props) acc.push(props);
		return acc;
	}, []);
};
var getOptionsFromChildren = (children) => {
	return getTypedChildrenArray(children).reduce((acc, { props }) => {
		if ("label" in props) {
			const options = props.options || getOptionsFromOptgroupChildren(props.children);
			acc.push({
				options,
				label: props.label
			});
		}
		if ("value" in props) acc.push({ ...props });
		return acc;
	}, []);
};
var getNextQuickSearch = (keyCode, quickSearch) => {
	const writingSystemKeyPressed = keyCode.length === 1;
	const backspacePressed = keyCode === KeyCode$1.BACKSPACE;
	let nextQuickSearch = "";
	if (backspacePressed && quickSearch.length) nextQuickSearch = quickSearch.slice(0, quickSearch.length - 1);
	else if (writingSystemKeyPressed) nextQuickSearch = (quickSearch + keyCode).trim();
	return nextQuickSearch;
};
var getEscapedRegExp = (string) => {
	return new RegExp(string.replace(/[-/\\^$*+?.()|[\]{}]/g, "\\$&"), "i");
};
var findItemIndexByQuickSearch = (quickSearch, items) => {
	if (!items) return -1;
	return items.findIndex((item) => {
		if (isSelectGroupTitle(item)) return false;
		if (item.disabled) return false;
		const optionText = getOptionText(item);
		return getEscapedRegExp(quickSearch).test(optionText);
	});
};
var getListItems = (listRef) => {
	return listRef?.current?.getItems() || [];
};
var getActiveItem = (listRef) => {
	const items = getListItems(listRef);
	const activeItemIndex = listRef?.current?.getActiveItem();
	return typeof activeItemIndex === "number" ? items[activeItemIndex] : void 0;
};
var isOptionMatchedByFilter = (option, filter) => {
	const lowerOptionText = getOptionText(option).toLocaleLowerCase();
	const lowerFilter = filter.toLocaleLowerCase();
	return lowerOptionText.indexOf(lowerFilter) !== -1;
};
var getFilteredFlattenOptions = (args) => {
	const { options, filter, filterOption } = args;
	const filteredOptions = options.filter((option) => {
		if (isSelectGroupTitle(option)) return true;
		return filterOption ? filterOption(option, filter) : isOptionMatchedByFilter(option, filter);
	});
	return filteredOptions.reduce((acc, option, index) => {
		const groupTitle = isSelectGroupTitle(option);
		const previousGroupTitle = isSelectGroupTitle(acc[acc.length - 1]);
		const isLastOption = index === filteredOptions.length - 1;
		if (groupTitle && previousGroupTitle) acc.pop();
		if (!groupTitle || groupTitle && !isLastOption) acc.push(option);
		return acc;
	}, []);
};
function scrollToItem(node) {
	const container = node.offsetParent;
	if (container instanceof HTMLElement) {
		const height = container.offsetHeight;
		const scrollTop = container.scrollTop;
		const top = node.offsetTop;
		if (top + node.offsetHeight >= scrollTop + height) container.scrollTo({ top: top - height + node.offsetHeight });
		else if (top <= scrollTop) container.scrollTo({ top });
	}
	return true;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectList/GroupLabel.js
var b$41 = block("select-list");
var GroupLabel = ({ option, renderOptionGroup }) => {
	if (renderOptionGroup) return (0, import_jsx_runtime.jsx)("div", {
		className: b$41("group-label-custom"),
		children: renderOptionGroup(option)
	});
	else return (0, import_jsx_runtime.jsx)("div", {
		className: b$41("group-label", { empty: option.label === "" }),
		children: (0, import_jsx_runtime.jsx)("div", {
			className: b$41("group-label-content"),
			children: option.label
		})
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectList/OptionWrap.js
var b$40 = block("select-list");
var DefaultOption = ({ option }) => {
	const { content, children, disabled, title } = option;
	return (0, import_jsx_runtime.jsx)("span", {
		title,
		className: b$40("option-default-label", { disabled }),
		children: content || children
	});
};
var OptionWrap = (props) => {
	const { renderOption, value, option, multiple } = props;
	const selected = value.indexOf(option.value) !== -1;
	const optionContent = renderOption ? renderOption(option) : (0, import_jsx_runtime.jsx)(DefaultOption, { option });
	return (0, import_jsx_runtime.jsxs)("div", {
		"data-qa": option.qa,
		className: b$40("option", { disabled: option.disabled }),
		children: [multiple && (0, import_jsx_runtime.jsx)(Icon, {
			className: b$40("tick-icon", { shown: selected && multiple }),
			data: Check
		}), optionContent]
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectList/SelectLoadingIndicator.js
var SelectLoadingIndicator = (props) => {
	const ref = import_react.useRef(null);
	useIntersection({
		element: ref.current,
		onIntersect: props?.onIntersect
	});
	return (0, import_jsx_runtime.jsx)("div", {
		ref,
		className: selectListBlock("loading-indicator"),
		children: (0, import_jsx_runtime.jsx)(Loader, {})
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectList/SelectList.js
var loadingOption = {
	value: "__SELECT_LIST_ITEM_LOADING__",
	disabled: true
};
var SelectList = import_react.forwardRef((props, ref) => {
	const { onOptionClick, renderOption, renderOptionGroup, getOptionHeight, getOptionGroupHeight, size, flattenOptions, value, multiple, virtualized, mobile, loading, onLoadMore, id, activeIndex, onChangeActive } = props;
	const items = import_react.useMemo(() => loading ? [...flattenOptions, loadingOption] : flattenOptions, [flattenOptions, loading]);
	const selectedIndexes = import_react.useMemo(() => {
		if (value.length === 0) return [];
		const valueSet = new Set(value);
		return flattenOptions.reduce((acc, option, index) => {
			if ("value" in option && valueSet.has(option.value)) acc.push(index);
			return acc;
		}, []);
	}, [flattenOptions, value]);
	const optionsHeight = getOptionsHeight({
		options: items,
		getOptionHeight,
		getOptionGroupHeight,
		size,
		mobile
	});
	const getItemHeight = import_react.useCallback((option, index) => {
		return getPopupItemHeight({
			getOptionHeight,
			getOptionGroupHeight,
			size,
			option,
			index,
			mobile
		});
	}, [
		getOptionHeight,
		getOptionGroupHeight,
		mobile,
		size
	]);
	const renderItem = import_react.useCallback((option, isItemActive, itemIndex) => {
		if ("label" in option) return (0, import_jsx_runtime.jsx)(GroupLabel, {
			option,
			renderOptionGroup: renderOptionGroup ? (optionLocal) => {
				return renderOptionGroup(optionLocal, {
					isItemActive,
					itemHeight: getItemHeight(optionLocal, itemIndex)
				});
			} : void 0
		});
		if (option.value === loadingOption.value) return (0, import_jsx_runtime.jsx)(SelectLoadingIndicator, { onIntersect: itemIndex === 0 ? void 0 : onLoadMore });
		return (0, import_jsx_runtime.jsx)(OptionWrap, {
			option,
			value,
			multiple,
			renderOption: renderOption ? (optionLocal) => {
				return renderOption(optionLocal, {
					isItemActive,
					itemHeight: getItemHeight(optionLocal, itemIndex)
				});
			} : void 0
		});
	}, [
		renderOption,
		renderOptionGroup,
		value,
		multiple,
		getItemHeight,
		onLoadMore
	]);
	return (0, import_jsx_runtime.jsx)(List, {
		ref,
		className: selectListBlock({
			size,
			virtualized,
			mobile,
			multiple
		}),
		qa: SelectQa.LIST,
		itemHeight: getItemHeight,
		itemsHeight: virtualized ? optionsHeight : void 0,
		items,
		filterable: false,
		virtualized,
		renderItem,
		onItemClick: onOptionClick,
		selectedItemIndex: selectedIndexes,
		id,
		role: "listbox",
		activeItemIndex: activeIndex,
		onChangeActive,
		deactivateOnLeave: false,
		onScrollToItem: scrollToItem
	});
});
SelectList.displayName = "SelectList";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectPopup/middlewares.js
var adjustBorderWidth = (width) => {
	return width - 2;
};
var getMinWidth = (referenceWidth, virtualized) => {
	if (virtualized) return referenceWidth > 100 ? referenceWidth : 100;
	return adjustBorderWidth(referenceWidth);
};
var getPopupWidth = (width, controlWidth, virtualized) => {
	let popupWidth = controlWidth;
	if (typeof width === "number") popupWidth = width;
	else if (width === "fit") popupWidth = adjustBorderWidth(controlWidth);
	else popupWidth = getMinWidth(controlWidth, virtualized);
	return `${popupWidth}px`;
};
function sameWidthMiddleware(args) {
	const { width, virtualized } = args;
	return size$1({ apply(state) {
		if (typeof width !== "number" && Boolean(state.elements.floating.style.maxWidth)) return;
		const popupWidth = getPopupWidth(width, state.rects.reference.width, virtualized);
		const floatingStyle = {};
		if (typeof width !== "number" && width !== "fit") {
			floatingStyle.minWidth = popupWidth;
			floatingStyle.width = void 0;
		} else {
			floatingStyle.minWidth = popupWidth;
			floatingStyle.width = popupWidth;
		}
		floatingStyle.maxWidth = `max(90vw, ${adjustBorderWidth(state.rects.reference.width)}px)`;
		Object.assign(state.elements.floating.style, floatingStyle);
	} });
}
function getMiddlewares(args) {
	return [
		offset$1({
			mainAxis: 1,
			crossAxis: 1
		}),
		flip({ altBoundary: args.disablePortal }),
		shift$1({
			limiter: limitShift(),
			crossAxis: true,
			padding: 10,
			altBoundary: args.disablePortal
		}),
		sameWidthMiddleware(args)
	];
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/SelectPopup/SelectPopup.js
var b$39 = block("select-popup");
var DEFAULT_PLACEMENT$1 = [
	"bottom-start",
	"bottom-end",
	"top-start",
	"top-end"
];
var SelectPopup = import_react.forwardRef(({ handleClose, onAfterOpen, onAfterClose, width, open, placement = DEFAULT_PLACEMENT$1, controlRef, children, className, disablePortal, virtualized, mobile, id }, ref) => mobile ? (0, import_jsx_runtime.jsx)(Sheet, {
	qa: SelectQa.SHEET,
	className,
	visible: Boolean(open),
	onClose: handleClose,
	children
}) : (0, import_jsx_runtime.jsx)(Popup, {
	className: b$39(null, className),
	qa: SelectQa.POPUP,
	anchorRef: ref,
	placement,
	open,
	onClose: handleClose,
	disablePortal,
	returnFocus: controlRef,
	floatingMiddlewares: getMiddlewares({
		width,
		disablePortal,
		virtualized
	}),
	id,
	onTransitionIn: onAfterOpen,
	onTransitionOutComplete: onAfterClose,
	children
}));
SelectPopup.displayName = "SelectPopup";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/components/HiddenSelect/HiddenSelect.js
function HiddenSelect(props) {
	const { name, value, disabled, form, onReset } = props;
	const ref = useFormResetHandler({
		onReset,
		initialValue: value
	});
	if (!name || disabled) return null;
	if (value.length === 0) return (0, import_jsx_runtime.jsx)("input", {
		ref,
		type: "hidden",
		name,
		value,
		form,
		disabled
	});
	return (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: value.map((v, i) => (0, import_jsx_runtime.jsx)("input", {
		ref: i === 0 ? ref : void 0,
		value: v,
		type: "hidden",
		name,
		form,
		disabled
	}, v)) });
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/hooks/useQuickSearch.js
var useQuickSearch = (props) => {
	const { onChange, open, disabled } = props;
	const [search, setSearch] = import_react.useState("");
	const [timer, setTimer] = import_react.useState();
	const handleTimer = import_react.useCallback((nextSearch) => {
		clearTimeout(timer);
		if (nextSearch) {
			const nextTimer = window.setTimeout(() => setSearch(""), QUICK_SEARCH_TIMEOUT);
			setTimer(nextTimer);
		}
	}, [timer]);
	const handleSearch = import_react.useCallback((e) => {
		e.stopPropagation();
		const nextSearch = getNextQuickSearch(e.key, search);
		if (search !== nextSearch) {
			handleTimer(nextSearch);
			setSearch(nextSearch);
		}
	}, [handleTimer, search]);
	import_react.useEffect(() => {
		if (open && !disabled) document.addEventListener("keydown", handleSearch);
		else if (!open && !disabled) setSearch("");
		return () => {
			if (open && !disabled) document.removeEventListener("keydown", handleSearch);
		};
	}, [
		handleSearch,
		open,
		disabled
	]);
	import_react.useEffect(() => {
		if (!open) clearTimeout(timer);
		return () => clearTimeout(timer);
	}, [open, timer]);
	import_react.useEffect(() => {
		onChange(search);
	}, [onChange, search]);
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/hooks/useActiveItemIndex.js
function useActiveItemIndex({ options, value, open }) {
	const [activeIndex, setActiveIndex] = import_react.useState(() => {
		if (open) return getInitialActiveItemIndex(options, value);
	});
	const [prevOpen, setPrevOpen] = import_react.useState(open);
	if (prevOpen !== open) {
		setPrevOpen(open);
		if (open) setActiveIndex(getInitialActiveItemIndex(options, value));
	}
	return [open && activeIndex !== void 0 && activeIndex < options.length && !options[activeIndex].disabled ? activeIndex : void 0, setActiveIndex];
}
function getInitialActiveItemIndex(options, value) {
	let itemIndex = -1;
	if (value.length > 0) itemIndex = options.findIndex((item) => "value" in item && value.includes(item.value) && !item.disabled);
	if (itemIndex === -1) itemIndex = options.findIndex((item) => "value" in item && !item.disabled);
	return itemIndex === -1 ? void 0 : itemIndex;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/hooks-public/useSelectOptions/index.js
function isFlattenOptions(options) {
	return Boolean(options[FLATTEN_KEY]);
}
/**
* Helps to manage options data before passing it into `Select` component.
* @param {SelectOptions} options
* @returns preprared options for `Select` component.
* @example
*
* import {Select, getSelectFilteredOptions, useSelectOptions} from '@gravity-ui/uikit';
*
* function App() {
*   const options = useSelectOptions({options: [{value: '1'}, {value: '2'}]});
*   const filteredOptions = getSelectFilteredOptions(options);
*   // Do some staff with prepared options
*   return <Select options={options} />
* }
*/
function getSelectFilteredOptions(options) {
	if (!isFlattenOptions(options)) throw Error("You should use options generated by useSelectOptions hook");
	return options[FLATTEN_KEY].filteredOptions;
}
function useSelectOptions(props) {
	const { filter = "", filterable, filterOption } = props;
	const options = import_react.useMemo(() => {
		return isFlattenOptions(props.options) ? props.options : getFlattenOptions(props.options);
	}, [props.options]);
	const filteredOptions = import_react.useMemo(() => {
		return filterable ? getFilteredFlattenOptions({
			options,
			filter,
			filterOption
		}) : options;
	}, [
		filter,
		filterable,
		filterOption,
		options
	]);
	options[FLATTEN_KEY]["filteredOptions"] = filteredOptions;
	return options;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/tech-components.js
var Option = (_props) => null;
var OptionGroup = (_props) => null;
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Select/Select.js
var DEFAULT_RENDER_POPUP = ({ renderFilter, renderList }) => {
	return (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [renderFilter(), renderList()] });
};
var Select = import_react.forwardRef(function Select(rawProps, ref) {
	const props = useDefaultProps("Select", rawProps);
	const { onUpdate, onOpenChange, onFilterChange, renderControl, renderFilter, renderOption, renderOptionGroup, renderSelectedOption, renderEmptyOptions, renderPopup = DEFAULT_RENDER_POPUP, getOptionHeight, getOptionGroupHeight, filterOption, name, form, className, controlClassName, popupClassName, qa, value: propsValue, defaultValue, defaultOpen, open: propsOpen, label, placeholder, filterPlaceholder, width, popupWidth, popupPlacement, error, virtualizationThreshold = 50, view = "normal", size = "m", pin = "round-round", multiple = false, disabled = false, filterable = false, filter: propsFilter, disablePortal, hasClear = false, onClose, id, hasCounter, renderCounter, title } = props;
	const mobile = useMobile();
	const [filter, setFilter] = useControlledState(propsFilter, "", onFilterChange);
	const controlWrapRef = import_react.useRef(null);
	const controlRef = import_react.useRef(null);
	const filterRef = import_react.useRef(null);
	const listRef = import_react.useRef(null);
	const handleControlRef = useForkRef(ref, controlRef);
	const { value, open, toggleOpen, setValue, handleSelection, handleClearValue } = useSelect({
		onUpdate,
		value: propsValue,
		defaultValue,
		defaultOpen,
		multiple,
		open: propsOpen,
		onClose,
		onOpenChange,
		disabled
	});
	import_react.useEffect(() => {
		if (!open && filterable && mobile) setTimeout(() => {
			setFilter("");
		}, 300);
	}, [
		open,
		filterable,
		setFilter,
		mobile
	]);
	const options = useSelectOptions({
		options: props.options || getOptionsFromChildren(props.children),
		filter,
		filterable,
		filterOption
	});
	const filteredOptions = getSelectFilteredOptions(options);
	const selectedOptionsContent = import_react.useMemo(() => {
		return getSelectedOptionsContent(options, value, renderSelectedOption);
	}, [
		options,
		value,
		renderSelectedOption
	]);
	const virtualized = filteredOptions.length >= virtualizationThreshold;
	const { errorMessage, errorPlacement, validationState } = errorPropsMapper({
		error,
		errorMessage: props.errorMessage,
		errorPlacement: props.errorPlacement || "outside",
		validationState: props.validationState
	});
	const errorMessageId = useUniqId();
	const isErrorStateVisible = validationState === "invalid";
	const isErrorMsgVisible = isErrorStateVisible && Boolean(errorMessage) && errorPlacement === "outside";
	const isErrorIconVisible = isErrorStateVisible && Boolean(errorMessage) && errorPlacement === "inside";
	const handleOptionClick = import_react.useCallback((option) => {
		if (!option || option?.disabled || "label" in option) return;
		if (multiple) {
			const activeItemIndex = listRef?.current?.getActiveItem();
			if (!mobile) filterRef.current?.focus();
			if (typeof activeItemIndex === "number") setTimeout(() => {
				listRef?.current?.activateItem(activeItemIndex, true);
			}, 50);
		}
		handleSelection(option);
	}, [
		handleSelection,
		mobile,
		multiple
	]);
	const handleControlKeyDown = import_react.useCallback((e) => {
		if ([KeyCode$1.ENTER, KeyCode$1.SPACEBAR].includes(e.key) && open) {
			e.preventDefault();
			if (e.key === KeyCode$1.SPACEBAR) handleOptionClick(getActiveItem(listRef));
		}
		if ([KeyCode$1.ARROW_DOWN, KeyCode$1.ARROW_UP].includes(e.key) && !open) {
			e.preventDefault();
			toggleOpen();
		}
		if (e.key === KeyCode$1.ESCAPE && open) toggleOpen(false);
		listRef?.current?.onKeyDown(e);
	}, [
		handleOptionClick,
		open,
		toggleOpen
	]);
	const handleFilterKeyDown = import_react.useCallback((e) => {
		listRef?.current?.onKeyDown(e);
		if (e.key === KeyCode$1.ENTER) e.preventDefault();
	}, []);
	useQuickSearch({
		onChange: import_react.useCallback((search) => {
			if (search) {
				const itemIndex = findItemIndexByQuickSearch(search, getListItems(listRef));
				if (typeof itemIndex === "number" && itemIndex !== -1) listRef?.current?.activateItem(itemIndex, true);
			}
		}, []),
		open,
		disabled: filterable
	});
	const mods = { ...width === "max" && { width } };
	const inlineStyles = {};
	if (typeof width === "number") inlineStyles.width = width;
	const handleClose = import_react.useCallback(() => toggleOpen(false), [toggleOpen]);
	const { onFocus, onBlur } = props;
	const { focusWithinProps } = useFocusWithin({
		onFocusWithin: onFocus,
		onBlurWithin: import_react.useCallback((e) => {
			onBlur?.(e);
			if (!mobile) handleClose();
		}, [
			handleClose,
			mobile,
			onBlur
		])
	});
	const uniqId = useUniqId();
	const selectId = id ?? uniqId;
	const popupId = `select-popup-${selectId}`;
	const [activeIndex, setActiveIndex] = useActiveItemIndex({
		options: filteredOptions,
		open,
		value
	});
	const _renderFilter = () => {
		if (filterable) return (0, import_jsx_runtime.jsx)(SelectFilter, {
			ref: filterRef,
			size,
			value: filter,
			placeholder: filterPlaceholder,
			onChange: setFilter,
			onKeyDown: handleFilterKeyDown,
			renderFilter,
			popupId,
			activeIndex
		});
		return null;
	};
	const _renderList = () => {
		if (filteredOptions.length || props.loading) return (0, import_jsx_runtime.jsx)(SelectList, {
			ref: listRef,
			size,
			value,
			mobile,
			flattenOptions: filteredOptions,
			multiple,
			virtualized,
			onOptionClick: handleOptionClick,
			renderOption,
			renderOptionGroup,
			getOptionHeight,
			getOptionGroupHeight,
			loading: props.loading,
			onLoadMore: props.onLoadMore,
			id: popupId,
			activeIndex,
			onChangeActive: setActiveIndex
		});
		return (0, import_jsx_runtime.jsx)(EmptyOptions, {
			filter,
			renderEmptyOptions
		});
	};
	return (0, import_jsx_runtime.jsxs)("div", {
		ref: controlWrapRef,
		className: selectBlock(mods, className),
		...focusWithinProps,
		style: inlineStyles,
		tabIndex: -1,
		children: [
			(0, import_jsx_runtime.jsx)(SelectControl, {
				...filterDOMProps(props, { labelable: true }),
				toggleOpen,
				hasClear,
				clearValue: handleClearValue,
				ref: handleControlRef,
				className: controlClassName,
				qa,
				view,
				size,
				pin,
				label,
				placeholder,
				selectedOptionsContent,
				isErrorVisible: isErrorStateVisible,
				errorMessage: isErrorIconVisible ? errorMessage : void 0,
				open,
				disabled,
				onKeyDown: handleControlKeyDown,
				renderControl,
				value,
				popupId,
				selectId,
				activeIndex,
				hasCounter: multiple && hasCounter,
				renderCounter,
				title
			}),
			(0, import_jsx_runtime.jsx)(SelectPopup, {
				ref: controlWrapRef,
				className: popupClassName,
				controlRef,
				width: popupWidth,
				open,
				handleClose,
				disablePortal,
				virtualized,
				mobile,
				placement: popupPlacement,
				onAfterOpen: filterable ? () => {
					filterRef.current?.focus();
				} : void 0,
				onAfterClose: filterable && !propsFilter ? () => {
					setFilter("");
				} : void 0,
				children: renderPopup({
					renderFilter: _renderFilter,
					renderList: _renderList
				})
			}),
			(0, import_jsx_runtime.jsx)(OuterAdditionalContent, {
				errorMessage: isErrorMsgVisible ? errorMessage : null,
				errorMessageId
			}),
			(0, import_jsx_runtime.jsx)(HiddenSelect, {
				name,
				value,
				disabled,
				form,
				onReset: setValue
			})
		]
	});
});
Select.Option = Option;
Select.OptionGroup = OptionGroup;
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/utils.js
var import_uniq = /* @__PURE__ */ __toESM((/* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseUniq = require__baseUniq();
	/**
	* Creates a duplicate-free version of an array, using
	* [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
	* for equality comparisons, in which only the first occurrence of each element
	* is kept. The order of result values is determined by the order they occur
	* in the array.
	*
	* @static
	* @memberOf _
	* @since 0.1.0
	* @category Array
	* @param {Array} array The array to inspect.
	* @returns {Array} Returns the new duplicate free array.
	* @example
	*
	* _.uniq([2, 1, 2]);
	* // => [2, 1]
	*/
	function uniq(array) {
		return array && array.length ? baseUniq(array) : [];
	}
	module.exports = uniq;
})))(), 1);
function getNumerationList({ page, numberOfPages, mobile }) {
	return mobile ? getMobileNumerationList(page, numberOfPages) : getDesktopNumerationList(page, numberOfPages);
}
function getMobileNumerationList(page, numberOfPages) {
	return [
		page,
		"pageOf",
		numberOfPages
	];
}
function getDesktopNumerationList(page, numberOfPages) {
	const prevPage = Math.max(page - 1, 1);
	let rightPage = Math.min(page + 1, numberOfPages);
	const list = [
		prevPage,
		page,
		rightPage
	];
	if (page === 1) {
		rightPage = Math.min(rightPage + 1, numberOfPages);
		list.push(rightPage);
	}
	if (numberOfPages - rightPage >= 2) list.push("ellipsis");
	if (numberOfPages - page === 1) list.unshift(Math.max(page - 2, 1));
	if (page === numberOfPages) {
		list.unshift(Math.max(page - 2, 1));
		list.unshift(Math.max(page - 3, 1));
	}
	list.push(numberOfPages);
	return (0, import_uniq.default)(list);
}
function getNumberOfPages(pageSize, total = 0) {
	return Math.floor((total - 1) / pageSize) + 1;
}
function getResultTotal(total) {
	return total === void 0 || total > 0 ? total : 1;
}
function getSize({ propSize, mobile }) {
	if (propSize) return propSize;
	return mobile ? "l" : "m";
}
function getResultPage({ page, total, pageSize }) {
	return page > 0 && (total === void 0 || page <= getNumberOfPages(pageSize, total)) ? page : 1;
}
function getViews({ propView, mobile }) {
	return {
		buttonView: propView === "clear" ? "flat" : "outlined",
		inputView: mobile && propView === "clear" ? "clear" : "normal",
		pageSizerView: propView === "outlined" ? "normal" : "clear"
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/components/PaginationPageSizer/PaginationPageSizer.js
var PaginationPageSizer = ({ onUpdate, pageSize, size, page, pageSizeOptions, total, className, view }) => {
	const options = pageSizeOptions.map((pageSizeOption) => ({
		value: String(pageSizeOption),
		content: pageSizeOption,
		qa: getPaginationPageSizeOptionQa(pageSizeOption)
	}));
	const handleUpdate = ([newPageSizeOnUpdate]) => {
		const newPageSize = Number(newPageSizeOnUpdate);
		const numberOfPages = getNumberOfPages(newPageSize, total);
		if (!(numberOfPages > 0)) {
			onUpdate(1, newPageSize);
			return;
		}
		onUpdate(page > numberOfPages ? numberOfPages : page, newPageSize);
	};
	const { t } = i18n_default$8.useTranslation();
	return (0, import_jsx_runtime.jsx)(Select, {
		qa: PaginationQa.PaginationPageSizer,
		className,
		size,
		onUpdate: handleUpdate,
		options,
		value: [String(pageSize)],
		title: t("label_select_size"),
		view
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/hooks/usePagination.js
function usePagination({ page, pageSize, total, mobile }) {
	const numberOfPages = getNumberOfPages(pageSize, total);
	const hasTotal = numberOfPages !== 0;
	const isNextDisabled = hasTotal && page === numberOfPages || total === 0;
	let items;
	if (hasTotal) items = getNumerationList({
		page,
		numberOfPages,
		mobile
	}).map((item, index) => {
		if (item === "ellipsis") return { type: "ellipsis" };
		if (item === "pageOf") return { type: "pageOf" };
		const current = item === page;
		return {
			type: "page",
			current,
			page: item,
			simple: mobile ? current : false,
			key: mobile ? item + index : item
		};
	});
	else items = [{
		type: "page",
		current: true,
		page,
		simple: true,
		key: page
	}];
	items.unshift({
		type: "button",
		action: "previous",
		disabled: page <= 1
	});
	items.unshift({
		type: "button",
		action: "first",
		disabled: page <= 1
	});
	items.push({
		type: "button",
		action: "next",
		disabled: isNextDisabled
	});
	return {
		items,
		numberOfPages
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Pagination/Pagination.js
var b$38 = block("pagination");
var Pagination = (rawProps) => {
	const { page, pageSize, total, size: propSize, onUpdate, compact: propCompact = true, pageSizeOptions, showPages = true, showInput = false, view: propView = "outlined", className, qa } = useDefaultProps("Pagination", rawProps);
	const mobile = useMobile();
	const size = getSize({
		propSize,
		mobile
	});
	const compact = mobile ? true : propCompact;
	const resultTotal = getResultTotal(total);
	const resultPage = getResultPage({
		page,
		total: resultTotal,
		pageSize
	});
	const { buttonView, inputView, pageSizerView } = getViews({
		propView,
		mobile
	});
	const { items, numberOfPages } = usePagination({
		page: resultPage,
		pageSize,
		total: resultTotal,
		mobile
	});
	const pagination = items.map((item) => {
		switch (item.type) {
			case "page": return showPages && (0, import_jsx_runtime.jsx)(PaginationPage, {
				size,
				pageSize,
				item,
				onUpdate,
				className: b$38("pagination-item")
			}, item.key);
			case "ellipsis": return showPages && (0, import_jsx_runtime.jsx)(PaginationEllipsis, {
				size,
				className: b$38("pagination-item")
			}, item.type);
			case "pageOf": return showPages && (0, import_jsx_runtime.jsx)(PaginationPageOf, {
				className: b$38("pagination-item"),
				size
			}, item.type);
			case "button": return (0, import_jsx_runtime.jsx)(PaginationButton, {
				size,
				item,
				page: resultPage,
				pageSize,
				onUpdate,
				compact,
				className: b$38("pagination-item"),
				view: buttonView
			}, item.action);
			default: return null;
		}
	}).filter(Boolean);
	return (0, import_jsx_runtime.jsxs)("div", {
		className: b$38(null, className),
		"data-qa": qa,
		children: [
			pagination,
			showInput && (0, import_jsx_runtime.jsx)(PaginationInput, {
				numberOfPages,
				pageSize,
				size,
				onUpdate,
				className: b$38("input"),
				view: inputView
			}),
			pageSizeOptions && (0, import_jsx_runtime.jsx)(PaginationPageSizer, {
				onUpdate,
				page: resultPage,
				pageSize,
				pageSizeOptions,
				size,
				total: resultTotal,
				className: b$38("page-sizer"),
				view: pageSizerView
			})
		]
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Palette/hooks.js
function usePaletteGrid(props) {
	const direction = useDirection();
	const { focusWithinProps } = useFocusWithin({
		onFocusWithin: (event) => props.onFocus?.(event),
		onBlurWithin: (event) => props.onBlur?.(event)
	});
	const whenFocused = props.whenFocused;
	const base = {
		role: "grid",
		"aria-disabled": props.disabled,
		"aria-readonly": props.disabled,
		tabIndex: whenFocused ? -1 : 0,
		...focusWithinProps
	};
	if (!whenFocused) return base;
	return {
		...base,
		onKeyDown: (event) => {
			if (event.code === KeyCode$1.ARROW_RIGHT) {
				event.preventDefault();
				if (direction === "ltr") whenFocused.nextItem();
				else whenFocused.previousItem();
			} else if (event.code === KeyCode$1.ARROW_LEFT) {
				event.preventDefault();
				if (direction === "ltr") whenFocused.previousItem();
				else whenFocused.nextItem();
			} else if (event.code === KeyCode$1.ARROW_DOWN) {
				event.preventDefault();
				whenFocused.nextRow();
			} else if (event.code === KeyCode$1.ARROW_UP) {
				event.preventDefault();
				whenFocused.previousRow();
			} else if (event.code === KeyCode$1.SPACE || event.code === KeyCode$1.ENTER) {
				event.preventDefault();
				whenFocused.selectItem();
			}
		}
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Palette/utils.js
function getPaletteRows(options, columns) {
	if (columns <= 0) throw new Error("Palette.getPaletteRows: number of columns must greater than 0");
	const rows = [];
	let row = [];
	let column = 0;
	for (const option of options) {
		row.push(option);
		column += 1;
		if (column >= columns) {
			rows.push(row);
			row = [];
			column = 0;
		}
	}
	if (row.length > 0) rows.push(row);
	return rows;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Palette/Palette.js
var b$37 = block("palette");
var Palette = import_react.forwardRef(function Palette(rawProps, ref) {
	const props = useDefaultProps("Palette", rawProps);
	const { size = "m", multiple = true, options = [], columns = 6, disabled, style, className, rowClassName, optionClassName, qa, onFocus, onBlur } = props;
	const [focusedOptionIndex, setFocusedOptionIndex] = import_react.useState(void 0);
	const focusedOption = focusedOptionIndex === void 0 ? void 0 : options[focusedOptionIndex];
	const innerRef = import_react.useRef(null);
	const handleRef = useForkRef(ref, innerRef);
	const { value, handleSelection } = useSelect({
		value: props.value,
		defaultValue: props.defaultValue,
		multiple,
		onUpdate: props.onUpdate
	});
	const rows = import_react.useMemo(() => getPaletteRows(options, columns), [columns, options]);
	const focusOnOptionWithIndex = import_react.useCallback((index) => {
		if (!innerRef.current) return;
		const $options = Array.from(innerRef.current.querySelectorAll(`.${b$37("option")}`));
		if (!$options[index]) return;
		$options[index].focus();
		setFocusedOptionIndex(index);
	}, []);
	const tryToFocus = (newIndex) => {
		if (newIndex === focusedOptionIndex || newIndex < 0 || newIndex >= options.length) return;
		focusOnOptionWithIndex(newIndex);
	};
	const gridProps = usePaletteGrid({
		disabled,
		onFocus: (event) => {
			focusOnOptionWithIndex(0);
			onFocus?.(event);
		},
		onBlur: (event) => {
			setFocusedOptionIndex(void 0);
			onBlur?.(event);
		},
		whenFocused: focusedOptionIndex !== void 0 && focusedOption ? {
			selectItem: () => handleSelection(focusedOption),
			nextItem: () => tryToFocus(focusedOptionIndex + 1),
			previousItem: () => tryToFocus(focusedOptionIndex - 1),
			nextRow: () => tryToFocus(focusedOptionIndex + columns),
			previousRow: () => tryToFocus(focusedOptionIndex - columns)
		} : void 0
	});
	return (0, import_jsx_runtime.jsx)("div", {
		...filterDOMProps(props, { labelable: true }),
		...gridProps,
		ref: handleRef,
		className: b$37({ size }, className),
		style,
		"data-qa": qa,
		children: rows.map((row, rowNumber) => (0, import_jsx_runtime.jsx)("div", {
			className: b$37("row", rowClassName),
			role: "row",
			children: row.map((option) => {
				const isSelected = Boolean(value.includes(option.value));
				return (0, import_jsx_runtime.jsx)("div", {
					role: "gridcell",
					"aria-selected": option === focusedOption ? "true" : void 0,
					"aria-readonly": option.disabled,
					children: (0, import_jsx_runtime.jsx)(Button, {
						className: b$37("option", optionClassName),
						tabIndex: -1,
						style,
						disabled: disabled || option.disabled,
						title: option.title,
						view: isSelected ? "normal" : "flat",
						selected: isSelected,
						value: option.value,
						size,
						onClick: () => handleSelection(option),
						children: (0, import_jsx_runtime.jsx)(Button.Icon, { children: option.content ?? option.value })
					})
				}, option.value);
			})
		}, `row-${rowNumber}`))
	});
});
Palette.displayName = "Palette";
var i18n_default$6 = addComponentKeysets({
	en: { "label_one-of": "{{number}} of {{count}}, " },
	ru: { "label_one-of": "{{number}} из {{count}}, " }
}, "PinInput");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/PinInput/PinInput.js
var b$36 = block("pin-input");
var NUMERIC_REGEXP = /[0-9]+/;
var ALPHANUMERIC_REGEXP = /[0-9a-z]+/i;
var validate = (type, newValue) => {
	if (type === "numeric") return NUMERIC_REGEXP.test(newValue);
	else return ALPHANUMERIC_REGEXP.test(newValue);
};
var PinInput = import_react.forwardRef((rawProps, ref) => {
	const props = useDefaultProps("PinInput", rawProps);
	const { value, defaultValue, onUpdate, onUpdateComplete, onFocus, onBlur, length = 4, size = "m", type = "numeric", id: idProp, name, form, placeholder, disabled, autoFocus, otp, mask, responsive, note, validationState, errorMessage, apiRef, className, style, qa, ...restProps } = props;
	const refs = import_react.useRef({});
	const [activeIndex, setActiveIndex] = import_react.useState(0);
	const [focusedIndex, setFocusedIndex] = import_react.useState(-1);
	const updateCallback = import_react.useCallback((newValue) => {
		if (onUpdate) onUpdate(newValue);
		if (onUpdateComplete && newValue.every((v) => Boolean(v))) onUpdateComplete(newValue);
	}, [onUpdate, onUpdateComplete]);
	const [values, setValues] = useControlledState(value, defaultValue ?? Array.from({ length }, () => ""), updateCallback);
	const direction = useDirection();
	const errorMessageId = useUniqId();
	const noteId = useUniqId();
	const isErrorMsgVisible = validationState === "invalid" && errorMessage;
	const ariaDescribedBy = [
		props?.["aria-describedby"],
		note ? noteId : void 0,
		isErrorMsgVisible ? errorMessageId : void 0
	].filter(Boolean).join(" ");
	const handleRef = (index, inputRef) => {
		refs.current[index] = inputRef;
	};
	const focus = (index) => {
		setActiveIndex(index);
		refs.current[index]?.focus();
	};
	const focusPrev = (index) => {
		if (index > 0) focus(index - 1);
	};
	const focusNext = (index) => {
		if (index < length - 1) focus(index + 1);
	};
	const setValuesAtIndex = (index, nextValue) => {
		const newValues = Array.from({ length }, (__, i) => values[i] ?? "");
		if (nextValue.length > 0) for (let k = 0; k < nextValue.length && index + k < newValues.length; k++) newValues[index + k] = nextValue[k];
		else newValues[index] = "";
		if (newValues.every((__, i) => newValues[i] === values[i])) return;
		setValues(newValues);
	};
	const handleInputChange = (i, event) => {
		let nextValue = event.currentTarget.value;
		const currentValue = values[i];
		if (currentValue) {
			if (currentValue === nextValue[0]) nextValue = nextValue.slice(1);
			else if (currentValue === nextValue[nextValue.length - 1]) nextValue = nextValue.slice(0, -1);
		}
		if (!validate(type, nextValue)) return;
		if (nextValue.length > 1) {
			setValuesAtIndex(0, nextValue);
			focusNext(nextValue.length - 1);
		} else {
			setValuesAtIndex(i, nextValue);
			focusNext(i);
		}
	};
	const handleInputKeyDown = (i, event) => {
		switch (event.code) {
			case KeyCode$1.BACKSPACE:
				event.preventDefault();
				if (event.currentTarget.value) setValuesAtIndex(i, "");
				else if (i > 0) {
					setValuesAtIndex(i - 1, "");
					focusPrev(i);
				}
				break;
			case KeyCode$1.ARROW_LEFT:
			case KeyCode$1.ARROW_UP:
				event.preventDefault();
				if (direction === "rtl" && event.code === KeyCode$1.ARROW_LEFT) focusNext(i);
				else focusPrev(i);
				break;
			case KeyCode$1.ARROW_RIGHT:
			case KeyCode$1.ARROW_DOWN:
				event.preventDefault();
				if (direction === "rtl" && event.code === KeyCode$1.ARROW_RIGHT) focusPrev(i);
				else focusNext(i);
				break;
		}
	};
	const handleFocus = (index) => {
		setFocusedIndex(index);
		setActiveIndex(index);
	};
	const handleBlur = () => {
		setFocusedIndex(-1);
	};
	import_react.useEffect(() => {
		if (autoFocus) focus(0);
	}, []);
	import_react.useImperativeHandle(apiRef, () => ({ focus: () => {
		refs.current[activeIndex]?.focus();
	} }), [activeIndex]);
	const formInputRef = useFormResetHandler({
		initialValue: values,
		onReset: setValues
	});
	const { focusWithinProps } = useFocusWithin({
		onFocusWithin: onFocus,
		onBlurWithin: onBlur
	});
	let id = useUniqId();
	if (idProp) id = idProp;
	const { t } = i18n_default$6.useTranslation();
	return (0, import_jsx_runtime.jsxs)("div", {
		ref,
		...filterDOMProps(restProps, { labelable: true }),
		...focusWithinProps,
		className: b$36({
			size,
			responsive
		}, className),
		style,
		"data-qa": qa,
		role: "group",
		id,
		"aria-describedby": ariaDescribedBy,
		children: [(0, import_jsx_runtime.jsxs)("div", {
			className: b$36("items"),
			children: [Array.from({ length }).map((__, i) => {
				const inputId = `${id}-${i}`;
				const ariaLabelledBy = props["aria-labelledby"] || props["aria-label"] ? [inputId, props["aria-labelledby"] || id].join(" ") : void 0;
				return (0, import_jsx_runtime.jsx)("div", {
					className: b$36("item"),
					children: (0, import_jsx_runtime.jsx)(TextInput, {
						value: values[i]?.[0] ?? "",
						tabIndex: activeIndex === i ? 0 : -1,
						type: mask ? "password" : "text",
						size,
						id: inputId,
						disabled,
						placeholder: focusedIndex === i ? void 0 : placeholder,
						autoComplete: otp ? "one-time-code" : "off",
						validationState,
						controlProps: {
							inputMode: type === "numeric" ? "numeric" : "text",
							pattern: type === "numeric" ? "[0-9]*" : "[0-9a-zA-Z]*",
							className: b$36("control"),
							autoCapitalize: "none",
							"aria-label": t("label_one-of", {
								number: i + 1,
								count: length
							}),
							"aria-labelledby": ariaLabelledBy,
							"aria-describedby": ariaDescribedBy,
							"aria-details": props["aria-details"],
							"aria-invalid": validationState === "invalid" ? true : void 0
						},
						controlRef: handleRef.bind(null, i),
						onChange: handleInputChange.bind(null, i),
						onKeyDown: handleInputKeyDown.bind(null, i),
						onFocus: handleFocus.bind(null, i),
						onBlur: handleBlur
					})
				}, i);
			}), name ? (0, import_jsx_runtime.jsx)("input", {
				ref: formInputRef,
				type: "hidden",
				name,
				form,
				value: values.join(""),
				disabled
			}) : null]
		}), (0, import_jsx_runtime.jsx)(OuterAdditionalContent, {
			note,
			errorMessage: isErrorMsgVisible ? errorMessage : null,
			noteId,
			errorMessageId
		})]
	});
});
PinInput.displayName = "PinInput";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/PlaceholderContainer/PlaceholderContainer.js
var b$35 = block("placeholder-container");
var PlaceholderContainerAction = ({ text, ...buttonProps }) => {
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$35("action"),
		children: (0, import_jsx_runtime.jsx)(Button, {
			...buttonProps,
			className: b$35("action-btn"),
			children: text
		})
	});
};
var PlaceholderContainer = (rawProps) => {
	const { direction = "row", align = "center", size = "l", className, title, description, image, content, actions, maxWidth, qa } = useDefaultProps("PlaceholderContainer", rawProps);
	const renderTitle = () => {
		if (!title) return null;
		return (0, import_jsx_runtime.jsx)("div", {
			className: b$35("title"),
			children: title
		});
	};
	const renderDescription = () => {
		if (!description) return null;
		return (0, import_jsx_runtime.jsx)("div", {
			className: b$35("description"),
			children: description
		});
	};
	const renderImage = () => {
		if (typeof image === "object" && "src" in image) return (0, import_jsx_runtime.jsx)("img", {
			src: image.src,
			alt: image.alt || ""
		});
		return image;
	};
	const renderAction = () => {
		if (!actions || !(import_react.isValidElement(actions) || Array.isArray(actions))) return null;
		if (import_react.isValidElement(actions)) return (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: actions });
		return (0, import_jsx_runtime.jsx)("div", {
			className: b$35("actions"),
			children: actions.map((actionItem) => (0, import_jsx_runtime.jsx)(PlaceholderContainerAction, { ...actionItem }, actionItem.text))
		});
	};
	const renderContent = () => {
		const contentNode = content || (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [renderTitle(), renderDescription()] });
		return (0, import_jsx_runtime.jsxs)("div", {
			className: b$35("content", { size }),
			children: [contentNode, renderAction()]
		});
	};
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$35({
			direction,
			align,
			size
		}, className || ""),
		"data-qa": qa,
		children: (0, import_jsx_runtime.jsxs)("div", {
			className: b$35("body"),
			style: maxWidth ? { maxWidth } : void 0,
			children: [(0, import_jsx_runtime.jsx)("div", {
				className: b$35("image", { size }),
				children: renderImage()
			}), renderContent()]
		})
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Progress/constants.js
var progressBlock = block("progress");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Progress/ProgressInnerText.js
function ProgressInnerText(props) {
	const { text, offset = 0 } = props;
	if (!text) return null;
	return (0, import_jsx_runtime.jsx)("div", {
		className: progressBlock("text-inner"),
		style: { transform: `translateX(calc(var(--g-flow-direction) * ${-offset}%))` },
		children: text
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Progress/ProgressStackItem.js
function ProgressStackItem({ item }) {
	const { value, color, className, theme, title, content, loading } = item;
	const modifiers = { loading };
	if (typeof color === "undefined") modifiers.theme = theme || "default";
	if (Number.isFinite(value)) return (0, import_jsx_runtime.jsx)("div", {
		className: progressBlock("item", modifiers, className),
		style: {
			width: `${value}%`,
			backgroundColor: color
		},
		title,
		children: content
	});
	return null;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Progress/utils.js
function getOffset$1(value) {
	return value < 100 ? value - 100 : 0;
}
function getValueFromStack(stack) {
	return stack.reduce((sum, { value }) => sum + value, 0);
}
function getTheme(props) {
	const { theme, colorStops, colorStopsValue, value } = props;
	if (colorStops) {
		const matchingColorStopItem = colorStops.find((item, index) => {
			const currentValue = typeof colorStopsValue === "number" ? colorStopsValue : value;
			const minValue = index > 1 ? colorStops[index - 1].stop : 0;
			const maxValue = index < colorStops.length - 1 ? item.stop : 100;
			return currentValue >= minValue && currentValue <= maxValue;
		});
		return matchingColorStopItem ? {
			theme: matchingColorStopItem.theme,
			color: matchingColorStopItem.color
		} : { theme };
	}
	return { theme };
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Progress/ProgressWithStack.js
function ProgressWithStack(props) {
	const { stack, stackClassName, value, text } = props;
	const offset = getOffset$1(value || getValueFromStack(stack));
	return (0, import_jsx_runtime.jsxs)("div", {
		className: progressBlock("stack", stackClassName),
		style: { transform: `translateX(calc(var(--g-flow-direction) * ${offset}%))` },
		children: [
			(0, import_jsx_runtime.jsx)("div", {
				className: progressBlock("item"),
				style: { width: `${-offset}%` }
			}),
			stack.map((item, index) => (0, import_jsx_runtime.jsx)(ProgressStackItem, { item }, index)),
			(0, import_jsx_runtime.jsx)(ProgressInnerText, {
				offset,
				text
			})
		]
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Progress/ProgressWithValue.js
function ProgressWithValue(props) {
	const { value, loading, text } = props;
	const offset = getOffset$1(value);
	const { theme, color } = getTheme(props);
	const modifiers = { loading };
	if (typeof color === "undefined") modifiers.theme = theme || "default";
	if (Number.isFinite(value)) return (0, import_jsx_runtime.jsx)("div", {
		className: progressBlock("item", modifiers),
		style: {
			transform: `translateX(calc(var(--g-flow-direction) * ${offset}%))`,
			backgroundColor: color
		},
		children: (0, import_jsx_runtime.jsx)(ProgressInnerText, {
			offset,
			text
		})
	});
	return null;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Progress/types.js
function isProgressWithStack(props) {
	return props.stack !== void 0;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Progress/Progress.js
var Progress = import_react.forwardRef(function Progress(rawProps, ref) {
	const props = useDefaultProps("Progress", rawProps);
	const { text = "", theme = "default", size = "m", loading = false, className, qa } = props;
	const resolvedProps = {
		...props,
		text,
		theme,
		size,
		loading
	};
	return (0, import_jsx_runtime.jsxs)("div", {
		ref,
		className: progressBlock({ size }, className),
		"data-qa": qa,
		children: [(0, import_jsx_runtime.jsx)("div", {
			className: progressBlock("text"),
			children: text
		}), isProgressWithStack(resolvedProps) ? (0, import_jsx_runtime.jsx)(ProgressWithStack, { ...resolvedProps }) : (0, import_jsx_runtime.jsx)(ProgressWithValue, { ...resolvedProps })]
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Radio/Radio.js
var b$34 = block("radio");
var Radio = import_react.forwardRef(function Radio(rawProps, ref) {
	const props = useDefaultProps("Radio", rawProps);
	const { size = "m", disabled = false, content, children, title, style, className, qa } = props;
	const { checked, inputProps } = useRadio(props);
	const text = content || children;
	const control = (0, import_jsx_runtime.jsxs)("span", {
		className: b$34("indicator"),
		children: [
			(0, import_jsx_runtime.jsx)("span", { className: b$34("disc") }),
			(0, import_jsx_runtime.jsx)("input", {
				...inputProps,
				className: b$34("control")
			}),
			(0, import_jsx_runtime.jsx)("span", { className: b$34("outline") })
		]
	});
	return (0, import_jsx_runtime.jsx)(ControlLabel, {
		ref,
		title,
		style,
		size,
		disabled,
		className: b$34({
			size,
			disabled,
			checked
		}, className),
		qa,
		control,
		children: text
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/RadioGroup/RadioGroup.js
var b$33 = block("radio-group");
var RadioGroup = import_react.forwardRef(function RadioGroup(rawProps, ref) {
	const props = useDefaultProps("RadioGroup", rawProps);
	const { size = "m", direction = "horizontal", style, className, optionClassName, qa, children } = props;
	let options = props.options;
	if (!options) options = import_react.Children.toArray(children).map(({ props: radioProps }) => ({
		value: radioProps.value,
		content: radioProps.content || radioProps.children,
		disabled: radioProps.disabled,
		qa: radioProps.qa
	}));
	const { containerProps, optionsProps } = useRadioGroup({
		...props,
		options
	});
	return (0, import_jsx_runtime.jsx)("div", {
		...containerProps,
		ref,
		style,
		className: b$33({
			size,
			direction
		}, className),
		"data-qa": qa,
		children: optionsProps.map((optionProps) => (0, import_react.createElement)(Radio, {
			...optionProps,
			key: optionProps.value,
			className: b$33("option", optionClassName),
			size
		}))
	});
});
RadioGroup.Option = Radio;
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/SegmentedRadioGroup/SegmentedRadioGroupOption.js
var b$32 = block("segmented-radio-group");
var SegmentedRadioGroupOption = import_react.forwardRef(function SegmentedRadioGroupOption(props, ref) {
	const radioGroupContext = import_react.useContext(RadioGroupContext);
	if (!radioGroupContext) throw new Error("<SegmentedRadioGroup.Option> must be used within <SegmentedRadioGroup>");
	const { name, currentValue, disabled: disabledContext, onChange } = radioGroupContext;
	const { disabled: disabledProp, content, children, title, value } = props;
	const disabled = disabledContext || disabledProp;
	const { checked, inputProps } = useRadio({
		...props,
		name,
		disabled,
		checked: value === currentValue,
		onChange
	});
	const inner = content || children;
	const icon = isIcon(inner) || isSvg(inner);
	return (0, import_jsx_runtime.jsxs)("label", {
		className: b$32("option", {
			disabled,
			checked
		}),
		ref,
		title,
		children: [(0, import_jsx_runtime.jsx)("input", {
			...inputProps,
			className: b$32("option-control")
		}), inner && (0, import_jsx_runtime.jsx)("span", {
			className: b$32("option-text", { icon }),
			children: inner
		})]
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/SegmentedRadioGroup/SegmentedRadioGroup.js
var b$31 = block("segmented-radio-group");
var SegmentedRadioGroup = import_react.forwardRef(function SegmentedRadioGroup(rawProps, ref) {
	const props = useDefaultProps("SegmentedRadioGroup", rawProps);
	const { size = "m", width, style, className, qa, children } = props;
	const options = props.options;
	const { containerProps, optionsProps, contextProps } = useRadioGroup({
		...props,
		options
	});
	return (0, import_jsx_runtime.jsx)(RadioGroupContext.Provider, {
		value: contextProps,
		children: (0, import_jsx_runtime.jsx)("div", {
			...containerProps,
			ref,
			style,
			className: b$31({
				size,
				width
			}, className),
			"data-qa": qa,
			children: children || optionsProps?.map((optionProps) => (0, import_react.createElement)(SegmentedRadioGroupOption, {
				...optionProps,
				key: optionProps.value
			}))
		})
	});
});
SegmentedRadioGroup.Option = SegmentedRadioGroupOption;
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Skeleton/Skeleton.js
var b$30 = block("skeleton");
function Skeleton(rawProps) {
	const { className, style, width, height, qa, animation = "gradient", variant = "rect", size } = useDefaultProps("Skeleton", rawProps);
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$30({
			animation,
			variant,
			size
		}, className),
		style: {
			...width !== void 0 && { width },
			...height !== void 0 && { height },
			...style
		},
		"data-qa": qa
	});
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/typeof.js
function _typeof(o) {
	"@babel/helpers - typeof";
	return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
		return typeof o;
	} : function(o) {
		return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
	}, _typeof(o);
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/toPrimitive.js
function toPrimitive(t, r) {
	if ("object" != _typeof(t) || !t) return t;
	var e = t[Symbol.toPrimitive];
	if (void 0 !== e) {
		var i = e.call(t, r || "default");
		if ("object" != _typeof(i)) return i;
		throw new TypeError("@@toPrimitive must return a primitive value.");
	}
	return ("string" === r ? String : Number)(t);
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/toPropertyKey.js
function toPropertyKey(t) {
	var i = toPrimitive(t, "string");
	return "symbol" == _typeof(i) ? i : i + "";
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/defineProperty.js
function _defineProperty(e, r, t) {
	return (r = toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
		value: t,
		enumerable: !0,
		configurable: !0,
		writable: !0
	}) : e[r] = t, e;
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/objectSpread2.js
function ownKeys(e, r) {
	var t = Object.keys(e);
	if (Object.getOwnPropertySymbols) {
		var o = Object.getOwnPropertySymbols(e);
		r && (o = o.filter(function(r) {
			return Object.getOwnPropertyDescriptor(e, r).enumerable;
		})), t.push.apply(t, o);
	}
	return t;
}
function _objectSpread2(e) {
	for (var r = 1; r < arguments.length; r++) {
		var t = null != arguments[r] ? arguments[r] : {};
		r % 2 ? ownKeys(Object(t), !0).forEach(function(r) {
			_defineProperty(e, r, t[r]);
		}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function(r) {
			Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
		});
	}
	return e;
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/arrayLikeToArray.js
function _arrayLikeToArray(r, a) {
	(null == a || a > r.length) && (a = r.length);
	for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
	return n;
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/arrayWithoutHoles.js
function _arrayWithoutHoles(r) {
	if (Array.isArray(r)) return _arrayLikeToArray(r);
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/iterableToArray.js
function _iterableToArray(r) {
	if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r);
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/unsupportedIterableToArray.js
function _unsupportedIterableToArray(r, a) {
	if (r) {
		if ("string" == typeof r) return _arrayLikeToArray(r, a);
		var t = {}.toString.call(r).slice(8, -1);
		return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0;
	}
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/nonIterableSpread.js
function _nonIterableSpread() {
	throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/toConsumableArray.js
function _toConsumableArray(r) {
	return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread();
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/arrayWithHoles.js
function _arrayWithHoles(r) {
	if (Array.isArray(r)) return r;
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/iterableToArrayLimit.js
function _iterableToArrayLimit(r, l) {
	var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
	if (null != t) {
		var e, n, i, u, a = [], f = !0, o = !1;
		try {
			if (i = (t = t.call(r)).next, 0 === l) {
				if (Object(t) !== t) return;
				f = !1;
			} else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0);
		} catch (r) {
			o = !0, n = r;
		} finally {
			try {
				if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return;
			} finally {
				if (o) throw n;
			}
		}
		return a;
	}
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/nonIterableRest.js
function _nonIterableRest() {
	throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/slicedToArray.js
function _slicedToArray(r, e) {
	return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest();
}
/*!
Copyright (c) 2018 Jed Watson.
Licensed under the MIT License (MIT), see
http://jedwatson.github.io/classnames
*/
//#endregion
//#region node_modules/.pnpm/rc-util@5.44.4_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-util/es/hooks/useEvent.js
var import_classnames = /* @__PURE__ */ __toESM((/* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function() {
		"use strict";
		var hasOwn = {}.hasOwnProperty;
		function classNames() {
			var classes = "";
			for (var i = 0; i < arguments.length; i++) {
				var arg = arguments[i];
				if (arg) classes = appendClass(classes, parseValue(arg));
			}
			return classes;
		}
		function parseValue(arg) {
			if (typeof arg === "string" || typeof arg === "number") return arg;
			if (typeof arg !== "object") return "";
			if (Array.isArray(arg)) return classNames.apply(null, arg);
			if (arg.toString !== Object.prototype.toString && !arg.toString.toString().includes("[native code]")) return arg.toString();
			var classes = "";
			for (var key in arg) if (hasOwn.call(arg, key) && arg[key]) classes = appendClass(classes, key);
			return classes;
		}
		function appendClass(value, newClass) {
			if (!newClass) return value;
			if (value) return value + " " + newClass;
			return value + newClass;
		}
		if (typeof module !== "undefined" && module.exports) {
			classNames.default = classNames;
			module.exports = classNames;
		} else if (typeof define === "function" && typeof define.amd === "object" && define.amd) define("classnames", [], function() {
			return classNames;
		});
		else window.classNames = classNames;
	})();
})))());
function useEvent(callback) {
	var fnRef = import_react.useRef();
	fnRef.current = callback;
	return import_react.useCallback(function() {
		var _fnRef$current;
		for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
		return (_fnRef$current = fnRef.current) === null || _fnRef$current === void 0 ? void 0 : _fnRef$current.call.apply(_fnRef$current, [fnRef].concat(args));
	}, []);
}
//#endregion
//#region node_modules/.pnpm/rc-util@5.44.4_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-util/es/Dom/canUseDom.js
function canUseDom() {
	return !!(typeof window !== "undefined" && window.document && window.document.createElement);
}
//#endregion
//#region node_modules/.pnpm/rc-util@5.44.4_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-util/es/hooks/useLayoutEffect.js
/**
* Wrap `React.useLayoutEffect` which will not throw warning message in test env
*/
var useInternalLayoutEffect = canUseDom() ? import_react.useLayoutEffect : import_react.useEffect;
var useLayoutEffect = function useLayoutEffect(callback, deps) {
	var firstMountRef = import_react.useRef(true);
	useInternalLayoutEffect(function() {
		return callback(firstMountRef.current);
	}, deps);
	useInternalLayoutEffect(function() {
		firstMountRef.current = false;
		return function() {
			firstMountRef.current = true;
		};
	}, []);
};
var useLayoutUpdateEffect = function useLayoutUpdateEffect(callback, deps) {
	useLayoutEffect(function(firstMount) {
		if (!firstMount) return callback();
	}, deps);
};
//#endregion
//#region node_modules/.pnpm/rc-util@5.44.4_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-util/es/hooks/useState.js
/**
* Same as React.useState but `setState` accept `ignoreDestroy` param to not to setState after destroyed.
* We do not make this auto is to avoid real memory leak.
* Developer should confirm it's safe to ignore themselves.
*/
function useSafeState(defaultValue) {
	var destroyRef = import_react.useRef(false);
	var _React$useState2 = _slicedToArray(import_react.useState(defaultValue), 2), value = _React$useState2[0], setValue = _React$useState2[1];
	import_react.useEffect(function() {
		destroyRef.current = false;
		return function() {
			destroyRef.current = true;
		};
	}, []);
	function safeSetState(updater, ignoreDestroy) {
		if (ignoreDestroy && destroyRef.current) return;
		setValue(updater);
	}
	return [value, safeSetState];
}
//#endregion
//#region node_modules/.pnpm/rc-util@5.44.4_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-util/es/hooks/useMergedState.js
/** We only think `undefined` is empty */
function hasValue(value) {
	return value !== void 0;
}
/**
* Similar to `useState` but will use props value if provided.
* Note that internal use rc-util `useState` hook.
*/
function useMergedState(defaultStateValue, option) {
	var _ref = option || {}, defaultValue = _ref.defaultValue, value = _ref.value, onChange = _ref.onChange, postState = _ref.postState;
	var _useState2 = _slicedToArray(useSafeState(function() {
		if (hasValue(value)) return value;
		else if (hasValue(defaultValue)) return typeof defaultValue === "function" ? defaultValue() : defaultValue;
		else return typeof defaultStateValue === "function" ? defaultStateValue() : defaultStateValue;
	}), 2), innerValue = _useState2[0], setInnerValue = _useState2[1];
	var mergedValue = value !== void 0 ? value : innerValue;
	var postMergedValue = postState ? postState(mergedValue) : mergedValue;
	var onChangeFn = useEvent(onChange);
	var _useState4 = _slicedToArray(useSafeState([mergedValue]), 2), prevValue = _useState4[0], setPrevValue = _useState4[1];
	useLayoutUpdateEffect(function() {
		var prev = prevValue[0];
		if (innerValue !== prev) onChangeFn(innerValue, prev);
	}, [prevValue]);
	useLayoutUpdateEffect(function() {
		if (!hasValue(value)) setInnerValue(value);
	}, [value]);
	return [postMergedValue, useEvent(function(updater, ignoreDestroy) {
		setInnerValue(updater, ignoreDestroy);
		setPrevValue([mergedValue], ignoreDestroy);
	})];
}
//#endregion
//#region node_modules/.pnpm/rc-util@5.44.4_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-util/es/warning.js
var warned = {};
var preWarningFns = [];
/**
* Pre warning enable you to parse content before console.error.
* Modify to null will prevent warning.
*/
var preMessage = function preMessage(fn) {
	preWarningFns.push(fn);
};
/**
* Warning if condition not match.
* @param valid Condition
* @param message Warning message
* @example
* ```js
* warning(false, 'some error'); // print some error
* warning(true, 'some error'); // print nothing
* warning(1 === 2, 'some error'); // print some error
* ```
*/
function warning(valid, message) {
	if (!valid && console !== void 0) {
		var finalMessage = preWarningFns.reduce(function(msg, preMessageFn) {
			return preMessageFn(msg !== null && msg !== void 0 ? msg : "", "warning");
		}, message);
		if (finalMessage) console.error("Warning: ".concat(finalMessage));
	}
}
/** @see Similar to {@link warning} */
function note(valid, message) {
	if (!valid && console !== void 0) {
		var finalMessage = preWarningFns.reduce(function(msg, preMessageFn) {
			return preMessageFn(msg !== null && msg !== void 0 ? msg : "", "note");
		}, message);
		if (finalMessage) console.warn("Note: ".concat(finalMessage));
	}
}
function resetWarned() {
	warned = {};
}
function call(method, valid, message) {
	if (!valid && !warned[message]) {
		method(false, message);
		warned[message] = true;
	}
}
/** @see Same as {@link warning}, but only warn once for the same message */
function warningOnce(valid, message) {
	call(warning, valid, message);
}
/** @see Same as {@link warning}, but only warn once for the same message */
function noteOnce(valid, message) {
	call(note, valid, message);
}
warningOnce.preMessage = preMessage;
warningOnce.resetWarned = resetWarned;
warningOnce.noteOnce = noteOnce;
//#endregion
//#region node_modules/.pnpm/rc-util@5.44.4_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-util/es/isEqual.js
/**
* Deeply compares two object literals.
* @param obj1 object 1
* @param obj2 object 2
* @param shallow shallow compare
* @returns
*/
function isEqual(obj1, obj2) {
	var shallow = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
	var refSet = /* @__PURE__ */ new Set();
	function deepEqual(a, b) {
		var level = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 1;
		var circular = refSet.has(a);
		warningOnce(!circular, "Warning: There may be circular references");
		if (circular) return false;
		if (a === b) return true;
		if (shallow && level > 1) return false;
		refSet.add(a);
		var newLevel = level + 1;
		if (Array.isArray(a)) {
			if (!Array.isArray(b) || a.length !== b.length) return false;
			for (var i = 0; i < a.length; i++) if (!deepEqual(a[i], b[i], newLevel)) return false;
			return true;
		}
		if (a && b && _typeof(a) === "object" && _typeof(b) === "object") {
			var keys = Object.keys(a);
			if (keys.length !== Object.keys(b).length) return false;
			return keys.every(function(key) {
				return deepEqual(a[key], b[key], newLevel);
			});
		}
		return false;
	}
	return deepEqual(obj1, obj2);
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/objectWithoutProperties.js
function _objectWithoutProperties(e, t) {
	if (null == e) return {};
	var o, r, i = _objectWithoutPropertiesLoose(e, t);
	if (Object.getOwnPropertySymbols) {
		var n = Object.getOwnPropertySymbols(e);
		for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
	}
	return i;
}
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/util.js
function getOffset(value, min, max) {
	return (value - min) / (max - min);
}
function getDirectionStyle(direction, value, min, max) {
	var offset = getOffset(value, min, max);
	var positionStyle = {};
	switch (direction) {
		case "rtl":
			positionStyle.right = "".concat(offset * 100, "%");
			positionStyle.transform = "translateX(50%)";
			break;
		case "btt":
			positionStyle.bottom = "".concat(offset * 100, "%");
			positionStyle.transform = "translateY(50%)";
			break;
		case "ttb":
			positionStyle.top = "".concat(offset * 100, "%");
			positionStyle.transform = "translateY(-50%)";
			break;
		default:
			positionStyle.left = "".concat(offset * 100, "%");
			positionStyle.transform = "translateX(-50%)";
			break;
	}
	return positionStyle;
}
/** Return index value if is list or return value directly */
function getIndex(value, index) {
	return Array.isArray(value) ? value[index] : value;
}
//#endregion
//#region node_modules/.pnpm/rc-util@5.44.4_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-util/es/KeyCode.js
/**
* @ignore
* some key-codes definition and utils from closure-library
* @author yiminghe@gmail.com
*/
var KeyCode = {
	/**
	* MAC_ENTER
	*/
	MAC_ENTER: 3,
	/**
	* BACKSPACE
	*/
	BACKSPACE: 8,
	/**
	* TAB
	*/
	TAB: 9,
	/**
	* NUMLOCK on FF/Safari Mac
	*/
	NUM_CENTER: 12,
	/**
	* ENTER
	*/
	ENTER: 13,
	/**
	* SHIFT
	*/
	SHIFT: 16,
	/**
	* CTRL
	*/
	CTRL: 17,
	/**
	* ALT
	*/
	ALT: 18,
	/**
	* PAUSE
	*/
	PAUSE: 19,
	/**
	* CAPS_LOCK
	*/
	CAPS_LOCK: 20,
	/**
	* ESC
	*/
	ESC: 27,
	/**
	* SPACE
	*/
	SPACE: 32,
	/**
	* PAGE_UP
	*/
	PAGE_UP: 33,
	/**
	* PAGE_DOWN
	*/
	PAGE_DOWN: 34,
	/**
	* END
	*/
	END: 35,
	/**
	* HOME
	*/
	HOME: 36,
	/**
	* LEFT
	*/
	LEFT: 37,
	/**
	* UP
	*/
	UP: 38,
	/**
	* RIGHT
	*/
	RIGHT: 39,
	/**
	* DOWN
	*/
	DOWN: 40,
	/**
	* PRINT_SCREEN
	*/
	PRINT_SCREEN: 44,
	/**
	* INSERT
	*/
	INSERT: 45,
	/**
	* DELETE
	*/
	DELETE: 46,
	/**
	* ZERO
	*/
	ZERO: 48,
	/**
	* ONE
	*/
	ONE: 49,
	/**
	* TWO
	*/
	TWO: 50,
	/**
	* THREE
	*/
	THREE: 51,
	/**
	* FOUR
	*/
	FOUR: 52,
	/**
	* FIVE
	*/
	FIVE: 53,
	/**
	* SIX
	*/
	SIX: 54,
	/**
	* SEVEN
	*/
	SEVEN: 55,
	/**
	* EIGHT
	*/
	EIGHT: 56,
	/**
	* NINE
	*/
	NINE: 57,
	/**
	* QUESTION_MARK
	*/
	QUESTION_MARK: 63,
	/**
	* A
	*/
	A: 65,
	/**
	* B
	*/
	B: 66,
	/**
	* C
	*/
	C: 67,
	/**
	* D
	*/
	D: 68,
	/**
	* E
	*/
	E: 69,
	/**
	* F
	*/
	F: 70,
	/**
	* G
	*/
	G: 71,
	/**
	* H
	*/
	H: 72,
	/**
	* I
	*/
	I: 73,
	/**
	* J
	*/
	J: 74,
	/**
	* K
	*/
	K: 75,
	/**
	* L
	*/
	L: 76,
	/**
	* M
	*/
	M: 77,
	/**
	* N
	*/
	N: 78,
	/**
	* O
	*/
	O: 79,
	/**
	* P
	*/
	P: 80,
	/**
	* Q
	*/
	Q: 81,
	/**
	* R
	*/
	R: 82,
	/**
	* S
	*/
	S: 83,
	/**
	* T
	*/
	T: 84,
	/**
	* U
	*/
	U: 85,
	/**
	* V
	*/
	V: 86,
	/**
	* W
	*/
	W: 87,
	/**
	* X
	*/
	X: 88,
	/**
	* Y
	*/
	Y: 89,
	/**
	* Z
	*/
	Z: 90,
	/**
	* META
	*/
	META: 91,
	/**
	* WIN_KEY_RIGHT
	*/
	WIN_KEY_RIGHT: 92,
	/**
	* CONTEXT_MENU
	*/
	CONTEXT_MENU: 93,
	/**
	* NUM_ZERO
	*/
	NUM_ZERO: 96,
	/**
	* NUM_ONE
	*/
	NUM_ONE: 97,
	/**
	* NUM_TWO
	*/
	NUM_TWO: 98,
	/**
	* NUM_THREE
	*/
	NUM_THREE: 99,
	/**
	* NUM_FOUR
	*/
	NUM_FOUR: 100,
	/**
	* NUM_FIVE
	*/
	NUM_FIVE: 101,
	/**
	* NUM_SIX
	*/
	NUM_SIX: 102,
	/**
	* NUM_SEVEN
	*/
	NUM_SEVEN: 103,
	/**
	* NUM_EIGHT
	*/
	NUM_EIGHT: 104,
	/**
	* NUM_NINE
	*/
	NUM_NINE: 105,
	/**
	* NUM_MULTIPLY
	*/
	NUM_MULTIPLY: 106,
	/**
	* NUM_PLUS
	*/
	NUM_PLUS: 107,
	/**
	* NUM_MINUS
	*/
	NUM_MINUS: 109,
	/**
	* NUM_PERIOD
	*/
	NUM_PERIOD: 110,
	/**
	* NUM_DIVISION
	*/
	NUM_DIVISION: 111,
	/**
	* F1
	*/
	F1: 112,
	/**
	* F2
	*/
	F2: 113,
	/**
	* F3
	*/
	F3: 114,
	/**
	* F4
	*/
	F4: 115,
	/**
	* F5
	*/
	F5: 116,
	/**
	* F6
	*/
	F6: 117,
	/**
	* F7
	*/
	F7: 118,
	/**
	* F8
	*/
	F8: 119,
	/**
	* F9
	*/
	F9: 120,
	/**
	* F10
	*/
	F10: 121,
	/**
	* F11
	*/
	F11: 122,
	/**
	* F12
	*/
	F12: 123,
	/**
	* NUMLOCK
	*/
	NUMLOCK: 144,
	/**
	* SEMICOLON
	*/
	SEMICOLON: 186,
	/**
	* DASH
	*/
	DASH: 189,
	/**
	* EQUALS
	*/
	EQUALS: 187,
	/**
	* COMMA
	*/
	COMMA: 188,
	/**
	* PERIOD
	*/
	PERIOD: 190,
	/**
	* SLASH
	*/
	SLASH: 191,
	/**
	* APOSTROPHE
	*/
	APOSTROPHE: 192,
	/**
	* SINGLE_QUOTE
	*/
	SINGLE_QUOTE: 222,
	/**
	* OPEN_SQUARE_BRACKET
	*/
	OPEN_SQUARE_BRACKET: 219,
	/**
	* BACKSLASH
	*/
	BACKSLASH: 220,
	/**
	* CLOSE_SQUARE_BRACKET
	*/
	CLOSE_SQUARE_BRACKET: 221,
	/**
	* WIN_KEY
	*/
	WIN_KEY: 224,
	/**
	* MAC_FF_META
	*/
	MAC_FF_META: 224,
	/**
	* WIN_IME
	*/
	WIN_IME: 229,
	/**
	* whether text and modified key is entered at the same time.
	*/
	isTextModifyingKeyEvent: function isTextModifyingKeyEvent(e) {
		var keyCode = e.keyCode;
		if (e.altKey && !e.ctrlKey || e.metaKey || keyCode >= KeyCode.F1 && keyCode <= KeyCode.F12) return false;
		switch (keyCode) {
			case KeyCode.ALT:
			case KeyCode.CAPS_LOCK:
			case KeyCode.CONTEXT_MENU:
			case KeyCode.CTRL:
			case KeyCode.DOWN:
			case KeyCode.END:
			case KeyCode.ESC:
			case KeyCode.HOME:
			case KeyCode.INSERT:
			case KeyCode.LEFT:
			case KeyCode.MAC_FF_META:
			case KeyCode.META:
			case KeyCode.NUMLOCK:
			case KeyCode.NUM_CENTER:
			case KeyCode.PAGE_DOWN:
			case KeyCode.PAGE_UP:
			case KeyCode.PAUSE:
			case KeyCode.PRINT_SCREEN:
			case KeyCode.RIGHT:
			case KeyCode.SHIFT:
			case KeyCode.UP:
			case KeyCode.WIN_KEY:
			case KeyCode.WIN_KEY_RIGHT: return false;
			default: return true;
		}
	},
	/**
	* whether character is entered.
	*/
	isCharacterKey: function isCharacterKey(keyCode) {
		if (keyCode >= KeyCode.ZERO && keyCode <= KeyCode.NINE) return true;
		if (keyCode >= KeyCode.NUM_ZERO && keyCode <= KeyCode.NUM_MULTIPLY) return true;
		if (keyCode >= KeyCode.A && keyCode <= KeyCode.Z) return true;
		if (window.navigator.userAgent.indexOf("WebKit") !== -1 && keyCode === 0) return true;
		switch (keyCode) {
			case KeyCode.SPACE:
			case KeyCode.QUESTION_MARK:
			case KeyCode.NUM_PLUS:
			case KeyCode.NUM_MINUS:
			case KeyCode.NUM_PERIOD:
			case KeyCode.NUM_DIVISION:
			case KeyCode.SEMICOLON:
			case KeyCode.DASH:
			case KeyCode.EQUALS:
			case KeyCode.COMMA:
			case KeyCode.PERIOD:
			case KeyCode.SLASH:
			case KeyCode.APOSTROPHE:
			case KeyCode.SINGLE_QUOTE:
			case KeyCode.OPEN_SQUARE_BRACKET:
			case KeyCode.BACKSLASH:
			case KeyCode.CLOSE_SQUARE_BRACKET: return true;
			default: return false;
		}
	}
};
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/context.js
var SliderContext = /*#__PURE__*/ import_react.createContext({
	min: 0,
	max: 0,
	direction: "ltr",
	step: 1,
	includedStart: 0,
	includedEnd: 0,
	tabIndex: 0,
	keyboard: true,
	styles: {},
	classNames: {}
});
/** @private NOT PROMISE AVAILABLE. DO NOT USE IN PRODUCTION. */
var UnstableContext = /*#__PURE__*/ import_react.createContext({});
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/Handles/Handle.js
var _excluded$1 = [
	"prefixCls",
	"value",
	"valueIndex",
	"onStartMove",
	"onDelete",
	"style",
	"render",
	"dragging",
	"draggingDelete",
	"onOffsetChange",
	"onChangeComplete",
	"onFocus",
	"onMouseEnter"
];
var Handle = /*#__PURE__*/ import_react.forwardRef(function(props, ref) {
	var prefixCls = props.prefixCls, value = props.value, valueIndex = props.valueIndex, onStartMove = props.onStartMove, onDelete = props.onDelete, style = props.style, render = props.render, dragging = props.dragging, draggingDelete = props.draggingDelete, onOffsetChange = props.onOffsetChange, onChangeComplete = props.onChangeComplete, onFocus = props.onFocus, onMouseEnter = props.onMouseEnter, restProps = _objectWithoutProperties(props, _excluded$1);
	var _React$useContext = import_react.useContext(SliderContext), min = _React$useContext.min, max = _React$useContext.max, direction = _React$useContext.direction, disabled = _React$useContext.disabled, keyboard = _React$useContext.keyboard, range = _React$useContext.range, tabIndex = _React$useContext.tabIndex, ariaLabelForHandle = _React$useContext.ariaLabelForHandle, ariaLabelledByForHandle = _React$useContext.ariaLabelledByForHandle, ariaRequired = _React$useContext.ariaRequired, ariaValueTextFormatterForHandle = _React$useContext.ariaValueTextFormatterForHandle, styles = _React$useContext.styles, classNames = _React$useContext.classNames;
	var handlePrefixCls = "".concat(prefixCls, "-handle");
	var onInternalStartMove = function onInternalStartMove(e) {
		if (!disabled) onStartMove(e, valueIndex);
	};
	var onInternalFocus = function onInternalFocus(e) {
		onFocus === null || onFocus === void 0 || onFocus(e, valueIndex);
	};
	var onInternalMouseEnter = function onInternalMouseEnter(e) {
		onMouseEnter(e, valueIndex);
	};
	var onKeyDown = function onKeyDown(e) {
		if (!disabled && keyboard) {
			var offset = null;
			switch (e.which || e.keyCode) {
				case KeyCode.LEFT:
					offset = direction === "ltr" || direction === "btt" ? -1 : 1;
					break;
				case KeyCode.RIGHT:
					offset = direction === "ltr" || direction === "btt" ? 1 : -1;
					break;
				case KeyCode.UP:
					offset = direction !== "ttb" ? 1 : -1;
					break;
				case KeyCode.DOWN:
					offset = direction !== "ttb" ? -1 : 1;
					break;
				case KeyCode.HOME:
					offset = "min";
					break;
				case KeyCode.END:
					offset = "max";
					break;
				case KeyCode.PAGE_UP:
					offset = 2;
					break;
				case KeyCode.PAGE_DOWN:
					offset = -2;
					break;
				case KeyCode.BACKSPACE:
				case KeyCode.DELETE:
					onDelete === null || onDelete === void 0 || onDelete(valueIndex);
					break;
			}
			if (offset !== null) {
				e.preventDefault();
				onOffsetChange(offset, valueIndex);
			}
		}
	};
	var handleKeyUp = function handleKeyUp(e) {
		switch (e.which || e.keyCode) {
			case KeyCode.LEFT:
			case KeyCode.RIGHT:
			case KeyCode.UP:
			case KeyCode.DOWN:
			case KeyCode.HOME:
			case KeyCode.END:
			case KeyCode.PAGE_UP:
			case KeyCode.PAGE_DOWN:
				onChangeComplete === null || onChangeComplete === void 0 || onChangeComplete();
				break;
		}
	};
	var positionStyle = getDirectionStyle(direction, value, min, max);
	var divProps = {};
	if (valueIndex !== null) {
		var _getIndex;
		divProps = {
			tabIndex: disabled ? null : getIndex(tabIndex, valueIndex),
			role: "slider",
			"aria-valuemin": min,
			"aria-valuemax": max,
			"aria-valuenow": value,
			"aria-disabled": disabled,
			"aria-label": getIndex(ariaLabelForHandle, valueIndex),
			"aria-labelledby": getIndex(ariaLabelledByForHandle, valueIndex),
			"aria-required": getIndex(ariaRequired, valueIndex),
			"aria-valuetext": (_getIndex = getIndex(ariaValueTextFormatterForHandle, valueIndex)) === null || _getIndex === void 0 ? void 0 : _getIndex(value),
			"aria-orientation": direction === "ltr" || direction === "rtl" ? "horizontal" : "vertical",
			onMouseDown: onInternalStartMove,
			onTouchStart: onInternalStartMove,
			onFocus: onInternalFocus,
			onMouseEnter: onInternalMouseEnter,
			onKeyDown,
			onKeyUp: handleKeyUp
		};
	}
	var handleNode = /*#__PURE__*/ import_react.createElement("div", _extends({
		ref,
		className: (0, import_classnames.default)(handlePrefixCls, _defineProperty(_defineProperty(_defineProperty({}, "".concat(handlePrefixCls, "-").concat(valueIndex + 1), valueIndex !== null && range), "".concat(handlePrefixCls, "-dragging"), dragging), "".concat(handlePrefixCls, "-dragging-delete"), draggingDelete), classNames.handle),
		style: _objectSpread2(_objectSpread2(_objectSpread2({}, positionStyle), style), styles.handle)
	}, divProps, restProps));
	if (render) handleNode = render(handleNode, {
		index: valueIndex,
		prefixCls,
		value,
		dragging,
		draggingDelete
	});
	return handleNode;
});
Handle.displayName = "Handle";
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/Handles/index.js
var _excluded = [
	"prefixCls",
	"style",
	"onStartMove",
	"onOffsetChange",
	"values",
	"handleRender",
	"activeHandleRender",
	"draggingIndex",
	"draggingDelete",
	"onFocus"
];
var Handles = /*#__PURE__*/ import_react.forwardRef(function(props, ref) {
	var prefixCls = props.prefixCls, style = props.style, onStartMove = props.onStartMove, onOffsetChange = props.onOffsetChange, values = props.values, handleRender = props.handleRender, activeHandleRender = props.activeHandleRender, draggingIndex = props.draggingIndex, draggingDelete = props.draggingDelete, onFocus = props.onFocus, restProps = _objectWithoutProperties(props, _excluded);
	var handlesRef = import_react.useRef({});
	var _React$useState2 = _slicedToArray(import_react.useState(false), 2), activeVisible = _React$useState2[0], setActiveVisible = _React$useState2[1];
	var _React$useState4 = _slicedToArray(import_react.useState(-1), 2), activeIndex = _React$useState4[0], setActiveIndex = _React$useState4[1];
	var onActive = function onActive(index) {
		setActiveIndex(index);
		setActiveVisible(true);
	};
	var onHandleFocus = function onHandleFocus(e, index) {
		onActive(index);
		onFocus === null || onFocus === void 0 || onFocus(e);
	};
	var onHandleMouseEnter = function onHandleMouseEnter(e, index) {
		onActive(index);
	};
	import_react.useImperativeHandle(ref, function() {
		return {
			focus: function focus(index) {
				var _handlesRef$current$i;
				(_handlesRef$current$i = handlesRef.current[index]) === null || _handlesRef$current$i === void 0 || _handlesRef$current$i.focus();
			},
			hideHelp: function hideHelp() {
				(0, import_react_dom.flushSync)(function() {
					setActiveVisible(false);
				});
			}
		};
	});
	var handleProps = _objectSpread2({
		prefixCls,
		onStartMove,
		onOffsetChange,
		render: handleRender,
		onFocus: onHandleFocus,
		onMouseEnter: onHandleMouseEnter
	}, restProps);
	return /*#__PURE__*/ import_react.createElement(import_react.Fragment, null, values.map(function(value, index) {
		var dragging = draggingIndex === index;
		return /*#__PURE__*/ import_react.createElement(Handle, _extends({
			ref: function ref(node) {
				if (!node) delete handlesRef.current[index];
				else handlesRef.current[index] = node;
			},
			dragging,
			draggingDelete: dragging && draggingDelete,
			style: getIndex(style, index),
			key: index,
			value,
			valueIndex: index
		}, handleProps));
	}), activeHandleRender && activeVisible && /*#__PURE__*/ import_react.createElement(Handle, _extends({ key: "a11y" }, handleProps, {
		value: values[activeIndex],
		valueIndex: null,
		dragging: draggingIndex !== -1,
		draggingDelete,
		render: activeHandleRender,
		style: { pointerEvents: "none" },
		tabIndex: null,
		"aria-hidden": true
	})));
});
Handles.displayName = "Handles";
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/Marks/Mark.js
var Mark = function Mark(props) {
	var prefixCls = props.prefixCls, style = props.style, children = props.children, value = props.value, _onClick = props.onClick;
	var _React$useContext = import_react.useContext(SliderContext), min = _React$useContext.min, max = _React$useContext.max, direction = _React$useContext.direction, includedStart = _React$useContext.includedStart, includedEnd = _React$useContext.includedEnd, included = _React$useContext.included;
	var textCls = "".concat(prefixCls, "-text");
	var positionStyle = getDirectionStyle(direction, value, min, max);
	return /*#__PURE__*/ import_react.createElement("span", {
		className: (0, import_classnames.default)(textCls, _defineProperty({}, "".concat(textCls, "-active"), included && includedStart <= value && value <= includedEnd)),
		style: _objectSpread2(_objectSpread2({}, positionStyle), style),
		onMouseDown: function onMouseDown(e) {
			e.stopPropagation();
		},
		onClick: function onClick() {
			_onClick(value);
		}
	}, children);
};
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/Marks/index.js
var Marks = function Marks(props) {
	var prefixCls = props.prefixCls, marks = props.marks, onClick = props.onClick;
	var markPrefixCls = "".concat(prefixCls, "-mark");
	if (!marks.length) return null;
	return /*#__PURE__*/ import_react.createElement("div", { className: markPrefixCls }, marks.map(function(_ref) {
		var value = _ref.value, style = _ref.style, label = _ref.label;
		return /*#__PURE__*/ import_react.createElement(Mark, {
			key: value,
			prefixCls: markPrefixCls,
			style,
			value,
			onClick
		}, label);
	}));
};
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/Steps/Dot.js
var Dot = function Dot(props) {
	var prefixCls = props.prefixCls, value = props.value, style = props.style, activeStyle = props.activeStyle;
	var _React$useContext = import_react.useContext(SliderContext), min = _React$useContext.min, max = _React$useContext.max, direction = _React$useContext.direction, included = _React$useContext.included, includedStart = _React$useContext.includedStart, includedEnd = _React$useContext.includedEnd;
	var dotClassName = "".concat(prefixCls, "-dot");
	var active = included && includedStart <= value && value <= includedEnd;
	var mergedStyle = _objectSpread2(_objectSpread2({}, getDirectionStyle(direction, value, min, max)), typeof style === "function" ? style(value) : style);
	if (active) mergedStyle = _objectSpread2(_objectSpread2({}, mergedStyle), typeof activeStyle === "function" ? activeStyle(value) : activeStyle);
	return /*#__PURE__*/ import_react.createElement("span", {
		className: (0, import_classnames.default)(dotClassName, _defineProperty({}, "".concat(dotClassName, "-active"), active)),
		style: mergedStyle
	});
};
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/Steps/index.js
var Steps = function Steps(props) {
	var prefixCls = props.prefixCls, marks = props.marks, dots = props.dots, style = props.style, activeStyle = props.activeStyle;
	var _React$useContext = import_react.useContext(SliderContext), min = _React$useContext.min, max = _React$useContext.max, step = _React$useContext.step;
	var stepDots = import_react.useMemo(function() {
		var dotSet = /* @__PURE__ */ new Set();
		marks.forEach(function(mark) {
			dotSet.add(mark.value);
		});
		if (dots && step !== null) {
			var current = min;
			while (current <= max) {
				dotSet.add(current);
				current += step;
			}
		}
		return Array.from(dotSet);
	}, [
		min,
		max,
		step,
		dots,
		marks
	]);
	return /*#__PURE__*/ import_react.createElement("div", { className: "".concat(prefixCls, "-step") }, stepDots.map(function(dotValue) {
		return /*#__PURE__*/ import_react.createElement(Dot, {
			prefixCls,
			key: dotValue,
			value: dotValue,
			style,
			activeStyle
		});
	}));
};
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/Tracks/Track.js
var Track = function Track(props) {
	var prefixCls = props.prefixCls, style = props.style, start = props.start, end = props.end, index = props.index, onStartMove = props.onStartMove, replaceCls = props.replaceCls;
	var _React$useContext = import_react.useContext(SliderContext), direction = _React$useContext.direction, min = _React$useContext.min, max = _React$useContext.max, disabled = _React$useContext.disabled, range = _React$useContext.range, classNames = _React$useContext.classNames;
	var trackPrefixCls = "".concat(prefixCls, "-track");
	var offsetStart = getOffset(start, min, max);
	var offsetEnd = getOffset(end, min, max);
	var onInternalStartMove = function onInternalStartMove(e) {
		if (!disabled && onStartMove) onStartMove(e, -1);
	};
	var positionStyle = {};
	switch (direction) {
		case "rtl":
			positionStyle.right = "".concat(offsetStart * 100, "%");
			positionStyle.width = "".concat(offsetEnd * 100 - offsetStart * 100, "%");
			break;
		case "btt":
			positionStyle.bottom = "".concat(offsetStart * 100, "%");
			positionStyle.height = "".concat(offsetEnd * 100 - offsetStart * 100, "%");
			break;
		case "ttb":
			positionStyle.top = "".concat(offsetStart * 100, "%");
			positionStyle.height = "".concat(offsetEnd * 100 - offsetStart * 100, "%");
			break;
		default:
			positionStyle.left = "".concat(offsetStart * 100, "%");
			positionStyle.width = "".concat(offsetEnd * 100 - offsetStart * 100, "%");
	}
	var className = replaceCls || (0, import_classnames.default)(trackPrefixCls, _defineProperty(_defineProperty({}, "".concat(trackPrefixCls, "-").concat(index + 1), index !== null && range), "".concat(prefixCls, "-track-draggable"), onStartMove), classNames.track);
	return /*#__PURE__*/ import_react.createElement("div", {
		className,
		style: _objectSpread2(_objectSpread2({}, positionStyle), style),
		onMouseDown: onInternalStartMove,
		onTouchStart: onInternalStartMove
	});
};
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/Tracks/index.js
var Tracks = function Tracks(props) {
	var prefixCls = props.prefixCls, style = props.style, values = props.values, startPoint = props.startPoint, onStartMove = props.onStartMove;
	var _React$useContext = import_react.useContext(SliderContext), included = _React$useContext.included, range = _React$useContext.range, min = _React$useContext.min, styles = _React$useContext.styles, classNames = _React$useContext.classNames;
	var trackList = import_react.useMemo(function() {
		if (!range) {
			if (values.length === 0) return [];
			var startValue = startPoint !== null && startPoint !== void 0 ? startPoint : min;
			var endValue = values[0];
			return [{
				start: Math.min(startValue, endValue),
				end: Math.max(startValue, endValue)
			}];
		}
		var list = [];
		for (var i = 0; i < values.length - 1; i += 1) list.push({
			start: values[i],
			end: values[i + 1]
		});
		return list;
	}, [
		values,
		range,
		startPoint,
		min
	]);
	if (!included) return null;
	var tracksNode = trackList !== null && trackList !== void 0 && trackList.length && (classNames.tracks || styles.tracks) ? /*#__PURE__*/ import_react.createElement(Track, {
		index: null,
		prefixCls,
		start: trackList[0].start,
		end: trackList[trackList.length - 1].end,
		replaceCls: (0, import_classnames.default)(classNames.tracks, "".concat(prefixCls, "-tracks")),
		style: styles.tracks
	}) : null;
	return /*#__PURE__*/ import_react.createElement(import_react.Fragment, null, tracksNode, trackList.map(function(_ref, index) {
		var start = _ref.start, end = _ref.end;
		return /*#__PURE__*/ import_react.createElement(Track, {
			index,
			prefixCls,
			style: _objectSpread2(_objectSpread2({}, getIndex(style, index)), styles.track),
			start,
			end,
			key: index,
			onStartMove
		});
	}));
};
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/hooks/useDrag.js
/** Drag to delete offset. It's a user experience number for dragging out */
var REMOVE_DIST = 130;
function getPosition(e) {
	var obj = "targetTouches" in e ? e.targetTouches[0] : e;
	return {
		pageX: obj.pageX,
		pageY: obj.pageY
	};
}
function useDrag(containerRef, direction, rawValues, min, max, formatValue, triggerChange, finishChange, offsetValues, editable, minCount) {
	var _React$useState2 = _slicedToArray(import_react.useState(null), 2), draggingValue = _React$useState2[0], setDraggingValue = _React$useState2[1];
	var _React$useState4 = _slicedToArray(import_react.useState(-1), 2), draggingIndex = _React$useState4[0], setDraggingIndex = _React$useState4[1];
	var _React$useState6 = _slicedToArray(import_react.useState(false), 2), draggingDelete = _React$useState6[0], setDraggingDelete = _React$useState6[1];
	var _React$useState8 = _slicedToArray(import_react.useState(rawValues), 2), cacheValues = _React$useState8[0], setCacheValues = _React$useState8[1];
	var _React$useState10 = _slicedToArray(import_react.useState(rawValues), 2), originValues = _React$useState10[0], setOriginValues = _React$useState10[1];
	var mouseMoveEventRef = import_react.useRef(null);
	var mouseUpEventRef = import_react.useRef(null);
	var touchEventTargetRef = import_react.useRef(null);
	var _React$useContext = import_react.useContext(UnstableContext), onDragStart = _React$useContext.onDragStart, onDragChange = _React$useContext.onDragChange;
	useLayoutEffect(function() {
		if (draggingIndex === -1) setCacheValues(rawValues);
	}, [rawValues, draggingIndex]);
	import_react.useEffect(function() {
		return function() {
			document.removeEventListener("mousemove", mouseMoveEventRef.current);
			document.removeEventListener("mouseup", mouseUpEventRef.current);
			if (touchEventTargetRef.current) {
				touchEventTargetRef.current.removeEventListener("touchmove", mouseMoveEventRef.current);
				touchEventTargetRef.current.removeEventListener("touchend", mouseUpEventRef.current);
			}
		};
	}, []);
	var flushValues = function flushValues(nextValues, nextValue, deleteMark) {
		if (nextValue !== void 0) setDraggingValue(nextValue);
		setCacheValues(nextValues);
		var changeValues = nextValues;
		if (deleteMark) changeValues = nextValues.filter(function(_, i) {
			return i !== draggingIndex;
		});
		triggerChange(changeValues);
		if (onDragChange) onDragChange({
			rawValues: nextValues,
			deleteIndex: deleteMark ? draggingIndex : -1,
			draggingIndex,
			draggingValue: nextValue
		});
	};
	var updateCacheValue = useEvent(function(valueIndex, offsetPercent, deleteMark) {
		if (valueIndex === -1) {
			var startValue = originValues[0];
			var endValue = originValues[originValues.length - 1];
			var maxStartOffset = min - startValue;
			var maxEndOffset = max - endValue;
			var offset = offsetPercent * (max - min);
			offset = Math.max(offset, maxStartOffset);
			offset = Math.min(offset, maxEndOffset);
			offset = formatValue(startValue + offset) - startValue;
			flushValues(originValues.map(function(val) {
				return val + offset;
			}));
		} else {
			var offsetDist = (max - min) * offsetPercent;
			var cloneValues = _toConsumableArray(cacheValues);
			cloneValues[valueIndex] = originValues[valueIndex];
			var next = offsetValues(cloneValues, offsetDist, valueIndex, "dist");
			flushValues(next.values, next.value, deleteMark);
		}
	});
	return [
		draggingIndex,
		draggingValue,
		draggingDelete,
		import_react.useMemo(function() {
			var sourceValues = _toConsumableArray(rawValues).sort(function(a, b) {
				return a - b;
			});
			var targetValues = _toConsumableArray(cacheValues).sort(function(a, b) {
				return a - b;
			});
			var counts = {};
			targetValues.forEach(function(val) {
				counts[val] = (counts[val] || 0) + 1;
			});
			sourceValues.forEach(function(val) {
				counts[val] = (counts[val] || 0) - 1;
			});
			var maxDiffCount = editable ? 1 : 0;
			return Object.values(counts).reduce(function(prev, next) {
				return prev + Math.abs(next);
			}, 0) <= maxDiffCount ? cacheValues : rawValues;
		}, [
			rawValues,
			cacheValues,
			editable
		]),
		function onStartMove(e, valueIndex, startValues) {
			e.stopPropagation();
			var initialValues = startValues || rawValues;
			var originValue = initialValues[valueIndex];
			setDraggingIndex(valueIndex);
			setDraggingValue(originValue);
			setOriginValues(initialValues);
			setCacheValues(initialValues);
			setDraggingDelete(false);
			var _getPosition = getPosition(e), startX = _getPosition.pageX, startY = _getPosition.pageY;
			var deleteMark = false;
			if (onDragStart) onDragStart({
				rawValues: initialValues,
				draggingIndex: valueIndex,
				draggingValue: originValue
			});
			var onMouseMove = function onMouseMove(event) {
				event.preventDefault();
				var _getPosition2 = getPosition(event), moveX = _getPosition2.pageX, moveY = _getPosition2.pageY;
				var offsetX = moveX - startX;
				var offsetY = moveY - startY;
				var _containerRef$current = containerRef.current.getBoundingClientRect(), width = _containerRef$current.width, height = _containerRef$current.height;
				var offSetPercent;
				var removeDist;
				switch (direction) {
					case "btt":
						offSetPercent = -offsetY / height;
						removeDist = offsetX;
						break;
					case "ttb":
						offSetPercent = offsetY / height;
						removeDist = offsetX;
						break;
					case "rtl":
						offSetPercent = -offsetX / width;
						removeDist = offsetY;
						break;
					default:
						offSetPercent = offsetX / width;
						removeDist = offsetY;
				}
				deleteMark = editable ? Math.abs(removeDist) > REMOVE_DIST && minCount < cacheValues.length : false;
				setDraggingDelete(deleteMark);
				updateCacheValue(valueIndex, offSetPercent, deleteMark);
			};
			var onMouseUp = function onMouseUp(event) {
				event.preventDefault();
				document.removeEventListener("mouseup", onMouseUp);
				document.removeEventListener("mousemove", onMouseMove);
				if (touchEventTargetRef.current) {
					touchEventTargetRef.current.removeEventListener("touchmove", mouseMoveEventRef.current);
					touchEventTargetRef.current.removeEventListener("touchend", mouseUpEventRef.current);
				}
				mouseMoveEventRef.current = null;
				mouseUpEventRef.current = null;
				touchEventTargetRef.current = null;
				finishChange(deleteMark);
				setDraggingIndex(-1);
				setDraggingDelete(false);
			};
			document.addEventListener("mouseup", onMouseUp);
			document.addEventListener("mousemove", onMouseMove);
			e.currentTarget.addEventListener("touchend", onMouseUp);
			e.currentTarget.addEventListener("touchmove", onMouseMove);
			mouseMoveEventRef.current = onMouseMove;
			mouseUpEventRef.current = onMouseUp;
			touchEventTargetRef.current = e.currentTarget;
		}
	];
}
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/hooks/useOffset.js
/** Format the value in the range of [min, max] */
/** Format value align with step */
/** Format value align with step & marks */
function useOffset(min, max, step, markList, allowCross, pushable) {
	var formatRangeValue = import_react.useCallback(function(val) {
		return Math.max(min, Math.min(max, val));
	}, [min, max]);
	var formatStepValue = import_react.useCallback(function(val) {
		if (step !== null) {
			var stepValue = min + Math.round((formatRangeValue(val) - min) / step) * step;
			var getDecimal = function getDecimal(num) {
				return (String(num).split(".")[1] || "").length;
			};
			var maxDecimal = Math.max(getDecimal(step), getDecimal(max), getDecimal(min));
			var fixedValue = Number(stepValue.toFixed(maxDecimal));
			return min <= fixedValue && fixedValue <= max ? fixedValue : null;
		}
		return null;
	}, [
		step,
		min,
		max,
		formatRangeValue
	]);
	var formatValue = import_react.useCallback(function(val) {
		var formatNextValue = formatRangeValue(val);
		var alignValues = markList.map(function(mark) {
			return mark.value;
		});
		if (step !== null) alignValues.push(formatStepValue(val));
		alignValues.push(min, max);
		var closeValue = alignValues[0];
		var closeDist = max - min;
		alignValues.forEach(function(alignValue) {
			var dist = Math.abs(formatNextValue - alignValue);
			if (dist <= closeDist) {
				closeValue = alignValue;
				closeDist = dist;
			}
		});
		return closeValue;
	}, [
		min,
		max,
		markList,
		step,
		formatRangeValue,
		formatStepValue
	]);
	var offsetValue = function offsetValue(values, offset, valueIndex) {
		var mode = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "unit";
		if (typeof offset === "number") {
			var nextValue;
			var originValue = values[valueIndex];
			var targetDistValue = originValue + offset;
			var potentialValues = [];
			markList.forEach(function(mark) {
				potentialValues.push(mark.value);
			});
			potentialValues.push(min, max);
			potentialValues.push(formatStepValue(originValue));
			var sign = offset > 0 ? 1 : -1;
			if (mode === "unit") potentialValues.push(formatStepValue(originValue + sign * step));
			else potentialValues.push(formatStepValue(targetDistValue));
			potentialValues = potentialValues.filter(function(val) {
				return val !== null;
			}).filter(function(val) {
				return offset < 0 ? val <= originValue : val >= originValue;
			});
			if (mode === "unit") potentialValues = potentialValues.filter(function(val) {
				return val !== originValue;
			});
			var compareValue = mode === "unit" ? originValue : targetDistValue;
			nextValue = potentialValues[0];
			var valueDist = Math.abs(nextValue - compareValue);
			potentialValues.forEach(function(potentialValue) {
				var dist = Math.abs(potentialValue - compareValue);
				if (dist < valueDist) {
					nextValue = potentialValue;
					valueDist = dist;
				}
			});
			if (nextValue === void 0) return offset < 0 ? min : max;
			if (mode === "dist") return nextValue;
			if (Math.abs(offset) > 1) {
				var cloneValues = _toConsumableArray(values);
				cloneValues[valueIndex] = nextValue;
				return offsetValue(cloneValues, offset - sign, valueIndex, mode);
			}
			return nextValue;
		} else if (offset === "min") return min;
		else if (offset === "max") return max;
	};
	/** Same as `offsetValue` but return `changed` mark to tell value changed */
	var offsetChangedValue = function offsetChangedValue(values, offset, valueIndex) {
		var mode = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "unit";
		var originValue = values[valueIndex];
		var nextValue = offsetValue(values, offset, valueIndex, mode);
		return {
			value: nextValue,
			changed: nextValue !== originValue
		};
	};
	var needPush = function needPush(dist) {
		return pushable === null && dist === 0 || typeof pushable === "number" && dist < pushable;
	};
	return [formatValue, function offsetValues(values, offset, valueIndex) {
		var mode = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "unit";
		var nextValues = values.map(formatValue);
		var originValue = nextValues[valueIndex];
		nextValues[valueIndex] = offsetValue(nextValues, offset, valueIndex, mode);
		if (allowCross === false) {
			var pushNum = pushable || 0;
			if (valueIndex > 0 && nextValues[valueIndex - 1] !== originValue) nextValues[valueIndex] = Math.max(nextValues[valueIndex], nextValues[valueIndex - 1] + pushNum);
			if (valueIndex < nextValues.length - 1 && nextValues[valueIndex + 1] !== originValue) nextValues[valueIndex] = Math.min(nextValues[valueIndex], nextValues[valueIndex + 1] - pushNum);
		} else if (typeof pushable === "number" || pushable === null) {
			for (var i = valueIndex + 1; i < nextValues.length; i += 1) {
				var changed = true;
				while (needPush(nextValues[i] - nextValues[i - 1]) && changed) {
					var _offsetChangedValue = offsetChangedValue(nextValues, 1, i);
					nextValues[i] = _offsetChangedValue.value;
					changed = _offsetChangedValue.changed;
				}
			}
			for (var _i = valueIndex; _i > 0; _i -= 1) {
				var _changed = true;
				while (needPush(nextValues[_i] - nextValues[_i - 1]) && _changed) {
					var _offsetChangedValue2 = offsetChangedValue(nextValues, -1, _i - 1);
					nextValues[_i - 1] = _offsetChangedValue2.value;
					_changed = _offsetChangedValue2.changed;
				}
			}
			for (var _i2 = nextValues.length - 1; _i2 > 0; _i2 -= 1) {
				var _changed2 = true;
				while (needPush(nextValues[_i2] - nextValues[_i2 - 1]) && _changed2) {
					var _offsetChangedValue3 = offsetChangedValue(nextValues, -1, _i2 - 1);
					nextValues[_i2 - 1] = _offsetChangedValue3.value;
					_changed2 = _offsetChangedValue3.changed;
				}
			}
			for (var _i3 = 0; _i3 < nextValues.length - 1; _i3 += 1) {
				var _changed3 = true;
				while (needPush(nextValues[_i3 + 1] - nextValues[_i3]) && _changed3) {
					var _offsetChangedValue4 = offsetChangedValue(nextValues, 1, _i3 + 1);
					nextValues[_i3 + 1] = _offsetChangedValue4.value;
					_changed3 = _offsetChangedValue4.changed;
				}
			}
		}
		return {
			value: nextValues[valueIndex],
			values: nextValues
		};
	}];
}
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/hooks/useRange.js
function useRange(range) {
	return (0, import_react.useMemo)(function() {
		if (range === true || !range) return [
			!!range,
			false,
			false,
			0
		];
		var editable = range.editable, draggableTrack = range.draggableTrack, minCount = range.minCount, maxCount = range.maxCount;
		warning(!editable || !draggableTrack, "`editable` can not work with `draggableTrack`.");
		return [
			true,
			editable,
			!editable && draggableTrack,
			minCount || 0,
			maxCount
		];
	}, [range]);
}
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/Slider.js
/**
* New:
* - click mark to update range value
* - handleRender
* - Fix handle with count not correct
* - Fix pushable not work in some case
* - No more FindDOMNode
* - Move all position related style into inline style
* - Key: up is plus, down is minus
* - fix Key with step = null not align with marks
* - Change range should not trigger onChange
* - keyboard support pushable
*/
var Slider$1 = /*#__PURE__*/ import_react.forwardRef(function(props, ref) {
	var _props$prefixCls = props.prefixCls, prefixCls = _props$prefixCls === void 0 ? "rc-slider" : _props$prefixCls, className = props.className, style = props.style, classNames = props.classNames, styles = props.styles, id = props.id, _props$disabled = props.disabled, disabled = _props$disabled === void 0 ? false : _props$disabled, _props$keyboard = props.keyboard, keyboard = _props$keyboard === void 0 ? true : _props$keyboard, autoFocus = props.autoFocus, onFocus = props.onFocus, onBlur = props.onBlur, _props$min = props.min, min = _props$min === void 0 ? 0 : _props$min, _props$max = props.max, max = _props$max === void 0 ? 100 : _props$max, _props$step = props.step, step = _props$step === void 0 ? 1 : _props$step, value = props.value, defaultValue = props.defaultValue, range = props.range, count = props.count, onChange = props.onChange, onBeforeChange = props.onBeforeChange, onAfterChange = props.onAfterChange, onChangeComplete = props.onChangeComplete, _props$allowCross = props.allowCross, allowCross = _props$allowCross === void 0 ? true : _props$allowCross, _props$pushable = props.pushable, pushable = _props$pushable === void 0 ? false : _props$pushable, reverse = props.reverse, vertical = props.vertical, _props$included = props.included, included = _props$included === void 0 ? true : _props$included, startPoint = props.startPoint, trackStyle = props.trackStyle, handleStyle = props.handleStyle, railStyle = props.railStyle, dotStyle = props.dotStyle, activeDotStyle = props.activeDotStyle, marks = props.marks, dots = props.dots, handleRender = props.handleRender, activeHandleRender = props.activeHandleRender, track = props.track, _props$tabIndex = props.tabIndex, tabIndex = _props$tabIndex === void 0 ? 0 : _props$tabIndex, ariaLabelForHandle = props.ariaLabelForHandle, ariaLabelledByForHandle = props.ariaLabelledByForHandle, ariaRequired = props.ariaRequired, ariaValueTextFormatterForHandle = props.ariaValueTextFormatterForHandle;
	var handlesRef = import_react.useRef(null);
	var containerRef = import_react.useRef(null);
	var direction = import_react.useMemo(function() {
		if (vertical) return reverse ? "ttb" : "btt";
		return reverse ? "rtl" : "ltr";
	}, [reverse, vertical]);
	var _useRange2 = _slicedToArray(useRange(range), 5), rangeEnabled = _useRange2[0], rangeEditable = _useRange2[1], rangeDraggableTrack = _useRange2[2], minCount = _useRange2[3], maxCount = _useRange2[4];
	var mergedMin = import_react.useMemo(function() {
		return isFinite(min) ? min : 0;
	}, [min]);
	var mergedMax = import_react.useMemo(function() {
		return isFinite(max) ? max : 100;
	}, [max]);
	var mergedStep = import_react.useMemo(function() {
		return step !== null && step <= 0 ? 1 : step;
	}, [step]);
	var mergedPush = import_react.useMemo(function() {
		if (typeof pushable === "boolean") return pushable ? mergedStep : false;
		return pushable >= 0 ? pushable : false;
	}, [pushable, mergedStep]);
	var markList = import_react.useMemo(function() {
		return Object.keys(marks || {}).map(function(key) {
			var mark = marks[key];
			var markObj = { value: Number(key) };
			if (mark && _typeof(mark) === "object" && !/*#__PURE__*/ import_react.isValidElement(mark) && ("label" in mark || "style" in mark)) {
				markObj.style = mark.style;
				markObj.label = mark.label;
			} else markObj.label = mark;
			return markObj;
		}).filter(function(_ref) {
			var label = _ref.label;
			return label || typeof label === "number";
		}).sort(function(a, b) {
			return a.value - b.value;
		});
	}, [marks]);
	var _useOffset2 = _slicedToArray(useOffset(mergedMin, mergedMax, mergedStep, markList, allowCross, mergedPush), 2), formatValue = _useOffset2[0], offsetValues = _useOffset2[1];
	var _useMergedState2 = _slicedToArray(useMergedState(defaultValue, { value }), 2), mergedValue = _useMergedState2[0], setValue = _useMergedState2[1];
	var rawValues = import_react.useMemo(function() {
		var valueList = mergedValue === null || mergedValue === void 0 ? [] : Array.isArray(mergedValue) ? mergedValue : [mergedValue];
		var _valueList$ = _slicedToArray(valueList, 1)[0];
		var returnValues = mergedValue === null ? [] : [_valueList$ === void 0 ? mergedMin : _valueList$];
		if (rangeEnabled) {
			returnValues = _toConsumableArray(valueList);
			if (count || mergedValue === void 0) {
				var pointCount = count >= 0 ? count + 1 : 2;
				returnValues = returnValues.slice(0, pointCount);
				while (returnValues.length < pointCount) {
					var _returnValues;
					returnValues.push((_returnValues = returnValues[returnValues.length - 1]) !== null && _returnValues !== void 0 ? _returnValues : mergedMin);
				}
			}
			returnValues.sort(function(a, b) {
				return a - b;
			});
		}
		returnValues.forEach(function(val, index) {
			returnValues[index] = formatValue(val);
		});
		return returnValues;
	}, [
		mergedValue,
		rangeEnabled,
		mergedMin,
		count,
		formatValue
	]);
	var getTriggerValue = function getTriggerValue(triggerValues) {
		return rangeEnabled ? triggerValues : triggerValues[0];
	};
	var triggerChange = useEvent(function(nextValues) {
		var cloneNextValues = _toConsumableArray(nextValues).sort(function(a, b) {
			return a - b;
		});
		if (onChange && !isEqual(cloneNextValues, rawValues, true)) onChange(getTriggerValue(cloneNextValues));
		setValue(cloneNextValues);
	});
	var finishChange = useEvent(function(draggingDelete) {
		if (draggingDelete) handlesRef.current.hideHelp();
		var finishValue = getTriggerValue(rawValues);
		onAfterChange === null || onAfterChange === void 0 || onAfterChange(finishValue);
		warningOnce(!onAfterChange, "[rc-slider] `onAfterChange` is deprecated. Please use `onChangeComplete` instead.");
		onChangeComplete === null || onChangeComplete === void 0 || onChangeComplete(finishValue);
	});
	var onDelete = function onDelete(index) {
		if (disabled || !rangeEditable || rawValues.length <= minCount) return;
		var cloneNextValues = _toConsumableArray(rawValues);
		cloneNextValues.splice(index, 1);
		onBeforeChange === null || onBeforeChange === void 0 || onBeforeChange(getTriggerValue(cloneNextValues));
		triggerChange(cloneNextValues);
		var nextFocusIndex = Math.max(0, index - 1);
		handlesRef.current.hideHelp();
		handlesRef.current.focus(nextFocusIndex);
	};
	var _useDrag2 = _slicedToArray(useDrag(containerRef, direction, rawValues, mergedMin, mergedMax, formatValue, triggerChange, finishChange, offsetValues, rangeEditable, minCount), 5), draggingIndex = _useDrag2[0], draggingValue = _useDrag2[1], draggingDelete = _useDrag2[2], cacheValues = _useDrag2[3], onStartDrag = _useDrag2[4];
	/**
	* When `rangeEditable` will insert a new value in the values array.
	* Else it will replace the value in the values array.
	*/
	var changeToCloseValue = function changeToCloseValue(newValue, e) {
		if (!disabled) {
			var cloneNextValues = _toConsumableArray(rawValues);
			var valueIndex = 0;
			var valueBeforeIndex = 0;
			var valueDist = mergedMax - mergedMin;
			rawValues.forEach(function(val, index) {
				var dist = Math.abs(newValue - val);
				if (dist <= valueDist) {
					valueDist = dist;
					valueIndex = index;
				}
				if (val < newValue) valueBeforeIndex = index;
			});
			var focusIndex = valueIndex;
			if (rangeEditable && valueDist !== 0 && (!maxCount || rawValues.length < maxCount)) {
				cloneNextValues.splice(valueBeforeIndex + 1, 0, newValue);
				focusIndex = valueBeforeIndex + 1;
			} else cloneNextValues[valueIndex] = newValue;
			if (rangeEnabled && !rawValues.length && count === void 0) cloneNextValues.push(newValue);
			var nextValue = getTriggerValue(cloneNextValues);
			onBeforeChange === null || onBeforeChange === void 0 || onBeforeChange(nextValue);
			triggerChange(cloneNextValues);
			if (e) {
				var _document$activeEleme, _document$activeEleme2;
				(_document$activeEleme = document.activeElement) === null || _document$activeEleme === void 0 || (_document$activeEleme2 = _document$activeEleme.blur) === null || _document$activeEleme2 === void 0 || _document$activeEleme2.call(_document$activeEleme);
				handlesRef.current.focus(focusIndex);
				onStartDrag(e, focusIndex, cloneNextValues);
			} else {
				onAfterChange === null || onAfterChange === void 0 || onAfterChange(nextValue);
				warningOnce(!onAfterChange, "[rc-slider] `onAfterChange` is deprecated. Please use `onChangeComplete` instead.");
				onChangeComplete === null || onChangeComplete === void 0 || onChangeComplete(nextValue);
			}
		}
	};
	var onSliderMouseDown = function onSliderMouseDown(e) {
		e.preventDefault();
		var _containerRef$current = containerRef.current.getBoundingClientRect(), width = _containerRef$current.width, height = _containerRef$current.height, left = _containerRef$current.left, top = _containerRef$current.top, bottom = _containerRef$current.bottom, right = _containerRef$current.right;
		var clientX = e.clientX, clientY = e.clientY;
		var percent;
		switch (direction) {
			case "btt":
				percent = (bottom - clientY) / height;
				break;
			case "ttb":
				percent = (clientY - top) / height;
				break;
			case "rtl":
				percent = (right - clientX) / width;
				break;
			default: percent = (clientX - left) / width;
		}
		changeToCloseValue(formatValue(mergedMin + percent * (mergedMax - mergedMin)), e);
	};
	var _React$useState2 = _slicedToArray(import_react.useState(null), 2), keyboardValue = _React$useState2[0], setKeyboardValue = _React$useState2[1];
	var onHandleOffsetChange = function onHandleOffsetChange(offset, valueIndex) {
		if (!disabled) {
			var next = offsetValues(rawValues, offset, valueIndex);
			onBeforeChange === null || onBeforeChange === void 0 || onBeforeChange(getTriggerValue(rawValues));
			triggerChange(next.values);
			setKeyboardValue(next.value);
		}
	};
	import_react.useEffect(function() {
		if (keyboardValue !== null) {
			var valueIndex = rawValues.indexOf(keyboardValue);
			if (valueIndex >= 0) handlesRef.current.focus(valueIndex);
		}
		setKeyboardValue(null);
	}, [keyboardValue]);
	var mergedDraggableTrack = import_react.useMemo(function() {
		if (rangeDraggableTrack && mergedStep === null) {
			warningOnce(false, "`draggableTrack` is not supported when `step` is `null`.");
			return false;
		}
		return rangeDraggableTrack;
	}, [rangeDraggableTrack, mergedStep]);
	var onStartMove = useEvent(function(e, valueIndex) {
		onStartDrag(e, valueIndex);
		onBeforeChange === null || onBeforeChange === void 0 || onBeforeChange(getTriggerValue(rawValues));
	});
	var dragging = draggingIndex !== -1;
	import_react.useEffect(function() {
		if (!dragging) {
			var valueIndex = rawValues.lastIndexOf(draggingValue);
			handlesRef.current.focus(valueIndex);
		}
	}, [dragging]);
	var sortedCacheValues = import_react.useMemo(function() {
		return _toConsumableArray(cacheValues).sort(function(a, b) {
			return a - b;
		});
	}, [cacheValues]);
	var _React$useMemo2 = _slicedToArray(import_react.useMemo(function() {
		if (!rangeEnabled) return [mergedMin, sortedCacheValues[0]];
		return [sortedCacheValues[0], sortedCacheValues[sortedCacheValues.length - 1]];
	}, [
		sortedCacheValues,
		rangeEnabled,
		mergedMin
	]), 2), includedStart = _React$useMemo2[0], includedEnd = _React$useMemo2[1];
	import_react.useImperativeHandle(ref, function() {
		return {
			focus: function focus() {
				handlesRef.current.focus(0);
			},
			blur: function blur() {
				var _containerRef$current2;
				var activeElement = document.activeElement;
				if ((_containerRef$current2 = containerRef.current) !== null && _containerRef$current2 !== void 0 && _containerRef$current2.contains(activeElement)) activeElement === null || activeElement === void 0 || activeElement.blur();
			}
		};
	});
	import_react.useEffect(function() {
		if (autoFocus) handlesRef.current.focus(0);
	}, []);
	var context = import_react.useMemo(function() {
		return {
			min: mergedMin,
			max: mergedMax,
			direction,
			disabled,
			keyboard,
			step: mergedStep,
			included,
			includedStart,
			includedEnd,
			range: rangeEnabled,
			tabIndex,
			ariaLabelForHandle,
			ariaLabelledByForHandle,
			ariaRequired,
			ariaValueTextFormatterForHandle,
			styles: styles || {},
			classNames: classNames || {}
		};
	}, [
		mergedMin,
		mergedMax,
		direction,
		disabled,
		keyboard,
		mergedStep,
		included,
		includedStart,
		includedEnd,
		rangeEnabled,
		tabIndex,
		ariaLabelForHandle,
		ariaLabelledByForHandle,
		ariaRequired,
		ariaValueTextFormatterForHandle,
		styles,
		classNames
	]);
	return /*#__PURE__*/ import_react.createElement(SliderContext.Provider, { value: context }, /*#__PURE__*/ import_react.createElement("div", {
		ref: containerRef,
		className: (0, import_classnames.default)(prefixCls, className, _defineProperty(_defineProperty(_defineProperty(_defineProperty({}, "".concat(prefixCls, "-disabled"), disabled), "".concat(prefixCls, "-vertical"), vertical), "".concat(prefixCls, "-horizontal"), !vertical), "".concat(prefixCls, "-with-marks"), markList.length)),
		style,
		onMouseDown: onSliderMouseDown,
		id
	}, /*#__PURE__*/ import_react.createElement("div", {
		className: (0, import_classnames.default)("".concat(prefixCls, "-rail"), classNames === null || classNames === void 0 ? void 0 : classNames.rail),
		style: _objectSpread2(_objectSpread2({}, railStyle), styles === null || styles === void 0 ? void 0 : styles.rail)
	}), track !== false && /*#__PURE__*/ import_react.createElement(Tracks, {
		prefixCls,
		style: trackStyle,
		values: rawValues,
		startPoint,
		onStartMove: mergedDraggableTrack ? onStartMove : void 0
	}), /*#__PURE__*/ import_react.createElement(Steps, {
		prefixCls,
		marks: markList,
		dots,
		style: dotStyle,
		activeStyle: activeDotStyle
	}), /*#__PURE__*/ import_react.createElement(Handles, {
		ref: handlesRef,
		prefixCls,
		style: handleStyle,
		values: cacheValues,
		draggingIndex,
		draggingDelete,
		onStartMove,
		onOffsetChange: onHandleOffsetChange,
		onFocus,
		onBlur,
		handleRender,
		activeHandleRender,
		onChangeComplete: finishChange,
		onDelete: rangeEditable ? onDelete : void 0
	}), /*#__PURE__*/ import_react.createElement(Marks, {
		prefixCls,
		marks: markList,
		onClick: changeToCloseValue
	})));
});
Slider$1.displayName = "Slider";
//#endregion
//#region node_modules/.pnpm/rc-slider@11.1.9_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/rc-slider/es/index.js
var es_default = Slider$1;
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Slider/BaseSlider/BaseSlider.js
var b$29 = block("base-slider");
var BaseSlider = import_react.forwardRef(function BaseSlider({ stateModifiers, ...restProps }, ref) {
	return (0, import_jsx_runtime.jsx)(es_default, {
		...restProps,
		ref,
		className: b$29(stateModifiers),
		classNames: {
			handle: b$29("handle", stateModifiers),
			rail: b$29("rail", stateModifiers),
			track: b$29("track", stateModifiers)
		},
		pushable: false,
		dots: false,
		keyboard: true
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Slider/SliderTooltip/SliderTooltipPin.js
var SliderTooltipPin = ({ className }) => (0, import_jsx_runtime.jsx)("span", {
	className,
	children: (0, import_jsx_runtime.jsx)("svg", {
		width: "12",
		height: "6",
		viewBox: "0 0 12 6",
		fill: "none",
		xmlns: "http://www.w3.org/2000/svg",
		children: (0, import_jsx_runtime.jsx)("path", {
			d: "M5.99996 5.02325C5.99996 5.02325 7.7674 -1.90735e-06 11.3023 -1.90735e-06H0.697632C4.23252 -1.90735e-06 5.99996 5.02325 5.99996 5.02325Z",
			fill: "currentColor"
		})
	})
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Slider/SliderTooltip/SliderTooltip.js
var b$28 = block("slider-tooltip");
var SliderTooltip = ({ className, style, stateModifiers, children }) => (0, import_jsx_runtime.jsx)("div", {
	className: b$28(stateModifiers, className),
	style,
	children: (0, import_jsx_runtime.jsxs)("div", {
		className: b$28("card", stateModifiers),
		children: [
			children,
			stateModifiers.disabled && (0, import_jsx_runtime.jsx)(SliderTooltipPin, { className: b$28("pin", { background: true }) }),
			(0, import_jsx_runtime.jsx)(SliderTooltipPin, { className: b$28("pin") })
		]
	})
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Slider/HandleWithTooltip/HandleWithTooltip.js
var HandleWithTooltip = ({ originHandle, originHandleProps, stateModifiers, tooltipFormat, className }) => {
	const autoVisible = stateModifiers["tooltip-display"] === "auto";
	const alwaysVisible = stateModifiers["tooltip-display"] === "on";
	const [tooltipVisible, setTooltipVisible] = import_react.useState(false);
	const [focused, setFocused] = import_react.useState(false);
	const [hovered, setHovered] = import_react.useState(false);
	if (alwaysVisible && !tooltipVisible) setTooltipVisible(true);
	if (autoVisible && !tooltipVisible && originHandleProps.dragging) setTooltipVisible(true);
	const styleProp = stateModifiers.rtl ? "right" : "left";
	const tooltipContent = tooltipFormat ? tooltipFormat(originHandleProps.value) : originHandleProps.value;
	const handleTooltipVisibility = ({ currentHovered, currentFocused }) => {
		if (autoVisible) setTooltipVisible((currentHovered === void 0 ? hovered : currentHovered) || (currentFocused === void 0 ? focused : currentFocused));
	};
	const handle = alwaysVisible ? originHandle : import_react.cloneElement(originHandle, {
		onMouseEnter: (event) => {
			originHandle.props.onMouseEnter?.(event);
			setHovered(true);
			handleTooltipVisibility({ currentHovered: true });
		},
		onMouseLeave: (event) => {
			originHandle.props.onMouseLeave?.(event);
			setHovered(false);
			handleTooltipVisibility({ currentHovered: false });
		},
		onFocus: (event) => {
			originHandle.props.onFocus?.(event);
			setFocused(true);
			handleTooltipVisibility({ currentFocused: true });
		},
		onBlur: (event) => {
			originHandle.props.onBlur?.(event);
			setFocused(false);
			handleTooltipVisibility({ currentFocused: false });
		}
	});
	return (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [handle, tooltipVisible && (0, import_jsx_runtime.jsx)(SliderTooltip, {
		className,
		style: { insetInlineStart: originHandle.props.style?.[styleProp] },
		stateModifiers,
		children: tooltipContent
	})] });
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Slider/constants.js
var CLEAR_MARK_STYLE = {
	left: "",
	right: "",
	transform: ""
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Slider/utils.js
function prepareSingleValue({ value, min, max }) {
	if (typeof value === "undefined" || value < min) return min;
	else if (value > max) return max;
	return value;
}
function prepareStartPoint({ min, max, startPoint }) {
	if (startPoint === void 0) return;
	return prepareSingleValue({
		min,
		max,
		value: startPoint
	});
}
function prepareArrayValue({ value = [], min = 0, max = 100 }) {
	return [prepareSingleValue({
		max,
		min,
		value: value[0]
	}), prepareSingleValue({
		max,
		min,
		value: value[1]
	})].sort((v1, v2) => v1 - v2);
}
function calculateMarksArray({ count = 0, max, min }) {
	if (!count) return [];
	if (max === min) return [min];
	if (count === 1) {
		const step = Math.abs(max - min) / 2;
		return [Math.round((min + step) * 100) / 100];
	}
	if (count > 2) {
		const points = [];
		const step = Math.abs(max - min) / (count - 1);
		for (let i = 0; i < count; i++) points.push(Math.round((min + step * i) * 100) / 100);
		return points;
	}
	return [min, max];
}
function createMarks({ points, markFormat, min, max }) {
	const marks = {};
	points.forEach((point) => {
		const pointContent = markFormat ? markFormat(point) : point;
		if (point === min || point === max) marks[point] = {
			label: pointContent,
			style: CLEAR_MARK_STYLE
		};
		else marks[point] = pointContent;
	});
	return marks;
}
/**
* Calculates the basic properties of the Slider component depending on the passed parameters
* @returns {SliderInnerState} Properties to pass to the Slider
*/
function prepareSliderInnerState({ max = 100, min = 0, defaultValue, step, value, markFormat, marks, tooltipDisplay, tooltipFormat, startPoint }) {
	const state = {
		value,
		defaultValue: defaultValue ?? min,
		range: false,
		max,
		min,
		step,
		tooltipDisplay
	};
	state.tooltipFormat = tooltipFormat ? tooltipFormat : markFormat;
	if (max < min) {
		state.max = min;
		state.min = max;
	}
	if (Array.isArray(marks)) state.marks = createMarks({
		points: marks,
		markFormat,
		min: state.min,
		max: state.max
	});
	else state.marks = marks === 0 ? {} : createMarks({
		points: calculateMarksArray({
			count: marks,
			max,
			min
		}),
		markFormat,
		min,
		max
	});
	state.startPoint = prepareStartPoint({
		min: state.min,
		max: state.max,
		startPoint
	});
	if (defaultValue === void 0) state.defaultValue = state.startPoint === void 0 ? state.min : state.startPoint;
	if (value === void 0) {
		const isArray = Array.isArray(state.defaultValue);
		state.range = isArray;
		state.defaultValue = isArray ? prepareArrayValue({
			min: state.min,
			max: state.max,
			value: state.defaultValue
		}) : prepareSingleValue({
			min: state.min,
			max: state.max,
			value: state.defaultValue
		});
	} else {
		const isArray = Array.isArray(value);
		state.range = isArray;
		state.value = isArray ? prepareArrayValue({
			min: state.min,
			max: state.max,
			value
		}) : prepareSingleValue({
			min: state.min,
			max: state.max,
			value
		});
	}
	return state;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Slider/Slider.js
var b$27 = block("slider");
var Slider = import_react.forwardRef(function Slider(rawProps, ref) {
	const { value, defaultValue, size = "m", min = 0, max = 100, step = 1, markFormat, marks = 2, tooltipDisplay, tooltipFormat = markFormat, errorMessage, validationState, disabled = false, startPoint, inverted, onBlur, onUpdate, onUpdateComplete, onFocus, autoFocus = false, tabIndex, className, style, qa, apiRef, "aria-label": ariaLabelForHandle, "aria-labelledby": ariaLabelledByForHandle, name, form, ...restProps } = useDefaultProps("Slider", rawProps);
	const direction = useDirection();
	const innerState = prepareSliderInnerState({
		defaultValue,
		max,
		min,
		step,
		value,
		markFormat,
		marks,
		tooltipDisplay,
		tooltipFormat,
		startPoint: inverted ? Math.max(min, max) : startPoint
	});
	const [innerValue, setValue] = useControlledState(innerState.value, innerState.defaultValue, onUpdate);
	const inputRef = useFormResetHandler({
		initialValue: innerValue,
		onReset: import_react.useCallback((v) => {
			setValue(v);
			onUpdateComplete?.(v);
		}, [onUpdateComplete, setValue])
	});
	const stateModifiers = {
		size,
		error: validationState === "invalid" && !disabled,
		disabled,
		"tooltip-display": innerState.tooltipDisplay,
		rtl: direction === "rtl",
		"no-marks": Array.isArray(marks) ? marks.length === 0 : marks === 0,
		inverted: innerState.startPoint === innerState.max && !innerState.range,
		"with-start-point": Boolean(innerState.startPoint && !innerState.range && innerState.startPoint !== innerState.max && innerState.startPoint !== innerState.min)
	};
	const handleRender = (originHandle, originHandleProps) => {
		const handle = innerState.tooltipDisplay === "off" ? originHandle : (0, import_jsx_runtime.jsx)(HandleWithTooltip, {
			originHandle,
			originHandleProps,
			stateModifiers,
			className: b$27("tooltip"),
			tooltipFormat: innerState.tooltipFormat
		});
		return (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [handle, (0, import_jsx_runtime.jsx)("input", {
			ref: inputRef,
			type: "hidden",
			name,
			form,
			value: originHandleProps.value,
			disabled
		})] });
	};
	return (0, import_jsx_runtime.jsxs)("div", {
		...filterDOMProps(restProps),
		className: b$27(null, className),
		style,
		ref,
		children: [
			(0, import_jsx_runtime.jsx)("div", { className: b$27("top", {
				size,
				"tooltip-display": tooltipDisplay
			}) }),
			(0, import_jsx_runtime.jsx)(BaseSlider, {
				ref: apiRef,
				value: innerValue,
				min: innerState.min,
				max: innerState.max,
				step: innerState.step,
				range: innerState.range,
				disabled,
				marks: innerState.marks,
				startPoint: innerState.startPoint,
				onBlur,
				onFocus,
				onChange: setValue,
				onChangeComplete: onUpdateComplete,
				stateModifiers,
				autoFocus,
				tabIndex,
				"data-qa": qa,
				handleRender,
				reverse: stateModifiers.rtl,
				ariaLabelForHandle,
				ariaLabelledByForHandle
			}),
			stateModifiers.error && errorMessage && (0, import_jsx_runtime.jsx)("div", {
				className: b$27("error", { size }),
				children: errorMessage
			})
		]
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Stepper/context.js
var StepperContext = import_react.createContext({
	size: "m",
	onUpdate: void 0,
	value: void 0
});
var useStepperContext = () => {
	return import_react.useContext(StepperContext);
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Stepper/utils.js
var b$26 = block("stepper");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Stepper/StepperItem.js
var StepperItem = import_react.forwardRef((props, ref) => {
	const { id, children, view = "idle", disabled = false, className, icon: customIcon, ...restButtonProps } = props;
	const { onUpdate, value, size } = useStepperContext();
	const onClick = (e) => {
		props.onClick?.(e);
		onUpdate?.(id);
	};
	const icon = import_react.useMemo(() => {
		if (customIcon) return customIcon;
		switch (view) {
			case "idle": return CircleDashed;
			case "error": return CircleExclamation;
			case "success": return CircleCheck;
			default: return CircleDashed;
		}
	}, [view, customIcon]);
	const selectedItem = id === void 0 ? false : id === value;
	return (0, import_jsx_runtime.jsxs)(Button, {
		ref,
		title: typeof children === "string" ? children : void 0,
		...restButtonProps,
		width: "auto",
		className: b$26("item", {
			view,
			disabled,
			selected: selectedItem,
			size
		}, className),
		onClick,
		disabled,
		size,
		view: "outlined",
		children: [(0, import_jsx_runtime.jsx)(Icon, {
			data: icon,
			className: b$26("item-icon", { view })
		}), children]
	});
});
StepperItem.displayName = "StepperItem";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Stepper/StepperSeparator.js
var StepperSeparator = ({ separator }) => {
	const direction = useDirection();
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$26("separator"),
		"aria-hidden": true,
		children: separator ?? (0, import_jsx_runtime.jsx)(Icon, { data: direction === "rtl" ? ChevronLeft : ChevronRight })
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Stepper/Stepper.js
var Stepper = (rawProps) => {
	const props = useDefaultProps("Stepper", rawProps);
	const { children, value, size = "s", className, onUpdate, separator } = props;
	const stepItems = import_react.useMemo(() => {
		return import_react.Children.map(children, (child, index) => {
			const itemId = child.props?.id || index;
			const clonedChild = import_react.cloneElement(child, { id: itemId });
			return (0, import_jsx_runtime.jsxs)("li", {
				className: b$26("list-item"),
				children: [clonedChild, Boolean(index !== import_react.Children.count(children) - 1) && (0, import_jsx_runtime.jsx)(StepperSeparator, { separator })]
			}, itemId);
		});
	}, [children, separator]);
	return (0, import_jsx_runtime.jsx)(StepperContext.Provider, {
		value: {
			size,
			onUpdate,
			value
		},
		children: (0, import_jsx_runtime.jsx)("ol", {
			...filterDOMProps(props, { labelable: true }),
			className: b$26(null, className),
			style: props.style,
			"data-qa": props.qa,
			children: stepItems
		})
	});
};
Stepper.Item = StepperItem;
Stepper.displayName = "Stepper";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Spin/Spin.js
var b$25 = block("spin");
var Spin = import_react.forwardRef(function Spin(rawProps, ref) {
	const { size = "m", style, className, qa } = useDefaultProps("Spin", rawProps);
	return (0, import_jsx_runtime.jsx)("div", {
		ref,
		style,
		className: b$25({ size }, className),
		"data-qa": qa,
		children: (0, import_jsx_runtime.jsx)("div", { className: b$25("inner") })
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Switch/Switch.js
var b$24 = block("switch");
var Switch = import_react.forwardRef(function Switch(rawProps, ref) {
	const props = useDefaultProps("Switch", rawProps);
	const { size = "m", disabled = false, loading = false, content, children, title, style, className, qa } = props;
	const { checked, inputProps } = useCheckbox({
		...props,
		controlProps: {
			...props.controlProps,
			role: "switch"
		}
	});
	const text = content || children;
	const control = (0, import_jsx_runtime.jsxs)("span", {
		className: b$24("indicator"),
		children: [
			(0, import_jsx_runtime.jsx)("input", {
				...inputProps,
				className: b$24("control")
			}),
			(0, import_jsx_runtime.jsx)("span", { className: b$24("outline") }),
			(0, import_jsx_runtime.jsx)("span", { className: b$24("slider") })
		]
	});
	return (0, import_jsx_runtime.jsx)(ControlLabel, {
		ref,
		title,
		style,
		size,
		disabled,
		className: b$24({
			size,
			disabled,
			checked,
			loading
		}, className),
		labelClassName: b$24("text"),
		qa,
		control,
		children: text
	});
});
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseHas.js
var require__baseHas = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/** Used to check objects for own properties. */
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	/**
	* The base implementation of `_.has` without support for deep paths.
	*
	* @private
	* @param {Object} [object] The object to query.
	* @param {Array|string} key The key to check.
	* @returns {boolean} Returns `true` if `key` exists, else `false`.
	*/
	function baseHas(object, key) {
		return object != null && hasOwnProperty.call(object, key);
	}
	module.exports = baseHas;
}));
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/i18n/en.json
var import_has = /* @__PURE__ */ __toESM((/* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseHas = require__baseHas();
	var hasPath = require__hasPath();
	/**
	* Checks if `path` is a direct property of `object`.
	*
	* @static
	* @since 0.1.0
	* @memberOf _
	* @category Object
	* @param {Object} object The object to query.
	* @param {Array|string} path The path to check.
	* @returns {boolean} Returns `true` if `path` exists, else `false`.
	* @example
	*
	* var object = { 'a': { 'b': 2 } };
	* var other = _.create({ 'a': _.create({ 'b': 2 }) });
	*
	* _.has(object, 'a');
	* // => true
	*
	* _.has(object, 'a.b');
	* // => true
	*
	* _.has(object, ['a', 'b']);
	* // => true
	*
	* _.has(other, 'a');
	* // => false
	*/
	function has(object, path) {
		return object != null && hasPath(object, path, baseHas);
	}
	module.exports = has;
})))(), 1);
var i18n_default$5 = addComponentKeysets({
	en: {
		label_empty: "No data",
		"label-actions": "Actions",
		"label-row-select": "Select"
	},
	ru: {
		label_empty: "Нет данных",
		"label-actions": "Действия",
		"label-row-select": "Выбрать"
	}
}, "Table");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/Table.js
var DASH = "—";
function warnAboutPhysicalValues(propName) {
	warnOnce(`[Table] Physical values (left, right) of "${propName}" property are deprecated. Use logical values (start, end) instead.`);
}
function normalizeSides(side, propName) {
	if (side === "left") {
		warnAboutPhysicalValues(propName);
		return "start";
	}
	if (side === "right") {
		warnAboutPhysicalValues(propName);
		return "end";
	}
	return side;
}
var b$23 = block("table");
var EMPTY_VALUES = [
	void 0,
	null,
	""
];
var Table = class Table extends import_react.Component {
	static defaultProps = { edgePadding: true };
	static getRowId(props, item, rowIndex) {
		const { data, getRowId, getRowDescriptor } = props;
		const index = rowIndex ?? data.indexOf(item);
		const descriptor = getRowDescriptor?.(item, index);
		if (descriptor?.id !== void 0) return descriptor.id;
		if (typeof getRowId === "function") return getRowId(item, index);
		if (getRowId && getRowId in item) return String(item[getRowId]);
		return String(index);
	}
	static getHeadCellContent(column) {
		const { id, name } = column;
		let content;
		if (typeof name === "function") content = name();
		else if (typeof name === "string") content = name;
		else content = id;
		return content;
	}
	static getBodyCellContent(column, item, rowIndex) {
		const { id, template, placeholder } = column;
		let placeholderValue;
		if (typeof placeholder === "function") placeholderValue = placeholder(item, rowIndex);
		else placeholderValue = placeholder ?? DASH;
		let value;
		if (typeof template === "function") value = template(item, rowIndex);
		else if (typeof template === "string") value = (0, import_get.default)(item, template);
		else if ((0, import_has.default)(item, id)) value = (0, import_get.default)(item, id);
		if (EMPTY_VALUES.includes(value) && placeholderValue) return placeholderValue;
		return value;
	}
	static getDerivedStateFromProps(props, state) {
		if (props.columns.length === state.columnHeaderRefs.length) return null;
		return { columnHeaderRefs: Array.from(props.columns, () => import_react.createRef()) };
	}
	state = {
		activeScrollElement: "scrollContainer",
		columnsStyles: Array.from(this.props.columns, () => ({})),
		columnHeaderRefs: Array.from(this.props.columns, () => import_react.createRef())
	};
	tableRef = import_react.createRef();
	scrollContainerRef = import_react.createRef();
	horizontalScrollBarRef = import_react.createRef();
	horizontalScrollBarInnerRef = import_react.createRef();
	tableResizeObserver;
	columnsResizeObserver;
	componentDidMount() {
		if (this.props.stickyHorizontalScroll) {
			this.tableResizeObserver = new ResizeObserver((entries) => {
				const { contentRect } = entries[0];
				this.horizontalScrollBarInnerRef.current?.style.setProperty("width", `${contentRect.width}px`);
			});
			if (this.tableRef.current) this.tableResizeObserver.observe(this.tableRef.current);
			if (this.scrollContainerRef.current) {
				this.scrollContainerRef.current.addEventListener("scroll", this.handleScrollContainerScroll);
				this.scrollContainerRef.current.addEventListener("mouseenter", this.handleScrollContainerMouseenter);
			}
			if (this.horizontalScrollBarRef.current) {
				this.horizontalScrollBarRef.current.addEventListener("scroll", this.handleHorizontalScrollBarScroll);
				this.horizontalScrollBarRef.current.addEventListener("mouseenter", this.handleHorizontalScrollBarMouseenter);
			}
		}
		this.columnsResizeObserver = new ResizeObserver((entries) => {
			window.requestAnimationFrame(() => {
				if (!Array.isArray(entries) || !entries.length) return;
				this.updateColumnStyles();
			});
		});
		if (this.tableRef.current) this.columnsResizeObserver.observe(this.tableRef.current);
		this.updateColumnStyles();
	}
	componentDidUpdate(prevProps) {
		if (this.props.columns !== prevProps.columns) this.updateColumnStyles();
	}
	componentWillUnmount() {
		if (this.props.stickyHorizontalScroll) {
			if (this.tableResizeObserver) this.tableResizeObserver.disconnect();
			if (this.scrollContainerRef.current) {
				this.scrollContainerRef.current.removeEventListener("scroll", this.handleScrollContainerScroll);
				this.scrollContainerRef.current.removeEventListener("mouseenter", this.handleScrollContainerMouseenter);
			}
			if (this.horizontalScrollBarRef.current) {
				this.horizontalScrollBarRef.current.removeEventListener("scroll", this.handleHorizontalScrollBarScroll);
				this.horizontalScrollBarRef.current.removeEventListener("mouseenter", this.handleHorizontalScrollBarMouseenter);
			}
		}
		if (this.columnsResizeObserver) this.columnsResizeObserver.disconnect();
	}
	render() {
		const { columns, stickyHorizontalScroll, className, qa } = this.props;
		const withPrimary = columns.some(({ primary }) => primary);
		return (0, import_jsx_runtime.jsx)("div", {
			className: b$23({
				"with-primary": withPrimary,
				"with-sticky-scroll": stickyHorizontalScroll
			}, className),
			"data-qa": qa,
			children: stickyHorizontalScroll ? (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [(0, import_jsx_runtime.jsx)("div", {
				ref: this.scrollContainerRef,
				className: b$23("scroll-container"),
				children: this.renderTable()
			}), this.renderHorizontalScrollBar()] }) : this.renderTable()
		});
	}
	renderHead() {
		const { columns, edgePadding, wordWrap } = this.props;
		const { columnsStyles } = this.state;
		return (0, import_jsx_runtime.jsx)("thead", {
			className: b$23("head"),
			children: (0, import_jsx_runtime.jsx)("tr", {
				className: b$23("row"),
				children: columns.map((column, index) => {
					const { id, align: rawAlign, primary, sticky: rawSticky, className } = column;
					const align = normalizeSides(rawAlign, "column.align");
					const sticky = normalizeSides(rawSticky, "column.sticky");
					const content = Table.getHeadCellContent(column);
					return (0, import_jsx_runtime.jsx)("th", {
						ref: this.state.columnHeaderRefs[index],
						style: columnsStyles[index],
						className: b$23("cell", {
							align,
							primary,
							sticky,
							["edge-padding"]: edgePadding,
							["word-wrap"]: wordWrap
						}, className),
						children: content
					}, id);
				})
			})
		});
	}
	renderBody() {
		const { data } = this.props;
		return (0, import_jsx_runtime.jsx)("tbody", {
			className: b$23("body"),
			children: data.length > 0 ? data.map(this.renderRow) : this.renderEmptyRow()
		});
	}
	renderTable() {
		const { width = "auto" } = this.props;
		return (0, import_jsx_runtime.jsxs)("table", {
			...filterDOMProps(this.props, { labelable: true }),
			ref: this.tableRef,
			className: b$23("table", { width }),
			children: [this.renderHead(), this.renderBody()]
		});
	}
	renderRow = (item, rowIndex) => {
		const { columns, isRowDisabled, onRowClick, onRowMouseEnter, onRowMouseLeave, onRowMouseDown, getRowClassNames, verticalAlign, edgePadding, wordWrap, getRowDescriptor, qa } = this.props;
		const { columnsStyles } = this.state;
		const descriptor = getRowDescriptor?.(item, rowIndex);
		const disabled = descriptor?.disabled || isRowDisabled?.(item, rowIndex) || false;
		const additionalClassNames = descriptor?.classNames || getRowClassNames?.(item, rowIndex) || [];
		const interactive = Boolean(!disabled && (descriptor?.interactive || onRowClick));
		return (0, import_jsx_runtime.jsx)("tr", {
			onClick: !disabled && onRowClick ? onRowClick.bind(null, item, rowIndex) : void 0,
			onMouseEnter: !disabled && onRowMouseEnter ? onRowMouseEnter.bind(null, item, rowIndex) : void 0,
			onMouseLeave: !disabled && onRowMouseLeave ? onRowMouseLeave.bind(null, item, rowIndex) : void 0,
			onMouseDown: !disabled && onRowMouseDown ? onRowMouseDown.bind(null, item, rowIndex) : void 0,
			className: b$23("row", {
				disabled,
				interactive,
				"vertical-align": verticalAlign
			}, additionalClassNames.join(" ")),
			"data-qa": qa && `${qa}-row-${rowIndex}`,
			children: columns.map((column, colIndex) => {
				const { id, align: rawAlign, primary, className, sticky: rawSticky } = column;
				const content = Table.getBodyCellContent(column, item, rowIndex);
				const align = normalizeSides(rawAlign, "column.align");
				const sticky = normalizeSides(rawSticky, "column.sticky");
				return (0, import_jsx_runtime.jsx)("td", {
					style: columnsStyles[colIndex],
					className: b$23("cell", {
						align,
						primary,
						sticky,
						["edge-padding"]: edgePadding,
						["word-wrap"]: wordWrap
					}, className),
					children: content
				}, id);
			})
		}, Table.getRowId(this.props, item, rowIndex));
	};
	renderEmptyRow() {
		const { columns, emptyMessage } = this.props;
		return (0, import_jsx_runtime.jsx)("tr", {
			className: b$23("row", { empty: true }),
			children: (0, import_jsx_runtime.jsx)("td", {
				className: b$23("cell"),
				colSpan: columns.length,
				children: (0, import_jsx_runtime.jsx)(i18n_default$5.Translation, { children: ({ t }) => emptyMessage ? emptyMessage : t("label_empty") })
			})
		});
	}
	renderHorizontalScrollBar() {
		const { stickyHorizontalScroll, stickyHorizontalScrollBreakpoint = 0 } = this.props;
		return (0, import_jsx_runtime.jsx)("div", {
			ref: this.horizontalScrollBarRef,
			className: b$23("horizontal-scroll-bar", { "sticky-horizontal-scroll": stickyHorizontalScroll }),
			style: { bottom: `${stickyHorizontalScrollBreakpoint}px` },
			"data-qa": "sticky-horizontal-scroll-breakpoint-qa",
			children: (0, import_jsx_runtime.jsx)("div", {
				ref: this.horizontalScrollBarInnerRef,
				className: b$23("horizontal-scroll-bar-inner")
			})
		});
	}
	updateColumnStyles() {
		this.setState((prevState) => {
			const columnsWidth = prevState.columnHeaderRefs.map((ref) => ref.current === null ? void 0 : ref.current.getBoundingClientRect().width);
			return { columnsStyles: this.props.columns.map((_, index) => this.getColumnStyles(index, columnsWidth)) };
		});
	}
	getColumnStyles(index, columnsWidth) {
		const { columns } = this.props;
		const column = columns[index];
		const style = {};
		if (typeof column.width === "string") return {
			maxWidth: 0,
			width: column.width
		};
		if (typeof column.width !== "undefined") style.width = column.width;
		if (!column.sticky) return style;
		const filteredColumns = column.sticky === "left" || column.sticky === "start" ? columnsWidth.slice(0, index) : columnsWidth.slice(index + 1);
		const styleName = column.sticky === "left" || column.sticky === "start" ? "insetInlineStart" : "insetInlineEnd";
		style[styleName] = filteredColumns.reduce((start, width) => {
			return typeof width === "number" ? start + width : start;
		}, 0);
		return style;
	}
	handleScrollContainerMouseenter = () => {
		this.setState({ activeScrollElement: "scrollContainer" });
	};
	handleScrollContainerScroll = () => {
		if (this.state.activeScrollElement === "scrollContainer" && this.horizontalScrollBarRef.current && this.scrollContainerRef.current) this.horizontalScrollBarRef.current.scrollLeft = this.scrollContainerRef.current.scrollLeft;
	};
	handleHorizontalScrollBarMouseenter = () => {
		this.setState({ activeScrollElement: "scrollBar" });
	};
	handleHorizontalScrollBarScroll = () => {
		if (this.state.activeScrollElement === "scrollBar" && this.horizontalScrollBarRef.current && this.scrollContainerRef.current) this.scrollContainerRef.current.scrollLeft = this.horizontalScrollBarRef.current.scrollLeft;
	};
};
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseDifference.js
var require__baseDifference = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SetCache = require__SetCache();
	var arrayIncludes = require__arrayIncludes();
	var arrayIncludesWith = require__arrayIncludesWith();
	var arrayMap = require__arrayMap();
	var baseUnary = require__baseUnary();
	var cacheHas = require__cacheHas();
	/** Used as the size to enable large array optimizations. */
	var LARGE_ARRAY_SIZE = 200;
	/**
	* The base implementation of methods like `_.difference` without support
	* for excluding multiple arrays or iteratee shorthands.
	*
	* @private
	* @param {Array} array The array to inspect.
	* @param {Array} values The values to exclude.
	* @param {Function} [iteratee] The iteratee invoked per element.
	* @param {Function} [comparator] The comparator invoked per element.
	* @returns {Array} Returns the new array of filtered values.
	*/
	function baseDifference(array, values, iteratee, comparator) {
		var index = -1, includes = arrayIncludes, isCommon = true, length = array.length, result = [], valuesLength = values.length;
		if (!length) return result;
		if (iteratee) values = arrayMap(values, baseUnary(iteratee));
		if (comparator) {
			includes = arrayIncludesWith;
			isCommon = false;
		} else if (values.length >= LARGE_ARRAY_SIZE) {
			includes = cacheHas;
			isCommon = false;
			values = new SetCache(values);
		}
		outer: while (++index < length) {
			var value = array[index], computed = iteratee == null ? value : iteratee(value);
			value = comparator || value !== 0 ? value : 0;
			if (isCommon && computed === computed) {
				var valuesIndex = valuesLength;
				while (valuesIndex--) if (values[valuesIndex] === computed) continue outer;
				result.push(value);
			} else if (!includes(values, computed, comparator)) result.push(value);
		}
		return result;
	}
	module.exports = baseDifference;
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/difference.js
var require_difference = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseDifference = require__baseDifference();
	var baseFlatten = require__baseFlatten();
	var baseRest = require__baseRest();
	var isArrayLikeObject = require_isArrayLikeObject();
	module.exports = baseRest(function(array, values) {
		return isArrayLikeObject(array) ? baseDifference(array, baseFlatten(values, 1, isArrayLikeObject, true)) : [];
	});
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/union.js
var require_union = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseFlatten = require__baseFlatten();
	var baseRest = require__baseRest();
	var baseUniq = require__baseUniq();
	var isArrayLikeObject = require_isArrayLikeObject();
	module.exports = baseRest(function(arrays) {
		return baseUniq(baseFlatten(arrays, 1, isArrayLikeObject, true));
	});
}));
//#endregion
//#region node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/without.js
var require_without = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var baseDifference = require__baseDifference();
	var baseRest = require__baseRest();
	var isArrayLikeObject = require_isArrayLikeObject();
	module.exports = baseRest(function(array, values) {
		return isArrayLikeObject(array) ? baseDifference(array, values) : [];
	});
}));
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/hoc/withTableSelection/withTableSelection.js
var import_difference = /* @__PURE__ */ __toESM(require_difference(), 1);
var import_union = /* @__PURE__ */ __toESM(require_union(), 1);
var import_without = /* @__PURE__ */ __toESM(require_without(), 1);
var b$22 = block("table");
var selectionColumnId = "_selection";
function withTableSelection(TableComponent) {
	const displayName = `withTableSelection(${getComponentName(TableComponent)})`;
	return class extends import_react.Component {
		static displayName = displayName;
		lastCheckedIndex;
		render() {
			const { selectedIds, onSelectionChange, columns, onRowClick, getRowDescriptor, ...restTableProps } = this.props;
			return (0, import_jsx_runtime.jsx)(TableComponent, {
				...restTableProps,
				columns: this.enhanceColumns(columns),
				onRowClick: this.enhanceOnRowClick(onRowClick),
				getRowDescriptor: this.enhanceGetRowDescriptor(getRowDescriptor)
			});
		}
		renderHeadCell = () => {
			const { data, selectedIds } = this.props;
			let disabled = true;
			let indeterminate = false;
			let checked = true;
			data.forEach((item, index) => {
				if (this.isDisabled(item, index)) return;
				else disabled = false;
				const id = Table.getRowId(this.props, item, index);
				if (selectedIds.includes(id)) indeterminate = true;
				else checked = false;
			});
			if (checked) indeterminate = false;
			if (disabled) {
				checked = false;
				indeterminate = false;
			}
			return this.renderCheckBox({
				disabled,
				checked,
				handler: this.handleAllCheckBoxUpdate,
				indeterminate
			});
		};
		renderBodyCell = (item, index) => {
			const { selectedIds } = this.props;
			const id = Table.getRowId(this.props, item, index);
			const checked = selectedIds.includes(id);
			return this.renderCheckBox({
				disabled: this.isDisabled(item, index),
				checked,
				handler: this.handleCheckBoxUpdate.bind(this, id, index)
			});
		};
		renderCheckBox({ disabled, checked, handler, indeterminate }) {
			return (0, import_jsx_runtime.jsx)(i18n_default$5.Translation, { children: ({ t }) => (0, import_jsx_runtime.jsx)(Checkbox, {
				size: "l",
				checked,
				indeterminate,
				disabled,
				onChange: handler,
				className: b$22("selection-checkbox", { "vertical-align": this.props.verticalAlign }),
				controlProps: { "aria-label": t("label-row-select") }
			}) });
		}
		handleCheckBoxUpdate = (id, index, event) => {
			const { checked } = event.target;
			const isShiftPressed = event.nativeEvent.shiftKey;
			const { data, selectedIds, onSelectionChange } = this.props;
			if (isShiftPressed && this.lastCheckedIndex !== void 0 && this.lastCheckedIndex >= 0) {
				const begin = Math.min(this.lastCheckedIndex, index);
				const end = Math.max(this.lastCheckedIndex, index);
				const diffIds = data.map((item, i) => Table.getRowId(this.props, item, i)).filter((_id, i) => begin <= i && i <= end && !this.isDisabled(data[i], i));
				onSelectionChange(checked ? (0, import_union.default)(selectedIds, diffIds) : (0, import_without.default)(selectedIds, ...diffIds));
			} else onSelectionChange(checked ? [...selectedIds, id] : (0, import_without.default)(selectedIds, id));
			this.lastCheckedIndex = index;
		};
		handleAllCheckBoxUpdate = (event) => {
			const { checked } = event.target;
			const { data, selectedIds, onSelectionChange } = this.props;
			const dataIds = data.map((item, index) => Table.getRowId(this.props, item, index));
			const notDisabledItemIds = dataIds.filter((_id, index) => !this.isDisabled(data[index], index));
			onSelectionChange(checked ? (0, import_union.default)(selectedIds, notDisabledItemIds) : (0, import_difference.default)(selectedIds, dataIds));
		};
		enhanceColumns = (0, import_memoize.default)((columns) => {
			return [{
				id: selectionColumnId,
				name: this.renderHeadCell,
				template: this.renderBodyCell,
				className: b$22("checkbox_cell"),
				sticky: columns?.[0]?.sticky === "start" ? "start" : void 0
			}, ...columns];
		});
		enhanceOnRowClick = (0, import_memoize.default)((onRowClick) => {
			if (!onRowClick) return onRowClick;
			return (item, index, event) => {
				const checkboxClassName = b$22("selection-checkbox");
				if (event.nativeEvent.target.matches(`.${checkboxClassName}, .${checkboxClassName} *`)) return;
				return onRowClick(item, index, event);
			};
		});
		enhanceGetRowDescriptor = (0, import_memoize.default)((getRowDescriptor) => {
			const currentGetRowDescriptor = (item, index) => {
				const { selectedIds, getRowClassNames } = this.props;
				const descriptor = getRowDescriptor?.(item, index) || {};
				if (descriptor.classNames === void 0) descriptor.classNames = getRowClassNames?.(item, index) || [];
				const id = Table.getRowId(this.props, item, index);
				const selected = selectedIds.includes(id);
				descriptor.classNames.push(b$22("row", { selected }));
				return descriptor;
			};
			return currentGetRowDescriptor;
		});
		isDisabled = (item, index) => {
			const { isRowDisabled, isRowSelectionDisabled, getRowDescriptor } = this.props;
			if (isRowSelectionDisabled && isRowSelectionDisabled(item, index)) return true;
			return getRowDescriptor?.(item, index)?.disabled || isRowDisabled?.(item, index) || false;
		};
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/hoc/withTableActions/withTableActions.js
var actionsColumnId = "_actions";
function enhanceSystemColumn(columns, enhancer) {
	const existedColumn = columns.find(({ id }) => id === actionsColumnId);
	const systemColumn = existedColumn || {
		id: "_actions",
		name: "",
		sticky: "end",
		width: 28,
		placeholder: ""
	};
	enhancer(systemColumn);
	return existedColumn ? columns : [...columns, systemColumn];
}
var isActionGroup = (config) => {
	return Array.isArray(config.items);
};
var b$21 = block("table");
var actionsCn = b$21("actions");
var actionsButtonCn = b$21("actions-button");
var bPopup = block("table-action-popup");
var menuCn = bPopup("menu");
var menuItemCn = bPopup("menu-item");
var DEFAULT_PLACEMENT = ["bottom-end", "top-end"];
var DefaultRowActions = ({ index, item, getRowActions, getRowDescriptor, rowActionsSize, rowActionsIcon, isRowDisabled, tableQa }) => {
	const [isPopupOpen, , closePopup, togglePopup] = useBoolean(false);
	const anchorRef = import_react.useRef(null);
	const rowId = useUniqId();
	const { t } = i18n_default$5.useTranslation();
	if (getRowActions === void 0) return null;
	const renderPopupMenuItem = (action, index) => {
		if (isActionGroup(action)) return (0, import_jsx_runtime.jsx)(Menu$1.Group, {
			label: action.title,
			children: action.items.map(renderPopupMenuItem)
		}, index);
		const { text, icon, handler, href, ...restProps } = action;
		return (0, import_jsx_runtime.jsx)(Menu$1.Item, {
			onClick: (event) => {
				event.stopPropagation();
				handler(item, index, event);
				closePopup();
			},
			href: typeof href === "function" ? href(item, index) : href,
			iconStart: icon,
			contentClassName: menuItemCn,
			...restProps,
			children: text
		}, index);
	};
	const disabled = getRowDescriptor?.(item, index)?.disabled || isRowDisabled?.(item, index);
	const actions = getRowActions(item, index);
	if (actions.length === 0) return null;
	return (0, import_jsx_runtime.jsxs)("div", {
		className: actionsCn,
		children: [(0, import_jsx_runtime.jsx)(Popup, {
			open: isPopupOpen,
			anchorRef,
			placement: DEFAULT_PLACEMENT,
			onOutsideClick: closePopup,
			id: rowId,
			className: bPopup(),
			qa: tableQa && `${tableQa}-actions-popup`,
			children: (0, import_jsx_runtime.jsx)(Menu$1, {
				className: menuCn,
				size: rowActionsSize,
				children: actions.map(renderPopupMenuItem)
			})
		}), (0, import_jsx_runtime.jsx)(Button, {
			view: "flat-secondary",
			className: actionsButtonCn,
			onClick: togglePopup,
			size: rowActionsSize,
			ref: anchorRef,
			disabled,
			qa: tableQa && `${tableQa}-actions-trigger-${index}`,
			"aria-label": t("label-actions"),
			"aria-expanded": isPopupOpen,
			"aria-controls": rowId,
			children: rowActionsIcon ?? (0, import_jsx_runtime.jsx)(Icon, { data: Ellipsis })
		})]
	});
};
function withTableActions(TableComponent) {
	const displayName = `withTableActions(${getComponentName(TableComponent)})`;
	return class extends import_react.Component {
		static displayName = displayName;
		state = {
			popupOpen: false,
			popupData: null
		};
		render() {
			const { renderRowActions, getRowActions, columns, onRowClick, ...restTableProps } = this.props;
			return (0, import_jsx_runtime.jsx)(TableComponent, {
				...restTableProps,
				columns: this.enhanceColumns(columns),
				onRowClick: this.enhanceOnRowClick(onRowClick)
			});
		}
		renderBodyCell = (item, index) => {
			const { getRowActions, rowActionsSize, renderRowActions, rowActionsIcon, isRowDisabled, getRowDescriptor, qa } = this.props;
			if (renderRowActions) return renderRowActions({
				item,
				index
			});
			return (0, import_jsx_runtime.jsx)(DefaultRowActions, {
				index,
				item,
				getRowActions,
				rowActionsSize,
				rowActionsIcon,
				getRowDescriptor,
				isRowDisabled,
				tableQa: qa
			});
		};
		enhanceColumns = (0, import_memoize.default)((columns) => enhanceSystemColumn(columns, (systemColumn) => {
			systemColumn.template = this.renderBodyCell;
		}));
		enhanceOnRowClick = (0, import_memoize.default)((onRowClick) => {
			if (!onRowClick) return onRowClick;
			return (item, index, event) => {
				if (event.nativeEvent.target.closest(`.${menuCn}`)) return;
				if (event.nativeEvent.target.matches(`.${actionsButtonCn}, .${actionsButtonCn} *`)) return;
				return onRowClick(item, index, event);
			};
		});
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/hoc/withTableCopy/withTableCopy.js
var b$20 = block("table");
function withTableCopy(TableComponent) {
	const displayName = `withTableCopy(${getComponentName(TableComponent)})`;
	return class extends import_react.Component {
		static displayName = displayName;
		render() {
			const { columns, onRowClick, ...restTableProps } = this.props;
			return (0, import_jsx_runtime.jsx)(TableComponent, {
				...restTableProps,
				columns: this.enhanceColumns(columns),
				onRowClick: this.enhanceOnRowClick(onRowClick)
			});
		}
		enhanceColumns = (0, import_memoize.default)((columns) => {
			return columns.map((column) => {
				const meta = column.meta;
				if (!meta || !meta.copy) return column;
				return {
					...column,
					template: (item, index) => {
						const originContent = Table.getBodyCellContent({
							...column,
							placeholder: ""
						}, item, index);
						if (!originContent) return originContent;
						let copyText;
						if (typeof meta.copy === "function") copyText = String(meta.copy(item, index));
						else if (typeof originContent === "string" || typeof originContent === "number") copyText = String(originContent);
						if (!copyText) return originContent;
						return (0, import_jsx_runtime.jsxs)("div", {
							className: b$20("copy"),
							children: [(0, import_jsx_runtime.jsx)("div", {
								className: b$20("copy-content"),
								children: originContent
							}), (0, import_jsx_runtime.jsx)("div", {
								className: b$20("copy-button"),
								children: (0, import_jsx_runtime.jsx)(ClipboardButton, {
									text: copyText,
									size: "xs"
								})
							})]
						});
					}
				};
			});
		});
		enhanceOnRowClick = (0, import_memoize.default)((onRowClick) => {
			if (!onRowClick) return onRowClick;
			return (item, index, event) => {
				const buttonClassName = b$20("copy-button");
				if (event.nativeEvent.target.matches(`.${buttonClassName}, .${buttonClassName} *`)) return;
				return onRowClick(item, index, event);
			};
		});
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/hoc/withTableSorting/SortIndicator/SortIndicator.js
function SortIndicator({ order }) {
	let icon;
	switch (order) {
		case "asc":
			icon = ArrowUp;
			break;
		case "desc":
			icon = ArrowDown;
			break;
		default: icon = ArrowUpArrowDown;
	}
	return (0, import_jsx_runtime.jsx)(Icon, {
		data: icon,
		size: 14
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/hoc/withTableSorting/withTableSorting.js
var TableSortIndicator = SortIndicator;
var b$19 = block("table");
function withTableSorting(TableComponent) {
	const displayName = `withTableSorting(${getComponentName(TableComponent)})`;
	function defaultCompareFunction(itemA, itemB, columnId) {
		if ((0, import_get.default)(itemA, columnId) === (0, import_get.default)(itemB, columnId)) return 0;
		else return (0, import_get.default)(itemA, columnId) > (0, import_get.default)(itemB, columnId) ? 1 : -1;
	}
	return class extends import_react.Component {
		static displayName = displayName;
		state = { sort: this.props.defaultSortState ?? [] };
		render() {
			const { columns, ...restTableProps } = this.props;
			return (0, import_jsx_runtime.jsx)(TableComponent, {
				...restTableProps,
				data: this.getSortedData(),
				columns: this.enhanceColumns(columns)
			});
		}
		getSortedData() {
			const { data, columns, disableDataSorting = this.isControlledState() } = this.props;
			const sortState = this.getSortState();
			if (disableDataSorting || sortState.length === 0) return data;
			return data.slice().sort((itemA, itemB) => {
				let i = 0;
				while (i < sortState.length) {
					const state = sortState[i++];
					const compareFunction = columns.find((c) => c.id === state.column)?.meta?.sort;
					if (!compareFunction) continue;
					const compareValue = typeof compareFunction === "function" ? compareFunction(itemA, itemB) : defaultCompareFunction(itemA, itemB, state.column);
					if (compareValue !== 0) return state.order === "asc" ? compareValue : -compareValue;
				}
				return 0;
			});
		}
		enhanceColumns = (0, import_memoize.default)((columns) => {
			return columns.map((column) => {
				const meta = column.meta;
				if (meta && meta.sort) return {
					...column,
					meta: {
						...column.meta,
						_originalName: column.name
					},
					name: () => {
						const sortState = this.getSortState();
						let sortOrder;
						if (sortState.length > 0) {
							const state = sortState.find((s) => s.column === column.id);
							if (state) sortOrder = state.order;
						}
						const originContent = Table.getHeadCellContent(column);
						const content = [(0, import_jsx_runtime.jsx)("div", {
							className: b$19("sort-content"),
							children: originContent
						}, "content"), (0, import_jsx_runtime.jsx)("div", {
							className: b$19("sort-indicator"),
							children: (0, import_jsx_runtime.jsx)(SortIndicator, { order: sortOrder })
						}, "indicator")];
						if (column.align === "right" || column.align === "end") content.reverse();
						const onClick = this.handleColumnSortClick.bind(this, column);
						const onKeyDown = createOnKeyDownHandler(onClick);
						return (0, import_jsx_runtime.jsx)("div", {
							role: "button",
							tabIndex: 0,
							className: b$19("sort", { active: Boolean(sortOrder) }),
							onClick,
							onKeyDown,
							children: content
						});
					}
				};
				else return column;
			});
		});
		handleColumnSortClick = (column, event) => {
			const sortState = this.getSortState();
			const currentStateIndex = sortState.findIndex((state) => state.column === column.id);
			const currentState = sortState[currentStateIndex];
			const nextColumnSort = this.getNextSortForColumn(column, currentState);
			if (!event.shiftKey) {
				this.handleSortStateChange(nextColumnSort);
				return;
			}
			if (currentState) this.handleSortStateChange([
				...sortState.slice(0, currentStateIndex),
				...sortState.slice(currentStateIndex + 1),
				...nextColumnSort
			]);
			else this.handleSortStateChange([...sortState, ...nextColumnSort]);
		};
		getSortState() {
			const { sortState } = this.props;
			const { sort } = this.state;
			return this.isControlledState() ? sortState : sort;
		}
		handleSortStateChange(newSortState) {
			const { onSortStateChange } = this.props;
			if (!this.isControlledState()) this.setState({ sort: newSortState });
			if (onSortStateChange) onSortStateChange(newSortState);
		}
		isControlledState() {
			const { sortState, onSortStateChange } = this.props;
			return Boolean(sortState && onSortStateChange);
		}
		getColumnDefaultSortOrder(column) {
			return column.meta?.defaultSortOrder || "asc";
		}
		getNextSortForColumn(column, currentState) {
			const orderStack = this.getColumnDefaultSortOrder(column) === "desc" ? [
				"desc",
				"asc",
				void 0
			] : [
				"asc",
				"desc",
				void 0
			];
			const nextOrder = orderStack[(orderStack.indexOf(currentState?.order) + 1) % orderStack.length];
			if (!nextOrder) return [];
			return [{
				column: column.id,
				order: nextOrder
			}];
		}
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/utils/isTreeItemGuard.js
var isTreeItemGuard = (item) => {
	return item !== null && typeof item === "object" && "data" in item;
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/utils/defaultFilterItems.js
function defaultFilterItems(items, filterFn) {
	const getChildren = (result, item) => {
		if (isTreeItemGuard(item) && item.children) {
			const children = item.children.reduce(getChildren, []);
			if (children.length) result.push({
				...item,
				data: item.data,
				children
			});
			else if (filterFn(item.data)) result.push({
				...item,
				data: item.data,
				children: []
			});
		} else if (isTreeItemGuard(item) && filterFn(item.data)) {
			const { children: _children, ...newItem } = item;
			result.push(newItem);
		} else if (!isTreeItemGuard(item) && filterFn(item)) result.push(item);
		return result;
	};
	return items.reduce(getChildren, []);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/hooks/useListFilter.js
function defaultFilterFn(value, item) {
	return item && typeof item === "object" && "title" in item && typeof item.title === "string" ? item.title.toLowerCase().includes((value || "").toLowerCase()) : true;
}
/**
* Ready-to-use logic for filtering tree-like data structures
*
* ```tsx
* const {item: filteredItems,...listFiltration} = useListFIlter({items});
* const list = useList({items: filteredItems});
*
* <TextInput {...listFiltration} />
* ```
*/
function useListFilter({ items: externalItems, initialFilterValue = "", filterItem, onFilterChange, filterItems, debounceTimeout = 300 }) {
	const filterRef = import_react.useRef(null);
	const [filter, setFilter] = import_react.useState(initialFilterValue);
	const [prevItems, setPrevItems] = import_react.useState(externalItems);
	const [filteredItems, setFilteredItems] = import_react.useState(externalItems);
	const filterItemsFn = import_react.useCallback((nextFilterValue, items) => {
		if (filterItems) return () => filterItems(nextFilterValue, items);
		if (nextFilterValue) {
			const filterItemFn = filterItem || defaultFilterFn;
			return () => defaultFilterItems(items, (item) => filterItemFn(nextFilterValue, item));
		}
		return () => items;
	}, [filterItem, filterItems]);
	if (externalItems !== prevItems) {
		setFilteredItems(filterItemsFn(filter, externalItems));
		setPrevItems(externalItems);
	}
	const debouncedFn = import_react.useCallback((0, import_debounce.default)((value) => setFilteredItems(filterItemsFn(value, externalItems)), debounceTimeout), [
		setFilteredItems,
		filterItemsFn,
		externalItems,
		debounceTimeout
	]);
	const { onFilterUpdate, reset } = import_react.useMemo(() => {
		return {
			reset: () => {
				setFilter(initialFilterValue);
				onFilterChange?.(initialFilterValue);
				debouncedFn(initialFilterValue);
			},
			onFilterUpdate: (nextFilterValue) => {
				setFilter(nextFilterValue);
				onFilterChange?.(nextFilterValue);
				debouncedFn(nextFilterValue);
			}
		};
	}, [
		debouncedFn,
		initialFilterValue,
		onFilterChange
	]);
	return {
		filterRef,
		filter,
		reset,
		items: filteredItems,
		onFilterUpdate
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/utils/getListItemId.js
var getListItemId = ({ item, groupedId, getItemId }) => {
	let id = groupedId;
	if (typeof getItemId === "function") id = getItemId(isTreeItemGuard(item) ? item.data : item);
	else if (item && typeof item === "object" && "id" in item && item.id) id = item.id;
	return id;
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/constants.js
var LIST_ITEM_DATA_ATR = "data-list-item";
var modToHeight = {
	s: [22, 44],
	m: [26, 44],
	l: [34, 52],
	xl: [44, 62]
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/utils/groupItemId.js
var getGroupItemId = (index, parentId) => parentId ? `${parentId}-${index}` : `${index}`;
var parseGroupItemId = (id) => id.split("-");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/utils/flattenItems.js
function flattenItems({ items, getItemId, expandedById = {} }) {
	const rootIds = [];
	const visibleFlattenIds = [];
	const visit = (item, index, parentId) => {
		const id = getListItemId({
			groupedId: getGroupItemId(index, parentId),
			item,
			getItemId
		});
		if (!parentId) rootIds.push(id);
		visibleFlattenIds.push(id);
		if (isTreeItemGuard(item) && item.children) {
			if (!(id in expandedById && !expandedById[id])) for (let i = 0; i < item.children.length; i++) visit(item.children[i], i, id);
		}
	};
	for (let i = 0; i < items.length; i++) visit(items[i], i);
	const idToFlattenIndex = {};
	for (let i = 0; i < visibleFlattenIds.length; i++) idToFlattenIndex[visibleFlattenIds[i]] = i;
	return {
		rootIds,
		visibleFlattenIds,
		idToFlattenIndex
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/hooks/useFlattenListItems.js
/**
* Pick ids from items and flatten children.
* Returns flatten ids list tree structure representation.
* Not included items if they in `expandedById` map
*/
function useFlattenListItems({ items, expandedById, getItemId }) {
	return import_react.useMemo(() => {
		return flattenItems({
			items,
			expandedById,
			getItemId
		});
	}, [
		items,
		expandedById,
		getItemId
	]);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/utils/getListParsedState.js
function getListParsedState({ items, defaultExpandedState = "expanded", getItemId }) {
	const result = {
		itemsById: {},
		groupsState: {},
		itemsState: {},
		initialState: {
			disabledById: {},
			selectedById: {},
			expandedById: {}
		}
	};
	const traverseItem = ({ item, index }) => {
		const id = getListItemId({
			groupedId: String(index),
			item,
			getItemId
		});
		result.itemsById[id] = item;
		if (!result.itemsState[id]) result.itemsState[id] = { indentation: 0 };
		if (item && typeof item === "object") {
			if ("selected" in item && typeof item.selected === "boolean") result.initialState.selectedById[id] = item.selected;
			if ("disabled" in item && typeof item.disabled === "boolean") result.initialState.disabledById[id] = item.disabled;
		}
	};
	const traverseTreeItem = ({ item, index, parentGroupedId, parentId }) => {
		const groupedId = getGroupItemId(index, parentGroupedId);
		const id = getListItemId({
			groupedId,
			item,
			getItemId
		});
		if (parentId) result.groupsState[parentId].childrenIds.push(id);
		result.itemsById[id] = item.data;
		if (!result.itemsState[id]) result.itemsState[id] = { indentation: 0 };
		if (typeof parentId !== "undefined") result.itemsState[id].parentId = parentId;
		if (typeof item.selected !== "undefined") result.initialState.selectedById[id] = item.selected;
		if (typeof item.disabled !== "undefined") result.initialState.disabledById[id] = item.disabled;
		if (groupedId) result.itemsState[id].indentation = parseGroupItemId(groupedId).length - 1;
		if (item.children) {
			result.groupsState[id] = { childrenIds: [] };
			if (result.initialState.expandedById) if (typeof item.expanded === "undefined") result.initialState.expandedById[id] = defaultExpandedState === "expanded";
			else result.initialState.expandedById[id] = item.expanded;
			item.children.forEach((treeItem, index) => {
				traverseTreeItem({
					item: treeItem,
					index,
					parentGroupedId: groupedId,
					parentId: id
				});
			});
		}
	};
	items.forEach((item, index) => isTreeItemGuard(item) ? traverseTreeItem({
		item,
		index
	}) : traverseItem({
		item,
		index
	}));
	return result;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/hooks/useListParsedState.js
/**
* From the tree structure of list items we get meta information and
* flatten list in right order without taking elements that hidden in expanded groups
*/
function useListParsedState({ items, getItemId: propsGetItemId, defaultExpandedState }) {
	const getItemId = import_react.useRef(propsGetItemId).current;
	return import_react.useMemo(() => {
		return getListParsedState({
			items,
			getItemId,
			defaultExpandedState
		});
	}, [
		getItemId,
		defaultExpandedState,
		items
	]);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/hooks/useListState.js
var useListState = ({ initialState, withExpandedState }) => {
	const initialStateRef = import_react.useRef(initialState);
	const needToUpdateInitValues = initialStateRef.current !== initialState;
	initialStateRef.current = initialState;
	const [disabledById, setDisabled] = import_react.useState(() => initialState?.disabledById ?? {});
	const [selectedById, setSelected] = import_react.useState(() => initialState?.selectedById ?? {});
	const [expandedById, setExpanded] = import_react.useState(() => initialState?.expandedById ?? {});
	const [activeItemId, setActiveItemId] = import_react.useState(() => initialState?.activeItemId);
	if (needToUpdateInitValues) {
		if (initialState?.disabledById) setDisabled((prevValues) => ({
			...initialState.disabledById,
			...prevValues
		}));
		if (initialState?.selectedById) setSelected((prevValues) => ({
			...initialState.selectedById,
			...prevValues
		}));
		if (initialState?.expandedById) setExpanded((prevValues) => ({
			...initialState.expandedById,
			...prevValues
		}));
		setActiveItemId((prevValue) => prevValue ?? initialState?.activeItemId);
	}
	const result = {
		disabledById,
		selectedById,
		activeItemId,
		setDisabled,
		setSelected,
		setActiveItemId
	};
	if (withExpandedState) {
		result.expandedById = expandedById;
		result.setExpanded = setExpanded;
	}
	return result;
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/hooks/useList.js
/**
* Take array of items as a argument with params described what type of list initial data represents.
*/
var useList = ({ items, getItemId, defaultExpandedState = "expanded", withExpandedState = true, initialState: initialValues, controlledState }) => {
	const { itemsById, groupsState, itemsState, initialState } = useListParsedState({
		items,
		getItemId,
		defaultExpandedState
	});
	const innerState = useListState({
		initialState: import_react.useMemo(() => {
			return {
				expandedById: {
					...initialState.expandedById,
					...initialValues?.expandedById
				},
				selectedById: {
					...initialState.selectedById,
					...initialValues?.selectedById
				},
				disabledById: {
					...initialState.disabledById,
					...initialValues?.disabledById
				},
				activeItemId: initialValues?.activeItemId
			};
		}, [
			initialState.disabledById,
			initialState.expandedById,
			initialState.selectedById,
			initialValues?.activeItemId,
			initialValues?.disabledById,
			initialValues?.expandedById,
			initialValues?.selectedById
		]),
		withExpandedState
	});
	const realState = import_react.useMemo(() => {
		if (controlledState) return {
			...innerState,
			...controlledState
		};
		return innerState;
	}, [controlledState, innerState]);
	const { visibleFlattenIds, idToFlattenIndex, rootIds } = useFlattenListItems({
		items,
		/**
		* By default controlled from list items declaration state
		*/
		expandedById: realState.expandedById,
		getItemId
	});
	return {
		state: realState,
		structure: {
			rootIds,
			items,
			visibleFlattenIds,
			idToFlattenIndex,
			itemsById,
			groupsState,
			itemsState
		}
	};
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/utils/findNextIndex.js
var findNextIndex = ({ list, index, step, disabledItemsById = {} }) => {
	const dataLength = list.length;
	let currentIndex = (index + dataLength) % dataLength;
	for (let i = 0; i < dataLength; i += 1) {
		const id = list[currentIndex];
		if (id && !disabledItemsById[id]) return currentIndex;
		currentIndex = (currentIndex + dataLength + step) % dataLength;
	}
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/utils/scrollToListItem.js
var scrollToListItem = (itemId, containerElement) => {
	if (document) {
		const element = (containerElement || document).querySelector(`[${LIST_ITEM_DATA_ATR}="${itemId}"]`);
		if (element) element.scrollIntoView?.({ block: "nearest" });
	}
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/hooks/useListKeydown.js
var useListKeydown = ({ containerRef, onItemClick, enabled, list }) => {
	const activateItem = import_react.useCallback((index, scrollTo = true) => {
		if (typeof index === "number" && list.structure.visibleFlattenIds[index]) {
			if (scrollTo) scrollToListItem(list.structure.visibleFlattenIds[index], containerRef?.current);
			list.state.setActiveItemId?.(list.structure.visibleFlattenIds[index]);
		}
	}, [
		list.structure.visibleFlattenIds,
		list.state,
		containerRef
	]);
	const handleKeyMove = import_react.useCallback((event, step, defaultItemIndex = 0) => {
		event.preventDefault();
		const maybeIndex = typeof list.state.activeItemId === "string" ? list.structure.visibleFlattenIds.findIndex((i) => i === list.state.activeItemId) : -1;
		const nextIndex = findNextIndex({
			list: list.structure.visibleFlattenIds,
			index: (maybeIndex > -1 ? maybeIndex : defaultItemIndex) + step,
			step: Math.sign(step),
			disabledItemsById: list.state.disabledById
		});
		activateItem(nextIndex);
	}, [
		activateItem,
		list.state.activeItemId,
		list.state.disabledById,
		list.structure.visibleFlattenIds
	]);
	useLayoutEffect$4(() => {
		const anchor = containerRef?.current;
		if (enabled || !anchor) return;
		const handleKeyDown = (event) => {
			switch (event.key) {
				case KeyCode$1.ARROW_DOWN:
					handleKeyMove(event, 1, -1);
					break;
				case KeyCode$1.ARROW_UP:
					handleKeyMove(event, -1);
					break;
				case KeyCode$1.SPACEBAR:
				case KeyCode$1.ENTER:
					if (list.state.activeItemId && !list.state.disabledById[list.state.activeItemId]) {
						event.preventDefault();
						onItemClick?.({ id: list.state.activeItemId });
					}
					break;
				default:
			}
		};
		anchor.addEventListener("keydown", handleKeyDown);
		return () => {
			anchor.removeEventListener("keydown", handleKeyDown);
		};
	}, [
		containerRef,
		enabled,
		handleKeyMove,
		list.state.activeItemId,
		list.state.disabledById,
		onItemClick
	]);
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/components/ListItemExpandIcon/ListItemExpandIcon.js
var b$18 = block("list-item-expand-icon");
var ListItemExpandIcon = ({ expanded, behavior = "action", disabled }) => {
	return (0, import_jsx_runtime.jsx)(ArrowToggle, {
		direction: getIconDirection({
			behavior,
			expanded
		}),
		className: b$18(null, colorText({ color: disabled ? "hint" : void 0 })),
		size: 16
	});
};
function getIconDirection({ behavior, expanded }) {
	if (expanded && behavior === "action") return "top";
	else if (expanded && behavior === "state") return "bottom";
	else if (expanded && behavior === "state-inverse") return "bottom";
	else if (behavior === "action") return "bottom";
	else if (behavior === "state") return "right";
	else if (behavior === "state-inverse") return "left";
	return "bottom";
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/components/ListItemView/styles.js
var b$17 = block("list-item-view");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/components/ListItemView/ListItemViewContent.js
var isListItemContentPropsGuard = (props) => {
	return typeof props === "object" && props !== null && "title" in props;
};
var ListItemViewSlot = ({ children, indentation = 1, className, ...props }) => {
	return (0, import_jsx_runtime.jsx)(Flex, {
		width: indentation * 16,
		className: b$17("slot", className),
		...props,
		children
	});
};
var renderSafeIndentation = (indentation) => {
	if (indentation && indentation >= 1) return (0, import_jsx_runtime.jsx)(ListItemViewSlot, { indentation: Math.floor(indentation) });
	return null;
};
var ListItemViewContent = ({ startSlot, subtitle, endSlot, disabled, hasSelectionIcon, isGroup, indentation, expanded, selected, title, expandIconPlacement = "start", renderExpandIcon: RenderExpandIcon = ListItemExpandIcon }) => {
	const expandIconNode = isGroup ? (0, import_jsx_runtime.jsx)(RenderExpandIcon, {
		behavior: expandIconPlacement === "start" ? "state" : "action",
		expanded,
		disabled
	}) : null;
	return (0, import_jsx_runtime.jsxs)(Flex, {
		alignItems: "center",
		justifyContent: "space-between",
		gap: "4",
		className: b$17("content"),
		children: [(0, import_jsx_runtime.jsxs)(Flex, {
			gap: "2",
			alignItems: "center",
			grow: true,
			children: [
				hasSelectionIcon && (0, import_jsx_runtime.jsx)(ListItemViewSlot, { children: selected ? (0, import_jsx_runtime.jsx)(Icon, {
					data: Check,
					size: 16,
					className: colorText({ color: "info" })
				}) : null }),
				renderSafeIndentation(indentation),
				expandIconPlacement === "start" && expandIconNode,
				startSlot,
				(0, import_jsx_runtime.jsxs)("div", {
					className: b$17("main-content"),
					children: [typeof title === "string" ? (0, import_jsx_runtime.jsx)(Text, {
						ellipsis: true,
						color: disabled ? "hint" : void 0,
						variant: isGroup ? "subheader-1" : void 0,
						children: title
					}) : title, typeof subtitle === "string" ? (0, import_jsx_runtime.jsx)(Text, {
						ellipsis: true,
						color: disabled ? "hint" : "secondary",
						children: subtitle
					}) : subtitle]
				})
			]
		}), (0, import_jsx_runtime.jsxs)(Flex, {
			gap: "2",
			children: [expandIconPlacement === "end" && expandIconNode, endSlot]
		})]
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/components/ListItemView/ListItemView.js
var ListItemView = import_react.forwardRef(function ListItemView({ id, as: asProps, size = "m", active, selected, disabled, selectionViewType = "multiple", activeOnHover: propsActiveOnHover, className, height, dragging, style: propsStyle, content, role = "option", onClick: _onClick, ...rest }, ref) {
	const Tag = asProps || "li";
	const onClick = disabled ? void 0 : _onClick;
	const activeOnHover = typeof propsActiveOnHover === "boolean" ? propsActiveOnHover : Boolean(onClick);
	const style = {
		minHeight: `var(--g-list-item-height, ${height ?? modToHeight[size][Number(Boolean(isListItemContentPropsGuard(content) ? content?.subtitle : false))]}px)`,
		...propsStyle
	};
	return (0, import_jsx_runtime.jsx)(Tag, {
		[LIST_ITEM_DATA_ATR]: id,
		role,
		"aria-selected": selected,
		onClick,
		className: b$17({
			active: dragging || active,
			selected: selected && selectionViewType === "single",
			activeOnHover,
			radius: size,
			size,
			dragging,
			clickable: Boolean(onClick)
		}, spacing({ px: 2 }, className)),
		style,
		ref,
		...rest,
		children: isListItemContentPropsGuard(content) ? (0, import_jsx_runtime.jsx)(ListItemViewContent, {
			...content,
			hasSelectionIcon: selectionViewType === "multiple",
			selected,
			disabled
		}) : content
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/components/ListRecursiveRenderer/ListRecursiveRenderer.js
var b$16 = block("list-recursive-renderer");
function ListItemRecursiveRenderer({ id, itemSchema, list, ...props }) {
	const node = props.children(id, list.structure.idToFlattenIndex[id]);
	if (isTreeItemGuard(itemSchema) && itemSchema.children) {
		const isExpanded = list.state.expandedById && id in list.state.expandedById ? list.state.expandedById[id] : true;
		return (0, import_jsx_runtime.jsxs)("ul", {
			style: props.style,
			className: b$16(null, props.className),
			role: "group",
			children: [node, isExpanded && Boolean(list.structure.groupsState[id]?.childrenIds) && itemSchema.children.map((item, index) => (0, import_jsx_runtime.jsx)(ListItemRecursiveRenderer, {
				list,
				id: list.structure.groupsState[id].childrenIds[index],
				itemSchema: item,
				...props
			}, index))]
		});
	}
	return node;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/components/ListContainerView/ListContainerView.js
var b$15 = block("list-container-view");
var ListContainerView = import_react.forwardRef(function ListContainerView({ as = "div", role = "listbox", children, id, className, fixedHeight, extraProps, qa, style }, ref) {
	return (0, import_jsx_runtime.jsx)(Flex, {
		qa,
		as,
		direction: "column",
		ref,
		grow: true,
		tabIndex: -1,
		id,
		role,
		style,
		className: b$15({ "fixed-height": fixedHeight }, className),
		...extraProps,
		children
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/components/ListContainer/ListContainer.js
function ListContainer({ containerRef, renderItem, list, ...props }) {
	return (0, import_jsx_runtime.jsx)(ListContainerView, {
		ref: containerRef,
		...props,
		children: list.structure.items.map((item, index) => (0, import_jsx_runtime.jsx)(ListItemRecursiveRenderer, {
			itemSchema: item,
			id: list.structure.rootIds[index],
			list,
			children: renderItem
		}, index))
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/utils/getListItemClickHandler.js
var getListItemClickHandler = ({ list, multiple }) => {
	const onItemClick = ({ id }) => {
		if (list.state.disabledById[id]) return;
		list.state.setActiveItemId(id);
		if (list.state.expandedById && id in list.state.expandedById && list.state.setExpanded) list.state.setExpanded((prevState) => ({
			...prevState,
			[id]: !prevState[id]
		}));
		else list.state.setSelected((prevState) => ({
			...multiple ? prevState : {},
			[id]: multiple ? !prevState[id] : true
		}));
	};
	return onItemClick;
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/utils/getListItemQa.js
var getListItemQa = (qa, id) => `${qa}-${id}`;
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/useList/utils/getItemRenderState.js
/**
* Map list state and parsed list state to item render props
*/
var getItemRenderState = ({ qa, list, onItemClick, mapItemDataToContentProps, size = "m", multiple = false, id }) => {
	const context = {
		...list.structure.itemsState[id],
		...list.structure.groupsState[id],
		isLastItem: id === list.structure.visibleFlattenIds[list.structure.visibleFlattenIds.length - 1]
	};
	const props = {
		id,
		size,
		selected: Boolean(list.state.selectedById[id]),
		disabled: Boolean(list.state.disabledById?.[id]),
		active: id === list.state.activeItemId,
		onClick: onItemClick ? (e) => onItemClick({ id }, e) : void 0,
		selectionViewType: Boolean(multiple) && !context.childrenIds ? "multiple" : "single",
		content: {
			expanded: list.state.expandedById?.[id],
			indentation: context.indentation,
			isGroup: list.state.expandedById && id in list.state.expandedById,
			...mapItemDataToContentProps(list.structure.itemsById[id])
		}
	};
	if (qa) props.qa = getListItemQa(qa, id);
	return {
		data: list.structure.itemsById[id],
		props,
		context
	};
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/TreeList/TreeList.js
var b$14 = block("tree-list");
var TreeList = ({ qa, id, size = "m", className, list, multiple, containerRef: propsContainerRef, renderItem: propsRenderItem, renderContainer = ListContainer, onItemClick: propsOnItemClick, mapItemDataToContentProps }) => {
	const uniqId = useUniqId();
	const treeListId = id ?? uniqId;
	const containerRefLocal = import_react.useRef(null);
	const containerRef = propsContainerRef ?? containerRefLocal;
	const onItemClick = import_react.useMemo(() => {
		if (propsOnItemClick === null) return;
		const handler = (arg, e) => {
			const payload = {
				id: arg.id,
				list
			};
			if (propsOnItemClick) propsOnItemClick?.(payload, e);
			else getListItemClickHandler({
				list,
				multiple
			})(payload, e);
		};
		return handler;
	}, [
		propsOnItemClick,
		list,
		multiple
	]);
	useListKeydown({
		containerRef,
		onItemClick,
		list
	});
	const renderItem = (itemId, index, renderContainerProps) => {
		const renderState = getItemRenderState({
			qa,
			id: itemId,
			size,
			multiple,
			mapItemDataToContentProps,
			onItemClick,
			list
		});
		if (propsRenderItem) return propsRenderItem({
			id: itemId,
			data: renderState.data,
			props: renderState.props,
			context: renderState.context,
			index,
			renderContainerProps,
			list
		});
		return (0, import_jsx_runtime.jsx)(ListItemView, {
			...renderState.props,
			...renderContainerProps
		});
	};
	return renderContainer({
		qa,
		id: `list-${treeListId}`,
		size,
		containerRef,
		className: b$14(null, className),
		list,
		renderItem
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/TreeSelect/hooks/useControlledValue.js
var prepareParams = (selectedById) => Object.entries(selectedById).reduce((acc, [id, value]) => {
	if (value) acc.push(id);
	return acc;
}, []);
var useControlledValue = ({ defaultValue = [], value: valueProps, onUpdate }) => {
	const [innerValue, setInnerValue] = import_react.useState(defaultValue);
	const value = valueProps ?? innerValue;
	const uncontrolled = !valueProps;
	return import_react.useMemo(() => {
		const selectedById = value.reduce((acc, val) => {
			acc[val] = true;
			return acc;
		}, {});
		const setSelected = (payload) => {
			const preparedValue = prepareParams(typeof payload === "function" ? payload(selectedById) : payload);
			if (uncontrolled) setInnerValue(preparedValue);
			else onUpdate?.(preparedValue);
		};
		return {
			value,
			selectedById,
			setSelected,
			/**
			* Available only if `uncontrolled` component valiant
			*/
			setInnerValue: uncontrolled ? setInnerValue : void 0
		};
	}, [
		onUpdate,
		uncontrolled,
		value
	]);
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/TreeSelect/TreeSelect.js
var b$13 = block("tree-select");
var defaultItemRenderer = (renderState) => {
	return (0, import_jsx_runtime.jsx)(ListItemView, {
		...renderState.props,
		...renderState.renderContainerProps
	});
};
var TreeSelect = import_react.forwardRef(function TreeSelect({ id, qa, title, placement, slotBeforeListBody, slotAfterListBody, size = "m", defaultOpen, width, containerRef: propsContainerRef, className, containerClassName, popupClassName, open: propsOpen, multiple, popupWidth, popupDisablePortal, items, value: propsValue, defaultValue, placeholder, disabled = false, withExpandedState = true, defaultExpandedState = "expanded", hasClear, errorMessage: propsErrorMessage, errorPlacement: propsErrorPlacement, validationState: propsValidationState, onClose, onOpenChange, onUpdate, renderControl, renderItem = defaultItemRenderer, renderContainer, mapItemDataToContentProps, onFocus, onBlur, getItemId, onItemClick }, ref) {
	const mobile = useMobile();
	const uniqId = useUniqId();
	const treeSelectId = id ?? uniqId;
	const popupId = `tree-select-popup-${treeSelectId}`;
	const controlWrapRef = import_react.useRef(null);
	const controlRef = import_react.useRef(null);
	const containerRefLocal = import_react.useRef(null);
	const containerRef = propsContainerRef ?? containerRefLocal;
	const { errorMessage, errorPlacement, validationState } = errorPropsMapper({
		errorMessage: propsErrorMessage,
		errorPlacement: propsErrorPlacement || "outside",
		validationState: propsValidationState
	});
	const errorMessageId = useUniqId();
	const isErrorStateVisible = validationState === "invalid";
	const isErrorMsgVisible = isErrorStateVisible && Boolean(errorMessage) && errorPlacement === "outside";
	const isErrorIconVisible = isErrorStateVisible && Boolean(errorMessage) && errorPlacement === "inside";
	const handleControlRef = useForkRef(ref, controlRef);
	const { toggleOpen, open } = useOpenState({
		defaultOpen,
		onClose,
		onOpenChange,
		open: propsOpen
	});
	const { value, selectedById, setSelected } = useControlledValue({
		value: propsValue,
		defaultValue,
		onUpdate
	});
	const list = useList({
		controlledState: {
			selectedById,
			setSelected
		},
		items,
		getItemId,
		defaultExpandedState,
		withExpandedState
	});
	const handleItemClick = import_react.useMemo(() => {
		if (onItemClick === null) return;
		const handler = (arg, e) => {
			const payload = {
				id: arg.id,
				list
			};
			if (onItemClick) onItemClick?.(payload, e);
			else {
				getListItemClickHandler({
					list,
					multiple
				})(payload, e);
				const isGroup = list.state.expandedById && arg.id in list.state.expandedById;
				if (!multiple && !isGroup) toggleOpen(false);
			}
		};
		return handler;
	}, [
		onItemClick,
		list,
		multiple,
		toggleOpen
	]);
	useLayoutEffect$4(() => {
		if (open) containerRef.current?.focus({ preventScroll: true });
		return () => list.state.setActiveItemId(void 0);
	}, [open]);
	const handleClose = import_react.useCallback(() => toggleOpen(false), [toggleOpen]);
	const { focusWithinProps } = useFocusWithin({
		onFocusWithin: onFocus,
		onBlurWithin: import_react.useCallback((e) => {
			onBlur?.(e);
			handleClose();
		}, [handleClose, onBlur])
	});
	const controlProps = {
		list,
		open,
		placeholder,
		toggleOpen,
		clearValue: () => list.state.setSelected({}),
		ref: handleControlRef,
		size,
		value,
		disabled,
		id: treeSelectId,
		activeItemId: list.state.activeItemId,
		title,
		errorMessage: isErrorIconVisible ? errorMessage : void 0,
		errorPlacement,
		validationState,
		hasClear,
		isErrorVisible: isErrorStateVisible
	};
	const togglerNode = renderControl ? renderControl(controlProps) : (0, import_jsx_runtime.jsx)(SelectControl, {
		...controlProps,
		selectedOptionsContent: import_react.Children.toArray(value.map((itemId) => itemId in list.structure.itemsById ? mapItemDataToContentProps(list.structure.itemsById[itemId]).title : "")).join(", "),
		view: "normal",
		pin: "round-round",
		popupId,
		selectId: treeSelectId
	});
	const mods = { ...width === "max" && { width } };
	const inlineStyles = {};
	if (typeof width === "number") inlineStyles.width = width;
	return (0, import_jsx_runtime.jsxs)("div", {
		ref: controlWrapRef,
		...focusWithinProps,
		className: b$13(mods, className),
		style: inlineStyles,
		children: [
			togglerNode,
			(0, import_jsx_runtime.jsxs)(SelectPopup, {
				ref: controlWrapRef,
				className: b$13("popup", { size }, popupClassName),
				controlRef,
				width: popupWidth,
				placement,
				open,
				handleClose,
				disablePortal: popupDisablePortal,
				mobile,
				id: popupId,
				children: [
					slotBeforeListBody,
					(0, import_jsx_runtime.jsx)(TreeList, {
						list,
						size,
						className: b$13("list", containerClassName),
						qa,
						multiple,
						id: `list-${treeSelectId}`,
						containerRef,
						onItemClick: handleItemClick,
						renderContainer,
						mapItemDataToContentProps,
						renderItem: renderItem ?? defaultItemRenderer
					}),
					slotAfterListBody
				]
			}),
			(0, import_jsx_runtime.jsx)(OuterAdditionalContent, {
				errorMessage: isErrorMsgVisible ? errorMessage : null,
				errorMessageId
			})
		]
	});
});
var i18n_default$4 = addComponentKeysets({
	en: {
		button_apply: "Apply",
		button_reset: "Reset",
		button_switcher: "Columns"
	},
	ru: {
		button_apply: "Применить",
		button_reset: "Сбросить",
		button_switcher: "Колонки"
	}
}, "TableColumnSetupInner");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/hoc/withTableSettings/TableColumnSetup/TableColumnSetup.js
var b$12 = block("inner-table-column-setup");
var controlsCn = b$12("controls");
var filterInputCn = b$12("filter-input");
var emptyPlaceholderCn = b$12("empty-placeholder");
var reorderArray = (list, startIndex, endIndex) => {
	const result = [...list];
	const [removed] = result.splice(startIndex, 1);
	result.splice(endIndex, 0, removed);
	return result;
};
var prepareStickyState = (itemsById, visibleFlattenIds) => {
	let lastStickyStartIdx = 0;
	for (; lastStickyStartIdx !== visibleFlattenIds.length; lastStickyStartIdx++) {
		const item = itemsById[visibleFlattenIds[lastStickyStartIdx]];
		if (item?.sticky !== "left" && item?.sticky !== "start") break;
	}
	let firstStickyEndIdx = visibleFlattenIds.length;
	for (; firstStickyEndIdx !== 0; firstStickyEndIdx--) {
		const item = itemsById[visibleFlattenIds[firstStickyEndIdx - 1]];
		if (item?.sticky !== "right" && item?.sticky !== "end") break;
	}
	return {
		stickyStartItemIdList: visibleFlattenIds.slice(0, lastStickyStartIdx),
		sortableItemIdList: visibleFlattenIds.slice(lastStickyStartIdx, firstStickyEndIdx),
		stickyEndItemIdList: visibleFlattenIds.slice(firstStickyEndIdx)
	};
};
var prepareValue = (tableColumnItems) => {
	const selectedIds = [];
	tableColumnItems.forEach(({ id, isSelected }) => {
		if (isSelected) selectedIds.push(id);
	});
	return selectedIds;
};
var RENDER_DRAG_DISABLED_CONTAINER_PROPS = { isDragDisabled: true };
var useDndRenderContainer = ({ onDragEnd, renderControls }) => {
	const uniqId = useUniqId();
	const dndRenderContainer = ({ renderItem, list, containerRef, id, className }) => {
		const renderDndActiveItem = (provided, snapshot, rubric) => {
			const renderContainerProps = {
				provided,
				snapshot
			};
			return renderItem(list.structure.visibleFlattenIds[rubric.source.index], rubric.source.index, renderContainerProps);
		};
		const { stickyStartItemIdList, sortableItemIdList, stickyEndItemIdList } = prepareStickyState(list.structure.itemsById, list.structure.visibleFlattenIds);
		const stickyStartItemList = stickyStartItemIdList.map((visibleFlattenId, idx) => {
			return renderItem(visibleFlattenId, idx, RENDER_DRAG_DISABLED_CONTAINER_PROPS);
		});
		const sortableItemList = sortableItemIdList.map((visibleFlattenId, idx) => {
			return renderItem(visibleFlattenId, idx + stickyStartItemIdList.length);
		});
		const stickyEndItemList = stickyEndItemIdList.map((visibleFlattenId, idx) => {
			return renderItem(visibleFlattenId, stickyStartItemList.length + sortableItemList.length + idx, RENDER_DRAG_DISABLED_CONTAINER_PROPS);
		});
		return (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [(0, import_jsx_runtime.jsxs)(ListContainerView, {
			ref: containerRef,
			id,
			className,
			children: [
				stickyStartItemList,
				(0, import_jsx_runtime.jsx)(DragDropContext, {
					onDragEnd,
					children: (0, import_jsx_runtime.jsx)(ConnectedDroppable, {
						droppableId: uniqId,
						renderClone: renderDndActiveItem,
						children: (droppableProvided) => {
							return (0, import_jsx_runtime.jsxs)("div", {
								...droppableProvided.droppableProps,
								ref: droppableProvided.innerRef,
								children: [sortableItemList, droppableProvided.placeholder]
							});
						}
					})
				}),
				stickyEndItemList
			]
		}), (0, import_jsx_runtime.jsx)("div", {
			className: controlsCn,
			children: renderControls()
		})] });
	};
	return dndRenderContainer;
};
var useDndRenderItem = (sortable) => {
	const renderDndItem = ({ data: item, props, index, renderContainerProps }) => {
		const isDragDisabled = sortable === false || renderContainerProps?.isDragDisabled === true;
		const endSlot = isDragDisabled ? void 0 : (0, import_jsx_runtime.jsx)(Icon, {
			data: Grip,
			size: 16
		});
		const startSlot = item.isRequired ? (0, import_jsx_runtime.jsx)(Icon, { data: Lock }) : void 0;
		const selected = item.isRequired ? false : props.selected;
		const commonProps = {
			...props,
			selected,
			selectionViewType: item.isRequired ? "single" : "multiple",
			content: {
				...props.content,
				startSlot,
				endSlot
			}
		};
		if (isDragDisabled) return (0, import_react.createElement)(ListItemView, {
			...commonProps,
			key: commonProps.id
		});
		const renderItem = (provided, snapshot) => (0, import_jsx_runtime.jsx)(ListItemView, {
			...commonProps,
			...provided.draggableProps,
			...provided.dragHandleProps,
			ref: provided.innerRef,
			dragging: snapshot.isDragging
		});
		if (renderContainerProps?.provided && renderContainerProps.snapshot) return renderItem(renderContainerProps.provided, renderContainerProps.snapshot);
		return (0, import_jsx_runtime.jsx)(PublicDraggable, {
			draggableId: props.id,
			index,
			isDragDisabled,
			children: renderItem
		}, `item-key-${props.id}`);
	};
	return renderDndItem;
};
var mapItemDataToContentProps = (item) => {
	return { title: item.title };
};
var defaultFilterSettingsFn = (value, item) => {
	return typeof item.title === "string" ? item.title.toLowerCase().includes(value.trim().toLowerCase()) : true;
};
var useEmptyRenderContainer = (placeholder) => {
	const emptyRenderContainer = () => (0, import_jsx_runtime.jsx)(Text, {
		className: emptyPlaceholderCn,
		children: placeholder
	});
	return emptyRenderContainer;
};
var TableColumnSetup$1 = (props) => {
	const { renderSwitcher, popupWidth, popupPlacement, items: propsItems, onUpdate: propsOnUpdate, sortable, renderControls, className, defaultItems = propsItems, showResetButton: propsShowResetButton, filterable, filterPlaceholder, filterEmptyMessage, filterSettings = defaultFilterSettingsFn, hideApplyButton } = props;
	const [open, setOpen] = import_react.useState(false);
	const [sortingEnabled, setSortingEnabled] = import_react.useState(sortable);
	const [prevSortingEnabled, setPrevSortingEnabled] = import_react.useState(sortable);
	if (sortable !== prevSortingEnabled) {
		setPrevSortingEnabled(sortable);
		setSortingEnabled(sortable);
	}
	const [items, setItems] = useControlledState(hideApplyButton ? propsItems : void 0, propsItems, hideApplyButton ? propsOnUpdate : void 0);
	const [prevPropsItems, setPrevPropsItems] = import_react.useState(propsItems);
	if (propsItems !== prevPropsItems) {
		setPrevPropsItems(propsItems);
		if (!hideApplyButton) setItems(propsItems);
	}
	const { t } = i18n_default$4.useTranslation();
	const filterState = useListFilter({
		items,
		filterItem: filterSettings,
		debounceTimeout: 0
	});
	const onApply = () => {
		const newSettings = items.map(({ id, isSelected }) => ({
			id,
			isSelected
		}));
		propsOnUpdate(newSettings);
		onOpenChange(false);
	};
	const DefaultApplyButton = () => (0, import_jsx_runtime.jsx)(Button, {
		view: "action",
		width: "max",
		onClick: onApply,
		children: t("button_apply")
	});
	const onDragEnd = ({ destination, source }) => {
		if (destination?.index !== void 0 && destination?.index !== source.index) {
			const reorderedItems = reorderArray(items, source.index, destination.index);
			setItems(reorderedItems);
		}
	};
	const showResetButton = typeof propsShowResetButton === "function" ? propsShowResetButton(items) : propsShowResetButton;
	const dndRenderContainer = useDndRenderContainer({
		onDragEnd,
		renderControls: () => renderControls ? renderControls({
			DefaultApplyButton,
			onApply
		}) : (0, import_jsx_runtime.jsxs)(Flex, {
			gapRow: 1,
			direction: "column",
			className: controlsCn,
			children: [showResetButton && (0, import_jsx_runtime.jsx)(Button, {
				onClick: () => {
					if (hideApplyButton) propsOnUpdate(defaultItems);
					setItems(defaultItems);
				},
				width: "max",
				children: t("button_reset")
			}), !hideApplyButton && (0, import_jsx_runtime.jsx)(DefaultApplyButton, {})]
		})
	});
	const dndRenderItem = useDndRenderItem(sortingEnabled);
	const renderControl = ({ toggleOpen }) => {
		const onKeyDown = createOnKeyDownHandler(toggleOpen);
		return renderSwitcher?.({
			onClick: toggleOpen,
			onKeyDown
		}) || (0, import_jsx_runtime.jsxs)(Button, {
			onClick: toggleOpen,
			onKeyDown,
			children: [(0, import_jsx_runtime.jsx)(Icon, { data: Gear }), t("button_switcher")]
		});
	};
	const onOpenChange = (open) => {
		setOpen(open);
		if (open === false) {
			setItems(propsItems);
			setSortingEnabled(sortable);
			filterState.reset();
		}
	};
	const onUpdate = (selectedItemsIds) => {
		const newItems = items.map((item) => ({
			...item,
			isSelected: item.isRequired || selectedItemsIds.includes(item.id)
		}));
		setItems(newItems);
	};
	const value = import_react.useMemo(() => prepareValue(items), [items]);
	const emptyRenderContainer = useEmptyRenderContainer(filterEmptyMessage);
	const onFilterValueUpdate = (value) => {
		filterState.onFilterUpdate(value);
		setSortingEnabled(!value.length);
	};
	const slotBeforeListBody = filterable ? (0, import_jsx_runtime.jsx)(TextInput, {
		size: "m",
		view: "clear",
		placeholder: filterPlaceholder,
		value: filterState.filter,
		className: filterInputCn,
		onUpdate: onFilterValueUpdate,
		hasClear: true
	}) : null;
	const renderContainer = filterState.filter && !filterState.items.length ? emptyRenderContainer : dndRenderContainer;
	return (0, import_jsx_runtime.jsx)(TreeSelect, {
		className: b$12(null, className),
		mapItemDataToContentProps,
		multiple: true,
		size: "l",
		open,
		value,
		items: filterState.filter ? filterState.items : items,
		onUpdate,
		popupWidth,
		onOpenChange,
		placement: popupPlacement,
		slotBeforeListBody,
		renderContainer,
		renderControl,
		renderItem: dndRenderItem
	});
};
var i18n_default$3 = addComponentKeysets({
	en: { label_settings: "Table settings" },
	ru: { label_settings: "Настройки таблицы" }
}, "withTableSettings");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Table/hoc/withTableSettings/withTableSettings.js
function filterColumns(columns, settings) {
	const filteredColumns = settings.map(({ id, isSelected }) => ({
		isSelected,
		columnSettings: columns.find((column) => id === column.id)
	})).filter(({ isSelected, columnSettings }) => isSelected && columnSettings).map(({ columnSettings }) => columnSettings);
	if (columns[0] && columns[0].id === "_selection") filteredColumns.unshift(columns[0]);
	const lastColumn = columns.at(-1);
	if (lastColumn && lastColumn.id === "_actions") filteredColumns.push(lastColumn);
	return filteredColumns;
}
function getColumnStringTitle(column) {
	const displayName = column.meta?.displayName;
	if (typeof displayName === "string") return displayName;
	if (typeof column.name === "string") return column.name;
	const originalName = column.meta?._originalName;
	if (typeof originalName === "string") return originalName;
	return column.id;
}
var getTableColumnSetupItem = (id, isSelected, column) => {
	const isProtected = Boolean(column?.meta?.selectedAlways);
	return {
		id,
		isSelected: isProtected ? true : isSelected,
		isRequired: isProtected,
		title: column ? getColumnStringTitle(column) : id,
		sticky: column?.sticky
	};
};
function getActualItems(columns, settings) {
	const sortableItems = [];
	settings.forEach(({ id, isSelected }) => {
		const column = columns.find((column) => id === column.id);
		if (column) sortableItems.push(getTableColumnSetupItem(id, isSelected, column));
	});
	columns.forEach((column) => {
		if (column.id !== "_actions" && column.id !== "_selection" && settings.every((setting) => setting.id !== column.id)) {
			const isSelected = column.meta?.selectedByDefault !== false;
			sortableItems.push(getTableColumnSetupItem(column.id, isSelected, column));
		}
	});
	return sortableItems;
}
var b$11 = block("table");
var POPUP_PLACEMENT = [
	"bottom-end",
	"bottom",
	"top-end",
	"top"
];
function withTableSettings(ComponentOrOptions) {
	function tableWithSettingsFactory(TableComponent, { width, sortable, filterable } = {}) {
		const componentName = getComponentName(TableComponent);
		function TableWithSettings({ updateSettings, settings, columns, settingsPopupWidth, renderControls, defaultSettings, showResetButton, settingsFilterPlaceholder, settingsFilterEmptyMessage, filterSettings, ...restTableProps }) {
			const defaultActualItems = import_react.useMemo(() => {
				if (!defaultSettings) return;
				return getActualItems(columns, defaultSettings);
			}, [columns, defaultSettings]);
			const { t } = i18n_default$3.useTranslation();
			const enhancedColumns = import_react.useMemo(() => {
				const actualItems = getActualItems(columns, settings || []);
				return enhanceSystemColumn(filterColumns(columns, actualItems), (systemColumn) => {
					systemColumn.name = () => (0, import_jsx_runtime.jsx)("div", {
						className: b$11("settings"),
						children: (0, import_jsx_runtime.jsx)(TableColumnSetup$1, {
							popupWidth: settingsPopupWidth || width,
							popupPlacement: POPUP_PLACEMENT,
							sortable,
							filterable,
							filterPlaceholder: settingsFilterPlaceholder,
							filterEmptyMessage: settingsFilterEmptyMessage,
							filterSettings,
							onUpdate: updateSettings,
							items: actualItems,
							renderSwitcher: ({ onClick }) => (0, import_jsx_runtime.jsx)(Button, {
								view: "flat",
								className: b$11("settings-button"),
								"aria-label": t("label_settings"),
								onClick,
								children: (0, import_jsx_runtime.jsx)(Icon, { data: Gear })
							}),
							renderControls,
							defaultItems: defaultActualItems,
							showResetButton
						})
					});
				});
			}, [
				columns,
				settings,
				settingsPopupWidth,
				settingsFilterPlaceholder,
				settingsFilterEmptyMessage,
				filterSettings,
				updateSettings,
				renderControls,
				defaultActualItems,
				showResetButton,
				t
			]);
			return (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: (0, import_jsx_runtime.jsx)(TableComponent, {
				...restTableProps,
				columns: enhancedColumns
			}) });
		}
		TableWithSettings.displayName = `withTableSettings(${componentName})`;
		return TableWithSettings;
	}
	if (typeof ComponentOrOptions === "function") return tableWithSettingsFactory(ComponentOrOptions);
	else return (TableComponent) => tableWithSettingsFactory(TableComponent, ComponentOrOptions);
}
var i18n_default$2 = addComponentKeysets({
	en: { button_switcher: "Columns" },
	ru: { button_switcher: "Колонки" }
}, "TableColumnSetup");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/TableColumnSetup/TableColumnSetup.js
var b$10 = block("table-column-setup");
var TableColumnSetup = (rawProps) => {
	const { switcher, renderSwitcher: renderSwitcherProps, disabled, popupWidth, popupPlacement, className, items: propsItems, sortable = true, showStatus, onUpdate: propsOnUpdate, hideApplyButton } = useDefaultProps("TableColumnSetup", rawProps);
	const { t } = i18n_default$2.useTranslation();
	const renderStatus = () => {
		if (!showStatus) return null;
		const status = `${propsItems.reduce((acc, cur) => cur.selected ? acc + 1 : acc, 0)}/${propsItems.length}`;
		return (0, import_jsx_runtime.jsx)("span", {
			className: b$10("status"),
			children: status
		});
	};
	const renderSwitcher = (switcherProps) => {
		return renderSwitcherProps?.(switcherProps) || switcher || (0, import_jsx_runtime.jsxs)(Button, {
			disabled,
			onClick: switcherProps.onClick,
			children: [
				(0, import_jsx_runtime.jsx)(Icon, { data: Gear }),
				t("button_switcher"),
				renderStatus()
			]
		});
	};
	const items = propsItems.map(({ id, title, required, selected, sticky }) => ({
		id,
		title,
		isRequired: required,
		isSelected: selected,
		sticky
	}));
	const onUpdate = (newSettings) => {
		propsOnUpdate(newSettings.map(({ id, isSelected }) => {
			const prevItem = propsItems.find((item) => item.id === id);
			return {
				id,
				selected: isSelected,
				title: prevItem?.title,
				required: prevItem?.required
			};
		}));
	};
	return (0, import_jsx_runtime.jsx)(TableColumnSetup$1, {
		hideApplyButton,
		items,
		onUpdate,
		popupPlacement,
		popupWidth,
		renderSwitcher,
		sortable,
		className: b$10(null, className)
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/contexts/TabContext.js
var TabContext = import_react.createContext(void 0);
TabContext.displayName = "TabContext";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/TabProvider.js
var TabProvider = (rawProps) => {
	const { value, onUpdate, children } = useDefaultProps("TabProvider", rawProps);
	const id = useUniqId();
	const contextValue = import_react.useMemo(() => ({
		value,
		onUpdate,
		id,
		isProvider: true
	}), [
		value,
		onUpdate,
		id
	]);
	return (0, import_jsx_runtime.jsx)(TabContext.Provider, {
		value: contextValue,
		children
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/lab/Menu/MenuContext.js
var MenuContext = import_react.createContext(null);
MenuContext.displayName = "MenuContext";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/lab/Menu/MenuDivider.js
var b$9 = block("lab-menu-divider");
function MenuDivider() {
	return (0, import_jsx_runtime.jsx)(Divider, { className: b$9() });
}
MenuDivider.displayName = "Menu.Divider";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Button/constants.js
var BUTTON_ICON_SIZE_MAP = {
	xs: 12,
	s: 14,
	m: 16,
	l: 16,
	xl: 20
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/mergeProps.js
function mergeProps(...propsList) {
	const eventMap = /* @__PURE__ */ new Map();
	return propsList.reduce((acc, props) => {
		Object.entries(props).forEach(([key, value]) => {
			if (key.startsWith("on") && key.charCodeAt(2) >= 65 && key.charCodeAt(2) <= 90) {
				if (!eventMap.has(key)) eventMap.set(key, []);
				if (typeof value === "function") {
					eventMap.get(key)?.push(value);
					acc[key] = (...args) => {
						return eventMap.get(key)?.map((fn) => fn(...args)).find((v) => v !== void 0);
					};
				}
			} else if ((key === "className" || key.endsWith("ClassName")) && typeof acc[key] === "string" && typeof value === "string") acc[key] = `${acc[key]} ${value}`;
			else acc[key] = value;
		});
		return acc;
	}, {});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/lab/Menu/MenuItemContext.js
var MenuItemContext = import_react.createContext(null);
MenuItemContext.displayName = "MenuItemContext";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/lab/Menu/utils.js
function isComponentType(component, type) {
	if (!import_react.isValidElement(component)) return false;
	if (typeof component.type === "string") return false;
	return component.type.displayName === type;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/lab/Menu/MenuItem.js
function isMenuItemComponentProps(p) {
	return p.component !== void 0;
}
function isMenuItemLinkProps(p) {
	return p.href !== void 0;
}
var b$8 = block("lab-menu-item");
var MenuItem = import_react.forwardRef((props, ref) => {
	const { theme, selected, disabled, icon, arrow, children, className, qa, ...restComponentProps } = props;
	const [submenuOpen, setSubmenuOpen] = import_react.useState(false);
	const [hasFocusInside, setHasFocusInside] = import_react.useState(false);
	const [isHovered, setIsHovered] = import_react.useState(false);
	const isRTL = useDirection() === "rtl";
	const menuContext = import_react.useContext(MenuContext);
	const menuItemContext = import_react.useContext(MenuItemContext);
	if (!menuContext) throw new Error("<MenuItem> must be used within <Menu>");
	const item = useListItem();
	const tree = useFloatingTree();
	const isActive = item.index === menuContext.activeIndex;
	const tabIndex = menuContext.inline && item.index === 0 || isActive ? 0 : -1;
	let submenu = null;
	const preparedChildren = [];
	if (icon) preparedChildren.push((0, import_jsx_runtime.jsx)("div", {
		className: b$8("icon"),
		"aria-hidden": true,
		children: icon
	}, "icon"));
	for (const child of import_react.Children.toArray(children)) if (!menuContext.inline && isComponentType(child, "Menu")) submenu = child;
	else preparedChildren.push(child);
	if (arrow || submenu) preparedChildren.push((0, import_jsx_runtime.jsx)("div", {
		className: b$8("arrow"),
		"aria-hidden": true,
		children: arrow ? arrow : (0, import_jsx_runtime.jsx)(Icon, {
			data: isRTL ? ChevronLeft : ChevronRight,
			size: BUTTON_ICON_SIZE_MAP[menuContext.size]
		})
	}, "arrow"));
	const handleRef = useForkRef(ref, item.ref);
	const handleClick = (event) => {
		props.onClick?.(event);
		if (!submenu) tree?.events.emit("click");
	};
	const handleFocus = (event) => {
		props.onFocus?.(event);
		setHasFocusInside(false);
		menuItemContext?.setHasFocusInside(true);
	};
	const handlePointerEnter = (event) => {
		props.onPointerEnter?.(event);
		setIsHovered(true);
	};
	const handlePointerLeave = (event) => {
		props.onPointerLeave?.(event);
		setIsHovered(false);
	};
	let component;
	let componentProps;
	const commonComponentProps = {
		role: "menuitem",
		tabIndex,
		className: b$8({
			theme,
			size: menuContext.size
		}, className),
		"data-qa": qa,
		...menuContext.getItemProps({
			...restComponentProps,
			onClick: handleClick,
			onFocus: handleFocus,
			onPointerEnter: handlePointerEnter,
			onPointerLeave: handlePointerLeave
		})
	};
	if (isMenuItemComponentProps(props)) {
		component = props.component;
		componentProps = {
			...commonComponentProps,
			"aria-disabled": disabled ?? void 0
		};
	} else if (isMenuItemLinkProps(props)) {
		component = "a";
		componentProps = {
			...commonComponentProps,
			rel: props.target === "_blank" && !props.rel ? "noopener noreferrer" : props.rel,
			"aria-disabled": disabled ?? void 0
		};
	} else {
		component = "button";
		componentProps = {
			...commonComponentProps,
			type: "button",
			disabled,
			"aria-disabled": disabled ?? void 0,
			"aria-pressed": selected ?? void 0
		};
	}
	const content = (0, import_jsx_runtime.jsx)(ListItemView$1, {
		isContainer: true,
		component,
		componentProps,
		ref: handleRef,
		size: menuContext.size,
		disabled,
		active: isActive && !hasFocusInside && !isHovered,
		hovered: hasFocusInside || !isActive && submenuOpen || isHovered,
		selected,
		selectionStyle: "highlight",
		children: preparedChildren
	});
	const contextValue = import_react.useMemo(() => ({ setHasFocusInside }), []);
	if (submenu) return (0, import_jsx_runtime.jsx)(MenuItemContext.Provider, {
		value: contextValue,
		children: import_react.cloneElement(submenu, {
			trigger: (triggerProps, triggerRef) => {
				return import_react.cloneElement(content, {
					ref: mergeRefs(triggerRef, handleRef),
					componentProps: mergeProps(triggerProps, componentProps)
				});
			},
			onOpenChange: (open, event, reason) => {
				submenu.props.onOpenChange?.(open, event, reason);
				setSubmenuOpen(open);
				if (!open) setHasFocusInside(false);
			}
		})
	});
	return content;
});
MenuItem.displayName = "Menu.Item";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/lab/Menu/MenuTrigger.js
var MenuTrigger = import_react.forwardRef(({ size = "m", children, icon = "horizontal", ...restProps }, ref) => {
	return (0, import_jsx_runtime.jsx)(Button, {
		ref,
		size,
		...restProps,
		children: children ? children : (0, import_jsx_runtime.jsx)(Icon, {
			data: icon === "vertical" ? EllipsisVertical : Ellipsis,
			size: BUTTON_ICON_SIZE_MAP[size]
		})
	});
});
MenuTrigger.displayName = "Menu.Trigger";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/lab/Menu/Menu.js
var b$7 = block("lab-menu");
function MenuPopupContent({ open, onRequestClose, isNested, children, className, style, qa }) {
	const tree = useFloatingTree();
	const nodeId = useFloatingParentNodeId();
	const parentId = import_react.useContext(MenuContext)?.floatingParentId;
	import_react.useEffect(() => {
		if (!tree) return;
		function handleTreeClick() {
			if (!isNested) onRequestClose();
		}
		function handleSubMenuOpen(event) {
			if (event.nodeId !== nodeId && event.parentId === parentId) onRequestClose();
		}
		tree.events.on("click", handleTreeClick);
		tree.events.on("menuopen", handleSubMenuOpen);
		return () => {
			tree.events.off("click", handleTreeClick);
			tree.events.off("menuopen", handleSubMenuOpen);
		};
	}, [
		onRequestClose,
		tree,
		nodeId,
		parentId,
		isNested
	]);
	import_react.useEffect(() => {
		if (open && tree) tree.events.emit("menuopen", {
			parentId,
			nodeId
		});
	}, [
		open,
		tree,
		nodeId,
		parentId
	]);
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$7(null, className),
		style,
		"data-qa": qa,
		children
	});
}
function Menu({ trigger, inline = false, defaultOpen, open, onOpenChange, placement = "bottom-start", disabled, children, size = "m", className, style, qa }) {
	const [anchorElement, setAnchorElement] = import_react.useState(null);
	const [floatingElement, setFloatingElement] = import_react.useState(null);
	const [isOpen, setIsOpen] = useControlledState(open, defaultOpen ?? false, onOpenChange);
	const [activeIndex, setActiveIndex] = import_react.useState(null);
	const isRTL = useDirection() === "rtl";
	const itemsRef = import_react.useRef([]);
	const parentMenu = import_react.useContext(MenuContext);
	const floatingParentId = useFloatingParentNodeId();
	const isNested = Boolean(parentMenu);
	const floatingContext = useFloatingRootContext({
		open: isOpen && !disabled,
		onOpenChange: setIsOpen,
		elements: {
			reference: anchorElement,
			floating: floatingElement
		}
	});
	const hover = useHover$1(floatingContext, {
		enabled: isNested,
		delay: { open: 100 },
		handleClose: safePolygon({ blockPointerEvents: true })
	});
	const click = useClick(floatingContext, {
		toggle: !isNested,
		ignoreMouse: isNested
	});
	const dismiss = useDismiss(floatingContext, { enabled: !isNested });
	const role = useRole(floatingContext, { role: "menu" });
	const listNavigation = useListNavigation(floatingContext, {
		listRef: itemsRef,
		activeIndex,
		nested: isNested,
		onNavigate: setActiveIndex,
		rtl: isRTL
	});
	const detectOverflowOptions = { padding: 4 };
	const middlewares = [
		offset$1({
			mainAxis: isNested ? 3 : 4,
			alignmentAxis: isNested ? -4 : 0
		}),
		flip({ ...detectOverflowOptions }),
		shift$1({ ...detectOverflowOptions })
	];
	const interactions = [
		hover,
		click,
		dismiss,
		role,
		listNavigation
	];
	const { getReferenceProps, getItemProps } = useInteractions(interactions);
	const anchorRef = useForkRef(setAnchorElement, import_react.isValidElement(trigger) ? getElementRef(trigger) : void 0);
	const anchorProps = import_react.isValidElement(trigger) ? getReferenceProps(trigger.props) : getReferenceProps();
	const anchorNode = import_react.isValidElement(trigger) ? import_react.cloneElement(trigger, {
		...anchorProps,
		ref: anchorRef
	}) : typeof trigger === "function" ? trigger(anchorProps, anchorRef) : null;
	const handleContentRequestClose = import_react.useCallback(() => {
		setIsOpen(false);
	}, [setIsOpen]);
	const getItemPropsInline = import_react.useCallback((userProps) => {
		const handleItemPointerEnter = (event) => {
			userProps?.onPointerEnter?.(event);
			const element = event.currentTarget;
			const index = [...element.closest("[role=\"menu\"]")?.querySelectorAll("[role=\"menuitem\"]") ?? []].indexOf(element);
			if (!element.disabled && !element.ariaDisabled && index >= 0) {
				element.focus();
				setActiveIndex(index);
			} else setActiveIndex(null);
		};
		const handleItemPointerLeave = (event) => {
			userProps?.onPointerLeave?.(event);
			setActiveIndex(null);
		};
		return {
			"data-active": void 0,
			...userProps,
			onPointerEnter: handleItemPointerEnter,
			onPointerLeave: handleItemPointerLeave
		};
	}, []);
	const contextValue = import_react.useMemo(() => ({
		inline: parentMenu?.inline ?? inline,
		size: parentMenu?.size ?? size,
		activeIndex,
		floatingParentId,
		getItemProps: inline ? getItemPropsInline : getItemProps
	}), [
		parentMenu,
		inline,
		size,
		activeIndex,
		floatingParentId,
		getItemPropsInline,
		getItemProps
	]);
	import_react.useEffect(() => {
		if (!anchorNode) if (trigger) {
			floatingContext.refs.setPositionReference(trigger);
			setIsOpen(true);
		} else setIsOpen(false);
	}, [trigger]);
	if (inline) {
		const preparedChildren = import_react.Children.toArray(children).map((child, index) => {
			if (!import_react.isValidElement(child) || !isComponentType(child, "Menu.Item")) return child;
			return (0, import_jsx_runtime.jsx)(CompositeItem, { render: (props) => import_react.cloneElement(child, {
				...child.props,
				...props
			}) }, index);
		});
		return (0, import_jsx_runtime.jsx)(MenuContext.Provider, {
			value: contextValue,
			children: (0, import_jsx_runtime.jsx)(Composite, {
				render: (0, import_jsx_runtime.jsx)("div", {
					role: "menu",
					className: b$7(null, className),
					style,
					"data-qa": qa
				}),
				orientation: "vertical",
				loop: false,
				rtl: isRTL,
				activeIndex: activeIndex ?? void 0,
				onNavigate: setActiveIndex,
				children: preparedChildren
			})
		});
	}
	return (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [anchorNode, (0, import_jsx_runtime.jsx)(Popup, {
		open: floatingContext.open,
		placement: isNested ? `${isRTL ? "left" : "right"}-start` : placement,
		disablePortal: isNested,
		disableEscapeKeyDown: isNested,
		disableOutsideClick: isNested,
		floatingContext,
		floatingRef: setFloatingElement,
		floatingMiddlewares: middlewares,
		floatingInteractions: interactions,
		children: (0, import_jsx_runtime.jsx)(MenuContext.Provider, {
			value: contextValue,
			children: (0, import_jsx_runtime.jsx)(FloatingList, {
				elementsRef: itemsRef,
				children: (0, import_jsx_runtime.jsx)(MenuPopupContent, {
					open: isOpen,
					onRequestClose: handleContentRequestClose,
					isNested,
					className,
					style,
					qa,
					children
				})
			})
		})
	})] });
}
Menu.displayName = "Menu";
Menu.Trigger = MenuTrigger;
Menu.Item = MenuItem;
Menu.Divider = MenuDivider;
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/constants.js
var bTabList = block("tab-list");
var bTab = block("tab");
var bTabPanel = block("tab-panel");
var bTabListCollapseItem = block("tab-list-collapse-item");
var TAB_DATA_ATTRIBUTE = "data-tab";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/TabContent.js
function TabContent({ icon, children, value, counter, label }) {
	return (0, import_jsx_runtime.jsxs)("div", {
		className: bTab("content"),
		children: [
			icon && (0, import_jsx_runtime.jsx)("div", {
				className: bTab("icon"),
				children: icon
			}),
			(0, import_jsx_runtime.jsx)("div", {
				className: bTab("title"),
				children: children || value
			}),
			counter !== void 0 && (0, import_jsx_runtime.jsx)("div", {
				className: bTab("counter"),
				children: counter
			}),
			label && (0, import_jsx_runtime.jsx)(Label, {
				className: bTab("label"),
				theme: label.theme,
				children: label.content
			})
		]
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/hooks/useTab.js
function useTab(tabProps) {
	const tabContext = import_react.useContext(TabContext);
	if (!tabContext) throw new Error("<Tab> must be used within <TabList>");
	const currentValue = tabContext.value;
	const tabId = `${tabContext.id}:t:${tabProps.value}`;
	const panelId = tabContext.isProvider ? `${tabContext.id}:p:${tabProps.value}` : void 0;
	const isSelected = currentValue === tabProps.value;
	const isDisabled = tabProps.disabled;
	const isFocused = tabContext.isFocused;
	const onClick = (event) => {
		if (tabProps.disabled) return;
		if (tabProps.onClick) tabProps.onClick(event);
		if (!event.defaultPrevented) tabContext.onUpdate?.(tabProps.value);
	};
	const onKeyDown = (event) => {
		if (tabProps.disabled) return;
		if (tabProps.onKeyDown) tabProps.onKeyDown(event);
		if (!event.defaultPrevented && (event.key === KeyCode$1.SPACEBAR || event.key === KeyCode$1.ENTER)) tabContext.onUpdate?.(tabProps.value);
	};
	const { value: _value, icon: _icon, counter: _counter, label: _label, disabled: _disabled, href: _href, component: _component, qa: _qa, isMenuItem: _isMenuItem, ...htmlProps } = tabProps;
	const { role: _role, tabIndex: _tabIndex, ...htmlRest } = htmlProps;
	const tabRoleProps = {
		role: "tab",
		tabIndex: isSelected && !isDisabled && !isFocused ? 0 : -1
	};
	return {
		...htmlRest,
		...tabProps.isMenuItem ? {} : tabRoleProps,
		"aria-selected": isSelected,
		"aria-disabled": isDisabled,
		"aria-controls": panelId,
		id: tabId,
		onClick,
		onKeyDown,
		className: bTab({
			active: isSelected,
			disabled: isDisabled
		}, tabProps.className),
		"data-qa": tabProps.qa,
		[TAB_DATA_ATTRIBUTE]: tabProps.value
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/utils.js
function isTabComponentProps(p) {
	return p.component !== void 0;
}
function isTabLinkProps(p) {
	return p.href !== void 0;
}
/**
* Finds the first Tab in the subtree (e.g. Tab wrapped in Tooltip) and returns it.
*/
function getTabNodePropsFromReactNode(node) {
	if (!import_react.isValidElement(node)) return;
	if (node.type === Tab) return node.props;
	const props = node.props;
	if (props.children === null) return;
	for (const child of import_react.Children.toArray(props.children)) {
		const nested = getTabNodePropsFromReactNode(child);
		if (nested !== void 0) return nested;
	}
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/Tab.js
var Tab = import_react.forwardRef(function Tab(rawProps, ref) {
	const props = useDefaultProps("Tab", rawProps);
	const tabProps = useTab(props);
	const content = (0, import_jsx_runtime.jsx)(TabContent, {
		icon: props.icon,
		value: props.value,
		counter: props.counter,
		label: props.label,
		children: props.children
	});
	if (isTabComponentProps(props)) return import_react.createElement(props.component, {
		...tabProps,
		ref,
		isMenuItem: props.isMenuItem || false
	});
	if (isTabLinkProps(props)) {
		const rel = props.target === "_blank" && !props.rel ? "noopener noreferrer" : props.rel;
		if (props.isMenuItem) return (0, import_jsx_runtime.jsx)(MenuItem, {
			...tabProps,
			ref,
			href: props.href,
			rel,
			children: content
		});
		return (0, import_jsx_runtime.jsx)("a", {
			...tabProps,
			ref,
			href: props.href,
			rel,
			children: content
		});
	}
	if (props.isMenuItem) return (0, import_jsx_runtime.jsx)(MenuItem, {
		...tabProps,
		ref,
		type: props.type || "button",
		disabled: props.disabled,
		children: content
	});
	return (0, import_jsx_runtime.jsx)("button", {
		...tabProps,
		ref,
		type: props.type || "button",
		children: content
	});
});
Tab.displayName = "Tab";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/TabListCollapseItem/TabListCollapseItem.js
var CHEVRON_SIZE = {
	m: 16,
	l: 16,
	xl: 20
};
var TabListCollapseItem = import_react.forwardRef(({ children, triggerChild, moreLabel = "More", size = "m" }, ref) => {
	const childrenCount = import_react.Children.count(children);
	if (!childrenCount && !triggerChild) return null;
	const triggerChildTabProps = triggerChild ? getTabNodePropsFromReactNode(triggerChild) : void 0;
	return (0, import_jsx_runtime.jsx)(Menu, {
		placement: ["bottom-start", "bottom-end"],
		trigger: (0, import_jsx_runtime.jsxs)(Flex, {
			ref,
			as: "button",
			alignItems: "center",
			gap: "2",
			className: [bTabListCollapseItem({ lone: Boolean(triggerChildTabProps) }), triggerChildTabProps ? "g-tab_active" : void 0].filter(Boolean).join(" "),
			children: [
				triggerChildTabProps ? (0, import_jsx_runtime.jsx)(TabContent, { ...triggerChildTabProps }) : (0, import_jsx_runtime.jsx)(Text, {
					variant: "inherit",
					className: bTabListCollapseItem("text"),
					children: moreLabel
				}),
				(0, import_jsx_runtime.jsx)(Text, {
					variant: "inherit",
					className: bTabListCollapseItem("count"),
					children: childrenCount
				}),
				(0, import_jsx_runtime.jsx)(Icon, {
					data: ChevronDown,
					size: CHEVRON_SIZE[size],
					className: bTabListCollapseItem("chevron")
				})
			]
		}),
		children: import_react.Children.map(children, (child, index) => {
			if (!import_react.isValidElement(child)) return null;
			const key = child.key ?? index;
			const isMenuItem = true;
			const className = [bTabListCollapseItem("menu-item"), child.props.className].filter(Boolean).join(" ");
			if (child.type === Tab) return import_react.cloneElement(child, {
				key,
				isMenuItem,
				className
			});
			const tabProps = getTabNodePropsFromReactNode(child);
			if (!tabProps) return null;
			return (0, import_react.createElement)(Tab, {
				...tabProps,
				key,
				isMenuItem,
				className
			});
		})
	});
});
TabListCollapseItem.displayName = "TabListCollapseItem";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/hooks/useTabList.js
var getAllTabElements = (tabElement) => {
	return [...tabElement.closest("[role=\"tablist\"]")?.querySelectorAll(`[data-tab]`) ?? []];
};
var isTabDisabled = (tabElement) => {
	return tabElement.getAttribute("aria-disabled") === "true";
};
var getTabValue = (tabElement) => {
	return tabElement.getAttribute(TAB_DATA_ATTRIBUTE);
};
var focusNearestTab = (event, isInverse, isRTL) => {
	const tabElement = event.target;
	const allTabElements = getAllTabElements(tabElement);
	const index = allTabElements.indexOf(tabElement);
	let distance = 1;
	while (distance < allTabElements.length) {
		const nextIndex = index + distance * (isRTL ? -1 : 1) * (isInverse ? -1 : 1);
		const nextTabElement = allTabElements.at(nextIndex % allTabElements.length);
		if (nextTabElement && !isTabDisabled(nextTabElement)) {
			nextTabElement.focus();
			return nextTabElement;
		}
		distance++;
	}
	return tabElement;
};
var focusFurthestTab = (event, isInverse) => {
	const tabElement = event.target;
	const allTabElements = getAllTabElements(tabElement);
	const stopIndex = isInverse ? 0 : allTabElements.length - 1;
	let index = allTabElements.indexOf(tabElement);
	let lastFocusableTabElement = tabElement;
	while (index !== stopIndex) {
		index += isInverse ? -1 : 1;
		const nextTabElement = allTabElements.at(index);
		if (nextTabElement && !isTabDisabled(nextTabElement)) lastFocusableTabElement = nextTabElement;
	}
	lastFocusableTabElement.focus();
	return lastFocusableTabElement;
};
function useTabList(tabListProps) {
	const tabContext = import_react.useContext(TabContext);
	const isRTL = useDirection() === "rtl";
	const activateOnFocus = (tabElement) => {
		const value = getTabValue(tabElement);
		if (tabListProps.activateOnFocus && value) {
			tabListProps.onUpdate?.(value);
			tabContext?.onUpdate?.(value);
		}
	};
	const onKeyDown = (event) => {
		switch (event.code) {
			case KeyCode$1.ARROW_LEFT:
				event.preventDefault();
				activateOnFocus(focusNearestTab(event, true, isRTL));
				break;
			case KeyCode$1.ARROW_RIGHT:
				event.preventDefault();
				activateOnFocus(focusNearestTab(event, false, isRTL));
				break;
			case KeyCode$1.HOME:
				event.preventDefault();
				activateOnFocus(focusFurthestTab(event, true));
				break;
			case KeyCode$1.END:
				event.preventDefault();
				activateOnFocus(focusFurthestTab(event, false));
				break;
		}
		tabListProps.onKeyDown?.(event);
	};
	const { value: _value, onUpdate: _onUpdate, size: _size, activateOnFocus: _activateOnFocus, qa: _qa, contentOverflow, moreLabel: _moreLabel, ...htmlProps } = tabListProps;
	return {
		...htmlProps,
		role: "tablist",
		"aria-orientation": "horizontal",
		onKeyDown,
		className: bTabList({
			size: tabListProps.size ?? "m",
			overflow: contentOverflow
		}, tabListProps.className),
		"data-qa": tabListProps.qa
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/hooks/useTabListGapFromContainer.js
function getTabListGapFromContainer(container, childSelector) {
	const directChildren = Array.from(container.querySelectorAll(`:scope > :where(${childSelector})`));
	for (const child of directChildren) {
		if (child === container.lastElementChild) continue;
		return parseFloat(getComputedStyle(child).marginInlineEnd) || 0;
	}
	return 0;
}
/**
* Resolves the horizontal gap between tabs
*/
function useTabListGapFromContainer(containerRef, { enabled, childrenHash, childSelector }) {
	const [gap, setGap] = import_react.useState(0);
	const calculateGap = import_react.useCallback(() => {
		const el = containerRef.current;
		if (!el) return;
		setGap((prev) => {
			const next = getTabListGapFromContainer(el, childSelector);
			return next === prev ? prev : next;
		});
	}, [containerRef, childSelector]);
	useLayoutEffect$4(() => {
		if (enabled) calculateGap();
		else setGap(0);
	}, [
		childrenHash,
		enabled,
		calculateGap
	]);
	useResizeObserver({
		ref: containerRef,
		onResize: calculateGap
	});
	return gap;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/hooks/useTabListCollapsedChildren.js
var TAB_LIST_COLLAPSE_CHILD_SELECTOR = `.${bTab()},.${bTabListCollapseItem()}`;
var useTabListCollapsedChildren = (children, tabListValue, containerRef, enabled) => {
	const collapseItemRef = import_react.useRef(null);
	const childrenHash = getReactNodeHash(children);
	const listGap = useTabListGapFromContainer(containerRef, {
		enabled,
		childrenHash,
		childSelector: TAB_LIST_COLLAPSE_CHILD_SELECTOR
	});
	const { recalculate, visibleCount: visibleChildrenCount } = useCollapseChildren({
		enabled,
		containerRef,
		preservedRefs: [collapseItemRef],
		direction: "end",
		minCount: 0,
		gap: listGap,
		childSelector: TAB_LIST_COLLAPSE_CHILD_SELECTOR
	});
	import_react.useEffect(() => {
		recalculate();
	}, [childrenHash, tabListValue]);
	useResizeObserver({
		ref: collapseItemRef,
		onResize: recalculate
	});
	const isReactNodeCollapsible = import_react.useCallback((child) => {
		const tabProps = getTabNodePropsFromReactNode(child);
		if (tabProps === void 0) return true;
		return tabListValue !== tabProps["value"];
	}, [tabListValue]);
	const childrenList = [];
	import_react.Children.forEach(children, (child, index) => {
		if (!import_react.isValidElement(child)) return;
		if (child.key === void 0 || child.key === null) {
			childrenList.push(import_react.cloneElement(child, { key: index }));
			return;
		}
		childrenList.push(child);
	});
	if (!enabled) return {
		shownChildren: childrenList,
		collapsedChildren: [],
		collapseItemRef,
		triggerChild: void 0
	};
	if (visibleChildrenCount === 0) {
		const selectedChild = childrenList.find((child) => !isReactNodeCollapsible(child)) ?? childrenList[0];
		const otherChildren = childrenList.filter((c) => c !== selectedChild);
		if (otherChildren.length === 0) return {
			shownChildren: selectedChild ? [selectedChild] : [],
			collapsedChildren: [],
			collapseItemRef,
			triggerChild: void 0
		};
		const selectedTabProps = getTabNodePropsFromReactNode(selectedChild);
		const hiddenSelectedChild = selectedTabProps ? import_react.createElement(Tab, {
			...selectedTabProps,
			key: selectedTabProps.value,
			style: {
				visibility: "hidden",
				pointerEvents: "none",
				position: "absolute"
			},
			"aria-hidden": true,
			tabIndex: -1
		}) : void 0;
		return {
			shownChildren: hiddenSelectedChild ? [hiddenSelectedChild] : [],
			collapsedChildren: otherChildren,
			collapseItemRef,
			triggerChild: selectedChild
		};
	}
	const notCollapsibleChildren = childrenList.filter((child) => !isReactNodeCollapsible(child));
	const shownChildren = [];
	const collapsedChildren = [];
	let reservedToShowChildrenCount = notCollapsibleChildren.length;
	childrenList.forEach((child) => {
		if (!isReactNodeCollapsible(child)) {
			shownChildren.push(child);
			reservedToShowChildrenCount--;
			return;
		}
		if (shownChildren.length < visibleChildrenCount - reservedToShowChildrenCount) shownChildren.push(child);
		else collapsedChildren.push(child);
	});
	return {
		shownChildren,
		collapsedChildren,
		triggerChild: void 0,
		collapseItemRef
	};
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/TabList.js
var TabList = import_react.forwardRef((rawProps, ref) => {
	const props = useDefaultProps("TabList", rawProps);
	const tabContext = import_react.useContext(TabContext);
	const id = useUniqId();
	const listRef = import_react.useRef(null);
	const containerRef = useForkRef(ref, listRef);
	const value = tabContext?.value ?? props.value;
	const tabListProps = useTabList(props);
	const collapseEnabled = props.contentOverflow === "collapse";
	const collapsedChildrenResults = useTabListCollapsedChildren(props.children, value, listRef, collapseEnabled);
	const [isFocused, setIsFocused] = import_react.useState(false);
	const { focusWithinProps } = useFocusWithin({ onFocusWithinChange: setIsFocused });
	const innerContextValue = import_react.useMemo(() => ({
		value,
		onUpdate: tabContext?.onUpdate ?? props.onUpdate,
		id: tabContext?.id ?? id,
		isProvider: tabContext?.isProvider ?? false,
		isFocused
	}), [
		tabContext,
		value,
		props.onUpdate,
		id,
		isFocused
	]);
	return (0, import_jsx_runtime.jsx)(TabContext.Provider, {
		value: innerContextValue,
		children: (0, import_jsx_runtime.jsx)("div", {
			ref: containerRef,
			...tabListProps,
			...focusWithinProps,
			children: collapseEnabled ? (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [collapsedChildrenResults.shownChildren, (0, import_jsx_runtime.jsx)(TabListCollapseItem, {
				ref: collapsedChildrenResults.collapseItemRef,
				triggerChild: collapsedChildrenResults.triggerChild,
				moreLabel: props.moreLabel,
				size: props.size,
				children: collapsedChildrenResults.collapsedChildren
			})] }) : props.children
		})
	});
});
TabList.displayName = "TabList";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/hooks/useTabPanel.js
function useTabPanel(tabPanelProps) {
	const tabContext = import_react.useContext(TabContext);
	if (!tabContext) throw new Error("<TabPanel> must be used within <TabProvider>");
	const currentValue = tabContext.value;
	const parentId = tabContext.id;
	const tabId = `${parentId}:t:${tabPanelProps.value}`;
	const panelId = `${parentId}:p:${tabPanelProps.value}`;
	const isSelected = currentValue === tabPanelProps.value;
	const { value: _value, qa: _qa, ...htmlProps } = tabPanelProps;
	return {
		...htmlProps,
		role: "tabpanel",
		"aria-labelledby": tabId,
		id: panelId,
		className: bTabPanel({ hidden: !isSelected }, tabPanelProps.className),
		"data-qa": tabPanelProps.qa
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/tabs/TabPanel.js
var TabPanel = import_react.forwardRef((rawProps, ref) => {
	const props = useDefaultProps("TabPanel", rawProps);
	const panelProps = useTabPanel(props);
	return (0, import_jsx_runtime.jsx)(TabContext.Provider, {
		value: void 0,
		children: (0, import_jsx_runtime.jsx)("div", {
			ref,
			...panelProps,
			children: props.children
		})
	});
});
TabPanel.displayName = "TabPanel";
var i18n_default$1 = addComponentKeysets({
	en: { "label_close-button": "Close" },
	ru: { "label_close-button": "Закрыть" }
}, "Toaster");
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toaster/Toast/Toast.js
var b$6 = block("toast");
var DEFAULT_TIMEOUT = 5e3;
var TITLE_ICONS = {
	normal: null,
	info: CircleInfoFill,
	success: CircleCheckFill,
	warning: TriangleExclamationFill,
	danger: TriangleExclamationFill,
	utility: ThunderboltFill
};
function renderActions({ actions, onClose }) {
	let component;
	if (Array.isArray(actions)) {
		component = actions.map(({ label, onClick, view = "outlined", removeAfterClick = true }, index) => {
			const onActionClick = () => {
				onClick();
				if (removeAfterClick) onClose();
			};
			return (0, import_jsx_runtime.jsx)(Button, {
				onClick: onActionClick,
				type: "button",
				size: "l",
				view,
				width: "auto",
				children: label
			}, `${label}__${index}`);
		});
		if (!actions.length) return null;
	} else {
		component = actions?.();
		if (!component) return null;
	}
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$6("actions"),
		children: component
	});
}
function renderIconByType({ theme }) {
	if (!theme || !TITLE_ICONS[theme]) return null;
	return (0, import_jsx_runtime.jsx)(Icon, {
		data: TITLE_ICONS[theme],
		size: 20,
		className: b$6("icon", { [theme]: true })
	});
}
var Toast = import_react.forwardRef(function Toast(props, ref) {
	const { name, content, actions, title, className, theme = "normal", renderIcon, autoHiding: timeoutProp = DEFAULT_TIMEOUT, isClosable = true, mobile = false, onClose, removeCallback } = props;
	const handleClose = import_react.useCallback(() => {
		removeCallback(name);
		if (onClose) onClose();
	}, [
		removeCallback,
		onClose,
		name
	]);
	const closeOnTimeoutProps = useCloseOnTimeout({
		onClose: handleClose,
		timeout: typeof timeoutProp === "number" ? timeoutProp : void 0
	});
	const mods = {
		mobile,
		theme
	};
	const hasTitle = Boolean(title);
	const hasContent = Boolean(content);
	const { t } = i18n_default$1.useTranslation();
	const icon = renderIcon ? renderIcon(props) : renderIconByType({ theme });
	return (0, import_jsx_runtime.jsxs)("div", {
		ref,
		className: b$6(mods, className),
		...closeOnTimeoutProps,
		"data-toast": true,
		children: [icon && (0, import_jsx_runtime.jsx)("div", {
			className: b$6("icon-container"),
			children: icon
		}), (0, import_jsx_runtime.jsxs)("div", {
			className: b$6("container"),
			children: [
				hasTitle && (0, import_jsx_runtime.jsx)("h3", {
					className: b$6("title"),
					children: title
				}),
				isClosable && (0, import_jsx_runtime.jsx)(Button, {
					size: "s",
					view: "flat",
					className: b$6("btn-close"),
					onClick: handleClose,
					"aria-label": t("label_close-button"),
					children: (0, import_jsx_runtime.jsx)(Icon, { data: Xmark })
				}),
				hasContent && (0, import_jsx_runtime.jsx)("div", {
					className: b$6("content", { "without-title": !hasTitle }),
					children: content
				}),
				renderActions({
					actions,
					onClose: handleClose
				})
			]
		})]
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toaster/Provider/ToasterContext.js
var ToasterContext = import_react.createContext(null);
ToasterContext.displayName = "ToasterContext";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toaster/hooks/useToaster.js
function useToaster() {
	const toaster = import_react.useContext(ToasterContext);
	if (toaster === null) throw new Error("Toaster: `useToaster` hook is used out of context");
	return import_react.useMemo(() => ({
		add: toaster.add.bind(toaster),
		remove: toaster.remove.bind(toaster),
		removeAll: toaster.removeAll.bind(toaster),
		update: toaster.update.bind(toaster),
		has: toaster.has.bind(toaster)
	}), [toaster]);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toaster/withToaster.js
function withToaster() {
	return function(WrappedComponent) {
		function WithToaster(props) {
			const toaster = useToaster();
			return (0, import_jsx_runtime.jsx)(WrappedComponent, {
				...props,
				toaster
			});
		}
		WithToaster.displayName = `WithToaster(${getComponentName(WrappedComponent)})`;
		return WithToaster;
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toaster/Provider/ToastsContext.js
var ToastsContext = import_react.createContext([]);
ToastsContext.displayName = "ToastsContext";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toaster/ToastList/ToastList.js
var desktopTransitionClassNames = getCSSTransitionClassNames(block("toast-animation-desktop"));
var mobileTransitionClassNames = getCSSTransitionClassNames(block("toast-animation-mobile"));
function ToastList(props) {
	const { toasts, mobile, removeCallback } = props;
	return (0, import_jsx_runtime.jsx)(TransitionGroup, {
		component: null,
		children: toasts.map((toast) => (0, import_jsx_runtime.jsx)(CSSTransition, {
			nodeRef: toast.ref,
			classNames: mobile ? mobileTransitionClassNames : desktopTransitionClassNames,
			addEndListener: (done) => toast.ref?.current?.addEventListener("animationend", done),
			onEnter: () => updateToastHeightCssProperty(toast),
			onExit: () => updateToastHeightCssProperty(toast),
			children: (0, import_jsx_runtime.jsx)(Toast, {
				...toast,
				ref: toast.ref,
				mobile,
				removeCallback
			})
		}, `${toast.name}_${toast.addedAt}`))
	});
}
function updateToastHeightCssProperty(toast) {
	if (toast.ref?.current) toast.ref.current.style.setProperty("--_--item-height", `${toast.ref.current.offsetHeight}px`);
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toaster/ToasterComponent/ToasterComponent.js
var b$5 = block("toaster");
function ToasterComponent({ className, mobile, hasPortal = true }) {
	const defaultMobile = useMobile();
	const { remove } = useToaster();
	const toaster = (0, import_jsx_runtime.jsx)(ToastList, {
		toasts: import_react.useContext(ToastsContext),
		removeCallback: remove,
		mobile: mobile ?? defaultMobile
	});
	if (!hasPortal) return toaster;
	return (0, import_jsx_runtime.jsx)(Portal, { children: (0, import_jsx_runtime.jsx)("div", {
		className: b$5({ mobile: mobile ?? defaultMobile }, className),
		"aria-live": "assertive",
		children: toaster
	}) });
}
ToasterComponent.displayName = "ToasterComponent";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toaster/Provider/ToasterProvider.js
var ToasterProvider = ({ toaster, children }) => {
	const [toasts, setToasts] = import_react.useState([]);
	import_react.useEffect(() => {
		const unsubscribe = toaster.subscribe(setToasts);
		return () => {
			unsubscribe();
		};
	}, [toaster]);
	return (0, import_jsx_runtime.jsx)(ToasterContext.Provider, {
		value: toaster,
		children: (0, import_jsx_runtime.jsx)(ToastsContext.Provider, {
			value: toasts,
			children
		})
	});
};
ToasterProvider.displayName = "ToasterProvider";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toc/TocItem/TocItem.js
var b$4 = block("toc-item");
var TocItem = (props) => {
	const { active = false, childItem = false, content, href, onClick, depth } = props;
	const { onKeyDown } = useActionHandlers(onClick);
	const item = href === void 0 ? (0, import_jsx_runtime.jsx)("div", {
		role: "button",
		tabIndex: 0,
		className: b$4("section-link"),
		onClick,
		onKeyDown,
		children: content
	}) : (0, import_jsx_runtime.jsx)("a", {
		href,
		onClick,
		className: b$4("section-link"),
		children: content
	});
	return (0, import_jsx_runtime.jsx)("div", {
		className: b$4("section", {
			child: childItem,
			depth,
			active
		}),
		children: item
	});
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toc/TocSections/TocSections.js
var b$3 = block("toc");
function TocSections(props) {
	const { value: activeValue, items, onUpdate, childItem, depth = 1, onItemClick } = props;
	if (depth > 6) return null;
	return (0, import_jsx_runtime.jsx)("ul", {
		className: b$3("sections"),
		children: items.map(({ value, content, href, items: childrenItems }) => (0, import_jsx_runtime.jsxs)("li", {
			"aria-current": activeValue === value,
			children: [(0, import_jsx_runtime.jsx)(TocItem, {
				content,
				href,
				active: activeValue === value,
				onClick: (event) => {
					onItemClick?.(event);
					if (value === void 0 || !onUpdate) return;
					onUpdate?.(value);
				},
				childItem,
				depth
			}), childrenItems && childrenItems.length > 0 && (0, import_jsx_runtime.jsx)(TocSections, {
				items: childrenItems,
				onUpdate,
				childItem: true,
				depth: depth + 1,
				value: activeValue
			})]
		}, value ?? href))
	});
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/Toc/Toc.js
var b$2 = block("toc");
var Toc = import_react.forwardRef(function Toc(rawProps, ref) {
	const { value: activeValue, items, className, onUpdate, qa, onItemClick } = useDefaultProps("Toc", rawProps);
	return (0, import_jsx_runtime.jsx)("nav", {
		className: b$2(null, className),
		ref,
		"data-qa": qa,
		children: (0, import_jsx_runtime.jsx)(TocSections, {
			items,
			value: activeValue,
			onUpdate,
			depth: 1,
			onItemClick
		})
	});
});
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/User/constants.js
var DEFAULT_USER_SIZE = "m";
var COMPACT_SIZES$1 = /* @__PURE__ */ new Set([
	"xs",
	"2xs",
	"3xs"
]);
var UserQa = {
	NAME: "user-name",
	DESCRIPTION: "user-description"
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/User/User.js
var b$1 = block("user");
var User = import_react.forwardRef((rawProps, ref) => {
	const props = useDefaultProps("User", rawProps);
	const { avatar, name, description, size = "m", className, style, qa } = props;
	const nameTitle = typeof name === "string" ? name : void 0;
	const descriptionTitle = typeof description === "string" ? description : void 0;
	let avatarView = null;
	if (typeof avatar === "string") avatarView = (0, import_jsx_runtime.jsx)(Avatar, {
		imgUrl: avatar,
		size,
		title: nameTitle
	});
	else if (import_react.isValidElement(avatar)) avatarView = avatar;
	else if (avatar) avatarView = (0, import_jsx_runtime.jsx)(Avatar, {
		...avatar,
		size,
		title: avatar.title || nameTitle
	});
	const showDescription = Boolean(description && !COMPACT_SIZES$1.has(size));
	return (0, import_jsx_runtime.jsxs)("div", {
		className: b$1({ size }, className),
		style,
		"data-qa": qa,
		ref,
		...filterDOMProps(props, { labelable: true }),
		children: [avatarView ? (0, import_jsx_runtime.jsx)("div", {
			className: b$1("avatar"),
			children: avatarView
		}) : null, name || showDescription ? (0, import_jsx_runtime.jsxs)("div", {
			className: b$1("info"),
			children: [name ? (0, import_jsx_runtime.jsx)("span", {
				className: b$1("name"),
				title: nameTitle,
				"data-qa": UserQa.NAME,
				children: name
			}) : null, showDescription ? (0, import_jsx_runtime.jsx)("span", {
				className: b$1("description"),
				title: descriptionTitle,
				"data-qa": UserQa.DESCRIPTION,
				children: description
			}) : null]
		}) : null]
	});
});
User.displayName = "User";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/UserLabel/constants.js
var DEFAULT_USER_LABEL_SIZE = "s";
var COMPACT_SIZES = /* @__PURE__ */ new Set([
	"m",
	"s",
	"xs",
	"2xs",
	"3xs"
]);
var BORDER_COLOR = "var(--g-color-line-generic-solid)";
var ICON_SIZES = {
	"3xs": 12,
	"2xs": 12,
	xs: 12,
	s: 16,
	m: 16,
	l: 16,
	xl: 16
};
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/UserLabel/i18n/index.js
var i18n_default = addComponentKeysets({
	en: { "label_remove-button": "Remove" },
	ru: { "label_remove-button": "Удалить" }
}, `g-user-label`);
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/UserLabel/UserLabel.js
var b = block("user-label");
var UserLabel = import_react.forwardRef((rawProps, ref) => {
	const { type = "person", view = "outlined", size = "s", avatar, text, description, onClick, onCloseClick, className, style, qa } = useDefaultProps("UserLabel", rawProps);
	const clickable = Boolean(onClick);
	const closeable = Boolean(onCloseClick);
	const MainComponent = clickable ? "button" : "div";
	let avatarView = null;
	let avatarProps;
	if (typeof avatar === "string") avatarProps = { imgUrl: avatar };
	else if (avatar && !import_react.isValidElement(avatar)) {
		if ("imgUrl" in avatar && avatar.imgUrl || "icon" in avatar && avatar.icon || "text" in avatar && avatar.text) avatarProps = avatar;
		else if (typeof text === "string") avatarProps = {
			text,
			borderColor: BORDER_COLOR,
			...avatar
		};
	} else if (!avatar && typeof text === "string") avatarProps = {
		text,
		borderColor: BORDER_COLOR
	};
	switch (type) {
		case "email":
			avatarView = (0, import_jsx_runtime.jsx)(Avatar, {
				icon: Envelope,
				...avatarProps,
				size
			});
			break;
		case "empty":
			avatarView = null;
			break;
		default:
			if (import_react.isValidElement(avatar)) avatarView = avatar;
			else if (avatarProps) avatarView = (0, import_jsx_runtime.jsx)(Avatar, {
				...avatarProps,
				size
			});
			break;
	}
	const showDescription = Boolean(description && !COMPACT_SIZES.has(size));
	const { t } = i18n_default.useTranslation();
	return (0, import_jsx_runtime.jsxs)("div", {
		className: b({
			view,
			size,
			empty: !avatarView,
			clickable,
			closeable
		}, className),
		style,
		"data-qa": qa,
		ref,
		children: [(0, import_jsx_runtime.jsxs)(MainComponent, {
			className: b("main"),
			type: clickable ? "button" : void 0,
			onClick,
			children: [avatarView ? (0, import_jsx_runtime.jsx)("div", {
				className: b("avatar"),
				children: avatarView
			}) : null, (0, import_jsx_runtime.jsxs)("div", {
				className: b("info"),
				children: [(0, import_jsx_runtime.jsx)("span", {
					className: b("text"),
					children: text
				}), showDescription ? (0, import_jsx_runtime.jsx)("span", {
					className: b("description"),
					children: description
				}) : null]
			})]
		}), onCloseClick ? (0, import_jsx_runtime.jsx)("button", {
			className: b("close"),
			type: "button",
			"aria-label": t("label_remove-button"),
			onClick: onCloseClick,
			children: (0, import_jsx_runtime.jsx)(Icon, {
				className: b("close-icon"),
				data: Xmark,
				size: ICON_SIZES[size]
			})
		}) : null]
	});
});
UserLabel.displayName = "UserLabel";
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/class-transform.js
function parseClass(strClass) {
	const split = strClass.split("_").filter((str) => str);
	if (strClass.includes("__")) return {
		block: split[0],
		element: split[1],
		mod: split[2] ? {
			key: split[2],
			value: split[3] ? split[3] : true
		} : void 0
	};
	return {
		block: split[0],
		mod: split[1] ? {
			key: split[1],
			value: split[2] ? split[2] : true
		} : void 0
	};
}
function formatClass(objClass) {
	let result = objClass.block;
	if (objClass.element) result = `${result}__${objClass.element}`;
	if (objClass.mod?.value) {
		result = `${result}_${objClass.mod.key}`;
		if (typeof objClass.mod.value === "string") result = `${result}_${objClass.mod.value}`;
	}
	return result;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/withEventBrokerDomHandlers.js
function withEventBrokerDomHandlers(Component, eventTypes, eventBrokerData) {
	const displayName = `withEventBroker(${getComponentName(Component)})`;
	const LoggedComponent = import_react.forwardRef((props, ref) => {
		const decoratedHandlers = eventTypes.reduce((handlers, eventType) => {
			const originalHandler = props[eventType];
			return {
				...handlers,
				[eventType]: (event) => {
					eventBroker.publish({
						eventId: eventType.replace(/^on/, "").toLowerCase(),
						domEvent: event,
						...eventBrokerData
					});
					return originalHandler && originalHandler(event);
				}
			};
		}, {});
		return (0, import_jsx_runtime.jsx)(Component, {
			...props,
			...decoratedHandlers,
			ref
		});
	});
	LoggedComponent.displayName = displayName;
	return LoggedComponent;
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/components/utils/xpath.js
var import_md5 = /* @__PURE__ */ __toESM((/* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function($) {
		"use strict";
		/**
		* Add integers, wrapping at 2^32.
		* This uses 16-bit operations internally to work around bugs in interpreters.
		*
		* @param {number} x First integer
		* @param {number} y Second integer
		* @returns {number} Sum
		*/
		function safeAdd(x, y) {
			var lsw = (x & 65535) + (y & 65535);
			return (x >> 16) + (y >> 16) + (lsw >> 16) << 16 | lsw & 65535;
		}
		/**
		* Bitwise rotate a 32-bit number to the left.
		*
		* @param {number} num 32-bit number
		* @param {number} cnt Rotation count
		* @returns {number} Rotated number
		*/
		function bitRotateLeft(num, cnt) {
			return num << cnt | num >>> 32 - cnt;
		}
		/**
		* Basic operation the algorithm uses.
		*
		* @param {number} q q
		* @param {number} a a
		* @param {number} b b
		* @param {number} x x
		* @param {number} s s
		* @param {number} t t
		* @returns {number} Result
		*/
		function md5cmn(q, a, b, x, s, t) {
			return safeAdd(bitRotateLeft(safeAdd(safeAdd(a, q), safeAdd(x, t)), s), b);
		}
		/**
		* Basic operation the algorithm uses.
		*
		* @param {number} a a
		* @param {number} b b
		* @param {number} c c
		* @param {number} d d
		* @param {number} x x
		* @param {number} s s
		* @param {number} t t
		* @returns {number} Result
		*/
		function md5ff(a, b, c, d, x, s, t) {
			return md5cmn(b & c | ~b & d, a, b, x, s, t);
		}
		/**
		* Basic operation the algorithm uses.
		*
		* @param {number} a a
		* @param {number} b b
		* @param {number} c c
		* @param {number} d d
		* @param {number} x x
		* @param {number} s s
		* @param {number} t t
		* @returns {number} Result
		*/
		function md5gg(a, b, c, d, x, s, t) {
			return md5cmn(b & d | c & ~d, a, b, x, s, t);
		}
		/**
		* Basic operation the algorithm uses.
		*
		* @param {number} a a
		* @param {number} b b
		* @param {number} c c
		* @param {number} d d
		* @param {number} x x
		* @param {number} s s
		* @param {number} t t
		* @returns {number} Result
		*/
		function md5hh(a, b, c, d, x, s, t) {
			return md5cmn(b ^ c ^ d, a, b, x, s, t);
		}
		/**
		* Basic operation the algorithm uses.
		*
		* @param {number} a a
		* @param {number} b b
		* @param {number} c c
		* @param {number} d d
		* @param {number} x x
		* @param {number} s s
		* @param {number} t t
		* @returns {number} Result
		*/
		function md5ii(a, b, c, d, x, s, t) {
			return md5cmn(c ^ (b | ~d), a, b, x, s, t);
		}
		/**
		* Calculate the MD5 of an array of little-endian words, and a bit length.
		*
		* @param {Array} x Array of little-endian words
		* @param {number} len Bit length
		* @returns {Array<number>} MD5 Array
		*/
		function binlMD5(x, len) {
			x[len >> 5] |= 128 << len % 32;
			x[(len + 64 >>> 9 << 4) + 14] = len;
			var i;
			var olda;
			var oldb;
			var oldc;
			var oldd;
			var a = 1732584193;
			var b = -271733879;
			var c = -1732584194;
			var d = 271733878;
			for (i = 0; i < x.length; i += 16) {
				olda = a;
				oldb = b;
				oldc = c;
				oldd = d;
				a = md5ff(a, b, c, d, x[i], 7, -680876936);
				d = md5ff(d, a, b, c, x[i + 1], 12, -389564586);
				c = md5ff(c, d, a, b, x[i + 2], 17, 606105819);
				b = md5ff(b, c, d, a, x[i + 3], 22, -1044525330);
				a = md5ff(a, b, c, d, x[i + 4], 7, -176418897);
				d = md5ff(d, a, b, c, x[i + 5], 12, 1200080426);
				c = md5ff(c, d, a, b, x[i + 6], 17, -1473231341);
				b = md5ff(b, c, d, a, x[i + 7], 22, -45705983);
				a = md5ff(a, b, c, d, x[i + 8], 7, 1770035416);
				d = md5ff(d, a, b, c, x[i + 9], 12, -1958414417);
				c = md5ff(c, d, a, b, x[i + 10], 17, -42063);
				b = md5ff(b, c, d, a, x[i + 11], 22, -1990404162);
				a = md5ff(a, b, c, d, x[i + 12], 7, 1804603682);
				d = md5ff(d, a, b, c, x[i + 13], 12, -40341101);
				c = md5ff(c, d, a, b, x[i + 14], 17, -1502002290);
				b = md5ff(b, c, d, a, x[i + 15], 22, 1236535329);
				a = md5gg(a, b, c, d, x[i + 1], 5, -165796510);
				d = md5gg(d, a, b, c, x[i + 6], 9, -1069501632);
				c = md5gg(c, d, a, b, x[i + 11], 14, 643717713);
				b = md5gg(b, c, d, a, x[i], 20, -373897302);
				a = md5gg(a, b, c, d, x[i + 5], 5, -701558691);
				d = md5gg(d, a, b, c, x[i + 10], 9, 38016083);
				c = md5gg(c, d, a, b, x[i + 15], 14, -660478335);
				b = md5gg(b, c, d, a, x[i + 4], 20, -405537848);
				a = md5gg(a, b, c, d, x[i + 9], 5, 568446438);
				d = md5gg(d, a, b, c, x[i + 14], 9, -1019803690);
				c = md5gg(c, d, a, b, x[i + 3], 14, -187363961);
				b = md5gg(b, c, d, a, x[i + 8], 20, 1163531501);
				a = md5gg(a, b, c, d, x[i + 13], 5, -1444681467);
				d = md5gg(d, a, b, c, x[i + 2], 9, -51403784);
				c = md5gg(c, d, a, b, x[i + 7], 14, 1735328473);
				b = md5gg(b, c, d, a, x[i + 12], 20, -1926607734);
				a = md5hh(a, b, c, d, x[i + 5], 4, -378558);
				d = md5hh(d, a, b, c, x[i + 8], 11, -2022574463);
				c = md5hh(c, d, a, b, x[i + 11], 16, 1839030562);
				b = md5hh(b, c, d, a, x[i + 14], 23, -35309556);
				a = md5hh(a, b, c, d, x[i + 1], 4, -1530992060);
				d = md5hh(d, a, b, c, x[i + 4], 11, 1272893353);
				c = md5hh(c, d, a, b, x[i + 7], 16, -155497632);
				b = md5hh(b, c, d, a, x[i + 10], 23, -1094730640);
				a = md5hh(a, b, c, d, x[i + 13], 4, 681279174);
				d = md5hh(d, a, b, c, x[i], 11, -358537222);
				c = md5hh(c, d, a, b, x[i + 3], 16, -722521979);
				b = md5hh(b, c, d, a, x[i + 6], 23, 76029189);
				a = md5hh(a, b, c, d, x[i + 9], 4, -640364487);
				d = md5hh(d, a, b, c, x[i + 12], 11, -421815835);
				c = md5hh(c, d, a, b, x[i + 15], 16, 530742520);
				b = md5hh(b, c, d, a, x[i + 2], 23, -995338651);
				a = md5ii(a, b, c, d, x[i], 6, -198630844);
				d = md5ii(d, a, b, c, x[i + 7], 10, 1126891415);
				c = md5ii(c, d, a, b, x[i + 14], 15, -1416354905);
				b = md5ii(b, c, d, a, x[i + 5], 21, -57434055);
				a = md5ii(a, b, c, d, x[i + 12], 6, 1700485571);
				d = md5ii(d, a, b, c, x[i + 3], 10, -1894986606);
				c = md5ii(c, d, a, b, x[i + 10], 15, -1051523);
				b = md5ii(b, c, d, a, x[i + 1], 21, -2054922799);
				a = md5ii(a, b, c, d, x[i + 8], 6, 1873313359);
				d = md5ii(d, a, b, c, x[i + 15], 10, -30611744);
				c = md5ii(c, d, a, b, x[i + 6], 15, -1560198380);
				b = md5ii(b, c, d, a, x[i + 13], 21, 1309151649);
				a = md5ii(a, b, c, d, x[i + 4], 6, -145523070);
				d = md5ii(d, a, b, c, x[i + 11], 10, -1120210379);
				c = md5ii(c, d, a, b, x[i + 2], 15, 718787259);
				b = md5ii(b, c, d, a, x[i + 9], 21, -343485551);
				a = safeAdd(a, olda);
				b = safeAdd(b, oldb);
				c = safeAdd(c, oldc);
				d = safeAdd(d, oldd);
			}
			return [
				a,
				b,
				c,
				d
			];
		}
		/**
		* Convert an array of little-endian words to a string
		*
		* @param {Array<number>} input MD5 Array
		* @returns {string} MD5 string
		*/
		function binl2rstr(input) {
			var i;
			var output = "";
			var length32 = input.length * 32;
			for (i = 0; i < length32; i += 8) output += String.fromCharCode(input[i >> 5] >>> i % 32 & 255);
			return output;
		}
		/**
		* Convert a raw string to an array of little-endian words
		* Characters >255 have their high-byte silently ignored.
		*
		* @param {string} input Raw input string
		* @returns {Array<number>} Array of little-endian words
		*/
		function rstr2binl(input) {
			var i;
			var output = [];
			output[(input.length >> 2) - 1] = void 0;
			for (i = 0; i < output.length; i += 1) output[i] = 0;
			var length8 = input.length * 8;
			for (i = 0; i < length8; i += 8) output[i >> 5] |= (input.charCodeAt(i / 8) & 255) << i % 32;
			return output;
		}
		/**
		* Calculate the MD5 of a raw string
		*
		* @param {string} s Input string
		* @returns {string} Raw MD5 string
		*/
		function rstrMD5(s) {
			return binl2rstr(binlMD5(rstr2binl(s), s.length * 8));
		}
		/**
		* Calculates the HMAC-MD5 of a key and some data (raw strings)
		*
		* @param {string} key HMAC key
		* @param {string} data Raw input string
		* @returns {string} Raw MD5 string
		*/
		function rstrHMACMD5(key, data) {
			var i;
			var bkey = rstr2binl(key);
			var ipad = [];
			var opad = [];
			var hash;
			ipad[15] = opad[15] = void 0;
			if (bkey.length > 16) bkey = binlMD5(bkey, key.length * 8);
			for (i = 0; i < 16; i += 1) {
				ipad[i] = bkey[i] ^ 909522486;
				opad[i] = bkey[i] ^ 1549556828;
			}
			hash = binlMD5(ipad.concat(rstr2binl(data)), 512 + data.length * 8);
			return binl2rstr(binlMD5(opad.concat(hash), 640));
		}
		/**
		* Convert a raw string to a hex string
		*
		* @param {string} input Raw input string
		* @returns {string} Hex encoded string
		*/
		function rstr2hex(input) {
			var hexTab = "0123456789abcdef";
			var output = "";
			var x;
			var i;
			for (i = 0; i < input.length; i += 1) {
				x = input.charCodeAt(i);
				output += hexTab.charAt(x >>> 4 & 15) + hexTab.charAt(x & 15);
			}
			return output;
		}
		/**
		* Encode a string as UTF-8
		*
		* @param {string} input Input string
		* @returns {string} UTF8 string
		*/
		function str2rstrUTF8(input) {
			return unescape(encodeURIComponent(input));
		}
		/**
		* Encodes input string as raw MD5 string
		*
		* @param {string} s Input string
		* @returns {string} Raw MD5 string
		*/
		function rawMD5(s) {
			return rstrMD5(str2rstrUTF8(s));
		}
		/**
		* Encodes input string as Hex encoded string
		*
		* @param {string} s Input string
		* @returns {string} Hex encoded string
		*/
		function hexMD5(s) {
			return rstr2hex(rawMD5(s));
		}
		/**
		* Calculates the raw HMAC-MD5 for the given key and data
		*
		* @param {string} k HMAC key
		* @param {string} d Input string
		* @returns {string} Raw MD5 string
		*/
		function rawHMACMD5(k, d) {
			return rstrHMACMD5(str2rstrUTF8(k), str2rstrUTF8(d));
		}
		/**
		* Calculates the Hex encoded HMAC-MD5 for the given key and data
		*
		* @param {string} k HMAC key
		* @param {string} d Input string
		* @returns {string} Raw MD5 string
		*/
		function hexHMACMD5(k, d) {
			return rstr2hex(rawHMACMD5(k, d));
		}
		/**
		* Calculates MD5 value for a given string.
		* If a key is provided, calculates the HMAC-MD5 value.
		* Returns a Hex encoded string unless the raw argument is given.
		*
		* @param {string} string Input string
		* @param {string} [key] HMAC key
		* @param {boolean} [raw] Raw output switch
		* @returns {string} MD5 output
		*/
		function md5(string, key, raw) {
			if (!key) {
				if (!raw) return hexMD5(string);
				return rawMD5(string);
			}
			if (!raw) return hexHMACMD5(key, string);
			return rawHMACMD5(key, string);
		}
		if (typeof define === "function" && define.amd) define(function() {
			return md5;
		});
		else if (typeof module === "object" && module.exports) module.exports = md5;
		else $.md5 = md5;
	})(exports);
})))(), 1);
function withoutClassMods(converter = (arg) => arg) {
	return (parsedClass, strClass) => parsedClass.mod ? void 0 : converter(parsedClass, strClass);
}
function isElement(node) {
	return node.nodeType === Node.ELEMENT_NODE;
}
function getXpathByNode(node, options) {
	if (!node || !isElement(node)) return "";
	const tag = node.tagName.toLowerCase();
	let token = `/${tag}`;
	const convertedId = node.id && !options.withoutId ? options.idConverter(node.id) : void 0;
	if (convertedId) token += `[@id='${convertedId}']`;
	else {
		const classes = [];
		node.classList.forEach((className) => {
			const currentClass = options.classConverter({
				...parseClass(className),
				tag
			}, className);
			if (currentClass) classes.push(formatClass(currentClass));
		});
		if (classes.length) token += `[@class='${classes.join(" ")}']`;
	}
	return getXpathByNode(node.parentElement, options) + token;
}
var defaultXpathOptions = {
	classConverter: (arg) => arg,
	idConverter: (arg) => arg,
	withoutId: false
};
function getXpath(event, options) {
	const internalOptions = {
		...defaultXpathOptions,
		...options || {}
	};
	const xpath = getXpathByNode(event.currentTarget || event.target, internalOptions);
	return {
		xpath,
		hash: (0, import_md5.default)(xpath)
	};
}
//#endregion
//#region node_modules/.pnpm/@gravity-ui+uikit@7.44.2_@babel+runtime@7.29.7_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/@gravity-ui/uikit/build/esm/utils/dom.js
function isTouchDevice() {
	if (typeof window === "undefined") return false;
	if (typeof navigator !== "undefined" && navigator.maxTouchPoints > 0) return true;
	if (window.matchMedia && window.matchMedia("(any-pointer:coarse)").matches) return true;
	return "ontouchstart" in window;
}
//#endregion
export { Overlay as $, useLayoutEffect$4 as $n, require_isTypedArray as $r, Popup as $t, Switch as A, useDirection as An, require_keysIn as Ar, copyText as At, Progress as B, Icon as Bn, require_round as Br, Alert as Bt, withTableSettings as C, require_isArray as Ci, addComponentKeysets as Cn, Platform as Cr, Modal as Ct, withTableActions as D, withDirection as Dn, getConfig as Dr, Popover as Dt, withTableCopy as E, DisclosureQa as En, configure as Er, HelpMark as Et, Skeleton as F, ThemeSettingsContext as Fn, require__getPrototype as Fr, AvatarStack as Ft, DEFAULT_RENDER_POPUP as G, useViewportSize as Gn, require__baseIteratee as Gr, sp as Gt, PinInput as H, colorText as Hn, require__isIterateeCall as Hr, Container as Ht, SegmentedRadioGroup as I, useCollapseChildren as In, require__copyArray as Ir, Avatar as It, OptionGroup as J, useResizeObserver as Jn, require__getAllKeys as Jr, Col as Jt, Select as K, useTimeout as Kn, require_identity as Kr, spacing as Kt, SegmentedRadioGroupOption as L, require_isEqual as Ln, require__cloneTypedArray as Lr, getAvatarDisplayText as Lt, Stepper as M, useTheme as Mn, require__assignValue as Mr, Checkbox as Mt, Slider as N, ThemeProvider as Nn, require_isArrayLikeObject as Nr, Breadcrumbs as Nt, withTableSelection as O, withThemeValue as On, require_groupBy as Or, ClipboardButton as Ot, prepareSliderInnerState as P, useLang as Pn, require__initCloneObject as Pr, _BreadcrumbsItem as Pt, SelectQa as Q, useListNavigation$1 as Qn, require__isPrototype as Qr, require_throttle as Qt, RadioGroup as R, useUniqId as Rn, require__cloneArrayBuffer as Rr, AVATAR_SIZES as Rt, TableColumnSetup as S, require__Symbol as Si, Accordion as Sn, MobileContext as Sr, DialogBody as St, withTableSorting as T, ArrowToggle as Tn, Lang as Tr, DefinitionListItem as Tt, Palette as U, useVirtualElement as Un, require__baseRest as Ur, Flex as Ut, PlaceholderContainer as V, TEXT_COLORS as Vn, require_debounce as Vr, Card as Vt, Pagination as W, useVirtualElementRef as Wn, require__baseEach as Wr, Box as Wt, useSelectOptions as X, PortalProvider as Xn, require_isArrayLike as Xr, ActionsPanel as Xt, getSelectFilteredOptions as Y, usePortalContainer as Yn, require_keys as Yr, useLayoutContext as Yt, isSelectGroupTitle as Z, useOutsideClick as Zn, require__baseKeys as Zr, DropdownMenuExport as Zt, Toast as _, require__arrayMap as _i, Portal as _n, withPlatform as _r, Divider as _t, formatClass as a, require__baseGetAllKeys as ai, MenuGroup as an, useFileInput as ar, Sheet as at, Tab as b, require_isSymbol as bi, require__arrayIncludesWith as bn, useMobile as br, DialogFooter as bt, DEFAULT_USER_LABEL_SIZE as c, require__Stack as ci, text as cn, generateCustomColor as cr, ListItem as ct, UserQa as d, require__arrayPush as di, useEventBroker as dn, getThemeType as dr, Loader as dt, require__nodeUtil as ei, useLayer as en, useIntersection as er, NumberInput as et, Toc as f, require_capitalize as fi, EventBroker as fn, useControlledState as fr, PasswordInput as ft, useToaster as g, require__baseGet as gi, getElementRef as gn, withMobile as gr, Drawer as gt, withToaster as h, require_get as hi, Tooltip as hn, ThemeContext as hr, AutoSizer as ht, withEventBrokerDomHandlers as i, require_stubArray as ii, MenuItem$1 as in, useFocusWithin as ir, getFileType as it, Spin as j, useThemeSettings as jn, require__copyObject as jr, ClipboardIcon as jt, Table as k, withTheme as kn, require_merge as kr, CopyToClipboard as kt, User as l, require__baseFlatten as li, Button as ln, useThemeType as lr, defaultRenderItem as lt, ToasterComponent as m, withNaming as mi, ActionTooltip as mn, useActionHandlers as mr, TextArea as mt, getXpath as n, require_isBuffer as ni, layerManager as nn, mergeRefs as nr, Label as nt, parseClass as o, require__cacheHas as oi, Text as on, useColorGenerator as or, List as ot, ToasterProvider as p, require_toNumber as pi, eventBroker as pn, useAsyncActionHandler as pr, TextInput as pt, Option as q, useSelect as qn, require__getTag as qr, Row as qt, withoutClassMods as r, require__getSymbols as ri, Menu$1 as rn, setRef as rr, FilePreview as rt, UserLabel as s, require__SetCache as si, TEXT_VARIANTS as sn, generateColor as sr, listDefaultProps as st, isTouchDevice as t, require__baseUnary as ti, getLayersCount as tn, useForkRef as tr, Link as tt, DEFAULT_USER_SIZE as u, require_isArguments as ui, ButtonIcon as un, useThemeValue as ur, ListQa as ut, TabPanel as v, require_memoize as vi, Hotkey as vn, getComponentName as vr, Dialog as vt, TableSortIndicator as w, Disclosure as wn, rootMobileClassName as wr, DefinitionList as wt, TabProvider as x, require_isObjectLike as xi, require__arrayIncludes as xn, MobileProvider as xr, DialogDivider as xt, TabList as y, require_isObject as yi, parseHotkeys as yn, usePlatform as yr, DialogHeader as yt, Radio as z, getUniqId as zn, require__cloneBuffer as zr, DEFAULT_AVATAR_SIZE as zt };
