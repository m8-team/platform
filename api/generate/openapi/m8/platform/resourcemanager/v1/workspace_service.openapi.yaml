# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: M8 Platform API
    description: HTTP API specifications generated from protobuf contracts.
    version: v1
paths:
    /v1/organizations/{organization}/workspaces:
        get:
            tags:
                - WorkspaceService
            summary: List
            description: Returns a paginated list of workspaces under the specified organization.
            operationId: list
            parameters:
                - name: organization
                  in: path
                  description: The organization id.
                  required: true
                  schema:
                    type: string
                - name: pageSize
                  in: query
                  description: |-
                    Optional. The maximum number of workspaces to return.
                     Valid values are in the range 0 to 1000.
                     If omitted, the service returns up to 50 workspaces.
                     The service may return fewer results than requested.
                  schema:
                    type: integer
                    format: int32
                - name: pageToken
                  in: query
                  description: |-
                    Optional. A page token returned by a previous `ListWorkspaces` call.
                     Use this field to retrieve the next page of results.

                     When paginating, all other parameters provided to `ListWorkspaces`
                     must match the call that produced the page token.
                     The token must not exceed 1024 characters.
                  schema:
                    type: string
                - name: filter
                  in: query
                  description: |-
                    Optional. AIP-160 compatible filter expression.
                     The expression must not exceed 1024 characters.
                  schema:
                    type: string
                - name: orderBy
                  in: query
                  description: |-
                    Optional. AIP-132 compatible ordering expression.
                     The expression must not exceed 128 characters.
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListWorkspacesResponse'
        post:
            tags:
                - WorkspaceService
            summary: Create
            description: Creates a new workspace under the specified organization.
            operationId: create
            parameters:
                - name: organization
                  in: path
                  description: The organization id.
                  required: true
                  schema:
                    type: string
                - name: workspaceId
                  in: query
                  description: |-
                    Optional. The client-assigned ID to use for the workspace.
                     This value becomes the final {workspace} segment of the resource name.
                     The ID must be 3 to 63 characters long, start with a lowercase letter,
                     and contain only lowercase letters, digits, and hyphens.
                  schema:
                    type: string
                - name: requestId
                  in: query
                  description: Optional. A caller-provided request ID used to make create retries idempotent.
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/Workspace'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Workspace'
    /v1/organizations/{organization}/workspaces/{workspace}:
        get:
            tags:
                - WorkspaceService
            summary: Get
            description: Returns a single workspace by its resource name.
            operationId: get
            parameters:
                - name: organization
                  in: path
                  description: The organization id.
                  required: true
                  schema:
                    type: string
                - name: workspace
                  in: path
                  description: The workspace id.
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Workspace'
        delete:
            tags:
                - WorkspaceService
            summary: Delete
            description: Deletes a workspace by its resource name.
            operationId: delete
            parameters:
                - name: organization
                  in: path
                  description: The organization id.
                  required: true
                  schema:
                    type: string
                - name: workspace
                  in: path
                  description: The workspace id.
                  required: true
                  schema:
                    type: string
                - name: requestId
                  in: query
                  description: Optional. A caller-provided request ID used to make delete retries idempotent.
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content: {}
        patch:
            tags:
                - WorkspaceService
            summary: Update
            description: Updates mutable fields of an existing workspace.
            operationId: update
            parameters:
                - name: organization
                  in: path
                  description: The organization id.
                  required: true
                  schema:
                    type: string
                - name: workspace
                  in: path
                  description: The workspace id.
                  required: true
                  schema:
                    type: string
                - name: updateMask
                  in: query
                  description: |-
                    Required. The field mask that selects which mutable fields to update.
                     Paths in the mask should refer only to fields that clients are allowed to modify.
                  schema:
                    type: string
                    format: field-mask
                - name: requestId
                  in: query
                  description: Optional. A caller-provided request ID used to make update retries idempotent.
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/Workspace'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Workspace'
components:
    schemas:
        ListWorkspacesResponse:
            type: object
            properties:
                workspaces:
                    type: array
                    items:
                        $ref: '#/components/schemas/Workspace'
                    description: Workspaces returned for the current page.
                nextPageToken:
                    type: string
                    description: |-
                        Token to retrieve the next page.
                         This field is empty when there are no more results.
                totalSize:
                    type: integer
                    description: The total number of workspaces that match the request.
                    format: int32
            description: ListWorkspacesResponse returns a single page of workspaces.
        Workspace:
            type: object
            properties:
                name:
                    type: string
                    description: |-
                        Identifier. The resource name of the workspace.
                         Format: organizations/{organization}/workspaces/{workspace}
                         The {organization} and {workspace} segments must each be 3 to 63
                         characters long, start with a lowercase letter, and contain only
                         lowercase letters, digits, and hyphens.
                uid:
                    readOnly: true
                    type: string
                    description: |-
                        Output only. System-assigned stable unique identifier for the workspace.
                         The value is generated by the server and is returned as a UUID4 string.
                state:
                    readOnly: true
                    enum:
                        - STATE_UNSPECIFIED
                        - CREATING
                        - ACTIVE
                        - SUSPENDED
                        - DELETING
                        - DELETED
                        - FAILED
                    type: string
                    description: |-
                        Output only. Server-computed lifecycle state of the workspace.
                         The value is always one of the defined non-zero enum values.
                    format: enum
                displayName:
                    type: string
                    description: |-
                        Optional. Human-readable display name of the workspace.
                         The value may be updated by clients and must not exceed 256 characters.
                description:
                    type: string
                    description: |-
                        Optional. Free-form description of the workspace.
                         The value may be updated by clients and must not exceed 256 characters.
                createTime:
                    readOnly: true
                    type: string
                    description: Output only. Time when the workspace was created.
                    format: date-time
                updateTime:
                    readOnly: true
                    type: string
                    description: Output only. Time when the workspace was most recently updated.
                    format: date-time
                deleteTime:
                    readOnly: true
                    type: string
                    description: Output only. Time when the workspace was soft-deleted.
                    format: date-time
                purgeTime:
                    readOnly: true
                    type: string
                    description: Output only. Time when the soft-deleted workspace is scheduled to be purged.
                    format: date-time
                etag:
                    type: string
                    description: |-
                        Optional. Opaque concurrency token for the workspace.
                         If this value is provided on update or delete, it must exactly match the
                         current server-side etag or the request is rejected.
                annotations:
                    type: object
                    additionalProperties:
                        type: string
                    description: |-
                        Optional. Client-provided metadata for tooling and integrations.
                         Use namespaced keys such as "example.com/key" to avoid collisions between
                         independent producers of annotations.
            description: Workspace stores canonical metadata for a workspace resource within an organization.
tags:
    - name: WorkspaceService
