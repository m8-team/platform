# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: M8 Platform Organization API
    description: Resource-oriented API for reading and updating organizations.
    version: v1
paths:
    /v1/organizations:
        get:
            tags:
                - OrganizationService
            description: ListOrganizations returns a paginated list of organizations.
            operationId: OrganizationService_ListOrganizations
            parameters:
                - name: pageSize
                  in: query
                  description: Maximum number of organizations to return.
                  schema:
                    type: integer
                    format: int32
                - name: pageToken
                  in: query
                  description: Token returned by a previous `ListOrganizations` call.
                  schema:
                    type: string
                - name: filter
                  in: query
                  description: Optional AIP-160 style filter expression.
                  schema:
                    type: string
                - name: orderBy
                  in: query
                  description: Optional AIP-132 style ordering expression.
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListOrganizationsResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/organizations/{name}:
        get:
            tags:
                - OrganizationService
            description: GetOrganization returns a single organization by stable name.
            operationId: OrganizationService_GetOrganization
            parameters:
                - name: name
                  in: path
                  description: Stable organization name.
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Organization'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/organizations/{organization.name}:
        patch:
            tags:
                - OrganizationService
            description: UpdateOrganization updates mutable organization fields.
            operationId: OrganizationService_UpdateOrganization
            parameters:
                - name: organization.name
                  in: path
                  required: true
                  schema:
                    type: string
                - name: updateMask
                  in: query
                  description: List of organization fields to update.
                  schema:
                    type: string
                    format: field-mask
                - name: expectedVersion
                  in: query
                  description: Aggregate version expected by the caller for optimistic concurrency control.
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/Organization'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Organization'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ListOrganizationsResponse:
            type: object
            properties:
                organizations:
                    type: array
                    items:
                        $ref: '#/components/schemas/Organization'
                    description: Organizations returned for the current page.
                nextPageToken:
                    type: string
                    description: Token to retrieve the next page. Empty when there are no more results.
            description: ListOrganizationsResponse returns a single page of organizations.
        Organization:
            type: object
            properties:
                id:
                    readOnly: true
                    type: string
                    description: Stable system-assigned aggregate identifier.
                name:
                    type: string
                    description: |-
                        Stable machine-readable organization name used in resource identifiers and APIs.
                         Must be 3..63 characters, start with a lowercase letter, and contain only
                         lowercase letters, digits, and hyphens.
                state:
                    enum:
                        - STATE_UNSPECIFIED
                        - PROVISIONING
                        - ACTIVE
                        - SUSPENDED
                        - DELETED
                    type: string
                    description: Lifecycle state of the organization aggregate.
                    format: enum
                title:
                    type: string
                    description: 'Optional human-readable display title. Maximum length: 256 characters.'
                description:
                    type: string
                    description: 'Optional organization description. Maximum length: 256 characters.'
                createdAt:
                    readOnly: true
                    type: string
                    description: Time when the organization was created. Output only. Rendered as RFC3339 in JSON.
                    format: date-time
                updatedAt:
                    readOnly: true
                    type: string
                    description: Time of the most recent successful mutation. Output only. Rendered as RFC3339 in JSON.
                    format: date-time
                deletedAt:
                    readOnly: true
                    type: string
                    description: |-
                        Time when the organization was soft-deleted. Unset until state becomes DELETED.
                         Output only. Rendered as RFC3339 in JSON.
                    format: date-time
                version:
                    readOnly: true
                    type: string
                    description: |-
                        Aggregate version used for optimistic concurrency control.
                         Starts at 1 on creation and is incremented after each successful state change.
                         Output only.
            description: Organization stores canonical organization metadata for the organization aggregate.
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: OrganizationService
